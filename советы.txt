рз0) https://doka.guide/ - изучить обязательно
1) Для анимации лучше использовать transform: translate
2) Советуют использовать системные шрифты для сайта, глаз к ним привык.
Список есть в вк, и в видео латышского блогера:
Это с вк: -apple-system, BlinkMacSystemFont, Roboto, Open Sans, Helvetica Neue, 'Noto Sans Armenian', 'Noto Sans Bengali', 'Noto Sans Cherokee', 'Noto Sans Devanagari', 'Noto Sans Ethiopic', 'Noto Sans Georgian', 'Noto Sans Hebrew', 'Noto Sans Kannada', 'Noto Sans Khmer', 'Noto Sans Lao', 'Noto Sans Osmanya', 'Noto Sans Tamil', 'Noto Sans Telugu', 'Noto Sans Thai', sans-serif;
Есть еще шрифт system-ui - шрифт винды (Segoe UI)
Шрифты:
  100 - thin
  300 - light
  400 - regular, normal
  500 - medium
  600 - semi-bold
  700 - bold
  800 - extra-bold
  900 - black	

font-size на обычной версии делать 20px, на мобильной не меньше 16px
font-weight делать 400

3) В title можно помещать символы, которые будут показываться на вкладке браузера. Но проверить как относятся поисковики
	<title>😉 Документ</title>

3) <wbr> - рекомедуемое место переноса, если перенос понадобится
           можно ставить во всех местах, где согласны на перенос
	https://news.mail.ru/<wbr>svo/63068211/<wbr>?frommail=1

4) Если скорость интернета низкая, а картинка тяжелая, то нужно задавать размеры либо контейнеру картинки, либо самой картинке
   через width, height.
   Иначе при загрузке <img> займёт место как строка текста, а после загрузки сдвинет текст


3) Для скорости работы сайта:
	- минифицируем css и js
	- минифицируем картинки (webP, svg)
	- png только для изображений без фона. Фото в png весит много
	- используем кэширование и сжатие от сервера
	- явным образом задаем width и height для изображений
  Количество запросов, объем данных, время загрузки сайта есть во вкладке Network последней строкой внизу

111) Проверка текста письма на спам:
	https://www.mail-tester.com/

111) Закрытие от индексации:
     В robots.txt:
	Disallow: /

     В head:
	<meta name="robots" content="noindex, nofollow">
        <meta name="googlebot" content="noindex, nofollow">
        <meta name="yandex" content="noindex, nofollow">

111) web.dev/explore/fast - статьи по ускорению загрузки сайта.

111) ngrok http localhost:8000 - запуск сайта ларавель с компа

     ngrok http --host-header=tipirovanie.loc 8080 - запуска сайта с openserver
     
111) https://web2zip.ru - скопировать сайт с файлами

4) Если подключаем JQuery, то через cdn (файл кэшируется на компьютере)
5) Тестирование скорости сайта от гугла https://pagespeed.web.dev/
6) Вместо display: none можно дом узлу задавать hidden, будет то же самое <div hidden>Текст</div>
7) Мигание: setInterval(() => elem.hidden = !elem.hidden, 1000);
8) Любая нода это объект со свойствами и методами. Когда браузер парсит html он записывает 
стандартные атрибуты тегов в свойства узлов-объектов и в свойство attributes. 
Нестандартные атрибуты только в свойство attributes. 
Это свойство - коллекция всех атрибутов тега(стандартных, нестандартных, css атрибутов, data атрибутов). 
Общение с  attributes происходит методами setAttribute, getAttribute, removeAttribute и др. 
Также в объекте ноды есть свойство dataset. В html сюда записываются атрибуты через data-название. 
Это предпочтительный способ, так как нестандартный атрибут может в будущем конфликтовать 
с внедренными в язык html атрибутами с таким же именем. 
Стили css записываются так: <div style='width: 200px'></div>, соответственно это свойство 
попадает в div.style.width, а также в div.attributes.  
Стили через js в узел записываются как: div.style.width = "200px"

9) Атрибуты и свойства Дом взаимно синхронизируются. Одно из исключений value (напр у инпутов). 

Дом свойство elem.value - поменяет визуальное отображение в инпуте, Но атрибут тега в верстке не меняется.
elem.setAttribute('value', '123') - меняется верстка  --> меняется elem.value(если нигде не установлен) --> меняется текст в инпуте

elem.value имеет приоритет для визуального отображения, elem.setAttribute - для атрибута в верстке

Значение по умолчанию  <input value="1111">   в верстке тега имеет наименьший приоритет, его меняет elem.setAttribute

10) <input type="radio" checked> - пишем так. Меняем через дом input.checked = false
elem.setAttribute будет работать некорректно, так как даже если установим так false браузер будет считать как checked, 
так как после этого слова сразу начинает отображать заполненный чекбокс.

11) Предпочтительнее использовать DOM-свойства, чем setAttribute, getAttribute

12) 	elem.cloneNode(true) - клонирует elem c атрибутами и дочерними элементами
	elem.cloneNode(false) - клонирует elem без дочерних элементов
	node.append(elem) - вставить elem в конец node	
	node.prepend(elem) - вставить elem в начало node
	node.after(elem) - вставить elem после node	
	node.before(elem) - вставить elem перед node
	node.replaceWith(elem) - заменить ноду на другую ноду (node на elem)
	node.insertAdjacentHTML - вставить верстку относительно node (например beforebegin)	
	node.remove() - удалить ноду
	new DocumentFragment() - нода-обертка, вставляющая содержимое и удаляющаяся

    Проверка на существование ОДНОЙ ноды, например перед навешиванием события:
	let node = document.querySelector('#elem');
	Добавлено:
	ЧатГпт сказал, что достаточно if(node), а нижестоящий код избыточен

	if(typeof(node) != 'undefined' && node != null){ console.log(node) }
	Или прерыванием: if( typeof(elem) == 'undefined' || elem == null ) return false;

    Проверка на существование хотя бы одной из нод:
	let slides = document.querySelectorAll('.slide') // вернется пустой nodeList или заполненный
	if(slides.length > 0){ console.log(slides.length) } // ошибки не будет, так как длина пустого nodeList равна нулю
	
13) В стрелочных функциях если несколько строк, то и фигурные скобки и ретерн. Если есть аргументы и поставил фигурные скобки для одной строки, 
то ретерн обязательно. Фигурные скобки также нужны если в ввозвращении несколько слов: throw new Error(e)

13) Стрелочные и анонимные функции в php:
	1) Можно записывать в переменные:
		$func = function ($a, $b){ return $a + $b; };
		echo $func();

	2) Внешние переменные у обычных функций принимаются через global,
           а у анонимных через use (но внешняя переменная не изменится):
		$num = 5;
		$func = function () use ($num) { return $num**2; };
		echo $func();

	3) Чтобы внешняя переменная изменилась ее нужно принять по ссылке через &:
		function func (&$num){ $num =  $num ** 2; }; - для обычных функций
		$func = function () use (&$num) { $num =  $num ** 2; }; - для анонимных



13)
	event.clientX/event.clientY - координаты курсора(например клика) относительно окна браузера
	event.pageX/event.pageY - координаты курсора(напр. клика) относительно документа (при прокрутке документа меняется)
	event.screenX/event.screenY - координаты курсора(напр. клика) относительно экрана монитора
	elem.style.top/left - координата относительно первого позиционированного родителя (или body)
	offsetLeft - сдвиг слева сотносительно позиционированного родителя(или body)
	offsetTop - сдвиг сверху сотносительно позиционированного родителя(или body)
	offsetWidth - полная ширина элемента с рамкой, пэддингом
	offsetHeight - полная высота элемента с рамкой, пэддингом
	clientTop - высота рамки (расстояние от внешней границы до контента)
	clientLeft - ширина рамки (расстояние от внешней границы до контента)
	clientWidth - ширина контента + пэддинг без рамки и прокрутки
	clientHeight - высота контента + пэддинг без рамки и прокрутки
	scrollWidth/Height - размеры элемента включая включая еще непрокрученную скрытую часть
	scrollLeft/scrollTop - прокрутка элемента встроенным скроллом самого элемента (не окна). Можно менять. 
                               Если привязать к body или html, то можно отслеживать любую прокрутку:  
                               if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20)
	window.scrollBy(x,y) - прокручивает окно относительно его текущего положения
	window.scrollTo(pageX,pageY) прокручивает окно до абсолютных координат (pageX,pageY)
	elem.scrollIntoView() прокручивает окно, чтобы elem оказался вверху
	document.body.style.overflow = "hidden" - запрещает прокрутку фона
	window.pageYOffset - сколько прокрутили окно сверху
	window.pageXOffset - сколько прокрутили окно слева
	window.outerWidth/outerHeight - ширина/высота всего окна браузера (с вкладками), применяется редко
	window.innerWidth/Height - ширина/высота окна включая полосу прокрутки если есть (window.innerWidth - выводит ширину вьюпорта, 
			           равен значениям @media в  css)
	document.documentElement.clientHeight - на learn.javascript использует для получения высоты окна
	document.documentElement.clientWidth/clientHeight - ширина/высота окна без полосы прокрутки - приоритетнее чем window.innerWidth/Height
	window.screenX/screenY - положение окна браузера (т.е. его x и y координат) относительно экрана
	elem.getBoundingClientRect() - координаты элемента относительно окна браузера (ширина, высота,  top, bottom, left, right)
	document.elementFromPoint(x, y) - показывает элемент расположенный в заданной точке

	(document.documentElement.clientHeight - elem.getBoundingClientRect().top)/elem.offsetHeight * 100 - процент на который
 													     прокрутили блок

15) getComputedStyle(elem) - получает объект с вычисленными css свойствами (css + инлайн style). 
    Свойства объекта - итоговые css свойства элемента. Изменять свойства нельзя

15) window.innerWidth - получение ширины вьюпорта в js 
     
    if(window.innerWidth <= 1200) { ... что-то делаем в js аналогично css @media (max-width: 1200px) }

16) Полная высота документа с прокручиваемой частью: 
let scrollHeight = Math.max(
  document.body.scrollHeight, document.documentElement.scrollHeight,
  document.body.offsetHeight, document.documentElement.offsetHeight,
  document.body.clientHeight, document.documentElement.clientHeight
);  

16) Нумерация в верстке:
	<ul>
  	  <li>Текст</li>
  	  <li>Текст</li>
  	  <li>Текст</li>
	</ul>

    ul { counter-reset: list 0; } - название счетчика и начальное значение
    li { counter-increment: list 1; } - какой счетчик увеличиваем и насколько
    li::marker { content: counter(list); } - вписываем счетчик куда нужно. Также можно вписывать в ::before, ::after и пр.

16) <meter min="1" max="100" low="20" high="80" optimum="50" value="50"></meter> - цветная полоса с уровнем

16) Инпут типа range с показом значения:
	 <input type="range" step="25" list="rangeList" id="rangeInput" oninput="this.nextElementSibling.value = this.value">
  	 <output for="rangeInput">50</output> - здесь показывается

	 Если нужно нарисовать деления:
  	 <datalist id="rangeList">
    	   <option value="0">
    	   <option value="25">
    	   <option value="50">
    	   <option value="75">
    	   <option value="100">
  	 </datalist>

17) Тег datalist выводит подсказки - предопределенные значения для инпутов типа:
    text, search, tel, url, email, date, month, week, time, number, range, color

17) Отличие datalist от select в том, что datalist выводит подсказки, но можно ввести свое значение

17) inputmode - какую клавиатуру показывать
	<input type="text" inputmode="numeric"> - на телефоне показываются цифровая клавиатура
	<input type="text" inputmode="text"> - показывается текстовая клавиатура

    Также есть inputmode: none, tel, email, url и пр.

17) <progress id="progress" value="50" max="100"></progress> - полоска с законченностью процесса
    value нужно изменять через js

17) progress и meter похожи, но progress по умолчанию длиннее и уже

17) <meter class="meter" min="1" max="10" low="2" high="5" optimum="8" value="3"> - полоска с уровнем чего-либо 
                                                                                    (напр. место на диске). Красится в цвета

17) <object type="application/pdf" data="example.pdf" width="1200" height="700"></object> - встраивание готового постраничного просмотра pdf
                                
17) <ol>
  	<li value="5"> ... </li> - нумерация нумерованного списка начнется с 5
    </ol>

17) try, catch ловит ошибки либо в синхронном коде, либо в промисах. Чтобы применять с асинхронным кодом нужно 
либо использовать then и catch (потому что then возвращает промис), либо во внутренней асинхронной функции 
возвращать промис (resolve, reject) и ждать результат промиса через await.  
Следует иметь ввиду, что fetch по умолчанию возвращает промис, поэтому удобно await fetch(url)

Пример: 

;(async()=>{
    try {
      await f()
    } catch (e){
      console.log(e)
    }
  })()

    function f(){
      return new Promise(((resolve, reject) => {
        reject(new Error('ошибка'))
      }))
    }

18) В инпуте html можно указывать регулярное выражение для валидации <input type="email" pattern="^[a-zA-Z]+$"> - здесь слово на латинице
    <input type="password" pattern=".{6,}" title="Минимум 6 символов"> - можно указать сообщение об ошибке

19) У инлайн-блочных элементов по умолчанию зарезервировано расстояние между собой для хвостов букв. Для того, чтобы это расстояние убрать
    делаем их блочными. Это часто применяется для картинок (проще: vertical-align: top). Либо, если элементы с возможностью текстового содержимого, 
    родителю устанавливаем font-size: 0. Есть много других способов (межбукв. интервал и прочее)

20) Один из способов выстроить li в гор. ряд - сделать их инлайн-блочными. Чтобы убрать расстояние между ними делаем для родителя ul  font-size: 0;

21) В php когда отправляешь ответ на фетч запрос, то при получении методом await response.text() нужно из php отправляеть число строкой

22) При клике(либо др. событие) событие всплывает и ищет обработчики. Сначала на самом элементе, потом на его родителе 
    и так до document (иногда до window). Например: span -> p -> div -> ... -> html -> document. Почти все события всплывают. (напр. focus не всплывает)

23)	event.target - самый глубоко вложенный элемент на котором произошло событие. 
	event.currentTarget - текущий элемент который обрабатывает событие (на нем висит обработчик). Равен this

24) 
	event.stopPropagation() - останавливает всплытие
	event.stopImmediatePropagation() - останавливает другие обработчики на этом элементе (иначе отработают)

25) {capture: true} - обработчики будут срабатывать на погружении от html -> ... -> span

26) Для правильного переноса, кавычек и прочего для текста используем типограф Лебедева

27) В верстке сверху была полоса header, ниже секция, у которой было большое изображение фона. Чтобы этот фон был также для хедера 
    для него сделал position: absolute (left: 0, top: 0) он соответственно перестал занимать место и фон поднялся. 
    Также так как абс. позиционированный элемент становится размером по содержимому, то хедеру нужно сделать ширину 100%.
    Также можно изображение фона делать для всего body или wrapper, чтобы не было этих танцев.

28) img {max-width: 100%; height: auto}

29) .container {max-width: 1110px; margin: 0 auto; padding: 0 15px;} Паддинг нужен для того, чтобы при сжатии по ширине по бокам оставалось пустое место. Этот паддинг * 2 прибавляем к max-width. Вроде был еще способ с отрицательным margin.

30) Переменные css можно задавать для любых нод/селекторов, причем переменные будут доступны на всех дочерних нодах/селекторах.
	1) Обычно устанавливают для корневого селектора html (--root):
	   	:root { --name: #0d6efd; } - переменная --name будет доступна во всем документе
	2) В других селекторах:
	   	.burger_line {--name: 11px;} - будет доступна также в дочерних селекторах
	3) Переменную можно устанавливать ноде через js:
	   	elem.style.setProperty('--progress', '0.5') => <div class="item" style="--progress: 0.5;"></div>
	   А в самом классе этой ноды использовать (то есть установили в js, а пользуемся в css):
		opacity: var(--progress); - то есть возьмется не из root, а из инлайнового стиля элемента
		transform: translateY(calc(10% - 10%*var(--progress, 0)));
	4) Через JS можно записать переменную в корень html:
		document.documentElement.style.setProperty("--main-background-color", "green");
		или
		document.documentElement.setAttribute("style", "--main-background-color: green");
  
     Обращаться к переменным нужно так: var(--name)

     Получение переменной в JS:
	document.documentElement.style.getPropertyValue('--myVariable') - вернет если для установки исп. .setProperty()
	getComputedStyle(document.documentElement,null).getPropertyValue('--myVariable') - вернет всегда, даже если уст. в :root

    Css переменные можно вкладывать друг в друга, например:
	:root {
		--bs-blue: #0d6efd;
		--color-body: var(--bs-blue);
		--primary: #eee; }

Все эти установки работают одинаково:
  1) elem.style.setProperty('width', '50px')
  2) elem.style.width = '50px'
  3) elem.style.cssText = "width: 50px"; - удалит все инлайновые стили (т.е. свойство ноды и атрибут тега). Можно через ; писать несколько свойств.
  4) elem.setAttribute("style", "width: 50px") - удалит все инлайновые стили (то же самое). Можно через ; писать несколько свойств.

31) Кнопки не наследуют от body шрифты

32) border-bottom: 2px solid currentColor; - вариант вместо text-decoration; currentColor - возьмется цвет надписи
 
33) currentColor - ключевое слово currentColor соответствует значению свойства color для текущего элемента

34) Двигать непозиционированные элементы: отрицательный и положительный margin, transform: translate

35) Строчные элементы (span, a и т.д.) не понимают размеры, марджины, поэтому делаем их inline-block,
    либо если нужно внутри центровать, то inline-flex

35) Чтобы между inline-block элементами не было пробелов задаем родителю font-size: 0;

36) Нижняя ссылка в колонке не понимает margin-top: auto. Но можно превратить колонку в флекс,  flex-direction: column; и margin-top: auto сработает. 
    Однако она станет блочной и клик будет работать даже справа по пустому месту, поэтому делаем align-items: flex-start;

37) Не обязательно давать dataset атрибутам значения, можно просто писать <input type="button" data-counter>. 
    В этом случает dataset.counter = '' и проверяется через if(input.dataset.counter)

37) <data value="2020138256"> - оборачивающий тег для установки служебных данных, можно использовать вместо dataset
	MacBook Pro 13		
     </data>

38) Привязка обработчика события к document и далее вызов функции через определение dataset атрибута кликнутого элемента - обычная практика 
    - называется делегирование (вешаем обработчик на корневой элемент и отлавилваем события через event.target)

39) Варианты для скрытия:
 1) elem.hidden = !elem.hidden  - используем когда элемент может понадобиться (это то же самое, что dispay: none)
 2) elem.remove() - когда элемент больше не нужен будет
 3) opacity, visibility, clip-path
 4) позиционируем элемент и выводим за область видимости top: -100 %, родитель: overflow: hidden. Также без позиционирования: transform: translate
 5) color: transparent; font-size: 0;
 6) transform: scale(0)
	
40) На псевдоэлемент before и after не получится навесить обработчик, так как эти элементы не определяются в DOM

41) Обрезка элемента через css как угодно: clip-path: polygon, circle, inset, ellipse

42) У transform движение возможно по 2-3 осям: translate(120px, 50%), scale(2, 0.5), rotate(0.5turn)

43) Поворот как у двери: rotateY + transform-origin

44) Размеры elem.offsetWidth и др. можно определить только если элемент уже есть на странице, например, сначала document.body.append, 
    а затем измеряем размеры. То же самое, если display: none, то размеров пока нет.

45) Активно используем полупрозрачные цвета и тени rgba(0 0 0 .3)

46) Для псевдоэлементов не забываем указывать display: inline-block, иначе размеры не применятся

47) Привязка через onclick и addEventListener - разные функции, соответственно не перезаписывают друг друга и не отменяют друг друга

48) Отмена действий по умолчанию: 
	1) через addEventListener  - e.preventDefault()
	2) a.onclick = () => {return false}    или    a.onclick = () => false
	3) a.onclick = () => {какой-то код ..... ; return false }

49) При отмене события, работающего в связке второе событие не наступает. отмененный mousedown заблокирует focus

50) Порядок событий при клике: mouseover -> mousedown -> mouseup -> click

51) При mousedown ->движение зажатой мышью -> mouseup  - событие click не происходит

52) e.defaultPrevented - показывает было ли отменено действие по умолчанию, всплывает и также показывает было ли отменено 
                         для какого-либо элемента ниже e.currentTarget 

53) События:
	click - клик левой кнопкой мыши, происходит после mousedown и mouseup
	dblclick - двойной клик правой кнопкой над элементом
	contextmenu - клик правой кнопкой мыши над элементом
	mousedown - любая кнопка мыши нажата над элементом (можно отменить выделение текста, второй способ css: user-select: none)
	mouseup - любая кнопка мыши отпущена над элементом
	mouseover - мышь появилась над элементом
	mouseout - мышь ушла с элемента
	mouseenter/mouseleave - более простой вариант наведения, событие срабатывает и на элементе и на его потомках, не всплывает. // используем его
	mousemove - каждое движение мыши над элементом генерирует это событие
	touchmove - движение пальцем по экрану, аналогично mousemove
	focus - инпут в фокусе
        blur - потеря фокуса у инпута (можно проверить данные через js)
        focusin и focusout - срабатывают на родителях при focus и blur (эти не всплывают) на дочерних инпутах, привязываются только через addEventListener
	submit - при нажатии на submit или Enter в форме
	keydown - при нажатии клавиши (event.key - значение(зав от языка), event.code - физ. адрес(всегда одинаков))
	keyup - при отпускании клавиши
	copy - копированиe (можно отменить копирование)
	cut - вырезать
	paste - вставить
	scroll - прокрутка
	pointerdown - универсальное событие в т.ч. для сенсорных экранов, соотвествует mousedown (pointerup, pointermove, pointerover, pointerout, pointerenter, pointerleave)
        change - изменение инпута (в текстовом при потере фокуса)
        input - срабатывает каждый раз при изменении инпута
	DOMContentLoaded - построено дом дерево, но картинки и стили могут быть еще не загружены. Срабатывает на document
	load - страница (стили, картинки и др.) полностью загружена. Срабатывает на window
	beforeunload – пользователь покидает страницу, можно что-нибудь спросить. Срабатывает на window
	unload - почти ушел, можно отправить статистику и др. Срабатывает на window
	load и error срабатывают на сторонних ресурсах с src (картинки, скрипты и др.). Можно отследить успешную загрузку и выполнить дальнейшие связанные действия
	document.onselectionchange - событие при изменении выделения текста
	input.select() – выделяет всё содержимое input (textarea)
	input.onselect - срабатывает, когда выделение завершено
	dragstart - перетаскивание начато
	dragend - перетаскивание завершено
	storage - обновление localStorage, доступно в других окнах window этого источника
	wheel - прокрутка колесиком

!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

53) document.forms[0].submit() - отправка формы, можно привязать к чему-либо кроме кнопки Отправить;

54) Кнопки используем если ожидаем действие, ссылки если переходим на другую страницу

55) Разница между elem.href (elem.src) и elem.getAttribute('href'): в первом случае ссылка может быть дописана браузером. getAttribute же передаст именно то, что записано в html. Поэтому для получения ссылки используем именно elem.getAttribute('href')
То же самое для elem.src!!!

56) elem.contains  - проверяет наличие именно НОДЫ, УЗЛА внутри, а не тега или класса
    elem.matches('li.class') - удовлетворяет ли элемент указанному CSS селектору (теги, классы, атрибуты и пр - любой селектор)
    elem.classList.contains('class') - проверяет есть ли класс	

57) Чтобы у изображений пропало место под засечки делаем display: block; (или vertical-align: top)

58) Если есть изменить, если нет вставить (или увеличить): INSERT INTO table_name SET field = ..., count = ... ON DUPLICATE KEY UPDATE count = (count + 1);
    Важно: field - делаем первичным ключом, ON DUPLICATE KEY будет проверять по нему
    Второй вариант: $res = запрос на UPDATE, если $res == false (нет такого айди), то делаем запрос на вставку 

59) Если заходит робот, то $_SERVER['HTTP_USER_AGENT'] показывает что-то наподобие Bot или robot или Slurp или yahoo

60) res=>res.text() не принимает цифры из ajax.php. Нужно преобразовывать в строки (string)число

61) Копирование объектов: 
let user2 = structuredClone(user1) - современная функция для глубокого копирования
let user2 = {...user1}  - неглубокое 
let user2 = JSON.parse(JSON.stringify(user1)) - глубокое, но json не копирует функции, undefined и Symbol
let clone = Object.assign({}, user) - неглубокое
let clone = Object.defineProperties({}, Object.getOwnPropertyDescriptors(obj)); - неглубокое ? Копирует вместе с флагами и неперечисляемыми свойствами
Глубокий вариант - используем библиотеки, например lodash

62) Генерация события на примере формы:   let event = new Event('submit');   form.dispatchEvent(event)

63) e.button - номера кнопок для события mousedown: левая - 0, средняя - 1, правая - 2   / event.which - устарело

64) e.shiftKey, e.altKey, e.ctrlKey - была ли нажата клавиша при клике (не сработает на мобильных)

65) Стандартные цвета:
	#2B2D3A - темный, ближе к черному
	#252631 - темный, ближе к черному
	#ccc - серый темный
	#eee - серый светлый
	#bfbfbf - серый темный
	box-shadow: 0 5px 15px rgba(0, 0, 0, .3) - для окон с серой тенью
	#D4D8DB - приятный серый для границ
	background: #F8F8F8; - приятный серый для фона
	#E14CA0 - темно-розовый
	#f1f1f1 - светлый серый
	#04AA6D - зеленый
	#2DD4BF - светло-зеленый
	#2253DD - темно-синий
	#2196F3 - синий
	#4189FE - синий
	#0B63F6 -синий
	#51ADFF - голубой
	#ff9800 - оранжевый
	#f44336 - красный
	#e7e7e7 - серый
	#EE3C35 - дорогой красный цвет
	DodgerBlue - синий, при наведении RoyalBlue - красивая кнопка
	https://tailwindcss.ru/docs/customizing-colors/ - красивые цвета

66) Позиционирование ровно по центру:
	1) Через флексы родителю: display: flex; align-items: center; justify-content: center;
	2) Через гриды родителю:  display: grid; align-items: center; justify-content: center;
	3) Через позиционирование: Родитель: relative; Элемент: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);

67) mouseover: event.target - на какой элемент курсор пришел, event.relatedTarget - элемент откуда курсор пришел
    mouseout: event.target - элемент откуда курсор пришел, event.relatedTarget - на какой элемент курсор пришел	

68) Если двигать курсор очень быстро, то некоторые элементы могут быть пропущены, так как браузер считывает положение курсора периодически, а не на каждом пикселе

69) mouseout срабатывает даже если курсор все еще внутри элемента, но перешел на дочерний элемент. По логике браузера курсор может находится только над одним самым глубоко вложенным элементом с верхним z-index. 

70) Так как mouseover всплывает, то при переходе на дочерний элемент будет срабатывать и mouseout и mouseover (всплыло с дочернего на родителя, а у него есть обработчик). Поэтому отслеживаем target, relatedTarget когда внутри элемента есть дочерние, либо используем mouseenter, mouseleave

71) mouseenter/mouseleave - то же самое, но переходы на потомки не считаются и эти события не всплывают (не можем делегировать их). Поэтому, если нужны простые обработчики на единичные элементы, то mouseenter/mouseleave.  Если же обрабатываем, например, движение по ячейкам таблицы, то используем mouseover/mouseout делегированные на всю таблицу - а в общем это шляпа с работой над логикой target, relatedTarget, поэтому просто прогоняем mouseenter, mouseleave.

72) При открытии модального окна чтобы убрать прокрутку-скролл для body:
	1 способ:  добавляем класс body: {height: 100%; overflow: hidden;}.
	2 способ: добавляем класс body: .fixed { position: fixed; } - справа при открытии бокового меню появлялся лишний элемент, проверить
	3 способ: window.onscroll = function(){return false;}

74) Затемнение фона при модальном окне: нужно сделать пустой див с классом: 
.overlay {
	position: fixed;
	top: 0;
	left: 0;
	bottom: 0;
	right: 0;
	background: rgba(0,0,0,0.5);
	backdrop-filter: blur(4px); // размытие по желанию
	z-index: 10;
	display: block; - если в верстке стояло hidden} 
Форме даем position: absolute; z-index: 11; background: #fff; left: 50%; top: 50%; transform: translate(-50%, -50%); display: block; - если в верстке стояло hidden
Готовое модальное лежит в Полезное


75) Чтобы не было смещения при закрытии модального окна: 
0 способ: html,body { scrollbar-gutter: stable; } - при открытии и закрытии мод окна нет прыжков из-за прокрутки
1 способ: body {padding: 0 calc(20px - (100vw - 100%)) 0 0;} - прокрутки фона нет
2 способ: html {width:100vw;} - прокрутки фона нет
3 способ: html {width:100vw; overflow-x:hidden;} - прокрутка фона есть


76) position: fixed правильнее ведет себя при прокрутке страницы, чем position: absolute. Видимо он отсчитывается от угла окна, а absolute от угла элемента relative, который остается наверху при прокрутке страницы.

77) Удобно отправлять форму привязав к ней событие submit и загоняя в body: new FormData(this - форма)

78) Удобно проверять инпуты через регулярные выражения в атрибуте pattern

79) Срабатывание keydown только один раз: проверить, что event.repeat = false.  При повторах равен true

80) Событие input универсально для инпутов, сработает при событии клавиш, вставки, удалении.

81) В фигме можно мерить размеры фреймом (в виде знака обрезки слева сверху)

82) Блокам можно указать min-width: 320px; (меньше размеры тел не будут)

83) Стили для контейнеров:
	.container {
		max-width: var(--wrapper-width, 1170px); (если не задать переменную будет использовано второе значение)
		margin-left: auto;
    		margin-right: auto;
		padding-left: 16px;
    		padding-right: 16px;		
}

Подбор и сравнение цветов рядом: https://palettte.app/
https://tailwindcss.ru/docs/customizing-colors/ - красивые цвета

84) При перемещении объектов (ползунков) удобно использовать событие pointerdown + привязка elem.setPointerCapture(event.pointerId).
 В этом случае при pointerup привязки pointer к элементу будут автоматически удалены https://learn.javascript.ru/pointer-events

85) Отключить кэширование можно формированием уникальной ссылки. 
Например в ссылку на изображение в гет параметр добавить src += '?nocache=' + Math.random() . Причем nocache - ключ не имеет значения

86) Формы можно найти через document.forms - это HTML коллекция форм
    document.forms.my - форма с именем "my" (name="my")
    document.forms[0] - первая форма в документе
    document.forms.my.elements - коллекция инпутов формы
    document.forms.my.text - в форме с именем "my" (name="my") input с name="text"
	
87) fieldset - окантовка формы
    legend - подпись на окантовке

88) У каждого элемента формы есть ссылка на форму: elem.form

89) Значение для тектовых инпутов: input.value, для логических: input.checked

90) <input type="checkbox" checked="false"> - будет отмечен, неважно какое значение. 
Нужно убрать checked или изменить в DOM через input.checked = false

91) Все атрибуты доступны как свойства ноды: elem.type, elem.id и т.д.

92) select.options - коллекция options
    select.value - значение выбранного option
    select.selectedIndex – номер выбранного option (option.selected)

93) Способы изменить выбранный option:
	1) option.selected = true  (select.options[0].selected = true)
	2) select.value = значение нужного option
	3) select.selectedIndex = номер нужного option

94) multiple - атрибут позволяет выбрать несколько option

95) Array.from(nodeList или HTML коллекция) - создать массив с функциями

95) BigInt - число в js превышающее 2 в 53 степени -1 (наибольшее безопасное целове значение в js)

96) option.index - номер option
    option.value - value
    option.text - текст (option.textContent)

97) По умолчанию fetch не отправляет куки. Для того чтобы их включить (работать с сессиями):
	let promise = fetch('/ajax/', {credentials: 'include'});

98) package.json - js пакеты для фронта, composer.json - php пакеты для бэка

    в package.json в ключе scripts можно указывать команды для запуска скриптов библиотек из консоли.
    Например: "start": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch" // запускает слежение и конвертирование tailwindcss
    Теперь в консоли пишем: npm run start

99) В examples бутстрапа выбираем нужный элемент (напр. шапку, исследуем, copy element, вставляем)

100) Ларавель:
	Установка: если композер уже установлен глобально, то заходим в папку сайтa:
	cd название папки сайта
	в терминале пишем composer create-project --prefer-dist  laravel/laravel ./
		          composer create-project laravel/laravel:^10.0 . - если версия ниже
		          composer create-project --prefer-dist laravel/laravel:10.* ./ - установка 10 версии

	в опен сервере в доменах выбираем корневую папку проекта \laravel.ru\public
        имя домена пишем как будет на хостинге, чтобы для локальной и удаленной версии совпадало APP_URL (а если разные .env использовать?)
	Либо запускаем через php artisan serve
	APP_URL будет использоваться в asset
	В APP_URL сразу пишем домен с https://

	Запуск: php artisan serve
	в .env устанавливаем название базы 
	в config database ставим:
		 charset = utf8mb4, 
		 collation = utf8mb4_general_ci (utf8mb4_0900_ai_ci - поддерживает эмодзи) - должна совпадать с кодировкой в БД => Операции

	php artisan storage:link - сразу делаем ссылку
	
	в layout @yield('main_content') - в это поле будут вставляться данные из доп. слоев между тегов @section('main_content') ... @endsection
	Внимание, можно сократить до @section('title', 'My blog'), например для тайтлов, здесь второй параметр - значение 
	по умолчанию, которое срабатывает, так как секцию не заполнили

	Также вместо @yield('xxx') на главной странице можно использовать @section('xxx') 111 @show - будет работать 
	как yield с двумя параметрами @yield('xxx', 111) - то есть если переменная 'xxx' перезаписана, то показать перезаписанное,
        иначе показать значение по умолчанию.

	Отличие @yield(xxx) от @section(xxx)  @show в том, что во втором случае мы можем в дочерней странице включать вывод 
	родительской секции через @parent:
	@section(xxx) 111 @show - в layout
        @section(xxx) @parent 222 @endsection - в дочерней включаем показ родительской секции
        Итого вывод: 111 222, через просто @yield такое взаимодействие не получится

	сам главный слой вставляется во внутренние через @extends('layout'). Пишем эту команду сверху, а ниже перечиляем данные через @section('') ... @endsection
	В ларавел переход по папкам происходит через точку, напр. в extends, view

101) autofocus - атрибут автофокус для инпута
     required - обязательное
     pattern - проверка регуляркой

102) elem.matches('p#elem') - соответствует ли нода css селектору (проверка на тег, класс, атрибут и любой селектор, даже :hover)
     elem.classList.contains('class') - содержит ли класслист данный класс
     elem.closest('.class') - ищет ближайшую родительскую ноду с таким селектором, сам элемент включен в поиск
     elem.closest('.class,.class1,#id,p') - внутри можно перечислять
     elem.classList.add - добавить класс (либо elem.className += " class")
     elem.classList.remove - удалить класс (либо elem.className = elem.className.replace(" class", ""))
     elem.classList.toggle - чередовать класс

103) elem.style.cssText	- переписать атрибут style

     Все эти установки работают одинаково:
  	1) elem.style.setProperty('width', '50px')
  	2) elem.style.width = '50px'
  	3) elem.style.cssText = "width: 50px"; - удалит все инлайновые стили (перепишет свойство ноды style - 
						 оно же атрибут тега style). Можно через ; писать несколько свойств.
  	4) elem.setAttribute("style", "width: 50px") - удалит все инлайновые стили (свойство ноды и атрибут тега). 
                                                       Можно через ; писать несколько свойств.

104) let style = getComputedStyle(elem)	- получить объект со всеми итоговыми стилями элемента (внешний css, инлайновые стили)

105) let contains = parent.contains(child) - содержит ли нода другую ноду

106) Есть методы для фокуса и блюра: input.focus() (можно применить если поле зап неверно) и input.blur() 

107) Метод для отправки формы: form.submit()

108) :focus = css селектор элемента в фокусе
     :focus-within - выделяет элемент, если внутри него есть другой элемент в фокусе
     :link - только для непосещенных ссылок
     :visited - только для посещенных ссылок (можно стилизовать цвета, другие стили будут игнорироваться)  
     :hover - элемент при наведении (кнопки, ссылки, дивы и т.д.)
     :active - элемент при нажатии (кнопки, ссылки, дивы и т.д.)
     :empty - у элемента нет дочерних узлов, помогает убрать внешние отступы, рамки у пустых родителей
     :root - выбор корневого элемента html (исп. для переменных, то же самое что html {})
     :checked - выбранный radio, checkbox и option
     :enabled - включенный элемент
     :disabled - отключенный элемент 

109) Волшебное свойство tabindex.   focus работает только при клике по инпутам, чекбоксам и т.д. 
Однако если задать элементу tabindex="число", 
то событие focus будет срабатывать и для них, а в css можно будет отслеживать :focus (то есть клик) 
даже для дивов и т.д. Самому такому элементу можно назначить даже атрибут autofocus

110) tabindex + :focus заменит навешивание js клика и смены состояния элемента

111) Атрибут tabindex как и обычный атрибут можно навесить через DOM : elem.tabIndex

112) document.activeElement - показывает текущий элемент в фокусе. 
Можно даже заменить e.target и отлавливать клики по нужным элементам, если дать им tabindex: 
document.addEventListener('focusin', function () {console.log(this.activeElement)})
Здесь focusin срабатывает если у кликнутого элемента был tabindex или он являеется инпутом

113) Атрибут contenteditable делает элемент редактируемым
	<div class="editor" contenteditable> редактируемый текст </div>
     Также можно задать для всего документа в js:
	document.designMode = "on";

114) Если записал ноду в переменную, то при замене ее через replaceWith она по-прежнему в памяти
 и ее можно редактировать, а затем обратно поменять (например замена дива на textarea и обратно)

115) тег pre - текст и символы внутри будет с такими же переносами как в  html, можно рисовать картинки символами

116) getBoundingClientRect() отсчитывается от vieport
     position fixed; left и top тоже отсчитывается от vieport + оказывается absolute то же самое, что fixed
     То есть их можно использовать для взаимодействия

117) getComputedStyle(div) - объекст с рассчитанными стилями

118) Дмигать можно элементы с любым таким позиционированием, соседние элементы они двигать не будут:
     position: relative - элемент в обычном потоке там где должен стоять
     position: absolute - другие элементы его не видят, 
                          находится в верхнем левом углу ближайшего спозиционированного родителя, 
                          либо если его нет - то относительно документа. 
                          Размер становится по содержимому (как inline-block)
     position: fixed - подвид абсолютного позиционирования. 
                       Позиционирует объект точно так же, как absolute, но относительно window, 
                       остается на месте при прокручивании

119) Встраивание html в php:
	<?php for($i=0; $i<5; $i++): ?>
		<div>Текст</div>
    	<?php endfor; ?>
     
120) Устарело:
     Запись в буфер обмена: div.addEventListener('copy', function (e) {e.clipboardData.setData('text/plain', 'foo'); e.preventDefault()})
     Вставка из буфера: input.addEventListener('paste', function (e) {div.innerHTML = e.clipboardData.getData('text/plain')})

     Новый способ:
	navigator.clipboard.writeText('текст')

120) Показ окошка "Скопировано" при клике на элемент:
	.alert-span {
      	  position: fixed; // это для прилипающей шапки. Если ссылка в обычной шапке, то absolute
	  z-index: 100;
      	  margin-top: -1.3em;} // поднимаем окошко над элементом
        .alert-span::before {
      	  content: "Скопировано"; // текст берется отсюда
          font-size: 0.6em;
          padding: 2px;
          background-color: white;
          border: 1px solid #eee;}

        <a href="#" id="elem">rodnovspb@mail.ru</a>

    	elem.addEventListener('click', (e) => {
	   e.preventDefault();
	   navigator.clipboard.writeText(e.target.textContent)
	    .then(() => {  // через then потому что в копировании используется Promise
		const messageElement = document.createElement('span')
		messageElement.classList.add('alert-span')
                const linkRect = elem.getBoundingClientRect()
		messageElement.style.left = `${linkRect.left}px`;
		messageElement.style.top = `${linkRect.top}px`;
                document.body.appendChild(messageElement)
                setTimeout(() => { messageElement.remove()}, 700);})
	    .catch(error => { console.error(`Текст не скопирован ${error}`) })})

121) Навешивание одной функции на несколько действий: input.oncut = input.oncopy = input.onpaste = func  

122) Отмена копирования и вырезания: div.oncut = div.oncopy = ()=>false

123)  Отмена отправки формы:  form.onsubmit = ()=> false   либо   form.addEventListener('submit', function (e) {e.preventDefault()})

124) Принудительная отправка формы btn.onclick = ()=>form.submit()

125)    DOMContentLoaded - построено дом дерево, можно искать узлы и навешивать события. Размеры не известны (если картинки не закешированы). Срабатывает на document
	load - все загружено, стили применены, размеры картинок известны. Срабатывает на window
	beforeunload – пользователь покидает страницу, можно что-нибудь спросить. Срабатывает на window
	unload - почти ушел, можно отправить статистику и др. Срабатывает на window

126) DOMContentLoaded - сначала размеры картинок img.offsetWidth и img.offsetHeight покажет 0, 
     во второй раз правильные, так как картинки возьмутся из кеша. Проверить можно через добавление разных гет параметров к ссылке картинки

127) DOMContentLoaded выполнится когда обычные скрипты загрузятся и отработают. Исключение: async скрипты и document.createElement('script')

128) После DOMContentLoaded грузится автозаполнение в инпуты. 
     Соотвественно когда наблюдаем задержку автозаполнения, то это еще не отработал DOMContentLoaded ждущий скрипты

129) window.onload можно использовать для обнаружения полной загрузки страницы с картинками, стилями и др.

130) window.onunload - по событию обычно отправляю статистику, данные по кликам, просмотрам и т.д.

131) Для отправки такой статистики применяется navigator.sendBeacon(url, data). Страница закрылась, но отправка продолжается в фоне без ожидания ответа:
        window.addEventListener("unload", function() {
          navigator.sendBeacon("/analytics", JSON.stringify(analyticsData));}); 
Размер данных ограничен 64 Кб.

132) document.readyState показывает текущее состояние загрузки документа: 
       loading – документ загружается
       interactive – документ был полностью прочитан
       complete - все загружено (картинки и др)

133) readystatechange - событие срабатывает на document при смене состояния document.readyState: loading, interactive, complete. 
                        Появилось давно, сейчас используется редко

134) Порядок срабатывания: 
	1) document.readyState = "loading"
	2) document.readyState = "interactive"
	3) DOMContentLoaded
	4) События load на изображениях, др.
	5) document.readyState = "complete"
	6) window.onload

135) Событие load срабатывает на window и на загрузке сторонних ресурсов: картинках, скриптах с src, фреймах и др.

136) Просмотр буфера обмена win + v

137) Перекрытия кнопок и картинок-фонов лечатся через position: relative; z-index или для картинки pointer-events: none;

138) Если два элемента накладываются друг на друга, то выше будет тот элемент, который идет ниже в HTML коде. 
     Регулировать можно сменой мест или через z-index
     z-index не будет поднимать элемент выше контекста наложения родителя, есть ниже условия

139) Обычные скрипты (src  между script) не видят DOM элементы ниже себя

140) Обычные скрипты загружаются в порядке очереди и блокируют дальнейшую загрузку, пока не загрузятся

141) defer и async - скрипты грузятся фоном, не блокируют загрузку страницы. Применяется только для внешних скриптов с src

142) defer - отложенная загрузка. Скрипт запускается после построения DOM
	     Загружается весь HTML, строится DOM, грузится этот скрипт, событие DOMContentLoaded
	     Удобно использовать в head (<script src="script1.js" defer></script>) вместо вставки скрипта перед закрытием body

143) async - скрипт грузится и выполняется независимо и параллельно верстке и другим скриптам, не влияет на DOMContentLoaded.
     Какой первый async агрузится первым, тот и выполнится.

144) async скрипты применяются для сторонних счетчиков, рекламы и др.

145) Динамически создаваемые скрипты document.body.append(script) ведут себя как async. 
     Это регулируется через script.async = false . Теперь будут выполняться как расположены в документе.

146) Так как defer срабатывает до применения стилей (до DOMContentLoaded(DOM построен), до load(все загружено)), а верстка уже видна,
     то если скрипт большой имеет смысл пока отключить взаимодействие с пользователем (показать индикатор загрузки и др.)

147) На практике defer применяется для скриптов взаимодействующих со всем DOM или если важен порядок выполнения;
     async хорош для независимых скриптов

148) События load и error срабатывают после загрузки сторонних ресурсов c src

149) События load и error на элементах показывают только успешность/неуспешность загрузки.
     
150) Чтобы отловить саму ошибку скрипта нужно использовать window.onerror. Прописать перед скриптами.
     window.onerror = (message, url, lineNumber)=>{}
     Ловит только ошибки скриптов (внешних и внутренних своего домена). Предназначена для фиксирования ошибок, страница упадет все равно.

151) Если ошибка произошла в скрипте со стороннего сайта, то чтобы ее отловить через window.onerror нужно:
     1) Чтобы в скрипте стоял атрибут crossorigin="anonymous" или crossorigin="use-credentials"
     2) Удалённый сервер должен поставить заголовок Access-Control-Allow-Origin

152) Браузер грузит изображение как только увидит его src. То есть если стоит <img src="" alt="">, 
     то браузер грузит изображение и показывает по мере загрузки. 
     Можно же заранее загрузить изображение в кеш и мгновенно показать в нужном месте:
	Для этого отслеживаем, что изображение загрузилось в кеш через событие img.onload (img.onerror)
	Как только сработало, добавляем document.body.append(img)
	Можно добавлять ноды-изображения в массив и увеличивать счетчик. 
	Как только массив заполнится одновременно добавляем все изображения в цикле document.body.append(elem)

   Чтобы изображение не грузилось построчно его также можно оборачивать в див.
     	
153) Движение блоков: полож. и отр. марджины, трансформ транлейт, position: abs, rel

154) Для :before и :after можно задавать картинку не только бэкграундом, но и content: url("...");

155) MutationObserver - встроенный объект наблюдающий за DOM и запускающий колбэк каждый раз 
в случае изменения DOM
Как работает: 
let observer = new MutationObserver(callback)  // в callback передается объект с изменениями
observer.observe(node, config)	// node - узел за которым наблюдаем, config - настройки наблюдения
Пример: 
 let observer = new MutationObserver(mutation => {
  console.log(mutation) // mutation - массив с объектами изменения внутри
});
В объекте mutation будет информация о типе изменения (атрибут, текст, изменения в потомках, 
target - где произошло изменение, addedNodes/removedNodes – добавленные/удалённые узлы и др.)

observer.observe(div, {
	childList: true, // наблюдать за изменениями в непостредственных детях div
	subtree: true, // во всем потомках div
	attributes: true, // в атрибутах div
	attributeFilter: [...] // в отпределенных атрибутах
	characterData: true // наблюдать за изменением текста в div
})
Более подробно: https://learn.javascript.ru/mutation-observer

IntersectionObserver - запускает функцию, если элемент попадает в область видимости (или в др. область)
	let observer = new IntersectionObserver((entry)=>{
    	if(entry[0].isIntersecting) {console.log('входим') - если блок появился в области видимости
        } else {console.log('выходим')}}) - если блок вышел из области видимости

	observer.observe(document.querySelector('.elem')) - отслеживаемый блок

Ленивая загрузка картинок (браузер запрашивает картинку как только видит заполненный атрибут src):
		if(!!window.IntersectionObserver){ - если браузер поддерживает
  		   let observer = new IntersectionObserver((entries, observer) => { 
    		      entries.forEach(entry => { - перебираем картинки
    		       if(entry.isIntersecting){ - если картинка появляется
      			  entry.target.src = entry.target.dataset.src; - вставляем в ее src путь из ее data
      			  observer.unobserve(entry.target);}}); - отключаем отслеживание
  			  }, {rootMargin: "0px 0px -200px 0px"}); - настраиваем срабатывание наблюдателя относительно нижней границы браузера

  		   document.querySelectorAll('img').forEach(img => { observer.observe(img) });} - перебираем все картинки, навешиваем наблюдатель

Плавное появление блоков:
1) Нужным блокам задаем класс smooth
2) Стили:
	.smooth { transition: opacity 0.7s ease, transform 1s ease;}
	.smooth.dn { opacity: 0; transform: translateY(8%);}
   Два класса smooth и dn задал для того, чтобы если браузер не поддерживает наблюдение, то блоки были изначально видимые.
   Если поддерживает, то js навесит dn и блоки скроются

3)  if(!!window.IntersectionObserver){ - если браузер поддерживает
       let observer = new IntersectionObserver(((entries, observer) => {
          entries.forEach(entry => { - перебираем блоки 
             if(entry.isIntersecting){ - если блок появился
                 entry.target.classList.remove('dn') - удаляем скрывающий класс
                 observer.unobserve(entry.target)}})})) - отключаем наблюдение

       let items = document.querySelectorAll('.smooth') - находим блоки с плавным появлением

       if(items && items.length > 0){ - если такие есть
           items.forEach(item => {
             item.classList.add('dn') - скрываем их
             observer.observe(item)})}} - начинаем наблюдать

Также можно запускать бесконечную загрузку, выполнять другие действия связанные с появлением элементов 
во viewport
Есть настройки процентов входа
Подробнее: misha.agency/javascript/intersection-observer-api.html


156) Порядок верхних нод: document (но не узел пишут) <- document.documentElement (HTML) <- document.body

157) Если скрипт расположен в ноде document.head , то document.body для него равен null, так как еще не прочитан

158) Связи: 
	узлы - элементы (html-теги), комментарии, text, пробелы
	элементы - только html-теги
	дочерние узлы/элементы - непосредственные дети
	потомки - все потомки

	parentNode - родительский узел (совпадает с parentElement кроме тега html: 
			document.documentElement.parentNode = document, a document.documentElement.parentElement = null. 
			Причина в том, что document - не элемент)
	parentElement - родительский элемент
	previousElementSibling - предыдущий элемент в одном родителе
	previousSibling - предыдущий узел в одном родителе
	nextElementSibling - следующий элемент
	nextSibling - следующий узел в одном родителе
	children - HTML коллекция дочерних элементов
	childNodes - ноделист дочерних узлов
	firstElementChild - первый дочерний элемент
	firstChild - первый дочерний узел
	lastElementChild - последний дочерний элемент
	lastChild - последний дочерний узел

159) Вышеперечисленные связи используются только для чтения. 
     Для изменения используют методы node.append(elem) и т.д.

160) 	table.rows (tbody.rows) - коллекция строк tr
	tr.cells - коллекция <td> и <th> ячеек, находящихся внутри строки <tr>
	tr.rowIndex – номер строки <tr>
	td.cellIndex – номер ячейки в строке <tr>

169) DOM-коллекции живые (children)

170) Чтобы превратить коллекцию и ноделист в массив (+методы) применяют Array.from(коллекция)

171) document.querySelectorAll(':hover') - псевдоклассы тоже работают, вернутся элементы под курсором

172) 	innerHTML - содержание элемента вместе с тегами (если вставим теги, то будет отображаться по правилам тегов)
	outerHTML - внешний код элемента с тегами
	nodeValue/data - содержимое текстового узла (не элемента)
	textContent - содержание элемента за вычетов тегов (если вставим теги, то они будут отображаться символами)
	nodeName/tagName - название элемента/узла ("DIV")
	nodeType - тип DOM-узла (1 - элемент, 3 - текстовый узел и т.д.)

173)    elem.hasAttribute(name) – проверяет наличие атрибута
	elem.getAttribute(name) – получает значение атрибута
	elem.setAttribute(name, value) – устанавливает значение атрибута
	elem.removeAttribute(name) – удаляет атрибут
	
	Атрибуты – это то, что написано в HTML. Свойства – это то, что находится в DOM-объектах.
	Атрибуты и свойства Дом взаимно синхронизируются. Одно из исключений value (напр у инпутов)
	Предпочтительнее использовать DOM-свойства, чем setAttribute, getAttribute

174) Нестандартные атрибуты записываем в dataset

175) elem.classList.add/remove("class") – добавить/удалить класс
     elem.className += " class" - второй способ добавить класс
     elem.className = elem.className.replace(" class", ""); - второй способ удалить класс	
     elem.classList.toggle("class") – добавить класс, если его нет, иначе удалить
     elem.classList.contains("class") – проверка наличия класса, возвращает true/false 

176) При зажимании SHIFT колесо двигает страницу по горизонтали

177) Перенос можно делать назначением элементу display: block;

178) Чтобы текст в блоке переносился можно задавать ему максимальную ширину или пэддинг правый

179) Затемнение картинки: назначаем картинке before, 
     и его затемняем полупрозрачностью бэкграунда(rgba) или opacity

180) Картинки для before и after можно добавлять не только бэкграундом, но и content: url("...")

181) Вставка карты Гугл: Находим адрес, поделиться, встраивание карт, копировать html, 
     вставить куда нужно этот фрейм (убираем из фрейма ширину и высоту, чтобы под размер дива встроилось)
     iframe задаем высоту и ширину 100%	

182) Для ul можно прописывать количество колонок: ul {columns: 2; width: 200px; (это ширина ul)}

183) Получить выделенный текст: document.getSelection().toString(), например: 
     document.oncopy = ()=>{console.log(document.getSelection().toString())}

184) document.onselectionchange - событие срабатывает при изменении выделения

185) 	input.select() – выделяет всё содержимое input (textarea)
	elem.onselectstart - начато выделение, в т.ч. на p, div и т.д.
	input.onselect - срабатывает, когда выделение завершено (mouseup)

186) <input type="text" disabled> - пользователь не может менять значение (но можно через js)

186) inert - выполняет то же действие, что disabled, но inert можно применить для любого тега!!
             на такой тег/блок нельзя кликнуть, сделать фокус, ввести данные и найти через поиск по странице и пр.

	<form inert> - форма не будет работать, хотя будет показываться как обычно
		...
	</form>

187) Способы сделать невыделяемым(однако выделится если начать с другого элемента):
	1) Через css: user-select: none;
	2) elem.onselectstart = () => false
	3) Через css сделать как будто не выделяется: *::selection {background: transparent;}
	4) Доп. средство: можно убрать курсор cursor: none;

188) ::selection - псевдокласс для выделяемого текста

189) Если для ::selection  сделать color: transparent, то будет эффект ластика

190) Oбъекты Selection и Range позволяют работать с выделяемым текстом (выбирать, изменять и пр.)

191) Задачи браузер складывает в очередь и выполняет по очередности. Первой пришла, первой выполнилась.

192) Если браузер выполняет трудоемкую задачу, то ничего больше не выполняется (даже отрисовка страницы) - 
отсюда зависание при бесконечном цикле.

193) Поэтому применяется разбиение трудной задачи на подзадачи через setTimeout с нулевой задержкой.
     Как если функция count ведет счет до 1 млдр. Разбиваем на этапы: сначала до миллиона, 
     если результат не равен 1 млдр, то через setTimeout(count) - нулевая задержка -  запускаем заново,
     где переменная i берется из глобальной о.в. Между этими setTimeout браузер проверяет 
     есть ли другие задачи и выполняет их	https://learn.javascript.ru/event-loop

194) do {делать это} while (пока это равно true)

195) Для визуального отображения выполнения трудной задачи можно использовать индикатор:
     выполняем до какого-то промежуточного значения, отрисовываем индикатор (например, увеличиваем style.width), 
     проверяем выполнилось ли полностью, если не выполнилось, то setTimeout с нулевой задержкой

let i = 0;

  function count(){
    do {
      i++
      progress.style.width = i/1085 + 'px'
    } while ( i % 10000 != 0 )

    if(i < 1000000){
      setTimeout(count)
    }

  }

  count()

196) Порядок выполнения кода:  
     Синхронный js -> Микрозадачи (промисы) по порядку -> Отрисовка страницы -> Макрозадачи (setTimeout) по порядку

197) Удобно верстать в файлах php потому что футер, header и пр. можно подключать через require

198) Верстают по типу:
	<body>
    	<div class="wrapper">
        <header class="header">
            <div class="container">
		...
            </div>
        </header>

        <main class="main">
            <section class="nameSection">
                <div class="container">
		...                    
                </div>
            </section>
        </main>
        
        <footer class="footer">
            <div class="container">
		...                
            </div>
        </footer>
    	</div>
    	</body>

Классы: body { overflow-x: hidden } - если просто overflow, то прокрутка запретится, а для wrapper ставить hidden нельзя, 
                                      так как sticky тогда не работает
	.wrapper {display: flex; flex-direction: column; width: 100%; min-height: 100vh;}
	.header {position: sticky; top: 0; z-index: 100;}
        .container {position: relative; width: 100%; max-width: 1200px; margin-left: auto; margin-right: auto; padding: 0 15px;}
	.main {flex-grow: 1;}
	.footer {margin-top: auto;}

Для расстояния между родителем и дочерним отступы задаем родителю. Это делается, чтобы при смене дочернего отступ был всегда:
	<header class="header" style='padding: 10px 0'> - отступ сделали в родительском элементе
	    <div class='child'></div>
	</header>

199) Если нужно обрезать уголки, то делаем border-radius: 50%;

200) Затемнение картинки можно делать через before + его затемняем

201) this по умолчанию передается в функцию: 
p.addEventListener('click', func)
function func(){
    this.textContent = this.textContent**2}

202) Способы перейти по ссылке:
     1) let a = document.createElement('a'); a.href = 'https://yandex.ru/'; a.click()
     2) window.open('https://yandex.ru/') или open('...')
     3) window.location.href = 'https://yandex.ru/' или location = '...'
     4) window.location.href = '{{ route('index', ['id' => 1]) }}' - на роут ларавель
     5) window.location.href = `{{ route('index') }}?test=${var}` - на роут ларавель переменную js

203) Настройки: window.open(url, name, params), где name - имя окна, 
                params: координаты, размеры; наличие прокрутки, инструментов, навигации

204) window.open возвращает ссылку, по которой можно редактировать новое окно:
	let newWindow = window.open('/');
	let html = '<p>Привет!</p>';
	newWindow.onload = ()=> newWindow.document.body.insertAdjacentHTML('afterbegin', html);

205) Закрыть попап: newWindow.close()

206) Альтернативы попапу: div c высоким z-index, <iframe>

207) Два URL имеют одинаковый источник (Same Origin) если совпадает протокол, домен и порт.
	В этом случае одна страница имеет доступ к document, переменным второй.

208) <iframe> - в него встраивается независимый document со своими отдельными <html><head><body>

209) clickjacking - размещение прозрачного позиционированного <iframe> поверх элемента. 
Например невидимый <iframe> с кнопкой "Нравится" социальной сети поверх видимого завлекающего заголовка.
Для предотвращения открытия вашего сайта во фрейме можно использовать 
заголовок сервера X-Frame-Options: SAMEORIGIN


209) Кодировка.
     Буквенные символы хранятся в компьютере как цифры. Соответственно слово - набор цифр.
     Таблица с соответствием: символ = определенная цифра = 8 битов = 1 байт - и есть кодировка (в js это UTF-16)
     'a' = 97 = 01100001	
     'a'.charCodeAt(0) => 97
  
     Соответственно сравнение слов в js:
	'а' > 'б' => false (так как 1072 меньше 1073)

209) Служебные символы в строке:
	\n - перенос строки
	\t - табуляция

210) 1 байт это 8 битов(0 или 1). 2*8 степени это 256. 
Так как счет идет от 0, то номер последнего символа 255 (127 в базовой ASCII)
Символ из ASCII занимает 1 байт(например ! - 00100001): базовая таблица 128 символов, расширенная 256.
Символы в Unicode занимают от 1 до 4 байт. Первые 256 - те же ASCII.
Например русская буква 'e' кодируется двумя символами юникода: 208 181 (11010000 10110101)

211)  Преобразование в двоичную систему: 15..toString(2)

212) let buffer = new ArrayBuffer(16) - создана непрерывная область памяти заполненная 16 битами,
		  это необработанные бинарные данные.

213) ArrayBuffer можно разделить на части, например new Uint8Array(buffer) делит buffer по 8 бит

214) Данные можно превращать в объект Blob: 
     let blob = new Blob(['hello,', 'Denis'], {type: 'text/plain'}) //можно помещать строки, другие blob, BufferSource

215) Blob это те же бинарные данные, но с типом. В такой объект можно превращать текст, изображения, url и др.
Также наоборот.

216) <a download="29.html" href="#">загрузить</a> - атрибут download указывает делать 
     выгрузку файла на компьютер пользователя вместо перехода

217) <input type="file" multiple> - загрузка нескольких файлов

218) Загрузку через <input type="file" id="input"> отслеживаем через событие "change". 
     Полученный файл лежит в input.files[0]

219) Загруженный файл читается через создание FileReader, у которого есть три метода чтения:
     - readAsArrayBuffer - чтение в бинарный код
     - readAsText - чтение в строку
     - readAsDataURL - чтение в src в формате base64 (для img и др.)
В течение чтения происходят события: loadstart - начато, progress, load - закончено без ошибок, error - ошибка,
loadend – чтение завершено (успешно или нет).
Результат будет лежать в reader.result
Ошибка в reader.error

Пример показа миниверсии загруженной картинки:	
	input.onchange = ()=>{
	let file = input.files[0] 
	let reader = new FileReader() //создаем FileReader
	reader.readAsDataURL(file) //читаем для src
	reader.onload = ()=> img.src = reader.result
	reader.onerror = ()=> console.log(reader.error)}

220) Всякие значки, плюсики и прочее, откликающиеся на нажатие делаем через button

221) Изменить размеры значков Unicode можно через font-size

222) Неспозиционированные абсолютно before и after могут применять margin, реагируют на display: flex;

223) Инлайновые и инлайн-блочные элементы двигаются к краям через float, например: float: right;

224) fetch выполняется в два этапа: 
	1) Можно проверить статус HTTP-запроса (узнаем выполнился ли запрос: есть ли интернет и есть ли сайт)
	   Статусы 404 и 500 не являются ошибками. Проверка делается через свойства status (возвращает код)
           или ok (возвращает true или false).
	2) Можно получить тело ответа (body) через методы: 
	   response.text() - читаем ответ как текст
           response.json() - декордируем из json
	   response.formData() - возвращает ответ как объект FormData
           response.blob(), response.arrayBuffer() - ответ как blob или бинарные данные
Примеры: 
(async () =>{
    try {
      let response = await fetch('29.html') // 1 этап, можно получить заголовки ответа
      if(response.ok){
        let data = await response.json() // 2 этап, получаем тело ответа
        console.log(data)
      } else {
        console.log("Ошибка HTTP: " + response.status)
      }
    } catch (e){
      console.log(e)
    }
  })()	

fetch('29.html').then(res=>res.text()).then(res=>console.log(res)).catch(e=>console.log(e))

225) Пример получения картинки через fetch методом blob:
(async () =>{
    try {
      let response = await fetch('arrow.svg')
      if(response.ok){
        let blob = await response.blob()
        img.src = URL.createObjectURL(blob)
      } else {
        console.log("Ошибка HTTP: " + response.status)
      }
    } catch (e){
      console.log(e)
    }
  })()

226) Помимо статуса response.status и response.ok в ответе сервера можно получить 
     заголовки response.headers (объект наподобие Map)

227) Заголовки запроса же устанавливаем так: 
     fetch('29.html', {headers: {Authentication: 'secret'}})
     Многие заголовки обеспечивают достоверность данных и корректную работу протокола HTTP, их установить не сможем.

228) По умолчанию fetch отправляет запрос методом get. (также можно отправить данные в get параметрах) 
     Если же нужно отправить более крупные данные на сервер, то используем метод POST:
fetch('29.php', {
     method: 'POST',
     body: JSON.stringify(obj})
В body можно отправить: FormData, Blob, обычную строку, UrlSearchParams (то есть вместо того, 
чтобы записывать в get параметры), BufferSource

229) Если запрашиваем много данных через fetch, например, в цикле и результаты (fetch всегда возвращает промисы) 
помещаем в массив, то можно дождаться заполнения массива через let results = await Promise.all(arr)
Пример (без обработки ошибок): 
	(async ()=>{
    	let arr = []
    	for(let name of ['cined', 'qwerty', 'robot', 'qqq']){
      	let res = fetch(`https://api.github.com/users/${name}`).then(res=>res.json())
      	arr.push(res)}

    	let results = await Promise.all(arr)
    	console.log(results)})()

То есть в массив отправляем промисы, они выполняются, а мы в это время перебираем другие аккаунты

230) Отправить все поля формы за один раз в объекте FormData:
	method: 'POST',
	body: new FormData(form) // кодировать не надо, все уйдет само с заголовком Content-Type: form/multipart;
Файлы тоже отправляются
В Ларавель получить эту форму: $form = $request->all();

231) Методы FormData: formData.append(name, value), formData.delete(name), formData.get(name), formData.has(name)

232) <input type="file" accept="image/*"> - разрешенные MIME-типы
		        accept=".jpg, .jpeg, .png"

233) Рисование канвас:
	<body style="margin:0">
	   <canvas id="canvasElem" width="100" height="80" style="border:1px solid"></canvas>	
	</body>
	<script>
           canvasElem.onmousemove = function(e) {
           let ctx = canvasElem.getContext('2d');
           ctx.lineTo(e.clientX, e.clientY);
           ctx.stroke();};
	</script>
Этот канвас можно отправить в FormData на сервер: (https://learn.javascript.ru/formdata)
	canvasElem.toBlob
	formData.append("image", imageBlob, "image.png")

234) Получение данных о прогрессе загрузки происходит с помощью потока для чтения response.body.
В общем: 
- Получаем этот поток
- Получаем общий размер файла через response.headers.get('Content-Length')
- В цикле проверяем размер очередного фрагмента двоичного файла, пушим этот фрагмент в массив
- В линии прогресса показываем сколько закачалось из общего размера файла
- Как только файл загружен собираем массив фрагментов в один файл и декодируем в нужный формат
https://learn.javascript.ru/fetch-progress

235) Прижать блочный элемент к низу или краю: margin-top, margin-left: auto; Во флексах все элементы блочные;

236) Прерывание fetch и др. асинхронных задач:
 let controller = new AbortController();
 fetch('29.html', {signal: controller.signal}).then(res=>{...}).catch(e=>{
	if(e.name == 'AbortError') { console.log('Прервано') } // прерывание выбрасывае ошибку, ее можно обработать
        else {console.log(e)}}) 
controller.abort() // вызываем событие abort на controller.signal - прерываем fetch

236) Получается, что form.submit(), link.click() - тоже вызовы событий на форме и ссылке

237) Функция map возвращает новый массив, изначальный массив не меняется, если не переписать его
     arr = arr.map(elem=>elem**2)

238) Функция forEach ничего не возвращает (undefined) и не меняет изначальный массив, она только перебирает:
     arr.forEach(elem=>{console.log(elem**2)})

239) Запрос js, fetch на другой источник (протокол, домен, порт) чаще всего невозможен. Как реализована работа:
- Для простых запросов (GET, POST или HEAD + небольшой список разрешеннных заголовков, которые могут быть сделаны также через <form> или <script>)
1) Браузер всегда добавляет к запросу на другой сайт preflight запрос с заголовком Origin с доменом скрипта
2) Сервер проверяет Origin и если доступ разрешен, то в заголовке ответа ставит: 
   200 OK
   Access-Control-Allow-Origin: домен запрашивающего скрипта или *
3) Скрипт отправляет основной запрос или  выводится ошибка, если доступа нет

- Для сложных запросов (PATCH, DELETE и др или неразрешенные заголовки в простых запросах)
1) Браузер делает предзапрос OPTIONS с Origin, Access-Control-Request-Method: напр, PATCH, 
списком неразрешенных заголовков для простых запросов: 
Access-Control-Request-Headers: Content-Type,API-Key
2) Сервер должен прислать разрешение на метод и заголовки:
Access-Control-Allow-Methods: PATCH
Access-Control-Allow-Headers: Content-Type,API-Key
3) Браузер видит разрешение и посылает основной запрос

В браузерах часто стоят блокировщики рекламы (Privacy Badger и пр), запрещающие отправлять аякс запросы на другой источник (поэтому выскакивают ошибки метрики и пр)
Например, у меня fetch на api гео яндекса заработал только после отключения Privacy Badger.
Поэтому лучше отправлять аякс запрос на php страницу на своем сайте, а она уже пусть отправляет запрос через cUrl, газзл и пр на другой сайт.

Также есть старый подход JSONP:
	<script> function func(a){ return a*5 } </script> - в эту функцию попадет ответ из нижнего скрипта
	<script src="https://site.ru?id=123&callback=func"></script> - ответ будет обернут в функцию func, она отработает с полученными со стороннего сайта данными
                                                                       этот скрипт можно создавать и затем вставлять в head через append
Вместо JSONP лучше применять аякс с соответствующими заголовками CORS.

240) Запросы через JS на другой источник не включают куки в отличие от простого запроса страницы браузером.
Например, fetch('http://another.com') не посылает никаких куки. 
Чтобы включить отправку куки нужно написать:
fetch('http://another.com', {
  credentials: "include"
});
Сервер в ответе должен разрешить принятие авторизационных данных: 
200 OK
Access-Control-Allow-Origin: https://javascript.info
Access-Control-Allow-Credentials: true  // разрешил

241) Заголовки headers в fetch обычно ставятся автоматически браузером

242) Настройки, которые можно сделать в fetch запросе:
1) keepalive - запрос переживет страницу. Обычно нужно, чтобы отправить статистику:
window.onunload = function() {
  fetch('/analytics', {
    method: 'POST',
    body: "statistics",
    keepalive: true });};
Однако отправка ограничена 64 кб (более крупные можно отправлять регулярно заранее)

2) referrer и referrerPolicy - устанавливают правила для заголовка Referer - страница откуда идет запрос.
Например, если идет запрос с админки на другой сайт и мы не хотим светить структуру url админки
3) mode - разрешение делать запросы на другие источники, например если url для фетч приходит со стороны
4) credentials - разрешение отправлять куки на другие источники
5) cache - настройка использования HTTP-кеша (так понял брать данные из кеша если есть)
6) redirect - следовать редиректам или нет

243) Класс URL позволяет работать с url и гет параметрами:
- автоматически кодируя символы если необходимо
- разбирать адрес на url.protocol, url.host, url.pathname, url.search и др. 

Объект URL можно применять везде, где js ждет строку адреса (напр, в fetch)

Для изменения гет параметров используем объект url.searchParams
Пример: 
  let url = new URL('https://javascript.info')
  url.searchParams.append('key', 'новое значение') //русские символы и др. неразрешенные в url кодируются как %B2
  Методы: 
append(name, value) - добавить
delete(name) - удалить
get(name) - получить
set(name, value) – задать/заменить параметр

244) Также кодировать и декордировать url можно через encodeURI(весь адрес), decodeURI, encodeURIComponent(компонент адреса), decodeURIComponent.
     Но вообще, лучше не париться и использовать new URL и url.searchParams, например:
     new URL('https://google.com?вфыв=аыва&sad=222') дает правильный закодированный адрес, 
     посмотреть его можно через toString()

245) \n - переход на новую строку, спецсимвол, длина как у обычного символа 1
	В Windows также используется \r\n, но она понимает и просто \n
	В preg_match \R ищет заменяет любое из этих значений

246) '!'.codePointAt(0) - получение номера символа в Unicode (ищем нулевой символ в строке '!')

247) String.fromCodePoint(90) - получение символа по номеру в Unicode

248) При объявлении флекса блоки внутри меняют размеры в зависимости от содержимого, 
например, блоки с фото становятся меньше. Тогда для них нужно использовать flex-shrink: 0;

249) Между секциями верстальщик использует верхние и нижние пэддинги -  
делит расстояние между соседними пополам, а внутри секций двигает элементы марджинами и иногда пэдинги, 
если есть белые поля блоков с текстом

250) По правилам в секции обязательно д.б. заголовок

251) Изменение скролла в верстке:
div::-webkit-scrollbar-track - фон и ширина пути
div::-webkit-scrollbar-thumb - держатель(цвет, закругление)
div::-webkit-scrollbar-thumb:hover держатель при наведении

252) Как работают куки:
	1) При входе на сайт сервер через HTTP-заголовок Set-Cookie устанавливает куки, напр. идентификатор сессии
	2) При последующих запросах браузер всегда посылает на сервер HTTP-заголовок Cookie
	3) То есть куки - общие переменные небольшого объема между конкретным браузером пользователя и сервером сайта, 
	которые передаются при каждом HTTP запросе и могут устанавливать сервером. Хранятся на сервере и в браузере.

253) document.cookie - доступ к куки через js

254) Запись в куки: 
document.cookie = "user=John" - сессионные, удался при закрытии вкладки
document.cookie = "user=John; max-age=3600" - удалятся через 3600 секунд
Для правильного форматирования: document.cookie = encodeURIComponent(name) + '=' + encodeURIComponent(value);

255) Ограничения: пара name=value не больше 4кб (4000 символов ASCII, 2000 русс.), количество куки на 1 домен примерно 20

256) HTTPS: данные шифруются, промежуточные прокси просто пересылают шифры; HTTP - прокси видят данные

257) Атака CSRF (XSRF): 
Допустим пользователь залогинен на сайте, соотвественно у него в браузере лежат сессионные куки этого сайта.
Если сайт мошенник разместит у себя на странице автоматически отправляющуюся форму банка form.submit():
<form action="https://bank.com/pay">...</form>, то браузер при отправке прикрепит куки сайта банка
(то есть куки удостоверили, что форма отправлена с браузера пользователя), и банк выполнит операции в форме.

Пример: 
	<form action="https://bank.com/user/email" method="POST">
    		<input type="email" value="cined@mail.ru"> // изменили email пользователя на email злоумышленника
	</form>

	<script> document.forms[0].submit(); </script>

Как решается: сайт банка для сессии пользователя создает секретный ключ, на основе ключа и соли генерируется токен.
Ключ хранится на сервере в сессии пользователя, а токен и соль включаются в форму: 
<input type="hidden" name="csrf" value="1234:5ad02792a3285252e524ccadeeda3401">

Получив форму сервер заново генерирует КЛЮЧ сессии + пришедшая СОЛЬ и сравнивает с пришедшим токеном.
Если все совпадает, то форма создана сервером на сессии пользователя и ей можно доверять.

В ларавель защита реализована через токен CSRF (@csrf), создающийся для каждой сессии и хранящийся в сессии.

258) Второй способ защиты от атаки CSRF: установка samesite=lax
В этом случае куки отправятся если используется HTTP GET запрос (получение страницы) 
и изменяется строка url в браузере. Тогда, например, можно перейти на сайт банка из ссылки в заметках и банк узнает тебя.
Либо просто samesite=strict - тогда куки отправятся только если url браузера - сайт банка.

259) При установке куки (Set-Cookie) сервер может запретить читать эти куки из js через заголовок httpOnly.
Теперь если хакер внедрит свой js код тебе на сайт, то этот js не увидит httpOnly куки сервера.

260) Для работы с куки из js приходится разбивать строку куки по ; через регулярные выражения, декодировать и пр. 
Обычно для работы с куки через js пользуются библиотеками.

261) Сторонние куки: допустим на сайте грузится банер со стороннего сайта: <img src="https://ads.com/banner.png">
В этом случае браузер пользователя посылает GET запрос на этот банер на сайт ads.com.
Сервер ads.com может установить заголовок Set-Cookie с куки.
В следующий раз при новом запросе банера браузер отправит на ads.com сохраненные куки.
Таким образом, если этот банер <img src="https://ads.com/banner.png"> установлен на разных сайтах,
то ads.com будет видеть передвижение пользователя по сайтам: 
браузер каждый раз будет отправлять куки сайта ads.com при запросе банера - так собирается статистика 

Важно: сторонние куки могут блокироваться браузерами.
Однако если куки установлены через внешний скрипт, напр. <script src="https://google-analytics.com/analytics.js">
установкой document.cookie, то такие куки не являются сторонними – куки принадлежит домену текущей веб-страницы.

262) Законодательство GDPR - в Европе для отслеживающих и авторизующих куки следует получить согласие.
     1) Для автоматической авторизации пользователей: при регистрации должен быть установлен флаг  
     «принять политику конфиденциальности»
     2) Для отслеживания всех пользователей: при первом посещении модальное окно на использование куки

263) Куки - небольшой объем данных, передается браузером серверу при каждом запросе, могут устанавливаться сервером через заголовок Set-Cookie
     То есть куки больше нужны серверу для работы с браузером пользователя. 
     Он их устанавливает и браузер потом каждый раз передает их ему.
 
     sessionStorage и localStorage не отправляются при кажом запросе, отправляются только если это нужно скрипту.
     Хранятся в браузере.
     Объем данных там больше: sessionStorage - 5 мб, localStorage - от 5 до 10 мбайт.
	
     sessionStorage - уничтожается при закрытии вкладки (при обновлении сохраняется). 
                      Для каждой вкладки даже одного источника создается свой sessionStorage, 
                      к которому нет доступа с другой вкладки

     localStorage - сохраняется при закрытии вкладки и браузера. Один для одного источника даже на разных вкладках.
	            Данные не имеют срока давности.
		    Работать с данными может только сторона пользователя.
 
     sessionStorage и localStorage не могут устанавливаться сервером через HTTP заголовки.
     ВАЖНО: sessionStorage и $_SESSION в php это разные хранилища

264) Методы sessionStorage и localStorage:
	setItem(key, value) - сохранить пару ключ/значение.
    	getItem(key) - получить данные по ключу key
	removeItem(key) – удалить данные с ключом key
	clear() – удалить всё
	length - количество элементов

	Можно устанавливать и читать как localStorage.test = 2, но не рекомендуется

265) Один источник: домен, протокол, порт

266) continue - прерывание итерации и переход к следующей
     break - прерывание всего цикла	

267) В sessionStorage и localStorage можно хранить только строки, хотя браузер цифры и превратит в строки.
     Поэтому объекты нужно JSON.stringify, иначе будет операция Obj.toString()  => [object Object]	


268) sessionStorage и localStorage - объекты наподобие Map, можно перебирать циклом. Например:
     let keys = Object.keys(localStorage);
     for(let key of keys) {
     alert(`${key}: ${localStorage.getItem(key)}`);}
     	
269) Событие storage срабатывает для localStorage на других окнах window одного источника при изменении данных хранилища.
Свойства event: event.key - ключ, который обновился
		event.oldValue - старое значение
	  	event.newValue - новое значение
		event.url – url документа, где произошло обновление

270) У инпутов можно оставить только нижнюю границу:  outline: none; border: none; border-bottom: 1px solid blue;

271)  display: inline-flex - контейнер ведет себя как inline-block, 
	                     то есть по размеру содержимого и помещается на одной строке с другими элементами
      display: flex - контейнер ведет себя как блок и занимает все пространство по горизонтали, 
	              если не имзенить размеры
В обоих случаях дочерние элементы ведут себя как блочные.

272) Поля блоку со строчкой (напр. кнопка) можно задавать: 
	1) padding
	2) line-height (равный высоте блока)
	3) height + inline-flex + .jb + .vc;

273) css переход и css анимация:
	transition: 
	  1) мы описываем первое и последнее состояние, переход возможен от А до Б 
	  2) Запуск по изменению свойства пользователем: добавлен :hover, :focus и др., 
		    по изменению свойства через js (напр., через setTimeout изменен класс)
	  3) Можно сделать промежуточные этапы через cubic-bezier
 	  4) Зациклить можно: setInterval, либо привязаться к событию transitionEnd
	  5) Предопочтительнее для связки действий пользователя + css (например перемещение объекта в точку клика)
	  6) Свойства: transition-property, transition-duration, transition-timing-function, transition-delay - задержка начала перехода
	  7) Можно менять несколько свойств: transition: font-size 3s, color 2s;
          8) Нельзя анимировать свойства не имеющие промежуточных состояний: display: none, visibility: hidden, 
             auto (не вычислится)
	  9) Для плавности при hover туда и обратно transition вешается на начальное состояние. 
	     Иначе, если повесить перееход на второе состояние, то к нему движение будет плавное, 
             а как hover убран, то и транзишн у элемента пропадает вместе с состоянием
	  10) отрицательный transition-delay позволяет начать переход например, с середины
	  11) linear, ease, ease-in, ease-out и ease-in-out - это короткие записи кривой безье cubic-bezier()
	  12) Кривую безье можно редактировать в devtools по клику в стилях внутри transition 
	  13) steps позволяет разбить переход на любое количество шагов: steps(9, start). 
	      start и end означают, напр. начинать ли сразу по клику
              transition: transform 9s steps(9, start);
	  14) transitionEnd - событие окончания анимации. Можно что-то привязать, либо поменять анимацию и запустить заново.
	  15) cubic-bezier позволяет переходу выходить за границы начального и конечного состояния.
	  	  
	animation: 
	  1) Это тот же самый transition, но с большим количество переходов
	  2) Возможно множество промежуточных этапов keyframes:
		@keyframes name {
			0% {margin-left: 50px;}
			100% {margin-left: 150px;}}
		.move {animation: name 3s infinite alternate;}
	  3) Встроена возможность настраивать количество итераций, зацикленности
	  4) Автоматический запуск

   js анимации: 
	  1) Еще гибче, чем css анимации (которые ограничены кривой Безье), 
             но создают доп. нагрузку на процессор
	  2) Больше возможностей: создание новых элементов, взрывы и др.
	  3) Так как такие анимации затратны, то их следуется помещать в requestAnimationFrame,
             тогда браузер будет применять все js анимации в момент запланированных перерисовок страницы.
	
274) margin: 0 auto работает только если заданы размеры центрируемого элемента

275) transform: translateX(-90%); заданный в % считает 90 % от размеров самого элемента. 
Однако, если вложить дочерний див в родительский и ограничить родителя по ширине, 
то для расчета 90% будет браться только та ширина дочернего, которая поместилась в родителя,
остальное overflow даже не выделяется, если тыкнуть элемент в браузере.
Для того, чтобы overflow принималось во внимание нужно либо явно задать размеры дочернему, 
либо сделать его inline-block;

276) Размеры блочного элемента c незаданными размерами ограничены размерами родителя, 
     содержимое дочернего не позволяет выйти дочернему за пределы родителя, 
     вышедшее содержимое считается overflow

277) Обращать внимание на приоритет селекторов: класс не перебьет такие же свойства привязанные к id.
     Если сократить, то приортитет по убыванию: id (1.0.0), класс (0.1.0), тег (0.0.1) 
	

277) В css стилях можно повторять тот же класс и это увеличивает вес селектора:
	.q.q {
            background-color: blue; - будет этот
        }
        .w {
            background-color: brown;
        }

	<p class="q w">Lorem ipsum dolor sit amet.</p>

278) Движение elem.style.left кроме += можно сделать делением как elem.style.left = (Date.now() - startTime)/10 + "px"
     Например если setInterval эту функцию раз в 20мс, то left будет просто перезаписываться увеличивающейся цифрой

279) performance.now() - время прошедшее с момента начала загрузки страницы в миллисекундах

280) Кнопки бургер меню, модальные окна находятся на странице с display: none, 
     а при нужном размере экрана (клике на модалку) устанавливаются в display: block, flex

281) Положение во флексе настраивается ордерами, например даем бургеру order: -1; и он становится первым слева

282) В обычный style.css можно импортировать другие отдельные стили @import url('style2.css') или @import './style.css';

     Также можно импортировать скрипты в 1 файл:
     <script type="module" crossorigin src="/template/js/app.js"></script> - это в layout
     Это в app.js: import './script.js' и др.
   

283) Внутрь button можно вставлять дивы. Например для полосок бургера. Также видел вариант с псевдоэлементами.

284) Мобильное меню выводим на 100% в сторону:
	position: absolute;
  	left: -100%;
  	top: 0;
	opacity: 0;
	width: 100vw;
  	height: 100vh;
	transition: 0.3s all ease;
	z-index: 1;	
При клике по бургеру: left: 0; opacity: 1;
Однако для плавности и производительности правильнее исп translateX

285) Бэкграунд блоку можно задавать вставкой позиционированной картинки в блок, напр. если бэкграунд половина блока

286) Когда мы поднимаем блок на вышестоящий через отрицательный margin-top он налезает, 
     потому что идет в верстке следующим, у него выше z-index по умолчанию  

287) Обычная частота монитора 60 fps (60 кадров в секунду) // просто информация
     Частота анимации, видео, чтобы была плавность - 24-30 кадров в секунду.
     То есть менять объект для плавности нужно каждые 1000ms/30 кадров = 33ms
     Но может быть и несколько анимаций и все они будут заставлять перерисовывать 
     документ в разное время + браузер сам перерисовывает документ периодически.
     Чтобы совместить перерисовку js анимаций с перерисовками браузера существует 
     функция 
                          requestAnimationFrame, 
     делающая анимации гладкими и красивыми, синхронизированными с графическим процессором (GPU) и
     ставящая js анимации на паузу при неактивной вкладке

288) requestAnimationFrame(func) - запускает func при следующей перерисовке экрана. 
     Однако внутри func нужно опять запланировать себя в перерисовку: 
	function func(){
	Код js, например div.style.left += значение
	requestAnimationFrame(func)} // планируем себя же при перерисовке экрана

	requestAnimationFrame(func) // запланируем запуск func, аналогично как если func()

     Насколько понял, если requestAnimationFrame перезапускаем по какому-то условию, то cancelAnimationFrame
     делать не нужно. То есть cancelAnimationFrame нужна если функция сама себя не остановит, 
     так как у нее нет проверки на запуск.

Пример:
	let count = 0
	let id = requestAnimationFrame(function move(){ - можно так, а можно отдельно функцию move прописать 
	   if(count < 100){ - какое-то условие 
		elem.style.transform = `translate(${count}px, ${count}px)`; - двигаем блок по диагонали
		count++
		requestAnimationFrame(move) - ставим себя же опять в очередь
           } else {
		cancelAnimationFrame(id);}}) - убираем функцию из очереди. Насколько понял, так как есть условие
					       на перезапуск, то этот этап здесь не обязателен

Соответственно все js анимации, которые запланированы через requestAnimationFrame будут выполняться 
одновременно в момент перерисовки экрана.
Торможение js анимации запускающейся через setInterval(func, 30) - очень заметно 
в сравнении с requestAnimationFrame(func)

289) Функция, меняющая значение от нуля до заданного значения endPos за определенное время duration:
	div.style.left  = ((Date.now() - startTime) / duration * endPos  % endPos) + 'px';	

290) Постоянно растущее значение % 500 - всегда будет давать цифру от 0 до 500

291) Отключение requestAnimationFrame делается аналогично setInterval:
	let id = requestAnimationFrame(callback)
	cancelAnimationFrame(id);

292) func(a, b, ...rest) - остаточные параметры собираются в теле функции в массив rest
     let arr = [0, ...arr1, 2, ...arr2] - оператор расширения, извлекает элементы из массива

293) Array.from(obj) - создает массив как из итерируемых объектов, так и из псевдомассивов
     [...obj] - создает массив только из итерируемых объектов, напр. [...'привет']

294) Итерируемые объекты имеют метод Symbol.iterator, псевдомассивы имеют индексы и length

295) Object.keys(obj) - массив ключей
     Object.values(obj) – массив значений
     Object.entries(obj) - массив массивов [ключ, значение]
     Object.fromEntries([array]) - превращает массив в объект

296) Массив [] - тот же объект со строковыми ключами 0, 1, 2 и т.д. 

297) Map - коллекция ключ/значение, как и Object, но ключи могут быть любого типа.
Методы: new Map(), map.set(key, value), map.get(key), map.has(key), map.delete(key), map.size, map.clear()

298) Set - коллекция уникальных значений
Методы: new Set(arr), set.add(value), set.delete(value), set.has(value), set.clear(), set.size

299) Деструктуризирующее присваивание массивов: 
	     let[var1, var2] = ['Денис', 'Роднов']
либо удобно: let [firstName, surname] = "Ilya Kantor".split(' ');
Можно пропускать запятыми: let[var1, ,var2] = ['Денис', 'элемент' ,'Роднов']
Можно обменивать переменные:  let a=1, b=2;  [a, b] = [b,a]
Можно поместить в остаточные параметры: let [name1, name2, ...rest] = [1,2,3,4]
Значения по умолчанию: let [name = "Guest", surname = "Anonymous"] = ["Julius"];

300) Деструктуризирующее присваивание объектов: 
let {name, surname} = {name: 'Денис', surname: 'Роднов', job: 'backend'} // часто извлекают в модулях
let {name: newname, surname} = {name: 'Денис', surname: 'Роднов'} // поменяли название переменной
let {name = "Людвиг", surname} = {surname: 'Роднов'} // сделали значение по умолчанию
let {title, ...rest} = options; // поместили все остальные свойства в объект rest (rest.height//200)

301) Методы массивов: 
	push(...items) - добавление в конец
	pop() – извлекает элемент с конца
	shift() – извлекает элемент с начала
	unshift(...items) – добавляет элементы в начало
	splice(pos, deleteCount, ...items) - с позиции pos удаляет deleteCount число элементов и вставляет items
	slice(start, end) - создает новый массив копируя элементы от-до
	includes(value) - true, если элемент есть в массиве
	filter(func) - возвращает элементы для которых func вернула true
	find(func) - возвращает первый элемент для которого func вернула true
	
	forEach(func) - вызывает func для элементов, ничего не возвращает, просто перебирает
	map(func) - возвращает новый массив из результатов вызова func для элементов
	sort(func) – сортирует массив + возвращает его
	reverse() - переворачивает массив + возвращает его
	split/join – преобразует строку в массив и обратно
	reduce((acc, item)=>..., 0) - редуцирует массив в одно значение
	Array.isArray(arr) - проверяет, является ли arr массивом

302) Сделать весь блок кликабельным - поместить его внутрь ссылки

303) Текст в диве ведет себя как элемент флекса, если сделать див флексом. 
     То есть текст можно передвигать по диву через align-items: flex-end, align-items: center и др.
     Так, можно отцентрировать текст: align-items: center; justify-content: center;
     Текст при этом не нужно оборачивать в span или др. тег.

304) В фигме не показываются все инструменты? Значит режим View only, нужно Duplicate to your drafts
     Но в этом режиме нет возможности экспорта картинок, поэтому имей ввиду. Уточнение:
     проверь экспорт во вкладке Дизайн, внизу

305) Измерять расстояния в фигме: инструмент рамка

306) Если вставлять иконку через before, after, то лучше не в контент, а background-image, чтобы потом
менять размеры при адаптиве через background-size: contain;
Иначе иконка svg вставленная в content: url(путь) не реагировала на width, height для before
Также не забываем отключать повторение, так как оно по умолчанию: background-repeat: no-repeat;

307) Позиционирование резиновой картинки в диве:
     div {width: 100px; height: 50px;} //
     img {width: 100%; height: 100%; object-fit: cover;} // object-fit: contain - чтобы помещалась целиком без обрезки	
	
Картинку можно двигать через object-position: x y;
Если нужна картинка-банер от края до края, 
то не указываем в диве ширину, див будет шириной с wrapper.
Либо часто делают, что в мобилах картинка шириной с контейнер - 
- тоже не задана ширина родительсокму диву карнинки.

308) Исключение нескольких значений в css: 
div:not(:first-child):not(:nth-child(2)):not(:nth-child(3))
div:not(.class1):not(.class2):not(.class3)
input:not([hidden="true"]) - выбрать только видимые инпуты

309) body :not(p) - выберет в body все элементы не являющиеся абзацами
     .elem:not(.class1, .class2)  - внутри not можно перечислять селекторы

309) Если в мобиле элемент флекса начал занимать всю ширину,
делаем родителю flex-wrap и элементу width:100%;

310) Адаптивность делаю до, до, до. А не от-до, от-до. Иначе много повторяющегося кода

311) Контейнеру задаем ширину через max-width: ...px; 

312) Это вариант общего контейнера
   .wrapper {
	width: 90%; //  вместо левого и правого пэддинга
	max-width: 1170px; // среднее значение максимальной ширины контента
	margin-left: auto;
	margin-right: auto;}

313) Медиазапросы: 
	@media (min-width: 575.98px)
	@media (min-width: 767.98px)	
	@media (min-width: 991.98px)
	@media (min-width: 1199.98px)
	
	@media (max-width: 1400.98px)
	@media (max-width: 1200.98px)
	@media (max-width: 990.98px)
	@media (max-width: 820.98px)
	@media (max-width: 768.98px)
	@media (max-width: 576.98px)
	@media (max-width: 390.98px)

    Секрет таких пикселей в том, чтобы исключить неприменение стилей в слепой зоне, например, 
	@media (max-width: 320px) { стили для <= 320px }
	@media (min-width: 321px) { стили для >= 320px }
    Окно просмотра может быть дробных размеров за счет плотности пикселей или в результате масштабирования.
    Чтобы исключить эту слепую зону между 320 и 321px делают так.

313) @media print {} - медиазапрос срабатывающий при печати страницы, например:
		       @media print a::after {content: " (источник: " attr(href) ") "; }

313) Новый синтаксис медиазапросов:
	Было:
	   @media (min-width: 680px) and (max-width: 1280px)
	Стало:
	   @media (680px <= width >= 1280px)

314) В полезном лежат стили для секций высчитывающих боковой пэддинг автоматически, контейнеры не нужны

315) Большую таблицу или объект, который трудоемко адаптировать можно сделать прокручиваемым.
Родителю нужно задать overflow: scroll;

316) Лучше при адаптиве применять проценты для размеров, например 90% от контейнера

317) Отмена максимальной ширины, например для картинок по умолчанию в диве max-width: none;

318) Элементы списка не увеличат высоту самого списка, если он фиксирован по высоте.
     Поэтому в таких случаях высоту списка ставим auto

318) Создание пользовательского элемента:
	верстка: <my-el></my-el>
	
	class MyElement extends HTMLElement { // создаем класс элемента
	   connectedCallback(){ // эта функция вызывается в момент вставки элемента на страницу
		this.innerHTML = new Date()
		this.style.color = this.getAttribute('color') || undefined  }
	   disconnectedCallback(){} // вызывается при удалении элемента, например можно clearInterval(this.timer)}

	customElements.define('my-el', MyElement) // сообщаем браузеру, что тег my-el обслуживается классом MyElement
						  // обязательно тире в названии тега

319) Модифицирование встроенного элемента:
	верстка: <button is="hello-btn">Нажми</button> // в is пишем название модификации из define
	
	class Hello extends HTMLButtonElement {
    	   connectedCallback(){ // // эта функция вызывается в момент вставки элемента на страницу
      		this.addEventListener('click', function (e) {alert('Привет')})}}
	
	customElements.define('hello-btn', Hello, {extends: 'button'}); // указываем в extends 
                                                                        // для какого элемента создали модификуцию

320) В браузере есть предустановленные элементы, например <input type="range">
     Их стили записаны в теневом DOM (ShadowDom).
     Мы тоже можем создавать теневой DOM для элементов:

let shadow = elem.attachShadow({mode: 'open'}) // создаем для elem теневой DOM, в данном случае
shadow - это корень нового DOMa, аналогичный корню обычного DOMа -  <html>

Либо можно добавить через shadow.append, но тогда p должен создаваться через document.createElement("p")

321) Теневой дом применяется для вставки сторонних компонентов, виджетов, у которых свои стили 
и скрипты, не пересекающиеся с основным DOM, даже если одинаковые названия свойств.

ВАЖНО: Теневые стили, записанные в shadow не влияют на стили документа. Как уже говорилось, теневой дом
и обычный дом - две обособленнные вещи. Свойства теневого дома не доступны в обычном DOM.

shadow.innerHTML = `<style> p {background-color: red} </style> <p>Hello, John!</p>` // в теневом доме устанавливаем 
новое содержание elem - оно будет приоритетное и его не поменяешь через обычный DOM.

Либо: shadow.innerHTML = <style>p {background-color: #00d084;}</style> - эти стили работают только
для элементов теневого DOM

Можно также создавать в теневом доме css переменные и использовать их 
для оформления элементов теневого дома.

322) В теге <template> можно хранить верстку, стили, скрипты и пр. которые не применятся, не выполнятся на странице.
Этот тег как бы находится в состоянии dispay: none;
Однако в скрипте мы можем обратиться к потомкам этой ноды через свойство content 
и вставить их куда нужно. // как innerHTML не получится, так как там лежат ноды-объекты
	верстка: <template><style>body {background-color: #00d084;}</style></template>
	скрипт: document.querySelector('style').append(document.querySelector('template').content)

	верстка: <template id="template">Lorem ipsum</template>
	         <div id="div"></div>
	скрипт: div.append(template.content)

322) <template>
	<script>alert(1)</script> - этот скрипт не сработает
     </template>
	
323) Как мы знаем теневой дом заменяет light dom элемента. Но мы имеем возможность 
воздействовать через верстку (light dom) на новое содержание вставленное через теневой дом, делается
это через атрибут slot в верстке:
Верстка: <user-card><span slot="username">Иван Иванов</span></user-card>
Скрипт: 
customElements.define('user-card', class extends HTMLElement {
    connectedCallback(){
      let root = this.attachShadow({mode: 'open'})
      root.innerHTML = 'Имя <slot name="username"></slot>'
    }
  })

Перед размещением теневого дома браузер возьмет из light dom слоты и вставит их в теневой дом.
Атрибут ре="..." работает только на непосредственных детях элемента-хозяина теневого дерева 
(в нашем примере это элемент <user-card>) 

324) Виртуальный DOM - это js объект-копия обычного DOM. 
Так как перерисовка DOM это трудоемкая для браузера операция, то фреймворк (React и Vue) создает
объект js копию DOM, и меняет в нем свойства, а затем соспоставляет вносит изменения в обычный DOM
Порядок: 
1) Создается копия DOM - js объект ( виртуальный DOM) и js объект Diff для фиксирования изменений
2) Измененные свойства из Diff рендерятся в обычный DOM
Теперь браузер не переписывает весь DOM, а вносит изменения в конкретные части (компоненты)  

325) Как уже говорилось, смысл внедрения теневого дома - инкапсуляция независимых стилей, 
     элементов, скриптов. Внешние скрипты не видят элементы теневого дома.
     Так, если мы обрабатываем клики на документе, то клик на параграфе теневого дома всплывает 
     и будет обработан на ближайшем родителе основного DOMa(всплытие из теневого дома регулируется через composed: true)

326) При обновлении Ларавел может потребоваться обновить зависимости в composer.json
     После редактирования зависимостей пишем в терминале проекта: composer update.
     Может оказаться, что какой-нибудь пакет другой пакет не будет совместим с новой версией Ларавел,
     тогда либо удаляем пакет, либо проверяем на Гитхабе есть ли версия этого пакета 
     совместимая с нужной версией Ларавел. Если есть, редактируем версию зависимости и заново composer update

327) SQLITE - простая БД, без сервера. Установка с Ларавель:
	1) В .env удаляем все настройки DB_, оставляем только DB_CONNECTION=sqlite
	2) В папке database создаем файл database.sqlite
	   Теперь ларавель подключена к этой базе, можно даже сделать миграцию.
	   Чтобы удобно взаимодействовать с этим файлом делаем следующее.
	3) В phpStorm справа сверху вкладка Database, дальше нажимаем +, затем выбираем Data Source from path,
	   выбираем наш файлик database.sqlite, драйвер - SQLite, дальше проверяем соединение: Test Connection
	   В окне Database выбираем строчку SQLite, сверху кнопка обновления, появятся папки

326) Laravel UI – официальный пакет, предусматривающий генерацию страниц авторизации на основе CSS стилей фреймворка 
		  Bootstrap, Vue, React. Сейчас устарел, Ларавель советует Laravel Breeze или Laravel Jetstream.
     Установка: 
	1) composer require laravel/ui // в корне проекта Ларавель
	2) php artisan ui bootstrap
	3) php artisan ui:auth
	4) npm install && npm run dev
	5) если ошибки: npm run dev
	6) Теперь при заходе на главную страницу сайта сверху справа есть ссылки на вход и регистрацию

Laravel Jetstream - скелет приложения, включающий возможности: авторизация, регистрация, восстановление пароля, верификация почты, 
                    двухфакторная авторизация, управление паролем и пр.

327) Swiper слайдер косячит при установке кода js, бывает нужно жестко перезагружать страницу
     Готовый слайдер есть в практика/swiper слайдер

     Если внутри слайдов инпуты формы (напр. радио), то отключить бесконечную прокрутку loop, 
     так как при бесконечной прокрутке неверно навешивается checked часто

327) https://fotorama.io/ - простой слайдер


328) С фигмы svg можно копировать правой кнопкой: Copy as -> Copy as SVG


329) Рисунок на фоне:
background: #0dcaf0 url("img.png") center no-repeat;
background-size: cover;

330) Секции делить верхним и нижним пэддингом у каждой секции.

331) Выгрузка php переменных в js:
	<script>
	     let a = '<?= $var ?>' - обычный php
	     let b = {{ $product->id }} - в Ларавель блейд
	</script>

332) css фильтры на изображение img:
	filter: grayscale(100%) - черно-белое изображение
	filter: blur(5px) - размывает изображение, клевая штука
	filter: brightness(50%) - затемняет изображение до черного
	filter: contrast(50%) - контрастность, т.е. разница между светлыми и темными тонами
	filter: drop-shadow(0 0 15px) - аналогично box-shadow и text-shadow
	filter: hue-rotate(50deg) - меняет цвета по цветовому кругу от 0 до 360deg
	filter: invert(100%) - инвертирует цвета
	filter: opacity(50%) - прозрачность
	filter: saturate(50%) - насыщенность цветов, превращает в ч/б изображение
	filter: sepia(50%) - устаревание, ретро-фотографии
	filter: none - сброс фильтров, по умолчанию
	backdrop-filter - много фильтров применяемых для фона под элементом

111) backdrop-filter: blur(40px) - эффект мутного стекла, размытого изображения-подложки для фиксированной шапки
     background-color: #ffffff1a;
	
333) Адаптируемая картинка-фон на весь экран:
	background-image: url(...)
	background-size: cover;
        background-repeat: no-repeat;
        background-position: center;
        height: 100vh;

334) <a href="tel:+78121111111">(812) 123-45-67</a> // ссылка на телефон
     <a href="mailto:mail@htmlacademy.ru">Напишите нам</a> // ссылка на почту
     a[href^="mailto:"] - css выбор тега

     <a href="mailto:rodnovspb@mail.ru?subject=Отписаться&amp;body=Здравствуйте, отпишите от рассылки">Отписаться от рассылки</a>

335) transform: scaleX(-1) - отзеркалить изображение
     transform: rotateY(180deg) - повернуть как дверцу
     transform-origin - управляет вектором поворота
     backface-visibility: hidden - не видно заднюю грань элемента, когда он повёрнут к пользователю

336) Поворот с показом обратной стороны как у двери:
	<div class="block"> 
    	   <div class="block__inner">
        	<img class="block__front" src="2.jpg">
        	<div class="block__back">Текст</div>
    	   </div>
	</div>
    
     Стили:
	.block {perspective: 1000px; width: 500px;}
	 img {width: 100%;height: auto;}
	.block__inner {transition: all .8s;position: relative;transform-style: preserve-3d;}
	.block__front {backface-visibility: hidden;}
	.block:hover .block__inner {transform: rotateY(180deg);}
	.block__back {position: absolute;top: 0;left: 0;height: 100%;width: 100%;transform: rotateY(180deg);backface-visibility: hidden;background-color: #555;color: white;display: flex;align-items: center;justify-content: center;}

     Как работает:
	1) Общий родитель с perspective: 1000px;
	2) Сама дверь отн. позиционированная + transform-style: preserve-3d (дочерние элементы отображаются в 3D пространстве)
	3) В двери два дочерних элемента, второй дочерний делаем абсолютным и поднимаем посередине первого и 
           разворачиваем задней частью transform: rotateY(180deg); 
	4) У обоих элементов скрываем заднюю часть backface-visibility: hidden;
	5) При наведении на дверь проворачиваем ее transform: rotateY(180deg);
	6) Соответственно первый элемент разворачивается и скрывается, а второй появляется

337) Дрожащее изображение при наведении:
	При наведении вешаем анимацию, @keyframes разбиваем на много частей со двигами: 
		0% { transform: translate(1px, -1px) rotate(0deg)} и т.д.

338) Чтобы текст на фоне фотографии не размывался:
	text-shadow: 0 0 4px rgba(0,0,0, .25);
	text-shadow: 0 0 4px #fff;

     Также такой вариант для надписи:
	padding: 2px; - размер обводки
  	line-height: 1; - чтобы по вертикали и горизонтали одинаково было
	background-color: #fff;

339) beforeunload на window срабатывает при закрытии вкладки, записал данные в Localstorage, все хорошо сохранилось

111) <select name="select[]" id="select" multiple> - для селектов с множественным выбором делаем name как массив, иначе придет только одно значение

111) text-underline-offset - позволяет двигать линию подчеркивания ссылок и текста, напр.:
			     text-underline-offset: 4px;

111) text-underline-position: under; - положение линии подчеркивания снизу

111) text-decoration-skip-ink: none; - чтобы хвосты букв не прерывали подчеркивание

111) text-decoration-thickness: 2px - толщина подчеркивания

111) text-decoration-color: #f8fafc - цвет подчеркивания

111) text-indent: 20px - отступ первой строки

111) border-style: hidden - скрывает границу (обычно solid) // можно border-style:none

111) mix-blend-mode - как смешивается цвет элемента с низлежащими слоями

111) filter: drop-shadow(0 1px 1px rgb(0 0 0 / 0.05)) - наподобие box-shadow, только тень вокруг только непрозрачных элементов

111) accent-color: rgb(255, 255, 128) - устанавливает цвет для инпутов: chekbox, radio, range, progress

111) <progress max="100" value="70"> 70% </progress> - html тег процента выполнения чего-нибудь

111) appearance: none - сбрасывает стили для элементов форм установленных браузерами. Точно работает для select

111) <meta name="format-detection" content="telephone=no"> - чтобы дизайн номера телефона не изменялся браузерами

111) cursor: none - курсор исчезает
     cursor: no-drop - запрет красным перечеркнутым кругом
     cursor: zoom-in - лупа
     cursor: wait и cursor: progress - ожидание

111) pointer-events: none - курсор не воздействует на элементы (нет hover, click и др.)
                     можно также задать атрибут inert, тоже не будет реагировать

111) resize - дает возможность управлять размерами textarea, напр. resize: both, none

111) scroll-snap-align - где остановится элемент при прокрутке(пальцем тоже наверно)
     scroll-snap-align: center - по центру, слева и справа одинаковое расстояние до экрана 

111) user-select: none - текст в элементе не выделяется
     user-select: all - текст выделяется весь при одном клике, удобно для копирования

111) fill: #f8fafc - цвет svg элемента
     stroke: #f8fafc - цвет обводки svg
     stroke-width: 2 - толщина обводки svg

111) Кэш браузера является межстраничным, поэтому он работает для любой страницы, загруженной в браузер. 
Таким образом, вы можете предварительно кэшировать в одном месте на своем сайте, и кэш браузера 
будет работать для всех других страниц на вашем сайте.

111) <link rel="preload" as="script" href="critical.js" crossorigin> 
	preload - предварительная загрузка и кеширование скриптов, стилей, шрифтов. 
		  Например:
		    <link rel="preload" href="1.jpeg" as="image" crossorigin> // в head заранее даем команду на загрузку изображения в кеш	   
		    <img src="1.jpeg"> // в верстке грузим его уже из кеша. Без верхнего preload картинка начала бы грузиться только на этой строке

		  Файл скачивается и предоставляется как только понадобится. Также, можно заранее скачать шрифт, 
		  который запрашивается в стилях через @face:
			<link rel="preload" href="lora.woff2" as="font" crossorigin /> // грузим шрифт заранее
			<link rel="stylesheet" href="fonts.css"> - ниже устанавливаем его на страницу

		  Атрибут as обязателей, помогает браузеру определить приоритет загрузки, проверить наличие его в кеше.
 		  Допустимые значения для этого атрибута: script, style, font, image, audio, video
		  Хром работает.
       prefetch - файл загрузится в фоне, как только браузер освободится. Например, можно заранее подгружать файлы для
		  других страниц. Хром работает.
	
111)  <link rel= "preconnect" href="https://app.com" />
	preconnect - установка соединения с сервером для последующего запроса каких-либо файлов.
		     Порядок обычного соединения:
			1) Резолвинг DNS: ищется IP-адрес сервера (216.58.215.78) для адреса (google.com)
			   DNS (Domain Name System) сопоставляют буквенные адреса с IP-адресом сервера (216.58.215.78)	
			2) Рукопожатие TCP: Обмен пакетами (клиент → сервер → клиент) для установки TCP-соединение с сервером
			3) Рукопожатие TLS (для HTTPS): Два обмена пакетами (клиент → сервер → клиент → сервер → клиент), 
			   чтобы инициировать безопасный сеанс TLS.
		     Вот preconnect и выполняет такие операции заранее, чтобы потом запросить файл без них.

111) <link rel="prerender" href="https://my-site.ru/pricing" />
	prerender - страница загружается и хранится на как бы в фоновой вкладке браузера. Браузер загрузит все ресурсы, 
		    построит DOM, применит CSS и JS. А когда пользователь перейдет по ссылке, скрытая страница станет 
		    вместо текущей и загрузится моментально. Однако поддержка caniuse 76%.
		    Хром непонятно работает, нет.

111) Итоги. Когда использовать:
  <link rel= "preload"> — когда вам понадобится ресурс ниже в коде
  <link rel= "prefetch"> — когда понадобится ресурс на следующей странице
  <link rel= "preconnect"> — когда вы знаете, что вам скоро понадобится ресурс, но вы ещё не знаете его полный URL
  <link rel= "prerender"> — когда уверены, что пользователи перейдут на опр. страницу, и хотите ускорить её отображение

  Все эти элементы можно генерировать через js по мере необходимости:
	let el = document.createElement("link");
	el.setAttribute("rel", 'prerender');
    	el.setAttribute("href", url);
    	document.getElementsByTagName("head")[0].appendChild(el);

111) На разных страницах можно использовать одинаковый id, желательно, чтобы они были привязаны к элементам, 
     представляющим одинаковые данные

111) Кнопкам внутри форм, если они не должны отправлять форму устанавливаем type="button", иначе они отправят форму

111) Чтобы все картинки слайдера грузились сразу нужно просто навесить всем display: none и отменять для нужных

111) Темная и светлая темы для сайта:
	:root { color-scheme: light; } // или dark
	body {
    		color: light-dark(black, white); - будет само переключаться на light или dark тему
    		background-color: light-dark(white, black);
	     }

111) js функция разбиения массива на подмассивы:
	function unflat(src, count) {
    	   const result = [];
    	   for (let s = 0, e = count; s < src.length; s += count, e += count)
       		 result.push(src.slice(s, e));
    		 return result;}

	console.log(unflat([1,2,3,4,5,6,7,8,9,0], 3));

     Эта функция полезна для слайдера:
	1) Находим все нужные элементы
	2) Разбиваем их на подмассивы по нужному количеству на экране
	3) При клике на боковую стрелку слайдера всем элементам даем display:none, 
	   а у элементов нужного подмассива убираем его

111) Фильтрация на Ларавель:

	В верстке при выборе пункта через js перенаправляемся на метод контроллера + добавляется get параметр  filter
	<select name="select_othergoods_page" onchange="location = this.value;">
                  <option value="{{ route('othergoods.index') }}" disabled selected>Выбрать страницы</option>
                  <option value="{{ route('othergoods.index') }}">Все страницы</option>
                @foreach($pages as $page)
                  <option value="{{ route('othergoods.index', ['filter' => $page->uri]) }}" @if($selectedPage && $selectedPage->uri == $page->uri) selected @endif>{{ $page->meta_title }}</option>
                @endforeach
        </select>

	Или просто стрелочками из юникода:
	<a href="{{ route('othergoods.index', ['sort' => 'order']) }}">⇅</a>

	В контроллере:
	public function index(Request $request){
		  if($request->filter){ получаем сущности в соответствии с фильром } // если есть гет параметр filter
		  else { получаем все сущности }
		return  view('...', compact( сущности))

	Можно при сохранении, обновлении данных попадать обратно на ту же страницу фильтра сущностей, если пробрасывать
	гет параметр:
	 1) в кнопке перехода в контроллер:
	    <a href="{{ route('othergoods.edit', ['othergood' => $good->id, 'filter' => request()->filter ]) }}">Редактировать</a>
	    
	 2) в самом контроллере после обновления сущности (перед обновлением unset($data['filter']);):	
	    return redirect()->route('othergoods.index', request()->query()); // прикрепим тот же массив
	 
111) Для того, чтобы после редактирования/создания записи перенаправлялись на тот же номер страницы (при пагинации) + на тот
     же фильтр мы должны дополнительно прикрепить 'page':
	1) В ссылках перехода (или в action формы) прописать вручную нужные гет параметры:
	     form action="{{ route('goods.update', ['othergood' => $good->id, 'page' => request()->page, 'filter' => request()->filter]) }}"
	2) Удалить из массива $request ненужные параметры, иначе при update будет ошибка:
	    $data = $request->all();
	    unset($data['filter']);
      	    unset($data['page']);

	2) Теперь контроллер может получить эти гет параметры и сделать редирект на ту же страницу откуда пришли.
	   Сработает и прошлый фильтр, напр: Показать Печати ИП + будет та же страница пагинации:
	     return redirect()->route('othergoods.index', request()->query());

111) Фильтрация по возрастанию-убыванию через стрелочки:
	В контроллере проверям был ли передан порядок сортировки гет параметром при переходе по ссылке ?sort=price&ord=asc:
	   $ord = (empty($request->ord) || $request->ord == 'desc' || $request->ord != 'asc') ? 'desc' : 'asc';
	Если не был передан или некорректное значение, то установим как desc, если был равен как asc, то запишем asc.
	Выведем продукты в соответствии с условиями сортировки:

	   $products = Product::query()->orderBy('price', $ord)->simplePaginate(10)->withQueryString();	
	
	В конце контроллера в представление передадим инвертированный порядок:
	   $ord = $ord == 'desc' ? 'asc' : 'desc';

 	Чтобы новое нажатие по ссылке изменило сортировку:
	   <a href="{{ route('products.index', ['sort' => 'price', 'ord' => $ord]) }}">⇅</a>

111) Тернарные операторы ?? можно записывать цепочкой:
	$elem = $arr['test1'] ?? $arr['test2'] ?? 'empty';

     Их удобно ставить в Ларавель верстку:
	<input name="price" value="{{ old('price') ?? $product->price }}"> // либо короче: old('price', $product->price)
     Это код для выбора тех же страниц select multiple, если была ошибка валидации.
     Где сначала проверяется был ли старый массив в old, если нет, то используем массив $selectedPages:
	$selectedPages = $product->pages()->pluck('page_id')->toArray(); // изначальный массив выбр. страниц, получаем в контроллере

	<option value="{{ $page->id }}" @if(in_array($page->id, old('pages_for_products') ?? $selectedPages)) selected @endif>{{ $page->title}}</option>

111) Тернарные операторы If цепочкой:
	$item->name ?: $item->other ?: null

111) Порядок и цены можно задавать с возможностью десятичных частей:
	$table->double('order',8,2)->unsigned()->nullable(); - миграция  //double не выводит в верстку нули, если число целое, т.е. не будет 11,00
	'order' => 'numeric|nullable' - валидация
	<input type="number" step="any" name="order" /> - верстка. Теперь пользователю не нужно будет перестраивать все,
							  чтобы поместить новый макет между существующими
						          max - макс. значение (для double(8,2) делаем max:999999
111) Убрать selected у циклом у всех option:
	forEach(elem => elem.selected = false)
     elem.removeAttribute('selected') почему-то не работает

111) try {} catch(e){} в js нужны чтобы в случае ошибки дальнейший код продолжил работу. Иначе скрипт просто свалится
     при первой ошибке

111) Чтобы при изменении макетов в заказе всегда сохранялся изначальный макет:
     1) Привязываем value инпута к id записи продукта из таблицы mysql, а удаляем макеты только через softDelete
     2) Теперь даже если заменим макет в админке используем показ заказа вместе с макетами withTrashed:
	
111) ;(function (){ ... })() - скрипты можно оборачивать в самозапускающуюся функцию, чтобы переменные не попадали
			      в глобальную область видимости и не переписывали переменные из др. файлов скриптов

     Когда обработчик JS видит слово function, как вводное слово, он ожидает, что сейчас будет объявлена функция.
     Если поставить знак перед function, то обработчик будет рассматривать function как выражение. 

     Несколько вариантов записи самозапускающихся функций:
	1) ;(function (){ ... })()
	2) !function() { ... })() // вместо ! можно поставить любой унарный оператор (+, -, ~ и др.)
	3) void function() { ... })()	
	4) (function() { ... }())

     Еще одним преимуществом кроме скрытия переменных от ГОВ является возможность записывать результат самозапускающейся функции в
     переменную:
	let a = (function(){ return 111 }())
	let a = function(){ return 111 }() - можно без скобок, так как есть = и js понимает, что будет выражение

     Также в самозапускающуюся функцию можно передавать параметры:
	(function(a, b, c) { ... }(10, 20, 30))
	
111) Запрет перетаскивания картинок:
	1) <img src="" draggable="false" />
	2) pointer-events: none;
     	3) user-select: none; - дополнительно, чтобы не было темного квадрата у картинки при выделении

111) Не ошибаться!
	if(...){...}; - всегда проверится
	if(...){...}; - всегда проверится
	if(...){...}; - всегда проверится

     Это НЕ то же самое, что:
	if(...){...} - всегда проверится
	elseif(...){...} - проверится если не выполнится верхнее условие
	elseif(...){...} - проверится если не выполнится ни одно верхнее условие
	else {...} - а это сработает если ни одно верхнее условие не выполнилось

    Это одинаково работает для php, js и blade @if() @elseif()

111) Отключение атрибутов через js:
	elem.required = false
	option.selected = false

111) Увеличения высоты textarea, если знаков станет больше:
	if(elem.value.length > 45) { elem.rows = 2 }

111) Простое решение для показа товаров вместо слайдера:
  	display: flex;
	flex-wrap: wrap;
	height: 300px;
	overflow: auto;

111) Лучше сразу для изображений прописывать абсолютные ссылки через secure_asset
     А в APP_URL установить https:.... - через него будут работать именованные маршруты

111) Перемещать элемент лучше через CSS-трансформации translateX и translateY, а не через left и top с position.
	Преимущества translateX и translateY:
	   1) Не перестраивается DOM
	   2) Если есть ускорение на карте, то используется
	   3) Поэтому анимация более плавная
	   4) Сразу можно использовать другие css трансформации, например scale	

111) Улет товара в корзину:
	1) let cart = document.querySelector('#cart') // корзина
	   let good = document.querySelector('#good') // товар
	2) По клику находим координаты:
	   let cartPos = cart.getBoundingClientRect()
	   let goodPos = good.getBoundingClientRect()
	3) Клонируем товар и помещаем над оригинальным:
	   let cloneGood = good.cloneNode(true)
	   cloneGood.style.position = 'fixed'
           cloneGood.style.left = goodPos['left'] + 'px'
           cloneGood.style.top = goodPos['top'] + 'px'
           cloneGood.style.border="none";
           cloneGood.style.zIndex=99;
	4) Находим координаты центра товара и корзины:
	   let x_centerCart = cartPos.left + 0.5 * cartPos.width
           let y_centerCart = cartPos.top + 0.5 * cartPos.height
	   let x_centerGood = goodPos.left + 0.5 * cartPos.width
           let y_centerGood = goodPos.top + 0.5 * cartPos.height
	5) Рассчитываем на какое расстояние сместить товар чтобы был в центре корзины:
	   let delta_x = x_centerCart - x_centerGood
	   let delta_y = y_centerCart - y_centerGood
	6) Добавляем товар и перемещаем:
	   document.body.appendChild(cloneGood)
	   void cloneGood.offsetWidth;
	   cloneGood.style.transform = `translate(${delta_x}px, ${delta_y}px)`
           cloneGood.style.transform += `scale(1.2)`
	   cloneGood.style.transition = `0.5s`
	7) Удаляем перемещенный клон товара:
	   setTimeout(()=>document.body.removeChild(cloneGood),500);

111) Сохранение переменной в условии if в js:
	let contact
    	if(contact = localStorage.getItem('contact')){ используем contact }	
     Присваивание всегда возвращает назначенное значение, поэтому работает 

111) Удобно посмотреть через js данные формы:
	let data = new FormData(document.querySelector('.form'))
	console.log([...data])    

111) Атрибуты input:
	max - максимальное число, для double(8,2) ставим max="999999"
	maxlength - максимальное количество символов в  text, email, search, password, tel, или url
		    для varchar ставим 	maxlength="255"

111) Внутри тегов можно писать обработчики событий с js кодом:
	<input type="number" onKeyPress="if(this.value.length === 3) return false"> - не даст ввести 4 значное число
     Хотя для ограничения длины текста есть готовый атрибут maxlength, но он не работает для input type="number"

111) css тег input:user-invalid - сработает, для неверного заполнения поля при наличии max, min и пр.
                  input:in-range и :out-of-range сработают если значение в пределах min-max или вне его, напр., для type="number"

111) Перенос строки внутри textarea:
	textarea.value = `${...}\r\n${...}`

111) Вставка нескольких прикрепленных файлов в БД через Ларавель и транзакцию:
	
	public function store(Request $request){
		$data = $request->all();
		unset($data['files']); // удаляем, так как основная таблица не ждет поле files
		$arr = []; // создаем здесь, чтобы не было ошибки если файлы не прикрепляли к заказу
		if($request->hasFile('files')){ // если есть файлы
           	   $folder = date('d-m-Y'); // папка для файлов на каждый день
          	   foreach ($request->file('files') as $file){ // $request->file() - метод, а files - имя инпута (files[])
            	   if($file->isValid()){ // если файл загружен без ошибок
              		$subArr = []; // субмассив нужен чтобы в БД вставить все записи за один раз, а не циклом перебирать
              		$subArr['originalName'] = $file->getClientOriginalName();
              		$subArr['path'] = $file->store("/files/{$folder}");
              		$arr[] = $subArr;}}} получили массив массивов:	
		 		[["originalName" => "бот.txt""path" => "files/21-03-2023/TlCJZyzhLTYsTe.txt"],
				 ["originalName" => "док.txt""path" => "files/21-03-2023/TlCJdasDLTYsTe.txt"]]

		
		DB::transaction(function() use ($data, $arr){ // транзакция, так как вставляем связанные записи
          	   $order = Order::create($data);  // вставляем сам заказ
          	   if(count($arr) > 0){ // если к заказу были прикреплены файлы
              	       for($i = 0; $i < count($arr); $i++){ // перебираем массив массивово
                           $arr[$i]['order_id'] = $order->id;} // дописываем номер заказа
            	   File::query()->insert($arr);}}, 2); // вставляем за один раз неск записей в модель File, 2 попытки отрабатывания транзакции
        	return redirect()->back();}

111) В Ларавель можно вставлять одновременно несколько записей, но они должны быть в подмассивах:
	People::insert([['name' => 'Вася', 'salary' => 300], ['name' => 'Миша', 'salary' => 500]])

111) Сменить часовой пояс в Ларавель в config/app.php:
	'timezone' => 'Europe/Moscow'
     Теперь в БД сохраняется верное время

111) Удобное форматирование даты:
	{{ $order->created_at->format('d-m-Y H:i:s') }}

111) $startMemory = memory_get_usage(); - объем памяти в байтах выделенный для php скрипта на этот момент
     $startTime = microtime(true) - время timestamp в микросекундах

     $maxMemory = memory_get_peak_usage() - пиковое значение объёма памяти, выделенное PHP

111) В Ларавель удобная функция для проверки времени выполнения:
	Benchmark::dd(fn () => User::count(), iterations: 10); // 0.5 ms

111) Код для проверки времени выполнения скрипта и выделенной на него памяти:
	$startTime = microtime(true); - текущее время
	$startMemory = memory_get_usage(); - выделенная память на начало работы скрипта
	for ($i=0; $i < 1000; $i++) { ... } - какая-нибудь работа скрипта
	$time = microtime(true) - $startTime; - затраченное время
	$memory = memory_get_usage() - $startMemory; - затраченная память

	file_put_contents(__DIR__ . '/time.txt', $time . PHP_EOL, FILE_APPEND);
	file_put_contents(__DIR__ . '/memory.txt', $memory . PHP_EOL, FILE_APPEND);
	file_put_contents(__DIR__ . '/peak.txt', memory_get_peak_usage() . PHP_EOL, FILE_APPEND); - запишем также пиковое значение выделенной памяти

111) str_starts_with('где ищем', 'что ищем') - начинается ли
     str_ends_with('где ищем', 'что ищем') - начинается заканчивается ли

111) Абсолютные и относительные пути
     <a href="/dir/sub/page.html">text</a> - путь относительно домена (абсолютный)
     <a href="dir/page.html">text</a> - путь относительно текущей страницы (относительный)

111) Простейшая работа с БД:
	mb_internal_encoding('UTF-8');
	error_reporting(E_ALL);
	ini_set('display_errors', 'on');
	$host = "localhost";
	$user = 'root';
	$pass = '';
	$name = 'mydb';
	$link = mysqli_connect($host, $user, $pass, $name);
	mysqli_query($link, "SET NAMES 'utf8'");

	$query = "SELECT id, surname, deleted FROM employees";
	$res = mysqli_query($link, $query) or die(mysqli_error($link));
	for($data=[]; $row = mysqli_fetch_assoc($res); $data[] = $row);

111) Чтобы не было ошибок берем в одинаковые кавычки вставляемые переменные:
	$query = "INSERT INTO pages SET title='$title', text='$text'";

111) ini_get(...) - получение настройки в php.ini
     ini_set(...) - установка настройки

     $val = ini_get('allow_url_fopen') - проверка значения
     ini_set('allow_url_fopen', true) - установка в true

111) Отправка форм.
	Когда форма отправляется, то осуществляется запрос страницы из action формы (переход на нее).
	Если отправляем методом get, то данные прописываются в гет параметрах ссылки, post - в теле запроса.
	В обоих случаях происходит переход на страницу из action. Просто можно отправлять их на ту же страницу, 
	тогда она просто перезагрузится, а ее php код обработает параметры, либо на сторонний адрес, тогда
	уже на нем нужно делать редирект обратно на форму (в Ларавель: return redirect()->back()) или др. переходы. 
	
111) И GET и POST - по сути один и тот же запрос и получение страницы, просто отличаются тем как передаются данные 
     на запрашиваемую страницу.

111) Можно получить много информации о запросе-ответе: 
        инструменты разработчика -> сеть -> правой кнопкой на сайт -> копировать:
		- заголовки запроса и ответа // копируем как фетч, там список заголовков
		- сам ответ (верстка), то же самое во вкладке 'Ответ'
		- копировать как fetch (получится готовый запрос fetch c заголовками, body, методом и пр)
		- копировать как cURL

111) http_build_query() - php функция, превращает массив в query строку для вставки в гет параметры
	$get = ['name'  => 'Alex', 'email' => 'mail@example.com'];
	http_build_query($get) => name=Alex&email=mail%40example.com

     Не забываем про символ ?: 	https://www.youtube.com/?' . http_build_query($get)

111) htmlspecialchars() - преобразует специальные символы HTML в сущности, чтобы их можно было без проблем вывести в верстку:
			  htmlspecialchars("<b> a </b>") => "&lt;b&lt; a &lt;/b&gr;" 
     mysqli_real_escape_string() - экранирует спецсимволы, чтобы можно было безопасно сделать запрос в БД, 
				   то есть добавляет \ перед косой чертой, кавычками (как одинарными, так и двойными) 
				   и всем остальным, что может испортить запрос mysql
				   ' => \'
     Пример использования mysqli_real_escape_string():
	$name = "Papa's grill";
	$name = mysqli_escape_string($link, $name) => "Papa\'s grill" // $link - из настроек соединения, нужно для опр. кодировки
	$query = "INSERT INTO orgs SET name = '$name'"

     Но вообще лучше использовать Eloquent или RedBeanPHP, там все встроено.

111) Автозагрузка классов с помощью Composer:
	Описана автозагрузка для уже существующего composer.json и папки vendor (то есть уже устанавливали какой-нибудь пакет):
	В composer.json:
{
    "require": {
        "illuminate/database": "^9.52" - какая-нибудь библиотека
    },
    "autoload": {
        "classmap": [ - применяется для загрузки классов именование которых не соотв. PSR-4
            "classes/", - будут загружать все классы из папки classes
	    "services/myserv/Ksl.php" - можно так, тогда загрузится этот файл
        ],
	"psr-4" : { - это уже загрузка для стандарта PSR-4
            "Models\\": "app/models/" - где Models - сокращение для пути app/models
		},
	"files": [  - так можно подключать файлы, вместо того, чтобы прописывать в коде require_once 'config.php';
    	    "config.php"]}}

Выполняем команду:
	 composer dump-autoload -o   // обновит файл автозагрузчика классов Composer 

В «точке входа» в файле index.php д.б. для того, чтобы автозагрузка работала подключаем:
	require_once 'vendor/autoload.php'

Для подключения классов из папки classes:
	Теперь в файле index.php пользуемся классом из папки classes, подключенным в classmap:
	$user = new User();

Для подключения классов psr-4:
	Например лежит класс Product в app/models .
	Сверху в нем самом должно быть: namespace Models;
	А там где подключаем этот класс д.б.: use Models\Product;
	

111) Установка Eloquent без Ларавель:
	1) composer require illuminate/database - устанавливаем Eloquent
	2) Создаем файл с настройками подключения bootstrap.php:
		require "vendor/autoload.php"; - то есть в файле index.php его уже не надо подключать
		use Illuminate\Database\Capsule\Manager as Capsule;
		$capsule = new Capsule;
		$capsule->addConnection([
    		  "driver" => "mysql",
    		  "host" =>"localhost",
    		  "database" => "smolensk", - название БД
    		  "username" => "root",
    		  "password" => ""]);
		$capsule->setAsGlobal();
		$capsule->bootEloquent();
	3) В composer.json прописываем автозагрузку папки:
		"autoload": { "classmap": ["classes/"]}
	5) В папке classes создаем модель для таблицы products в БД:
		use Illuminate\Database\Eloquent\Model as Eloquent;
		class Product extends Eloquent {		
    			protected $guarded = [];
    			public $timestamps = false;} - отключаем вставку дат 
	4) Обновил автозагрузочный файл классов:
		composer dump-autoload -o   // ВАЖНО!!! Это нужно делать после каждого добавления класса
	6) В файле index.php пользуемся:
		require 'bootstrap.php';
		$product = Product::create(['name' => 'Помидоры']);

111) Были проблемы с вставкой кода эмодзи в mysql при колировке utf8_general_ci.
     Пишут, что для новых приложений лучше использовать utf8mb4_0900_ai_ci или utf8mb4_general_ci, где:
	utf8 - кодировка
	mb4 - сколько байт используется для одного символа (4 байта). Если не указано, то mb3(3байта), отсюда, наверно проблемы с эмодзи
	0900 - версия алгоритма сопоставления Unicode
	ai - диакритическая нечувствительность (например, древнегреческие ᾱ, ᾰ)
	ci - нечувствительность к регистру (без разницы прописные или строчные), если cs, то чувствительность к регистру

111) DIRECTORY_SEPARATOR - константа правильного слеша
	$path = __DIR__ . DIRECTORY_SEPARATOR . 'cookie.txt';

111) __DIR__ - иногда ошибки, если программа ожидает латинские названия папок, а в __DIR__  русские названия папок,
	       например, cURL не будет писать куки в файлы с такими путями, а без __DIR__ пишет

111) Решение капчи через https://rucaptcha.com/:
	1) composer require 2captcha/2captcha
	2) require_once '..vendor/autoload.php' - грузим композерский автозагрузчик
	3) $solver = new \TwoCaptcha\TwoCaptcha('YOUR_API_KEY') - прописываем ключ с rucaptcha.com
	4) Сначала, так понял, нужно получить саму страницу с формой и капчой, чтобы был известны данные капчи:
	   data-sitekey для reCaptcha, картинки и т.п., отправляем это на разгадывание, ждем, получаем ответ, 
	   вставляем в форму и отправляем.
	5) $result = $solver->recaptcha( - это метод для капчи "Я не робот"
		[
    		'sitekey' => здесь прописываем data-sitekey элемента #dynamic_recaptcha_1,
    		'url'     => здесь url страницы на которой капча 
		])->code;
	6) Метод sleep() встроен в верхний метод, как только result появится код продолжится
	7) Дальше по замыслу вставляем полученный код в #g-recaptcha-response и курлом отправляем форму.
	   Но ничего не работало, потому что не находился #g-recaptcha-response, он генерировался динамически

111) Вход на сайт:
	$curl = curl_init('https://xdan.ru/login?task=user.login'); - ссылка из action формы
	$post = ['username'=>'cined', 'password'=>'111111'];
	curl_setopt($curl, CURLOPT_COOKIEFILE, 'cookie.txt');
	curl_setopt($curl, CURLOPT_COOKIEJAR,  'cookie.txt');
	curl_setopt($curl, CURLOPT_POST, 1);
	curl_setopt($curl, CURLOPT_POSTFIELDS, http_build_query($post));
	curl_exec($curl);

111) Отслеживание запроса и эмулирование его:
	1) Заходим на сайт и открываем инстр. разр.
	2) Нажимаем на перечеркнутый круг слева (удобно, так как стирает все и видно только запросы после действия)
	3) Включаем Preserve log и отключаем кеш
	4) Выполняем действия на сайте ручками
	5) Выгружаем лог и смотрим что делалось браузером

111) Для того, чтобы сэмулировать работу браузера нужно передать те же заголовки и включить куки в курле.
     Основные заголовки:
	1) Accept - в каком формате ждем данные (JSON, HTML, XML и т.д.)
	2) Content-Type - в каком формате отправляем данные (application/x-www-form-urlencoded)
	3) Referer - URL-адрес, с которого мы отправляем запрос, сам сайт-жертва (webparse.ru)
	4) X-Requested-With - говорим, что отправляем AJAX, например сайт может ожидать значение XMLHttpRequest  
	5) User-Agent - информация о браузере, передавать обязательно
     Итого заголовки (писать также через двоеточие): 
	$headers = [
	'Accept: application/json, text/javascript, */*; q=0.01',
    	'Referer: https://webparse.ru/',
    	'User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/77.0.3865.90 Safari/537.36',
    	'X-Requested-With: XMLHttpRequest',
	'cookie: ... ' ]	

    Ответ сервера на запрос смотрим во вкладке Preview, в соотв. с ответом сервера можно писать условия if на ответ сервера

111) Сайт javascript.ru не хотел показывать страницу регистрации без установленных предварительно кук в $headers.
     Так понял, что установились на странице проверки браузера.

111) Сохранение изображения из url двумя способами:
	1) file_put_contents('1.jpg', file_get_contents($url));
	2) copy($url, 'captcha.jpg');

111) Как работает fetch в браузере:
	1) перед запросом (допустим, POST), браузер делает предзапрос (preflight) на сервер в виде запроса 
	   методом OPTIONS с указанием origin (домен запрашивающего скрипта)
	2) сервер смотрит на origin  и решает, обрабатывать Ваш запрос, или нет.
	3) если он согласен, то отправляет ответ со статусом 200 + заголовок Access-Control-Allow-Origin:
           * - разрешены ответы всем сайтам, либо с перечнем разрешенных сайтов
	4) браузер проверяет, что пришедший Access-Control-Allow-Origin разрешает доступ домену скрипта 
           и только после этого оправляет Ваш главный запрос,  который Вы указали в Вашем fetch

111) cURL - это тот же браузер в скрипте php, в котором можно обмануть сервер установив любые заголовки и куки
            Поэтому можно выполнять любые запросы

111) Шифрование данных в js:
	btoa('123') => MTIz - зашифровали
	atob('MTIz') => 123 - расшифровали

     Как это работает:
	 1) строка кодируется в последовательность бит	
	 2) Биты преобразуются в в ACSII строку по алгоритму base-64 (64 символа A-Z, a-z, 0-9, +, /, =)

     https://obfuscator.io/ - обсфуцирование данных

111) Lumen - микрофреймворк на основе облегченной Ларавель, созданный для поддержки микросервисов

111) laravel-breadcrumbs - пакет для хлебных крошек. 
                           Описание на русском https://govno-coder.ru/post-item/breadcrumbs-xlebnye-kroski-dlia-saita-na-laravel
			   Сохранил также в папке Полезное
                             
111) laracasts/flash - пакет для флеш сообщений

111) Ларавель самостоятельно переводит массивы в json если из контроллера возвращать http вывод return [];

111) Вывод вьюхи через фасад: return View::make('index')->with('success') // + добавили флеш сообщение

111) Функции помощники (view(), with() и пр.) подключаются в файлах helpers.php
     Эти файлы могут быть в разных пакетах в папке vendor/laravel/framework/src/Illuminate
     При установке пакета эти файлы могут подгружаться в композере в строке files

111) Можно писать в моделях phpdoc, чтобы свойства в шторме находились:
	/**
	 * @property int $id
	 * @property string $name
	*/
	class User { ... }

111) Вместо массового создания через $request->all() можно использовать метод only:
	User::create($request->only(['name', 'email']))

111) Обход поля fillable модели:
	User::forceCreate([...]) - создает
	User::forceFill([...]) - заполняет выше созданную(или полученную из БД) запись

111) laravelcollective - пакет для генерирования элементов форм:
	{!! Form::open(['route' => 'route_name']) !!} ... {!! Form::close() !!} - форма, авт. вставилось поле @csrf
	{!! Form::open(['url' => 'foo/bar', 'method' => 'put']) !!} ... {!! Form::close() !!} - авт. вставились @csrf и @method
            Form::text('username') - текстовый инпут
	    Form::radio('name', 'value') - радио переключатель
	    Form::select('size', ['L' => 'Large', 'S' => 'Small']) - выпадающий список (не нужен foreach) и т.д.

111) Ошибки и исключения не одно и то же, но связаны.
	Ошибки — это то, что уже нельзя исправить, об этом можно только сообщить: записать в лог, отправить email разработчику 
                и извинится перед пользователем. Например, если движок не может подключиться к БД, и мы это не обработали, то это ошибка. 
		Всё. Точка. Без БД сайт не работает, и я не могу с этим ничего сделать, только написать «Извините, сайт не работает» 

	Исключения — особые ситуации, которые нужно как-то обработать.
		     Например, если в калькуляторе вы попробуете разделить на ноль, то калькулятор не зависнет.
		     Такие ситуации можно обрабатывать обычным if-ом. Строго говоря, исключения — это конструкция языка позволяющая 
		     управлять потоком выполнения. Их основное предназначение: пробрасывать по каскаду.

       В итоге главное: если мы гипотетически можем обработать ситуацию через if и ОБРАБАТЫВАЕМ ее через try-catch, то это ИСКЛЮЧЕНИЕ.
	                Если ситуацию нельзя обработать через if или ее не обработали через try-catch, то будет ОШИБКА.

	Исключения выбрасываются - они предназначены для перехвата.
	Ошибки, как правило, уже неисправимы.

	При вставке в БД записи с одинаковым id возникает ошибка(повторяющийся идентификатор), которую можно отловить как исключение
	через try{} catch{} и ПРОДОЛЖИТь РАБОТУ СКРИПТА!!!
	Выполнение программы продолжится если мы отловили ошибку в catch.  Исключение будет рассматриваться как ошибка,
        если оно не будет перехвачено. Перехват позволит продолжить выполнение программы и после сбоя.

111) Выброс ошибок используется, так как они поднимаются вверх по уровням вложенности, их удобно отлавливать. 
     Например, исключения выбрасываются в нескольких местах и сообщают разные причины в сообщении. А на верхнем уровне мы увидим точную причину.
     Также может быть сложная логика и через условия придется постоянно проверять if($result->success), например:
	$user = User::find(1);
	if($user === null) { // обрабатываем ошибку }
	$user->doSomething();
     А можно проще с исключением:
	$user = User::findOrFail(1);
	$user->doSomething();

     Например, метод может выбросить 10 ошибок разных и надо разные действия выполнить, false уже не прокатит, так как придется 10 разных false делать.
     Исключения затратны для обработки, лучше их в исключительных случаях применять.

111) Некоторые исключения являются нормой: интернет не идеален. Но их следует просматривать и следить за их ростом.
     Есть сервисы, которые позволят группировать исключения и работать с ними намного удобнее.

111) Можно использовать встроенные типы ошибок, напр. \DomainException, но его исп. библиотеки, поэтому можно создать свой класс ошибки:
	class BusinessException extends \Exception {
		private $userMessage;
		 public function __construct(string $userMessage) {
        		$this->userMessage = $userMessage;
        		parent::__construct("Business exception");}
		public function getUserMessage(): string {
        		return $this->userMessage; }}	

	Теперь во вспомогательной функции func() можно выбросить: 
		throw new BusinessException("Old password is not valid");

	И отлавливать:
		try { $service->func(...);} 
		catch(BusinessException $e) { ... }
		catch(Exception $e) { ... }

111) Глобальный обработчик исключений Ларавель app/Exceptions/Handler.php.
     Этот обработчик  выполняет логирование исключений и сообщение пользователям о них.
     	protected $dontReport = [ BusinessException::class ] - эти ошибки не будут логироваться, но будут показаны пользователю
     В коде можно использовать помощники:
	1) report($e) - функция записи ошибки в лог
	try { ... }
	catch(Exception $e ) {
	   report($e) - запишет исключение в файл laravel.log, код продолжится }

	2) render($request, $e) - можно прописать в Handler.php или в своем классе ошибки, возвращаем вьюху ошибки

111) В файле  app/Exceptions/Handler.php можно настроить показ ошибок. Так, если APP_DEBUG=false на большинство исключений
     Ларавель генерирует HTTP ответ с кодом 500. Можно изменить показ ошибок в render():
	public function render($request, Throwable $e){
           if($e instanceof HttpException){
               return response()->view('error.view', [], 501);} - если попали в условие показывает нашу вьюху
               return parent::render($request, $e); } - либо обычный показ 500 ошибки

     Можно настроить возврат ошибки в json для api запросов:
	if($e instanceof \DomainException && $request->expectsJson()){ - если пришел заголовок Accept: application/json, то есть от нас ждут json (это часто api запрос)
            return response()->json(['error' => $e->getMessage()], 400); - возвращаем текст ошибки в json
        }
     Теперь для api контроллеров нам не нужны try-catch, если в заголовке принятого запроса стоит Accept: application/json,
     будут возвращаться ошибки в json: {"message": "Server Error"} и др.
  
111) Ларавель сама без прописывания try, catch отлавливает исключения и показывает их с помощью пакета laravel-ignition
     То есть если где-то выбросится throw new \Exception() и подобное, то Ларавель авт. выведет подробную страницу исключения.
     Но дальнейший код остановится на моменте выброса ошибки в общий обработчик Ларавель!!!
     Поэтому, если мы хотим продолжить код, то отлавливаем ошибки сами, через try{} catch{}, делаем что-то в catch и тогда КОД ПРОДОЛЖИТСЯ!!!
    
     Также мы можем сами обрабатывать исключения, останавливать их проброс в Ларавель и соответственно настраивать дальнейшую работу скрипта:
	class User {
		public static function func(){
        		throw new \Exception('не получилось');} - искусственно создали, может не быть, например Ларавель сама выбросит исключение 

		public function index(){
			try {
			     self::func();
         	        } catch (\Exception $e){ - отлавливаем ошибки 
            		     echo $e->getMessage();} - вывод на экран сообщения: 'не получилось'
			     report($e); - либо можем пробросить ошибку в обработчик исключений Ларавель, запишется сообщение в журнал, но скрипт продолжится
			  так как ошибку отловили, то дальнейший код продолжит работать!!!!!! } 

111) Внимание! Если поймали исключение через catch, то можно выполнить любые действия (записать в журнал ее) и скрипт ПРОДОЛЖИТСЯ!
     Для того, чтобы он не продолжался можно делать редирект: return back()->with('error', $e->getMessage()) }

     ЕСЛИ ошибку не ловили и ее поймал общий обработчик ошибок Ларавель, то по умолчанию скрипт остановится и Ларавель выдаст HTTP ответ, 
     напр. 500 - ошибка сервера.
     На все исключения (при APP_DEBUG=false) Ларавель, чтобы не показывать внутреннюю информацию 
     выдает HTTP ответ с кодом 500 (ошибка сервера). Это не касается белого списка исключений: Bad Request, Forbidden, Not Found - у них
     показывается сообщение об ошибке.
     Это поведение можно настроить в app/Exceptions/Handler.php
     Функции orFail или abort(404) выбрасывают исключения с перенаправлением по коду 404.
    
     Если вывод ошибок включен, то отработает пакет laravel-ignition и скрипт тоже остановится на показе ошибки.
     Если хотим все же поймать ошибку в catch и остановить скрипт, то return false

111) Пример работы работы с Exception:
	try {
	    ... какая-то работа ...
	    if(...){ throw new \Exception('исключение'); } - если была ошибка, то пробрасываем исключение
 	} catch(\Exception $e){ return back()->with('error', $e->getMessage()) } - если исключение, то редирект + ошибка в сессию
	return redirect()->route('admin') - если все хорошо, то выполнится этот код

111) В Ларавель можно настраивать обработку исключений в app/Exceptions/Handler.php
     
111) Можно выбирать какой тип ошибки отловим, а какой пусть идет выше в Ларавель:
	catch (\SQLException $e){ - ловим только SQLException, остальные сюда не попадут и отловятся выше в Ларавель
                return 'ошибка SQL';
              }
                return 'Другая ошибка';
        }

111) Можно отлавливать тип ошибки каскадом:
	catch (\LogicException $e) { ... } - сначала ловим этот тип ошибки и останавливаем отлов если поймали
	catch (\Exception $e) { ... } - в итоге отлавливаем любые другие
    
     Или через логический оператор ИЛИ:
	catch (MyCustomException | MyAnotherCustomException $e)

111) Также можно проверять тип исключения:
	if($e instanceof \ErrorException){}

111) Объект общего класса Exception отловит все типы исключений catch (\Exception $e)

111) При вложенных методах можно при неудачном внутреннем выбрасывать исключение, а во внешнем отлавливать его.
     Пример:
	Внутренний метод: добавить товар в избранное, если уже в избранном, то выбросить исключение
	Внешний метод: try { какая-то логика + внутренний метод} catch(\Exception $e){ return back()->with('error', $e->getMessage()) } }

111) Можно заглушать ошибки и продолжать код:
	 try {
            abort(404); - без разницы какая ошибка, она будет заглушена через пустой catch.
         } catch (\Exception $exception){} - пустое тело функции
	 echo 'Код продолжился' - код продолжится

111) В Ларавель встраивать можно также объекты на основе своих классов - так и встраиваются Сервисы:
	class MyController { - свой класс
    	   public $var;
    	   public function __construct(){
        	$this->var = 111;}}

	class MainController extends Controller {
    	   public function index(MyController $controller){ - здесь делаем инъекцию, Ларавель создает объект создается автоматически
        	dd($controller->var);}} => 111               

    Таким образом можно писать вспомогательные классы в папке app или app/Http
    Эти классы как-то становятся доступными для инъекций. Хотя вроде как инъекции можно делать того, что хранится в контейнере.
    Возможно в контейнере хранятся уже созданные объекты для работы приложения, а в таких классах они создаются только при обращении и
    можно вставлять параметры.

111) Контейнер Ларавель создается таким образом:
	1) При запуске приложения загружается список классов-провайдеров из config/app.php => providers
	 
	2) Laravel проходит через метод register этих сервис провайдеров, создает их объекты и заполняет ими контейнер $app:
		protected function registerSessionManager() {
        		$this->app->singleton('session', function ($app) { - в app по свойству 'session' записался результат метода 
           			 return new SessionManager($app);});}	     теперь к нему можно обращаться: app('session')
	3) В каждом провайдере также запускается метод boot()

111) Таким образом мы тоже можем запустить свой класс-провайдер при создании контейнера Ларавель и даже внедрить в контейнер объект:
	1) php artisan make:provider MyServiceProvider	
	2) Регистрируем его в массиве config/app.php => providers 
	3) В методе register() нашего провайдера:
	        можем использовать в контейнере какой-нибудь вспомогательный класс:
		$this->app->bind(MyClassB::class, function ($app){ return new MyClassB }) - связь получения объекта класса при обращении через контейнер
		$this->app->singleton(MyClassB::class, function ($app){ return new MyClassB }) - обращение к классу MyClassB создает instance объект 	
	   	class MyClass { // отдельный файл где-нибудь в app/                              также можно делать инъекцию в методы (MyClassB $obj)
    			public $var = 5;}

	4) Теперь можно пользоваться нашим объектом в коде:
	        $api = $this->app->make(MyClassB::class)->var ; - работает в методе boot
	        $api = App::make(MyClassB::class)->var - так работает в других местах
		public function func(MyClassB $obj){ ... } - встраиваем

	5) Либо внедрять такие объекты как инъекции:
		public function func(MyClassB $obj){}
	6) А метод boot() нашего провайдера срабатывает автоматически при загрузке провайдера (то есть при запуске приложения).
	   Здесь мы можем регистрировать обработчики событий, добавлять вьюкомпозеры и пр.
	7) Поэтому вместо создания своего провайдера можно писать код в AppServiceProvider  boot() - он тоже запустится при запуске приложения

  Итог, что понял: 
	1) Вспомогательный классы с методами пишем в подпапках app, они загрузятся через композер
	2) Если нужен неизменяемый заранее созданный объект, то удобно создавать его в провайдере: $this->app->singleton(...)
	3) Удобно внедрять зависимости созданные в провайдере и записанные в контейнер
	4) Объекты вспомогательных классов тоже можно вставлять инъекцией
	5) К контейнеру можно обращаться:
		 App::
		 app('...')
	         app()->метод
		 app() - весь контейнер, внутри все провайдеры и много всего
		 Container::getInstance() - весь контейнер
	         $this->app->...  - в методе boot()
	6) Сам объект контейнера - экземпляр класса Application
	7) В контейнер через провайдер можно вставить объект через $this->app->bind(MyClassB::class, $callback)
	8) Вообще объект (и соотв. его вспомогательные методы) внедренный в контейнер ($this->app->bind) тоже работает, 
	   но лучше вспомог. методы писать в app/классы, чем внедрять в контейнер через объект

111) Встраивание своего настроенного instance объекта использующего пароль в контейнер:
	1) Создаем класс SmsSender(), прописываем в нем конструктор принимающий пароль, другую логику
	2) В .env файле создаем контстанту:
		SMS_PASS=111
	3) Создаем в папке config файл sms.php:
		return ['password' => env('SMS_PASS')];
	4) В AppServiceProvider в методе register:
		$this->app->singleton(SmsSender::class, function($app){ // $app - пробрасывается объект контейнера
			$config = $app->make('config')->get('sms') - получаем из папки config массив файла sms.php
			$config = config('sms.password') - более простой вариант, здесь уже и password получили
			return new SmsSender($config['password']) }) - создаем объект SmsSender и встраиваем в него пароль
	5) Теперь если делать инъекцию в методах контроллера, то этот instance объект встроится:
		public function func(SmsSender $sms) { ... }
	6) Также можно обращаться к такому объекту так:
		app(SmsSender::class)
	7) Метод singleton создал объект который переиспользуется. Если бы делали через bind, то объект пересоздавался бы
	8) Удобно встраивать такие предварительно настроенные объекты в контейнер и затем пользоваться. Например 
           можно встроить клиент ElasticSearh
									
111) Пакет tinker: php artisan tinker
     Теперь в командной строке можно писать php код и он будет отрабатывать в консоли. Это вместо того, чтобы запускать файлы php, чтобы посмотреть
     результат.

111) Массив $hidden в Модели - перечень полей, которые не выведутся при сериализации объекта Модели в json
                               То есть если отдаем наружу по api или в браузер весь массив return User::find(1), то Ларавель преобразует такой массив
			       в json и исключит поля из $hidden
                               Например: если protected $hidden = ['password'], 
		                         то return User::find(1) => {"id":1,"name":"\u0414\u0435\u043d\u0438\u0441","email":"rodnovspb@mail.ru"}
			       Однако, если вернуть не массив, а само поле то выведется: return User::find(1)->password  => пароль 

111) composer dump-autoload -o  - композер пересоздаст файл vendor/composer/autoload_classmap со списком классов, файлов, 
				  которые настроны в поле autoload composer.json
     Например там по умолчанию прописано autoload: ... "App\\": "app/" - то есть все что в папке app грузится в автозагрузку композера

111) Генерирование консольных команд:
	1) php artisan make:command VerifyUser	- в папке app/Console/Commands создается класс VerifyUser
	2) Пишем название команды в protected $signature = 'user:verify {email}' - так, если c параметром, напр.: php artisan user:verify rodnov@mail.ru
                                    protected $signature = 'user:verify'; - так, если без параметра: php artisan user:verify
	   К принятому аргументу можно будет обращаться через $this->argument('email')				      
	3) В protected $description = 'Описание команды' пишем описание, выводимое рядом с командой, напр., при php artisan		       
	4) В public function handle() пишем сам метод, например:
		public function handle() {
			$email = $this->argument('email'); - получили принятый email
        		$user = User::where('email', $email)->first();
       		        $user->status = 1;  - верифицировали
                        $user->save();
                        $this->info('Верифицировали'); - вывели в консоль
			... отправляем письмо на email ...
                        return Command::SUCCESS;} - это чтобы вернуть true
	5) Теперь верифицировать можно напрямую из командной строки: php artisan user:verify rodnov@mail.ru
	6) Для того, чтобы защититься от нескольких срабатываний, если случайно ввели одинаковые команды и пр., можно выбрасывать исключение в общий
           обработчик Ларавель, тогда так как нет собственного try-catch код не будет продолжаться:
		if($user->status === 1) { throw new \DomainException('пользователь уже верифицирован') } // то есть мы не отлавливали через catch, 
												            поэтому код остановится
111) Также можно настроить изменение БД по расписанию. Крон запускает планировщик Ларавель, а тот запускает консольные команды.
     Например снятие с публикации истекших объявлений
	1) php artisan make:command ExpireAdvert
	2) protected $signature = 'advert:expire';
	3) public function handle(){ 
		foreach(User::where('date', '<', 'дата')->cursor() as $user){ изменяем статус }}
	4) В Kernel в ларавелевском планировщике задач:
	   protected function schedule(Schedule $schedule) {
        	$schedule->command('advert:expire')->hourly(); - запускать нашу команду раз в час
		$schedule->command('inspire')->daily();} - другая команда
	5) Теперь в кроне не нужно запускать разные команды, а достаточно запускать планировщик Ларавель раз в минуту:
	   * * * * * php /path-to-your-project/artisan schedule:run >> /dev/null 2>&1 - что-то по типу такого

111) Аутентификация - определение вошедшего пользователя
     Авторизация - определение прав пользователя (права модератора, администратора и др.)

111) Определение прав через шлюзы:
	1) Создаем шлюз delete-post  в AuthServiceProvider в методе boot:
		Gate::define('delete-post', function (User $user, Post $post) {
       			 return $user->id === $post->user_id; });
	2) Используем в контроллере: 
		Gate::authorize('delete-post'); -  если нет прав, то выбросится AuthorizationException и браузер покажет 403 страницу THIS ACTION IS UNAUTHORIZED.
	        Gate::inspect('delete-post') - этот метод вернет true или false, соотв. можно использовать в if
	3) Также можно писать шлюз сразу в контроллере:
		Gate::allowIf(fn (User $user) => $user->role === 'admin'); - иначе 403 страница THIS ACTION IS UNAUTHORIZED.
		Gate::allowIf(fn (User $user) => $user->name === 'Денис'); - иначе 403 страница THIS ACTION IS UNAUTHORIZED.
	4) Можно вставлять шлюзы в другие места:
		Route::post('/post/{post}', $callback(Post $post))->middleware('can:delete-post,post') - через посредники в роуты
		Route::post('/post/{post}', $callback(Post $post))->can('delete-post', 'post') - через метод can в роуты
		@can('delete-post', $post) ... @else ... @endcan - в blade верстку
		Auth::user()->can('delete-post', $post) - в контроллер тоже так можно вместо фасада Gate
		$user->can('delete-post', $post) - то же самое, применяется в контроллере

111) Bouncer - пакет для авторизации c настройкой ролей

111) Фильтрацию Ларавель можно выполнять достраиванием запроса.
     <form method='get'>тут инпуты</form>

     $query = User::query() - начало запроса
     if(!empty($value = $request->status)){ $query->where('status', $value) }	
     if(!empty($value = $request->email)){ $query->where('email', 'like', "%$value%") }
     if(!empty($value = $request->filter)){ $query->orderBy($value, 'desc') }
     $users = $query->pageinate(20)

    Также можно применять ссылки вместо формы:
    	<a href="{{ route('products.index', ['sort' => 'price', 'ord' => $ord]) }}">⇅</a>
    Параметры из такого роута тоже доступны в $request->sort и т.д., то есть можно использовать блок с верхними условиями
	
111) Выделение новых заказов и др. непросмотренных сущностей в Ларавель:
	1) Для сущностей в БД добавляем поле $table->integer('shown')->unsigned()->default(0);
	2) При показе страницы товара меняем на 1: public function show(){ $good->shown = 1 }
	3) В списке товаров используем условия: @if($good->shown == 0) <li class='green'> ... </li> @else <li> ... </li>  @endif

111) https://laradock.io/ - готовые настройки докера для Ларавель

111) Способы хранения связанных записей: 
	1) В одной таблице через parent_id. Поля: id, name, parent_id - если родительская и дочерная запись одинаковой структуры
	2) Один к одному или один ко многим. В таблицу добавляем связь со второй: user_id 
	3) Многие ко многим через связующую таблицу: user_id, post_id - исп. связи hasMany и пр.
	4) nestedset - в бд есть поля left и right описывающие границы элемента. Хорошо подходит, если вложенность древовидная(глубокая), 
           таблицу редко изменяют, но часто делают выборку. Напр. если есть категории, подкатегории и товары, то для получения всех товаров 
           категории делается один запрос SELECT * WHERE lft IS BETWEEN parent.lft AND parent.rgt, а не множество рекурсивных
	   Пакеты: laravel-nestedset, baum и т.д.
	   $node->prependToNode($parent)->save(); - добавление
	   $result = $node->getSiblings(); - получение и т.д.
	5) Для записи создать поле path в которой хранить строку вложенности по предкам: '1/15/24/36'. Postgresql умеет делать так из коробки.
           Соответственно получить всех потомков категории 15: SELECT * FROM goods WHERE path LIKE '1/15'
	   Также можно хранить вместо id записей путь по слагам: 'qqq/www/eee'

111) Если строки содержат ссылку (напр. user_id) на другую запись из той же таблицы или другой, то возникнут ошибки 
     если удалить родительскую запись без удаления дочерних. Ларавель предусматривает защиту.
     Пример создания таблицы users:
	$table->increments('id');
	$table->foreign('user_id')->references('id')->on('users'); - поле user_id типа UNSIGNED BIGINT привязанное на id записи в таблице users
     Для того, чтобы управлять дерервом связей предусмотрены методы: onUpdate(...), onDelete(...) 
	onUpdate('cascade') - обновления связей каскадируются, насколько понял, если у родителя изменится id, то в дочерней изменится user_id
	onUpdate('restrict') - обновления связей запрещаются, насколько понял у родителя нельзя изменить id
	onDelete('cascade') - запись авт. удалится, если удалится родительская
	onDelete('restrict') - родительскую запись удалить не получится, пока существует эта дочерняя (работает по умолчанию)
	onDelete('set null') - установить связующее с родительской таблицей поле user_id как null 
     Как это выглядит:
	$table->foreign('user_id')->references('id')->on('users')->onUpdate('cascade') - обновить user_id дочерних записей каскадом
	$table->foreign('user_id')->references('id')->on('users')->onDelete('cascade') - при удалении родителя удалить эти дочерние записи

111) Рекурсивные фабрики в Ларавель для создания связанных записей:
	1) User модель должна иметь posts метод, определяющий отношения
	2) $user = User::factory()
            	   ->has(Post::factory()->count(3)) - создается пользователь и для него создаются 3 новые сообщения
                   ->create();
	3) То же самое, но укорочено с пом. магич. метода: $user = User::factory()->hasPosts(3)->create();
	4) Также можно запускать новую связанную фабрику внутри кода самой фабрики в definition(): 'user_id' => Post::factory()
		
111) Можно создавать в два этапа: сначала запускать фабрику для пользователя, затем фабрику для сообщений привязанных к нему:
	$user = User::factory()->create(); - создали пользователя
	$posts = Post::factory()->count(3)->for($user)->create(); - привязали новые посты к нему

111) При работе faker можно создавать связанные записи:
	'name' => $name = $faker->unique()->city,
	'slug' => slug($name)

111) У библиотек на гитхабе смотрим issues (вопросы, проблемы), pull requests, обновления библиотеки, активность автора

111) Pull request - предложение изменения кода в чужом репозитории. Вы делаете форк чужого репозитория (который иногда и сам может быть форком), 
                    производите изменения в своём форке → посредством pull request предлагаете изменения владельцам репозитория, чей форк Вы сделали.

111) Библиотеки удобно искать на packagist, там есть фильтры и ссылки на гитхаб, рейтинг, кол-во форков, установок и пр. 

111) $table->json('list') - в таком поле таблицы удобно хранить списки

111) Удобно настроить обработку поля типа json ($table->json('list')) через $casts в модели:
	$casts = ['list' => 'array'] - поле будет автоматически преобразовываться из json в массив при получении из БД и наоборот в json из массива
				       при сохранении в БД. ОЧЕНЬ  УДОБНО!!!

111) Route::redirect('index', '/') - редирект в роутах

111) <li @if (Route::is('home')) class="active" @endif>Главная</li> - проверка роута

111) $user = User::make(request->all())	 - метод make только создает сущность в памяти
     $user = User::create(request->all())- метод create создает сущность в памяти и вызывает ->save() сохранение в БД 

111) Привязка телефона к пользователю.
     В БД у пользователя должны быть поля:
	идентификатор пользователя или email
	phone - теелфон
	verified (true или false) - верифицировали ли телефон
	verify_token - ключ верификации
	expire_verify_token - время  пригодности ключа (обычно 300 сек(5 мин) для телефона)
 
     Пользователь вводит телефон, в БД сохраняется ключ и его время пригодности, ключ отправляется на привязываемый телефон,
     пользователь должен ввести его в поле на сайте, ключ проверяется по таблице на правильность и пригодность по времени, пользователь активируется.
     Также нужно ограничить время отправки повторного ключа - проверяем, если у пользователя в таблице ключей еще не истекло время прошлого ключа (это
     удобно делать сравнением дат через методы Карбон), то отказываем (чтобы сохранилось в логах можно пробросом исключения или report(...))

111) Для времени устанавливаем в БД тип datetime или timestamp.
     Они оба устанавливают дату такого формата: 2023-05-09 18:15:23
     Текущее время можно устанавливать через now() или Carbon::now() и применять методы:
	Carbon::create(Product::find(3)->date) - создали объект Карбон из 2023-05-09 18:15:23
	Carbon::create(Product::find(1)->date)->addMonth(1)->toDateTimeString() - прибавили 1 месяц (2023-06-09 00:00:00)

111) Если сделать protected $casts = ['date' => 'datetime'], то дата типа 2023-05-09 18:15:23 при извлечении 
     будет авт. преобразовываться в объект Карбон

111) Методы  findOrFail, firstOrFail, saveOrFail и пр. при ошибке выбрасывают исключение вызывающее перенаправление
     Если отловить такое исключение, то перенаправления не будет:
	try {
            Product::findOrFail(1111); - такой id не существует
        } catch (\Exception $exception){ - поймается исключение ModelNotFoundException
            dd($exception->getMessage()); - выведет No query results for model, перенаправления не будет
        }

111) isset($_POST['name'], $_POST['surname'], $_POST['patronymic']) - функция isset может принимать несколько переменных

111) Observer можно применять для отслеживания изменения БД.
     Отследил изменение через обсервер - перекешировал вывод БД: Cache::put('users', User::get());
     Но может замедлять, например, если БД часто меняют.
     Можно использовать перекеширование по крону, когда вьюхи, запросы к БД кешируем по времени

111) Класс декоратор - принимает в конструктор другой класс и преобразует логику дочернего класса.
 	
	1) Класс, отправляющий смс
	class SmsRu(){ - дочерний класс
		public function send($number, $text){ ...отправка смс... }
	}
	
	2) Класс-декоратор, проверяющий номер, логирующий отправку + любые др. действия
	class Decorator(){
		public $obj;
		parent::__construct(SmsRu $sms) { - принимает при создании объект класса SmsRu
			$this->obj = $sms} - записывает этот объект в свое свойство
		public function sendSms($number, $text){ - функция, проверяющая номер
			if(...проверяем $number...){
			    $this->$obj->send($number, $text) - если все хорошо вызывается метод дочернего класса
			} else {
			    Log::info('неверный номер')	}}}
	3) Пользуемся
	$sms = new Decorator(new SmsRu()) - вызываем декоратор, передаем в его конструктор класс передающий смс 
	$sms->sendSms('89514466332', 'Привет') - вызываем метод декоратора, который проверит номер

111) Класс stdClass - вспомогательный пустой класс встроенный в php. 
	$obj = new stdClass();
	$obj->name = 'Денис'
	var_dump($obj->name) => 'Денис'
     То есть можно создать объект без создания своего класса, используя встроенный.

     Также stdClass помогает  преобразовывать асс. массив в объект с помощью приведения типов (object):
	$place = ['city' => 'Pokhara', 'country' =>'Nepal'];
	$obj = (object)$place;
        echo $obj->city;  => 'Pokhara'

     Также stdClass используется в работе функции json_decode()
	$json = '{"Harry":27,"Tony":24,"Juan":32}'; - строка json
	$obj = json_decode($json); - преобразовываем json в объект stdClass()
	var_dump($obj) => object(stdClass) { ["Harry"]=> int(27) ["Tony"]=> int(24) ["Juan"]=> int(32) } - создался экземпляр stdClass()

     Если в json_decode() установить флаг true, то json строка преобразуется в асс. массив:
	$arr = json_decode($json, true);
	var_dump(arr) => ["Harry" => 27, "Tony" => 24, "Juan" => 32]

111) Простая отправка СМС:
	1) Заходим на https://sms.ru/php, скачиваем класс SMSRU(), помещаем его во вспомогательные классы Ларавель или грузим через require_once
	2) Используем:
		public function send(){
        		$sender = new SMSRU('F42F1F6B-E9DC-0497-BE58-CC17DD10FF0F');
        		$data = new stdClass();
        		$data->to = '79507261797';
        		$data->text = 'Привет';
        		$sms = $sender->send_one($data);
	3) В документации есть вспомогательные методы. По умолчанию используется отправитель SMS.RU. За абон. плату можно поменять.

111) Также на sms.ru есть функция звонка и проговаривания номера голосом, стоит 40 копеек.

111) Таким образом с помощью sms.ru удобно делать двухфакторную аутентификацию:
	1) Вход по паролю
	2) + вход по смс присланной с sms.ru

111) Отправлять http запросы можно:
	1) через cURL: curl_init("https://sms.ru/sms/send") и т.д. - можно настраивать
	2) через file_get_contents:  file_get_contents("https://sms.ru?key=111") - только get запросы
	3) через guzzle - много способов отправки. Под коробкой cURL или если cURL не установлен - оболочка PHP для отправки HTTP-запросов.
			  Вариант удобнее, чем cURL, так как не нужно обрабатывать ошибки cURL.

111)  При наведении можно красиво выделять ссылку увеличивая font-weigh, для белого шрифта на черном фоне смотрелось очень красиво (it-help.biz)
	
111) Красивый шрифт font-family: "RobotoCondensed-Light"

111) Пространства имен в шапке класса: use App\Models\User на самом деле нужны для укорочения пути,
     теперь в коде $var = new User() на самом деле просто короткий вариант $var = new App\Models\User()

111) В коде нельзя обращаться к .env, потому что при развертывании после команды config:cache файл .env становится недоступным и вызов
     константы вернет null. 
     Поэтому используем посредника, например config/services.php:
	'my_const' => ['key' => env('APP_PAYSTACK_KEY', 'sk_test_b6c0b4925403blablabla')],
     Вызываем в коде: config('services.my_const.key')

111) В файле .env можно хранить только строки, функция env() переводит их в строки, числа и булевы значения
     Если нужно в настройках хранить массив или др. сложные данные, то храним их в файлах config, 
     обращаемся через config('имя_файла.ключ_массива') или можно переключать через .env так:
     1) В файле .env храним константу, напр.: CITY='chelyabinsk'
     2) В файле config/file.php используем эту константу:
	   return [
		'driver' => env('CITY', 'default'),
		'drivers' => [
			'chelyabinsk' => [массив настроек для Челябинска],
			'ufa' => [массив настроек для Уфы],
			'default' => [настройки по умолчанию]]]
     3) $key = config('file.driver') - получили 'chelyabinsk'
	$arr = config("file.drivers.$key") => [массив настроек для Челябинска] - получили массив
     4) Соответственно, теперь можно менять массив управляя константой из .env, например CITY='ufa'
     5) Почему нельзя хранить массивы без прокладки driver и обращаться сразу config("file.env('CITY', 'default')") ?? 
        Возможно для того, чтобы все перенастраивать можно было изменяя только файл .env, а значение по умолчанию только в config/file.php

111) Доступ к константам .env из js файлов:
	0) Не так все просто, нужен какой-то <script setup> или <script type="module">
	1) VITE_DADATA_TOKEN=213123
	   VITE_APP_URL="${APP_URL}" - в .env помещаем нужную константу в прокладку, начинающуюся на VITE_
	2) console.log(import.meta.env.VITE_APP_URL) - в файле js получаем ее

111) Если константа нужна в скрипте из blade, то проще:
	<script>
	 	let apiUrl = "{{ env('API_URL') }}"; //правильнее через config(...)
	</script>

111) Также можно помещать в скрытый инпут или тег meta при рендеринге блейда:
	<input type="hidden" id="token" value="{{ config('qqq.aaa') }}"> // для ево вариант
        let token = document.querySelector('#token').value  

111) Вставка в таблицу поля после другого поля:	
	$table->boolean('phone_auth')->default(false)->after('phone')

111) Короткая запись отрицания длинного выражения в условии:
	if(!$session = $request->session()->get('auth'))

111) Auth::login($user, true) - второй параметр означает «запомнить меня». Пользователь будет входить без атентификации каждый раз, пока не выйдет
                                вручную (Auth::logout())

111) Советует для лучшей индексации и удаления страниц из индекса использовать периодическое перегенерирование sitemap.xml,
     например раз в 2 часа. В sitemap указывается последнее обновление страницы, что помогает поисковикам.
     Также у Яндекс есть возможность загрузить товарный YML-фид в Яндекс.Вебмастер, что позволит отображать товар в поиске.
     			
111) Оператор ?: - сокращенный оператор на if:
	z = x ?: y =>  то же самое, что z = x ? x : y, то же самое, что if(x)
    Оператор ?? - сокращенный оператор на проверки на isset:
	z = x ?? y => то же самое, что if(isset(x))

    То есть операторы ? (?:) и ?? отличаются для некоторых сущностей, например: 
	isset($x = false) => true
	if($x = false) => ветвь уйдет в else
	isset($x = []) => true
	if($x = []) => ветвь уйдет в else и т.д.

111) Чтобы прикрепленный файл не слетал при валидации в Ларавель говорят отправлять форму через Аякс. Ошибки в этом случае вернутся в 
     json

111) $date->isoFormat('D MMMM HH:mm')

111) На одной странице можно записывать сразу несколько секций blade:
	@extends('layout')
	@section('text1') первая секция @endsection
	@section('text2') вторая секция @endsection

111) Если на родительскую страницу нужно прикрепить скрипты и подобное из дочерней, то можно сделать это так:
	1) Писать стразу в коде: <script> alert(111) </script>
	2) Помещать скрипт в секцию и выводить ее через @yield в layout:
		@section('scripts') <script> alert(111) </script> @endsection - это на дочерней странице
		@yield('scripts') - это в layout
	3) Правильный путь:
		Там где нужно вставляем принимающий стек, например в head из layout: 
			@stack('scripts')
		На любой врутренней странице помещаем в этот стек код:
			@push('scripts') <script>alert(222)</script> @endpush - в конец стека
			@prepend('scripts') <script>alert(111)</script> @endprepend - в начало стека

			@push('scripts')
    				<script src="/script.js"></script>  // либо крепим файлом
			@endpush

111) Вместо @yield('xxx') в layout можно использовать секцию через @show:
	 @section('xxx') ... @show - выведется содержимое внутренней секции @section('xxx') ... @endsection, а если ее нет, то указанный код
				     
     Также через @parent можно указать во внутренней секции, что все-таки нужно вывести содержимое родительской секции в указанном месте: 
	@section('xxx') @parent .... @endsection

111) @yield(xxx, default) - почти то же самое, что @section(xxx) default @show - если не переписали переменную xxx, то показать default
                            @section(xxx) default @show - используется в layout 
			    Отличие в том, что внутренняя @section может вставлять родительскую @section через @parent, а при просто 
                            @yield такое дополнение невозможно :        
     @section(xxx) 111 @show - в layout
     @section(xxx) @parent 222 @endsection - в дочерней
     Итого вывод: 111 222, через просто @yield такое взаимодействие не получится

111) Встраивание промежуточных действий в создание записи в БД.
     Так как метод User::create(...) сразу сохраняет сущность в БД, можно создавать запись так:
	$user = new User();
	$user->name = 'QQQ' - промежуточная логика
	$user->save(); - сохранение
     Или через make:
	$user = User::make(request->all())
	$user->name = 'ZZZ' - промежуточная логика
	$user->save(); - сохранение

111) $user->getAttributes() - получение массива свойств модели

111) В одной миграции можно создавать несколько таблиц:
	Shema::create(...);
	Shema::create(...);

111) Составной первичный ключ в миграции:
	$table->primary(['user_id', 'parent_id']); - но такая связка значений должна быть уникальной

111) DB::table('users')->whereJsonContains('opt->lang', 'en') - вернет пользователей у которых в поле opt хранится json 
							        в котором по ключу lang лежит en
		       ->whereJsonContains('opt->roles', ['admin', 'mod']) - по ключу roles лежит масссив, содержащий 'admin' и 'mod' ???

111)  image.intervention - php библиотека для работы с изображениями.
	
      Возможности: изменение размеров, вставка подяного знака, обрезка, яркость и др. изменение цвета, наложение маски, размытие,
                   поворот, пикселизация, чтение мим типа, чтение мета данных, перекодирование в другой формат (psd, webp и др.),
		   изменение качества, показ ширины-высоты, наложение текста, кеширование изображений и др.    
	
111) Когда в MySQL оператор используется для работы с разными типами один из операндов преобразуется для совместимости.
     Например: SELECT 1+'1'; -> 2

111) Для того, чтобы с одного сайта отправить ajax запрос на другой сайт тот второй должен ответить на preflight запрос заголовком: 
 	Access-Control-Allow-Origin: '*' или Access-Control-Allow-Origin: 'адрес вашего сайта'

111) array_chunk($arr, 3) - разбиение массива на подмассивы
     array_chunk(['a', 'b', 'c', 'd'], 2) => [['a', 'b'], ['c', 'd']]

     Может быть полезен при разбиении массива сущностей на подблоки:
	@foreach(array_chunk($arr, 3) as $chunk)
	    <div>
	    @foreach($chunk as $item)
	    	<p>{{ $item }}</p>
	    @endforeach
	    </div>
	@endforeach

111) Создание FileRequest для валидации нескольких файлов <input type='file' name='files[]' multiple> :
	public function rules() {
		return ['files.*' => 'required|image']}	 - то есть * означает, что во внутреннем цикле провалидируются все сущности

111) public function func(): void { ... } - функция ничего не возвращает

111) Принцип ООП: метод вызывается у объекта модели - пишем его в Модели. Например, проверка $user->isAdmin().
                  В модели User он будет такой: public function isAdmin(){ return $this->role === 'admin' }
		  Метод использует сложную логику работы с БД - в модели.
                  Если объект из модели уже получен в контроллер - можно писать в контроллере
                  метод работает только с существующим объектом - пишем его в контроллере, по возможности делаем приватным
		  метод взаимодействует с другими сущностями, моделями - пишем его в сервисном классе
                  Например: 
		  Обновление или проверка свойства записи в БД - метод в Модели или Контроллере 
	          Работа над объектом связанная с изменениями др. моделей, сущностей - пишем сервисный класс

111) mb_strimwidth('строка', начало, ширина) - php функция обрезающая строку.
    
     mb_strimwidth('qqqqq', 0, 3) => 'qqq'

111) Str::limit('qqqq', 2) - обрезающий строку Ларавель помощник, в конец добавляется многоточие => 'qq...'

111) Перенос строки в разных ОС осуществляется по разному:
	Linux – \n;
	Mac – \r;
	Windows – \r\n.
     Поэтому вместо этих символов для кроссплатформенности  рекомендуют использовать константу PHP_EOL

111) nl2br() - php функция добавляющая рядом с каждым переносом тег <br>
               $str = "lorem" . PHP_EOL . "ipsum";
	       nl2br($str) => lorem <br> 
			      ipsum

111) Яндекс карты - на сайте Яндекса в конструкторе создается скрипт, вставляется на сайт. 
                    Яндекс также может генерировать карту по переданному адресу. В этом случае так как число запросов ограничено, 
                    то чтобы при каждой перезагрузке страницы не отправлять запрос в Яндекс можно кешировать 

111) Показ телефона в объявлении по нажатии на кнопку:
	1) В href ссылки пишем адрес  для получения телефона
	2) По нажатию на кнопку отправляем fetch POST запрос на этот адрес 
           (POST для того, чтобы прикрепить csrf токен, чтобы нас не парсили get запросами)
	3) Полученный телефон показываем
        4) Однако не понял как избежать того, чтобы токен не парсили и не вставляли в headers

111) Передача параметра в роут:
	Route::get('/main/{city}')->name('index')
     Когда один параметр его можно крепить к ссылке просто:
   	route('index', $var) или route('index', [$var]) или route('index', ['city' => $var])
  
     Когда же роут ожидает несколько параметров:
	Route::get('/main/{city}/{dis}', [...])->name('index')
     То передавать параметры в ссылку можно только в массиве:
	route('index', [$var, $var2]) или route('index', ['city' => $var, 'dis' => $var2])

     Если передаем больше параметров, чем ожидает роут, то они добавляются к ссылке как гет-параметры

     Соответветственно, контроллер принимает:
	public function edit($city){ ... }

     Не путать с передачей переменных в представление:
	return view('main', compact('city'))

111) Работа с большими массивами данных при переборе:
	foreach(User::get() as $user) - в память грузятся все записи, для каждой сразу создается модель, используется 1 sql запрос
	User::chunk(100, func($users)){foreach ($users as $user)}) - в память грузится по 100 пользователей за неск. sql запросов
	foreach(User::lazy() as $user) - аналогично chunk несколько запросов (по 1000), но работать удобно, т.к. с одним потоком, а не передаем каждый фаргмент в замыкание
        foreach(User::cursor() as $user) - модель записи создается когда до нее дойдет цикл, необработанные записи из БД хранятся в PDO

111) Обычная коллекция Collection хранит в памяти объект со свойствами и методами для каждой ее сущности. Соответственно, если создаем
     коллекцию для 1000 записей из БД User::take(1000)->get() или collect([1,...1000]) - то создается 1000 сложных объектов 
     и все они хранятся в оперативной памяти. Ленивая коллекция создает модель только при вызове конкретной записи.
     Например: User::get() - сразу делает запрос к БД и создает модели для каждой записи.
	       Product::cursor() и User::lazy() - даже не делают запрос к БД! Один запрос делается как только начинаем перебирать 
               эти записи или обращаемся к какой-нибудь (напр. Product::cursor()->first())
     User::lazy() и User::cursor() создают ленивые коллекции (модель для записи создается при обращении к записи).
     User::chunk - делает несколько запросов к БД и для каждой партии полученных записей сразу создает модели
     User::lazy() - работает аналогично chunk, но по 1000 записей и отдает ленивую коллекцию.
     
111) $products = Product::cursor() - получен экземпляр LazyCollection. Все записи сохранились в PDO за 1 sql запрос.  
     Теперь их можно перебрать: foreach ($products as $product){ ... } - теперь только на этапе вызова записи для нее создается модель

111) Отличие lazy() от cursor() в том, что при cursor() в память PDO грузятся все записи из БД.
     А при lazy() по 1000 (запрос выглядит так: select * from `products` limit 1000 offset 0). Например при выборке 15000 записей lazy создал 16 sql запросов.
     И при cursor() и при lazy() в памяти модель для записи создается только при вызове записи, но lazy приоритетнее при миллионных выборках,
     так как грузит в PDO по 1000 записей. Иначе говоря lazy этот тот же chunk, но создает модель при вызове записи.
     Статистика для 100 000 записей:
	get(): память - 277 МБ, время - 2,72 сек.
	cursor(): память - 75 МБ, время - 5,8 сек.
     	lazy(): память - 9 МБ, время - 5,4 сек.
        chunk()на 500: память - 6 МБ, время - 7,56 сек.

    Если же записей мало (10 000), то быстрее всего работает get(), хотя и потребялет больше памяти - 26 МБ (но этим можно пренебречь)

    Использование памяти и время запроса удобно смотреть в Дебагбаре справа!!!!

111) Функцию env() нельзя использовать в коде. Ее можно использовать только в файлах config.
     При развертывании после команды config:cache файл .env становится недоступным и вызов константы вернет null. 
     Используем какой-нибудь посредник в config

111) Изменение поведения роутов, ждущих id:
	Route::get('/product/{product:slug}', [...])->name('show') - теперь роут будет ждать слаг, а не id как работает по умолчанию
	public function show(Product $product) - продукт будет встраиваться  по слагу
	<a href="{{ route('show', $product->slug) }}">Открыть</a> 

111) <a href="?">Кнопка</a> - эта ссылка перейдет на эту же страницу, но отрежет get параметры
     <a href="#">Кнопка</a> - перейти на эту же страницу с добавлением # к url. Ссылка перестает работать при повторных нажатиях!!

111) $users= Cache::remember('users', $seconds, function () { - если нет в кеше, то возвратить и добавить в кеш на столько секунд  
    		 return User::get();});
     Можно также сделать: Cache::rememberForever(...) - закешировать навсегда

111) Теперь просто используем:
	1) $users = Cache::rememberForever('users', function (){ return User::get(); }); - будет только один запрос к БД
	2) Создаем наблюдатель (есть ниже) и в его методах прописываем Cache::forget('users');
	3) МОжно настроить очереди. queueable можно использовать в наблюдателе
     
111) Для того, чтобы изменять кеш при изменении БД удобно привязаться к событиям Модели. Прослушивать их можно 
     через observer (если их много), в методе booted() модели и через отдельные классы для каждого события.
     а) через booted() Модели прослушиваем created и updated:
	class User extends Authenticatable {
		protected static function booted(){
			static::created(queueable(function (User $user) { - прослушиваем создание пользователя
            			Cache::forget('users'); })); - сбрасываем кеш в фоновом режиме используя очередь
			static::updated(queueable(function ($user) { - прослушиваем обновление пользователя
            			Cache::forget('users'}));}} - сбрасываем кеш в фоновом режиме используя очередь
     б) Если хотим прослушивать все события (так правильнее), то создаем наблюдатель модели:
	 1) php artisan make:observer UserObserver --model=User - в app/Observers создался наблюдатель
	 2) Наблюдатель отслеживает несколько событий: 
		class UserObserver {
			public $afterCommit = true; - для транзакций, чтобы события обрабатывались только при успешной транзакции
			public function created(){ Cache::put('users', User::get()); } - тут всех закешировал
			public function updated(User $user){}
			public function deleted(User $user){}
			public function restored(User $user){}
			public function forceDeleted(User $user){}}
	3) Пишем сброс кеша в нужных методах:
			Cache::forget('users')
	   Или лучше перекешируем сразу: Cache::put('users', User::get()) - так как секунды не указаны, то навсегда (до перекеширвоания)
	4) Регистрируем наблюдатель в EventServiceProvider:
		public function boot() { User::observe(UserObserver::class); }
	5) Все, теперь кеш для ключа 'users' всегда будет сбрасываться/перекешироваться при изменениях в таблице users
 
111) Если драйвер работы с кешем - редис или мемкэшед, можно привязать к ключу несколько ключевых слов - тегов:
	Cache::tags(['people', 'artists'])->put('John', $john, $seconds);
     Теперь можно удалить такой кеш зная его тег:
	Cache::tags(['people'])->flush();

111) Внутри класса к его свойствам и методам можно обращаться:
	1) self::  - к статическим и константам (будет работать и для нестат., но не рекомендуется)
	2) $this->  - к нестатическим
     Используйте $this для ссылки на текущий объект, а self — для ссылки на текущий класс

111) Первичным ключом может быть не только id, но и любое другое уникальное поле (например email) 
     или даже связка полей - составной первичный ключ (например в таблице связей user_id - post_id):
	$table->primary(['user_id', 'post_id'])

111) Способы удаления связи из pivot таблицы user_id - post_id:
	1) $user->post()->detach([1]) - удалили связь, где post_id = 1
	2) $user->post()->sync([3]) - создали связь, где post_id = 3, остальные удалили
    	3) $table->foreign('user_id')->references('id')->on('users')->onDelete('cascade'); - авт. удаление связи, если удалится родительский user
	   $table->foreign('post_id')->references('id')->on('posts')->onDelete('cascade'); - то же, если удалится родительский post
	4) DB::table('post_user')->where('user_id', $id)->delete() - удалить вручную
	  
111) Отправка email многим пользователям, например, если цена изменилась:
	1) Через очереди циклом: 
		foreach(User::cursor() as $user){
			Mail::to($user)->queue(new Note(...))} - настраиваем и запускаем отправку писем через очереди
	2) Через посредника в виде испускание события:
		event(new PriceChanged($price)) - если цена изменилась испускаем событие
		Пишем событие и слушателя. Слушатель ставит отправку писем в очередь.
	3) Также можно использовать наблюдателя, который будет сам отслеживать изменение цен

111) Laravel Horison - панель для наблюдения за выполнением очередей queue в Redis

111) Объекты в php копируются по ссылке. Для того, чтобы сделать независимую копию объекта (если внутри нет связей с др. объектами):
	$a = new MyClass();
	$clone = clone $a;
     Также можно клонировать объект внутри класса:
	$clone = clone $this;

111) $table->text('text')->fulltext(); - полнотектовый индекс. Быстрее чем LIKE и более настраиваемый. Подходит для поиска
                                         совпадений в больших объемах текста.
     Контроллер:
     	$products = Product::whereFullText('text', 'деревянный член')->get();
     Запрос:
     	select * from `products` where match (`text`) against ('Чичиков' in natural language mode)
     Но полнотекстовый поиск в отличие от LIKE ищет даже если подходит точная часть фразы, 
     показывая много результатов с уменьшающимся коэффициентом совпадения. 
     Работает интересно: душеньку != душенька, 'Нашел арбуз' = 'Куплю арбуз'
     И ищет все же долго, ввел 'видит', завис на 5 секунд.

111) Laravel Scout - пакет-оболочка для поиска по моделям взамен Эластиксерч. Для работы может использовать БД и
                     сторонние сервисы, например meilisearch.com - бесплатно 100 тыс документов, 10 тыс поисков в месяц
                     Без meilisearch работает как выше описанный полнотекстовый поиск.

     Установка Scout с meilisearch:
	1) composer require laravel/scout
	2) php artisan vendor:publish --provider="Laravel\Scout\ScoutServiceProvider"
	3) composer require meilisearch/meilisearch-php http-interop/http-factory-guzzle
	4) SCOUT_DRIVER=meilisearch
	   MEILISEARCH_HOST=https://ms-2219c625a903-3776.sfo.meilisearch.io
	   MEILISEARCH_KEY=ead0488d037110f4ac229afafc99c9bacf603570
	5) Настраиваем драйвер очереди, например QUEUE_CONNECTION=database + публикуем для него таблицу
	6) В config/scout.php меняем: 'queue' => ['connection' => 'database','queue' => 'scout'] //connection лучше redis
	7) В модель вставляем поле, разрешающее поиск. В дальнейшем сможем отключать показ записей через это поле: 
	     $table->boolean('searchable')->default(true);
	     $table->text('text')->fulltext(); - полнотекстовый индекс, если используем только Scout для поиска в БД
	7) В модель вставляем Product:
	     class Product extends Model {
		   use Searchable;
		   public function searchableAs(){
        		return 'product';}
		   #[SearchUsingFullText(['text'])] - для работы скаута, чтобы использовал полностекстовый поиск match-against, а не LIKE (смотрим запросы в дебагбаре)
		   public function toSearchableArray(){ - в этом методе пишем список полей, в которых будем искать
			return [
			   'name' => $this->name,
		           'text' => $this->text];}
		   public function shouldBeSearchable(){
        		return $this->searchable;}} - теперь в поиске только модели с searchable = 1
	8) php artisan scout:import "App\Models\Product" - пишем название Модели. Все модели индексируются. Добавилась строка в таблицу jobs.
           Переиндексация происходит автоматически при обновлении, создании и удалении модели.
	   php artisan scout:flush "App\Models\Product"  - удалить из индекса: 
	9) php artisan scout:index product - где product из в searchableAs()
	10) Проверяем работу Scout: 
		меняем SCOUT_DRIVER=database
		$products = Product::search('во что бог послал')->get(); - в $products окажутся модели имеющие точные совпадения 
                                                                           из массива в toSearchableArray()
	    В дебагбаре видим наш запрос: select * from `products` where (match (`products`.`text`) against ('вызолоченные' in natural language mode)) 
	11) Теперь устанавливаем SCOUT_DRIVER=meilisearch
	12) Дальше ничего не работатало, пока ручками не загрузил документы в meilisearch 
            и не поменял searchableAs на prods. Видимо это потому что сервер meilisearch не установлен на компьютер.
	    $client = new Client(config('scout.meilisearch.host'), config('scout.meilisearch.key'));
	    $prods = Product::all()->toArray();
            $client->index('prods')->addDocuments($prods); // появились документы в панели meilisearch
	13) Заработал поиск Product::search из meilisearch. В общем какая-то хуета, если искать мало, то
            лучше скрипт тот php поиска использовать, он хотя бы окончания обрезает. Возможно здесь есть какие-то настройки как в 
            Эластике для распиления слов на части. 
		
111) ElasticSearch - отдельное приложение наподобие сервера MySQL, синхронизирующее свои данные с БД. Хранит индексированные данные 
		     и взаимодействует с окружением через json строки.
     В MySQL данные хранятся в таблицах и поиск осуществляется в них. В ES вместо таблиц - индексы, поиск осущ. в них.

111) На Бегете предлагают установку поисковой программы Sphinx от 60 руб/мес. Вообще пишут она opensource.

111) array_column() - php функция, возвращает массив значений по полю из массива подмассивов.
                      Работает как связка Product::pluck('name')->all() для коллекций Ларавель
     $arr = [['key1' => 1, 'key2' => 2],['key1' => 3, 'key2' => 4]];
     array_column($arr, 'key1')  => [1, 3]

111) array_filter($arr, $callback) - php функция, оставит в массиве только значения для которых функция вернет true
     Если запустить без функции, то вернутся только непустые значения (авт. приведенные к true):
	array_filter([1, null, 2, false, 3, 0, '', ' ']) => ['0'=>1, '2 =>2, '4'=>3, '7'=>" "]

111) Программный порт — это «виртуальный разъём» в программе, через которую она может обмениваться данными с системой 
     и другими программами. У него нет физического устройства, а есть только число от 0 до 65 535 — оно называется адресом порта. 
     Чтобы при работе в сети программы и сервисы не путали данные между собой, разработчики придумали порты — они определяют,
     к какой программе относятся те данные, которые получает сетевая плата.
     С технической точки зрения порт — это просто число, которое прикрепляется к каждому пакету с данными, поступающими в компьютер.
     Система смотрит, какой номер порта стоит на пакете, находит программу, которая его использует, и отправляет ей. 
     Порт можно пробросить — например, можно научить систему, что если запрос пришёл на порт 1212 — отправить его на порт 2121. 
     Так устроены многие сетевые маршрутизаторы и, в частности, ваш интернет-провайдер. 
     Порты можно просканировать — перебрать все числа от 0 до 65535, чтобы посмотреть, придёт ли с какого-нибудь из них ответ. 
     Если придёт — на этом порте сидит какая-то программа. 

111) Узнать какой скрипт изменяет сайт.
     Выбрать элемент -> правая кнопка -> Break on -> выбрать на каком этапе остановиться и перейти к скрипту:
	Subtree Modifications - добавление, удаление или перемещение дочернего элемента
	Attributes Modifications - изменяются атрибуты элемента (класс, id и др)
 	Node Removal - удаление элемента
     Нужно правильно выбирать целевой элемент. Потому что если выбрали Subtree Modifications, то будет останавливать ТОЛЬКО
     на добавлении, удалении, перемещении дочерних узлов, но никак не на изменении их классов и пр.
     Изменение класса дочернего элемента не остановит, если точка остановы отслеживает изменение родительского класса.
     Точки остановы отслеживают только действия скриптов. 
     

     Убрать точку остановы: либо в Break on удалить галочку, либо удалить точку в Source->DOM Breakpoints 

111) Встроенная в Ларавель проверка на null:
	optional($obj)->var => если объект null, то такое обращение к свойствам/методам несуществующего объекта вернет null

111) $request->get('value', 'default') - если свойства не задано, то вернется default

111) Если контроллер или др. класс предполагает получение и работу с объектом (например Моделью), то можно встраивать эту модель
     в конструктор этого контроллера
	private $user;
	public function __construct(User $user){ - или сервисный класс какой-нибудь
        	$this->user = $user;}
	Соответственно, во всех методах контроллера будет доступен текущий пользователь:
	$this->user - пользователь
	$this->user->func() - и его свойства и методы

111) Автоматическое встраивание зависимостей от Ларавель:
	public func(User $user){} - если в такую функцию передать id пользователя (func(23)), то Ларавель авт. найдет пользователя по такому id
                                    и в функции будет готовый пользователь (не надо будет его получать как User::find(23))
			            Главное указать что ждем объект класса User $user

111) [$width, $height] = [11, 22] - такое присваивание работает также в php

111) Строки в коде правильнее хранить в константах:
	public const USER_STATUS = 'active'
     Теперь проверять соответствие в коде можно так:
	if($user->status === USER::USER_STATUS){}
	if($user->status === self::USER_STATUS){}

111) Можно сначала писать код, модели, а в конце под логику создавать таблицы в БД (Code first). Вместо того, чтобы добавлять миграции под каждое новое поле
     при написании кода (DB first)

111) Для платежных систем на packagist есть готовые пакеты (вводим к примеру robokassa)
		
111) md5() - php функция хеширования. Создает символьный оттиск данных или «сумм», при помощи которого можно проверить подлинность данных.

     Например для приема платежа от пользователя ему для оплаты генерируется ссылка http://robokassa.ru?.
     Хешируем сумму заказа 500 руб. и наш секретный ключ qwerty: md5('500:qwerty') => 4bd5f2ed08627bd61455fcee4d20d1e8
     Гет параметры:
	$query = [$sum => 500, $signature => '4bd5f2ed08627bd61455fcee4d20d1e8']
     Пользователю даем ссылку: http://robokassa.ru? . http_build_query($query); => http://robokassa.ru?sum=500&signature=4bd5f2ed08627bd61455fcee4d20d1e8
     Теперь на сервере будем точно знать подлинная ли ссылка, не изменена ли сумма и др.
     Также можно хранить пароль пользователя в БД в виде хеша.
     Нужно иметь ввиду, что при хешировании через md5() возвращается одно и то же значение:
	md5('11') => одно и то же значение при перезагрузке страницы

111) Hash::make() - функция для хеширования данных в Ларавель. Настраивается в config/hashing.php.
                    По умолчанию используется алгоритм bcrypt. Дает разные хеши для одного и того же значения при перезагрузке, что безопаснее.
     $hash = bcrypt('123'); // будет работать, но в Ларавель надо хешировать так: Hash::make('123')
     Hash::check('123', $hash) => true

111) Crypt::encryptString('...') - шифрование
     Crypt::decryptString('...') - расшифровка

     Не путать хеширование и шифрование. В шифровании можно расшифровать значение. Шифруется на основе APP_KEY. Также получается всегда разное 
     значение, но все равно расшифровывается.

111) POST запросы на страницы Ларавель проходят проверку на csrf токен. 
     Чтобы некоторые страницы не проверяли токен:
	app/http/middleware/verifycsrftoken =>  protected $except = ['payment/*'] - теперь для страницы http://наш_сайт/payment/result токен не нужен
     Это может понадобиться для сервисов оплат, которые присылают результат об успешности оплаты POST запросом

111) function test(?string $par1, string $par2, ?User $user){} - знак вопроса в параметрах означает, что в параметр можно отправлять null
	
     function func(?string $var){ var_dump($var); }
     func(null) => NULL - передали null, не будет ошибки

111) У объекта $request есть все данные о запросе. Например:
     $request->userAgent() => Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/113.0.0.0 Safari/537.36
     $request->ip() - ip 

111) oAuth2 регистрация и аутентификация.
     Смысл в том, что пользователю предоставляется возможность зарегистрироваться на сайте под аккаунтом вк и в дальнейшем заходить на сайт 
     нажимая иконку ВК. При регистрации под аккаунтом ВК он возвращает нам id в ВК, имя, email, аватарку пользователя и мы по ним регистрируем нового
     пользователя (некоторых данных может не быть). 
     Соответственно у таких пользователей сначала нет пароля, а у встроенной users он обязателен. Кроме того может не быть email (если регистрация по
     была по телефону). Поэтому для того, чтобы не делать отдельную таблицу для таких пользователей (а тогда не будет работать встроенный Auth::),
     нужно изменить таблицу users: сделать password и email nullable (не бояться - через обычную регистрацию не получится оставить их пустыми, так как стоит валидация required)
	1) php artisan make:migration alter_users --table=users
		$table->string('email')->nullable()->change();
            	$table->string('password')->nullable()->change();
            	$table->string('avatar')->nullable();
            	$table->string('phone')->nullable();
	2) Создать таблицу связей пользователя с аутентификацией через сети:
		php artisan make:model UserNetwork -m
	3) В модели UserNetwork:
		protected $table = 'user_networks';
		public $timestamps = false;
	4) В миграции для user_networks:
		$table->integer('user_id')->references('id')->on('users')->onDelete('CASCADE');
            	$table->string('network');
            	$table->string('identity');
            	$table->primary(['user_id', 'identity']);
	5) php artisan migrate
	6) В модели User добавляем:
		public function networks(){
        		return $this->hasMany(UserNetwork::class, 'user_id', 'id');}
	7) composer require laravel/socialite - пакет Socialite от Ларавель
	8) composer require socialiteproviders/vkontakte - пакет для вк (https://socialiteproviders.com)
	9) Добавляем в config/services.php:
		'vkontakte' => [    
  			'client_id' => env('VKONTAKTE_CLIENT_ID'),  
  			'client_secret' => env('VKONTAKTE_CLIENT_SECRET'),  
  			'redirect' => env('VKONTAKTE_REDIRECT_URI') ],
	10) В app/Providers/EventServiceProvider в массив listen добавляем:
		 \SocialiteProviders\Manager\SocialiteWasCalled::class => [
            		\SocialiteProviders\VKontakte\VKontakteExtendSocialite::class.'@handle'],
	11) Создаем контроллер:
		php artisan make:controller SocialController
	12) Создаем пути на два метода этого контроллера:
		Route::group(['middleware' => 'guest'], function (){
    			Route::get('login/{network}', [SocialController::class, 'redirect'])->name('network');
   			 Route::get('login/{network}/callback', [SocialController::class, 'callback'])->name('network.callback');});
	10) Сам контроллер:
		class SocialController extends Controller {
    			public function redirect($network){
        			return Socialite::driver($network)->redirect();} - метод перенаправления на вк для разрешения аутентификации

    			public function callback($network){ - срабатывает когда пользователь разрешил вход через вк
        			$socialUser = Socialite::driver($network)->user(); - вк возвращает объект своего пользователя (id, имя и пр)
        			$socialId = $socialUser->getId(); - вк Id
        			if($data = UserNetwork::where('identity', $socialId)->where('network', $network)->first()){ // ищем запись в таблице user_networks
            				Auth::loginUsingId($data->user_id);
            				return redirect()->back();}
        			if($socialUser->getEmail() && (User::where('email', $socialUser->getEmail()))->exists()){ // если полученный email от вк уже зарегистрирован как email для обычной аутентификации
            				return redirect()->back()->with('error', 'Пользователь уже зарегистрирован');}

        			$user = DB::transaction(function () use ($network, $socialUser){
            				$user = User::create([
                			'name' => $socialUser->getId(), ]); //в имени пишем вкАйди. можно дописать логику вставки аватара, телефона и пр.
           
            			$user->networks()->forceCreate([
                			'network' => $network,
                			'identity' => $socialUser->getId(),]);
            			return $user;});
        			Auth::login($user);
        			return redirect()->intended();}}
	11) Регистрируем как сайт на https://vk.com/editapp?act=create
	12) После регистрации заходим во вкладку Настройки
	13) в .env:
		VKONTAKTE_CLIENT_ID=51652995 - из id в Настройках
		VKONTAKTE_CLIENT_SECRET=EQ3z3T1diW9du7uA1JJr - из Защищённый ключ
		VKONTAKTE_REDIRECT_URI=http://localhost:8000/login/vkontakte/callback - составляем из роута коллбэка
	14) В Доверенный redirect URI пишем то же, что в VKONTAKTE_REDIRECT_URI
	15) Вставляем ссылку на вход через ВК: 	
		@guest <a href="{{ route('network', ['network' => 'vkontakte']) }}">Вход через ВК</a> @endguest
	16) Теперь по ссылке откроется вк форма входа, разрешаю, все работает. Однако проверить код, чтобы всякие ошибки не выскакивали, редиректы
            были на нужные страницы. По email из вк лучше в своей системе не входить, так как неизвестно как вк подтверждает, что это настоящий email.
            Также сделать контроллеры тонкими, а код перенести в сервисы. ВК проверяет разрешение только первый раз, затем сразу входит (видимо через
            сессию)
	
111) @dd(get_defined_vars()) - посмотреть в blade все полученные переменные

111) Использование СервисКонтроллеров:
	1) php artisan make:controller Services/UserService
	2) class UserService extends Controller {
    		public function getUser($id){ - обычный метод
        		return User::findOrFail($id);}}
		public static function showUser($id){ - статический метод
        		return User::findOrFail($id);}
	3) Встраиваем сервисконтроллер: - советуют так
		public function index(UserService $user) { - инъекция сервиса
        		return $user->getUser(13);} - пользуемся методом сервиса
	4) Или используем статический метод: - пишут, что статика не рекомендуется. А фасады Лары - не статика, а использование __callStatic
		public static function showUser($id){
        		return User::findOrFail($id);}
	5) Если мы используем сервис в нескольких методах контроллера, то, чтобы не встраивать сервис в каждый метод можно
           встроить его в приватное свойство контроллера через конструктор:
		class MainController extends Controller {
    		   private $service;
    
    		   public function __construct(UserService $user){
        		$this->service = $user;} - встраиваем один раз 
    
    		   public function index(){
        		return $this->service->getUser(14);}} - используем
		
111) swagger-php - пакет для авт. создания документации по api методам приложения

111) Узнать координаты через js.
     !!! navigator работает только на https.
     Сначала всплывает окошко: Разрешить доступ к геоданным. Затем оно не показывается.
     Для настольного компьютера координаты определяются неточно, возможно по провайдеру. Компьютер на Бармалеева, 7 показал 
     координаты рядом с Дворцовой площадью

	if(navigator.geolocation){ - проверка наличия функции в браузере
            navigator.geolocation.getCurrentPosition(
                function (pos){console.log(pos)}, - первая функция срабатывает, если не было ошибок. Здест показывается объект с координатами
                function (error){console.log(error.message)})} - если была ошибка
	
111) Так как все внутри функции js имеет локальную область видимости, то чтобы переменные и функции объявленные внутри {} были доступны снаружи
     можно записывать их в глобальный объект window
	function func(){ window.a = 5}
        func();
	console.log(window.a) => 5

111) Геокодирование в адрес и обратно.
     Внимание! Fetch запросы на другие сайты работают с предварительным запросом preflight, яндекс возвращает нужный заголовок-разрешение, 
     но запрос все равно не работал (BLOCKED BY CLIENT), дело было в блокировщике Privacy Badger. Нужно иметь ввиду, что эти блокировщики запрещают
     аякс запросы на сторонние сайты (поэтому ошибки выскакивают с яндекс-метрикой и пр)
     https://yandex.com/dev/geocode/doc/ru/examples/geocoder_examples - примеры

	let apiKey = '7c9c02ad-211c-4e76-bbdf-f654e63cec96' - с https://developer.tech.yandex.ru/services/3

         if(navigator.geolocation){
             navigator.geolocation.getCurrentPosition(
                 function (pos){
                     getAddress(pos.coords.latitude, pos.coords.longitude)
                 },
                 function (error){console.log(error.message)})}

        function getAddress(lat, lon){
            fetch(`https://geocode-maps.yandex.ru/1.x/?format=json&apikey=${apiKey}&geocode=${lon},${lat}`) - отправляем координаты
            .then(res=>res.json())
            .then(res=>console.log(res)) - яндекс пришлет объект с адресом. Работает и наоборот адрес->координаты
            .catch(e=>console.log(e))}

111) strip_tags — php функция, удаляет теги HTML и PHP из строки. Более настраиваемый способ - пакет purifier

111) <img src="data:image/png;base64,iVBORw0KGgoAAAANSUhEUg..."> - двоичные данные изображения переведены в кодировку base64
     Внимание! Так как url имеет ограничение по длине, что большие изображения не получится так закодировать.
     Картинка 46.37 KB закодировалась в строку 61.82 KB
	Применение base64 для картинок:
	- кодирование картинок в строку <img src="data:image/png;base64,iVBORw0KGgoAAAANSUhEUg...">
	- передача картинок и других данных в XML 
	- хранение изображений в базе данных (нужно понимать что делаешь)
	- включение изображений в email
	- вставка картинок в css: background: url("data:image/png;base64,iVBORw0KG...)
	- получение бинарных данных из canvas в виде текстового base64 представления

111) base64 - простой алгоритм шифрования, когда символы переводятся в двоичные данные (1 и 0), 
     а затем в 64 символа ASCII (A-Z, a-z, цифры 0-9). Или двоичные данные в ASCII
     Это обратимое кодирование, с возможностью простого восстановления.
     base64_encode(«АБВГД») => 0JDQkdCS0JPQlA==
     base64_decode('0JDQkdCS0JPQlA==') => АБВГД
       
111) Если у сущности предполагается несколько областей деятельности, то лучше разделять ее на отдельные, чтобы не создавать
     большой список методов в одной сущности. Например вместо Пользователь создать: Пользователь, Покупатель, Автор, Получатель и т.д.
     Это называется разделение на контексты. Все они могут пользоваться как разными, так и одной таблицей users.

111) Способы увеличить производельность Ларавель
    1) Сделать команды из Оптимизация   
    2) Запустить Nginx Load Balancer, который будет отправлять запросы на разные серверы с php-fpm. 
       Но тогда, так как http запросы от одного браузера отправляются на разные php-fpm серверы, нужно хранить отдельно
       - состояния (сессии) - например на Redis
       - загруженные файлы пользователя - на отдельных серверах для статики. На ютубе для загрузки файлов используется один сервер, а для 
         отдачи есть дополнительные, копирующие данные с первого.
    3) Можно хранить статику в облачных хранилищах, напр. S3 Amazon:
	FILESYSTEM_DISK=s3 или другое, посмотреть пакеты
    4) Сессии в Редисе: 
	SESSION_DRIVER=redis
    5) Кеширование данных Cache::rememberForever и перезапись их (в очереди) отслеживанием событий в БД наблюдателем. 
       Можно также кешировать данные из внешних источников. 	
    6) Файлы кеша в Редисе:
	CACHE_DRIVER=redis
    7) Использование очередей в письмах, уведомлениях и пр.:
        Можно использовать драйвер database (что возможно безопаснее), также redis и др.
	Устанавливаем в Ларавель пакет predis, включаем модуль Редис в openserver
	QUEUE_CONNECTION=redis
	запускаем очереди php artisan queue:work супервизором (его нет в shared хостинге) или кроном + shedule: 
		$schedule->command('queue:work --stop-when-empty')->everyMinute()->withoutOverlapping();
		Либо:
		$schedule->command('queue:work --max-time=300') - но запускать расписание кроном через 5 мин.
	1 способ - с implements ShouldQueue, use Queueable, queue и пр.
	  Вся логика скрыта, применять, если не нужно писать доп. логику, а просто отправить в очередь. 
	  Может вызываться где угодно, не требует дополнительной настройки или создания  классов заданий
	  Например отправка письма: Mail::queue(...)->...
	2 способ - создать класс задания, прописать в нем доп. методы, связанную логику.
          Например: php artisan make:job ProcessPodcast
          Используется для более гибкой настройки, добавления лополнительных связанных действий.
	  PhotoDownload::dispatch($photo) - Отправка в таблицу jobs. Можно выполнять последовательные задачи: загрузка, обрезка, сжатие, отправка email
    8) Испускаем события event(new Order($user)), а в слушателе ставим в очередь отправку уведомлений, кеширование и пр.

111) Бесконечный скрипт:
	while(true){
	   ...
	   sleep(5)}

111) public function func(array $list):string {...} - функция ждет массив, а отдает строку

111) Игнорировать все ошибки:
	try {
	 какой-то код
	} catch(\Exception $e){} - пустое тело и все ошибки игнорируются

111) Добавление метода в класс со стороны. Таким образом можно писать разные методы после создания объекта.
        Файл Loader.php
	     class Loader {
    		public $funcSuccess; - пустое свойство, куда будем добавлять метод из объекта
    		public function load($data){
        		($this->funcSuccess)($data);}} - оборачиваем пустой метод в скобки
	Файл index.php
	     require_once 'Loader.php';
	     $obj = new Loader;
	     $obj->funcSuccess = function ($data){ echo $data; }; - заполняем метод любой логикой
	     $obj->load('Текст'); => 'Текст'
	Файл index2.php
	     require_once 'Loader.php';
	     $obj = new Loader;
	     $obj->funcSuccess = function ($data){ Log::info($data) }; - заполняем метод другой логикой
	     $obj->load('Текст'); => пишем в лог

111) VPS - (Virtual Private Server, виртуальный частный сервер) - виртуализация сервера  на уровне операционной системы
     VDS - (Virtual Dedicated Server, виртуальный выделенный сервер) - предоставляет более высокий уровень изоляции на аппаратном уровне 

111) Убрать последний слеш в url excel (чтобы не было дублей строк):
	1) В столбце B в ячейку вставляем =ЕСЛИ(ПРАВСИМВ(A1;1)="/";ЛЕВСИМВ(A1;ДЛСТР(A1)-1);A1)
	2) Расширяем, получаем в столбце B строки без слешей
	3) Копируем столбец B и вставляем в другой через Специальная вставка -> Значения (иначе ссылками копировались)

111) Разбить строку по разделителю: данные -> текст по столбцам -> выбираем по какому разделителю
     Мне это понадобилось, чтобы убрать лишние url после запятой

111) Экспорт из mysql в csv. Для нынешней БД работает:
	Метод - обычный, Формат - CSV for MS Excel, кодировка - windows-1251

111) Импорт csv в mysql (если есть CSV UTF-8):
	1) Должно быть равное количество столбцов, т.е. в эксель вставить через прогрессию id
	2) В файле эксель: Файл -> Экспорт -> Изменить тип файла -> Сохранить как -> CSV UTF-8 (разделитель запятая) 
	3) В phpMyAdmin: Импорт -> Выбираем файл -> Формат: CSV -> в Разделитель полей ставим точку с запятой -> Вперед
     Если экспорта в CSV UTF-8 нет:
	1) Файл -> Экспорт -> Изменить тип файла -> Сохранить как -> Текст Юникод (*.txt)
	2) Открываем блокнотом и Правка, Заменить, выделяем промежуточный пробел, заменяем его на запятую для всех полей
	3) Файл -> Сохранить как -> file.csv, тип файла "Все файлы", кодировка UTF-8 или UTF-8 со спецификации
	4) При импорте в mysql разделитель - запятая
	5) Статья: https://www.ablebits.com/office-addins-blog/convert-excel-csv/

111) Экспорт excel из mysql:
	1) Заходим в таблицу, нажимаем Экспорт
	2) Выбираем Обычный метод (при быстром способе выгрузка была до первой русской строчки)
	3) Выбираем тип файла csv for excel
	4) Кодировка файла: utf-8
	5) Проверяем, что экспортировались все строки

     Были кракозябры на русских буквах, решались открытием csv в notepad и изменением кодировки на ANSI
     (кодировки -> преобразовать в ANSI)

111) Убрать последний слеш в столбце excel:
	=ЕСЛИ(ПРАВСИМВ(A1;1)="/";ЛЕВСИМВ(A1;ДЛСТР(A1)-1);A1)
     Убрать затем формулу:
	1) Копируем весь получившийся столбец
	2) Специальная вставка, вставить значения
     Убрать длинные строки:
	1) В соседнюю ячейку =ДЛСТР(A1) и растягиваем
	2) Упрядочиваем
     Проверяем, что нет пробелов перед текстом сайта, иначе:
	=ПОДСТАВИТЬ(B1;" ";"" ;1)

111) Пометка строки содержащей подстроку:
	=ЕСЛИ(ЕСЛИОШИБКА(ПОИСК("что ищем"; A1); 0) > 0; "Удалить эту строку"; "")

111) Улучшенная пометка строки, содержащей подстроку из диапазона:
	=ЕСЛИ(СУММ(ЕСЛИОШИБКА(1*ЕЧИСЛО(ПОИСК(E$1:E$2;B1));0))>0;B1;"")   // в E - диапазон исключений
     После вставки формулы в поле нажимаем Ctrl + Shift + Enter, иначе не работает
     Не забываем менять диапазон в формуле при вставке новых исключений

Например, исключаем:
---
.edu
.gov
.mil
.ru-
1c.ru
1cbit
abuse
admin
alert
autoresponder
beget
bounce
calendar.google.com
com--
domain
error
example
feedback
host
hosting
kopirka.ru
mailer-daemon
metrica.com
nic.ru
noreply
no-reply
notify
postmaster
pulscen.ru
rating
reg.ru
reglet.ru
report
robot
sentry
stacks
subscription
support
timeweb
tiu.ru
unsubscribe
valuehost.ru
vk-portal
warning
wix.com
wixpress
yougile
masterhost.ru
copy.ru
email.com
email.ru
pechati.ru


111) Найти все строки, которые есть в другом столбце:
	=СЧЁТЕСЛИ(A:A;B1)  - ищет строку B1 в указанном диапазоне

111) Получить самую длинную строку из БД:
	$city = DB::select('SELECT name FROM cities ORDER BY LENGTH(name) DESC LIMIT 1');

111) Скрипты в Ларавель перестают работать через 60 секунд.
     Ставим в контроллере:  set_time_limit(30000) и все работает

111) Пакет intervention/image для редактирования картинок:
	1) composer require intervention/image --dev
	2) В config/app.php => $providers добавляем: Intervention\Image\ImageServiceProvider::class
	3) Ниже в $aliases добавляем: 'Image' => Intervention\Image\Facades\Image::class
	4) php artisan vendor:publish --provider="Intervention\Image\ImageServiceProviderLaravelRecent"
	5) В config/image.php меняем расширение на Imagick

111) Создание банеров со вставкой города есть в "Полезное/банеры с вставкой города циклом"

111) Красивая анимация кнопки/банера:
	<div class="page-banner"><img src="..."></div> - здесь картинка черного цвета
	.page-banner {
    		max-width: 80%;
    		box-shadow: 0 0 10px #eee;
    		position: relative;
    		overflow: hidden;}
	.page-banner:before {
    		content: "";
    		position: absolute;
    		top: 0;
    		height: 100%;
    		width: 30%;
    		transform: skewX(45deg);
    		left: -150%;
    		background: linear-gradient(90deg, rgba(255, 255, 255, 0.05), rgba(255, 255, 255, 0.2), rgba(255, 255, 255, 0.05));
    		animation: flareAnimation 5s infinite linear;}
	@keyframes flareAnimation {
    	    0% { left: -150%; }
    	    100% { left: 150%;}}

111) Припарковать новый домен на хостинге для Ларавель.
        Как будет вести себя Ларавель? Ведь route() и asset() берут домен из контейнера app('url'), а он создается на основе APP_URL
	Возможно как-то менять url на этапе сборки контейнера в зависимости от запрашиваемого Host
        config()->set(['app.url' => $url]).
     Либо это Ларавель делает самостоятельно.	

111) Поместить в верстку теги, чтобы браузер не воспринимал их как команды а показывал строкой:
	&lt;img src="ссылка"&gt; => <img src="ссылка на логотип">

111) Эффекты на сайт смотреть на codepen, делаются через canvas, например: https://codepen.io/tmrDevelops/pen/RWLbEy

111) Подчеркивание слов через background:
	1) Слово оборачиваем в span
	2) Спану даем стили: background: linear-gradient(181deg,transparent 0,transparent 70%, #92ebff 70%);
  	3) background идет сверху вниз, чуть перекошен на 1 градус (линия будет не одной толщины)
	   от верха слова на 70 процентов его высоты будет прозрачный цвет, а ниже 30% высоты слово будет 
	   закрашено, получится как будто подчеркивание

111) Медленная отрисовка линий svg:
	1) Обводим текст или рисунок в Кореле с помощью B-Сплайн. Это нужно чтобы рисунок был векторной линией
           Обводить сразу нужной толщиной, чтобы потом при утолщении через stroke-width рисунок не вышел из viewBox
	2) ctrl + E, выбираем svg 
           Настройки:
		- Параметры моделирования: "атрибуты презентации" (выбрать лучше это, стили будут в тегах <path stroke="#2B2A29">)
					   или "лист в внутренним стилем" (тогда стили тегов будут через классы) 
		- Экспортировать текст как кривые (это если текст не отрисовывали как безье) 
	 	- Настройки документа ставим в пикселях
	
	5) Вставляем код svg в html файл и удаляем лишние теги проверяя, что рисунок отображается верно
	6) Теперь нужно вычислить длину линий path. Задаем им id и вычисляем:
		console.log(Math.round(one.getTotalLength())) => 165
    		console.log(Math.round(two.getTotalLength())) => 150
	   Эти числа нам понадобятся для того, чтобы прописать параметры в path:
		stroke-dasharray="165" stroke-dashoffset="165" - первая линия
		stroke-dasharray="150" stroke-dashoffset="150" - вторая линия
           stroke-dasharray и stroke-dashoffset точно не понял для чего, но задавать по длине, чтобы косяков не было.
	   При уменьшении изображения (просмотр на телефоне) длина сохраняется.

	   В итоге получится так:
	    <svg xmlns="http://www.w3.org/2000/svg"  width="500" height="515" viewBox="0 0 100 100">
  	       <path id="one" fill="none" stroke="#2B2A29" stroke-width="4"  d="..." stroke-dasharray="165" stroke-dashoffset="165">
      		  <animate attributeName="stroke-dasharray" begin="1s" dur="1s" values="0,165;165,0" fill="freeze"></animate>
    	       </path>

    	      <path id="two" fill="none" stroke="#2B2A29" stroke-width="4"  d="..." stroke-dasharray="150" stroke-dashoffset="150">
		 <animate attributeName="stroke-dasharray" begin="2s" dur="1s" values="0,150;150,0" fill="freeze"></animate>
	      </path>     
	    </svg>

	   Параметр attributeName="stroke-dasharray" означает, что анимация будет для длины линии и длины пробела
	   values="0,165;165,0" - в начале анимации длина линии (stroke) равна 0px, а пробел (dasharray) равен 165px,
				  то есть линия скрыта. В конце пробел 0px, а длина линии 165px
	   begin="1s" dur="1s" - анимация начнется через 1 cек. и будет длиться 1 сек.
	   fill="freeze" - нарисованное останется как есть, не исчезнет

           Если в начале анимации в конце конец линии (кусочек), то установил длину равную указанной во viewBox и это пятно пропало

           Анимация будет работать даже если svg вставить в img:
		<img  src="template/images/wave.svg">

	   Примеры естть в практика/svg отрисовка текста.

111) Чтобы svg повлялась и исчезала при прокрутке лучше всего вставлять его через js, иначе то исчезает, то нет, видимо кешируется браузером.
	Делал так:
	 const observer = new IntersectionObserver((entries) => {
        if (entries[0].isIntersecting) {
            if (!svg) {
                svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
                svg.setAttribute('width', '424');
                svg.setAttribute('height', '25');
                svg.setAttribute('viewBox', '0 0 424 25');
                svg.setAttribute('fill', 'none'); и так далее

111) Как обводить текст:
	1) Заходим на x-lines.ru/letters/cyrillic/script.html (браузер что-то начал качать фоном, проверяем), пишем, текст 
	2) Браузер сохранять не дает, поэтому копируем адрес картинки и сохраняем через новую вкладку
	3) Выбираем B-Сплайн, обводим по центру шрифта. Ставим точки на изломах. В конце формой можно менять углы.
	4) Делаем желтый цвет линии, подбираем толщину под толщину шрифта на картинке, правим 

111) Советы по SVG обводке:
	1) Обводка Художественным оформлением получается красиво, разной толщиной
	2) !!! Шрифты и другие дуги очень удобно обводить с B-Сплайн. Просто тыкаем точки, как раньше делал с факсимиле,
           а корел сам плавно их соединяет.
	
111)    svgartista.net - медленно показывает svg картинки
        vertopal.com - преобразует текст, картинки в svg 
        serblog.ru/demo/format-html/ - умеет форматировать svg код в читаемый вид
	x-lines.ru/letters/cyrillic/script.html - шрифты русские рукописные. Браузер что-то начал качать фоном, херня какая-то

111) Запрет индексирования для Яндекса:
	1) <!‐‐noindex‐‐> ... <!‐‐/noindex‐‐> - Понимает Яндекс, для Гугла это не сработает
	2) <noscript> ... </noscript> - содержимое не проиндексируется и не выведется на экран. Однако, выведется, если скрипты JS отключены

111) Запрет индексирования всей страницы для Яндекса и Гугла:
	  <meta name="robots" content="noindex, nofollow" />
	  <meta name="robots" content="none" /> - запрет на весь сайт

111) <noscript>В браузере отключен JS</noscript> - используется для предоставления альтернативного контента пользователям, 
						   у которых отключены скрипты в браузере или браузер их не поддерживает. 
					           В остальных случаях браузер игнорирует этот тег и всё, что располагается внутри него.

111) window.addEventListener('scroll', func, {once: true}) - once означает, что функция вызовется только на первом прокручивании или др. событии

111) Передача параметров в функцию обратного вызова addEventListener:
     В первых трех случаях не получалось отвязать, в 4 случае смог отвязать

	1) elem.addEventListener('click', () => func(param)) - но не сможем отвязать через removeEventListener
	2) elem.addEventListener('click', func.bind(null, 1, 2)) - передаем параметры 1 и 2. Не отвязывается
	3) elem.addEventListener('click', {handleEvent: func, a: 1, b: 2}) - где func: function func(){ let var = this.a, ... }. Не отвязывается
	4) let func;
	   elem.addEventListener('click', func = function (){ callback(param) })
	   elem.removeEventListener('click', func) - получилось отвязать

111) Правила повтора в css через nth-child:
     Допустим у нас пять разных галочек и нужно, чтобы галочки начали повторяться, если пунктов списка становится больше 5. Тогда:
	ul li:nth-child(5n+1):before { - такая галочка будет для 1 пункта и 6 если он появится
		background-image: url(mark1.svg); background-repeat: no-repeat; background-size: contain; }
	ul li:nth-child(5n+2):before { - такая галочка будет для 2 и 7 если он появится
		background-image: url(mark2.svg); background-repeat: no-repeat; background-size: contain; }

111) Иногда событие клика лучше привязать к обертке элемента, чем к нему самому. Потому что на мобильных устройствах активный
     нажатый элемент выделяется серым фоном. Если у обертки есть паддинг, то узкая выделенная строчка элемента выглядит некрасиво. 

111) Удобно быстро открывать блок по высоте, но медленно скрывать можно навесив два состояния:
	Начальное: max-height: 0;
	Конечное: max-height: 1000px;
	transition: all .3s ease;

     Как это работает:
	Блок имеет свою какую-то высоту, например, 100px.
	Но браузер для расчета скорости transition использует цифру max-height: 1000px.
	При открытии блока браузер начинает увеличить его высоту с 0 до 1000pх за .3s, но останавливается как только
        достигнет 100px - соответственно открывается быстро
	А при закрытии начинает уменьшать высоту блока от 1000px до 0, и закрывает его не сразу.

111) У табличек по умолчанию display:table. Чтобы они начали прокручиваться вбок при уменьшении экрана нужно задавать display: block:
	overflow-x: auto; - скролл появится при уменьшении экрана
        white-space: nowrap; - не переносим слова в ячейках
        display: block; 

111) Если при компиляции Ларавель не обнаружит запрашиваемой вьюхи, то вылетает ошибка и сервер вернет браузеру (боту яндекса) 500 ошибку
     Чтобы избежать этого можно втавлять вьюхи через @includeIf:
	@includeIf('parts.links') - если вьюхи parts.links не будет, приложение не вылетит с ошибкой

     Внимание! У @include нет значения по умолчанию @include('page', null)  - не сработает если вьюхи не существует и будет 500 ошибка сервера. 
     Во втором параметре у @include вставляются переменые для вьюхи, а не значение по умолчанию: @include('page', [$var => 1])

     Значение по умолчанию есть только у @yield ('page', default)

111) Удобно через @include вставлять шаблоны в цикле:
	@foreach ($posts as $post)
    		@include('parts.post',['data' => $post]) - теперь карточку поста можно держать в отдельном шаблоне
	@endforeach
	
111) Также при удалении вьюхи, если не знаешь где она переиспользуется можно вместо ее удаления писать в ней return null или делать файл пустым

111) Названия файлов картинок можно по-русски: коробки-заказ.jpg 

111) У svg картинок можно прописывать всплывающую подсказку title:
	<svg>
	   <use href="{{ asset('storage/images/sprite.svg#search') }}"></use>
	   <title>Подсказка</title>
	<svg>

	<svg xmlns="http://www.w3.org/2000/svg" ...>
	   <g>
	      <title>Подсказка</title>
	      <path d="M 179.89754857473488 ....></path>
	   </g>
	</svg> 

111) Сбросить все стили кнопки:
	button {
  		all: unset;
  		cursor: pointer;}

	button:focus {
  		outline: blue 1px auto;}

111) Плавно прокрутить страницу вверх:
	window.scrollTo({ top: 0, behavior: 'smooth' });

111) Ссылки якори:
	<h2 id="elem">Типирование</h2> - даем id тегу к которому переходить

	<a href="#elem">перейти к типированию</a> - якорная ссылка на той же странице
	<a href="https://site.ru/index#elem">перейти к типированию</a> - якорная ссылка на другой странице
	<a href="{{route('index')}}#elem">перейти к типированию</a> - якорная ссылка на другой странице в Ларавель

     Для того, чтобы прокрутка была плавной:
	html { scroll-behavior: smooth; }



111) Классы js тоже несложные:
	class MyClass {
		constructor(myVar){ - запускается при создании объекта класса
			this.height = myVar; - записываем переменную
			this.func(); } - запускаем функцию из класса
		func(){
			console.log(this.height)}}
	new MyClass(183) => в консоли выведется 183

111) class MyClass {
		 constructor(){ ... }
		 async send() { await fetch(...).then(...).catch(...).finally(...) }} - асинхронный fetch запрос
	
111) Права доступа:
	Три цифры:
		1) Для владельца user
		2) Для члена группы groups
		3) Для остальных пользователей
	 0 - запрещено все (-)
	 4 - чтение (r)
	 2 - запись (w)
	 1 - исполнение, запуск файла (x)

     Право получается сложением цифр:
	7 - разрешено все (4+2+1) (rwx)
	6 - разрешено чтение и запись (4+2) (r-x)
	4 - разрешено чтение (r)
	3 - разрешены запись и выполнение (2+1) (-wx)
	2 - разрешена запись (w)
	1 - разрешено выполнение (x)

     Например:
	700 - владелец может делать все, остальным все запрещено (rwx------)
	777 - разрешено все для владельца, группы и других пользователей (rwxrwxrwx)
	776 - разрешено все для владельца, группы, а остальные пользователи могут читать и записывать
	755 - владелец может делать все, остальные читать и исполнять файл
	644 - разрешено чтение и запись для владельца, группа и другие пользователи могут только читать
	444 - разрешено только чтение для всех

     Обычные права:
	Для файлов 644 - владелец может читать и записывать файлы (исполнять запрещено), члены группы (group) и остальные (others) могут лишь их читать (rw-r--r--)
	Для папок 755 - владелец может делать все, а группа и другие пользователи имеют доступ к директории, 
                        могут просматривать названия и атрибуты файликов, однако, не имеют прав их записывать, 
                        изменять названия и удалять (rwxr-xr-x)

     Рекомендации для папок и файлов CMS:
	777 - Для каталогов, где постоянно происходит запись и стирание файлов (например, для папки кэширования)
	755 - Применительно к папкам, в которые постоянно записываются файлы, но не удаляются
	666 - Для файлов, куда необходимо время от времени добавлять запись (пример — файл .htaccess)
	644 - Для файлов, используемых только для чтения (.php, .html и др.)
     
111) У 	инпутов checkbox и radio можно ставить value, оно придет значением в запросе.
     Если value нет, то для отмеченных инпутов приходит значение on

     Для option-ов у селекта если есть value приходит оно, если нет, то приходит текст option
	
111) api.dicebear.com/7.x/croodles/svg?seed=случайная строка - создание случайных аватарок

111) <template></template> - скрытый код html, содержимое которого может быть вставлено в верстку через js
 
     Пример:
	<template id="template">Текст</div> - браузер его не показывает

        document.querySelector('body').append(template.content) - вставили содержимое template в тег body

111) Промис - способ выполнения задержанной асинхронной задачи через объект "промис" у которого меняются свойства-состояния
              в зависимости от того, что вернула задача
	
	let promise = new Promise((resolve, reject)=>{
        	здесь задержанная задача, которая:
		- при успехе возвращает resolve(var)
		- при ошибке возвращает reject(var)
	})

	Теперь следим за состоянием объекта промиса через then:
	   promise.then(function(success){...}, function(error){...}):
	   - если задача вернула resolve() => в объекте-промисе прописалось состояние fulfilled/успешно => вызывается первая функция
	   - если задача вернула reject() => в объекте-промисе прописалось состояние rejected/ошибка  => вызывается вторая функция
			
	Эти две функции в короткой записи:
		promise.then(success => {...}, error => {...})

	Либо:   promise.then(success => {...}).catch(error => {...}) // что то же самое, что .then(null, error)

	После успеха/неуспеха можно запустить finally(() => {...})

111) Более удобный способ выполнения задержанной задачи - промиса через async/await:
     1) Если работаем с промисом - задержанной асинхронной задачей, то обозначаем такую функцию как async, 
        можно как самовыполяющуюся: (async () => {})()
     2) вместо promise.then используем await - команда на ожидание момента когда объект Промис перейдет в состояние завершения
     3) ловим ошибки: try {...} catch(err) {...}

     Пример обычного промиса:
     let promise = new Promise((res, rej) => {
	 setTimeout(() => {
	     let num = Math.random()
             num >= 0.5 ? res(num) : rej('меньше 0.5')
        }, 1000)})

      promise.then(success => { console.log(success) }).catch(error => { console.log(error)})

      То же на 	async/await:
      async function func() {
	 try {
	     let promise = new Promise((res, rej) => {
	     setTimeout(() => {
	         let num = Math.random()
		 num >= 0.5 ? res(num) : rej('меньше 0.5')
	     }, 1000)})

            let result = await promise // вместо promise.then пишем код построчно
            console.log(result)
	 } catch (e){ console.log(e)}}

	func()	

     Пример обычного промиса:
	fetch('https://site.ru', {...})
           .then(res => res.json())
           .then(res => console.log(res))
           .catch(e => { console.log(e) })
 
     Код  async/await:
	(async () => {
	   try {
	       let res = await fetch('https://site.ru', {...})
	       let data = await res.json()
	       console.log(data)
	   } catch (e) { console.log(e) }})()
	
111) Импортирование в JS:
	Скрипт module.js: export default { a: 5 }
	
	Файл html: <script type="module">
    			import obj from './module.js'
			console.log(obj)
		   </script>

111) Во вью написано, что для трансформаций можно использовать аппаратное ускорение если добавить одно из свойств:
	perspective: 1000px;
	backface-visibility: hidden;
	transform: translateZ(0);

111) cubic-bezier.com - сайт настройки кубик безье

111) web.dev/articles/lazy-loading-images?hl=ru - ленивая/отложенная загрузка изображений через IntersectionObserver

111) Порядок ленивой загрузки изображений:
	1) У изображения по умолчанию в src стоит легкая заглушка, а в data-src - путь к изображению
		<img class="lazy" src="images/default.jpg" data-src="images/1.jpg">
	2) При загрузке страницы ищем все изображения с классом lazy и навешиваем наблюдатель:
	   как только изображение становится в области видимости изменяем его src на текст из data-src - браузер сам начинает загрузку
	
	document.addEventListener("DOMContentLoaded", function() {
		let lazyImages = [].slice.call(document.querySelectorAll("img.lazy"));

		if ("IntersectionObserver" in window) {
			let lazyImageObserver = new IntersectionObserver(function(entries, observer) {
				entries.forEach(function(entry) {
					if (entry.isIntersecting) {
						let lazyImage = entry.target;
						lazyImage.src = lazyImage.dataset.src;
						lazyImage.classList.remove("lazy");
						lazyImageObserver.unobserve(lazyImage);
					}
				});
			});

			lazyImages.forEach(function(lazyImage) {
				lazyImageObserver.observe(lazyImage);
			});
		} else {
			// Possibly fall back to event handlers here
		}
	});

111) Содержимое тега template на странице не показывается, но мы может использовать его в скрипте:
	<template id="elem">
    		<img class="lazy" src="images/default.jpg" data-src="images/1 (1).jpg">
	</template>

	Два способа получения содержимого template:
	 1)  let node = document.importNode(template.content, true) - тут ссылка на #document-fragment
	 2)  let node = template.content.cloneNode(true)

	let img = node.querySelector('img') - тут ссылка на ноду img
	
	document.querySelector('#parent').appendChild(node) - вставляем изображение в див

111) В template можно поместить также стили и скрипты, то есть удобно писать верстку для вставки через js:
	<template>
  	  <style>
    	    p { font-weight: bold; }
  	  </style>
  	  <script>
    	    alert("Привет");
  	  </script>
	</template>

111)  URL-адреса, предоставленные пользователями должны обрабатываться перед вставкой в БД

111) Пакет sanitize_url очищает URL, чтобы его можно было безопасно использовать при редиректах или хранить в базе данных
	
111) window.history (или history) - объект для управления историей переходов в текущей вкладке. 
			            Каждая вкладка в браузере имеет свой объект history

     history.length - количество элементов в истории вкладки (при заходе 1, затем 2 и т.д.)
     history.back() - перейти на предыдущую страницу (кнопка "Назад" в браузере)
     history.forward() - перейти к следующей странице (кнопка "Вперед")
     history.go(-2) - перейти на 2 страницы назад
     history.go(1) - на следующую (то же history.forward())
     history.go() - обновление страницы (подобно location.reload())

     history.pushState([1,2,3], '', '/2.html') - добавление в историю нового адреса

     Пояснения history.pushState:
	1) В первом аргументе можно прокинуть данные (state), которые присоединятся к этой новой странице. 
           Используются они для того, чтобы создать страницу с каким-то состоянием. Например страницы сайта формируются
           путем ajax запросов на сервер. Мы создаем элемент истории с заранее прописанными данными, как будто страница отправляла 
	   ajax запрос. Либо можно вставить null: history.pushState(null, '', '/2.html')
	2) Добавлять можно url с таким же origin, что и текущий URL-адрес
	3) Адрес в браузере меняется на этот, но перехода не происходит, содержимое страницы само не меняется
	4) Метод используется для AJAX сайтов, где содержимое страницы формируется скриптами

     history.state - получение данных страницы из state => [1,2,3]

     history.replaceState("Привет", null, '/2.html') - изменение state страницы

     popstate - !не работало! событие при переходе по истории браузера (срабатывает на кнопках назад-вперед, history.back(), forward(), go() ) 
		window.addEventListener('popstate', function(e) { console.log('Перешли по истории') });

111) JS модули при работе в браузере без вебпака и ноды:
	1) Файл module.js:
		1) export default { count: 1 } - такой объект можно принимать с любым именем
		2) export const obj = { count: 1 } - такой объект можно принять только по имени

	2) Файл index.html:
		<script type="module">
    		    1) import counter from './module.js' - принимаем с любым именем, если экспорт был default
		    2) import {obj} from './module.js' - так принимаем именованный модуль
		       console.log(counter) => { count: 1 }
		       console.log(obj) => { count: 1 }
		</script>

    Также можно в каком-нибудь script.js файле:
		import {obj} from './module.js'
		console.log(obj)

     И запустить его в index.html:
		<script src="script.js" type="module"></script>

111) Экспорт нескольких переменных из точки сборки.
     Вместо того, чтобы по отдельности импортировать разные файлы js можно их импортировать в один файл, а его уже подключать 
     в нужное место.

	1) В файле api.js пишем переменные. Можно экспортировать несколько переменных:
		export const URL_1 = "https://site1.ru"
		export const URL_2 = "https://site2.ru"

	   В файле router.js другие переменные:
		export const PATHS = {HOME: '/', COCKTAIL: '/cocktails/:id'}

	2) В файле-точке сборки /constants/index.js собираем это и экспортируем наружу одной командой:
		export * from 'api'
		export * from 'router'

	3) В файле script.js принимаем переменные из точки сборки index.js:
		import {URL_1, URL_1} from "/constants"; - по умолчанию ищется файл index.js

111) ?. - js оператор проверяет есть ли у объекта определенное свойство, прежде чем его получить.
	  Без него будет ошибка вызова несуществующего свойства
	  С этим оператором, если свойство не найдено вернется undefined

     let elems = data?.value?.drinks - если у объекта data есть свойство value у которого есть свойство drinks, то вернуть drinks
				       если какого-то свойства нет, то elems = undefined

111) Вычисляемая ширина:
	width: calc((100% - 76px)/3) - для размещения трех элементов с расстоянием между ними 38px (38px назначаем через gap)
				       то есть: элемент - 38px - элемент - 38px - элемент	
		  
111) .elem:nth-child(3n + 3) - стили для каждого третьего, шестого, девятого и т.д.

111) !! - приведение значения к логическому типу (true/false):
	  этот оператор можно заменить на Boolean()
	!![] => true, потому что [] - истинное значение в js
	!!{} => true
	!!null => false
	!!undefined => false 
	!!0 => false

111) const today = new Date().toLocaleString('ru-RU', { // приведение даты
		  weekday: 'short', 
		  year: 'numeric', 
		  month: 'long', 
		  day: 'numeric'})  => чт, 26 октября 2023 г.
	
111) Тег в верстке превращается в объект DOM, к этому объекту можно обращаться сразу в инлайн коде в this:
	<img src="1.jpeg" onload="console.log(this); this.className='pic'">

111) padding-bottom заданный в % будет рассчитываться от ширины элемента. С его помощью можно задавать пропорции элемента:
	div {padding-bottom: 50%;} - блок с пропорциями 1/2

     Также работает aspect-ratio, но оно пока плохо поддерживается:
	aspect-ratio: 1 - ширина и высота равны

111) loading='lazy' - браузер сам будет решать когда загружать картинки:
     <img src="1.jpeg" loading="lazy">

     Атрибут работает, картинка грузится при прокручивании к ней

111) rel="noreferrer" - запрет передавать заголовок Referrer

    <a href="index.html" rel="noreferrer">ссылка без реф</a> - в http запросе не будет заголовка Referrer 
							       (адреса с которого пришел пользователь)

111) rel="nofollow" - запрет поисковику переходить по ссылке + не передавать ссылочный вес этой ссылке
		      ставить на ненадежные ресурсы или нетематические для вашего сайта

     <a href="..." rel="nofollow">Ссылка</a>

111) nofollow - запрет поисковикам переходить по ссылке + команда не учитывать эту ссылку как "голос" за страницу в рейтинге поисковика.
                
     <meta name="robots" content="nofollow" /> - запрет поисковикам переходить по всем ссылкам на странице
     
     <a href="sign.php" rel="nofollow">Войти</a> - запрет поисковикам переходить по этой ссылке

111) noindex - запрет поисковикам индексировать сайт или часть сайта (то же можно задать в Disallow файла robots.txt)
   
     <meta name="robots" content="noindex, nofollow" /> - запрет индексировать и переходить по ссылкам страницы

     <meta name="robots" content="noindex, follow" /> - запрет индексировать, но УЧЕСТЬ ссылки страницы

     <!--noindex-->это не будет индексировать Яндекс<!--/noindex--> - запретить часть сайта для индексации Яндексом
								      Гугл не понимает этот тег

111) file_get_contents('main.css') - читает содержимое файла как текст
     include 'index.php' - выводит содержимое и выполняет код php, если он есть внутри

    <?= file_get_contents('main.css') ?> - вывести содержимое файла main.css
    <?php include 'main.css' - вывести содержимое файла выполнив код php если он есть внутри

111) Смысл модулей в том, что это изолированный js код, который наружу через export выводит результат своей работы - переменные
     Если в верстку подключаем обычные скрипты (<script src='...'></script>), то к странице подключается весь код скрипта, 
     а не одна переменная как при подключении модуля.

     Common модули:
	module.exports = {a: 1} - модуль
	let obj = require('file.js') - подключаем модуль

     ES6 модули:
	export const a = 1 - модуль
	import {a} from './car.js' - подключаем модуль

     Если подключаем модуль в html, то:
	<script type='module'>
	   import {a} from './car.js'
	</script>

111) В верстке в p нельзя вкладывать другие блочные элементы (p, div, ul, table и пр.), поэтому проще верстать параграфы в div

111) backdrop-filter: blur(4px) - размытие заднего фона. Например, даем слою overlay
     filter: blur(5px) - то же самое для изображения  

111) Контекст наложения родителя при работе с z-index
     Если у предков элемента задан position (relative, absolute, sticky, fixed) и z-index, 
     то его дочерние элементы ограничены этим z-index

     То есть если у родителя z-index: 10, а у дочернего z-index: 500, то дочерний относительно внешних к родителю
     элементов не может подняться выше z-index 10 и его z-index: 500 имеет значение только внутри родителя.

     Например, у шапки был z-index: 10, у бургера внутри него z-index: 100, 
     но все перекрывал нижний overlay c z-index: 20

     Случаи когда у элемента создается контекст наложения для всех его дочерних элементов:
	1) Если это корневой <html>
	2) Если установлен position и z-index (его z-index становится верхним пределом для дочерних)
	3) Если есть свойство opacity меньше единицы (даем opacity: 0.5 и z-index дочерних обнуляется для внешних элементов)
	4) Если имеет свойство transform, filter, perspective, clip-path, mask/mask-image/mask-border отличное от none
	5) Если имеет свойство isolation со значением isolate
	
111) .content * { all: revert; } - сбросить все стили блока content к браузерным по умолчанию, было полезно с tiny,
				   до этого reset.css перезаписывал браузерные стили тегов

     revert - вернуть к браузерному стилю
     all - css свойство для всех свойств

111) Наследуемые свойства - все свойства для оформления текста (font-size, font-weight, color и пр.)
     Ненаследуемые свойства - все свойства, которые не относятся к оформлению текста (display, border, background и пр.)

     revert - установить браузерный стиль для тега
     inherit - наследовать свойство у ближайшего родителя
     initial - использовать значение по умолчанию для заданного CSS-свойства (напр., color: black). 
	       Не равно, но может совпадать с браузерными стилям             
     unset - если свойство наследуемое, то сбрасывается к inherit, если ненаследуемое - к initial	

111) .content * { all: unset; } - все наследуемые свойства сбрасываются к inherit, а ненаследуемые к initial

111) h1, h2 внутри тега section по браузерным стилям имеют одинаковые размеры, а пользователь в tiny видит их разными

111) object-fit: cover - задается картинке, устанавливается как она заполняет родительский контейнер
     object-position: top right; - сдвиг в любых единицах размера, чтобы была показана нужная часть картинки

     background-size: cover - то же, но для фоновых изображений
     background-position: 20px 30%; - сдвиг фона

111) Пользовательские дата атрибуты можно делать без значения: <div data-default></div>
     Проверять наличие такого атрибута можно так: if(node.hasAttribute('data-default'))

111) Преобразование в число пользовательского ввода: 
	let num = parseFloat(str.replace(',','.').replace(' ',''))

111) Повернуть текст в ячейке по вертикали:
	transform: rotate(-180deg);
    	writing-mode: vertical-lr;
    	text-align: center;

111) Более короткая версия $var = $num ? $num : $num2
	$var = num ?: num2

111) Кнопку submit можно легко вынести за форму используя js:
	<a href="javascript:;" onclick="document.forms.edit.submit();"></a> // где edit - name формы
	<button onclick="document.forms.edit.submit();">Сохранить</button> // где edit - name формы

111) Если в форме только один input, то при нажатии в нем Enter на форме будет отрабатывать событие submit
	<form action="">
          <input type="text" name="search" placeholder="Поиск">
	</form>

111) Также как для multiple загрузки файлов можно задавать группе галочек input type="checkbox" одно имя:
	<input type="checkbox" name="pages[]" value='5'> // обязательно задавать value, его значение запишется в массив
     Результат придет в массиве pages

111) Просмотр созданного в Eloquent mysql запроса:
	DB::enableQueryLog();
      	   $orders = Order::all();
      	dd(DB::getQueryLog());

111) Получить названия всех столбцов в таблице:
	$columns = Schema::getColumnListing((new Order())->getTable());

111) Функция поиска слова по всем полям таблицы:
	public function search(Request $request){
      	            $str = Str::of($request->search)->trim()->lower(); 
      		    $orders = Order::query();
      		    $columns = Schema::getColumnListing((new Order())->getTable());
		    $columns = array_diff($columns, ['created_at', 'updated_at', 'deleted_at']); // удаляем, так как на поле типа
                                                                                                    datetime LIKE покажет ошибку кодировки
		    
                    foreach ($columns as $i => $column){
                      if($i === 0){
                        $orders = $orders->where($column, 'LIKE', "%{$str}%");
                      } else {
                        $orders = $orders->orWhere($column, 'LIKE', "%{$str}%");
                      }}
                    $orders = $orders->orderBy('created_at', 'DESC')->paginate(20);} // либо ->get()

111) Если на каких-то страницах есть вертикальная полоса прокрутки, а на других нет (мало текста), то при переходе по страницам сайта
     будет сдвиг сайта из-за появления полосы. Это устраняется так:
	html {
  	   width:100vw;
           overflow-x:hidden;} // с hidden осторожно, перестало прилипать sticky, пришлось убрать

111) Свои функции помощники можно загружать автозагрузчиком. 
	1) В composer.json пишем: "autoload": {"files": ["lib/Foo.php", "lib/Bar.php"]}
	2) composer dump-autoload - в vendor в папке composer обновляется карта файлов

111) Также можно загрузить помощники через классы:
	1) "autoload": { "classmap": "lib" } - загрузятся все классы из папки lib
	2) composer dump-autoload
     
111) namespace - установка классу пространства имен. Это позволяет помещать классы в отдельные логические группы  и не конфликтовать 
                 названиям классов, если в разных папках есть одинаковые названия классов и нужно их подключить в один файл.
                 Главное, для чего нужен namespace - чтобы не конфликтовали одинаковые имена классов	

     namespace Admin;
     class Page {...}

     namespace User;
     class Page {...}

     Чтобы их подключить в другой файл нужно писать:	
     $myObject1 = new Admin\Page();
     $myObject2 = new User\Page();

     Чтобы сократить подключение можно писать
     use Admin\Page
     use User\Page as NewPage
     $myObject1 = new Page();
     $myObject2 = new NewPage();

111) В файле composer.json можно настроить автозагрузку классов:
	"psr-4": {
		 "EvolutionCMS\\Main\\": "packages/main/src/"} // при загрузке класс EvolutionCMS/Main заменять на packages/main/src/
	Теперь команда:
	   use EvolutionCMS\Main\Page; будет грузить класс packages/main/src/Page.php

111) То есть когда пишем в контроллере:
	use EvolutionCMS\ServiceProvider;
     Это означает, что класс ServiceProvider грузится из core/src/ServiceProvider.php



































		






















     
	
	

======================================================= Ларавель  =============================================================
	
	Установка: если композер уже установлен глобально, то заходим в папку сайтa (должна быть пустая):
	cd название папки сайта
	в терминале пишем:
		 composer create-project --prefer-dist  laravel/laravel ./    

	Либо composer create-project laravel/laravel my-site - тогда в директории терминала создаст папку my-site 
							       с приложением Ларавель

	в опен сервере в доменах выбираем корневую папку проекта \laravel.ru\public
        имя домена пишем как удобно (его вводим в браузере)

	Можно использовать БД SQLITE вместо MYSQL с openserver. Установка описана выше
	Для генерации страниц входа и регистрации на основе Бустрапа можно использовать Laravel UI (описано выше), Breeze (ниже) и др.

	В опенсервере я прописывал путь куда идут запросы - \laravel.ru\public
	Там ищется файл .htaccess, который перенаправляет запросы на соседний файл index.php.
	Можно файл .htaccess создать в общей папке \laravel.ru и прописать путь до .htaccess в папке public (RewriteEngine on  RewriteRule (.*) public/$1)/, тогда в опенсервере папка домена будет \laravel.ru

	Запуск: php artisan serve - можно будет заходить через localhost:порт; если есть openserver, то не нужно, так как можно заходить через домен
	в .env устанавливаем название базы 
	в config database ставим charset = utf8, collation = utf8_general_ci (не поддерживает эмодзи, лучше utf8mb4_0900_ai_ci)	

	Внимание! Не удаляем таблицу users - используем ее, так как потом все заново придется делать для верификации,
	сброса паролей (resets), создание таблицы для заданий (jobs) и пр.
	Если будет регистрироваться много пользователей, то лучше не изобретать велосипед с регистрацией, верификацией,
	сбросом паролей и использовать Breeze

	в layout @yield('main_content') - в это поле будут вставляться данные из доп. слоев между тегов @section('main_content') ... @endsection
	Внимание, можно сократить до @section('title', 'My blog'), например для тайтлов
	сам главный слой вставляется во внутренние через @extends('layout'). Пишем эту команду сверху, а ниже перечиляем данные через @section('') ... @endsection
	В ларавел переход по папкам происходит через точку, напр. в extends, return view('pages.about')

	Установка Breeze:
		1) Устанавливаем Ларавель
		2) Создаем в phpmyadmin БД с кодировкой utf8mb4_0900_ai_ci - поддерживает эмодзи
		3) Прописываем в .env->DB_DATABASE имя БД
		4) php artisan migrate
		5) composer require laravel/breeze --dev
		6) php artisan breeze:install (выбрать blade, и еще что-то)
		   npm install
		   npm run dev
		7) В правом углу приложения доступны ссылки на вход и регистрацию, маршруты прописаны в routes/auth.php
        
        Устанавливаем автопрефиксер: //чтобы лишние стили в браузере не мешали при разработке можно установить перед выкладкой на хостинг
		0) В Vite в Ларавель установлен пакет postcss. Через этот пакет можно устанавливать плагины обработки
                   стилей и скриптов. Список поддерживаемых postcss плагинов: https://github.com/postcss/postcss
		1) npm i autoprefixer -D - устанавливаем пакет autoprefixer
		2) В package.json пишем строчку: "browserslist": [ "cover 99.5%" ], // то есть поддерживаем 99.5 используемых браузеров
		3) В корень Ларавель создаем файл postcss.config.js // это файл настроек postcss
		4) Пишем в нем: module.exports = { plugins: { autoprefixer: {} }} // чтобы при работе postcss вызывался autoprefixer
		5) Все, теперь при npm run dev и npm run build в стилях для некоторых свойств появляются префиксы webkit и пр.

328)	В роуте передавать переменные в view можно через:
	1) with: return view('home')->with('var', 'value');
	2) вторым аргументом в массиве: return view('home', ['var1' => 'value1', 'var2' => 'value2']);
	3) php функцией compact: return view('home', compact('var1', 'var2')); // офигенно
	Функция compact создает ассоциативный массив, содержащий названия переменных и их значения. 
	Противоположная функция: extract  

	В шаблоне blade переменные доступны:
	 - обычным способом <?= $var ?>  // не приветствуется
	 - командой blade: {{ $var }} // фигурные скобки преобразуются в <? echo  ?>

329) Роут принимающий данные из формы методом post прописывается: Route::post('...', callback)	

330) В форме обязательно нужно указывать @csrf // проверяет, что форма с сессии пользователя

331) dump() - красиво распечатает данные
     dd() красиво распечатает данные и остановит скрипт
     Debugbar::info() - выводит то же самое в Дебагбаре, нужно установить 
                        и импортировать use Barryvdh\Debugbar\Facades\Debugbar;, есть внизу

332) Совместить два метода для одной страницы: 
     Route::match(['get', 'post'], '/contact', callback) // либо прописываем отдельно Route::get, Route::post

333) В роутах именнованные маршруты: Route::get('url', callback)->name('page')
     Теперь url доступен в php через переменную route('page'), в blade: <form action="{{ route('page') }}"></form>

334) Сокращенная запись роутов: 
	Route::view('/url', 'page'); // по запросу /url показываем view page.blade.php
	Route::view('/url', 'page', ['key' => 'value']); // передаем переменные во вьюху
Удобно, когда нет обработки гет параметров и др. действий и нужно просто вернуть html

335) Посмотреть какие аргументы ждет функция: нажимаем ctrl и наводим на функцию

336) Редирект: 
Route::redirect('/test', '/') // редирект на главную,  страница /test временно перемещена с кодом 302 (код для редиректа по умолчанию);
Route::redirect('/test', '/', 301) // редирект на главную,  страница /test постоянно перемещена с кодом 301

Либо есть функция автоматически прикрепляющая статус 301 (постоянно перемещена):
Route::permanentRedirect('/', 'contact');
Или: return redirect(route('site', $city), 301); - постоянный редирект с передачей слага


Как работает HTTP при редиректе:
Сначала идет GET запрос первой страницы, она присылает заголовок с кодом перемещения,
браузер запрашивает новую страницу. Итого два запроса страницы.

337) Передача параметров url:
Route::get('/{id}', function ($id){
	return view('home', ['var' => $id]);});

В Route::get('/{id} на месте id можно использовать [0-9a-Z_]

Данные из Route::get('/{id}/{slug} передаются в колбэк аргументами и названия можно менять:
Route::get('/{id}/{id1}', function($key1, $key2){return view('test', ['var1' => $key1, 'var2' => $key2]);})
Или через compact: return view('test', compact('key1', 'key2')); // ключи должны совпадать с названиями переменных

При таком способе: 
	Route::get('/{word?}', [MainController::class, 'index']); - необязательный параметр word
Они доступны так:
        public function index($word=111){ return $word; }
	public function index(Request $request, $word=111){ return $word; } - если есть зависимости, то их ставим перед переменными маршрута

338) Данные из url можно проверять: 
	Route::get('/{number}', [MainController::class, 'get_number'])->where('number', '[0-9]+');
        Route::get('/{number}', [MainController::class, 'get_number'])->whereNumber('number');
	Route::get('/category/{category}', [MainController::class, 'get_category'])->whereIn('category', ['movie', 'song']);

Route::get('/{id}/{slug}', function ($num, $word){
          return view('test', compact('num', 'word'));})->where(['num'=>'[0-9]+', 'word' => '\w+']); // проверка через регулярные выражения, 
						                                                        если все хорошо то пападем в роут

Можно прописать проверку параметров id и slug в урл автоматически без where.
Делается это в app/Providers/RouteServiceProvider в функции boot добавляем:
	Route::pattern('id', '[0-9]+');
        Route::pattern('slug', '\w+'); 

339) Параметры url можно сделать необязательными, делается это так: {num?}
Если в колбэке работаем с таким параметром, то ставим значение по умолчанию: 
	Route::get('/{id}/{slug}/{num?}', function ($var1, $var2, $var3=null){
    		return "$var1, $var2, $var3"; });

339) Route::get('/{page}', [MainController::class, 'page'])->where('page', '.*') - роут на все маршруты, любой вложенности и длины, 
                                                                                   включает главную страницу без конечного слеша

340) В php переменные можно писать вместе с текстом в двойных скобках: "Редактировать пост $id"

341) Можно группировать маршруты по одинаковой начальной части, чтобы постоянно не повторять:
	Route::prefix('admin')->group(function(){
    		Route::get('/post/create', function (){
        	return "Создать пост";});

    		Route::get('/post/{id}/edit', function ($id){
        	return "Редактировать пост $id";});});

Теперь эти роуты будут срабатывать на адресах начинающихся на admin: http://laravel.ru/admin/post/create

Также можно изменить им именование с добавлением префикса:
	Route::prefix('admin')->name('pages.')->group(function(){
    		Route::get('/post/create', function (){
        	return "Создать пост";})->name('create');

    		Route::get('/post/{id}/edit', function ($id){
        	return "Редактировать пост $id";})->name('edit');});

Теперь к такому маршруту обращаться нужно через точку: 
		<a href="{{ route('pages.create') }}">Ссылка на форму</a>

Также можно группировать так:
	Route::group(['prefix'=>'admin', 'middleware' => ['can:delete-post', 'auth']], function(){
    		Route::get('/', [MainController::class, 'index'])->name('admin.index');
    		Route::resource('/categories', CategoryController::class);})

342) HTML-формы не поддерживают действия PUT, PATCH или DELETE.
Если нужно передать форму, например, методом PUT, то в нее нужно добавить помимо токена csrf строчку:
   <input type="hidden" name="_method" value="PUT"> - это можно сделать строчкой @method('PUT')
Скрытый input с токеном csrf как мы знаем в blade добавляется с помощью команды @csrf. 

<form action="/example" method="POST">
    @method('PUT')
    @csrf
</form>

В роуте, соответственно принимаем: Route::match(['get', 'post', 'put'], url, callback);

343) Роуты с более точными правилами должны стоять выше общих роутов:
	Route::get('/page/edit', callback);
	Route::get('/page/{slug}', callback);

344) Если  роут/страница ждет обязательный параметр в url: Route::get('/post/{id}', callback)->name('link1'),
     то с верстки он передается таким образом:
     <a href="{{ route('link1', ['id' => 31]) }}">Ссылка</a> => <a href="/post/31">Ссылка</a>

345) Если в именованный маршрут передаем лишние параметры и роут их не ждет, 
     то в ссылку они дописываются гет-параметрами:
	Route::get('/admin/{id}', callback) -> name('posts')
	<a href="{{ route('posts', ['id' => 31, 'width' => 555]) }}">Ссылка</a>
	Ссылка будет: http://laravel.ru/admin/31?width=555

346) Если url не попадает под роуты, то ларавель показывает 404 страницу.
C помощью метода fallback можно редиректить все такие url на любую другую страницу, например на главную.
Но правильнее отдавать 404 страницу, чтобы в индекс поисковиков не попадали дубли адресов или адреса с гет-параметрами.
	Route::fallback(function (){
    		return redirect()->route('home');}); // у главной именнованный маршрут name('home'), 
							переход будет с 302 кодом (временно перемещена)

Либо возвращать свою страницу с ошибкой:
	Route::fallback(function (){
   		abort(404, 'здесь можно передать сообщение');});
Во вьюхах создаем папку errors и в ней по кодам ошибок создаем файлы, например для 404 ошибки: 404.blade.php
Перенаправление будет с 404 кодом.
Сообщение из abort можно получить так: <h2>{{ $exception->getMessage() }}</h2>

Роут Route::fallback должен стоять последним в роутах.

347) Параметры маршрута автоматически пробрасываются в принимающую функцию контроллера:
	Route::get('/page/{slug}', [PageController::class, 'show']);
	В функции show можно принять $slug

347) Проверка имени маршрута:
	if ($request->route()->named('login')) { ... }

348) Для операций создания, чтения, редактирования, удаления материала (напр., post)
     нужно создавать Route::get, Route::post, Route::put, Route::delete, 
     прописывать им url, привязывать именованные маршруты, создавать функции.
     Все это заменяется двумя строчками кода:  

  php artisan make:controller PostController --resource  // Создается контроллер с функциями:
  
  Не забываем прописать в роутах: Route::resource('posts', PostController::class);

  index - показать страницу со списком всех материалов 
	Route::get
	ссылка /posts
	route('posts.index') // именованные роуты это другими словами пути запуска конкретных функций

  create - показать страницу добавления нового материала  
	Route::get
	ссылка /posts/create
	route('posts.create')

  store - отправить созданный материал в базу 
	отпр. формой в Route::post // <form action="{{ route('posts.store') }} method="post">@csrf ... </form> // если есть картинка, то multipart/form-data
	ссылка /posts
	route('posts.store') // код сохранения: провалидировать, затем Post::create($request->all());
	

  show - показать материал по id, слагу и др (дальше по id)
	Route::get
	ссылка /posts/{post}
	route('posts.show', ['post' => число]) // <a href="{{ route('posts.show', ['post' => 1]) }}">Показать №1</a>

  edit - показать страницу редактирования материала с опр. id
	Route::get
	ссылка /posts/{post}/edit
	route('posts.edit')

  update - обновить материал с опр. id
	отпр. формой в Route::put // <form action="{{ route('posts.update', ['post' => $id]) }}" method="post"> @csrf @method('put') </form>  // если есть картинка, то multipart/form-data
	ссылка /posts/{post}
	route('posts.update')   // простой код обновления в контроллере: провалидировать, затем:  $post->fill($request->all())->save();
											      или Post::findOrFail($id)->fill($request->all())->save()
											      или Post::findOrFail($id)->update($request->all()) // под коробкой update тот же fill
  destroy - удалить материал с опр. id
	отпр. формой в Route::delete // <form action="{{ route('posts.destroy', ['post' => 1]) }}" method="post">@csrf @method('delete') <button type="submit">удалить</button></form>
	ссылка /posts/{post}
	route('posts.destroy') // код безопасного удаления: Post::destroy($post->id);
	
  На кнопку или ссылку удаления можно повесить скрипт:
	<button onclick="return confirm('Удалить?')" type="submit">Удалить</button>

  В web прописывается роут, который все запросы с url: /posts и опр. методами будет направлять 
							      на конкретную функцию из PostController

  Если добавить префикс в роутах, то все правильно работает, префикс не влияет на маршрутизацию к контроллеру ресурса:
	Route::middleware('auth:admin')->prefix('admin')->group(function () {
		Route::resource('pages', PageController::class);}
  Ссылка в браузере будет: http://pechati.loc/admin/pages - ведет на метод index, здесь прописываем показ всех сущностей

  Route::resource('posts', PostController::class);
		Также можно прописать в роуте третий аргумент, чтобы заменить в верстке post на id: 
           	['parameters' => ['posts' => 'id']] => <a href="{{ route('posts.show', ['id' => 1]) }}">

  Пример, допустим: 
	Запрос адреса /posts методом GET - срабатывает маршрут на функцию index, она показывает список постов
	Запрос адреса /posts  методом POST - срабатывает маршрут на функцию store (из action формы) она отправляет данные в базу
	Запрос адреса /posts/3/edit  методом GET - срабатывает маршрут на функцию edit, она показывает страницу редактирования поста №3,
	
	в последнем случае ссылку ларавель формирует самостоятельно, в верстке достаточно прописать:
	<a href="{{ route('posts.edit', ['post' => 3]) }}">редактировать</a>  => href="/posts/3/edit"

Посмотреть все маршруты в Ларавель: php artisan route:list (читать ниже), это удобно, так как там показаны именованные маршруты 
на методы resource, заголовки (get, post, put, delete // если форма и методы put или delete, 
то method="post" @method('put или delete') ), в том числе правильное написание передающихся аргументов, 
например при редактировании поста:
	admin/categories/{category}/edit => action="{{ route('categories.destroy', ['category'=>$category->id]) }}"

php artisan route:list не работает без папки Auth в контроллерах, ее можно скачать отсюда:
	https://github.com/laravel/laravel/tree/v5.6.12/app/Http/Controllers/Auth

Также расписал в 522

В ресурсный контроллер, также как в обычный можно вставлять метод missing:
	Route::resource('photos', PhotoController::class)->missing(function (Request $request){ ... }) - missing сработает, если не найдется сущность по id

Для api можно создать apiResource (будет обычный ресурсный контроллер без методов create и edit):
	php artisan make:controller PhotoController --api
	Route::apiResource('photos', PhotoController::class);
	
349) Ссылки вызывают только метод GET, формы только GET и POST.
     Поэтому в ларавель в форму можно добавлять @method('put') и др., 
     чтобы данные отправлялись фреймворком c доп. скрытым инпутом с value='put'.

350) Важно: при отправке формы методом delete, put и др. (@method('delete')) нужно обязательно 
     указывать в самой форме method="post", иначе форма срабатывает по умолчанию как get 
     и отправляет данные get параметрами в адресе

351) $_ENV - глобальный массив констант среды php, включающий константы из файла .env (файл констант Ларавель)
     
     Файл .env - загружается встроенной библиотекой DotEnv (new DotEnv())->load('.env')
     Она прогоняет файл .env и записывает ее константы через php метод putenv() в переменные среды, 
     а также записывает их в $_ENV (php переменные среды выполнения/интерпретатора)и $_SERVER (php переменные сервера).
     Соответственно обращаться к переменным .env можем как помощником Ларавель env(), так и через php метод getenv()
     Помощник env() внутри себя использует php метод getenv(), но позв. исп. значения по умолчанию, правильно обрабатывает кавычки из .env

     Файл .env должен быть в gitignore
	
     папка config - настройки приложения
     env('CONSTANTA', 'default') - возвращает значения  $_ENV, либо значение по умолчанию
     config('services.mailgun.endpoint') - папка config -> файл services -> массив mailgun -> ключ endpoint
     config(['app.my_var' => '111']) - установка настройки при работе скрипта

352) Миграции нужны для совместной работы, чтобы не перекидывать друг другу дампы БД.
     Позволяют задавать одинаковую структуру БД с одинаковыми свойствами полей разным разаработчикам.

353) Классы php, js нужны для:
	 1) создания копий независимых объектов с одинаковым набором свойств/методов
	 2) использования готовых методов класса
	 3) переиспользования кода (чтобы не было дублирования)

354) Порядок: 
1) создается миграция, в которой прописываются изменения БД
	php artisan make:migration create_posts_table
2) Изменения грузятся в БД: 
	php artisan migrate // ларавел находит непримененные миграции и отправляет их в БД

355) Откатить последнюю партию миграций: php artisan migrate:rollback
     Например, забыл вписать поле, откатил, вписал, заново migrate
     Откатить все миграции: php artisan migrate:reset

356) Создать миграцию для изменения конкретной таблицы (добавляем флаг):
	php artisan make:migration change_posts_table --table=posts

357) Лень писать запрос mysql ? 
     Отредактируй таблицу в phpMyAdmin вручную -> перед сохранением Предпросмотр SQL

356) Команда $table->timestamp дополнительно создает: created_at и updated_at
Так как там нет времени по умолчанию, то через миграции они изменяются через DB::statement:

метод up:
DB::statement("ALTER TABLE `posts` CHANGE `created_at` `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP, CHANGE `updated_at` `updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP;");

метод down:
DB::statement("ALTER TABLE `posts` CHANGE `created_at` `created_at` TIMESTAMP NULL, CHANGE `updated_at` `updated_at` TIMESTAMP NULL;");

SQL удобно скопирован через Предпросмотр SQL

Важно: если миграция создана через модель (php artisan make:model Post -m), то поля created_at и updated_at
заполняются автоматически по умолчанию.

357) Работа c БД осуществляется тремя способами способами:
	1) чистыми mysql запросами через фасад DB:
	   $posts = DB::select("SELECT * FROM posts") // select, update, insert, delete 
						      и statement - выполняет sql запрос ничего не возвращая
	
	2) с помощью построителя запросов через фасад DB:
	   $data = DB::table('country')->get(); // get - идет последним, либо first, find
	   Методов много, они похожи на sql: get, where, orWhere, select, first, limit, whereNull, inRandomOrde, 
					     insert, find, insertGetId, update, increment, decrement, take и т.д.
	   Построитель запросов защищает ваш код от инъекций и вам нет необходимости экранировать строки перед их передачей в запрос.


	3) через модели

358) Работа через параметры защищает запросы от sql инъекций:
DB::insert("INSERT INTO posts (title, content) VALUES (?, ?)", ['Статья 4', 'Контент статьи 4']);

DB::insert("INSERT INTO posts (title, content) VALUES (:key1, :key2)", ['key1' => 'Статья 4', 'key2' => 'Контент статьи 4']);

$posts = DB::select("SELECT * FROM posts WHERE id > :id", ['id' => 9]);

DB::update("UPDATE posts SET title = ? WHERE id = 1", ['Заголовок']);

359) date("Y-m-d H:i:s") - php функция времени
     time() - php timestamp в секундах	
     NOW() - mysql функция времени, возвращает год-месяц-день часы:минуты:секунды
     Date.now() - js timestamp в миллисекундах

360) Ларавель по умолчанию использует временную зону UTC: config->app->'timezone' => 'UTC'
     Меняем: 'timezone' => 'Europe/Moscow'

361) Команды потенциально возвращающие ошибки оборачиваем в try-catch

362) Транзакции DB::transaction позволяют проконтролировать, что совмещенные операции выполнились успешно, 
а если нет, то откатить все:

DB::transaction(function (){
            try { - в Ларавель try-catch не надо, нужно только если сами хотим обрабатывать исключения
                DB::update("UPDATE posts SET created_at = ? WHERE created_at IS NULL", [NOW()]);
                DB::update("UPDATE posts SET updated_at = ? WHERE updated_at IS NULL", [NOW()]);

            } catch (\Exception $e) {
                DB::rollBack(); // откатывает назад обе операции
                echo $e->getMessage();
            }});

Вторым аргументом в DB::transaction(callback, num) можно передать количество попыток

Вообще, этот метод можно использовать и без try:
	DB::transaction(function() use ($data, $request){
          $order = Order::create($data);
          $order->othergoods()->attach($request->other_good);
        }, 2); 
Просто с try мы сами обрабатываем ошибки, например, можем записать флеш переменную с ошибкой:
	return redirect()->back()->with('error', $e->getMessage())
а без try исключение пробросится в обработчик Ларавель и будет просто показ 500 страницы

363) Готовые БД есть на сайте MySQL

364) Модель Ларавель - это обычный PHP класс с набором методов, привязанный к таблице БД.
     При этом каждая запись в таблице будет представлять собой объект этого класса.
     Мы обращаемся к базе, вызывая методы соответствующего объекта.
     Такая обертка называется ORM - система объектно-реляционного отображения. 
     В Laravel такая система называется Eloquent.
     Все методы, доступные в конструкторе запросов, также доступны при работе
     с моделями Eloquent(get, where, orWhere, select, first, whereNull, inRandomOrde, insert, insertGetId, update, increment, decrement, take и т.д.)

     Если несколько условий OR, то orWhere нужно использовать с помощью замыкания:
	->where('top_menu_order', '>', 0)->orWhere(function ($query){$query->where(...)->where(...)})

365) Форма может отправлять как методом POST, так и GET, 
     просто во втором случае данные уходят get параметрами в адресе

366) Запись таблицы - объект класса Модели(таблица БД).
Соответсвенно у этого объекта будет набор свойств, которые соответствуют колонкам: $post->id и т.д. 

367) Помощь с флагами: php artisan help make:model

368) Создание модели с миграцией: php artisan make:model Post -m
     То есть мы создали файл модели в ларавель + создалась миграция 
     на создание в БД таблицы, теперь нужно дополнить нужные поля таблицы в миграции и migrate, 
     создастся таблица posts (во мн. числе).
     Важно: $table->timestamps(); будет автоматически заполняться объектами модели
            Если при создании таблицы удалили эту строчку, то отключаем заполнение в модели: public $timestamps = false;
	    Также можем удалить создание даты $table->timestamps() для pivot таблицы, если модели нет, то ошибки не будет 

369) Может получиться, что таблица в базе уже существует, либо поменяли ее и нужно перенастроить модель.
        Сначала создаем модель: php artisan make:model City // если ее нет 
     	Настраиваем:
	protected $table = 'posts'; // - здесь модель хранит название таблицы, то есть если модель и таблица 
				         отличаются названиями здесь можно переопределить
	protected $primaryKey = 'post_id'; // название первичного ключа
	public $incrementing = false; // первичный ключ не инкреминируем, например если он -  уникальное слово
	protected $keyType = 'string'; // первичный ключ - строка
	public $timestamps = false; // модель не будет авт. заполнять поля created, updated
	protected $attributes = ['content' => 'Lorem ipsum']; // если хотим, чтобы какое-то поле авт. заполняла модель
	protected $fillable = ['title']; // читать ниже
	protected $guarded = []; // теперь можно заполнять все поля без необходимости указывать их в $fillable
				 // это список защищенных от метода create полей

	protected $fillable - это поля, которые можно заполнять из присланной целиком формы. Например, контроллер 
	принял данные формы в $request и можно отправить все поля разом в базу так:
		public function store(Request $request){
        		Post::query()->create($request->all());}
	Форма распарсивается и вставляется в нужные поля, но может так получиться, что у нас в этой таблице есть
	поле is_admin. Жулик может добавить в html верстку <input name='is_admin'> с нужным значением и 
	отправить такую форму. Поэтому в protected $fillable мы перечисляем поля, которые могут заполняться 
	если форму прислали целиком без ручного распарсивания как ниже:
	(new Post())->fill(['title'=> $request->input('title'), 'content'=> $request->input('content')])->save();
	
370) Создали модель, миграцию на создание таблицы, сделали migrate, теперь можно пользоваться моделью.
     1) 
        А) Создание записи  в таблице c ручным заполнением полей:
	$post = new Post(); // создали новый объект модели
        $post->title = 'Статья 1'; // заполняем поля таблицы
        $post->content = 'Контент 1';
        $post->save(); // сохраняем запись в БД

	Короткая запись: (new Post())->fill(['title'=>'1111', 'content'=> '22222'])->save();

	Б) Массовая вставка через create, нужно заполнение fillable:
	Post::query()->create($request->all()) или Post::create(['title'=>'1111', 'content'=> '22222']);

	Как уже говорил created и updated заполняются моделью (в обоих случаях).

	Также есть forceFill() и forceCreate(), игнорирующие fillable

	Внимание! Ларавель автоматически преобразовывает пустые инпуты в null, поэтому нужно заранее при проведении миграций делать такие поля nullable,
	либо ->nullable->default('значение не равное пустой строке')
        default('') - не прокатит, возникнет ошибка так как будет вставляться null (попробовать ->nullable->default(''))
	То есть правило: те поля, которые могут быть пустыми обязательно превращаем в nullable или default c непустым значением, 
	так как Ларавель будет автоматически вставлять null

     2) Получение всех записей: 	
	$data = Country::all(); //вернется массив объектов класса Country, 
				у которых есть в том числе свойства - названия полей таблицы
	foreach ($data as $item) {
            dump($item->title); }

     3) Изменение/обновление записи: 
	$data = Country::find(1);
	$data->title = 'new title';
	$data->save();
	Либо: 
	     а) Country::where('id', 5)->update(['title'=>'newHead']);
	     б) $data = Country::find(1);
		$data->update($request->all()); //под коробкой update метод fill, только теперь коротко
	     в) $page = Page::findOrFail($id);
	        $page->fill($request->all())->save();

	Если мы изменяем у записи список тегов, то нужно редактировать связи из таблицы связей 
	post_tag (многие ко многим), делается это перед save() так:
		if(count($post->tags) > 0){ // если у поста были теги 
            	    $post->tags()->sync($request->tags);} // то на всякий случай синхронизируем связи

     4) Удаление: 
	$data = Country::find(10);
	$data->delete(); // сокр. Country::find(10)->delete()
	Если запись с таким Id не существует, то будет ошибка, так как объекта не существует.
	Также ошибка будет, если попытаться инъецировать несуществующий объект:
	public function delete(Order $item){} - если объекта по принятому id не существует, то вылетит ошибка.
	Поэтому:
	public function delete($id){ // принимаем только id, без инъицирования
	Post::destroy($id); // не будет ошибки и можно удалять несколько. sql ищет id как: where id in (10,11,12)
	Post::destroy([10,11,12]) // второй способ в массиве
	}

	
	Мягкое удаление:
	      1) use Illuminate\Database\Eloquent\SoftDeletes    // импортируем пр-во имен трейта сверху в модель 
	      2) use SoftDeletes; // прописываем трейт в модели
	      2) php artisan make:migration add_soft_delete_category --table=categories // создаем миграцию
	      3) up: $table->softDeletes(); // пишем миграцию на вставку поля deleted_at
	         down: $table->dropSoftDeletes(); 
	      4) php artisan migrate // в таблицу вставилось поле deleted_at
	Теперь в таблице создано поле deleted_at с типом timestamp, nullable в которое при удалении записи будет устанавливаться время удаления.
	Сама запись будет оставаться в таблице, но не попадать в выборки.
	
	$flight->restore(); //	восстанавливает запись, ставит null в поле deleted_at
	$flight->forceDelete(); // жесткое удаление
	$flights = Flight::withTrashed()->get(); // withTrashed включает разрешает отображать в т.ч. мягкоудаленные записи

	Внимание! Если есть мягко-удаленные поля, а валидатор проверяет поля на уникальность, то будет конфликт создаваемых
	записей и мягко-удаленных. Можно включить игнорирование валидатором мягко-удаленных полей при проверке уникальности.
	Описал. 
	Также нужно иметь ввиду, что MySQL не пропустит одинаковые поля, если у нее установлен ключ. 
	Даже если одно из таких полей мягко удалено.

	Внимание, при удалении сущностей, которые могут быть привязаны к другим будут возникать ошибки. 
	Как если где-то выводится список постов со своими категориями (найденными по их id) и мы удалим категорию, 
	будет ошибка. Поэтому перед удалением таких сущностей делаем проверку на привязку к другим сущностям:

	public function destroy($id){
              $category = Category::find($id);
              if($category->posts->count()){ // если категория hasMany постов больше нуля 
                  return redirect()->route('categories.index')->with('error', 'Ошибка, категория присвоена записи'); // добавим флеш переменную
	      }
              $category->delete(); // если нет привязок, то удаляем
              session()->flash('success', 'Удалено');
              return redirect()->route('categories.index');}

	В случае удаления общих сущностей, к которым привязаны другие через таблицу связей (post_tag),
	то вместе с записью (постом) удаляем связи с тегами в таблице связей:
	       public function destroy($id){
        	    $post = Post::find($id);
                    $post->tags()->sync([]);
       		    $post->delete();
                    session()->flash('success', 'Удалено');
                    return redirect()->route('posts.index');}		

     3) Другие методы:
	$data = Country::query()->find(15); // find находит запись по первичному ключу (если не id, то должны быть правильные $primaryKey, $incrementing, $keyType)
	$data = Country::query()->limit(5)->get();
	$data = Country::query()->where('Capital', '<', 20)->get();
	$data = Country::where('Name', 'Argentina')->first();
	и т.д., методы такие же как в построителе запросов

После добавления, обновления, удаления чтобы пользователь видел, что изменения произошли можно записывать флеш сообщение:
	session()->flash('success', 'Обновлено');
        return redirect()->route('categories.index');
Соответственно в лэйауты вставляем универсальный показ флеш переменных успешности и ошибок (есть внизу)
	
371) PhpStorm не видит многие статические методы Модели, 
     поэтому хак: ввели  метод query, который вернул объект построитель запросов Модели, 
     а у него уже запрашиваем методы. Будет работать и без query.
     Можно также подмешивать миксины, способ есть в видео А.Кудлая 'Модель и Eloquent ORM. Часть 2'

372) Нужно изменить таблицу? Создаем миграцию:
	php artisan make:migration add_rubric_to_posts --table=posts // указываем во флаге имя таблицы

373) Ларавель по умолчанию создает для модели Person таблицу People и потом не хочет подключаться к persons.
     В модели Person нужно прописать: protected $table = 'persons';

374) Eloquent и модели позволяют реализовать соединение таблиц более простыми способами, чем join mysql:
	
	Один к одному:
		Допустим, есть основная таблица users и дополнительная таблица phones.
		Нам нужно сделать, чтобы одному пользователю из users принадлежал один номер телефона из таблицы phones.
		Привязываем phones к users: создаем в phones поле user_id 
		Таблица phones, в которой прописали привязку user_id - считается зависимой и ее записи принадлежат (belongsTo) первой таблице.
		Теперь в модели Users создаем метод для получения зависимого объекта из phones:
		public function phone(){ // метод называем по тому, что получаем. То есть получаем phone. Если назвать по-другому, то будет ошибка
        		return $this->hasOne(Phone::class);}
		Метод phone считается динамическим и обращаться к нему нужно как к свойству:
		$person = User::find(1); // получаем юзера с id 1
        	$phone = $person->phone; // получаем привязанный к юзеру объект с телефоном
		$number = $phone->number // получаем номер по полю таблицы number

		В модели Phone создаем метод user для получения пользователя по телефону:
		public function user(){ // именно user, иначе ошибка
        		return $this->belongsTo(Person::class);} // телефон принадлежит пользователю, возвращаем этого пользователя
		
		dump(Phone::query()->find(1)->user->name); - сокр. запись, получаем имя пользователя

		Самое важное, что запомнить: 
			та таблица, где user_id - принадлежит другой, зависимая
			связи hasOne и belongsTo и методы получения по связям прописываются в моделях

	Один ко многим:
		По-прежнему, в таблице с полем  user_id лежат телефоны принадлежащие пользователям из user.
		Соответственно в модели User:
		public function phones(){ // здесь уже не обязательно именно phones
        		return $this->hasMany(Phone::class);} // возвратить телефоны, принадлежащие пользователю

		В объект user запишется свойство phones c массивом телефонов. Совмещение произошло по полю id в users и полю
		user_id в phones. Также можно перенастроить по каким полям связывать:
			 return $this->hasMany(Book::class, 'user_id', 'id'); // user_id - внешний ключ (foreign), id - свой ключ (local)
									      // если перенастраивают, то имена свои
									      
		В модели Phone ничего не изменилось, получаем пользователя которому принадлежит телефон.
		public function user(){ // именно user, иначе ошибка
        		return $this->belongsTo(Person::class);}

	Многие ко многим:
		Например, есть users и roles (админ, редактор, модератор и т.д.). 
		Один пользователь может иметь несколько ролей и одна роль может быть связана со многими пользователями.
		Для связи создается третья таблица: role_user // назв. в алф. порядке в ед. числе

		Если связываем таблицы с подчеркиванием: blog_categories и posts, то миграция:
			php artisan make:migration create_blog_category_post_table // получим таблицу blog_category_post
		Где поля будут: blog_category_id и post_id
	
		Для связующей таблицы модель обычно не нужна(если нужна, то php artisan make:model RoleUser -m) , 
	        создаем через миграции: php artisan make:migration create_role_user_table
		Поля role_user: id, user_id, role_id. 
		В ней идет перечисление по типу: 1-1, 1-2, 1-3, 2-2, 2-5, 2-6
			
		Для моделей User и Role создаются методы использующие belongsToMany:
		Модель User:
			public function roles(){ // нам нужны роли пользователя
       				 return $this->belongsToMany(Role::class);} //параметрами можно указывать: 
									      pivot таблицу, ключи pivot таблицы в сл. порядке:
									      belongsToMany(Role::class, 'role_user', 'user_id', 'role_id')
		Модель Role:
			public function users(){ // нам нужны пользователи этой роли
       				 return $this->belongsToMany(User::class);}

		Получаем все роли пользователя с id 1:
			$user = User::query()->find(1);
        		dump($user->name); // выводим имя пользователя с id 1
        		foreach ($user->roles as $role){ // здесь запрашиваем все роли пользователя методом roles
            			dump($role->title);} // выводим названия ролей


376) Ленивая и жадная загрузка.
     ---Допустим, нам нужны телефоны пользователей из таблицы users.
	Мы можем получить их лениво:
	$users = User::query()->get(); // сначала получаем всех пользователей из таблицы users

        foreach ($users as $user){
            dump($user->phone->number);} // для каждого пользователя запрашиваем привязанный телефон методом phone
					    каждый раз отправляется запрос в базу на телефон по id пользователя 				
        select * from `users`
	select * from `phones` where `phones`.`user_id` = 1
	select * from `phones` where `phones`.`user_id` = 2 и т.д.

     ---Либо можно воспользоваться жадной загрузкой и загрузить сразу все нужные модели:
	$good = Good::with('pages')->findOrFail($id); // with ставим до метода получения сущностей

	$users = User::query()->with('phone', 'tags')->get(); //phone - совпадает с названием методa модели
	foreach ($users as $user){
            dump($user->phone->number);
        }
	В этом случае выполнятся только два запроса:
	select * from `users`
	select * from `phones` where `phones`.`user_id` in (1, 2, 3, 4, 5 ...)

Жадная загрузка хороша, когда выводится список сущностей с соответствующими им связанными сущностями, например: посты-теги

376) Чтобы id было возрастающим числом, а не BigInt:
	$table->increments('id');

377) Переименовать колонку:
	$table->renameColumn('было', 'стало')

378) Скомпилированные файлы blade хранятся как обычный php код и кешируются до тех пор, пока не будут изменены.
     Также blade защищает от xss атак экранируя через htmlspecialchars символы в переменных:
	& - &amp;
	< - &lt;
	> - &gt;
	" - &quot;
	' - &#039;

Пример xss атаки:
Страница принимает от пользователя данные из инпута и показывает их на странице сразу, либо вносит в БД.    
В инпут вводим: <script>alert('текст')</script>, 
Строчка выводится на страницу как есть и скрипт отрабатывает.
Обычно данные отправляются в базу как есть, а при выводе на страницу уже применяется htmlspecialchars:
уголки и др. "опасные" символы превращаюсят  в html-сущности &lt;&gt; 
В blade htmlspecialchars применяется на этапе фигурных скобок {{ }}.

Однако в базе мы можем хранить и верстку с тегами, тогда чтобы браузер показывал пользователю 
форматированную верстку, а не строчки <h1>home page</h1> мы можем отключить htmlspecialchars используя:
{!! $name !!}
Также можно использовать пакет mews/purifier чтобы отредактировать пользовательский html

379) Шаблоны blade хранятся в resources-views, а скомпилированные php файлы в storage-framework-views

380) Файлы стилей, скрипты, картинки должны лежать в публичной папке public в css, js, img

381) Повторяющийся шаблон с header, footer обычно лежит в view/layouts/layout.blade.php

     Верстка layout.blade.php:
	Общая шапка
	   @yield('content') // выводим содержимое переменной 'content'
	Общий подвал

     Верстка home.blade.php:
	@extends('layouts.layout') // запрашиваем используемый layout
	@section('content')
		верстка <main>...</main> // в переменную content записываем верстку
	@endsection

То есть: контроллер выводит страницу home.blade.php, та запрашивает через @extends общий шаблон layout 
	 и записывает в переменную content верстку.
	 layout загружается на страницу и через @yield вставляет содержимое переменной content
Так, разные страницы могут запрашивать в @extends разный общий шаблон layout.

Также в @extends можно передавать переменные из внутренней страницы в главный шаблон:
	@extends('layout', ['title' => 'переменная'])

Показываем такую переменную так:
	{{ $title }}

Можно застраховаться от несуществования ожидаемой переменной:
	{{ $title ?? '' }}

Можно поставить значение по умолчанию для @yield если секция/переменная не существует:
	@yield('section', null);

@section применяется для записи в переменную, @yield для вывода переменной, либо запись и показ: @section верстка @show

Записывать в переменную могут не только внутренние страницы, но и сам layout.
То есть мы можем в layout записать что-нибудь в переменную @section('var') ... @endsection 
и ниже показать @yield('var').

Либо одновременно записать и показать командой @show:
@section('var')...@show // выведется это, если внутренняя @section('var') не перезаписала/дополнила ее

Возможно перезаписывание переменной @section: работает только в одну сторону, 
внутренняя страница может перезаписать переменную @section из шаблона layout и только для себя.
Переменные @section других страниц остаются прежними.

Допустим в layout header записывается в переменную и показывается:
@section('header') Шапка по умолчанию из layout  @show - показывается, если в дочерних шаблонах не определена @section('header')

а на странице с контентом мы можем изменить эту шапку для себя:
@section('header')  Шапка для этой страницы @endsection // будет показываться эта шапка:
Либо дополнить ее через @parent
@section('header') @parent  Дополнение к основной шапке @endsection.

Например в layout: <title>@section('title') Страница:  @show</title>
Дополняем из внутренней: @section('title') @parent 'О нас'  @endsection

Порядок: грузится внутренняя страница, через @extends запрашивается layout, в переменные content и header
записывается нужная верстка.
Layout подгружается, выводит содержимое @yield('content') и вставляет там где нужно 
содержимое переменных @section. Если видит, что его собственная переменная перезаписалась 
внутренней страницей, то показывает уже ее. 

Также можно записывать в переменную верстку из @include (или @includeIf):
	@section('sticky-inner')
    	   @include('parts.sticky-inner')
	@endsection

В лэйауте можно вставлять или не вставлять переменную, если ее нет:
	@yield('sticky-inner', null)

Верстка все равно кешируется, поэтому можно не переживать, если много @include

Внимание: 
1) во внутренних страницах подключаем главный шаблон и записываем переменные, 
   отдельную верстку вне переменных @section не пишем, так как она вставится выше layout.
2) в layout формируем структуру страницы путем размещения переменных из @yield и верстки из @include

Можно проверить существование шаблона перед подключением:
	1) @includeIf('some-view') - если есть подключить
	2) @if(view()->exists("some-view")) @include(...) @else @include("other-view") @endif

382) Удобный вариант просто подключать в нужное место готовую верстку (блоки, хедер, футер и др.):
	@include('layouts.footer')
	Для главного шаблона @include вставляем куда угодно, а в контенте такие вставки должны быть внутри
	@section, иначе браузер вставит их содержимое выше главного шаблона. 
	Код в @include видит переменные родительской страницы, также можно передавать переменные внутрь @include:
	@include('view.name', ['status' => 'complete'])

383) {!! $var !!} - просто вывод, без htmlspecialchars, можно применять если выводим свою верстку с тегами.
     Также в таких скобках можно применять функции, также как в обычных {{}} например: 
     {!! mb_strtoupper($var) !!}
     {{ time() }}
     <p style="text-align: center">Copyright {!! date('d-m-Y') !!}</p>
     {!! clean($html) !!} - пакет mews/purifier удалит опасные теги

384) Также нам могут понадобится переменные для js фреймоворка и др., 
	оставить переменные мы можем использовав @.
	Например, @{{ title }} => выведется строка {{ title }} и не будет ошибки, 
	иначе без @ Ларавель напишет, что переменной title не существует

385) В шаблонах blade мы можем использовать обычный синтаксис php:
	<?php if(условие): ?>
	    выводим верстку
	<?php else: ?>
	    выводим другую верстку
	<?php endif; ?>

Также в blade есть аналогичные директивы: 
  	@if(условие)
	     выводим верстку
	@elseif(условие)
	     выводим другую верстку
	@else
	    выводим другую верстку
	@endif

В функциях фигурные скобки (это же то же самое, что <?= ?>) для обращения к переменным не нужны. 
Они нужны для вывода переменных в верстку.

Также в фигурных скобках можно выводить знак @ => {{"@"}}
иначе без фигурных воспринимается как начало команды blade

386) Другие подобные директивы: 
	@php  @endphp - то же самое, что <?php  ?>
	@isset($var) выполняем если переменная определена и не равна null @endisset
	@empty($var) выполняем если переменная может быть приведена к false @endempty
	@auth пользователь аутентифицирован @endauth
	@guest пользователь не аутентифицирован @endguest
	@auth ... @else ... @endauth - есть и такой вариант
	@guest ... @else ... @endguest
	@switch($i) @case(1) First case... @break @default Default case... @endswitch
	@for ($i = 0; $i < 10; $i++) The current value is {{ $i }} @endfor
	@foreach ($users as $user) <p>This is user {{ $user->id }}</p> @endforeach
	@foreach(...) @if($loop->iteration == 1) .... @endif @endforeach - проверка итерации цикла
	@forelse ($users as $user) <div>{{$user}}</div> @empty <div>Не найдено</div> @endforelse - если в массиве есть элементы (но не null) 
										     переберет, иначе выведет @empty
	@continue, @break - в циклах начать с новой итерации / прервать цикл

	Команды в цикле @foreach:
	$loop->iteration - текущая итерация в цикле (начинается с 1)
	$loop->index - индекс текущей итерации (начинается с 0)
	@if($loop->first) - проверка на первую итерацию
	@if($loop->last) - проверка на последнюю итерацию
	$loop->count - общее количество элементов в итерируемом массиве
	@if($loop->even) - проверка на четную итерацию
	@if($loop->odd) - проверка на нечетную итерацию

387) PHPStorm комментирование:
	Многострочное: ctrl + /
	Все в один комментарий: ctrl + shift + /
     Комментирование в blade: {{-- $post --}}

388) Среда работы в .env константа APP_ENV:
	local - разработка
	production - продакшн

389) Проверка среды разработки:
	@production <h1>Продакшн</h1> @endproduction // например, для вывода инф-и во время разработки
	@env('local') <h1>Локал</h1> @endenv // то же самое, только по-другому

390) Для вывода материалов на сайте по дате:
     $posts = Post::orderBy('id', 'desc')->get(); // либо по полю created_at
     Затем в цикле выводим на странице.

391) Carbon - встроенная в Ларавель библиотека для работы с датами.
     Без импорта класса в blade:
     {{ \Carbon\Carbon::createFromFormat('Y-m-d H:i:s', $post->created_at)->format('d.m.Y') }} // => 08.09.2022
     С импортом класса:  <?php use Carbon\Carbon; ?>, тогда:
     {{ Carbon::createFromFormat('Y-m-d H:i:s', $post->created_at)->format('j.m.Y') }}   

То есть из пространства имен Carbon используем класс Carbon со статическим методом createFromFormat

392) Вместо того, чтобы писать такие конструкции в blade мы можем прописать 
эту функцию в модели и вызвать ее в blade. 
То есть $post - это объект модели Post и мы можем в модели(это тот же класс) Post сделать 
дополнительные методы для объектов $post:
	public function getPostDate(){
         	return Carbon::parse($this->created_at)->diffForHumans();} // данный метод Carbon возвращает 
									      дату в формате: 3 дня назад 
									      или 32 минуты назад

Важно: к создаваемым объектам модели внутри обращаемся как к $this, то есть это объект/запись БД, у которой
есть поле created_at.

А в blade вызываем метод объекта: {{ $post->getPostDate(); }}

393) IntlDateFormatter - php класс для форматирования даты в нужную локаль (ru)

394) Редирект в контроллере:
	return redirect()->route('home');

395) В верстке blade все ссылки делаем через роуты на именованные маршруты {{ route('home') }}, 
     даже если на главную <form method="post" action="{{ route('home') }}">

396) Нужно было в select вывести названия рубрик из базы, делается так:
	В контроллере запрашиваем из базы ассоциативный массив рубрик:  
	$rubrics = Rubric::query()->pluck('title', 'id')->all(); // метод pluck (строки какого поля будут значениями, строки какого поля будут ключами)
	return view('create', ['title' => 'Создать пост', 'rubrics'=>$rubrics]); // отправляем во вьюху

	Во вьюхе выводим через цикл:
	<select name="rubric_id">
            @foreach($rubrics as $k=>$v)
               <option value="{{ $k }}">{{ $v }}</option>
            @endforeach
    	</select>

397) Данные отправляются на роут методом post <form method="post" action="{{ route('home') }}">.
     Роут принимает запрос и отправляет на метод store: Route::post('/', [HomeController::class, 'store'])->name('posts.store');
     Контроллер принимает данные формы в объекте $response класса Response: // так понимаю, что данные в него записываются из $_REQUEST
	public function store(Request $request){
        	Здесь можно либо отправить форму целиком, предварительно 
		заполнив массив protected $fillable в Модели:
		Post::query()->create($request->all()); // $request->all() это массив $_REQUEST

		Либо распарсить и отправить:
		$post = new Post(); // создали новый объект модели
        	$post->title = $request->input('title'); // заполняем поля таблицы
        	$post->content = $request->input('content');
        	$post->save(); // сохраняем запись в БД

		Короткая запись: (new Post())->fill(['title'=> $request->input('title'), 'content'=> $request->input('content')])->save();
				
        	return redirect()->route('home');} // после отправки формы переходим на главную страницу

398) Перед тем как отправить данные в базу можно их проверить:
	public function store(Request $request){
        	$request->validate([
            		'title' => 'required|min:5|max:100',
            		'content' => 'required',
            		'rubric_id' => 'integer']);
        Post::query()->create($request->all());
        return redirect()->route('home');}

Если ошибок не было, то данные отправятся и отработает дальнейший код (у нас редирект). 
Если ошибки были, то произойдет перенаправление на страницу формы, ошибки будут доступны в объекте $errors:
  Выводим список ошибок:
	 @if ($errors->any())
            <ul>
            	@foreach ($errors->all() as $error)
                   <li>{{ $error }}</li>
            	@endforeach
            </ul>
        @endif

Если много форм, то можно разместить этот код в layout, либо сделать отдельный файл и инклюдить в нужные места.

Значения из инпутов последнего запроса записываются флеш переменными в сессию, когда происходит перенаправление обратно на форму 
из-за ошибок валидации, то мы можем заново вставить их в форму, чтобы пользователь не вводил еще раз:
	<input type="text" name="title" value="{{ old('title') }}">

	Либо в коде: $title = $request->old('title');

	old('title', $post->title) - если title не было в сессии, то вставляется $post->title

Также у нас доступна функция @error('input_name')  ... @enderror которая срабатывает, если есть ошибка в 'input_name'.
Например, мы можем добавить класс инпуту с ошибкой: 
<input name="title" type="text" value="{{ old('title') }}" class="form-control @error('title') is-invalid @enderror">

Также внутри функции @error доступна переменная @message, которая содержит 
сообщение об ошибке (которое показывали списком в цикле). Мы можем вывести его под целевым инпутом:
	@error('title') 
            <div>{{ $message }}</div>
        @enderror

Экскурс в php: флеш сообщения выводятся по такому алгоритму:
	if (isset($_SESSION['flash'])) {
		echo $_SESSION['flash']; 
		echo $_SESSION['flash'];
		echo $_SESSION['flash']; // вывести можно сколько угодно раз, она исчезает только ПОСЛЕ ПЕРЕЗАГРУЗКИ страницы!!!!!
		unset($_SESSION['flash']);}
В ларавель флеш хранится по ключу _flash в session()->all();

399) Ошибки выводятся на английском языке. Мы можем переписать их в настройках Ларавель, либо задать в функции:
	
public function store(Request $request){
        $rules = [
            'title' => 'required|min:5|max:100',
            'content' => 'required'];

        $messages = [
            'title.required' => 'Заполните название',
            'title.min' => 'Минимум 5 символов в названии'];

        Validator::make($request->all(), $rules, $messages)->validate();

        Post::query()->create($request->all());
        return redirect()->route('home');}

400) Либо устанавливаем регион ru (congig=>app=>'locale'=>'ru'). В папке lang создаем папку ru,
копируем туда validation.php и меняем строчки.
Там где стоит :attribute будет подставляться name инпута, мы можем задать им русские названия внизу в массиве:
	'attributes' => ['title' => 'Название', 'content' => 'Текст'],
 
401) Куски кода наподобие вывода ошибок @if ($errors->any()) можно вкладывать в отдельные blade страницы 
и вставлять в нужные места через include

402) Данные из инпутов записываются в сессию, сама же сессия хранится во все том же объекте $request.
В этом же объекте вижу данные из $_SERVER, $_COOKIE, $_METHOD, гет параметры $_GET и др.
Разобрался, $request - объект входящего HTTP-запроса
	
403) get_headers('http://url') - php функция возвращает все заголовки из ответа сервера на HTTP-запрос

403) К переменным сессии можем обратиться через:
	 $request->session->all();
	 через помощник session()->all(); // session() - сокращенная версия верхней функции

     В текущей сессии вижу:
	_previous - url предыдущей страницы
	_flash - флеш переменные (пустые)	
	_token - идентификатор сессии Ларавель, используется также для подписи формы @csrf

404) Работа с сессиями:
	session(['key' => 'value']); // записать в сессию, то же самое, что session()->put('key', 'value');
	session('test') - получить по ключу
	session('test', 0) - получить по ключу, если нет ключа, то значение по умолчанию
	session()->get('test') - получить по ключу
	session()->put('arr', ['a', 'b', 'c']); - записали по ключу arr массив
	session()->push('arr', 'd'); - добавили в этот массив элемент 
	session()->pull('test') - вывести переменную и удалить ее из сессии, думаю это те же флеш сообщения
	session()->forget('test'); - удалить переменную
	session()->flush(); - удалить все переменные сессии // очищается даже _token и тут же присваивается новый
	session()->flash('success', 'Данные сохранены'); - запись флеш сообщения, которое удаляется ПОСЛЕ обновления страницы
                                                           Флеш переменную можно вывести сколько угодно раз, она всегда существует
                                                           до следующего обновления страницы.
	
405) Сессии PHP хранятся на сервере в файле с идентификатором сессии (кука).
     Сессии JS хранятся в браузере в sessionStorage, они не пересекаются с php сессиями сервера
     Куки хранятся как в браузере в document.cookie, так и на сервере в $_COOKIE - они одни и те же 
	  и могут устанавливаться сервером при помощи заголовка Set-Cookie и js - document.cookie = "user=John"
     Однако параметр HttpOnly в PHP заголовке запрещает js (document.cookie) читать такую куку:
	Set-Cookie: id=a3fWa; Expires=Wed, 21 Oct 2015 07:28:00 GMT; Secure; HttpOnly

406) Ларавель шифрует куки, в браузере пользователя значения ключей в зашифрованном виде

407) Установка куки: 
	1) В очередь, отправятся со следующим ответом сервера
		Cookie::queue('test', 'test_cookie', 1); // 3 аргумент - минуты
	2) При ответе сервера:
		return view('post.show')->cookie('name', 'value', 10);

408) Чтение: 
	Cookie::get('test')
	$request->cookie('test')

409) Удаление:
	Cookie::queue(Cookie::forget('test'));

410) При некоторых операциях, например расчетах или запросах в БД, которые могут потребовать больших ресурсов ЦП
или занять несколько секунд, данные при первом запросе обычно кешируют на некоторое время, чтобы их можно было
быстро извлечь при последующих запросах тех же данных. 
	Cache::put('key', 'value', 3600)  - запись в кеш, третий аргумент  - время хранения в секундах	
	Cache::put('key', 'value') - запись в кеш  с хранением бесконечно
	Cache::get('key'); - получение данных из кеша
	Cache::has('key') - проверка существования по ключу

Также есть функция-помощник cache(), выполняющая то же самое:
	$value = cache('key'); // получить
	cache(['key' => 'value'], $seconds); // положить
	
Как это работает:

	if(Cache::has('posts')){ // проверяем есть ли в кеше
                $posts = Cache::get('posts'); // получаем если есть
            } else {
                $posts = Post::orderBy('id', 'desc')->get(); // если нет в кеше запрашиваем в БД
                Cache::put('posts', $posts); // ложим в кеш на случай следующих запросов
            }
	return view('home', compact('posts'));	

Теперь при следующем запросе страницы данные возьмутся из кеша, а sql запрос выполняться не будет

Теперь сервер всегда будет показывать данные из кеша, даже если мы обновили записи в БД, 
поэтому нужно предусмотреть сброс кеша при изменении данных, либо хранение его опр. время:

А) Храним сутки: Cache::put('posts', $post, 86400); // напр. добавляем статьи раз в сутки, поэтому и кеш храним сутки

Б) Либо удаляем из кеша принудительно при добавлении записей:
	1) Cache::pull('key') - показать и тут же удалить dump(Cache::pull('key')), аналогично session()->pull('key')
	2) Cache::forget('key') - просто удалить
	3) Cache::flush(); - очистить весь кеш

Файлы кеша лежат в Ларавель в storage-framework-cache-data.
Также Ларавель позволяет хранить файлы кеша в БД, в хранилищах с быстрым доступом данных, 
например, Memcached или Redis. Настраивается в config/cache.php

411) Для обработки фронтенд файлов Ларавель раньше использовал Ларавель Микс, сейчас Vite.
При создании сайта все хранится в resource в папках css, js, img. 
Сборщики наподобие Ларавель Микс забирают оттуда файлы, минимизируют, объединяют, выполняют другие 
функции и грузят в папку public, с которой сервер и использует файлы.

Vite в Ларавель установлен уже по умолчанию.

Меняем названия исходных и выходных папок в настройках Vite, делаем другие настройки.

В шапке layout можем подключить файлы как обычно: 
<link rel="stylesheet" href="./css/styles.css">

Либо воспользоваться функцией Ларавель asset, указывающей путь к файлам по константе ASSET_URL из .env (по умолчанию asset ведет на корень сайта, а корень мы прописали в .htaccess как /public):
<link rel="stylesheet" href="{{ asset('css/styles.css') }}"> // помощник asset() дополняет путь до абсолютного в соотв. с ASSET_URL или APP_URL 
При разработке через домен на опенсервере прописал константы вот так и файлы подключились:
	APP_URL=http://barmaley.ru
	ASSET_URL=http://barmaley.ru/assets // если использую vite, то так не нужно, потому что vite использует этот ASSET_URL для путей к build, а ложит build в корень
Соответственно в папке public/assets размещал папки css, js

Константа ASSET_URL:  ASSET_URL=http://laravel.ru  // работало и без этой константы, видимо если ее нет, то путь берется из APP_URL
Так, мы можем иcпользовать для хранения внешний сервис: ASSET_URL=http://example.com/assets

Итоговый url HTTP запроса получается такой: http://laravel.ru/css/styles.css

Получается все запросы идут в папку public, которая была указана в .htaccess // эта папка по умолчанию прописана еще где-то, так как работало и без этой настройки. Может на сервере

412) Также можно подключать отдельные скрипты через переменные страниц:
      Записываем скрипт в переменную страницы: 
	@section('scripts')
		<script>window.addEventListener('load', function (e) {alert('Текст')})</script>
	@endsection

      Выводим скрипт в layout, например перед закрывающим тегом body: 
	@yield('scripts')

413) через зону loc лучше называть сайты при разработке через openserver, иначе если сайт существует, например.
laravel.ru, то если посетил этот сайт, на локальный уже не заходит.
Переменная Ларавель для формирования ссылок устанавливается в .env константами 	APP_URL, ASSET_URL (если с vite, то лучше не менять, потому что использует ASSET_URL)

414) Отправка почты через Ларавель:
	1) в .env прописываем настройки почты, сохранил в Полезное на раб/ст.
	2) Создаем класс для формирования письма: php artisan make:mail TestMail. 
	   Класс создается для определенной темы, например: Пришел заказ, Ваш заказ отправлен и т.д.
	   Внутри класса TestMail {
		public $body; // сюда будем принимать текст письма
		public function __construct($body){$this->body = $body;} // здесь записываем текст в свойство класса
		public function build(){return $this->view('mail', ['body' => $this->body]);}} // здесь класс отдает вьюху письма с вставленным текстом
	3) Принимаем все запросы на метод send контроллера: 
		public function send(Request $request){ // этот метод можно не описывать, он встроен, ниже есть второй блок с описанием отправки письма
			 if($request->method() == 'POST'){ // и get и post запросы уходят на один и тот же метод, поэтому проверяем метод и либо отправляем письмо, либо просто показываем форму
			    $body = "<p><b>Имя: </b>{$request->input('name')}</p>"; // формируем тело письма
			    $body .= "<p><b>Сообщение: </b><br>" . nl2br($request->input('text')) . "</p>"; 
			    Mail::to('rodnovspb@mail.ru')->send(new TestMail($body)); // отправляем письмо 
			    Mail::to('rodnovspb@mail.ru')->queue(new TestMail($body)); // или ставим в очередь и затем запускаем ее
			    session()->flash('success', 'Сообщение отправлено');
            		    return redirect('/send');} // редирект на ту же страницу, чтобы флеш сообщение удалилось	
                         else { return view('send')}} // если был get запрос формы, то показываем ее
	4) Создаем роут для вызова метода send:
		Route::match(['get', 'post'], '/send', [ContactController::class, 'send'])->name('create_mail');

Как работает: 
	1) Если страница запрашиваеся методом get, то просто показывается форма.
	   Если пользователь через нее создает письмо, нажимает Отправить, форма 
           отправляется на эту же страницу методом POST.
	2) Метод send в первом ифе вызывает метод отправки письма Mail::to, создается новый объект класса TestMail:
		Mail::to('rodnovspb@mail.ru')->send(new TestMail($body));
	3) У объекта вызывается метод  build, который возвращает текст письма text, письмо отправляется:
		public function build(){return $this->view('mail', ['body' => $this->body]);}}

415) Прикрепление файла к письму делается в Классе письма в методе build:
	return $this->view('mail', ['body' => $this->body])->attach(url('img/1.jpg'));

416) Удобно get и post запросы отправлять на один метод контроллера, например на send, 
а внутри проверять каким методом пришел запрос:
	if($request->method() == 'POST'){} else{}
	
417) Флеш сообщение отправленное на ту же страницу не будет пропадать при обновлении страницы, если переход был 
просто через return view(). 
Поэтому используем редирект: return redirect('/page');

418) В textarea полученном из инпута перевод строки будет стоять как /n
     Если мы хотим вывести правильные переносы, то используем функцию nl2br(текст),
     которая перед каждым \r\n, \n\r, \n и \r ставит <br>

419) Проверка пароля при регистрации по полю Подтвердите пароль:
	Проверяемый инпут: <input type="password" name="password">
	Проверяющий инпут: <input type="password" name="password_confirmation">
	То есть к названию поля добавляется _confirmation
	
	Правило валидации задается через флаг confirmed:
		$request->validate([
            		'name' => 'required',
            		'email' => 'required|email|unique:users',
            		'password' => 'required|confirmed']);

420) Пароли отправляем в базу хешированными:
	Hash::make($request->password)

421) Проверка пароля:
	if (Hash::check('pass', $hashedPassword)) {// Пароли совпадают .. } // при входе Auth::attempt делает автоматически с хешированием

422) Аутенцифицировать пользователя на сайте:
	Auth::login($user) // сюда отправляем объект пользователя

423) Вся функция регистрации:
	public function store(Request $request){
        	$request->validate([
            	'name' => 'required',
            	'email' => 'required|email|unique:users', // unique проверяет уникальность в таблице users в колонке email
            	'password' => 'required|confirmed']);

        	$user = User::create([ // создаем запись в БД и возвращаем объект пользователя, чтобы сразу аутентифицировать
            	'name' => $request->name, // можно писать так, вместо $request->input('name')
            	'email' => $request->input('email'),
            	'password' => Hash::make($request->password)]); // в БД отправляем зашифрованный пароль, 
								   то же самое bcrypt($request->password)
        	session()->flash('success', 'Успешная регистрация');

		Внимание! проверяем в User:
		use Illuminate\Foundation\Auth\User as Authenticatable; - в шапке
		class User extends Authenticatable{}

        	Auth::login($user); // аутентифицируем, чтобы не вводил пароль заново для входа
        	return redirect()->home();} // способ сделать редирект на главную

424) Auth::login($user) - аутентификация по объекту пользователя

     В Ларавель начнутся сложности, если делать разные таблицы для админов и users. Например, новый код для попыток входа.
     Кроме того Auth:: работает по умолчанию с таблицей users. Поэтому все таки проще в таблицу users добавить поле is_admin или role

425) Auth::check() - два варинта проверки аутентификации, возвращает true или false, 
     auth()->check()   соответственно можно показывать кнопку для выхода, входа. 

426) Auth::logout(); - выйти.

     В роутах на страницу выхода пускаем только аутентифицированных:
        Route::get('/logout', [UserController::class, 'logout'])->name('logout')->middleware('auth');


     Контроллер для выхода:
	public function logout(){
        Auth::logout();
        return redirect()->route('login.create');}
	
427) Auth::attempt([]) - войти на основе введенной почты и пароля. Подробнее:
	if(Auth::attempt([
            'email' => $request->email,
            'password' => $request->password, // хешировать не нужно, проверяется автоматически
        ])) {
	    $request->session()->regenerate(); - защита от атаки фиксацией сессии
            return redirect()->home();
        };

428) Auth::user()->name   - 3 варианта получения свойства текущего пользователя
     auth()->user()->name
     $request->user()	 

     Проверка на права: 
	if(Auth::user()->isAdmin){ // если поле isAdmin равно 1
                return redirect()->route('admin.index');} // перенаправляем на админку	

429) Кроме того существуют удобные blade директивы:
	@auth
	    выполнится если авторизован
	@endauth

	@guest
	    выполнится если не авторизован 
	@endguest

430) return redirect()->home() - редирект на главную
     return redirect()->route('form') - редирект на именованный роут
     return redirect('/page') - редирект на адрес
     return redirect()->back() - редирект на эту же страницу, например, если неправильный логин или пароль на входе	
     return redirect()->back()->with('error', 'Неправильный пароль') - редирект на эту же страницу с флеш переменной в сессии
     return redirect()->action([HomeController::class, 'index']) - редирект на метод контроллера	
     return redirect()->route('profile', ['id' => 1]) - редирект на им. маршрут с параметрами

     Постоянный редирект с передачей слага из первого адреса во второй:
	Route::get('/{slug}/kupit', function ($slug){ return redirect()->route('site', $slug); });
     

431) Например, у нас есть раздел админки http://laravel.loc/admin
     Для того, чтобы в этот раздел не перешли гости или обычные пользователи существуют посредники middleware.

     Посредники выполняются после получения HTTP запроса к приложению, прежде чем роут перенаправит его в контроллер.
     Мы можем проверять аутентифицирован пользователь, админ ли он, регистрировать все HTTP запросы и т.д.
     То есть посредник выполняется перед отправкой запроса в контроллер, а шлюз можно прикрепить как в сам посредник ('can:delete') перед отправкой
     в контроллер, так и в самом контроллере: Gate::authorize('delete'). Посредник используем для более общих проверок (аутентифицирован), 
     а шлюзы для проверки прав (удалить пост)

432) Например, сделаем посредник для проверки на админа.
     Для этого в таблицу users нужно добавить колонку is_admin со значением по умолчанию 0
	1) php artisan make:migration alter_table_users --table=users   // создаем миграцию
	
	2)up: 	Schema::table('users', function (Blueprint $table) {
                 	$table->tinyInteger('is_admin')->default(0);});   // команда на вставку колонки is_admin
	  down: Schema::table('users', function (Blueprint $table) {
            		$table->dropColumn('is_admin');});

	3) php artisan migrate // изменяем БД

	4) php artisan make:middleware AdminMiddleware // создаем файл посредника в http/middleware
	
	5) Прописываем действия посредника внутри этого файла:
		public function handle(Request $request, Closure $next){ // эта функция сработает при обращении к посреднику
        		if(Auth::check() && Auth::user()->is_admin){  // если пользователь аутентифицирован и поле is_admin равно 1
            		   return $next($request);} // передаем HTTP запрос дальше
			else {
            		   abort(404);}} // иначе редирект на 404 страницу, либо на другую страницу

	6) Регистрируем посредника в app/http/Kernel -  в protected middlewareAliases добавляем имя:
		'admin' => \App\Http\Middleware\AdminMiddleware::class

	7) Прописываем посредника в роут админки:
		Route::get('/admin', [MainController::class, 'index'])->middleware('admin');

Наш посредник отправляет запрос дальше, срабатывают другие посредники и в итоге контроллер покажет нам админку.
     Пример с группировкой:
     Route::group(['prefix'=>'admin', 'middleware'=> 'admin'], function(){ // если группируем, то middleware прописываем здесь, а не в конце, иначе не работает
     Route::get('/', [MainController::class, 'index'])->name('admin.index');
     Route::resource('/categories', CategoryController::class);
     Route::resource('/tags', TagController::class);
     Route::resource('/posts', PostController::class);});	

433) В файле Kernel есть готовые посредники, в том числе auth и guest.
     Например для роутов страниц входа, регистрации (в обоих случаях get - показ формы, post - отправка формы) 
     ставим middleware guest:
     Route::group(['middleware' => 'guest'], function(){ // если группируем, то посредника указываем здесь, а не в конце
     Route::get('/register', [UserController::class, 'create'])->name('create.user');
     Route::post('/register', [UserController::class, 'store'])->name('store.user');
     Route::get('/login', [UserController::class, 'loginForm'])->name('login.form');
     Route::post('/login', [UserController::class, 'login'])->name('login');});	
				
     А так Ларавель будет пускать на страницу выхода только аутент. пользователя
     Route::get('/logout', [UserController::class, 'logout'])->name('logout')->middleware('auth'); //auth - встроенный посредник

     Можно встраивать посредник прямо в контроллере, для этого прописываем в нем конструктор, а в нем посредник:
	public function __construct(){ 
		$this->middleware('auth'); }

     Если пользователь авторизован и пытается попасть на страницу регистрации, логина, то его будет перенаправлять 
     в соответствии с путем в посреднике RedirectIfAuthenticated, прописываем там: return redirect()->route('home');
	
434) Аутентификация - проверка логина и пароля в базе на существование пользователя
     Авторизация - проверка прав на выполнение какого-либо действия (допустим, авторизован как модератор)

435) Если отправляем файл (input type="file"), то для формы нужно установить атрибут enctype="multipart/form-data" 	

435) Загрузка файлов в Ларавель на примере добавления пользователем аватара:
     1) php artisan make:migration alter_table_users_add_avatar --table=users
     2) up: $table->string('avatar')->nullable();
	down: $table->dropColumn('avatar');
     3) php artisan migrate
     4) <form method="post" action="" enctype="multipart/form-data"> // enctype для возможности отправки файлов
	<input type="file" name="avatar"> // инпут для файлов
     5) protected $fillable = [..., 'avatar'] // в модели User добавляем разрешенное поле для вставки в базу через create
     6) в UserController редактируем метод сохранения в базу store:
	  $request->validate([..., 'avatar' => 'nullable|image'] // сначала поле валидируется
     7)  После валидации:
	   if($request->hasFile('avatar')){ // если файл (или файлы) прикреплен, не рабоатет с CKFinder
	       если файлов несколько, то перебираем массив: $request->files('name') as $file и делаем для каждого	
               $folder = date('Y-m-d'); // создаем имя папки в виде текущей даты
               $avatarPath = $request->file('avatar')->store("images/$folder");} // грузим файл на сервер в storage/app/public/images , получаем путь к файлу
										
     8) $user = User::create([..., 'avatar' => $avatarPath ?? null,] // в БД создаем новую запись, в поле avatar прописываем путь к файлу или null 
     9) Так как файлы складываются в системной папке storage, а сайт к ней доступа иметь не будет,
	то нужно для storage в папке public создать символическую ссылку/ярлык:
	  php artisan storage:link
        Теперь в public появилась ссылка на storage и к ее файлам мы можем обращаться как:
	 laravel.ru/storage/images/2022-09-16/picture.jpg или через asset:
		@if(Auth::user()->avatar)
                   <img src="{{ asset("storage/" . Auth::user()->avatar) }}">
                @endif
	В поле Auth::user()->avatar в БД у нас хранится адрес картинки images/2022-09-16/picture.jpg

Также можем символическую ссылку не делать, а сразу складывать файлы в public, для этого в .env прописываем
	FILESYSTEM_DISK=public // установили канал public
а в самом канале public в root вместо storage_path (путь в storage) ставим public_path (путь в public).
Теперь можно в базу отправить ссылку на файл:
	 $data = $request->all();
	 if($request->hasFile('thumbnail')) { // можно исп. если файлов несколько // не рабоатет с CKFinder
            $folder = date('Y-m-d');
            $data['thumbnail'] = $request->file('thumbnail')->store("images/$folder");}
	 Post::create($data);

Удалить изображение из загруженных: 
	if($post->thumbnail){ // если существует
            Storage::delete($post->thumbnail); // в $post->thumbnail хранится путь к файлу
        }
	
436) Файлы из форм грузятся в storage/app
     Имя файла Ларавель по умолчанию создает свое
     Место хранения можно изменить в config/filesystems.php

437) Чтобы картинки складывались в storage/app/public нужно менять настройки хранения.
     В filesystems.php командой 'default' => env('FILESYSTEM_DISK', 'local') установлен драйвер хранения, 
     так как константы FILESYSTEM_DISK не существует, то используется драйвер по умолчанию local, который задает 
     место хранения как app (это все есть ниже в этом же файле). А другой драйвер public использует app/public
     Поэтому в .env добавляем константу: FILESYSTEM_DISK=public, теперь картинки складываются в app/public/images (images взято из команды store)

     Разница между local и public становится понятной, когда делаем симлинк стораджа в папку корневую папку public Ларавель. 
     В этом случае все что находится в storage/app/public становится доступным извне по пути: http://site.ru/storage/
     Соответственно при настройке public - все грузится в storage/app/public и доступно в http://site.ru/storage/
     А при local файлы грузятся в storage/app/ и недоступны извне, даже при симлинке. 
     К таким файлам можно обращаться только из Ларавель, напр.: Storage::get('file.jpg') - будет искаться в соотв. с настройкой
     корневой папки хранения local (local или public) - то есть в папке storage/app/

438) Допустим, мы выводим в подвале список рубрик, он ждет переменную $rubrics. Подвал выводится у каждой 
страницы, у которой есть свой контроллер, теперь нужно в каждом контроллере запрашивать список рубрик из базы и прикреплять к 
выводимой странице:
	$rubrics = Rubric::all();
	return view('page', compact('title', 'posts', 'rubrics'));

Мы можем создать view composer, который будет сам запрашивать и передавать нужные переменные во вьюху при ее выводе.

Делается так:
  1 способ) Регистрируем Вью Композер в app/Providers/AppServiceProvider:
	Пример 1:
	public function boot(){
        	view()->composer('layouts.footer', function($view){ // при выводе какой вьюхи будет срабатывать
                $view->with('rubrics', Rubric::all());});} // крепим пременную rubrics
	Пример 2:
	public function boot(){
        Paginator::useBootstrap(); // подключили отображение пагинации бутстрап, это сюда не относится
        view()->composer('layouts.sidebar', function($view){
            $view->with('popular_posts', Post::orderBy('views', 'desc')->limit(3)->get());});}

        Внимание: если вместо вьюхи в методе composer поставить '*', то переменная будет передаваться во все вьюхи

   2 способ) php artisan make:provider ViewServiceProvider  // создаем свой провайдер
	     Прописываем его в config/app.php в providers: App\Providers\ViewServiceProvider::class
	     Теперь все что пропишем в boot нашего провайдера будет срабатывать на каждой странице.
             Создаем папку ViewComposers в папке app, в ней создаем класс CategoriesComposer.php
	     В этом классе: public function compose(View $view) { // в шапке импортируем use Illuminate\View\View;
        			$categories = Category::query()->get();
        			$view->with('categories', $categories);} 
             В методе boot ViewServiceProvider пишем:
				View::composer('layouts.master', 'App\ViewComposers\CategoriesComposer');  // импортируем use Illuminate\Support\Facades\View;
	     Можно также помещать вьюхи в массив: View::composer(['layouts.master', 'categories'], 'App\ViewComposers\CategoriesComposer');
             В этом случае переменная categories будет пробрасываться во вьюху layouts.master и categories	        
	
То есть в нашем случае контроллер выводит вьюху какой-нибудь страницы, та вызывает свой layout, в нем
через include вставлена вьюха футера layouts.footer ждущая $rubrics. Теперь на этапе вставки футера  
отработает наш композер и прикрепит переменную rubrics

439) Для того, чтобы вывести пагинацию, нужно:
	1) При получении выборки заменить метод get на paginate:
	   	$posts = Post::orderBy('id', 'desc')->paginate(3); // аргументом указываем количество записей, в sql вставится:  select * from `posts` order by `id` desc limit 3 offset 3 // offset - сдвиг при выборе страницы
	   	return view('home', compact('posts'));
	2) Вставить вывод пагинации в верстку:
		<div>{{ $posts->links() }}</div>

440) Начиная с 8 версии Ларавель использует Tailwind CSS, мы же в этом проекте используем бутстрап, для того
чтобы Ларавель назначал бутстраповские классы элементам пагинации нужно в AppServiceProvider:
		1) подключить класс:  use Illuminate\Pagination\Paginator;	
		2) в методе boot прописать: Paginator::useBootstrap();
     Второй способ:
		 {{ $users->links('pagination::bootstrap-4') }}

441) В шаблонах назначаются классы элементам пагинации, сами же классы описываются в файлах стилей.
     Также мы можем использовать другие шаблоны или изменять их, для этого:
	1) Получаем готовые шаблоны пагинации в views/vendor/pagination :
	    php artisan vendor:publish --tag=laravel-pagination 
	2) Прописываем используемый шаблон:
	    {{ $posts->onEachSide(2)->links('vendor.pagination.bootstrap-4') }} 
     Мы можем также создать свой шаблон и так же его подключить.

441) Пагинация работает через гет параметр page: http://laravel.loc/?page=2
     При пагинации удаляются другие гет параметры, например параметры фильтров, инпутов. Для того, чтобы 
     другой гет параметр не удалялся нужно прикрепить его к выводу:
	{{ $posts->appends(['size'=> request()->size])->links() }} // разрешаем нужные гет параметры
	{{ $posts->appends(request()->query())->links() }} // либо разрешаем любые гет параметры

     Если в url будет гет-параметр size, то он не удалится. Если его не будет, то ошибки не будет тоже

     Либо в контроллере метод withQueryString: 
	$products = $productsQuery->paginate(6)->withQueryString();

442) request()->query() - массив гет параметров в Ларавель (["size" => "111", "page" => "4"])
     request()->query - коллекция гет параметров
     request()->getQueryString() - гет параметры в виде строки: "filter=ip&page=2"

443) Можно добавлять якоря к ссылкам пагинации:
	 {{ $posts->fragment('anchor')->links() }} => http://laravel.loc/?page=5#anchor

444) Регулирование количества отображаемых ссылок, работает только если страниц много:
	{{ $posts->onEachSide(3)->links() }}

445) Отображение только стрелками назад-вперед:
	$posts = Post::orderBy('id', 'desc')->simplePaginate(3);

446) php библиотека Faker используется для наполнения БД тестовыми данными. Ее методы встроены в фабрики Ларавель.
Можно генерировать случайные числа, строки, предложения, даты, имена, цвета, картинки, верстку, адреса и пр.
Устанавливается через композер composer require fakerphp/faker, в Ларавели уже встроена.
Для переключения Фейкера на русский язык меняем в config/app.php переменную 'faker_locale' => 'ru_RU',
методы фейкера для русского языка могут быть другими, например для анг. текста исп. команда text,
для русского realText: "content" => $this->faker->realText(15), - случайный текст длиной 15 символов
		       "text" => $this->faker->realText(mt_rand(10, 20)), - случайная текст длиной между 10 и 20 символами
									    минимум указывать 10, иначе ошибка

Есть интересная функция создания адреса картинки via.placeholder.com со случайным цветом:
	'src' => $this->faker->imageUrl(150,150, $this->faker->word(5), true) - размером 150*150, в центре случайное слово,
										цвет случайный. В описании есть др. настройки
	Получится: https://via.placeholder.com/150x150.png/006699?text=omnis+consequuntur		

446) Наполнение базы тестовыми данными (сидеры):
	1) php artisan make:seeder PostSeeder // создаем сидер
	2) php artisan make:factory PostFactory -m Post // создаем фабрику // лежит в database/factories
	3) В фабрике PostFactory используем встроеный в фабрику Faker:
	 	public function definition(){
             	     return [
            		"title" => $this->faker->words(3, true),
            		"content" => $this->faker->paragraph(1),
            		"created_at" => $this->faker->dateTime(),
            		"updated_at" => $this->faker->dateTime(),
            		"rubric_id" => $this->faker->numberBetween(1, 5),];}
	4) В модели Post должен быть прописан трейт use HasFactory; // обычно удалял его
	5) В PostSeeder прописываем метод:
		public function run(){
            		Post::factory(100)->create();} //в аргументе количество итераций; также не забыть use App\Models\Post; 
	6) DatabaseSeeder запускает сидеры, прописываем в нем:
		$this->call(PostSeeder::class); 
	7) php artisan db:seed // запускаем папу сидеров DatabaseSeeder, также можно запустить отдельные сидеры:
				  php artisan db:seed --class=UserSeeder - здесь минуя папу сидеров запускаем опр. сидер

Как работает: 
	1) команда php artisan db:seed запускает папу сидеров класс DatabaseSeeder
	2) Тот запускает сидер PostSeeder // или несколько перечисленных сидеров
	3) Сидер вызывает у модели Post метод factory и передает количество итераций
	4) PostFactory отрабатывает нужное количество раз

Также можно создавать сидеры на вставку определенных записей:
	1) php artisan make:seeder CurrencySeeder
	2)  public function run(){ DB::table('currencies')->insert([ // в CurrencySeeder создаем команду на вставку двух записей
            [
            'code' => 'RUB',
            'symbol' => '₽',
            'is_main' => 1,
            'rate' => 1
        ],
            [
                'code' => 'EUR',
                'symbol' => '€',
                'is_main' => 0,
                'rate' => 70]]);}}
	3) php artisan db:seed --class=CurrencySeeder // в БД вставилось две записи

	4) Можно вставить команду на вызов этого сидера в папу Сидеров DatabaseSeeder, чтобы отрабатывал при php artisan db:seed:
	   $this->call(CurrencySeeder::class);
		
PS: DB::table('users')->truncate(); - команда на удаление всех строк таблицы с обнулением ID, может применяться перед сбросом таблицы
    в ноль перед заполнением новыми данными.
    На продакшене естественно удалит все строки, быть осторожным.

Также Ларавель умеет создавать связанные записи, напр, в поле user_id. Для этого в поле нужно вставлять связанную фабрику:
	class ArticleFactory extends Factory {
		 public function definition() {
       			return [
           			'title' => $this->faker->sentence,
           			'user_id' => User::factory(),];}} - создастся новый пользователь и его id привяжется к полю user_id
Также есть другие способы. Есть в документации и здесь.

447) Мутаторы - функции модели, которые перехватывают поля и изменяют их значения перед вставкой записи в БД. Те же сеттеры.
     Например, контроллер вызывает метод модели:
	User::create(['name' => $request->name, 'email' => $request->input('email')])

     Модель прежде чем отработать вызывает мутатор поля name:

	protected function firstName(): Attribute { / писать сamelCase, если в свойстве есть _, то пропускать его
        	return Attribute::make(  // в шапке импортируем use Illuminate\Database\Eloquent\Casts\Attribute;
            		get: fn ($value) => ucfirst($value), - это аксессор 
            		set: fn ($value) => Str::title($value),);} - это мутатор // Метод Str::title делает первые буквы слов заглавными

		К свойству Модели можно также обращаться так:
			$this->attributes['name']
        
     Для преобразований можно использовать как встроенные помощники Ларавель, так и php код. 

     Больше делать ничего не надо, теперь значение инпута name вставлено в базу с большой буквы в каждом слове.
     Мутаторы отрабатывают при каждом обращении к модели, соответственно при посеве тоже. 

448) Аксессоры делают противоположную задачу - меняют значения полученные из БД перед тем как модель отдаст их 
     в представление, это те же геттеры.
     Аксессоры также вставляются в модель и отрабатывают по умолчанию:
		Начало кода берем из мутатора
	 	get: fn ($value) => Str::upper($value), // перевели title в верхний регистр, использовали хелпер Str::upper()
        	
    Больше делать ничего не надо.

449) В ларавель очень много т.н. хелперов, они сокращают названия функций Ларавель или заменяют функции php,
надо их изучить https://laravel.su/docs/8.x/helpers

450) Логирование.
В режиме разработки все ошибки показываются сразу, на продакшене ошибки принято скрывать, для этого в .env:
	  APP_DEBUG=false
В этом случае вместо текста ошибки будет показывать 500 страница.
Как же посмотреть ошибки?
Ларавель по умолчанию записывает ошибки в storage/logs/laravel.log
Однако файл ограничен по размеру и неудобно, когда все ошибки в одном большом файле. Можно раскидать ошибки по датам.
Настройки логгирования задаются в config/logging.php
Проще сделать это через .env:  меняем константу LOG_CHANNEL на LOG_CHANNEL=daily
Принцип работы здесь такой же как когда меняли папку для картинок через константу FILESYSTEM_DISK=public.

Можно создавать файлы для логирования любых действий. Создаем в logging.php новый канал:
	'sqllogs' => [ // это имя нашего канала
            'driver' => 'single', // здесь можно поставить daily для логгирования по дням
            'path' => storage_path('logs/sql.log'), // здесь задаем файл хранения логов
            'level' => env('LOG_LEVEL', 'debug'),],

Для логгирования используется фасад Log и методы info, error, warning // методы отличаются комментариями, без разницы
Например: Log::error($message);

Теперь по каналу можно записывать данные, например в методе boot ASP будем логгировать все sql запросы:
	DB::listen(function ($query){
            Log::channel('sqllogs')->info($query->sql); // по каналу sqllogs записываем все sql запросы
        });

451) Дебаг бар работает только если константа APP_DEBUG=true

452) Видимо метод boot в AppServiceProvider срабатывает постоянно в работе Ларавель, потому что через него можно
показывать все sql запросы, логгировать и т.д.

453) При разработке принято хранить файлы css, js, картинки и др. в resources, 
а через сборщик (laravel-mix, vite) копировать их в public

454) Помощник url формирует полную ссылку с http по указанной части пути, напр.:
	url('user/profile')
	url('/')
455) В миграциях при создании поля id лучше менять $table->id()/бигинт 8 байт на $table->increments('id') / инт 4 байта

456) $table->integer('views')->unsigned()->default(0):
	1) unsigned() - запрещаются отрицательные значения, увеличивает число хранимых положительных чисел в 2 раза
	2) default(0) - устанавливает значение по умолчанию
	3) nullable() - необязательное для заполнения поле, при пустом значении будет стоять NULL // отмена в down: nullable(false)->change()
	4) text - поле типа TEXT
	5) string - Поле VARCHAR
	6) increments('id') - первичный autoincrement ключ
	7) integer() - поле INTEGER
	8) timestamps() - столбцы created_at и updated_at
	9) timestamp('added_on') - поле TIMESTAMP
	10) tinyInteger('numbers') - поле TINYINT
	11) unique() - уникальная запись. Для такого поля применяется индексирование, по такому полю поиск быстрее
	12) index() - // тоже индексируется, Ларавель допускает дубликаты
	13) double() - дробные числа с указанной точностью $table->double('amount', 8, 2), исп, где расчеты конеч. цифр не важны;
		       double(8,2) - допускается не более 8 цифр, где 2 - после запятой
	14) decimal() - дробные числа, для фин. операций, более точно, то больше памяти $table->decimal('amount', 8, 2);
		        decimal(8,2) - в общем не более 8 цифр, где 2 будет после запятой (правило валидации max:999999)
	14) date() - дата  ГГГГ-ММ-ДД

457) Если классы лежат рядом в одной папке и указаны namespace App\Models, то импортировать 
     через use App\Models\Category не нужно

458) Слаг - ссылка транслитерацией
	Для создания в Ларавель есть два способа:
	1) Через помощник Str::slug:
	   $slug = Str::slug('Русский текст QWERTY!!!', '-', 'ru') => russkii-tekst-qwerty :
	       1) все нижним регистром
	       2) пробелы превращны в указанный разделитель
	       3) русский текст транслитом
	       4) удалены знаки
	       5) Внимание! Если не указывать язык 'ru', то будут неточности, например буква Х будет как X, а не H.
                  Поэтому ставить язык.
	       6) Настраивать транслитерацию: src/voku/helper/data/ascii_by_languages.php => в ключе 'ru' меняем то, что нужно	

Нужно иметь ввиду, что т.к. слаг будет ссылкой на что-то, то он должен быть уникальным. Если вставляем в БД через
эту функцию, то сначала должны проверить уникальность, поэтому есть более простой способ Laravel Eloquent-Sluggable

	2) Трейт Laravel Eloquent-Sluggable:
        	Порядок: 
	        1) composer require cviebrock/eloquent-sluggable // устанавливаем
		2) В моделях где нужны преобразования в слаги прописываем:
		    use Cviebrock\EloquentSluggable\Sluggable;
                        class Post extends Model {
                            use Sluggable;
    			    public function sluggable(): array {
        			return [
                                    'slug' => [  // в какое поле таблицы вставлять слаг, должен быть nullable
                		    'source' => 'title']];}} // из какого поля объекта на вставку в БД брать текст для превращения в слаг
   
При вставке в модель этого кода  больше ничего делать не надо.
Если такой слаг уже существует, то к слагу добавится -1, -2 и т.д.

Слаг не будет меняться, если мы потом будем обновлять запись, это нужно потому, что ссылка может быть проиндексирована.
Если же слаг нужно поменять при обновлении записи, то делаем так:
	$category = Category::query()->find($id);
        $category->slug=null; // эта команда включает обновление слага
        $category->update($request->all());

В метод sluggable можно встроить условие, когда он должен срабатывать. Например, мы в форме можем сами писать слаг.
	public function sluggable(): array {
        	return request()->uri ? [] : ['uri' => ['source' => 'meta_title']];} // если не заполнили слаг в uri, 
											то сработает sluggable для поля meta_title

459) Если на сайте будет применяться поиск по записи, например, по слагу, то применям команду unique (либо index()):
	$table->string('slug')->unique(); // Ларавель не допускает дубликаты
	$table->string('surname')->index(); // Ларавель допускает дубликаты
     Теперь в поле есть серый ключик Индекс, оно индексируется, теперь при выборке записи по слагу БД будет 
     отдавать запись быстрее.

460) Желтый ключик - Первичный индекс, ключевые поля таблицы, в нем обычно стоят id, не должно быть дубликатов.
	 $table->id() - в виде бигинт 8 байт;
	 $table->increments('id'); - в виде int 4 байта

     Серый ключик - Вторичный индекс, индексированное поле, MySQL допускает дубликаты, устанавливается и при unique и при index
	 $table->string('slug')->unique() - индексированная колонка, уникальные значения, Ларавель не допускает дубликаты. Достаточно для поиска в строке.
	 $table->string('slug')->index() - индексированная колонка, возможны дубликаты

     По индексированным полям выборка быстрее. Но индексы замедляют БД, поэтому их устанавливают на избранные поля.

461) То есть мы провели миграцию для создания таблиц, ничего не заполняли, 
     но решили изменить таблицы (напр. сделать поле индексируемым). Добавляем нужны строчки в старые миграции и выполняем:

     php artisan migrate:refresh

     Таблицы удалятся и заново создадутся.

462) Посмотреть все маршруты в Ларавель: php artisan route:list

463) В Admin LTE можно копировать верстку форм, окошек входа, инпутов, модальных окон, таблиц, календарей, почты, страниц пользователей и т.д.
	Выбираем нужное, F12, copy->copy Element -> если вставляется без отступов, 
        то форматируем через serblog.ru/demo/format-html/

464) Посмотреть все маршруты в Ларавель: php artisan route:list

465) Возвращаемый массив объектов из БД мы не сможем проверить на empty (т.к. из-за доп. информации он всегда непустой), 
проверяем на непустоту методом $categories->count() или count($categories):
	@if($categories->count())
		<div>$categories->title</div>
	@endif

466) Куски кода можно выносить в функцию модели, а в контроллере обращаться к этому методу. Например вынесли в модель функцию создания пути к загруженному файлу:
     Не забываем про multipart/form-data в форме

	public static function uploadImage(Request $request){
        if($request->hasFile('thumbnail')){ // если в инпуте type="file" есть файлы(файлы), не работает с CKFinder
            $folder = date('Y-m-d');
            return $request->file('thumbnail')->store("/images/{$folder}"); // сохраняем изобр. и возвращаем путь
        }
        return null;} // если не было изображения, то возвращаем null

	А в модели в массив инпутов записываем либо путь к файлу, либо null:

	$data = $request->all(); // получаем массив инпутов
        $data['thumbnail'] = Post::uploadImage($request); // по ключу thumbnail записываем путь
	Post::query()->create($data); // сохраняем
	
467) Либо подстановки в возвращаемый объект модели путь к файлу, когда изображения нет:
	public function getImage(){
        if(!$this->thumbnail){
            return asset('no-image.gif');
        }
        return asset('uploads/' . $this->thumbnail);}
	
    Теперь в верстке выводим изображение:
	<img src="{{ $post->getImage() }}" alt=""> // обращаемся к методу модели

468) Например, у поста есть соответствующие ему теги. Эти теги прописаны в таблице post_tag
     Например, на странице редактирования поста мы убрали некоторые теги, некоторые добавили.
     Теперь промежуточную таблицу связей post_tag можно отредактировать c помощью метода sync:
	 if(count($post->tags) > 0){ // если у поста есть какие-то теги, неважно прошлые или новые, то синхронизируем с таблицей связей
            $post->tags()->sync($request->tags);} //	где $request->tags - новый массив тегов поста, взятый из инпута.  
						Новые добавятся, те, которых нет в новом массиве удялятся из post_tag
						Метод $post->tags() - тот из модели где получаем теги belongsToMany
     
     Если же пост удаляем, то связи в таблице связей обнуляем: $post->tags()->sync([]);	// передали пустой массив

469) Если текст хранится в базе с тегами <h3>Это заголовок</h3>, например, после визуального редактора, 
     то выводить его нужно в таких скобках: 
	{!! $post->text !!}
	{!! clean($text) !!} - пакет mews/purifier удалит опасные теги
     

470) Проверка на страницу, например при добавлении класса:
	<section class="section lb @if(Request::is('/')) add_class  @endif">

471) Поиск файла в PhpStorm: нажать Shift 2 раза

472) В лэйауты вставлять универсальный показ флеш сообщений об успешности и ошибках:
		@if(session()->has('success'))
                    <div class="alert alert-success">
                        {{ session('success') }}
                    </div>
                @endif
                @if(session()->has('error'))
                    <div class="alert alert-danger">
                        {{ session('error') }}
                    </div>
                @endif

473) На случай, если записи может не оказаться существуют метода:
	Post::findOrFail(id) - найти запись по id или вернуть 404 ошибку
	Post::where('slug', $slug)->firstOrFail() - найти запись по условию или вернуть 404 ошибку

474) Получить количество связанных объектов методом withCount:
	$categories = Category::query()->withCount('posts')->orderBy('posts_count', 'desc')->get()
	Метод withCount помещает атрибут {relation}_count в результирующие объекты.
	Здесь мы просто отсортировали категории, однако также можем обратиться в объекте категории к полю posts_count.

475) Поиск в ларавель по базе данных:
	1) Сначала нужно навесить индекс на поле поиска, создаем миграцию:
	php artisan make:migration alter_table_posts_add_title_index --table=posts

	2) up: $table->index('title'); // навешиваем индекс на поле title
	   down: $table->dropIndex('title');
	   php artisan migrate

	3) php artisan make:controller SearchController // создаем контроллер

	4) Route::get('/search', [SearchController::class, 'index'])->name('search'); // создаем путь на этот контроллер

	5) Создаем форму поиска:
	   <form method="get" action="{{ route('search') }}"> // поисковые запросы принято передавать методом get
          	<input name="s" type="text" placeholder="Поиск" class="@error('s') is-invalid @enderror"> // +класс при неправильном заполнении
          	<button type="submit">Найти</button>
           </form>

	6) Пишем контроллер:
	   public function index(Request $request){
        	$request->validate([
            	's' => 'required']);} // валидируем запрос. При ошибке инпуту добавляется класс is-invalid

		$s = $request->s; //записываем в переменную s строку поиска
        	$posts = Post::where('title', 'LIKE', "%{$s}%")->with('category')->paginate(5); // оператор LIKE ищет по шаблону
											           with - жадно запрашивает категории постов
		return view('posts.search', compact('posts', 's')); // возвращаем вьюху + найденные записи и строку запроса

	Можно искать по всем полям таблицы (очень удобно):
		$s = $request->s;
		$orders = Order::query()
          		->where('id', 'LIKE', "%{$s}%")
          		->orWhere('page', 'LIKE', "%{$s}%")
         	        ->orWhere('address', 'LIKE', "%{$s}%")
          		->orWhere('inn', 'LIKE', "%{$s}%")
			->orderByDesc('id')->simplePaginate(10)->withQueryString();
	
	7) Во вьюхе выводим найденные посты или Ничего не найдено:
	   @if($posts->count())
              @foreach($posts as $post) 
		  {{ $post->content}}
	      @endforeach
            @else
                 <h3>Ничего не найдено</h3>
            @endif
	   
	   Также для того чтобы, в пагинации добавлялся поисковый запрос можно использовать appends:
	   {{ $posts->appends(['s'=> request()->s])->links() }}
   
           Также есть метод withQueryString, добавляющий гет параметры при переходе на др. страницу по пагинации:
		$products = $productsQuery->paginate(6)->withQueryString(); // пишем в контроллере

476) Частые части запросов или запросы полностью можно оборачивать в диапазоны (scopes).
     Внимание! Встраивать такой метод можно только в контроллере, в модели не получится.

     В Модели создаем функцию:
	public function scopeLike($query, $s){ // scope + имя будущего метода like, первый параметр - запрос, второй - аргумент (может быть опущен)
                        return $query->where('title', 'LIKE', "%{$s}%")} // возвращаем не готовый результат, а построитель запросов,
                                                                         // если хотим продолжать запрос в контроллере
     Теперь в контроллере можно встраивать метод like:
	$posts = Post::like($s)->with('category')->paginate(5); // построитель запросов $query передается автоматически

     Также в scopeLike можно полностью встраивать запрос, тогда метод like будет возвращать массив результатов:
	public function scopeLike($query, $s){
        	        return $query->where('title', 'LIKE', "%{$s}%")->with('category')->paginate(5);}
	
     А в контроллере просто используем like:
	$posts = Post::like($s);

477) Оператор LIKE используется в условии WHERE для поиска заданного шаблона.
	% - знак процента представляет нулевой, один или несколько символов
	_ - нижнее подчеркивание представляет собой один символ

     Примеры:
	WHERE name LIKE 'text%' - начинается на text
	WHERE name LIKE '%text' - заканчивается на text
	WHERE name LIKE '%text%' - всключает text в любой позиции
	WHERE name LIKE 'text%data' - начинается на text и заканчивается на data
	WHERE name LIKE 'text_%_%' - начинается на text и длиной не меньше 6 символов


=============================================== Ларавель продолжение ===============================================

478) Если Ларавель с Vite, то нужно запускать его при работе с сайтом, иначе ошибка: 
	1) npm run build  - для продакшена
	2) npm run dev - для разработки

479) В самом начале создаем модели и миграции:
	php artisan make:model Post -m // -m - миграции
     Название модели в ед. числе с большой буквы, ларавель сам сделает имена для таблиц во множ. числе

480) Другой вариант создания роутов:
	Route::group(['namespace' => 'App\Http\Controllers'], function () {
    		Route::get('/', 'StartController@index');});

481) Команда $table->timestamps() в миграции вставляет в таблицу поля created_at и updated_at.
     Модель их заполняет сама при создании записи.
     Если такие поля были не нужны и мы удалили в миграции  $table->timestamps(), 
     то отключаем заполнение этих полей в Модели: public $timestamps = false;

482) https://github.com/alexeymezenin/laravel-best-practices/blob/master/russian.md - лучшие практики Ларавель

483) Посмотреть флаги команд - добавляем -h, пример:
	php artisan make:model -h

484) php artisan make:model Article -fm - создать одновременно модель, фабрику и миграцию

485) Str:: - класс для работы со строками, например Str::lower($str) - переводит в нижний регистр

486) 'body' => $this->faker->paragraph(100, true)  - параграф из примерно 100 предложений
     'subject' => $this->faker->sentence('3') - предложение из трех слов
     'good_id' => $this->faker->randomElement([1,2,3,4]) - случайный элемент из массива

487) Генерация ссылок на изображения заглушки через faker в фабрике:
	'img' => "https://via.placeholder.com/600/33CCFF/FFF/?text=LARAVEL8"
     Также есть встароенный в faker метод:
	'src' => $this->faker->imageUrl(150,150, $this->faker->word(), true) // случайное изображение со случайным словом
     Получится: https://via.placeholder.com/150x150.png/007766?text=vitae+accusamus
     Там много настроек, можно также автоматом сохранять изображения методом $faker->image(...)	

488) https://via.placeholder.com/600х200/33CCFF/FFF/?text=LARAVEL8 // размер изображения, цвет фона, цвет текста, сам текст

488) Создание случайного изображения по ссылке:
	1) "https://picsum.photos/250/320?nocache=" . microtime();
	2) "https://source.unsplash.com/random/250x320?sig=" . rand(100,1000)

489) 'created_at' => $this->faker->dateTimeBetween('-1 years') - даты создаются в разное время, так понял в пределах этого года

490) Уникальность значений поля БД можно задавать:
	1) В mysql установкой ключа Уникальный
	2) В миграции установкой ->unique()

491) Если в БД часто достаем значения из поля, особенно если много строк, то ставим ключ Индекс

492) Ключи полей БД можно редактировать ниже таблицы

493) Получение параметров адреса и отправка в контроллер:
	Route::get('/mobiles/{product}', [MainController::class, 'product']);

494) Необязательность параметра адреса задается через ?:
	Route::get('/mobiles/{product?}', [MainController::class, 'product']);

     Тогда в контроллере нужно дать значение по умолчанию:
	public function product($product=null){return view('product', compact('product'));}

495) Передача переменных из контроллера в представление:
	1) public function product($product){return view('product', compact('product'));}
	2) public function product($product){return view('product', ['product' => $product]);}

496) php artisan rollback - откат последней миграции
     php artisan migrate:rollback --step=5 - откат последних 5 миграций
     php artisan migrate:reset - откат всех миграций
     php artisan migrate:refresh - откатит все миграции, а затем выполнит их снова

497) php artisan make:migration create_products_table - create_products_table  дает то, что в миграции заранее будут 
     вставлены команды на создание таблицы + основные поля.
     
498) В миграции можно задавать вторым параметром длину строки:
	$table->string('name', 255); - создается varchar длиной 255

499) Модель - класс для работы с таблицей БД.
     		Таблица: products, модель - Product

500) php artisan make:model Category -m - создание модели + миграции

501) Ожидаемая в blade переменная может быть по ошибке не передана, можно застраховаться:
	1) if(isset($var)) ... @endif;
	2) {{ $var ?? '' }}
	3) @isset($category) ... @endisset;
	
502) @include('card') - применяется для вставки верстки, можно передавать  переменные:
	@include('card', ['title' => 'Заголовок карточки'])

     Например, блока с ошибкой:
     @include('auth.layouts.error', ['fieldName' => 'description']) // эту команду вставляем в верстку

	@error($fieldName) // этот блок лежит в auth.layouts.error, соответственно реализована его универсальность через прием переменной $fieldName
  		<div class="alert alert-danger">{{ $message }}</div>
	@enderror

     Вообще ошибки можно записывать в сессию флеш переменными:
	return redirect()->back()->with('error', 'Ошибка заполнения')  // метод with внутри использует session->flash()
     А окошко выводить по условию:
	@if (session('success')) <div> {{ session('success') }} </div> @endif
	@if (session('error')) <div> {{ session('error') }} </div> @endif

503) php artisan route:list - посмотреть маршруты

504) В верстке blade используем только именованные маршруты:
	<a href="{{ route('index') }}">Интернет Магазин</a>
     Не должно быть никаких других ссылок. 
     Вместо <a href="/categories">Категории</a> делаем <a href="{{ route('categories') }}">Категории</a>

505) Проброс в контроллер параметра через именованный маршрут:
	<a href="{{ route('category', $category->code) }}">

506) $category = Category::query()->where('id', 12)->first();
     $category = Category::query()->where('id', 12)->get(); // запрашивает все, но т.к. по id, вернет коллекцию с 1 рез
	то же самое, что:
     $category = Category::query()->find($this->category_id); // find ищет по id

507)    Route::post('/basket/add/{id}', [BusketController::class, 'basketAdd'])->name('busket-add'); // роут ждет id товара
	На этот роут делаем вставляем маршрут в ссылку:
	{{ route('busket-add', $product) }} - если $product - объект, то по умолчанию в роут передается $product->id

508) Реализация отслеживания заказа/корзины пользователя через сессии:
	public function basketAdd($productId) { //срабатывает при добавлении товара в корзину, ожидает id товара
        $orderId = session('orderId'); // запрашиваем есть ли в сессии переменная заказа orderId
        if(is_null($orderId)){ // если нет
            $orderId = Order::query()->create()->id; // создаем в таблице orders новый заказ и запоминаем его id
            session(['orderId'=> $orderId]);}} // записываем в сессиию id заказа

509) class Order extends Model{
    	public function products(){
        	return $this->belongsToMany(Product::class);}} // возвращает связи заказа с продуктами из таблицы order_product
							       // связи в этой таблице реализованы полями order_id  и product_id
510) $order->products()->attach($productId); // добавляет в order_product новую связь order_id  и product_id

511) Полная функция добавления заказа в корзину через сессию:
	public function basketAdd($productId) {
        	$orderId = session('orderId');
        	if(is_null($orderId)){
           	$order = Order::query()->create(); // если заказа нет, то создаем
            	session(['orderId'=> $order->id]); // записываем в сессию id созданного заказа
		} else {
            	$order = Order::query()->find($orderId);} // находим заказ
        	$order->products()->attach($productId); // добавялем новую связь в таблицу order_product
		return redirect()->route('basket');} // избавляемся от повторного добавления в корзину при обновлении страницы

512) Методы attach и detach применяются для редактирования таблицы связей в отношениях многие-ко-многим. 
     К примеру, существует таблица Book и Author.
     Книга может иметь несколько авторов и автор может иметь несколько книг. Это связь belongsToMany.
     Связь belongsToMany реализуется через таблицу связи author_book, состоящую из полей: id, author_id, book_id
     К примеру метод для показа авторов книги:
	class Book extends Model{
    		public function authors(){return $this->belongsToMany('App\Author');}}

     attach - метод вставляет в таблицу связи новую связь: 
	      $book = App\Book::find(1); // находим книгу с id=1
	      $book->authors()->attach($authorId); // добавляем в таблицу связи нового автора для этой книги
     detach - метод удаляет связь:
	      а) $book->authors()->detach($authorId); // удаляем одного автора книги
	      б) $book->authors()->detach([4, 5, 8]); // удаляем несколько авторов
	      с) $book->authors()->detach(); - удаляем всех авторов вниги
     sync - метод подобен пересечению attach и detach. То есть он сохранит только те ассоциации, которые переданы ему
            в виде массива, все остальные удалит
	      $book->authors()->sync([5, 2, 10]); // у книги авторы с id 5, 2, 10
     syncWithoutDetaching - подобен sync, то не удаляет имеющиеся связи:
	       $book->authors()->syncWithoutDetaching([1, 2, 3]); // если нет таких связей, то добавим авторов 1, 2, 3

    Методы associate() и dissociate() используются в отношениях Принадлежит к (belongsTo), то есть дочерняя модель 
    имеет внешний ключ parent_id по которому привязывается к родительской модели.
    Например есть пользователи User и посты Post.
    В модели Post:
	public function user(){
		return $this->belongsTo(User::class)} 
    Привязываем пост к пользователю:
	$post->user()->associate($user) - то есть это то же самое, что команда: $post->user_id = id пользователя
	$post->user()->dissociate($user) - то же, что  $post->user_id = null
	 
513) При добавлении продукта в корзину(заказ) происходит создание новой связи в таблице связей order_product.
     Если добавляем тот же товар еще раз, то у нас создается дубль записи. Соответственно будут трудности с удалением товара,
     как если мы захотим удалить один товар из одинаковых двух, то метод $order->products()->detach($productId) удалит обе связи.
     Правильно реализуется так: в промежуточной таблице order_product вставляется поле count, код ниже

514) Для доступа к таблице связей из объектов полученных с ее помощью используется атрибут pivot:
	foreach ($user->roles as $role) { // используем получение ролей через таблицу связи
    		echo $role->pivot->created_at;} // запрашиваем через pivot поле created_at из таблицы связей
     Либо показ счетчика из order_product в blade:
	{{ $product->pivot->count }} 

     По умолчанию через pivot из таблицы связей мы можем получить только ключи pivot модели. Если у нас в таблице связей
     есть свои доп. поля, например, count из примера выше, то указываем это поле при создании связи: 
	return $this->belongsToMany(Role::class)->withPivot('count');

515) Проверка, что в таблице связей есть сущность:
	if($order->products->contains($productId)) {
            dd('Содержит такой продукт');}

516) Код инкремента поля count из таблицы связей:
	public function basketAdd($productId) {
        $orderId = session('orderId');
        if(is_null($orderId)){
            $order = Order::query()->create();
            session(['orderId'=> $order->id]);
        } else {
            $order = Order::query()->find($orderId);
        }
        if($order->products->contains($productId)) { // если в таблице связей есть продукт с таким id
            $pivotRow = $order->products()->where('product_id', $productId)->first()->pivot; // строка из order_product
            $pivotRow->count++; // увеличиваем поле count
            $pivotRow->update(); // обновляем запись в order_product
        } else {
            $order->products()->attach($productId); // если не было связи, то создаем
        }
        return redirect()->route('basket');}
	
517) Соответственно, код уменьшения count:
	 if($order->products->contains($productId)) {
            $pivotRow = $order->products()->where('product_id', $productId)->first()->pivot;
            if($pivotRow->count < 2) {
                $order->products()->detach($productId); // удаляем связь если count 1
            } else {
                $pivotRow->count--;
                $pivotRow->update();}}   

517) Получить привязанные к странице id оснасток из таблицы связей case_page:
	$page->cases()->pluck('case_page.case_id')

518) $request->all() - массив полей формы, тот же $_REQUEST
     $request->name - значение одного инпута

519) $obj->update(); - обновляет запись в БД, используется sql команда UPDATE
	Пример:
	   $order = Order::query()->find($orderId); // получили запись(объект) из БД
           $order->name = $request->name; // записали в объект значение из инпута
           $order->phone = $request->phone; // записали в объект значение из инпута
           $order->update(); // обновили запись в БД

520) $obj->save(); - сохраняет запись в БД, либо обновляет, если она существует. Если записи в БД нет используется INSERT, 
		     если есть, то UPDATE

Отличие update() и save() в том, что для update не обязательно получать объект в переменную, можно сделать сразу:
	App\Order::where('id', 1)->update(['name' => $request->name]);

521) Куски кода работы с БД размещаем в Моделях в обертках-функциях, в контроллерах вызываем эти функции

522) Аутентификацию, так понял, нужно подлючать отдельно.
     На этапе создания нового приложения ставить флаг --auth:   laravel new blog --auth

522) Готовая аутентификация на основе Бустрап, React, Vue: composer require laravel/ui "^1.0" --dev

523) Вместо laravel/ui написано более современное решение - Breeze

524) У меня не было папки Auth в контроллерах, не работала команда php artisan route:list
     Копировал ее с гитхаба ларавель

525) По видео rdavydov самостоятельно добавлял в views папку Auth и создавал в ней шаблоны blade входа,
     ранее в другом видео видел как все установили из коробки - laravel/ui (читать про установку)
     Однако видел, что сейчас применяются более совр. решения, например, Breeze

526) После добавления laravel/ui и вставки папки Auth заработало логирование и регистрация, больше ничего не делал.
     Все само подеключается к базе и работает через контроллеры в Auth.
     Однако нужно иметь ввиду, что ошибки в форме не показываются (напр. ошибки валидации RegisterController),
     поэтому можно подумать, что не работает регистрация и т.д.

526) Показ Войти-Выйти в blade:
	@guest <li><a href="{{ route('login') }}">Войти</a></li> @endguest
        @auth <li><a href="{{ route('logout') }}">Выйти</a></li> @endauth

527) middleware('auth') - посредник из коробки, проверяет аутентифицирован ли пользователь, 
                          если нет будет редирект на страницу login

528) Route::get('/home', [HomeController::class, 'index'])->name('home')->middleware('auth'); // единичный посредник
	
     Route::group(['middleware' => 'auth'], function(){ // установка посредника для группы маршрутов
     Route::get('/home', [HomeController::class, 'index'])->name('home');
});

529) Посмотреть сборку админ-панели для Ларавель:
	https://www.laravel-admin.org/docs/en/

530) Форматирование даты в Ларавель:
	1) $user = \App\User::find(1);
	   $newDateFormat = $user->created_at->format('d/m/Y'); // format - удобная функция, хз откуда, но не встр. в php
	
	2) $user = \App\User::find(1);
	   $newDateFormat2 = date('d/m/Y', strtotime($user->created_at)); // php функции, strtotime преобр дату в timestamp

	3) $user = \App\User::find(1);
	   $newDateFormat3 = \Carbon\Carbon::parse($user->created_at)->format('d/m/Y'); // встроенная библиотека Carbon

        4) public function getCreatedAtAttribute($date){ // в Модели аксессор поля created_at, работает, хоть в Ларе уже по-другому аксессоры
    		return Carbon::parse($date)->format('H:i:s');}
		

531) Создание посредника на примере проверки является ли админом.
	1) php artisan make:migration alter_users_table // Создаем миграцию для users
	2) Schema::table('users', function (Blueprint $table) {
            	$table->tinyInteger('is_admin')->default(0);}); // вставляем поле is_admin
	3) public function isAdmin(){return $this->is_admin === 1;} // в модели User создаем функцию проверки	 
	4) php artisan make:middleware CheckIsAdmin // создаем посредника
	5) Прописываем в посреднике функцию проверки:
		 public function handle(Request $request, Closure $next){
        		$user = Auth::user(); // получаем аутентифицированного пользователя
        		if(!$user->isAdmin()) { // обращаемся к функции isAdmin() модели User
            			session()->flash('warning', 'У вас нет прав администратора');
            			return redirect()->route('index'); // пользователь не админ, редиректим
        		} return $next($request);} // если пользователь админ отправляем запрос дальше

	6) Вставляем посредника в цепочку protected $routeMiddleware в Kernel:
		'is_admin' => \App\Http\Middleware\CheckIsAdmin::class,
	7) Вставляем посредника в роуты:
		1 способ) Route::group(['middleware' => 'is_admin'], function(){
        			Route::get('/orders', [OrderController::class, 'index'])->name('home');});
		2 способ) Route::get('/orders', [OrderController::class, 'index'])->middleware('is_admin')->name('home')
	
532) Auth::check() - проверка аутентифицирован ли пользователь
     Auth::id() - возвращает id аут. пользователя

533) Сохраняем в заказах в поле user_id пользователя:
	if(Auth::check()){
            // ранее создавали заказ и записывали в него поля формы $order = Order::create() и т.д.
            $order->user_id = Auth::id();
            $order->save();}

534) В Route::group можно добавлять общие namespace, middleware, prefix и т.д.:
	Route::group([
    		'middleware' => ['basket_not_empty', 'can:delete-post'],
    		'prefix' => 'basket'], function(){ //prefix будет добавляться в начало, как если ниже в роуте /basket
			Route::get('/', [BusketController::class, 'basket'])->name('basket');
			Другие роуты.... }

535) Метод findOrFail возвращает либо первый результат, либо выбрасывает исключение.
	$order = Order::query()->findOrFail($orderId);
     Если исключение не перехвачено, то клиенту автоматически отправляется HTTP-ответ 404

536) Мультиязычность:
	В файлах blade мы можем обращаться к переменным, которые будут взяты из словарей, пример:
		1) <title>@lang('main.var')</title> // где main - файл словаря, var - переменная из его массива
		2) <title>{{ __('main.var') }}</title> // функцию __ можно использовать также php коде в коде

	Сами словари лежат в resourse/lang по папкам локалей, например словарь main.php из папки локали en:
		return ['var' => 'Super Mega Company',
    			'sent' => 'We have emailed your password reset link!']

	Если словарь лежит в промежуточной папке mail:  lang/en/mail/main.php, то переход по папкам также через точку:
		<title>@lang('mail.main.var')</title>

	Также через точку обращение к вложенным массивам словаря:
	Словарь: return ['properties' => [
        			'hit' => 'Хит',
        			'new' => 'Новинка',
        			'recommend' => 'Рекомендуем'],] 
	Обращение: @lang('main.properties.recommend')

	Локали(т.е. папки en, ru и пр.) переключаются в config/app.php, где:
		locale - основной язык
		fallback_locale - запасной язык (если переменной не обнаружит в основной локали)

Переключение языков:
	1) <a href="{{ route('locale', 'ru') }}">Переключить на русский</a> // ссылка переключения на русский язык, 
						 соответственно будет несколько ссылок на разные языки, в верстке получится:
						 <a href="/locale/ru">Переключить на русский</a>
	2) Route::get('locale/{locale}', [MainController::class, 'changeLocale'])->name('locale') // роут на переключение
	3) public function changeLocale($locale) { // функция в MainController
			session(['locale'=>$locale]) // записываем в сессию установленную локаль
        		App::setLocale($locale); // функция установки локали на текущую страницу
			return redirect()->back();} // редиректим обратно
	4) php artisan make:middleware SetLocale // создаем посредник на отслеживание сессионной переменной 
						    и автоматической установки локали при переходе со страницы на страницу
	5) В посреднике устанавливаем локаль из переменной сессии:
		if(isset(session('locale'))){
            		App::setLocale(session('locale'));}
	6) Прописываем посредник в Kernel в $routeMiddleware:
		'set_locale' => \App\Http\Middleware\SetLocale::class
	7) Устанавливаем посредник маршрутам:
		Route::group(['middleware' => 'auth'], function(){
			Route::get('/', [MainController::class, 'index'])->name('index'); и т.п.}

        В словарях можно держать фразы магазина (купить, заказ добавлен и пр.), сами же Названия товаров, описания и др. 
        вместе с дублями их переводов обычно держат в БД в одной таблице в полях: name - русское назв., name_en - англ.

	Логика перевода:
		1) В вертске переводить будем своей функцией:
			<h3>{{ $product->__('name') }}</h3> // в name пишем имя запрашиваемого поля из БД, например: count, description и пр.
		
		2) Создаем трейт Translate c этой функцией __($field)
			public function __($field){
				$locale = App::getLocale() ?? 'ru'; // получаем строчку 'en' или 'ru'
				if($locale === 'en'){
					$field = $field . '_en';} // добавляем к пришедшему полю '_en'
				return $this->$field} // так как трейт встроен в Модель, то $this - объект записи, напр. $product
							соответственно, преобразуется в $product->name или  $product->name_en 
							и функция __() вернет переведенную фразу, напр. 'Телефон' или 'Phone'
		3) Встраиваем трейт в тело Модели:
			use Translate

		Соответственно, когда в верстке пишем $product->__('name'), то вызывается метод __() у объекта записи, а не
		сначала вызывается __('name'), затем его результат подставляется в $product->результат, с этим ломал голову.
		То есть вызывается только метод __() объекта $product и результат этого метода вставляется в верстку.

537) App::setLocale('ru') - установка локали только на текущую страницу
     App::getLocale() - запрос текущей локали

538) в blade файлах не нужно открывать код через <?php , иначе будет ошибка

539) @isset($category) ... @endisset - выполнится если не null

     @isset($category) ... @else ...  @endisset - вариант с else

540) php artisan make:controller Admin/CategoryController --resource --model=Category - создание контроллера ресурса, 
											связанного с моделью

541) php artisan route:list - просмотр роутов

542) Загрузка, хранение и показ загруженных файлов.
Файлы хранятся в папке storage в папках в соответствии с настройками в config/filesystems.php.
Снаружи в эту папку попасть нельзя, так как все запросы сервер направляет в public.
Поэтому создаем в public ссылку на storage:
	php artisan storage:link
Теперь в папке public появилась ссылка на storage, далее меняем в .env: FILESYSTEM_DRIVER=public

Сохранение файла на примере метода store ресурсного контроллера:
	public function store(Request $request){
        	$path = $request->file('image')->store('categories'); // файл из инпута сохраняем в storage в папке categories, запоминаем путь на него
        	$params = $request->all(); // получаем массив значений инпутов
        	$params['image'] = $path; // меняем значение инпута image на путь куда загрузили файл
        	Category::query()->create($params); // добавляем запись в БД
        	return redirect()->route('categories.index');} // редиректим 

Для отображения картинок в верстке:
  1) Меняем в .env APP_URL=http://адрес_сайта или для разработки: APP_URL=http://127.0.0.1:8000, иначе ссылка на картинку начинается с localhost
  2) В верстке для показа картинок используем Storage:
	<img src="{{ Storage::url($category->image) }}">

Для удаления файла из папки storage используем метод:
	Storage::delete($category->image); // где $category->image ссылка на файл хранящаяся в БД

543) $request->has('image') - проверка заполнен ли инпут (здесь image)

544) Валидация:
     1 способ - валидируем в каждой функции
	$request->validate([ // валидируем значения из инпутов
             'code' => 'required',
             'name' => 'required',
             'description' => 'required']);

	При ошибке будет редирект на прежнюю страницу с объектом $errors->all(), в верстке ошибки можно посмотреть так:
	1) @error('name') выводим_текст @enderror // где name - имя инпута
	2) @error('name') <div> {{ $message }} </div> @enderror // в $message выводится сообщение об ошибке валидации
   
     2 способ - меняем Request на CategoryRequest (название свое)
	1) php artisan make:request CategoryRequest // cоздаем класс запроса
	2) public function store(CategoryRequest $request){} // в контроллере используем вместо обычного Request наш класс
	3) Правила валидации теперь прописываем в CategoryRequest:
		public function rules(){
        		return [
            		  'code' => 'required',
            		  'name' => 'required',
            		  'description' => 'required'];}
	4) Также в CategoryRequest возвращаем true (по умолчанию false)
		public function authorize(){
        		return true;}
      
     3 способ - через валидатор с прописыванием ошибок подходит для Evo
	$validator = Validator::make($request->all(), 
	   ['message' => 'required|min:5',], // тут правила
	   ['message.required' => 'Сообщение обязательно',]); // тут ошибки
	
	if($validator->fails()){ $result['errors'] = $validator->errors(); } // если были ошибки
	else {Post::create([...])} // если ошибок не было

        Обычное продолжение после $validator->fails():
	if ($validator->fails()) {
      	   return redirect()->back()
           ->with('error', 'Ошибка заполнения') // флеш переменная error
           ->withErrors($validator) // ошибки и сообщения валидатора
           ->withInput();} // отдаем обратно данные инпутов

545) Ошибки показываются на английском, их можно менять в файле настроек, либо методом в нашем классе CategoryRequest:
	 public function messages() {
          	return [
              	  'required' => 'Поле :attribute обязательно',
                  'min' => 'Поле :attribute должно иметь минимум :min символов',
		  'code.min' => 'Поле Код должно иметь минимум :min символов',];} // отдельно правило для инпута с name='code'

546) <input value='{{ old('code', isset($category) ? $category->code : null) }}' name='code'> - у функции old есть второй
     параметр по умолчанию. В частности в этом примере реализован пример формы с инпутом, при ошибке показывающий введеное
     ранее значение, либо показывающий значение для редактирования, если переход на форму был по кнопке 
     Редактировать созданную ранее запись.
     old('value', 'default'); - короче

547) Route::currentRouteNamed('index') - проверка на текущий роут, к примеру:
	<li @if(Route::currentRouteNamed('index')) class="active" @endif>Пункт</li>

548) Route::currentRouteNamed('categor*') - вернет true, если маршрут начинается на categor

549) Скомпилированные из blade в обычный php код страницы лежат в storage/framework/views

550) Добавление своих @ команд в blade шаблон:
	1) Providers->AppServiceProvider-> в методе boot создаем Blade директиву:
		public function boot(){
        		Blade::directive('routeactive', function ($route){ // routeactive - пишем слитно, будет @routeactive, 
            		return "<?php echo Route::currentRouteNamed($route) ? 'class=\"active\"' : '' ?>";});} // здесь пишем
	какой код вставится при компиляции blade в php

	2) @routeactive("index") // в верстке blade 
	3) Чтобы PhpStorm не ругался на новую директиву: Settings->PHP->Blade->снимаем галочку Use defaults settings->
	   -> Directives-> + ->  добавляем routeactive -> теперь @routeactive не подсвечивается как ошибка
	4) Иногда не работает, нужно php artisan view:clear

551) Auth::check() - проверка на аутентифицированность
     Auth::user() - получение объекта аутентифицированного пользователя, соответственно можно выводить поля: Auth::user()->name и др.
     Auth::id() - id аутентифицированного пользователя

111) bcrypt($str) - ларавель помощник шифрующий строку, например перед выгрузкой пароля в БД

111) Artisan::call('migrate:fresh --seed') - вызов команды артизан из кода (контроллера напр.) =>
	=> то же самое в консоли: php artisan migrate:fresh --seed

111) Storage::deleteDirectory('images') - удаляет папку storage/images
     Storage::makeDirectory('images') - создание папки

111) File:: - фасад для работы с файлами
	File::allFiles(__DIR__) => покажет массив файлов из директории (в данном случае из директории файла) c полями:
			           path - полный путь к папке файла
				   pathname - полный путь к файлу
				   filename - название файла 
			           size - размер и т.д.

111) Чтобы в миграции на изменение таблицы заранее стояла болванка в методе up нужно добавлять какую таблицу изменяем:
	php artisan make:migration alter_table_products - пустой метод up
	php artisan make:migration alter_table_products --table=products - минимально заполненный

111) Проверка на булевское значение поля (0, 1):
	делается в Модели:
		public function isHit() {
        		return $this->hit === 1;}

111) Шаблон @error можно выносить в отдельный файл и встраивать с передачей в него параметра:
	@include('error', ['fieldName' => 'code']) // встраиваем в blade в показ ошибок инпута
	
	@error($fieldName) // шаблон в отдельном файле
  		<div class="alert alert-danger">{{ $message }}</div>
	@enderror

111) Мутатор - функция в Модели, которая вызывается автоматически при вставке в БД значения по опр. полю. 
    	       Имя поля указываем в camelCase:
		protected function topHeader(): Attribute {
        		return Attribute::make(
            			get: fn ($value) => strtolower($value),  - аксессор
            			set: fn ($value) => $value == 'on' ? 1 : 0,);} // вызывается при вставке в БД по полю top_header для чекбоксов, 
									          которые передают в реквест значение 'on', а в базу нужно 1

111) Аксессоры делают противоположную задачу, меняют данные при выводе записи из БД, описание есть выше

111) $this->attributes['field'] - так в объекте модели можно обращаться к значению инпута

111) Если вызываем в объекте связь, то такую функцию вызываем как свойство:
	$product->brend->name // где brend функция связи, но вызывается как свойство
   
     Соответственно была ошибка, забыл при вызове функции (не связи) поставить скобки: $product->isNew, ларавель написал, 
     что ждет связь:  must return a relationship instance 

111) Post и Get(в адресной строке) параматеры принимаются через инъекцию Request $request в контроллер.
     Весь массив инпутов лежит в $request->all(), отдельные значения в $request->name и пр.

111) Пагинация:
	Публикуем шаблоны пагинации: php artisan vendor:publish --tag=laravel-pagination - если будем менять стили, вообще это не надо
	Также должен быть подключен в шапку Бутстрап, или Тайлвинд (он подключен по умолчанию в Ларавель)

	Обычно мы получаем весь список сущностей из базы через $products = Product::get();
	Для того, чтобы встроить пагинацию заменяем это на: $products = Product::paginate(6); // Product::simplePaginate(3) - простая
	В верстку вставляем стрелочки: {{ $products->links() }}
	По умолчанию Tailwind CSS, если бутстрап, то: 
	в AppServiceProvider:
		1) подключить класс:  use Illuminate\Pagination\Paginator;	
		2) в методе boot прописать: Paginator::useBootstrap();

    Если Тайлвинд не используем и закомментировали его в стилях, то для пагинации достаточно включить только:
	@tailwind utilities; - кнопки будут показываться нормально

    Пагинация при переходе на др. страницу сбрасывает другие get параметры, например, сбросится фильтр.
    Для того, чтобы гет параметры передавались при пагинации исп withQueryString:
	$products = $productsQuery->paginate(6)->withQueryString();

111) if($request->has('price_from')) - заполнен ли инпут price_from, даже пустым значением ''  
     if($request->filled('price_from')) -  то же самое + не является пустой строкой
     if(isset($request->price_from)) - значение инпута не равно null; для пустой строки вернуло, что инпут не заполнен

     Отличие has от filled: пустые инпуты type="text" отправляют '', в этом случае has вернет true, filled - false

111) Условия при получении из БД, встраивание sql запроса:
	$products = Product::query()->where(условие)->get();

     То же самое, но с промеж. этапом: 
	class MainController extends Controller {
    		public function index(Request $request){
        	$productsQuery = Product::query(); // здесь формируем объект - построитель запросов sql
        	if($request->filled('price_from')){
            		$productsQuery->where('price', '>=', $request->price_from)} // дополняем, возвращать ничего не надо

                Соответственно построитель запроса можно дополнять дальше:
		$productsQuery->where('price', '<=', $request->price_to);

        	$products = $productsQuery->get(); // получается, что здесь запрос отправляется в БД
        	return view('index', compact('products'));}

111) Соответственно, для удобного поэтапного встраивания sql запросов:
	1) Получаем построитель запросов $productsQuery = Product::query();
	2) Дополняем его: 
		$productsQuery->where(условие);
		$productsQuery->where(условие);
	3) Получаем данные из БД: $products = $productsQuery->get();

111) <input value="{{ old('field')  }}"> - функция old возвращает прежнее значение инпута не только при редиректе обратно
                                           при ошибке, но и вообще все данные из предыдущей отправки формы

     Если old не срабатывает (не понял почему, м.б. потому что форма отправлялась через get), то можно использовать:
     <input type="text" name="price_from" value="{{ request()->price_from }}">

     $username = $request->old('username') - метод old извлечет ранее записанные входные данные из сессии

     Для select сохраняем выбранное значение так:
	<select name="type">
	   <option value="template" @if(old('type') == 'template') selected @endif>Макет</option>
	   ...
	</select>

     Если select multiple, то так:
	<select name="type" multiple>
	   @foreach($pages as $page)
              <option value="{{ $page->id }}" @if(in_array($page->id, old('type') ?? [])) selected @endif>{{ $page->title}}</option>
           @endforeach	
	</select>

111) Также можем вернуть чистую страницу, без старых значений инпутов:
	<a href="{{ route('index') }}">Сброс</a>

111) В blade мы тоже можем обращаться к объекту $request, но через помощник request():
	<input type="checkbox" name="new" @if(request()->has('new')) checked  @endif> // чтобы инпут был выделен после применения фильтра

111) dd(get_class_methods($request)) - посмотреть все методы

111) Валидация фильтра через свой реквест:
	1) php artisan make:request ProductsFilterRequest // создаем
	2) public function index(ProductsFilterRequest $request) // в контроллере заменяем стандартный реквест на наш
	3) в ProductsFilterRequest в authorize: return true;
	4) в ProductsFilterRequest в rules пишем правила:
		return [[
                'price_from' => 'numeric',
                'price_to' => 'numeric']];

111) Изменить таблицу модели.
     По умолчанию модель создается в ед. числе от названия таблицы: таблица products - модель Product.
     В Модели мы можем изменить таблицу с которой работает модель:
	protected $table = 'our_products'

     Также можно установить таблицу в самом отношении:
	$this->belongsToMany(Post:class, 'product_comments')

111) Если после получения модели необходимо добавить в нее новое поле перед отдачей в представление, 
     то добавляем в модель:
		protected $appends = ['field'];
     Теперь в это поле Модели в контроллере можно вставлять данные:
		$post = Post::find(1);
		$post->field = 'Свой текст';
        	dd($post->field) => 'Свой текст';

111) Логи Ларавель автоматически записываются в storage/logs

111) Ошибки по умолчанию показываются в браузере, в продакшене показ отключают в .env:
	APP_DEBUG=false
     Теперь пользователю будет показываться страница: 500 SERVER ERROR - ошибка на стороне сервера

111) Свои страницы ошибок можно разместить по пути:
	resources/views/errors/404.blade.php // где название файла - ошибка
	resources/views/errors/500.blade.php  - ошибка сервера (напр. ошибка в коде, код сломался)

     Ларавель сам обнаружит эти файлы и заменит стандартные страницы.
    
     Также можно опубликовать в этой же папке стандартные страницы Ларавель и изменить их:
	php artisan vendor:publish --tag=laravel-errors

111) Изменить ведение ошибок по дням, а не в одном файле:
	1 способ) config/logging.php => 'channels' => ['daily'],
	2 способ)  .env => LOG_CHANNEL=daily => затем в консоли php artisan config:cache

111) Получается в $request записаны данные всего HTTP запроса, в том числе заголовки => ip адрес
	$request->ip() (ip адрес пользователя)

111) get_class_methods($request) - показывает все методы объекта

111) Логирование:
	Log::info($request->ip()); // в общем логе пишется ip посетителя:  [2022-11-08 19:46:45] local.INFO: 127.0.0.1 

111) Логирование в свой файл:
	Log::channel('single')->info($request->ip()) // ранее мы изменили канал на daily,  
	
	Каналы описаны в config/logging.php.
	Ранее мы изменили канал общего лога single на daily, теперь логи создаются по дням с отдельными названиями.
	Теперь же для ip используем свободный файл laravel.log в соответствии с настройкой канала single

111) Также логи можно смотреть в дебагбаре

111) Очистка кеша настроек Ларавель:
	php artisan config:cache - закеширует все настройки из папки config в один файл
	php artisan config:clear - вроде удаляет и кеширует

111) Универсальная очистка: 
	php artisan optimize:clear - чистит events, views, cache, route, config, compiled
	Artisan::call('optimize:clear') - из кода

	Route::get('/clear', function() { - роут при переходе на который все очищается
    		Artisan::call('optimize:clear');
    		return redirect()->back();});
		
111) Не работает дебагбар? 
     Очисти кеш php artisan config:clear
     Либо php artisan serve
     Дебагбар не работает при APP_DEBUG=false в .env

111) Логирование также можно вести в Дебагбар:
	composer require barryvdh/laravel-debugbar --dev // в консоли
	use Barryvdh\Debugbar\Facades\Debugbar; // импортируем класс в контроллер
	Debugbar::info('1'); // пишем лог в панель Messages в Дебагбар - это замена dd(), делает то же самое

	{{ Debugbar::info(111) }} - в верстке блэйд

111) $request - объект HTTP запроса, его можно также посмотреть в Дебагбаре на панели Request

111) В дебагбаре также можно посмотреть все sql запросы и их время, в том числе общее (под основным временем в Queries)

111) При запросе списка сущностей, если есть запрос по связанной таблице, то связь запрашивается каждый раз. 
     $products = Product::with('category') - делаем так, где category - метод связи
     
     Теперь вместо кучи:
	select * from `categories` where `categories`.`id` = 1 limit 1 и т.д.
     Один запрос:		
	select * from `categories` where `categories`.`id` in (1, 2, 3)

111) Удобно смотреть запросы к БД после применения фильтров товаров

111) scope - механизм встраиваниния подготовленных условий в запрос в БД.
	Функция пишется в Модели:
		public function scopeHit($query) { // передаем запрос, который будем редактировать; можно получать аргументы (ниже есть)
        		return $query->where('hit', 1);} //здесь дополняем запрос, можно также выполнить и вернуть результат
	В контроллере используем:
		foreach (['hit', 'recommend', 'new'] as $field) { // здесь перебор чекбоксов, scope делаем для каждого
            		if($request->has($field)){
                	$productsQuery->$field();}} // соответственно при переборе сюда вставляются scope функции, напр. ->hit, ->new
		$products = $productsQuery->paginate(6);
		return view('index', compact('products'));
-----------
	Если в scope передаем агрументы, то работает так:
	В контроллере: $users = User::func($num)->get(); // передаем аргумент
	В Модели: public function scopeFunc($query, $age){return $query->where('age', '>', $age)} // получаем аргумент и используем 

111) При обращении к связям таблицы мы используем методы связи как свойства:
	$orders = $user->orders->phone // orders - функция hasMany, пишем как свойство, возвращает объекты
     Для того, же чтобы функция вернула построитель запроса, например для дополнения sql запроса, используем orders():
	$orders = $user->orders()->where(условие)->get() // здесь orders() позволяет дополнить запрос

111) middleware - посредник регулирующий поведение HTTP запроса перед тем как  отправить его в контроллер

111) $var = session('sum', 0); - второй параметр в Ларавель сессии - значение по умолчанию

111) public static function(){} -статичная функция означает, что не будет обращения к свойствам объекта

111) php artisan make:migration alter_table_products_count --table=products - миграция для изменения таблицы products

111) Создание новой колонки в таблице:
	1) Миграция
	2) Редактирование fillable в Модели

111) Методы работающие с базой данных выносим в Модель, обращаемся либо как к статическим методам модели, либо к методам 
     объекта записи: из верстки/контроллера $product->isNotNull();     из другого метода этой модели  $this->isNotNull(), 

111) php artisan migrate:rollback --step=1 - откат последней миграции
     php artisan migrate:rollback - откат последней партии миграций

111) $table->dropColumn('count') и др. методы в функции down в миграции нужны для откатов миграций, 
     например, при дополнении миграции

111) Установка метода мягкого удаления:
	1) Миграция:
		up: $table->softDeletes() // добавится поле 'deleted_at' с временем удаления
		down: $table->dropSoftDeletes(); // то же $table->dropColumn('deleted_at')
	2) В шапку модели импортируем ПИ use Illuminate\Database\Eloquent\SoftDeletes;
	   В саму модель трейт use SoftDeletes; // достаточно сделать это, PHPStorm ПИ сам пропишет

111) Если товар добавлен в корзину и затем магазин его удалил с витрины, то он может не показываться при выборке товаров
     в корзине. Для того, чтобы показывались в том числе удаленные магазином товары:
	$order->products()->withTrashed()->get() 

withTrashed() - удаляет из запроса строку WHERE `deleted_at` is null
               Эта строка всегда в запросе если таблица поддерживает мягкое удаление

111) $this->products - здесь запрашиваются связанные товары методом products
     $this->products()->withTrashed()->get() - то же самое, но ставим () и дополняем sql запрос, получаем в том числе 
					      значащиеся мягко-удаленными

111) withoutTrashed() - показ без мягко-удаленных сущностей (оно по умолчанию)

111) $this->trashed() - проверка в модели

111) rand(1, 10) - php функция, случайное от-до

111) Однотипные операции можно убирать в посредники для группы маршрутов. 
     Например, при переходе в маршруты корзины (подтвердить, удалить товар и др.) можно убрать проверку на наличие 
     товаров в корзине в посредник.
	$orderId = session('orderId'); // запрашиваем переменную заказа
        if(!is_null($orderId) && Order::getFullSum() > 0){ // если заказ существует и сумма > 0
            return $next($request);} // продолжаем маршрут
	session()->flash('warning', 'Ваша корзина пуста'); // иначе редиректим на главную с сообщением о пустоте
        return redirect()->route('index');

111) <html lang="{{ str_replace('_', '-', app()->getLocale()) }}"> - автоматом берет lang из настроек локали Ларавель

111) Используем findOrFail вместо find.
     find - запрос записи из БД по id
     findOrFail - то же самое, но если записи нет, то 404 ошибка
     
     Например, могут отредактировать адресную строку site.ru/add/12 и вместо существующего 12 товара поставить 
     несуществующий в БД id, тогда будет ошибка, так как метод find для несуществующего id возвращает null => 
     Attempt to read property "name" on null // обращаемся к name несущ. товара

     $product = Product::query()->findOrFail($productId); // если id нет в БД, то 404 ошибка

111) find($id) - возвращает одну запись, если не существует, то null
     find([1,2,3]) - возвращает несколько записей
     findOrFail($id) - возвращает одну запись, если не существует, то 404 ошибка
     first() - возвращает первую подходящую запись или null
     firstOrFail() - возвращает первую подходящую запись или 404 ошибка
     get() - возвращает коллекцию записей
     pluck($column) - возвращает коллекцию значений в столбце

     Метод pluck полезен, если нам нужно из таблицы получить ассоциативный массив. Так, метод all() или get() возвращает коллекцию моделей, которую обычно
     перебираем циклом и выводим записи. А если нам нужно передать во вьюху массив настроек и не перебирать, а выводить $options['phone_1'] ?
     Делается так:
	$options = Option::all()->pluck('content', 'name') - получили массив, где из поля 'name' вставляется ключ, из поля 'content' - значение.
        $options['phone_1'] => 89514466332 - по ключу 'phone_1' лежит значение
     Во вьюху передаем массив $options
     
     pluck и all - одновременно методы и построителя запросов и методы коллекций, поэтому можно ставить перед или после all:
	Option::all()->pluck('name') - вернется коллекция
	Option::pluck('name')->all() - вернется массив (метод all возвращает базовый массив коллекции)
     Однако pluck можно ставить только после get()

111) Можно создавать свои классы и использовать их в коде, например, вместо того, чтобы постоянно находить 
     в контроллерах заказ из сессии:
	$orderId = session('orderId');
        $order = Order::query()->findOrFail($orderId);

     Можно создать свой класс MyOrder:
	class MyOrder {
		protected $order;

    		public function __construct(){
        		$orderId = session('orderId');
        		$this->order = Order::query()->findOrFail($orderId);} // здесь Order это модель

    		public function getOrder(){ // геттер защищенного свойства $order
        		return $this->order;}}

     Теперь в контроллерах одна строка:
		$order = (new Basket())->getOrder();

111) Отправка email через драйвер smtp: // внимание, в документации Ларавель советуют использовать драйверы Mailgun и Postmark
     есть внизу в Email
	1) Создаем класс для формирования письма по опр. теме, например 'Заказ получен'
		php artisan make:mail OrderCreated
	   Объект этого класса будет создаваться при отправке письма методом Mail::to
	
	2) Описываем класс письма: 
	   Ларавель 9 немного поменялся, отправка теперь не через функцию build, а через content() - есть внизу в Email
	   Для того, чтобы использовать build нужно закомментировать content()
	   В новой функции envelope() можно указать тему письма
		class OrderCreated extends Mailable { // шаблон сократил для краткости
			protected $name; // имя пользователя будем принимать параметром при создании объекта письма
			
			public function __construct($name) { // при создании объекта письма примем аргумент и запишем
        			$this->name = $name;}
	
			public function content(){ // метод создает текст письма и передает в него параметры
        			return new Content(
            				view: 'parts.email', // вызывается вьюха с текстом, передаем параметры
					with: [ 'orderName' => $this->order->name]);}
			
	3) Текст письма во вьюхе order_created, вставляем переменные как в обычный blade через {{}} 
		Добрый день, {{ $name }}! Ваш заказ получен.			
 
	4) В контроллере создаем и отправляем письмо:
		Mail::to(почта)->send(new OrderCreated($name)); // можно использовать очереди Mail::to(почта)->queue(new OrderCreated($name))

	6) Константы почты в .env, настройки в config/mail.php
           Настройки есть в Полезное, меняем только .env

111) По умолчанию используется драйвер почты smtp (уст в .env MAIL_MAILER).
     Если изменить его на log, то почта отправленная через ларавель будет приходить в логи, напр. для тестирования
     После изменения этой настройки может понадобиться:
	php artisan cache:clear
	php artisan config:clear

	#для яндекса в почтовом ящике в настройках включить imap - Портальный пароль
	MAIL_MAILER=smtp // установка драйвера, список есть в config/mail.php
	MAIL_HOST=smtp.yandex.ru
	MAIL_PORT=465
	MAIL_USERNAME=rodnovspb123@yandex.ru
	MAIL_PASSWORD=cinedsmsQ261475288
	MAIL_ENCRYPTION=ssl
	MAIL_FROM_ADDRESS=rodnovspb123@yandex.ru
	MAIL_FROM_NAME="${APP_NAME}"
	
111) return redirect()->back()->with('success', 'Мы свяжемся как товар прибудет') - редирект обратно с флеш сообщением

111) Вставка в таблицу:
	Subscription::query()->create($request->all()); // массовая, разберет поля сам

	Subscription::query()->create(['name' => $request->name]); // с указанием полей

111) То есть любые действия с сервером делаются через гет и пост параметры.
     Гет параметры можно отправить: ссылкой или формой
     Пост параматеры: формой
     Также можно отправить айксом, аякс отправку можно привязать к любому действию на сайте

111) Записи в БД изменяются через Laravel методы модели: create, update, delete, save, destroy и др.
     При изменении записей Модель испускает события: ...ing - событие испускается до изменения, ...ed - после изменения 
	retrieved - извлечение записи из БД
	creating, created - создание новой записи в БД
	updating, updated - обновление записи в БД
	saving, saved - создание или обновление записи, даже если ничего не изменилось
	deleting, deleted - удаление
	trashed - мягко удален?
	forceDeleted - жестко удален?
	restoring, restored - восстановление мягко удаленных?
	replicating - ...

111) Для прослушивания событий Модели используются Обсерверы.

	1) php artisan make:observer ProductObserver --model=Product
           Заранее прописаны болванки для событий: created, updated, deleted, restored, forceDeleted
           Напр.: public function updated(Product $product){dd($product);}
		
	2) Регистрируем обсервер в AppServiceProvider в boot():
	   Product::observe(ProductObserver::class);

	3) Теперь можно прослушивать события Модели и реагировать на них. 
           Например, отследить поступление товара и отправить почту тем, кто его ожидает:
		public function updating(Product $product){ // слушаем в обсервере событие обновления товара
        		$oldCount = $product->getOriginal('count'); // getOriginal - позволяет получить старое значение
        		if($oldCount == 0 && $product->count > 0) { // если count было 0 и мы увеличили его, то
		        Subscription::sendEmailBySubscription($product);;}} // вызываем метод отправки почты и отправляем аргументом обновленный товар

111) config(app.locale) - помощник Ларавель, возвращает значение по ключу из файла конфига

111) get_class($this) - если вызвать в модели, то возвратит имя Модели, так как это класс
	
111) throw new \LogicException('no such attribute for model ' . get_class($this)) - выбрасывание ошибки, покажется на 
										 экране (в режиме debug) и запишется в логи
     throw new \Exception('Error...');

111) То есть получается в верстке blade можно не только вызывать свойства объектов, но и их функции и передавать
     в них аргументы: 
	<h3>{{ $product->__('name') }}</h3> // вызываем функцию __(), прописанную в модели и передаем в нее аргумент.
					       То, что функция вернет вставится в верстку

111) Создание Модели, т.к. Модель это таблица в БД, то сразу создается миграция -m:
	php artisan make:model Currency -m

111) Не обязательно в верстку blade передавать коллекции, как обычно делаем:
	return view('order', compact('order'));

     Можно из верстки обращаться к функциям:
	 @foreach(App\Models\Currency::get() as $currency) // запрашиваем через Модель все записи из таблицы currencies
             <li><a href="{{ route('currency', $currency->code) }}">{{ $currency->symbol }}</a></li>
         @endforeach

111) В верстке blade {{}} заменяют <?= ?> соответственно, можно обращаться к функциям:
	<div>{{ session('currency', 'RUB') }}</div>

111) Guzzle - библиотека для http запросов
     Работает также как file_get_contents('url'), но больше настроек и больше возможностей (просмотр заголовков и др.)

     https://docs.guzzlephp.org/en/stable/quickstart.html

     Подключение за пределами Ларавель:
	1) composer require guzzlehttp/guzzle
	2) Выбираем n - создать composer.json в текущей папке
	3) require 'vendor/autoload.php';
	
	   use GuzzleHttp\Client;
	   use GuzzleHttp\Exception\BadResponseException; - обработка ошибок
		
	   $client = new Client();
	   try { - будут отлавливаться 400, 500 ошибки. Можно в цикле перебирать сайты. Без отлова вылет при первой ошибке
		$response = $client->request('get', адрес_сайта)->getBody()->getContents();
		Затем можно вывести ответ:
		  echo $response;
		Или обработать ответ через DiDOM:
		 $document = new Document($response) - создали объект страницы
		 echo $document->first('p')->text() - вывели только первый абзац ответа
	   } catch (GuzzleHttp\Exception\ClientException $e){
        	$response = $e->getResponse();
        	$responseBodyAsString = $response->getBody()->getContents();}

           Отправка данных в форму (в action формы) через Газзл методом POST:
		$client = new Client();
		try {
		  $response = $client->request('POST', $url, [
        		'form_params' => [
            			'num1' => 11,
            			'num2' => 22,]])->getBody()->getContents();}...
	   То есть запросили страницу, но дополнительно прикрепили в тело запроса POST данные 
	   
     Как использовать Guzzle в Ларавель:
	1) Через сам Guzzle (встроен в Ларавель)

	use GuzzleHttp\Client; - включает класс в шапке
	$client = new Client(); - создаем объект Газзл
	$response = $client->request('GET', 'https://api.url.ru');
	$json = $response->getBody()->getContents() - получим json
	$arr = json_decode($json, true) - распарсим в асс. массив, без флага будет объект
	
	Команд много: request('POST', url, ['body' => 'data']), delete, put, getAsync, getHeader, getStatusCode и пр. 

	2) Через обертку Ларавель вокруг Guzzle, в ней реализованы самые распр. функции:
	
	use Illuminate\Support\Facades\Http;
	$response = Http::get('http://example.com');
	$response = Http::post('http://example.com/users', ['name' => 'Steve']); и др. команды

111) cURL (Client URL) -  это модуль в PHP. Guzzle по умолчанию использует cURL в качестве Http-обработчика.
	
     Guzzle: 
	1) Простой интерфейс
	2) Guzzle может использовать различные типы HTTP-клиентов
	3) Асинхронные и синхронные запросы
	4) В общем, Guzzle позволяет отправить HTTP-запрос легко, и его легко интегрировать с веб-сервисами

     cURL:
	1) Для загрузки cURL требуются дополнительные опции
	2) Нет асинхронных и синхронных запросов, рекурсивной загрузки
	3) Встроен в windows (в терминале ввести where curl) и linux
	

     Пример отправки через cURL POST запроса:
	$curl = curl_init();
	curl_setopt($curl, CURLOPT_URL, 'http://api.loc/index.php');
	curl_setopt($curl, CURLOPT_RETURNTRANSFER, 1);
	curl_setopt($curl, CURLOPT_POST, 1);

	$arr  = [1, 2, 3, 4, 5]; // будем отправлять в POST запросе в виде json
	$json = json_encode($arr); 
	$data = ['json' => $json];
	curl_setopt($curl, CURLOPT_POSTFIELDS, $data);
	
	$res = curl_exec($curl); // отправили запрос и получили ответ
	
	var_dump($res);	

      Пример самого простого get запроса:
	$curl = curl_init('http://rest.loc/good/5');
	curl_exec($curl); // браузер выведет страницу

	curl_exec() - функция выполнения запроса cURL, возвращает true, false или верстку (если уст. CURLOPT_RETURNTRANSFER)
		      Причем 404 ответ не означает ошибку (для таких случаев используем curl_getinfo())

111) Работа с cURL: 
	$headers = ['Accept-Language: en-US', 'Accept: text/html']; // создаем заголовки если нужны, писать через двоеточие,
							               список искать в конспекте
	$get = ['name'  => 'Alex', 'email' => 'mail@example.com'] - гет параметры

	Создаем объект curl:
	$curl = curl_init('https://phpstack.ru/');
	$curl = curl_init('https://phpstack.ru/?' . http_build_query($get)); - с параметрами

	POST запрос + прикрепить POST параметры:
	curl_setopt($ch, CURLOPT_POST, 1)
	curl_setopt($ch, CURLOPT_POSTFIELDS, http_build_query($get))

	Прикрепить файлы:
	curl_setopt($ch, CURLOPT_POSTFIELDS, ['photo' => curl_file_create(__DIR__ . '/image.png', 'image/png'])

	Есть также вариант с отправкой файла:
	curl_setopt($ch, CURLOPT_HTTPHEADER, array("Content-Type:multipart/form-data"))
	curl_setopt($ch, CURLOPT_POSTFIELDS, $params) - в $params вставляется путь к фото

	PUT, DELETE запросы:
	curl_setopt($ch, CURLOPT_CUSTOMREQUEST, 'PUT')

	Запись и отдача кук (лучше прописать прямо в $headers заголовок cookie с браузерными куками:
	curl_setopt($ch, CURLOPT_COOKIEFILE, __DIR__ . '/cookie.txt') - если в пути есть русские папки, 
			                                                то с DIR работать не будет, нужно без него (потом искать снаружи папки)
	curl_setopt($ch, CURLOPT_COOKIEJAR, __DIR__ . '/cookie.txt')

	Принудительно передать куки:
	curl_setopt($ch, CURLOPT_COOKIE, 'PHPSESSID=61445603b63')

	curl_setopt($curl, CURLOPT_FOLLOWLOCATION, 1) - следовать перенаправлению согласно любому заголовку "Location:"
							Намучился при парсе сайта без этого заголовка, так как Курл
							возвращал пустую строку и DiDOM вываливался с ошибкой на ""
	
	curl_setopt($curl, CURLOPT_VERBOSE, true); - выводить отладочную информацию
	curl_setopt($curl, CURLOPT_STDERR, fopen('./curl.log', 'w+')); - записывать ее в этот файл
	
	curl_setopt($ch, CURLOPT_HEADER, false) - отключаем вывод заголовков у себя

	curl_setopt($curl, CURLOPT_TIMEOUT, 5); - остановить запрос курл, если сайт завис или не отдается. Очень полезно при парсинге

	curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, 0) - отменить проверку сертификата SSL для https
	curl_setopt($ch, CURLOPT_SSL_VERIFYHOST, 0)   Избавляет от ошибок, но не рекомендуется. А без него бывает не работает

	curl_setopt($curl, CURLOPT_HTTPHEADER, $headers) - устанавливаем заголовки в запрос 

	curl_setopt($curl, CURLOPT_TIMEOUT, 30) - максимальное время работы в секундах

	curl_setopt($ch, CURLOPT_ENCODING, 'gzip'); - раскодировать из gzip (этот формат передается если запрашиваем страницу 
					              и устанавливаем заголовок 'Accept-Encoding: gzip, deflate',
						      В таком случае сервер вернет сжатый овтет и заголовок Content-Encoding: gzip
	
	curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1) - отключить авт. вывод ответа в браузер, напр. 
						      если записываем его в переменную 
	$html = curl_exec($curl) - выполняем запрос и записываем верстку в переменную
	curl_close($ch) - закрытие соединения (не обязательно, php сам очистит память от объектов без ссылок.
			  Нужно если дальше в коде меняем гет параметры, заголовки пр.)
	echo $html;

	Пример простого GET запроса (выше есть еще проще):
	$curl = curl_init('https://site.ru/');
	curl_setopt($curl, CURLOPT_RETURNTRANSFER, true) - ответ не выводить, а вернуть строкой
	curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, false);
	$html = curl_exec($ch);
	echo $html;

	Показываем ошибки cURL:
	$curl = curl_init('https://lenta12313.ru/');
	curl_setopt($curl, CURLOPT_RETURNTRANSFER, 1);
	$html = curl_exec($curl);
	if($html === false){
    	   echo curl_error($curl); => Could not resolve host: lenta12313.ru
	} else {
          echo $html;
	}

	Выводим информацию о последней операции:
	print_r(curl_getinfo($curl));
	
	Имитация браузера
	На некоторых сайтах стоит защита от парсинга, когда проверяются пришедшие заголовки. 
	Если в запросе их нет, то м.б. 404 или 500 ошибка. Поэтому можно прикрепить заголовки
	браузера: открываем любой сайт, инструменты разработчика, правой кнопкой на адрес сайта,
	копируем как фетч, там список заголовков, которые переписываем в массив.
	Можно смотреть в панели разр.
	Нужные equest заголовки: Accept, Referer, User-Agent, X-Requested-With (без этого не работал запрос курлом на фетч адрес),
	                         cookie (без этого не работала регистрация на javascript.ru)
111) Так понял, что защита методом ожидания проверки браузера работает через установку кук. Получил их через браузер, 
     вставил в headers курла и заработало. 

111) Пример работы с cURL:
	$curl = curl_init('https://www.cy-pr.com');
	curl_setopt($curl, CURLOPT_SSL_VERIFYPEER, false); - без этого не работало
	curl_setopt($curl, CURLOPT_RETURNTRANSFER, true);
	$html = curl_exec($curl);

	$document = new Document($html);
	$scripts = $document->find('script');

	foreach ($scripts as $script){
    	   $script->remove();  - удаляем все скрипты из файла
	}

	echo $document->html(); - потом показываем файл. Иначе скрипты перенаправляли
 
111) Удобно размещать файл настроек в папке config и затем использовать Ларавель функцию config.
     Например, config/params.php:
		return ['key' => 'daDE274as']

     Получение: config('params.key')

111) Выбрасывание ошибки:
	1) use Exception;  throw new Exception('Ошибка запроса валют');
	2) throw new \Exception('Ошибка запроса валют');

111) Обновление базы в соответствии с внешними данными, например, обновление курса валют:
	1) Получили курс по api, использовал guzzle
	2) $currency->update(['rate' => курс]); // обновили базу через update

111) $user->touch() - метод touch обновляет поле updated_at на текущее время

111) Auth::check() - проверка, что пользователь аутентифицирован
     Auth::id() - получение id пользователя

111) $table->dropColumn(['name', 'sursum']) - вместо двух строчек удаления колонок в down миграции

111) Делают и возвращают одно и то же.
     	$users = User::all() //  all - статический метод Eloquent\Model
     	$users = User::get() //  get - метод Eloquent\Builder

     Только перед get мы можем дополнить запрос, например через where().
     all() внутри себя вызывает метод get

111) map - метод для коллекций, прогоняющий все элементы через коллбэк

111) Композер Ларавель позволяет подгружать нужные переменные при загрузке определенных вьюх

111) php artisan make:model PropertyOption -m - создание модели property_options + миграция. Название модели пишем через CamelCase

111) php artisan make:migration create_sku_product_table --create=my_product - флаг --create устанавливает имя таблицы, 
                                                                               которая будет создаваться, создастся таблица my_product
111) В MySQL поле типа int: от -2147483648, до 2147483647 (2 млрд), если задать unsigned, то увеличивается в два раза до 4294967295:
	$table->unsignedInteger('product_id');
  то же $table->integer('product_id')->unsigned();   

111) php artisan down - переключает приложение в режим отладки, сайт становится недоступным - 503  SERVICE UNAVAILABLE 
     php artisan up - переход в обычный режим

111) protected $fillable = ['name', 'surname', 'salary'] - по этим элементам проходит цикл и в базу уходят только те поля инпутов,
                                                           которые имеют такой же name. То есть этот массив применяется при массовой
							   отправке полей формы в БД. Так, теперь не получится добавить в браузер 
							   в форму поле 'isAdmin' = 1; 

111) php artisan down -  режим обслуживания (c отдачей 503 кода), приложение частично отрабатывает (грузятся зависимости composer)
     php artisan down --render="errors::503" - режим обслуживания с моментальной отдачей своей страницы views/errors/503.blade.php.
     php artisan up - переход в обычный режим
     php artisan down --secret="cined" - переход в режим обслуживания с установкой токена для обхода режима:
		http://start.loc/cined - теперь при входе на сайт по этой ссылке для браузера установится куки для обхода режима
     php artisan vendor:publish --tag=laravel-errors - создание папки с ошибками во вьюхахах

111) Константы из .env при запуске приложения загружаются в PHP массив глобальных переменных $_ENV.
     Доступ к этим переменным возможен как через $_ENV, так и через помощник env()
     'debug' => env('APP_DEBUG', false) - читаем глобальную переменную APP_DEBUG, если не установлена, то возвращаем false

111) Переменные из .env используются в файлах config.
     Само приложение читает эти переменные не напрямую, а через файлы config.

     config() - помощник для чтения переменных из папки config 
	      $value = config('app.timezone', 'по умолчанию');
     config(['app.timezone' => 'America/Chicago']) - так устанавливаем переменные конфига прямо в скрипте

111) php artisan config:cache - используем на продакшене для объединения настроек в один файл для ускорения приложения.
			        Не используем во время разработки.
     Внимание! Если закешировали настройки таким образом, то env() не будет работать в скриптах, так как настройки из .env уже не подтягиваются.
     Поэтому env() можно использовать только в файлах папки config

111) При входящем http запросе и запуске приложения всегда отрабатывают провайдеры указанные в config/app.php->providers
     Одним из таких провайдеров является AppServiceProvider. В его методе boot мы можем выполнитить свои действия.

111) Мы можем создать свой провайдер: 
	php artisan make:provider MyProvider
     Для того, чтобы он запускался с приложением нужно зарегистрировать его в:
	config/app.php->providers (так понял, что отсюда все запускается в начале)	

111) Внедрение зависимостей и контейнер. Как я это понял.
     При запуске приложения создается контейнер (получить можно помощником app()) на основе класса App. 
     Этот контейнер умеет извлекать и отдавать привязанные к нему объекты классов. 
     Отдадим объект $request:

     	Route::get('/', function (Request $request) { - внедрили зависимость/инъекцию текущего запроса
			// ... });			
    			
     Обращение к контейнеру внутри кода:
	use Illuminate\Support\Facades\App; // импортируем пространство имен
	dd(App::getLocale()) // обращаемся к методам и свойствам контейнера
	
	app() - получить объект контейнера. Там видно и провайдеры и используемые другие классы (cache, session и т.д.)

111) В программировании Фасад - это класс, обертывающий сложную библиотеку, чтобы предоставить ей более простой и понятный интерфейс.

     Фасад Laravel - это класс, который обеспечивает удобный доступ к объекту в сервисном контейнере.
                     То есть фасад в Ларавель упрощает доступ к объектам сервис провайдеров прописанным в контейнере Ларавель

     Например, вместо того, чтобы прописывать: 
	cache()->put('hello','world', 600);
     С помощью фасада сделаем то же самое:
	Cache::put('hello','world', 600);

     То есть, например, фасад DB этот класс, который в своем статическом методе как бы (об этом ниже) обращается к instance контейнеру:
	class DB { public static function select($query){ return app('db')->select($query) }} - смысл такой
     Соответственно: DB::select($query) - мы обращаемся через класс-фасад к контейнеру

     Но в Ларавель это упрощено через маг. метод, чтобы для каждого метода фасада не возвращать app('db')->. 

     Request::fullUrl() это то же самое, что: 
	App::make('request')->fullUrl() - обращение через контейнер
	app('request')->fullUrl(); - обращение через контейнер
	request()->fullUrl() - через функцию помощник
	
111) Примеры фасадов: Auth, Cache, DB, Mail, Route, Validator и пр. - соответствующие объекты загрузились в контейнер из списка провайдеров
                                                                      при загрузке приложения
                                                                       
111) Вместо фасадов можно использовать помощники. Их не нужно импортировать как фасады, так как встроены глобально.
	View::make('profile') то же, что view('profile') // возвраты вьюхи

111) Создание своего фасада:
	1) Вставляем наш класс в автозагрузку провайдеров в config->app.php->providers:
		Intervention\Image\ImageServiceProvider::class,
	2) В этом же файле в aliases добавляем:
		'Image' => Intervention\Image\Facades\Image::class,
	3) Теперь в коде можно пользоваться фасадом Image::

111) Маршрутизация в web.php:
	1) Без контроллера:
		Route::get($uri, function (){}) // просто функция
		Route::get($uri, function (){return view('index')}) // возврат вьюхи
		Route::view('/welcome', 'welcome') // быстрый возврат вьюхи
	2) С контроллером:
		Route::get('/', [MainController::class, 'index']);

     Встраивание параметров маршрута в принимающую функцию:
	1) Без контроллера:
		Route::get('/{a}', function ($a){
    			return view('index', compact('a'));});
	2) С контроллером:
		Route::get('/{a}', [MainController::class, 'index']) // то есть ничего делать не надо при передаче
		
		В контроллере принимаем:
		public function index($var) { return view('index', compact('var')) } // и, например, отправляем как есть во вьюху

     Отправлять можно сколько угодно параметров:
		Route::get('/{a}/page/{b}', [MainController::class, 'index']);
		
		В контроллере принимаем:
		public function index($var1, $var2) { show($var1, 1); }

    Параметры маршрута внедряются в зависимости от порядка, имена не важны

111) Ограничение параметров маршрута через рег. выражение:
	Route::get('/{a?}', [MainController::class, 'index'])->where('a', '[0-9]+');
	Route::get('/user/{id}', function ($id) {})->where('id', '[0-9]+');

     Есть короткие методы:
	->whereNumber('a') - числа
	->whereAlpha('a') - буквы
	->whereAlphaNumeric('a') - числа и буквы

    Если входящий url не соответствует ограничениям, то будет возвращена 404 страница

111) Также можно глобально ограничить параметры для всех маршрутов, например сделаем чтобы id принимал только числа:
	Route::pattern('id', '[0-9]+'); // прописывается в RouteServiceProvider в boot

111) Можно указать необязательность параметра через ?:
	Route::get('/{a?}', [MainController::class, 'index']);

     При принятии нужно обязательно назначить параметр по умолчанию:
	public function index($var=333){}

111) Доступные методы маршрутизации: Route::get($uri, $callback): get, post, put, delete, putch, options
     Выборочно: Route::match(['get', 'post'], $callback)	
     Любой: Route::any(['get', 'post'], $callback)
	
     ВАЖНО: если для упрощения использовали match или any, но есть более частные случаи, то частные случаи должны быть над match или any

111) Внедрение зависимости в маршрут из контейнера Ларавель: 
	Route::get($uri, function (Request $request){})

     $request можно получить и в самом контроллере из того же общедоступного контейнера:
        Route::get('/', [MainController::class, 'index']);

	class MainController extends Controller { public function index(Request $request){ ... }}

111) Все формы, кроме get должны иметь токена @csrf, от защиты копирования формы на другой сайт

111) Перенаправление маршрута в web.php:
	Route::redirect('/here', '/there'); // 302 редирект
	Route::redirect('/here', '/there', 301) // код можно указывать	
	Route::permanentRedirect('/here', '/there'); // 301 код

111) Быстрый возврат вьюхи из файла web.php без посредника-контроллера:
	Route::view('/welcome', 'welcome') // показывает вьюху welcome.blade.php
	Route::view('/welcome', 'welcome', ['name' => 'Taylor']); // то же с прикреплением переменных

111) Именованные маршруты с помощью помощника name:
	Route::get('/', [MainController::class, 'index'])->name('main');

111) Вставка именованного маршрута в blade:
	{{ route('profile') }} 

111) Переход по именованным маршрутам:
	return redirect()->route('profile');

111) Если маршрут имеет параметры:
	Route::get('/{a}', [MainController::class, 'index'])->name('main');
	
	То вставляем их в именованный маршрут так:
		{{ route('main', ['a' => 111]) }} 

	Если передать лишние параметры в роут, то они вставятся как гет-параметры в адрес:
		<a href="{{ route('main', ['a' => 111, 'size' => 'small']) }}">Ссылка</a>  => /111?size=small

111) Группировка маршрутов позволяет применять к нескольким маршрутам одинаковые атрибуты, например: посредники, префиксы и др.
	Route::middleware('check')->group(function(){ // все маршруты пройдут через посредник check
    		Route::get('/', $callback1);
    		Route::get('/page', $callback2);});

	Route::prefix('admin')->group(function(){ // все url должны выглядеть как: /admin/...
    		Route::get('/', $callback1);
    		Route::get('/page', $callback2);});

	Route::name('page.')->group(function(){ // добавим к названию именованного маршрута строку page
    		Route::get('/', $callback1);
    		Route::get('/page', $callback2);})->name('first'); // в итоге им. маршрут page.first

	Route::prefix('admin')->group(function(){
		Route::middleware('check')->group(function(){ .... })}) // можно вкладывать группы друг в друга

	Route::group([ - вместо вложенной группировки можно пользоваться так
		'prefix' => 'admin', 
		'middleware' => 'auth'
		     ], function(){
			    Route::get('/', [MainController::class, 'index'])->name('index')})

111) Так как часто параметры url нужны для извлечения данных из таблиц по id, то можно укоротить вписав в фигурные скобки имя модели:
	Route::get('/{user}', [MainController::class, 'index']); // по умолчанию параметр принимает id и должен соответствовать имени Модели/таблицы
	Route::get('/{post:slug}', [MainController::class, 'index']); // можно извлекать модели Eloquent, используя столбец, отличный от id, здесь slug
	Route::get('/{user}', [MainController::class, 'index'])->withTrashed(); - для мягкоудаленных сущностей

public function index(User $user) { //в методе контроллера внедряем нужную модель 
        	dd($user->name)}; // все работает, пример: /32 => Денис
	То есть при такой привязке не нужно прописывать: $user = User::find($id), т.к.  user внедрился

111) В модели можно указать использование вместо id при неявной привязке другого поля:
	public function getRouteKeyName(): string { return 'name'; } - теперь будет искаться по полю name, в роутах не нужно писать 
                                                                       Route::get('/{user:name}', ...)

111) Также можно связать имя параметра и модель:
	Route::model('number', User::class); - пишется в методе boot в RouteServiceProvider
	Route::get('/users/{number}', ...) - при параметре number будет искаться модель User по id

111) Если в случае укороченного обращения к Модели запись будет не найдена, то можно предусмотреть это через missing:
	Route::get('/posts/{post:slug}, [MainController::class, 'index'])->missing(function (){выполнится если запись не найдется})

111) В RouteServiceProvider можно cвязать название параметра и таблицу:
	Route::model('qwerty', User::class);
     Теперь параметр qwerty будет означать, что извлекать будем из модели  User, в данном случае по id:
	Route::get('/{qwerty}', [MainController::class, 'index']);

111) Если адрес не существует, то возвращается 404 ошибка.
     Мы можем переопределить это поведение через fallback()

     Route::fallback(function (){return redirect()->route('home');}); 

111) Ограничение количества запросов:
	1) Встроенное:
	     Route::middleware('throttle:3,10')->group(function () { - применяется по ip, разрешается 3 запроса в течение 10 минут
    		Route::get('/', [MainController::class, 'index']);});  также можно middleware('throttle:3') - 3 запроса в течение минуты

	2) Настраиваемое:
	     Route::middleware(['throttle:bot'])->group(function () { - называем ограничитель bot
    		Route::get('/', [MainController::class, 'index']);});

     	     В RouteServiceProvider в методе boot пишем логику ограничения:
	        RateLimiter::for('bot', function (Request $request) {
                     return Limit::perMinute(100)->by($request->ip());}); - не более 100 запросов на ip

	             return $request->user()->vipCustomer() - если пользователь особенный
                       ? Limit::none() - снимаем ограничения на запросы
                       : Limit::perMinute(100)->by($request->ip()); - иначе даем только 100 запросов в минуту c ip

     Можно отдавать собственный текст при превышении количества запросов:
	    return Limit::perMinute(10)->response(function (Request $request, array $headers) {
                return response('Превышено количество запросов', 429, $headers);});
	
111) @method('PUT') - добавляем в форму, если нужен метод не get и не post, например, при отправке отредактированной записи через resource контроллер
	              При этом в форме все равно пишем method="post", иначе форма отправляется как get

111) $route = Route::current() - получение текущего маршрута, соотвествует $request->route() внедренного через инъекцию
     $name = Route::currentRouteName() - название имени роута (->name(...))
     $action = Route::currentRouteAction() - название метода роута

111) Посредники middleware обычно проверяют входящие http запросы.
     Создание посредника для проверки токена. Например пароль для входа 123: http://start.loc/?token=123
	1) php artisan make:middleware TokenIsValid
	2) Регистрируем название в Kernel в $routeMiddleware:
		'tokenisvalid' => \App\Http\Middleware\TokenIsValid::class
	3) Прописываем нужные действия в посреднике в методе handle:
		if(!empty($request->token) && $request->token == 123){ // если гет параметр есть и равен 123
            		return $next($request); // пропускаем
		} else {
            		dd('Токен неверный');} //иначе другие действия
	4) Устанавливаем посредника в маршрут:
		Route::get('/', [MainController::class, 'index'])->middleware('tokenisvalid');

111) Можно установить несколько посредников: ->middleware(['first', 'second'])

111) Можно установить наш посредник вообще для всех маршрутов, нужно зарегистрировать в Kernel -> $middleware

111) Если применяли посредник для группы маршрутов и нужно исключить для одного маршрута - withoutMiddleware:
	Route::middleware(['auth'])->group(function () {
		Route::get('/profile', $callback)->withoutMiddleware('auth')  })

111) middleware('throttle:3,10') - ограничение запросов с ip адреса к странице, здесь 3 запроса в течение 10 минут
     middleware('throttle:3') - 3 запроса в течение минуты
	Если запросов больше, то возвращается ошибка 429 Too many requests

111) В посредник можно передавать аргументы:
	->middleware('role:editor')

111) CSRF - межсайтовая подделка запроса (Cross Site Request Forgery – CSRF).
     Ларавель для каждой сессии создает csrf токен и хранит его в этой сессии, напр.: UJmQu7r3e1dNXeMgGSLQRD5CJJI1B3Hxn2hqDJZ8

     Получить токен csrf можно:
	1) Через помощник csrf_token()
	2) Через сессию $request->session()->token()
     
     Для всех html форм POST, а также «PUT», «PATCH» или «DELETE» (@method('PUT') и т.д.) мы должны крепить @csrf.

     @csrf это то же, что и <input type="hidden" name="_token" value="{{ csrf_token() }}" />
     @method('PUT') это то же, что и <input type="hidden" name="_method" value="put">
	
     То есть форма все равно отправляется как post, только Ларавель видит инпут скрытый с put и применяет для БД метод update	

     Посредник VerifyCsrfToken сравнивает _token пришедших форм с токеном в сессии пользователя.

111) Создание ресурсного контроллера:

     1) php artisan make:controller PhotoController --resource
     При таком методе методы создадутся по типу: public function edit($id)
     В функции нам понадобится получать запись из БД: $user = User::findOrFail($id);

     Можно воспользоваться неявной привязкой:
     2) php artisan make:controller PhotoController --model=Photo --resource
     В этом случае методы создадутся так: public function edit(Photo $photo)
     В функции можно сразу читать свойства записи: $name = $photo->name // $photo автоматически извлеклась при вызове edit(Photo $photo)
     
     Неявная привязка подробнее:
	Как выглядит метод показа заказа:
	<a href="{{ route('orders.show', ['order' => $order->id]) }}">Открыть</a> // в верстке
	Раньше в контроллере приходилось сначала находить заказ по полученному id:
		$order = Order::findOrFail($id)	 // нашли и теперь выполняем с ним действия
	А при неявной привязке он нам приходит уже найденный по id:
		public function show(Order $order){ // но нужно встроить зависимость Order
        		dd($order);}

     3) php artisan make:controller PhotoController --model=Photo --resource --requests
     Теперь создался класс Request, в котором можно будет проверять поля формы (validate)


     Установка маршрутов ресурсного контроллера:
	Route::resource('photos', PhotoController::class);

     Посмотреть получившиеся роуты (при включенном vite??: npm run dev показывался красивый список):
	php artisan route:list
	
     Примеры:
	Метод: get, uri: /photos, функция: index, им.маршрут: photos.index - показ всех фото
	Метод: post, uri: /photos, функция: store, им.маршрут: photos.store - сохранение фото (после создания)
	Метод: put, uri: /photos/{photo}, функция: update, им.маршрут: photos.update - сохранение/обновление фото (после редактирования)

111) При обновлении, удалении, получении фото используются uri c id фото, напр:  /photos/12 
     Если пользователь вручную введет несуществующий id, то Ларавель вернет 404 ошибку.
     Однако для таких случаев мы можем использовать метод missing: // должно работать только при неявной привязке, когда при создании рес. контроллера
     добавили флаг --model=Photo и создались методы с внедрением модели:  function edit(Photo $photo) вместо function edit($id)
	Route::resource('photos', PhotoController::class)->missing(function (Request $request) {
            return Redirect::route('photos.index');
        });	

111) При удалении, редактировании сущностей на отдельной странице нужно потом возвращаться на ту же страницу пагинации, 
     напр. http://start.loc/users?page=2
     но return redirect()->back() возвращает обратно на страницу изменения сущности.
     Нашел выход такой:
	1) При переходе на страницу изменения сущности помещаю в сессию предыдущую страницу:
		$url = url()->previous();
		session()->put('prev-url', $url);
	2) После изменения делаю редирект так:
		$url = session()->get('prev-url');
        	return redirect($url); // теперь возвращает на http://start.loc/users?page=2

111) Если одновременно получаем слаг из маршрута и внедряем зависимость:
	Route::put('/user/{id}', [UserController::class, 'update']);
     то сначала вредряем зависимость, потом слаг:
	public function update(Request $request, $id)

111) Illuminate\Http\Request - объект текущего http запроса.

111) Параметры маршрута Route::put('/user/{id}', [UserController::class, 'update']); указываются в принимающей функции после зависимости:
	public function update(Request $request, $id)

111) Методы для работы с HTTP-запросом:
	Сам реквест встраивается в контроллер через Request $request

	Также мы можем обращаться к нему без встраивания или вне контроллера через помощник request() - то же самое с теми же методами работает

	request()->uri - доступ к реквесту и его свойству вне контроллера

	$request->path() - путь запроса, например: /, list/page1
		->is('admin/*') - соответствует ли шаблону
		->routeIs('admin.*') - соответствует ли именованному маршруту
		->url() - полный url запроса без строки query: http://start.loc/page
		->fullUrl() - полный url со строкой запроса: http://start.loc/page?size=5
		->fullUrlWithQuery(['name' => 'Denis']) - добавляет новые гет-параметры к url: http://start.loc/page?size=5&name=Denis
		->method() - вернет метод входящего http запроса, например GET при запросе страниц или POST при отправке формы
		->isMethod('post') - проверка метода запроса
		->host(); - хост (lara.ru)
     		->header('Cookie') - заголовок запроса (здесь куки браузера для этого сайта)
		->ip() - ip адрес, напр.: 127.0.0.1
		->getAcceptableContentTypes() - типы содержимого, которые принимает браузер (text/html, application/xhtml+xml, image/webp и др.)
		->prefers(['text/html', 'application/json']) - покажет наиболее предпочтительный тип из указанных
		->expectsJson() - ожидает ли запрос только json данные
	
	Получить входящий json:
		$request->json()->get('key') - получаем значение ключа
		$request->key - то же самое, но короче авт. распарсивание json
		$request->input('user.name') - авт. распарсивание json и получение по ключу

	Получение данных полей ввода и гет-параметров:
		$request->all() - получить все данные массивом
			->collect() - получить все данные коллекцией
			->input() - получить все данные массивом
			->key - получить данные указанного инпута или гет параметр, независимо от метода запроса (гет или пост)
			->input('name') - получить данные указанного инпута или гет параметр, независимо от метода запроса (гет или пост)
			->input('name', "по_умолчанию") - можно указать значение по умолчанию
			->input('products.0.name') - если из поля пришел массив, то черех точку можно получить доступ к ключам
			->boolean('checkbox') - авт. преобразование флагов (возвращается true для 1, "1", true, "on", "yes")
			->date('birthday') - авт. преобразование даты в объект Carbon

		$request->query() - получить все гет параметры
			->query('name') - получить гет параметр по имени (проще $request->name)

			->boolean('checkbox') - превращает вернувшееся значение в true или false (приходит on или пусто)
			->date('date') - возвращает объект даты Carbon

			->only(['username', 'password']) - получить указанные инпуты или гет-параметры
			->except('credit_card') - получить все кроме указанных
	
	Можно сразу обращаться к имени инпута:
		$request->surname
		
			->has('name') - проверка заполнен ли указанный инпут. Однако пустой инпут возвращает "", что метод считает на true
			->has(['name', 'email']) - проверка, что заполнены все эти значения
			->filled('name') - проверка, что значение заполнено и это не пустая строка. Для пустого инпута возвращает false
			->missing('name') - проверка на отсутствие
			->merge(['votes' => 0]) - добавить или переписать значение
			->mergeIfMissing(['votes' => 0]) - добавить значение, если его нет

		$request->flash() - поместить данные инпутов во флеш сообщения, будут доступны 1 раз при следующем запросе

		return redirect()->route('create')->withInput() - редирект на маршрут с авт. сохранением инпутов в сессию		
						  ->with('error','username is invalid') - c добавлением своей переменной в сессию
	
	Для того, чтобы получить флеш переменные используем:
	 	1) old('username') - помощник в коде
		2) {{ old('username') }} - помощник в верстке

	$request->cookie('laravel_session') - получение кук (+ расшифровка посредником EncryptCookie, в браузере они в зашифрованном виде)
 
	В Ларавель по умолчанию для всех маршрутов включены посредники TrimStrings и ConvertEmptyStringsToNull,
	значения инпутов обрезаются от пробелов по краям, а пустые строки преобразуются в null
	Так, отправляя пустой инпут функция old('name') покажет null

111) Отключение превращения в ноль при вставке в БД:
	В ConvertEmptyStringsToNull в AppServiceProvider пишем: 
	    public function boot() {
    		ConvertEmptyStringsToNull::skipWhen(function ($request) {
        		return $request->template_id == 1;});} // не превращать данные в ноль если выполняется условие

111) Файлы в $request
	Сначала файл грузится во временное хранилище.
	
	$file = $request->file('photo') - получение загруженного файла (форма д.б. multipart/form-data)
	$file = $request->photo - то же самое
	$request->hasFile('photo') - проверка есть ли файл в запросе, не рабоатет с CKFinder
	$request->file('photo')->isValid() - проверка, что не было ошибок при загрузке файла
	$path = $request->photo->path() - получение пути к временно загруженному файлу C:\OpenServer\userdata\temp\upload\php84BF.tmp
  	$extension = $request->photo->extension() - Ларавель угадывает расширение файла

	Для того, чтобы сохранить временный файл как обычный в файловой системе исп. store:
	$path = $request->photo->store('images'); - сохраняем в папке storage/app/images, название файла меняется, получаем путь:
						    images/3jPvAOn4AxyyfMyzG0ytkrrH3I1Mikf9J7nynhjd.jpg , затем сохраняем в базу
						    Насколько понял имена меняются, чтобы не было конфликтов одинаковых имен
					            и чтобы злоумышленник не качал чужие файлы перебором имен
						    Итого таблица: id, originalName, path(путь к файлу с изм. названием), 
						    order_id (номер заказа которому принадл. файл)
	Лучше сохранять в папки по дням:
	$folder = date('Y-m-d');
        $path = $request->file('thumbnail')->store("/images/{$folder}"); // сохраняем изобр. и возвращаем путь

	Для работы с файлами понадобится прилинковать storage в папку public, так как браузер имеет доступ только в папку public:
		php artisan storage:link
	Также поменять в .env: 
		FILESYSTEM_DISK=public

	Теперь загруженные файлы доступны:
	1) Пользователь грузит файл:
		<form action="" enctype="multipart/form-data" method="post">
        	@csrf
        	<input type="file" name="file">
        	<input type="submit">
    		</form>

	2) Получаем файл в верстке:
		<img src="/storage/images/SNSWFxe4ekzAck8MEGL6oNVD1GV4bD1XMUX0DuFQ.jpg" alt=""> // ссылку прописать правильно,
												   главное файл доступен в public
	3) Также: <a href="/storage/images/9ivNa8yX4cngDIPNanphOyeDnrCuGMXTLhOifRLm.jpg" download>Скачать</a>
	   Также: <a href="{{ Storage::url($file->path) }}" download="{{ $file->originalName }}">{{ $file->originalName }}</a>
		  Где:
		    1) Storage::url выстраивает полный путь к файлу по пути из БД
		    2) download="имя" - здесь прописываем под каким именем отдать файл на скачивание 

	4) Также отдаем на скачиваение файл из контроллера:
		return response()->download('./storage/images/9ivNa8yX4cngDIPNanphOyeDnrCuGMXTLhOifRLm.jpg');
		
111) HTTP ответы Ларавель
	return 'qwerty' - возврат строки Ларавель авт. преобразует в полноценный http ответ
	return [1,2,3] - массивы автоматически конвертиртуются в json

	Route::get('/user/{user}', function (User $user) { // в {user} вписано название модели , принимаем здесь id записи из БД
    		return $user;}); - коллекции Eloquent также преобразуются в json
	
	return response('Hello World', 200) - возвращаем ответ с добавлением заголовков и кода ответа
                  ->header('Content-Type', 'text/plain');
		  ->header('X-Header-One', 'Header Value') // устанавливаем свой заголовок

	return response($content)
            	->withHeaders([...]) - устанавливаем много заголовков разом

	return response('Hello World')->cookie( - установка куки, они передаются в браузер зашифрованными
    		'name', 'value', $minutes); // имя куки, значение и количество минут

	Если объект ответа еще не сформирован:
		Cookie::queue('name', 'value', $minutes) - прикрепление кук к ближайшему http ответу
		Cookie::expire('name') - обнуление жизни куки
	
	return view('index') - отдаем вьюху
	return response()->json([]) - отдаст массив в json. Однако можно просто: 
	return [массив] - будет то же самое автоматически
	return User::find(1) - объект авт. преобразуется в json

	return response()->file('./storage/images/9ivNa8yX4cngDIPNanphOyeDnrCuGMXTLhOifRLm.jpg') - показать файл в браузере
	return response()->download('./storage/images/9ivNa8yX4cngDIPNanphOyeDnrCuGMXTLhOifRLm.jpg'); - отдать файл на скачивание

111) Отправка POST запросов из Ларавель: HTTP::post(адрес сайта, прикрепляемые данные)

111) Отправка ошибок приложения.
     При любой ошибке запускается Exceptions/Handler и отрабатывает метод register()
     В нем  можно написать любую логику, например отправка ошибки в телеграм:

	$this->reportable(function (Throwable $e) {
            HTTP::post('https://api.telegram.org/bot5993833177:AAEgydszALUZLtVcx4nITJQAsWTtDbWkjIE/sendMessage', [
                'chat_id' => 1894725120,
                'text' => $e->getMessage()]);}); // все ошибки из боевых приложений будут отправляться мне в телеграм от бота
		
111) Перенаправления
	return redirect('page') - редирект с указанием пути, в данном случае на http://start.loc/page
	return back() - переход обратно на прошлую страницу, например после post отправки формы переход обратно на форму
	return back()->withInput() - то же самое с флеш сохранением данных формы, например при ошибке валидации
	return redirect()->route('login') - перенаправелние на имен. маршрут
	return redirect()->route('profile', ['id' => 1]) - переход по им. маршруту + добавление гет параметров, принимать можно: $request->id
	
	Если маршрут неявно связан с таблицей и ожидает id, например:
		Route::get('/user/{user}', $callback)->name('get_user')
	То редирект на такой маршрут можно сделать так:
		return redirect()->route('get_user', [$user]);

	return redirect()->away('https://ya.ru/') - редирект на внешний сайт

	return redirect('main')->with('succes', 'Вы успешно зарегистрированы!') - редирект с записью во флеш сообщения
		такие флеш сообщения в верстке принимаем так:
			@if(session('success')) 
				<div>{{ session('success') }}</div>
			@endif

	return redirect()->action([UserController::class, 'index']) - перенаправление напрямую на метод контролера

111) Представления:

	Помощник view создает текст по шаблону и полученным переменным.
  	Верстку можно сохранять в переменную: 
		$text = view('page.error', ['e' => $error])

	Возврат представления с прикреплением переменных:
	return view('page', [$var => 111]) - возврат вьюхи page с передачей данных
	return view('index')->with('name', 'Денис') - то же самое, прикреплена переменная
	return view('page', compact('var')) - более удобный способ
	return view('admin.profile', $data) - возврат вьюхи profile в папке admin

     Делаем переменные доступными во всех вьюхах, для этого используем share в методе boot AppServiceProvider:
	View::share('key', 'Денис');

     Автоматически прикрепляем переменные при вызове конкретной вьюхи, делаем в boot AppServiceProvider:
	public function boot(){
        	view()->composer('layouts.footer', function($view){ // при выводе какой вьюхи будет срабатывать
                $view->with('rubrics', Rubric::all());});} // запрашиваем данные в БД и крепим пременную rubrics

     Для нескольких вьюх используем массив:
		view()->composer(['index', 'page'], function($view){...})

     Если поставить звездочку, то будет вызываться для всех вьюх:
		view()->composer('*', function($view){...})		
	
     Шаблоны лучше скомпилировать на боевой версии:
	php artisan view:cache
     Ларавель сравнит дату вьюхи из кеша и если она выше даты исходника, то будет показывать ее. Иначе перекомпилирует (затратно)

111)	Для вывода php кода в верстке blade используется {{  }}
	Все что находится в {{}} прогоняется через htmlspecialchars для предотвращения XSS-атак.
        htmlspecialchars() заменяет &, <, >, ", ' на html сущности, чтобы нельзя было встроить вредноносный код в верстку,
        например, при отправке в БД через форму '<script> код </script>'

	{!! !!} - если хотим выводить верстку из БД без обработки htmlspecialchars 
	{!! clean($text) !!} - либо с удалением опасных тегов пакетом mews/purifier

	{{ }} и {!! !!} - то же самое, что <?=  ?>, поэтому в них можно использовать любые php функции: {{ time() }}

	Все что получили от пользователей показываем в верстке только через {{ }}

	Имеем ввиду, что {{ }} и {!! !!} выполняют команду echo. Поэтому если в blade делаем операции php, то используем <?php ?>, 
	
111) @ - отключение шаблонизатора blade
	 
	Примеры:
		Hello, @{{ name }} - здесь {{ name }} - вставка js фреймворка, также использующего фигурные скобки для вывода
		@@if()  @@endif; - отмена директивы условия, выведется как: @if() @endif
	
     Директивы:
	@if, @elseif, @else, и @endif - условие
	@unless - если не (то же, что @if(! ... ))
	@isset - isset() - переменная определена и не равна null
	@empty - empty() - переменная не пустая
	@auth - пользователь аутентифицирован
	@auth('admin') - пользователь аутентифицирован как админ	
	@guest - не аутентифицирован
	@production - содержимое покажется только в боевом режиме (APP_ENV=production)
	@env('staging') - либо по названию режима в APP_ENV
	@session('status') {{ $value }} @endsession - вывод значения сессии если оно существуета
	@switch, @case, @break, @default и @endswitch - функция switch
	@for ($i = 0; $i < 10; $i++) ... @endfor - цикл
	@foreach ($users as $user) ... @endforeach - цикл
	@forelse ... @empty ... @endforelse - цикл с проверкой значения на пустоту
	@continue - пропустить интерацию
	@break - прервать цикл
	$loop - переменная внутри цикла, можно отслеживать:
		 $loop->first - первая итерация,
		 $loop->last - последняя итерация, 
		 $loop->index - номер итерации,
		 $loop->even - четная и пр.
	@class - добавление класса по условию (<span @class(['font-bold' => $isActive]) </span>)
	@checked, @selected, @readonly, @disabled, @required - для вставки соотв. свойств
	@include - подключение вьюхи, она будет иметь доступ к переменным родительской
	@include('page', ['status' => 'complete']) - подключение вьюхи и проброс туда переменных
	@includeWhen($var, 'page') - подключить если $var = true
	@includeUnless($var, 'page') - подключить если $var = false
	@includeIf('view.name') - подключить, если вьюха существует
	Внимание! не подключаем через __DIR__ и __FILE__ - потому что они ссылаются на кешированную вьюху
	@php - вставить php код
	@csrf - вставка в форму скрытого токена CSRF
	@method('PUT') - вставка в форму метода, если используем PUT, PATCH или DELETE
	@error('название инпута') - вставка значения при редиректе обратно на форму при ошибке валидации (компилируется в if):
		1) @error('title') <div>{{ $message }}</div>  @enderror - показ установленного сообщения об ошибке
		2) <input name="title" class="@error('title') is-invalid @enderror"> - установка класса ошибочно заполненному инпуту
		3) <input id="email" class="@error('email') is-invalid @else is-valid @enderror"> - то же с @else
	@error('email', 'login') - можно устаналивать разные ошибки, напр. если несколько форм
	@error "ошибка" @else "нет ошибки" @enderror - текст на случай если ошибки нет

     Макетирование:	
	@section('content') <p>Верстка</p>  @endsection - запись в переменную
	@section('title', 'Page Title') - короткая запись в переменную
	@section('content') <p>Верстка</p> @show - запись и немедленный показ:
		1) В главном шаблоне будет показано, если дочерний не предоставит @section('content')
		2) В дочернем шаблоне будет показано всегда, даже без @yield('content') на главном
	@yield('content') - показ содержимого переменной, можно использовать как в главном шаблоне, так и в дочернем
	@yield('content', 'значение по умолчанию') - можно назначать значение по умолчанию
	@extends - пишем в дочернем, указываем какой главный шаблон использовать

    	return Blade::render('<h1>{{ $name}}</h1>', ['name' => 'Денис']) - возврат html из контроллера без файла вьюхи

      Стеки - в них можно помещать стили, скрипты, верстку и пр. сколько угодно раз (даже в 1 стек), а потом выводить за 1 раз	
	@push('scripts') <script>alert(111)</script> @endpush - помещение в стек первый раз
	@push('scripts') <script src="/example.js"></script> @endpush - помещение в стек второй раз
	@prepend('scripts')  <script src="/example1.js"></script> @endprepend - помещение в начало стека

	@stack('scripts') - размещение стека, например в head

      Собственные директивы для bгlade:
	1) Регистрируем в AppServiceProvider в boot
		Blade::directive('mydir', function($var){return pow($var, 7)})
	2) Обновляем кеш вьюх php artisan view:clear
	4) Используем: @mydir(3)
	
111) Генерация url
	url('/post') - создает полный url: http://start.loc/post
	url()->current() - возвращает текущий url без query параметров: http://start.loc
	url()->full() - полный url с гет параметрами: http://start.loc/?token=123
	url()->previous() - предыдущий url
	
	route('index') - ссылка на именованный маршрут
	route('index', ['post' => 1]) - с передачей параметров в маршрут, например: /index/{post} => /index/1
	route('index', ['post' => 1, 'comment' => 3]) - лишние параметры вставятся как гет параметры: /index/1?comment=3
	
	route('page', ['post' => $post]) - если передавать объект модели, то id извлечется автоматически: /mypage/11

	$url = action([HomeController::class, 'index'], ['post' => 1]) - то, же что route, но не нужен именованный маршрут
	
	URL::signedRoute('unsubscribe', ['user' => 15]) - создание подписанных ссылок. Как пример ссылка для отписывания аккаунта, 			        высланная на почту: http://start.loc/unsubscribe/15?signature=2546e63dbe9eb05efecd530d7ea0e571d163db49b28
		        Ларавель проверит, что ссылка не изменена, чтобы намеренно не отписали другого пользователя
			https://site.ru?user=15&signature=01b9e37a17c31fad750f6556c

	URL::temporarySignedRoute('unsubscribe', now()->addMinutes(20), ['user' => 15]) - подписанная ссылка действующая 20 минут
			https://site.ru?expires=1712569080&signature=01b9e37a17c31fad750f6556c

	Затем проверить подпись:
		1) if (! $request->hasValidSignature()){abort(401);} - методом hasValidSignature()
		2) Route::post('/unsubscribe/{user}', $callback)->name('unsubscribe')->middleware('signed') - встроенным посредником signed

111) url()->current() - возвращает текущий URL-адрес страницы, на которой выполняется код
     request()->url() возвращает URL-адрес текущего запроса. GPT сказал, что это одно и то же, за исключением положения, когда
                      запрос был выполнен через AJAX: request()->url() вернет URL-адрес, который был запрошен через AJAX

111) $this->route('comment') - так можно прочитать именованный параметр в маршруте Route::post('/comment/{comment}');

111) Сессии
	Файлы сессий хранятся в соответствии с настройками в config/session.php	
	По умолчанию переменные сессии хранятся в файлах storage/framework/sessions
	Возможные варианты: в файле(по умолчанию), в куки, в БД, memcached / redis, dynamodb, array

     Работа с сессиями:
	1) $request->session()->get('key', 'по_умолчанию');
	2) session('key')

     Рассмотрим второй вариант:
	session('key') - получить переменную по ключу
	session('key', 'default') - получить по ключу или использовать значение по умолчанию	
	session(['key' => 123]) - установить переменную сессии
	session()->put('key', 123) - установить переменную сессии
	session()->push('arr', 111) - добавить в массив сессии arr новое значение
	session()->all() - получить все переменные сессии
	session()->has('key') - проверить, что переменная существует и не равна null
	session()->exists('key') - переменная существует, может быть равна null

	session()->pull('key', 'default') - показать и удалить
	session()->increment('count') - увеличение на 1
	session()->decrement('count') - уменьшение на 1
	
	session()->flash('success', 'Успешно') - запись во флеш, доступно только при одном след http запросе
	session()->reflash() - сохранит флэш данные для еще одного запроса

	session()->forget('name') - удалить переменную
	session()->forget(['key1', 'key2']) - удалить несколько
	session()->flush() - удалить все
	 
     Для предотвращения такой атаки как фиксация сессии стартовые наборы Breeze и Jetstream автоматически 
     пересоздают идентификатор сессии во время аутентификации. Также можно самостоятельно пересоздать:
	session()->regenerate() // новый _token в куках, на его основе новый файл сессии в storage/framework/sessions с новым именем, 
                                   но данные остаются с прошлой сессии    
	session()->invalidate() // то же, но данные прошлой сессии удаляются

111) Валидация

	Сама страница:

	<form action="" method="post">
	  @csrf
	  <input name="name" value="{{ old('name') }}"><span>@error('name') {{ $message }} @enderror</span>
          <input type="submit">
	</form>

	Пишем в методе сохранения записи в БД:

	public function store(Request $request) { //метод сохранения
    		$request->validate([ // валидация, + можно записать проверенные поля в объект: $validated = $request->validate([...])
        		'title' => 'required|unique:posts|max:255',
        		'body' => 'required']); //если ошибка, то редирект  + запись ошибок в сессию, к-рые читаются через $errors
    		$user = Student::query()->create($request->all());}

        Способы работы с ошибками:
	  1) 	
	    @if ($errors->any())
    	      <div class="alert alert-danger">
                <ul>
               	  @foreach ($errors->all() as $error)
                   <li>{{ $error }}</li>
               	  @endforeach
                </ul>
              </div>
            @endif
	
	2) <input name="name" class="@error('title') is-invalid @enderror">

	3) <div> @error('title') {{ $message }}  @enderror </div>

     Методы объекта $errors:
	$errors->any() - есть хотя бы одна ошибка
		  ->first('email') - первая ошибка для указанного поля
		  ->all() - все ошибки, можно перебирать в цикле
		  ->has('email') - есть ли ошибка у поля
	
      Правка ошибок:
      	1) Текст ошибок лежит в lang/en/validation.php - php artisan lang:publish // правим в основном массиве, либо в custom 
      	   Также можем создать каталог ru/validation.php c ошибками на русском, вроде язык нужно переключить в .env
	2) В этом же файле есть массив custom, там пишем свои правила и их названия
	3) Там же есть массив attributes, в нем можно указывать на что заменяется слово :attribute в правилах

      Внимание! Если форма была отправлена через аякс, то Ларавель не перенаправляет обратно, а отправляет json с ошибками

      Если произошли ошибки, Ларавель записывает инпуты во флеш сессию. Доступ к значениям полей:
	1) $title = $request->old('title') - в коде
	2) value="{{ old('title') }}" - в верстке

     Также можно создать собственный реквест для формы, чтобы не загромождать контроллер валидацией:
	php artisan make:request StoreRequest
	В authorize пишем return true - может ли текущий пользователь отправлять форму (напр. изменять комментарий), 
			                в случае чего запретить через false
	В rules пишем правила: return ['title' => 'required|unique:posts|max:255']
	В messages меняем сообщения об ошибках: 'title.required' => 'A title is required',
	В attributes меняем заполнитель :attribute:  'email' => 'email address',
	В контроллере встраиваем зависимость: public function store(StoreRequest $request), валидация будет срабатывать автоматически 
        до вызова метода контроллера

     Второй способ с созданием своего реквеста имеет преимущества, мы можем указать в классе StoreRequest свой адрес перенаправления 
     при ошибке:
	protected $redirect = '/dashboard' - на страницу
	protected $redirectRoute = 'dashboard' - на именованный роут
     Также можем указать свои сообщения об ошибках:
	public function messages(){return ['title.required' => 'A title is required']}

    Есть третий способ с созданием своего валидатора:
	$validator = Validator::make($request->all(), [    // то же самое validator()->make($request->all(), [$rules])
            'title' => 'required|unique:posts|max:255',
            'body' => 'required']);

	if ($validator->fails()){
	    return redirect()->route('...')
		->with('error', 'Ошибка заполнения') // всплывающее окошко
		->withErrors($validator) // ошибки заполнения
		->withInput();}

    Правила валидации public $rules  принято писать в модели, а в контроллер инъецировать в модель CaseItem $item 
    и встраивать: $item->rules	
	
     Некоторые правила валидации:
	regex - соответствие регулярному выражению, например: 'name' => 'regex:/^[a-zA-Z]+$/i|max:255',
	not_regex - не должно соответствовать регулярному выражению 
	bail - не проверять следующие правила ЭТОГО поля, как только была хоть одна ошибка.  
	       'bail|unique:posts|max:255' - если правило unique не будет пройдено, то правило max не будет выполняться
	nullable - поле может быть пустым (превратится в null, т.к. к null приводятся все пустые поля за счет посредников TrimStrings и ConvertEmptyStringsToNull)
	accepted - дб "yes", "on", 1, или true, применяется для полей по типу принятия условий
	after:дата - дата должна быть после указанной, напр. after:tomorrow. Обрабатывается php функцией strtotime.
		     echo strtotime('2025-12-31') => 1767128400
	before:date - то же, но до указанной даты
	alpha - только буквы (лат. и кирилл.)
	alpha_dash - буквы, цифры, тире, ниж. подч.
	alpha_num - буквы и цифры
	array - массив php
		"files" => "array|min:3" - массив минимум из 3 элементов
		"files.*"  => "string|distinct|min:3" - элементы массива д.б. неповторяющимися строками мин. из 3 символов
		'files.*' => 'nullable|file|max:10240' - элементы массива файлы не более 10 Мб
	boolean - можно преобразовать в true, false, 1, 0, "1", и "0"
	confirmed - должно совпадать с полем поле_confirmation, например с password_confirmation
	current_password - должно совпадать с паролем аутентифицированного пользователя
	date - дата
	declined - "no", "off", 0, или false
	different:field - отличается от поля field
	digits_between:min,max - число между
	dimensions - размеры изображения, напр. dimensions:min_width=100,min_height=200 (min_width, max_width, min_height, max_height, width, height)
						'image' => 'file|max:200' -  максимальный размер файла 512Кб
	email - email, стиль валидации можно менять через настройки, есть в док-ции
	exists:table,column - должно существовать в указанной таблице базы данных
	extensions:jpg,png - расширение
	mimes:jpg,bmp,png - mime тип файла (файл читается и угадывается его тип. Расширение файла не имеет значения)
	file - успешно загруженный на сервер файл
	filled - не должно быть пустым
	image - файл jpg, jpeg, png, bmp, gif, svg или webp
	integer - целое число (из инпутов type number правильно воспринимает, из др. не знаю)
	in - поле должно быть заполнено одним из значений: in:a,b (также: Rule::in(['a', 'b','c']))
	numeric - целое/дробное число или строка содержащая такое число (основано на is_numeric)
	json - строка json
	max:value - максимальное значение (у строк длина, у цифр значение, у массивов count, 'file|max:200' - файлы не более 200Кб)
		    для double(8 - это макс. общее кол. цифр, 2 - кол. цифр после зап.) ставим max:999999  (т.е. 6 цифр и 2 после зап.)
		    Также есть спец. класс для валидации файлов, описан ниже
	min:5 - то же самое, что у max и size (у строк длина, у цифр значение, у массивов count, у файлов размер в кб)
	required - обязательное значение не равное пустой строке, null, пустому массиву
	required_without_all:alt,title - поле должно быть заполнено, если перечисленные поля пусты
	RequiredIf - д.б. заполнено если функция вернет true, пример: 'field' => new RequiredIf($request->type == 'template')
	size:value - точно равно (у строк длина, у цифр значение, у массивов count)
	string - строка
	sometimes - проверять только если поле есть в $request->all()
	unique:table,column - не должно существовать в таблице, напр. 'email' => 'unique:users,email_address'
	url - действительный url
	uuid - действительный уникальный идентификатор (UUID)
	Password::min(8) - встроенный фасад для проверки паролей, принимает цепочки методов:
		Password::min(8) - минимум 8 символов
		Password::min(8)->letters() - и хотя бы одна буква
		Password::min(8)->mixedCase() - и хотя бы одна строчная + заглавная буква
		Password::min(8)->numbers() - хотя бы одна цифра
		Password::min(8)->symbols() - хотя бы один символ
		Password::min(8)->uncompromised() - пароль не скомпрометирован по сайту https://haveibeenpwned.com/
		Password::defaults() - свои правила по умолчанию для паролей, можно настроить 
	File::image()->min('1kb')->max('10mb') - встроенный фасад для проверки размера одного файла
	File::types(['mp3', 'wav'])->max(12 * 1024) - фасад по mime типу файла

     Внимание, если используем CKFinder, то нельзя валидировать инпуты с файлом, потому что CKFinder сам загружает файл, 
     минуя Ларавель. Можно только на required путь проверять из input type="text"
   
     Внимание! Если у нас есть правило на уникальность слага и мы обновляем, например, title этой записи, то при проверке на уникальность слага будет 
     конфликт с этой же записью лежащей в базе (так как слаг одинаковый). Для того, чтобы при обновлении не было этого конфликта:
	$request->validate([ 
            'slug' => "unique:pages,slug,{$id}"]); - пишем в двойных кавычках, pages - таблица, slug - поле, $id - id записи для которой 
						     не проверять слаг на уникальность. Этот id уже встроен в public function update(Request $request, $id)
     Валидация при наличии SoftDelete:
     Внимание! Если какая-то модель мягко удалена, и мы вставляем, например, другую модель с таким же слагом, а на валидаторе
     установлена проверка уникальности слага, то будет конфликт. То есть валидатор проверяет уникальность для всех полей,
     включая мягко удаленные. Чтобы поля мягко-удаленных записей не влияли на валидирование уникальности новых записей:
	public function store(Request $request){$request->validate(['unique:pages,uri,NULL,id,deleted_at,NULL'])}, где:
		pages - таблица, uri - проверяемое поле. Остальное оставляем как есть. 
	public function update(Request $request, $id){$request->validate[('unique:pages,uri,{$id},id,deleted_at,NULL')]}

    Внимание! Будет ошибка постоянная и мозгоебство, если в самой таблице стоит уникальный ключ на поле слага. То есть 
    валидатор теперь пропустит, а MySQL не позволит вставить две записи с одинаковым слагом, даже если одна мягко удалена.
    В этом случае либо снимаем уникальный ключ, либо насовсем удаляем запись, чтобы слаг мягко-удаленной записи не влиял на
    будущую работу, либо ставим includeTrashed:  'uri' => ['source' => 'meta_title', 'includeTrashed' => true]

    Метод слагабл игнорирует поля удаленных записей и не приписывает цифры новой записи, если в таблице уже есть 
    подобная мягко-удаленная запись. То есть модели с обратимым удалением не учитываются при проверке на уникальность.
    Можно учитывать: includeTrashed поставить true

111) Не нужно бояться, что пользователь пришлет слишком много файлов и Ларавель подписнет, так как php автоматически ограничивает размеры
     в переменных upload_max_filesize и post_max_size (вроде обычно в 50 мб)

111) Насколько понял, Ларавель проверяет только размер одного файла, но их может быть несколько. 
     Проверка общего размера нескольких файлов:

     if($request->hasFile('files')) {
      	$totalSize = 0;
      	foreach($request->file('files') as $file) {
        $totalSize += $file->getSize();}
      if($totalSize > 20971520) { // 20 MB in bytes
        return redirect()->back()->withErrors(['files' => 'Общий размер файлов не более 20 МБ']);}}
     			
111) Ларавель не дает изменять созданные поля в БД через миграции.
     Для того, чтобы, например, добавить атрибут существующему полю нужно установить пакет dbal:
	https://www.code.mu/ru/php/framework/laravel/book/prime/database/migrations/preparing-fields-changing/

111) Обработка ошибок
	Показ ошибок на время разработки: .env => APP_DEBUG=true . Константа используется в config => app.php => debug
	На боевом проекте отключаем. 
	Это подробный отчет об ошибке, который обычно вижу. Не путать со страницами 404, 500 и др.

     abort(код) - установка ответа на http запрос, напр.: abort(404) - вернет 404 страницу

     Можно установить свои страницы ошибок(404, 500, 301 и др), отличные от стандартных Ларавель:
	php artisan vendor:publish --tag=laravel-errors  // будут в resources/views/errors/

     Также можно можно создать 4xx.blade.php и 5xx.blade.php, которые будут запускаться если нет страницы для этого номера.

111) Логирование
	Логи пишутся в зависимости от выбранного канала в config/logging.php
	По умолчанию исп канал stack - указывает несколько каналов, которые пишутся в один журнал логов

     Если в .env сделать LOG_CHANNEL=daily, то будет создаваться файл для каждого дня
	
     Log::debug('Здесь пишем ошибку') - этот фасад одновременно записывает ошибку в логах + она показывается в дебагбаре
                                        Пример: Log::debug('Ошибка')  => [2023-01-06 02:42:13] local.DEBUG: Ошибка 
     Уровни ошибок: emergency, alert, critical, error, warning, notice, info и debug

     В лог можно передавать переменные:
	Log::info('Не найден', ['id' => $user->id]) => [2023-01-06 02:50:43] local.INFO: Не найден {"id":12} 
	
     Можно использовать канал null, тогда сообщения не пишутся, но дебагбар их показывает:
	Log::channel('null')->debug([1,2,3]);	

     Сделал эммет L => Log::info();

111) Один из способов добавить функции-помощники в Ларавель:
	1) app/Helpers/helpers.php - создаем файл
	2) "autoload": { 
		"files": [  // добавляем в composer.json
			"app/Helpers/helpers.php"],
	3) composer dump-autoload - в консоли
	4) Чтобы защититься от существования функций пишем их с проверкой:
	    if(!function_exists('func')) { function func(){ ... } }

     Более чистый способ через провайдер: https://badtry.net/sozdaniie-sobstviennyi-funktsii-khielpierov-v-laravel/

111) Команды artisan
	php artisan list -список всех команд
	php artisan help migrate - help перед командой позволяет показать все флаги команды
	
     Можно вызывать команды артизан из кода:
	Artisan::call('optimize:clear')  = php artisan optimize:clear - чистит весь кеш
	Artisan::call('send Denis') - с передачей параметров в виде обычной команды (то же php artisan send Denis)
	Artisan::call('send', ['user' => $user]) - с передачей параметров

111) Создание команд консоли:
	1) Отдельным классом:
		php artisan make:command SendEmails // появился файл команды в app/Console/Commands 
		Пишем в нем название команды: protected $signature = 'send' // будет запускаться по php artisan send
		В методе handle пишем саму команду: echo date('d-m-Y');
		Если в команде принимали аргументы ($signature = send {user}), то в handle они доступны так: $this->argument('user')

	2) В файле routes/console.php:
		Artisan::command('mail:send {user}', function (string $user) { // будет запускаться по php artisan mail:send Denis
    			echo "Sending email to: {$user}!";});

	Аргументы команд:
		mail:send {user} - обязательный аргумент
		mail:send {user?} - необязательный
		mail:send {user=foo} - необязательный с заданным по умолчанию

	Вопросы появляющиеся в консоли при выполнении команды handle:
		$name = $this->ask('What is your name?', 'Taylor') - вопрос и значение по умолчанию
		$password = $this->secret('What is the password?') - невидимый ввод при ответе на вопрос
		if ($this->confirm('Do you wish to continue?', true)) - запрос подтверждения со значением по умолчанию
		$name = $this->choice('What is your name?',['Taylor', 'Dayle']) - вопросы с выбором

	Вывод текста в консоли:
		$this->info('Успешно!') - зеленым цветом
		$this->error('Ошибка!') - красным цветом
		$this->line('Просто текст') - белым цветом
		echo 112 - белым цветом
	
111) Можно редактировать заготовки классов, создающихся через  make (напр. php artisan make:model User):
	1) php artisan stub:publish - публикуем заготовки
	2) Редактируем заготовки в папке stubs корня

111) Кеширование
	Кеш - данные для выдачи которых Ларавель затрачивает относительно больше времени. Например: данные из БД, трудоемкие расчеты и пр.
              Это не кеш вьюх, а другой кеш для переменных.
	Настройка хранения кеша:
		1) в .env: CACHE_DRIVER=file
		1) либо в config/cache.php . 
	По умолчанию используется драйвер file, кеш хранится в файле storage/framework/cache/data

	Ларавель позволяет хранить кеш: в файле, БД, Memcached, Redis, в БД, DynamoDB

        Для хранения переменных кеша в БД:
		1) php artisan cache:table
		2) php artisan migrate
		3) CACHE_DRIVER=database
		4) Теперь переменные кеша хранятся в БД в таблице cache

	Переменные кеша удаляются при php artisan cache:clear

	Помощник Ларавель:
	cache('key') - получить
	cache(['key' => 'value'], $seconds) - поместить
	
	Либо через фасад:
	Cache::put('key', 'value') - поместить в кеш бессрочно (10 лет стоит в expiration)
	Cache::forever('key', 'value') - тоже бессрочно (10 лет)
	Cache::put('key', 'value', 100) - поместить на 100 секунд
	Cache::put('key', 'value', now()->addMinutes(10)) - на 10 минут
	Cache::add('key', 'value', $seconds) - поместить в кеш только если ключ не существует в нем
	Cache::get('key') - получить кеш по ключу
	Cache::get('key', 'default') - default вернется, если переменной в кеше нет
	Cache::get('key', function(){ return DB::table(...)->get(); }) - функция выполнится, если переменной в кеше нет
	Cache::has('key') - проверка наличия в кеше по ключу
	Cache::increment('key') - увеличение счетчика (также Cache::decrement('key'))
	Cache::store('redis')->put('bar', 'baz', 600) - кеширование в опр. хранилище
	Cache::pull('key') - возвратить и удалить из кеша
	Cache::forget('key') - удалить
	Cache::flush() - очистить весь кеш

	Охрененная штука:
	$value = Cache::remember('users', $seconds, function () { - если нет в кеше, то возвратить и добавить в кеш,  
    		 return DB::table('users')->get();});		    то есть без всяких проверок if(Cache::has('key'))
								    rememberForever - навсегда

	В Ларавель есть несколько видов кеша (events, views, cache, route, config, compiled).
        Для работы с этими видами исп. разные команды:
		php artisan cache:clear - удалить кеш переменных (кеш, описанный выше)
		Cache::flush() - очистить из кода кеш переменных 
		php artisan config:cache - удалить и заново создать кеш настроек (.env + настройки их папки config)
		php artisan route:cache - закешировать маршруты, исп. только на продакшене, 
					  иначе придется постоянно перекешировать (можно удалить: php artisan route:clear)
		php artisan optimize - закешировать роуты и конфиг
		php artisan optimize:clear (очистить кеш events, views, cache, route, config, compiled)
		php artisan view:clear - удалить кеш вьюх
		
111) Коллекции
	Коллекции Ларавель - это объекты-оболочки над массивами для упрощения работы с ними.
	
	Вместо стандартной работы php:
		array_unique(array_merge($array1,$array2)) // вложенных функций может быть много
	Ларавель упрощает:
		collect($array1)->merge($array2)->unique()

	Модели Eloquent возвращают из БД именно коллекции.

	В основном методы коллекций не изменяют исходную коллекцию, а возвращают новую.
	Обычным способом работы с коллекциями является создание цепочки методов

	Методы коллекций:
		$collect = collect([1, 2, 3]) - создать коллекцию из массива.

		all() - преобразовать коллекцию в php массив. $collect->all() => [1, 2, 3]
		avg() - среднее арифметическое значений коллекции. $collect([1,2,3])->avg() => 2 
							           collect(['key1' => 3, 'key2' => 5])->avg() => 4
		chunk(num) - разбить коллекцию по указанному размеру.
				collect([1, 2, 3, 4, 5, 6, 7])->chunk(4)->all() => [[1, 2, 3, 4], [5, 6, 7]]
		collapse() - сворачивает коллекцию массивов в плоскую коллекцию. Примерно как flat в js
				collect([[1,2], [3,4]])->collapse()->all() => [1,2,3,4]
		combine() - слияние двух массивов в один ассоциативный. Аналогично array_combine()
				collect(['a', 'b', 'c'])->combine([1,2,3])->all() => ["a" => 1, "b" => 2, "c" => 3] 
		concat() - слияние коллекций, аналогично php функции array_merge(), но добавляются только значения, без ключей
				collect([1,2])->concat([3,4])->all() => [1,2,3,4] 
		contains() - содержит ли значение или ключ-значение. Примерно как in_array()
				collect([1,2,3])->contains(3) => true
				collect(['key' => 11])->contains('key', 11) => true
				collect([1,2,3])->contains(function($value, $key){return $value>4}) => false
		count() - количество элементов в коллекции, то же php count()
				collect([1,2,3])->count() => 3
		countBy() - подсчитывает количество вхождений элементов коллекции. То же array_count_values()
				collect([2,2,3])->countBy()->all() => ['2' => 2, '3' => 1]
		dd() - показ элементов коллекции и завершение скрипта
				collect([1,2,3])->dd() => [1,2,3]
		diff() - вернет значения первой коллекции, которых нет во второй. Примерно как array_diff()
				collect([1,2,3])->diff([1,4,5])->all() => ['1'=>2, '2'=>3]
		diffAssoc() - то же, но сравнение и по ключу и по значению
	
		diffKeys() - то же, но сравнение только по ключам

		dump() - тот же ->dd(), но скрипт не завершается

		duplicates() - вернет повторяющиеся значения
				collect([1,2,3,3,3])->duplicates()->all() => ['3' => 3, '4' => 3]
		each() - перебор коллекции наподобие foreach в js
				collect([1,2,3])->each(function ($item, $key){...})
		every() - аналогично every в js, проверка, что все элементы удовлетворяют замыканию
				collect([1, 2, 3])->every(function ($value, $key){ return $value > 2; })
		except() - вернет все элементы коллекции, кроме указанных ключей
				collect(['a'=>1, 'b'=>2, 'c'=>3])->except(['a'])->all() => ['b'=>2, 'c'=>3]
		filter() - фильтрует коллекцию по замыканию
				collect([1,2,3])->filter(function ($value, $key){return $value > 2;})->all() => [3]
		first() - первый элемент коллекции или первый, который удовлетворяет замыканию
				collect([1,2,3])->first() => 1
		firstOrFail() - то же, но если элемента нет, то выбросится исключение

		flip() - меняет местами ключи и значения аналогично array_flip()
				collect(['a'=>1, 'b'=>2])->flip()->all() => ['1' => 'a', '2' => 'b']
		forget('key') - удалить элемент из коллекции по ключу, меняет массив
				$collection = collect([1,2,3]);
				$collection->forget('0');
				dd($collection->all()) => ['1' => 2, '2' => 3]
		forPage(номер_страницы, кол-во_эл_на_страницах) - возвращает элементы, которые будут на указанном номере 
                               страницы
				collect([1,2,3,4,5])->forPage(2, 3)->all() => [4, 5]
		get('key') - вернет элемент по ключу
				collect([1,2,3])->get('0') => 1
		groupBy('key') - группирует коллекцию по указанному ключу

		has() - существует ли ключ в коллекции
				collect(['a'=>1, 'b'=>2])->has('a') => true

		intersect() - удаляет элементы в исходной коллекции, которых нет в переданной
	
		implode('-') - объединяет элементы коллекции в строку. В отличие от join() больше настроек, напр. возможность колбэка
				collect([1,2,3])->implode('-') => '1-2-3'
		
		isEmpty() - проверка на пустоту коллекции (обратный метод isNotEmpty)
				collect([])->isEmpty() => true
		join() - наподобие join в js, превращает в строку с указанным разделителем
				collect([1,2,3])->join('') => '123'
		keys() - вернет все ключи		
	    			collect(['a'=>1, 'b'=>2])->keys()->all() => ['a', 'b']
		last() - последний элемент коллекции
				collect([1,2,3])->last() => 3

		lazy() - превращает коллекцию в LazyCollection, полезно когда работаем с огромной коллекцией и неск методами
			         collect([1, 2, 3, 4])->lazy() - работать с коллекцией как с LazyCollection
						      ->filter()->where() // здесь не будут создаваться копии исходной коллекции
						      Метод экономит память на больших коллекциях, но увеличивает время

		map() - перебор коллекции с выполнением замыкания
    				collect([1,2,3])->map(function ($item, $key){return $item * 2;})->all() => [2,4,6]
		mapToGroups() - группирует элементы по замыканию

		max() - макс. значение
				collect([1,2,3])->max() => 3
				collect([['foo' => 10],['foo' => 20]])->max('foo') => 20
		merge() - слияние коллекций, но вместе с ключами, в отличие от concat. Перезаписывает совпадающие ключи
				collect(['a'=>1])->merge(['b'=>1])->all() => ['a'=>1, 'b'=>1]
		min() - мин. значение
				collect([1,2,3])->min() => 1
				collect([['foo' => 10],['foo' => 20]])->min('foo') => 10
		nth() - новая коллекция из каждого nth элемента коллекции
				collect(['a', 'b', 'c', 'd', 'e', 'f'])->nth(4)->all() => ['a', 'e']
		only() - вернет элементы коллекции только с указанными ключами
				collect(['a' => 1, 'b' => 2, 'c' => 3])->only(['a', 'b'])->all() => ['a' => 1, 'b' => 2]
		pad() - дополнит коллекцию до указанного размера, аналогично array_pad
				collect([1])->pad(3, 0)->all() => [1,0,0]
		percentage() - определяет процент элементов в коллекции, которые проходят заданное условие:
				collect([1, 2, 3])->percentage(fn($value) => $value > 1) // 66.67 
		pipe() - передает коллекцию в замыкание и возвращает результат
				collect([1,2,3])->pipe(function ($coll){return $coll->sum();}) => 6
		pluck() - извлекает все значения по указанному ключу
				collect([['a'=>1, 'b'=>2],['a'=>3, 'b'=>4]])->pluck('a')->all() => [1,3]
				Student::all()->pluck('name') => ['Денис', 'Зоя', 'Вася']
     		pop() - удаляет и возвращает последний(е) элемент(ы) // можно указать количество удаляемых
				$collection = collect([1,2,3])
				$collection->pop()
				dd($collection) => [1,2]
		prepend() - добавляет элемент в начало коллекции
				collect([1,2,3])->prepend(0)->all() => [0,1,2,3]
		push() - добавить элемент в конец
				collect([1,2,3])->push(4)->all() => [0,1,2,4]
		put() - помещает ключ-значение в коллекцию
				collect([1,2,3])->put('a', 4)->all() => [0,1,2,4, 'a'=>4]
		random() - случайный элемент из коллекции
				collect([1,2,3])->random() => 1
		range() - возвращает коллекцию с числами от и до
				collect()->range(3, 6)->all() => [3, 4, 5, 6]
		reverse() - перевернуть коллекцию
				collect([1,2,3])->reverse()->all() => [3,2,1]
		search() - поиск значения в коллекции
				collect([1,2,3])->search(2) => 1 (вернулся ключ)
		select() - выбрать значения по ключам подобно SELECT по полям в MySql
			
		shift() - удаляет и возвращает первый(е) элемент(ы) // можно указать количество удаляемых
				$collection = collect([1,2,3])
				$collection->->shift()
				dd($collection) => [2,3]
		shuffle() - перемешивает коллекцию
				collect([1,2,3])->shuffle()->all() => [2,1,3]
		skip() - пропустить элементы в начале и создать коллекцию из остатка
				collect([1,2,3,4])->skip(2)->all() => [3,4]
		slice() - новая коллекция начиная с указанного индекса
				collect([1,2,3,4])->slice(1)->all() => [2,3,4]
		sort() - сортирует коллекцию
				collect([4,2,3,1])->sort()->values()->all() => [1,2,3,4]
		sortBy('key') - сортирует по указанному ключу + есть много доп методов сортировки

		split() - разбиение коллекции на указанное число групп
				collect([1, 2, 3, 4, 5])->split(2)->all() => [[1,2,3], [4,5]]
		sum() - сумма всех элементов
				collect([1,2,3])->sum() => 6
		take() - взять столько элементов и создать новую коллекцию. Если передать отр. число, то берется с конца
				collect([1, 2, 3, 4, 5])->take(3)->all() => [1,2,3]
		times() - прогонит коллекцию через замыкание указанное число раз

		toArray() - привести к массиву. Работает как all, но приводит к массивам все, включая внутренние модели.
				$user = User::all() => получили из БД коллекцию массивов
				$users->all() - массив коллекций
				$users->toArray() - массив массивов
		toJson() - превратить коллекцию в json
				collect([1,2,3])->toJson() => "[1,2,3]"
		transform() - изменение элементов массива по замыканию, изменяет исходную коллекцию
				collect([1,2,3])->transform(function ($item){return $item*2;})->all() => [2,4,6]
				Если исходную коллекцию изменять не нужно, то используем map()
		map() - в отличие от transform не меняет исходную коллекцию, а возвращает новую

		unique() - вернет уникальные элементы коллекции
				collect([1,2,3,1])->unique()->all() - [1,2,3]
		unique('brand') - можно передавать ключ по которому подбирать уникальные значения

		values() - вернет массив значений, ключи сбросятся.
				collect(['a'=>1, 'b'=>2])->values()->all() => [1,2]
		where() - фильтрация по указанной паре ключ-значение
				collect([['a'=>0, 'b'=>1], ['a'=>2, 'b'=>3]])->where('a', 2)->all() => [[['a'=>2, 'b'=>3]]]
				
				where работает также как в eloquent метод where, но для коллекций.
				$products = Product::all() - получили все продукты
				$products->where('type', 'case') - получили продукты у которых type=case
				$products->where('price', '>', 500)->count() - число моделей у которых price > 500
		whereBetween(), whereIn() - работает подобно как в eloquent
		
	Ленивые коллекции LazyCollection используются, когда нужно обработать большие массивы данных при наименьшем потреблении памяти
		$users = User::all()->filter(function ($user) { // если записей миллион, то они загрузятся в память за раз
    			return $user->id > 500;}); 
	Можно создавать в памяти по одной модели через cursor(), при этом записи будут получены из БД за 1 запрос и сохранены в PDO:
		$users = User::cursor()->filter(function ($user) { - при cursor() в $users хранится объект LazyCollection
    			return $user->id > 500;});
	В этом случае функция фильтра будет срабатывать только когда нам понадобится модель:
		foreach ($users as $user) { так как в cursor() получили LazyCollection, то цикл будет создавать в памяти по одной модели
    			echo $user->id;}    Запрос к БД по-прежнему один.

       Пример: foreach(User::get() as $user) - если записей миллион, то в память загрузится миллион записей и для каждой создастся модель
               foreach(User::cursor() as $user) - cursor позволяет держать в памяти только одну текущую обрабатываемую модель, 
						  остальные хранятся в памяти как простые записи в PDO

111) События и слушатели.
     Например, нужно как только пользователь отправил заказ (нажал submit в форме) показать уведомление в телеграмме о новом
     заказе. 
	Алгоритм:
	  1) php artisan make:event OrderEvent - создаем событие	
	  2) php artisan make:listener TelegramListener --event=OrderEvent - создаем слушателя
	  3) В папке app появилась папка Listeners и Events
	  4) Регистрируем событие и слушателя в Providers/EventServiceProvider
		protected $listen = [
        		OrderEvent::class => [
           		 	TelegramListener::class,
				Другой слушатель и т.д.],];
	  4) В контроллере, к примеру, получаем пользователя и испускаем событие:
		$user = User::find(1); // не заморачивался, просто передам пользователя
		event(new OrderEvent($user)); // создаем объект события, передаем в него пользователя (либо OrderEvent::dispatch($user))
	  5) Прописываем в классе OrderEvent логику создания объекта события:
		class OrderEvent{
    			public $user; // здесь будем хранить пользователя
    			public function __construct(User $user){ // в конструкторе принимаем пользователя
        			$this->user = $user;}} // сохраняем его
	  6) В TelegramListener пишем вызываемые действия при событии:
			public function handle(OrderEvent $event){
        				dd($event);} // просто показал полученную переменную $user

    Теперь можно в любом месте испускать событие: event(new OrderEvent($user)), 
    его автоматически подхватит TelegramListener и отправит уведомление в тг. 
    Одно событие может слушать несколько слушателей.

111) Файловое хранилище
    	config/filesystems.php - настройки дисков
	
	Команды загрузки: 
		Storage::disk('public')->put('example.txt', 'Contents'); // грузим на диск public
		Storage::put('avatars/1.txt', 111) // грузим на диск по умолчанию. Создастся папка avatars с файлом 1.txt
   
	По умолчанию используется диск local, файлы загружаются в storage/app. Файлы здесь недоступны из интернета.

	Можно использовать диск public. Файлы теперь грузятся в storage/app/public - но для этой папки можно сделать
        символическую ссылку в корневой public: 
		php artisan storage:link - создать
		php artisan storage:unlink - удалить
	Теперь в папке public появилась папка storage(ссылка), где лежит общедоступный файл example.txt
	
	Показать путь к этому файлу: echo asset('storage/example.txt') => http://start.loc/storage/example.txt

	Вместо того, чтобы каждый раз прописывать в asset('storage/...) можно прописать в .env:
		ASSET_URL=http://start.loc/storage    // vite использует ASSET_URL, начнет ебать мозг при npm run build, лучше не менять
	Теперь путь будет выглядеть так: <img src="{{ asset('1.jpeg') }}" alt="">
	Помощник asset() дополняет путь до абсолютного с именем сайта в соотв. с ASSET_URL или APP_URL

	Как видим мы храним файлы в storage/app/public, на нее сделали символическую ссылку в public и путь к файлу
        получается: asset('storage/example.txt')  =>  http://start.loc/storage/example.txt.
	Алгоритм работы есть ниже в "Алгоритм на примере создания папки files"

	Также мы можем сделать новую символическую ссылку:
		1) Создадим папку storage/app/images  // лежит рядом с public
		2) в файле filesystems.php дополним массив links:
			public_path('images') => storage_path('app/images'),
		3) php artisan config:clear
		4) php artisan storage:link
		5) Теперь в корневой public лежит папка-ссылка images ведущая на storage/app/images
                   и путь к ее файлам выглядит как: asset('images/1.jpeg') => http://start.loc/images/1.jpeg
		6) Но как грузить в нее через Storage::disk нужно подумать. Наверно правильнее создавать папку images в 
		   storage/app/public/images, соответственно ссылка будет asset('storage/images/файл')
		   Теперь достаточно сделать одну встроенную линковку php artisan storage:link.

		Порядок на примере создания папки files:
			1) Создаем папку storage/app/public/files
			2) Линкуем storage/app/public  в корень сайта php artisan storage:link
			3) Прописываем в .env для удобства: ASSET_URL=http://start.loc/storage
			4) Грузим файл: Storage::disk('public')->put('/files/1.txt', 'текст');
			5) Теперь путь: asset('/files/1.txt'),  а адрес: http://start.loc/storage/files/1.txt

111) Методы работы с  файлами:
	Файлы ищутся в соответствии с переменной FILESYSTEM_DISK (.env) 
        относительно root указанного в настройках диска в файле filesystems.php
	Например, если использую диск public, то его root: app/public
	Соответственно: 
		Storage::exists('file.jpg') - проверяет есть ли файл в storage/app/public/file.jpg
		Storage::delete('file.jpg') - удаляет storage/app/public/file.jpg

	$orders = Storage::json('orders.json') - извлечь и декодировать из json
	Storage::exists('file.jpg') - проверка существования файла
	Storage::disk('public')->exists('file.jpg') - можно указать другой диск
	$data = Storage::get('file.jpg') - получить файл (строковое содержимое)
	return Storage::download('1.jpeg') - отправить файл пользователю, в браузере появится окно "Сохранение"
	return Storage::download('file.jpg', $name, $headers) - то же, где $name - под каким названием отдать, $headers - заголовки
	Storage::url('/files/1.jpeg') - url адрес файла: http://start.loc/storage/files/1.jpeg
	Storage::size('file.jpg') - размер файла в байтах
	Storage::lastModified('1.jpeg') - метка времени timestamp последнего изменения файла
	Storage::mimeType('file.txt') - MIME-тип файла (text/plain)
	Storage::path('/files/1.jpeg') - путь к файлу на сервере: 
		C:\Users\Owner\Desktop\studing\openserver-sites\start.loc\storage\app/public\files/1.jpeg
	Storage::put('file.jpg', $data) - сохранение файла на диске, в $data передаем тело файла
		Если канал public, то сохранится в storage/app/public/file.jpg + если есть симв. ссылка, то доступ будет по:
		Storage::url('file.jpg') =>  http://start.loc/storage/file.jpg
	if(Storage::put('file.txt', 'строка')) - проверка удачной записи файла
	$path = Storage::putFile('photos', new File('/path/to/photo')) - потоковое сохранение файлов уменьшающее расход 
		памяти. Где: "photos" - папка куда грузим, "/path/to/photo" - откуда грузим
	$path = $request->file('avatar')->store('avatars') - то же самое, что Storage::putFile
	$path = Storage::putFileAs('photos', new File('/path/to/photo'), 'photo.jpg') - сохранить как, потоковый способ

	Тело файла, передаваемое во второй параметр сохранения можно формировать:
		1) new File('path/to/photo') - в параметре используется путь на сервере
		2) Http::get('https://site.ru/1.jpg')->body - исп. библиотека Газзл
		3) file_get_contents('https://site.ru/1.jpg')
	
	Пример загрузки и сохранения картинки из интернет:
	$image = Http::get('https://site.ru/1.jpg') - получение объекта файла через Guzzle
	$path = Storage::put("/photos/" . uniqid() .".jpg", $image->body()) - но через потоковое putFile и putFileAs не работает

	Пример потокового сохранения:
	   Роуты:
		Route::get('/', [MainController::class, 'index'])->name('main');
		Route::post('/', [MainController::class, 'store'])->name('store');
	   Верстка:
		<form action="{{route('store')}}" method="post" enctype="multipart/form-data">
        	    @csrf
        	    <input type="file" name="file">
        	    <input type="submit">
    		</form>
	   Контроллер:
		public function index() {
        		return view('index');} // показ формы

		public function store(Request $request) {
			if (!$request->hasFile('file')){ return redirect()->back(); } // если не было файла идем обратно, сам придумал
        		$file = $request->file('file');				         нужно посмотреть как делается правильно
			$originalName = $file->getClientOriginalName(); - получаем имя файла, небезопасно, читать ниже
        		$path = Storage::putFile('files', new File($file)); // сохраняем в папку files под авт. уник. id
	     Или то же: $path = $request->file('file')->store('files') // сохраняем в папке files под уникальным id							    
        		dd($path);} // files/iq55Uo5C49nAX1pMflXI8FhvKlG6WSiQYOCV8OvV
			Теперь сохраняем в БД $originalName и путь к сохраненному файлу $path
			В дальнейшем файл можно получить так: Storage::get($path);
			Насколько понял имена меняются, чтобы не было конфликтов одинаковых имен
			и чтобы злоумышленник не качал чужие файлы перебором имен

		Пишут, что функция $request->hasFile() вернет false, если файл больше 2мб, , не рабоатет с CKFinder
		Это настраивается в upload_max_filesize в php.ini

	Продолжение методов:
	Storage::prepend('file.log', 'Prepended Text') - дописать в начало файла
	Storage::append('file.log', 'Appended Text') - дописать в конец файла
	Storage::copy('old/file.jpg', 'new/file.jpg') - копирование файла
	Storage::move('old/file.jpg', 'new/file.jpg') - перемещение файла
	$path = $request->file('avatar')->store('avatars') - сохранение файла из инпута (name='avatar') в папке avatars 
							     под новым названием и получение его пути: files/iq55Uo5C49nAX1pMflXI8Fhv
	$path = $request->file('avatar')->storeAs('avatars', 'свое_имя') - то же, но сохраняем под своим именем
	Внимание, если под своим названием, то "Недопустимые символы Unicode будут автоматически удалены из путей к файлам"
	
	$request->file('avatar')->getClientOriginalName() - получить настоящее имя файла, небезопасно
	$request->file('avatar')->getClientOriginalExtension() - получить расширение, небезопасно
	$request->file('avatar')->hashName() - захешировать имя, безопасно => dSU8MktEafgcVJDgI4LaDjlryDeNH2vWskiaMm9b.txt
					       Так и сохраняем как оригинальное имя в БД, расхешировать т.п. не получится
	$request->file('avatar')->extension() - получить расширение файла, безопасно

	Транслитерация имени файла перед загрузкой // если CKFinder то он сам это делать может, описание в его папке
	(между словами ставится тире, русские слова преобразуются в транслит. Иначе Ларавель хеширует название, если store):
	  $data = $request->all();
	  if($request->hasFile('src')){
          	$file = $request->file('src')->getClientOriginalName(); // получение полного имени
          	$filename = pathinfo($file, PATHINFO_FILENAME); // имя без расширения
          	$extension = pathinfo($file, PATHINFO_EXTENSION); // только расширение
          	$translitName = Str::slug($filename, '-', 'ru') . '.' . $extension; // так делается, чтобы не убиралась точка слагом
          	$pathImg = $request->file('src')->storeAs('/images/other_goods_images', $translitName);
		$data['src'] = $pathImg;}
	  Post::create($data);	

	
	Storage::delete('file.jpg') - удаление файла
	Storage::delete(['file.jpg', 'file2.jpg']) - удаление нескольких файлов
	Storage::files('files') - вернет массив всех файлов указанного каталога
	Storage::allFiles('/') - массив файлов каталога и подкаталогов, в данном случае файлы папки 
				 storage/app/public и вложенной папки files
	Storage::directories('/') - массив подкаталогов, получилось: [ 0 => "files"]
	Storage::allDirectories('/') - массив папок и вложенных подпапок

	Storage::makeDirectory('photos') - создание папки, в данном случае storage/app/public/photos
	Storage::makeDirectory('photos/city') - создание папки и подпапки, storage/app/public/photos/city
	
	Storage::deleteDirectory('photos') - удаление папки и подпапок

111) Функция обновления записи с изображением:
	$data = $request->all();

      	if($request->hasFile('src')){
           $file = $request->file('src')->getClientOriginalName();
           $filename = pathinfo($file, PATHINFO_FILENAME);
           $extension = pathinfo($file, PATHINFO_EXTENSION);
           $translitName = Str::slug($filename, '-', 'ru') . '.' . $extension;
           $pathImg = $request->file('src')->storeAs('/images/goods_images', $translitName);
           $data['src'] = $pathImg;}

      	Good::findOrFail($id)->update($data);

111) Помощники
      Массивы:
	Arr:: - множество методов работы с массивами, например:
	Arr::query(['key1' => 1, 'key2' => 2]) - создание строки гет-параметров как у http_build_query(): "key1=1&key2=2"
	Arr::random($array) - случайное значание из массива
      Числа:
	Number:: - методы для работы с числами, например:	      
	Number::abbreviate(489939) - 490K 
	Number::clamp(105, min: 10, max: 100) - заданное число останется в заданном диапазоне => 100
	Number::fileSize(1024) - перевод байтов в КБ, МБ и пр.: 1 KB
	Number::percentage(10) - переписывает в проценты: 10%
	Number::spell(1024, 'ru') - пишет число прописью: "одна тысяча двадцать четыре"
      Пути:
	app_path() - полный путь к папке app: C:\Users\Owner\Desktop\studing\openserver-sites\start.loc\app
	base_path() - к корню сайта C:\Users\Owner\Desktop\studing\openserver-sites\start.loc
	config_path() - к папке config C:\Users\Owner\Desktop\studing\openserver-sites\start.loc\config
	database_path() - к папке database C:\Users\Owner\Desktop\studing\openserver-sites\start.loc\database
	public_path() - к папке public C:\Users\Owner\Desktop\studing\openserver-sites\start.loc\public
	resource_path() - к папке resource C:\Users\Owner\Desktop\studing\openserver-sites\start.loc\resources
	storage_path() - к папке storage C:\Users\Owner\Desktop\studing\openserver-sites\start.loc\storage
	
	__('passwords.reset') - если локаль en, то покажет перевод по ключу reset в файле: lang/en/passwords.php
	
	e() - функция htmlspecialchars. Пример: e('<script></script>') => &lt;script&gt;&lt;/script&gt;

       Строки
	preg_replace_array('/:[a-z]+/', ['10:00', '18:00'], 'Работаем с :a до :b') - последовательная замена из массива:
										     Итог: Работаем с 10:00 до 18:00
	Str::after('В лесу родилась елочка', 'В лесу ') - вернет текст начиная с 2 аргумента:
										     Итог: родилась елочка
	Str::before('В лесу родилась елочка', 'родилась елочка ') - вернет текст до 2 аргумента:	
										     Итог: В лесу
	Str::between('This is my name', 'This', 'name') - вернет часть строки между значениями
										     Итог: is my
	Str::ascii('û') - транслитерация в ascii => u
	Str::camel('get age') - преобразование в верблюжий стиль: getAge
	Str::finish('/page/loc', '/') - добавляет в конец, если уже не заканчивается этими символами
										     Итог: /page/loc/
	Str::is('locat*', 'location') - соответствует ли шаблону => true
	Str::isUlid('laravel') - можно ли использовать как UUID => false
	Str::kebab('my funcGetCount') - преобразовать в шашлычный стиль => my-func-get-count
	Str::length('Laravel') - длина строки => 7
	Str::limit('В лесу родилась', 6) - усекает строку, ставит многоточие в конце => В лесу...
	Str::lower('LARAVEL') - нижний регистр => laravel
	Str::mask('89514466332', '*', 5) - маскировка строки с заменой символов => 89514******
	(string) Str::orderedUuid() - создает Uuid с добавлением в начало временной метки (то есть растет, можно сорировать по увеличению)
				      Можно сохранять как id в БД, которые будут возрастать:
				      9bfda819-dd30-43fc-82d8-12c0f772d8bf, 9bfda820-fa8c-4f08-9a60-536dbcd88b70
	(string) Str::ulid() - подобное, но более короткое => 01HXB3H6RW1G6RW9QVGAWHXPQG
	(string) Str::uuid() - генерирует UUID => ffd6761d-ccaa-45d1-ae29-2248fcdb9ee6

	Str::padRight('James', 10, '-') - дополняет строку символами до указанной длины => 'James-----'
	Str::password(5) - случайный пароль => 07%X5
	Str::plural('car') - множественное число, только анг. язык => cars
	Str::singular('children') - ед. число, только англ. язык => child
	Str::random(10) - случайная строка указанной длины => c3KGDIdscn
	Str::remove('https://', 'https://lenta.ru') - удалить подстроку => lenta.ru
	Str::replace('8.x', '9.x', 'Laravel 8.x') - замена => Laravel 9.x
	Str::reverse('Hello World') - переворачивает строку => dlroW olleH
	Str::replaceArray('?', ['8:30', '9:00'], 'Работаем с ? до ?') - последовательная замена из массива
				       Итог: Работаем с 10:00 до 18:00
	Str::slug('моя страница', '-', 'ru') - создание слага c транслитерацией!!! => moia-stranica
	Str::slug('my first page') => my-first-page

	Str::squish(' В лесу    родилась ') - удаляет лишние пробелы => 'В лесу родилась'

	Str::snake('fooBar') - преобразование в snake => foo_bar
	Str::start('page', '/') - добавляет символ в начало строки, если еще не начинается с этого символа
				  Итог: /page
	Str::title('вот такой случай') - все с большой буквы => Вот Такой Случай
	Str::ucfirst('вот такой случай') - первая заглавная буква => Вот такой случай
	Str::upper('laravel') - верхний регистр => LARAVEL
	
	Str::words('Вот такой случай произошел', 2) - усекает предложение, ставит многоточие => Вот такой...

      URL-адреса
	action([MainController::class, 'index']) - возвращает url адрес метода контроллера => http://start.loc
	action([MainController::class, 'index'], ['id' => 22]) - если адрес ждет параметры 
	
	asset('img/photo.jpg') - генерирует  абсолютный url в соответствии с ASSET_URL => http://example.com/assets/img/photo.jpg  // если с vite, то ASSET_URL не меняем
	secure_asset('img/photo.jpg') - то же, но https://

	route('main') - генерирует абс. url по именованному маршруту => http://start.loc
	route('main', ['id' => 12]) - если маршрут ждет параметры => http://start.loc/12
		      Route::get('/{id}', [MainController::class, 'index'])->name('main'); 
		
	route('main', [], false) - если нужен относительный url => '/'

	url('user/profile') - полный url для указанного части пути: http://start.loc/user/profile
	url('user/profile', [1]) - можно передавать параметры: http://start.loc/user/profile/1
	secure_url('user/profile') - полный https адрес: https://start.loc/user/profile
	 
	url()->current() - текущий url (что забили в адресную строку) => http://start.loc/121287	
	url()->previous() - предыдущий url (с какой страницы пришли)

      Разное:
	abort(404) - генерирует http ответ с указанным кодом
	abort(403, 'Unauthorized.', $headers) - можно передавать сообщения и заголовки
	
	app() - контейнер служб, т.п. куда Ларавель грузит провайдеры, служебные классы, зависимости и пр.
	
	auth() - объект аутентификатора, можно использовать вместо фасада Auth:
		 Auth::user() или auth()->user()
	auth('admin')->user() - использование охранника admin
	
	return back() - перенаправление обратно
	
	bcrypt() = хеширует, без возможности расшифровки. Можно только проверить что захешированы одинаковые значения
		   То же самое, что фасад Hash:make('...')
		   Пример: bcrypt('1') => $2y$10$A1b3RUoqsVy94VtvsXaN3.raUsLWFfrpQPRjyyh2vUqhMX4PrFcsK
		   Каждый раз разные значения дает, поэтому bcrypt('1') не равно bcrypt('1') не равно Hash:make('1')
		   Так понимаю, можно только проверить через check:
			Hash::check('1', bcrypt('1')) => true
	encrypt() - шифрование с возможностью расшифровать. То же самое, что Crypt::encrypt(). Примеры:
		    encrypt(1)	=> pdiI6IlIzTVVuTFZ...
		    decrypt(pdiI6IlIzTVVuTFZ...) => 1

	blank() - проверяет является ли переданное значение пустым:
		blank('') => true
		blank('   ') => true
		blank(null) => true
		blank(collect()) => true

		blank(0) => false
		blank(true) => false
		blank(false) => false

	filled('1') - проверяет, что не пустое, обратная функция для blank() 

	fake() - получает экземпляр Faker из контейнера, вставляет фейковые данные.
                 Для русского языка меняем в app.php: 'faker_locale' => 'ru_RU'
		 fake()->word(10) => blanditiis
		 fake()->url => http://kulikov.ru/eos-et-totam-quia.html
		 fake()->time => 05:35:30
		 fake()->firstName => Ростислав
		 fake()->city => Дмитров
		 fake()->address => 174020, Нижегородская область, город Щёлково, пр. Косиора, 55
		 {{ fake()->unique()->safeEmail() }} => pmills@example.net

	broadcast(new UserRegistered($user)) - транслирует событие слушателям
	event(new UserRegistered($user)) - отправляет событие слушателям

	cache() - для работы с кешем. cache('key') - получить по ключу. И много др.
	collect([1,2,3]) - создает объект коллекции
	config('app.timezone') - получить значение в папке config в файле app по ключу timezone
	config(['app.debug' => true]) - временное изменение настройки на время работы скрипта (не сработало)

	cookie() - создает куку: cookie('name', 'value', $minutes)
	
	{{ csrf_field() }} - вставляет в верстку скрытый инпут с csrf токеном (то же @csrf):
			     <input type="hidden" name="_token" value="2c7cqtzrphAG9u44Q4uxn56zDnvkTbf6Jh0LxTYF">
	csrf_token() - вернет сам токен: 2c7cqtzrphAG9u44Q4uxn56zDnvkTbf6Jh0LxTYF

	{{ method_field('put') }} - вставляет в верстку скрытый инпут с методом (то же @method('put'):
			            <input type="hidden" name="_method" value="put">
	
	dd(csrf_token(),1,2,3) - выведет переменные списком и завершит скрипт
	dump() - выведет переменные, но скрипт продолжится

	dispatch(new App\Jobs\SendEmails) - помещает задание в очередь

	env('APP_ENV') - вернет переменную из .env, после config:cache не работает

	info('текст') - запишет в журнал, то же: Log::info(), покажется в дебагбаре.

	now() - вернет экземпляр Carbon для времени. В верстке покажет время: {{ now() }} => 2023-01-14 03:29:08

	today() - экземпляр Carbon для даты

	old('value', 'default') - вернет прежнее значение инпута из флеш переменной сессии
				  <input name='age' value="{{ old('age') }}">

	optional() - позволяет избегать ошибок при вызове свойств объектов, если они могут не существовать
		     optional($student)->qwerty  => null
		     $student?->qwerty => null  (второй способ)

	redirect() - перенаправление:
		     return redirect('/home') - по адресу
		     return redirect()->route('main') - на им. роут

	report('Ошибка') - сообщит об исключении

	request() - объект http запроса, то же самое, что $request в контроллерах
	request()->input('name', 'default') - значение инпута
		 ->query() - гет параметры (пост???)
		 ->files - файлы
		 ->cookie() - куки
		 ->header() - заголовки
	         ->method() - метод (гет, пост) и пр.
		 ->json()->get('key') - принять json объект и получить данные по ключу key
			                request()->key - то же самое, авт. извлекает из json

	rescue() - выполняет внутри себя функцию, если будет ошибка, то ошибка запишется в лог, 
		   но скрипт будет работать как ни в чем не бывало. Также можно сделать по-другому через retry()
		   rescue(fn() => kva + sva) - ошибка внутри функции не остановит скрипт
		   return rescue(function () {return ошибка$thiйцуйцуйцуs->method();}, 'default');

	response() - создает экземпляр http ответа
		    	return response('Hello World', 200, $headers)
			return response()->json(['foo' => 'bar']); // можно также return json_encode(['foo' => 'bar']);

	retry() - выполняет переданную функцию указанное количество раз, игнорирует выброшенные исключения, 
		  кроме последней попытки. В итоге вернет либо первое удачное значение, либо последнее исключение
		  return retry(5, function () { throw new \Exception('ошибка');}, 1000) - браузер крутится, через 5 секунд выбросится исключение

	session('key') - работа с сессией

	transform() - выполняет замыкание для переданного значения
		      $result = transform(10, fn($num) => $num ** 2) // запишется 100
		      $result = transform(null, fn($num) => $num ** 2, 'по умолчанию') // запишется 'по умолчанию'
		      
	view() - вернет представление
		 return view('auth.login')

	return Blade::render('<h1>{{ $var}}</h1>', ['var' => 'Заголовок']) - готовая вьюха на сыром коде без файла

111) Измерение скорости функции:
	Benchmark::dd(fn() => Student::find(1)) - выведет время выполнения одного замыкания // 17.308ms
	Benchmark::dd(fn() => Student::all(), 10) - среднее арифметическое при выполнении 10 циклов // 333.227ms

	Benchmark::dd([ - сравнение нескольких замыканий
            fn() => Student::find(1), // 17.156ms
            fn() => Student::all()]); // 349.508ms  

111) Срабатывание функции на основе заданных шансов.
     Полезно, если нужно выполнить код только для определенного процента входящих запросов.

        $result =  Lottery::odds(1, 5) // 1 шанс из 5
            		->winner(fn () => true) // вернется в 1 случае из 5 срабатываний
            		->loser(fn () => false) // вернется в 4 случаях из 5 срабатываний
            		->choose();

111) Остановка кода на время (подобно php sleep()):
	 Sleep::for(1.5)->minutes() - остановка на 1,5 минуты (то же sleep(90))
	 Sleep::for(2)->seconds() - на 2 секунды 
	 Sleep::for(500)->milliseconds() - 500 миллисекунд   

111) HTTP клиент.
	В Ларавель встроен пакет Guzzle и обертка над ним - фасад Http.
	
	$response = Http::get('https://lenta.ru/') - отправить http get запрос и записать ответ в $response
	return $response - в браузер выведется запрошенная страница/данные

	return Http::get('https://lenta.ru/')['name'] - если ответ в json, то авт. распарсить и получить значение по ключу
	return Http::dd()->get('https://lenta.ru/') - вывести на экран объект запроса перед его отправкой и завершить скрипт
	
	$response->body() - тело ответа
		 ->status() - статус ответа (200,404)
		 ->ok() - тоже статус (true, false)
		 ->successful() - код ответа между 200 и 300
		 ->json() - вывести принятый json как массив ??
		 ->headers() - принятые заголовки от сервера
		 ->header('Set-Cookie') - значение опр. заголовка

	
	Http::get('https://lenta.ru/', ['name' => 'Steve']) - отправить get запрос с добавлением в адрес гет параметров
	Http::get('https://lenta.ru/?name=Steve') - то же самое

	Http::post('https://lenta.ru/', ['name' => 'Steve']) - отправить post(put, patch) запрос и прикрепить в тело данные
							       которые guzzle авт. преобразует в json

	Http::attach('attachment', file_get_contents('photo.jpg'), 'photo.jpg')->post('https://lenta.ru/') - отправить файл
	
	Http::withHeaders(['X-First' => 'foo']->post('https://lenta.ru/') - прикрепить заголовки и отправить пост запрос
	
	Http::accept('application/json')->get('https://lenta.ru/') - какой тип содержимого ожидаем
	Http::acceptJson()->get('https://lenta.ru/') - то же самое для json

	Http::withToken('token')->post(...) - добавить токен в заголовок Authorization

	Http::timeout(3)->get(...) - время ожидания ответа в секундах (по умолчанию 30 сек.) или выбрасываем исключение 
	
	Http::retry(3, 100)->post(...) - если будет ошибка повторять запрос столько раз через столько миллисекунд

	Если возникает ошибка запроса, то отследить ее можно так:
	$response->successful() - код ответа между 200 и 300
	$response->failed() - код ответа >= 400
	$response->clientError() - код ответа 400
	$response->serverError() - код ответа 500

	$response->throw() - выбросить исключение, если ошибка
	return Http::post(...)->throw()->json() - выбросить исключение или продолжить цепочку

	Http::fake() - режим возврата пустых ответов с кодом 200 для тестирования
	$response = Http::get('https://lenta.ru/') - вернется пустая страница с кодом 200

	$responses = Http::pool(fn (Pool $pool) => [ - отправка несколько запросов за раз
    		$pool->get('https://site.ru/1'),
    		$pool->get('https://site.ru/2')]);
	echo $responses[0] - ответы в массиве

	Ларавель запускает три события при отправке http запросов:
		RequestSending - до отправки запроса
		ResponseReceived - после получения ответа
		ConnectionFailed - ответ не получен
	Для них можно сделать слушатели, напр: php artisan make:listener RequestListener --event=RequestSending
	Зарегистрировать в EventServiceProvider и отслеживать. Все работает.

111) Локализация
	config/app.php=>locale - установка языка по умолчанию 
	config/app.php=>fallback_locale - запасной язык, если основной язык не имеет строки перевода
	lang - папка с переводами строк
		
	App::currentLocale() - текущий язык
	app()->getLocale() - текущий язык
	App::isLocale('en') - проверка на язык
	App::setLocale('ru') - установить язык для одного запроса

	php файлы перевода хранятся в папке lang по папкам языков. Эти файлы возвращают массивы: 
					return ['welcome' => 'Welcome to our application!',];

	echo __('messages.welcome') - строка из файла messages по ключу welcome => Welcome to our application
	
	Если указанная строка не существует, то вернется сам параметр:
	echo __('validation.key')  => validation.key

	Если переводимых строк много, то можно хранить в файлах json, 
        например, файл lang/ru.json: {"I love programming.": "Me encanta programar."}
        В этом случае можно не придумывать множество ключей, а сразу использовать нуждающееся в переводе словосочетание:
	echo __('I love programming.'). Если не существует, то вернется I love programming

	{{ __('auth.password') }} - вставка в blade

	Строки перевода имеют настраиваемое поле, начинающееся с :
	В это значение можно передавать значения.
	Например строка перевода: 'welcome' => 'Привет, :name'
	Передаем: __('messages.welcome', ['name' => 'Денис']) => 'Привет, Денис'

	Плюрализация:
	Файл lang/ru/fruits.php: return ['apple|apples' => 'Здесь яблоко|Здесь яблоки',]
	Обращаемся и передаем количество: 
		trans_choice('fruits.apple|apples', 3) => Здесь яблоки
		trans_choice('fruits.apple|apples', 1) => Здесь яблоко

111) Email
	Константы почты прописаны в .env
	config/mail.php использует их, в нем можно установить драйвер отправки. 
	Я использую драйвер smtp, настроен яндекс smtp в .env. Однако Ларвель советует использовать 
	драйверы Mailgun или Postmark - они платные, но могут отправлять миллионы писем в час.
	
	Если основная служба (у меня smtp от Яндекса) не будет работать, то можно настроить вспомогательную:
		1) в config/mail.php в failover->mailers пишем вспомогательные драйверы по порядку срабатывания: ['mailgun', 'log']
		2) в default пишем: 'default' => env('MAIL_MAILER', 'failover') // то есть failover сработает в случае ошибки
	
	Как создать письмо:
		1) php artisan make:mail OrderShipped  / Ship - отгружен
		   
		   class OrderShipped extends Mailable  {  
    			  protected $order; - сюда будет принимать переменную из конструктора письма
					      если сделать свойство как public, то не нужно будет прикреплять как with 
					      в методе content()

    			  public function __construct($order){
        			 $this->order = $order;} - приняли переменную и заполнили

    			  public function envelope(){
        			 return new Envelope( // здесь можно настроить тему письма, отправителя, запуск функции
            				subject: 'Заказ отправлен',);} - тема письма

    			  public function content(){
        			 return new Content(
            				view: 'mail.order_shipped', - вьюха письма
            				with: ['order' => $this->order],);} - переменные передаваемые в письмо. Если же было public $order, 
									      то $order доступен во вьюхе без прокидывания через with
    			  public function attachments(){
        			 return [
					  Attachment::fromStorage('files/order.pdf'), - прикрепляем файл к письму со Storage
					  Attachment::fromStorage('files/1.pdf')->as('order.pdf'), - можем переименовать
					  Attachment::fromPath('images/cow.jpg'), -  по пути public/images/cow.jpg	 
					];}}
		
		2) Вьюха письма order_shipped.blade.php - заполняем ее как обычную html верстку. Работают все теги.
			   <h1>Добрый день</h1>
			   <p>Ваш заказ {{ $order->id }} отправлен</p>

		3) Отправляем почту в контроллере:
			  Mail::to('rodnovspb@mail.ru')->send(new OrderShipped('Б-325'));
		   В to можно передавать объект у которого есть свойство email или name, Ларавель автоматически извлечет их:
			 Mail::to($order)->send(new OrderShipped('Б-325')) // или лучше через queue ставим в очередь

	Можно установить одновременную отправку копий писем:
		Mail::to($order)->cc('rodnovspb@mail.ru')->send(new OrderShipped('Б-325')) + копия письма
		Mail::to($order)->bcc('rodnovspb@mail.ru')->send(new OrderShipped('Б-325')) + скрытая копия письма
	
	Так как отправка писем может негативно повлиять на время отклика приложения (а если еще отправляются в цикле)
        письма обычно отправляют в фоновом режиме. Отправляется и так, но написано, что надо настроить очереди 
	(там просто, переключить соединение на database и опубликовать таблицу заданий).
		Mail::to('адрес')->queue(new OrderShipped('Б-325'))	
	Можно отложить отправку писем:
		Mail::to('адрес')->later(now()->addMinutes(1), new OrderShipped('Б-325')) - не работает, видимо надо настроить очереди 
											(по умолчанию исп драйвер sync - немедл. выполнение)
															
	Ларавель при отправке почты испускает события:
		1) MessageSending - перед отправкой сообщения
		2) MessageSent - после отправки	

111) Уведомления (смс, тг, email, fb и др.) - короткие сообщения. Каналов много, можно посмотреть на 
					      https://laravel-notification-channels.com/about/
				              Удобно отправлять сообщения сразу на несколько каналов одной командой, напр. $user->notify(...) 
     
	Как и для email для отправки уведомлений создается отдельный класс для сообщения
	php artisan make:notification OrderReady
	
	class OrderReady extends Notification{
    		use Queueable; // видимо для очередей

    		protected $user; // здесь принимаем аргументы из конструктора для вставки в уведомление

    		public function __construct($user){ 
        		$this->user = $user;} // сохраняем принятую переменную

    		public function via($notifiable){ - какие каналы и методы запустим (toMail(), toNexmo() - смс и др.)
        		return ['mail', 'database', SmsChannel::class];} - запустим методы toMail(), toDatabase(), затем метод send у своего класса SmsChannel

		public function toDatabase(){ - запускается на канале database, см. ниже про описание канала
        		return $this->user->toArray();} - всегда должен возвразать php массив

    		public function toMail(){ - запускается на канале mail, формирует текст уведомления
        		return (new MailMessage)
				->subject('Тема письма')
				->greeting('Привет!')
                    		->line("Уведомление для {$this->user->name}")
                    		->action('Notification Action', url('/')) - черная кнопка призыва к действию
				->error() - делает кнопку красной, нужно если уведомляем об ошибке, напр. оплаты
				->from('barrett@example.com', 'Barrett Blair') - можно поменять отправителя (берется из .env)
                    		->line('Thank you for using our application!')
				->attach('images/cow.jpg');}} - можно прикрепить файл (по пути public/images/cow.jpg)

		Метод toMail() кроме формирования стандартного макета уведомления может возвращать вьюху:
		public function toMail(){return (new MailMessage())->view('mail.order_ready', ['order' => 12]);}

		Также можно опубликовать и изменить стандартный макет в resources/views/vendor/notifications:
		php artisan vendor:publish --tag=laravel-notifications	

   А) Пример уведомлений на почту:
	1) В таблице users понадобится поле email
	2) В модели User прописать использование трейта: use Notifiable; (внутри класса)
	3) Отправляем уведомление в контроллере:
		$user = User::query()->find('120') - получаем пользователя
		1 способ:
		Notification::send($user, new OrderReady($user)) - отправляем уведомление, поле email возьмется из первого 
								   $user благодаря трейту Notifiable в модели
		2 способ:
		 $user->notify(new OrderReady($user));  - то же самое, передаем в конструктор уведомления $user

		3 способ:
		Notification::route('mail', 'rodnovspb@mail.ru')->notify(new OrderReady()) - сами вводим кому отправляем

   Очереди - чтобы отправлялось фоном. Нужно также настроить обработчик очереди (по умолчанию исп драйвер sync,
             то есть немедленное выполнение)
	class OrderReady extends Notification implements ShouldQueue { - используем интерфейс ShouldQueue
		use Queueable; ... } - подключаем трейт Queueable

   Теперь отправляя уведомления как обычно они будут уходить в фоне или ставится в очередь (в зав от QUEUE_CONNECTION):
	user->notify(new OrderReady($user)) - уйдет в фоне
	$user->notify((new OrderReady($user))->delay(now()->addMinutes(10))) - уйдет через 10 мин (но ушло сразу)

   Пример уведомлений в БД на основе встроенного способа сохранения уведомлений		
	1) php artisan notifications:table
	   php artisan migrate - создалась таблица notifications с датой, данными уведомления, временем прочтения уведомлений
	2) В методе via добавляем работу по каналу database:
		return ['mail', 'database'] 
	3) public function toDatabase(){ - 
        	return $this->user->toArray();} - возвращаем PHP массив, будет преобразован в json и сохранен в поле data
						  таблицы notifications(напр. можно пользователя и текст уведомления). 
						  Остальные поля заполнятся сами.
	4) user->notify(new OrderReady($user)) -  создаем уведомление, оно отправится как на почту, так и в БД

	В модель мы встраивали трейт Notifiable, он позволяет авт. получить все уведомления для объекта модели:
	$user = User::query()->find('120');
	$notes = $user->notifications - автоматически получили все его уведомления через связь Eloquent	 

    Б) Уведомления смс
       Выбираем пакет на https://laravel-notification-channels.com/about/
       Устанавливаем как написано, добавляем канал в via()
       Описываем метод, например: public function toSmscRu($notifiable){}
       Вызываем user->notify(new OrderReady($user)), уведомление отправляется. Не проверял, на smsc.ru стоимость около 4 р.

    В) Наш канал для отправки смс через SMSRU.  Есть и готовый пакет-канал для SMSRU
	1) Создаем класс SMSRU с этого сайта (копируем), создаем php artisan make:notification OrderReady если еще не создавали
	2) В классе уведомления OrderReady:
	   class OrderReady extends Notification implements ShouldQueue { - поддержка очередей. По умолчанию выполение сразу - sync
    		use Queueable;
		private $user;
		public function __construct($user) {
        	    $this->user = $user; }
		public function via($notifiable) {
        		return ['mail', SmsChannel::class]; } - добавили канал SmsChannel, у него здесь сработает метод send
		public function toSms(){ - также в классе-уведомлении метод с текстом. Сюда можно передавать параметры при создании уведомления $user->notify(new OrderReady($user));
        		return 'Ваш заказ готов';}
	3) Создаем класс-канал:
	   class SmsChannel {
    		private $sender;

    		public function __construct() {
        		$this->sender = new SMSRU(config('sms.password'));} - можно и в контейнер встраивать, сделал так. В параметре ключ от SMSRU

    		public function send(User $user, Notification $notification){ - метод send автоматически вызывается при запросе класса в списке via
        		$data = new stdClass();
        		$data->to = $user->phone; - телефон пользователя
        		$data->text = $notification->toSms(); - текст 'Ваш заказ готов'
        		$sms = $this->sender->send_one($data);}} - отправляем смс
	4) Вызываем уведомление в контроллере:
		$user->notify(new OrderReady($user));
	5) Можно сделать очереди QUEUE_CONNECTION=redis и затем запускать их php artisan queue:work

 События при отправке уведомлений:
	1) NotificationSending - отправляется, можно получить информацию о сообщении и в случае чего отменить отправку
	2) NotificationSent - отправилось

111) Пакеты - способы расширения функциональности Ларавель
     Пример: пакет Laravel Media Library (https://delay-delo.com/laravel-6-rabota-s-izobrazheniyami-pri-pomoshchi-spatie-media-library)
     Этот пакет: 
	1) автоматически крепит пути загружаемых изображений к модели через таблицу связей (сам сохраняет изображения??)
	2) может минимизировать, устанавливать размеры, конвертировать изображения

	if($request->hasFile('image') && $request->file('image')->isValid()){
       		$post->addMedia($request->file('image'))->toMediaCollection('images');}	 

111) Процессы - запускают команды в терминале сервера
	$result = Process::run('php -v');
	return $result->output() => PHP 8.1.1 (cli) (built: Dec 15 2021 10:31:43)

	Process::path(__DIR__)->run('ls -la') - запустить процесс по пути

	Примеры запуска процессов:
		Process::run('git pull')
		Process::run('node -v')

111) Очереди - нужны для выполнения в фоне трудоемких задач, занимающих время (напр. сохранение файла csv). 
	       То есть пользователь отправляет задачу в очередь, не ждет пока скрипт отработает и пользуется
	       сайтом дальше. Задание попадает в очередь и выполняется в фоновом режиме, 
               нужно чтобы была запущена команда php artisan queue:work (супервизором) или придумать другие способы ее запуска 
               (например по крон + shedule)

     Просматривать очереди jobs можно в Telescope

     Отличия очередей queue от dispatch:
	1 способ - с implements ShouldQueue, use Queueable, queue и пр.
	  Вся логика скрыта, применять, если не нужно писать доп. логику, а просто отправить в очередь. 
	  Может вызываться где угодно, не требует дополнительной настройки или создания  классов заданий
	  Например отправка письма: Mail::queue(...)->...
	2 способ - создать класс задания (php artisan make:job ProcessPodcast), прописать в нем доп. методы, связанную логику.
          Используется для более гибкой настройки, добавления дополнительных связанных действий.
	  PhotoDownload::dispatch($photo) - Отправка в таблицу jobs. Можно выполнять последовательные задачи: загрузка, обрезка, сжатие, отправка email
               
	config/queue.php - настройки очередей
	QUEUE_CONNECTION=database - настройка в .env соединения (по умолчанию sync)
	
	Для очередей в Ларавель используются соединения:
	1) sqs от Амазон
	2) redis (устанавливаем QUEUE_CONNECTION=redis + включаем модуль redis в openserver для лок. разработки). Выполняются по php artisan queue:work
	3) sync - немедленное выполнение в общем потоке (стоит по умолчанию, применять только для разработки)
	4) database - для таких очередей в БД создается таблица jobs - т.е. задания. Задания выполняются по команде php artisan queue:work

     В каждом соединении может быть несколько очередей для различных заданий. Очередь по умолчанию, куда будут
     отправляться все задания имеет ключ queue. Если отправляем задание без явного указания очереди, то задание 
     поставится в эту общую очередь.
	Примеры:
		SendMessage::dispatch() - в общую очередь
		SendMessage::dispatch()->onQueue('emails')  в очередь emails

   Очереди на основе БД:
	1) php artisan queue:table 
	   php artisan migrate - в БД создается таблица jobs
	   QUEUE_CONNECTION=database - устанавливаем в .env

	2) php artisan make:job SendMessage - создается класс задания в app/jobs

	3) Класс задания:
	   class SendMessage implements ShouldQueue {
    		use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;

    		protected $message; // здесь сохраняем данные из конструктора

    		public function __construct($message){ // при приеме данных грузим их в свойство protected $message
        		$this->message = $message;}

    		public function handle(){ - этот метод выполняется 
			sleep(5); - для примера, чтобы успеть увидеть запись в таблице jobs если не делаем delay()
        		info($this->message);}}	- в логах вставляется запись, но в дебагбаре ее не видно
						  и не видно через dd() - делаем соединение sync и видно

	4) SendMessage::dispatch('Отработать сразу') - отправляем задание в очередь в контроллере без задержки
	   SendMessage::dispatch('Отработать через минуту')->delay(now()->addMinutes(1)) - установка задержки в 1 минуту, выполнится
											   при условии, что очередь запущена
	   SendMessage::dispatchSync('...') - отработать немедленно, так понимаю это то же, что использовать соединение sync

	   Теперь при каждой команде SendMessage::dispatch() в БД отправляется sql команда  insert into `jobs` ..., 
	   в таблице jobs создается запись с сериализованным описанием задания и сразу удаляется как только выполняется. 
	   Чтобы успеть увидеть запись нужно вставить sleep(5) или сделать задержку для задания.

	5) php artisan queue:work - запускаем бесконечную работу очередей (запустится общая очередь). Свалится при сбое, лучше через schedule
	   php artisan queue:work --queue=название_очереди - если нужна другая очередь

	   Для запуска очередей можно использовать supervisor (нет в шаред хостинге) или запускать их по крону. 
           Например добавить в shedule отработку заданий в очереди, а shedule запускать ежеминутно в кроне:
	   	$schedule->command('queue:work --stop-when-empty')->everyMinute()->withoutOverlapping();
		Либо:
		$schedule->command('queue:work --max-time=300') - но запускать расписание кроном через 5 мин. 
	
	Внимание!!! Если стоит соединение database, то:
	1) нужно заново запускать php artisan queue:work если поменяли функцию handle() 
           либо запускаем т.н. supervisor или php artisan queue:listen
	2) не отрабатывает показ info() в дебагбаре (хотя в логах вставляется запись)  +  не работает dd()

	Однако при соединении sync дебагбар и dd() отрабатывает.

	Если мы сделали задание с задержкой:
	SendMessage::dispatch('Отработать через минуту')->delay(now()->addMinutes(1))
	и отключили работу очередей, то задание выполнится как только запустим заново php artisan queue:work,
	при условии, что время выполнения задания наступило.

        Создание очередей еще один порядок:
	php artisan queue:table (failed jobs уже создалась после установки Ларавель)
	php artisan migrate
 	QUEUE_DRIVER=database
	php artisan config:cache (если до этого уже кешировали)
	php artisan queue:work database --tries=1
	
	Примеры использования очереди:
	 1) public function store(Request $request){
        	$podcast = Podcast::create(...) - создаем объект записи в БД

        	ProcessPodcast::dispatch($podcast);} - отправляем его в очередь на сохранение в БД, как только
						       приложение освободится, чтобы не тормозило
	2) SendMessage::dispatchAfterResponse() - отработать после того как браузер отправил ответ пользователю.
						  Например, пользователь заполнил и отправил форму, 
						  как только страница перезагрузилась (HTTP-ответ был отправлен 
						  в браузер) выполнить задание.

	Может получиться, что при выполнении заданий БД может работать с транзакциями.
	Для того, чтобы задания выполнялись как только БД свободна:
	  1) в настройках соединения в config/queue.php: 'after_commit' => true
	  2) либо используем: SendMessage::dispatch('...')->afterCommit();

  Связанные задания - используются, если нужно запустить следующее задание по цепочке при успешном 
		     выполнении предыдущего. Например, при успешной загрузке фото запустить функцию наложения водяного знака.
  Алгоритм:
	1) Создаем задания:
		php artisan make:job PrepareJob
		php artisan make:job PublishMessage
		php artisan make:job SendMessage
	2) Пусть каждое задание что-нибудь выводит в лог
	3) Пишем в контроллере цепочку заданий, которые будут выполнять последоательно:
		Bus::chain([
            		new PrepareJob('Подготовка'),
            		new PublishMessage('Публикация'),
            		new SendMessage('Успешно'),
        		   ])->catch(function (Throwable $e){info($e);})->dispatch();
	4) php artisan queue:work - начинаем отслеживание. 
	   У меня почему-то при database задания писались в логи по 3 раза.

	throw new \Exception('Ошибка') - выбросить ошибку внутри задания. Следующее задание не сработает.

  Для задач выполнившихся с ошибкой можно создать таблицу (после установки Лары она уже есть):
	php artisan queue:failed-table
	php artisan migrate

  Затем их можно запустить:
	php artisan queue:retry all

  Просмотреть неудачные задания:
	php artisan queue:failed

   Удалить все неудачные задания:
	php artisan queue:flush	

   Удалить все задания:
	php artisan queue:clear	

111) Ограничение попыток
     Накладывается на ключ, который хранится в кеше вместе с количеством попыток в опр. время.
	$executed = RateLimiter::attempt( // в $executed запишется false, если попыток не осталось. Либо true или результат обратной функции
		$user->id, // ключ
		$perMinute = 5, // разрешаем 5 раз в минуту
		function() {}); // выполнять эту функцию

	if (! $executed) {
  		return 'Попыток нет';}

	RateLimiter::increment($user->id) - увеличить количество попыток
	RateLimiter::availableIn($user->id) - время до новых попыток
	RateLimiter::clear($user->id) - сбросить попытки

111) Laravel Horizon - пакет Ларавель для просмотра Redis очередей в браузере. 
     Для работы нужно php расширение для многопоточности pcntl, в windows не устанавливается. 
     Поэтому либо докер, линукс, homestead, вагрант, либо на хостинге можно пользоваться горизонтом.

111) Route::get('/clear', function() { - универсальный роут для полной очистки Ларавель
    	Artisan::call('optimize:clear');
    	return redirect()->back();});

111) Создание команд артизан
	В файле routes/console.php описываем нужную команду и что она делает:
	Artisan::command('show_date', function () {
    		echo date('Y-m-d');
	Теперь при вызове: php artisan show_date в консоли показывается время

111) Cron - утилита, позволяющая выполнять скрипты на сервере в назначенное время с заранее определенной 
            периодичностью. Обычно используют для регулярного создания резервных копий.
            Пишут, что крон запускается максимум раз в минуту
     У Бегета есть панель crontab, где можно выбрать скрипт на сервере и указать время его запуска.

     Ларавель предлагает настроить один метод app/Console/Kernel.php->schedule(), в котором можно описать
     логику работы, а в кроне на сервере раз в минуту вызывать команду schedule:run, которая вызывает этот метод:
	* * * * * php /path-to-your-project/artisan schedule:run >> /dev/null 2>&1 - что-то по типу такого

        php artisan schedule:run - однократный запуск функции Kernel.php/schedule
                                   Если эту команду запускать периодически в кроне, то она 
                                   запускает все задачи, время выполнения которых уже наступило
	php artisan schedule:work - непрерывный режим, подходит для разработки на компе и на хостинге.
                                    Ларавель сама проверяет раз в минуту в фоновом режиме нужно ли что-то 
                                    запускать из Kernel.php/schedule. Крон не нужен. Но команда может слететь,
                                    поэтому нужно держать ее на контроле в супервизоре
	php artisan schedule:listen - для разработки на компе. Планировщик задач Laravel работает в непрерывном режиме,
                                      потребляет больше ресурсов, но задачи выполняются секунда в секунду по установленному времени,
                                      а не проверкой раз в минуту как у schedule:work
                                       
     Расписание задач определяется в методе schedule файла app/Console/Kernel.php

     Примеры метода schedule():
	1) protected function schedule(Schedule $schedule){
        	info('Just for testing...');} - вызывается раз в минуту запись в логи

	2) protected function schedule(Schedule $schedule){
        	$schedule->command('show_date');} - вызываем нашу функцию, описанную в routes/console.php, 
		либо любую команду artisan.
	Таким образом в routes/console.php мы можем описать наши функции, а в schedule() запускать через command()
	
	3) protected function schedule(Schedule $schedule){
        	$schedule->call(function (){ - через call можем запускать любую функцию
            		echo 'Привет, Денис';})};   
	                                       
     Наша функция schedule() будет вызываться раз в минуту кронтабом (schedule:run) или на локалке самой Ларавель командой php artisan schedule:work
     Однако мы можем настроить время ее срабатывания
	$schedule->command('show_date')->everyTwoMinutes() - каждые две минуты
	$schedule->call(function (){...})->everyFiveMinutes() - каждые пять минут
	$schedule->job(new Heartbeat)->everyTenMinutes() - запускает задание каждые 10 минут
					 ->hourly() - каждый час
					 ->daily() - каждый день в полночь
					 ->dailyAt('13:00') - в опр. время
					 ->monthly() - ежемесячно первого числа в 00:00
				         ->yearly() - ежегодно 1 числа в 00:00
					 ->fridays() - по пятницам
					 ->when(Closure) - на основе истинности замыкания и много др. условий

	Таким образом, запустив на сервере через крон ежеминутное срабатывание schedule:run будет запускаться 
	наш метод schedule() и если нет ограничений по условиям - срабатывать.

	$schedule->command('emails:send')->withoutOverlapping() - предотвратить перехлест одинаковых заданий

	Также результат выполнения schedule() можно:
		1) ->sendOutputTo($filePath) отправить в файл
		2) ->emailOutputTo('taylor@example.com') - отправить на почту

111) Настройка cron + shedule на shared хостинге:
	https://talltips.novate.co.uk/laravel/using-queues-on-shared-hosting-with-laravel

111) Настройка планировщика в openserver + schedule.
     Чтобы потом просто запускать schedule в кроне таким же образом, но по другому пути
	1) В планировщике openserver ставим во времени везде * (то есть запуск ежеминутно)
	2) В пути планировщика:
		%progdir%\modules\php\%phpdriver%\php-win.exe -c %progdir%\userdata\temp\config\php.ini -q -f C:\Users\Owner\Desktop\sites\socionix.ru\public_html\artisan schedule:run
	3) $schedule->call(function (){
            	Log::info(time());
           })->name("task")->withoutOverlapping();
	3) Или запуск очередей:
	   $schedule->command('queue:work')->name("task")->withoutOverlapping();
	4) Но работать все это будет только 1 раз в минуту, так как крон запускается раз в минуту

111) Аутентификация - проверка, что пользователь зарегистрирован как пользователь сайта
	Принцип работы: 
		1) Пользователь через браузер.
		   Пользователь вводит логин и пароль, если они верны, то для пользователя создается файл сессии,
		   а id этого файла отправляется кукой в браузер пользователю.
		   При каждом запросе браузер высылает куку с id сессии, приложение по сессии, видит, 
		   что пользователь аутентифицирован.
		2) API
		   Так как браузер отсутствует, то и кук нет. В этом случае гетпараметром присылается токен, ларавель 
		   првоеряет его по таблице токенов.

	config/auth.php - настройки
	По своей сути аутентификация Ларавель построена на охраннике web, который использует
	сессиию (+куку) и провайдер работы с таблицей users на основе eloquent.
	То есть фасад Auth по умолчанию работает с таблицей users и через охранника web 

	Если работаем с таблицей admins, то нужно в config/auth.php настроить охранника (guard) и аутентифицировать админа так:
		Auth::guard('admin')->attempt(['email' => $request->email, 'password' => $request->password]);
	Получать объект админа:
		Auth::guard('admin')->user()
	
	Провайдер users использует проверку email и password пользователя в таблице users.
	Таблицу можно поменять все там же в config/auth.php
	
	Мы можем либо сами создать формы, маршруты и функции авторизации, либо использовать готовые решения.
	Auth::attempt([...]) - проверка логина и пароля по базе, затем внутри вызывается метод Auth::login($user)
			       Возвращает true или false
			       Пароль автоматически преобразывывается функцией bcrypt() и сравнивается с табличным	
	Auth::login($user) - аутентификация, например зарегистрировались и сразу аутентифицировались. Насколько
			     понял метод устанавливает куку и создает файл сессии для пользователя.
	Auth::check() - проверка аутентифицирован ли 
	
	password в таблице users, если создаем аутентификацию самостоятельно должен быть не меньше 60 символов:
		 $table->string('password', 70);
	remember_token в таблице users - поле для пользователей выбравших «Запомнить меня» (не обязательно). 
	
	Laravel Breeze - готовое решение для аутентификации через куку + сессию включающее верстку, маршруты, 
			верификацию и сброс пароля и пр.
	Laravel Sanctum - нужно если будут токены. Решение для аутентификации, включающее как работу через 
			  куку+сессию, так и Api токены. Сначала проверяется кука, если ее нет, то проверяется токен.
	
	Своя аутентификация:
	Auth::user() - 	получить текущего аутентифицированного пользователя (если из таблицы admins, то Auth::guard('admin')->user() + делаем гарда)
	$request->user() - то же самое
	Auth::id() - его id
	Auth::check() - проверка на аутентификацию.

	Внимание! Вместо Auth::check() принято разрешать доступ к маршрутам через встроенного посредника:
	Route::get('/cabinet', function () { ... })->middleware('auth');

	Если пользователь не аутентифицирован, то этот посредник перенаправляет на страницу login
	Страницу перенаправления можно настроить в Middleware/Authenticate.php в redirectTo()

	middleware('admin') - часто создают такого, внутри проверяется статус пользователя

	В Auth::attempt([]) передают логин или email и password, пароль не хешируют, проверка будет автоматическая.
	Если пользователь найден, то автоматически срабатывает Auth::login() и создается сессия.

	После Auth::attempt([]) принято пересоздавать сессию $request->session()->regenerate();
	Это защищает от атаки "Фиксация сессии"

	Auth::once([...]) - аутентификация только для одного запроса, то есть не создается сессия и кука
			    Например: if(Auth::once([...])){ разрешить действие }

	return redirect()->intended('dashboard') - метод intended означает, что пользователя перенаправит 
	return redirect()->intended()		   на ту страницу, куда изначально хотел. В скобках указан
						   запасной адрес, если страница intended не доступна
	

	remember_token - поле в users. Пользователь ставит галочку в форме входа, далее вход осуществляем 
			 c применением аргумента true:
			 Auth::attempt(['email' => $email, 'password' => $password], true)
			 Теперь пользователь при закрытии браузера не выйдет. 
			 Чтобы выйти нужно будет: Auth::logout();
	$table->rememberToken() - в миграции для вставки поля remember_token
	
	->setRememberToken(Str::random(60)) - установка токена в $user

	Также можно аутентифицировать так (например после регистрации):
		Auth::login($user)
		Auth::login($user, true) - использование 'Запомнить меня', т.е. бессрочная аутентификация
		Auth::loginUsingId(1) - по id пользователя
		Auth::loginUsingId(1, true) - по id  + 'Запомнить меня'
	Однако, модель User обязательно должна extends Authenticatable

	Внимание! Очень удобно, без вьюхи входа, используем встроенную форму входа от браузера:
	Route::get('/url', [...])->middleware('auth.basic') - появится форма входа от браузера, 
						              данные в users проверятся сами
111) Выход пользователя
	Auth::logout() - выход

     Функция выхода:
	public function logout(Request $request){
		Auth::logout()  // если админ из таблицы admins, то Auth::guard('admin')->logout() + настроить гарда
		$request->session()->invalidate() - рекомендуется аннулировать сессию пользователя
		$request->session()->regenerateToken() - также пересоздать токен csrf
		return redirect('/')};

111) Выход со всех других устройств (например, сменили пароль):
	1) Проверяем, что в списке посредников web в файле App\Http\Kernel есть:
	 	Illuminate\Session\Middleware\AuthenticateSession
	2) Запрашиваем пароль и выполняем:
		Auth::logoutOtherDevices($currentPassword);

111) $request->session()->passwordConfirmed() - подтвердить пароль. Функция применяется для подтверждения пароля
						через указанное в config/auth.php=>password_timeout время
111) События при аутентификации: Registered, Authenticated, Login, Failed, Logout и др.
		
111) Охранники guard
	Охранники обеспечивают аутентификацию за счет проверки логина(email) и пароля в таблице БД.
	По умолчанию в методе Auth::attempt([...]) используется встроенный охранник web, при входе проверяется 
	наличие пользователя и пароль в таблицe users.
	
	Мы можем создать охранника admin и использовать его для проверки пользователя и пароля в таблице admins:
	Auth::guard('admin')->attempt([...]) - то есть сначала проверяется, что такой админ есть,
					       устанавливается кука, что это админ, а затем входим
	Получить объект админа:
	Auth::guard('admin')->user();

	Как создать охранника admin:

	1) php artisan make:migration create_admins_table --create=admins
	      $table->id();
              $table->string('name');
              $table->string('email')->unique();
              $table->string('password');
              $table->rememberToken();
              $table->timestamps();
	   php artisan migrate

	2) Создаем модель Admin (просто копируем User)
	   class Admin extends Authenticatable {protected $guard = 'admin';}

	3) в config/auth.php добавляем охранника, провайдера и сброс пароля:
	'guards' => ['admin' => ['driver' => 'session','provider' => 'admins']]
	'providers' => ['admins' => ['driver' => 'eloquent','model' => App\Models\Admin::class]]	
	'passwords' => ['admins' => ['provider' => 'admins','table' => 'password_resets_tokens','expire' => 15]]
	
	4) Можно использовать:
		Auth::guard('admin')->attempt([...])
		->middleware('auth:admin') - используем охранника admin в посреднике

	Группировка роутов для админа:
		Route::middleware('auth:admin')->group(function () {
    		Route::get('/admin-panel', [AdminController::class, 'show_panel'])->name('admin-panel');});

111) Авторизация - проверка прав на выполнение действий на сайте

     В Ларавель авторизация осуществляется через:
	1) Шлюзы (как маршруты). Например, для просмотра панели администратора или раздела этой панели
	2) Политики (как контроллеры). Например, список методов для работы с моделью

     Создание шлюза:
	1) В AuthServiceProvider в методе boot:
		Gate::define('show-protected', [ProtectedController::class, 'inner']) - создали шлюз 'show-protected'
										        для показа защищенного раздела
	2) Метод inner в контроллере ProtectedController: - создали для удобства, можно было функцию описать в шлюзе
		public function inner (User $user=null){ 
			    return $user && $user->email == 'rodnovspb@mail.ru';} - если true, то шлюз пройден
		Внимание, нужно допустить до шлюза только аут. пользователя, чтобы не было ошибки на $user->email
     		
	3) Используем шлюз в контроллере показа вьюхи:
		public function show() {
        		Gate::authorize('show-protected-part'); - если шлюз вернет true, то код продолжится
        		return view('protected');}		  если false то ошибка 403 THIS ACTION IS UNAUTHORIZED.

	Также вместо проверки шлюза в контроллере можно проверять шлюз уже в роуте:
		->middleware('can:show-protected')

	Другие методы Gate:	
	   Gate::authorize(...) - при неудаче ошибка 403
	   Gate::check(...) - при неудаче вернет false
	   Gate::any(['update', 'delete']) - если пройден либо тот, либо другой?

	Внимание! Короткая запись шлюзов сразу в контролере:
		Gate::allowIf(fn ($user) => $user->isAdmin()); - здесь срабатывает метод isAdmin() из модели
		Gate::allowIf(fn ($user) => $user->email=='cinedsms@mail.ru');
		Gate::denyIf(fn ($user) => $user->banned());

    Насколько понял политика это просто методы проверок на действия, вынесенные в отдельный класс. Шлюзы - это 
    отдельные команды, а политики то же, но как классы с кучей описаний шлюзов для одной модели.
	Создание политики
	1) php artisan make:policy UserPolicy // если имя политики даем по типу: имя модели + Policy, 
						 то не нужно регистрировать в AuthServiceProvider

	2) Пишем методы проверок в UserPolicy которые д. возвращать true или false
		public function create(User $user){return $user && $user->id > 0} // если поль. существует, то может
										     создавать что-то

		public function destroy(User $user){return $user->isAdmin()} // пользуемся методом isAdmin() из модели
									       Если админ, то может удалять

		public function update(User $user, Post $post){return $user->id == $post->user_id;} - то есть обновлять 
												      можно только свой пост

	3) Используем политику в контроллере:
		public function delete(Request $request) {
        		$user = $request->user();
        		if($user->can('destroy', $user)){Код удаления} - здесь проверяем у пользователя разрешение
			else {return 'В доступе отказано';}}

	То есть метод can() - обращение к политике
	Auth::user()->can('update', $post) - может ли пользователь обновлять этот пост, где update - проверка

	Проверку на разрешение можно применять прямо в роуте:
	->middleware('can:delete, User::class) - как-то так

	Также можно применять в blade:
	@can('edit', $post) - то же самое, что @if (Auth::user()->can('edit', $post))
	   Кнопка редактирования	
	@else
    	   Кнопка просмотра	
	@endcan	

111) Подтверждение email после регистрации
	Написано, что в Breeze все это уже включено.
	
	Если же делаем сами, то вот порядок:

	1) Таблица users должна содержать поле email_verified_at, создаем миграцию и проводим ее:
		$table->timestamp('email_verified_at')->nullable();

	2) Модель должна включать интерфейс MustVerifyEmail:
		class User extends Authenticatable implements MustVerifyEmail

	3) После регистрации создаем событие о новой регистрации:
		$user = User::create([...]);
		Auth::login($user);
		event(new Registered($user)) - это событие слушает слушатель SendEmailVerificationNotification
					       он же отправляет email
		return redirect()->route('verification.notice') - запрашиваем страницу, что нужно подтвердить почту

	4) Теперь нужно создать три маршрута:
		1) Показывающий страницу, что нужно подтвердить почту (после регистрации)
			Route::get('/email/verify', function () {
    				return view('auth.verify-email');})->middleware('auth')->name('verification.notice');
			Соответственно, также создаем вьюху auth/verify-email.blade.php (подтвердите email),
			либо берем ее из исходников Breeze

		   Слушатель после события регистрации отправил на почту кнопку с ссылкой подтверждения:
			http://start.loc/email/verify/155/28b539c1b67c146efa050f144b8804c702dff377?expires=1674512885&signature=de9cd907569f6c8530db99b6e9a358026dc4d11f2fba606ace89d8c7b471678c
		
		2) Принимающий запрос сгенерированный при клике-подтверждении email
			Route::get('/email/verify/{id}/{hash}', function (EmailVerificationRequest $request) {
    				$request->fulfill(); - вызовет метод markEmailAsVerified для $request->user() 
						       и создаст событие Verified
    				return redirect()->route('main');})
				->middleware(['auth', 'signed'])->name('verification.verify');

		   Теперь при клике на кнопку из почты отрабатывает этот маршрут, проверяет подпись, в БД 
		   заполняется поле email_verified_at

		3) Повторная отправка письма на подтверждение почты (если потерялось например)	
		     A) Создаем принимающий роут	
			Route::post('/email/verification-notification', function (Request $request) {
    				$request->user()->sendEmailVerificationNotification();

    				return back()->with('message', 'Verification link sent!');
				})->middleware(['auth', 'throttle:6,1'])->name('verification.send');
		     Б) Вставляем куда надо кнопку на этот роут:
				<form action="{{ route('verification.send') }}" method="post">
   				    @csrf
    				   <button type="submit">Отправить еще раз</button>
				</form>

	5) Настраиваем письмо для верификации. Оно стандартное, но можно поменять 
		в AuthServiceProvider => boot(): VerifyEmail::toMailUsing ... тема, текст и др.

	6) Используем встроенный посредник verified для роутов куда может пройти только вериф. пользователь:
			Route::get('/profile', function () {...})->middleware('verified');	

111) Шифрование
     	Хеширование - мы хешируем строку Hash::make (), получаем хеш, но по хешу не сможем обратно получить строку.
		      Сможем только проверить совпадают ли два хеша и сделать вывод о правильности пароля.
		      Для хеширования не используется ключ APP_KEY !!!
		      Под капотом используется php функция bcrypt()
		      Длина строки не влияет на длину хеша

	Шифрование - encrypt('строка'). Мы можем также сделать расшифровку обратно. Используется ключ APP_KEY
		     Важно! Не шифруем пароли, только хешируем.
		     Шифрование используется также для кук, поэтому если поменяли  APP_KEY, то куки пользователей
		     будут недействительны, они выйдут из сессий.
		     Длина шифра увеличивается с длиной шифруемого.
		     Под капотом используется php функция openssl_encrypt

	Если перегенерировали ключ, то не сможем расшифровать то, что зашифровали прошлым ключом,
	показывается ошибка: The MAC is invalid
	Если меняем APP_KEY и хотим сохранить зашифрованное, то придется расшифровывать со старым ключом и 
	зашифровывать с новым.
	
	Для шифрования в Ларавель используется ключ из .env => APP_KEY
	Ключ генерируется при установке Ларавель.
	Если копируем приложение на хостинг, то лучше перегенерировать ключ:
		php artisan key:generate	

	Crypt::encryptString('строка') - без сериализации (быстрее и меньше шифр), в 9 версии исп. этот метод
	Crypt::decryptString('...') - расшифровка

	Crypt::encrypt('строка') - шифрование в Ларавель с сериализацией объектов
	encrypt('строка') - помощник, шифрование
	Crypt::decrypt('...') - расшифровка 
	decrypt('...') - помощник, расшифровка

	Пример шифрованной цифры 1:
		eyJpdiI6IlF6clBCc2FHUEp1N3JhYi9NbmhtQXc9PSIsInZhbHVlIjoidnBZUUtQQndhNWZZWlNOVGcrM3VCQT09Iiwi
		bWFjIjoiYzc5MjIzZDBlZTU5ZDk4NWY3OGE2MTI4ODljOTk1NmZhNjQxNmZjYzlhMzI5NzRhNWEyZGU0ZDFjYWQ0YzA4
	
	Внимание! Также как и хеш шифр для одного и того же создается каждый раз новый (но расшифровывается).
	
	Чем больше текст для шифрования, тем больше шифр.

111) Хеширование
	config/hashing.php - настройка хеширования. По умолчанию php функция bcrypt().
			     Там же можно настраивать количество циклов хеширования (по умолчанию 10).
		
	Hash::make('пароль') - хеширование
	Hash::check('пароль', хешированный пароль) - проверка совпадения

	Хеш каждый раз создается новый для одного и того же значения.
	С увеличением хешируемой строки хеш не увеличивается.
	Нельзя расшифровать обратно, можно только сверить хеши через Hash::check()


111) Сброс пароля
	Breeze включает сброс пароля.

    Если самостоятельно, то:
	1) Проверяем, что User содержит трейт Notifiable
	
	2) Ларавель отвечает на все запросы к приложению, независимо от того какой заголовок пришел в host.
	   Для безопасной работы сброса пароля нужно либо в .htaccess настроить, чтобы принимались только
	   хосты с названием сайта, либо:
		1) раскомментировать посредник TrustHosts::class в App/Http/Kernel
		2) указать в нем разрешенные хосты: hosts(){return ['start.loc', ...]

	3) Вкратце, для восстановления пароля используется класс Password::(sendResetLink, reset):
		1) "Забыл пароль" => срабатывает роут, показывается вьюха с инпутом, куда нужно ввести свою почту
			Route::get('/forgot-password', $callback)   
		2) Вводится почта, нажимается submit, 
			Route::post('/forgot-password', $callback)
		   почта валидируется, класс Password создает токен с ограничением по времени, 
		   записывает его в таблицу password_resets по типу: email - токен
		   Формируется ссылка с этим токеном в конце /reset-password/{token} и отправляется на почту		
		3) Пользователь на почте нажимает эту ссылку и переходит на страницу восстановления по роуту:
			Route::get('/reset-password/{token}, $callback)
		4) Показывается форма с инпутом "email" и "подтвердите пароль", токен записывается в скрытый инпут формы.
		   Пользователь вводит новую почту, нажимает submit, срабатывает роут:
			Route::post('/reset-password', $callback)
		   Валидируется почта, проверяется соответствие токена с хранимым в reset_password для этой почты,
	           в БД сохраняется хешированный пароль.

	Токены с истекшим сроком действия по-прежнему лежат в БД, если нужно удалить:
		php artisan auth:clear-resets

	Либо поставить в расписание:
		$schedule->command('auth:clear-resets')->everyFifteenMinutes();

111) База данных
	Ларавель поддерживает работу с: MariaDB, MySQL, PostgreSQL, SQLite, Microsoft SQL Server
	Для работы с БД используется либо сырой SQL, либо модели Eloquent ORM
	
	config/database.php - настройки, берутся из .env
	
	Создание файловой БД sqlite в phpstorm
	1) view->tool windows->database-> на панели database нажимаем +
	2) Выбираем Data Source -> sqlite - тут же можно выбрать другую БД и подключиться к ней
	3) В появившемся окне если нужно меняем имя файла с identifier.sqlite
	4) Во вкладке Advanced в поле Working directory оставляем или меняем путь для этого файла с корня на другой
	5) Меняем в .env: 	
		DB_CONNECTION=sqlite
		DB_FOREIGN_KEYS=true
		Нужны только эти 2 строки, остальное можно закомментировать	
	6) Важно! Нужно поменять константу DB_DATABASE, чтобы в ней стоял абсолютный путь к файлу identifier.sqlite
	   Проще это сделать сразу в config/database.php => sqlite:
		'database' => dirname(__DIR__). '/identifier.sqlite'
	6) php artisan migrate - миграции прошли успешно
	   User::query()->create([...]) тоже получилось

111) Для работы с БД мы можем использовать:
	1) Сырой SQL в фасаде DB::  DB::select('select * from users where id = ?', [1])
	2) DB:: + методы построителя запросов (->where(...) и др.) 
	3) Модели Eloquent $user = User::find(1)

111) Сырой SQL в фасаде DB
	Фасад DB поддерживает статические методы: select, update, insert, delete, и statement
	Возвращается всегда массив массивов. Если получили только один объект пишем так: dd($users[0]->name)

	А у построителя запросов возвращается коллекция (а у нее есть методы как помним, 
	которые и использует построитель запросов): DB::table('users')->get()->first()
	
	Пример запроса:  
		$users = DB::select('select * from users')

	Для защиты от sql инъекций в случае сырого sql используем ? или именованные псевдопеременные:
		$users = DB::select('select * from users WHERE id = ?', [1])
		$users = DB::select('select * from users where id = :id', ['id' => 1])
		DB::insert('insert into users (id, name) values (?, ?)', [1, 'Денис'])
	Если это построитель запросов или Eloquent, то такая защита не нужна!!!

	Примеры:
		DB::insert("INSERT INTO users (name, email) VALUES (?, ?)", ['Денис', '1@mail.ru'])
		DB::update("UPDATE users SET name = ? WHERE id = ?", ['Гриша', 1]);
		DB::delete("DELETE FROM users WHERE id = ?", [163])

	Можно использовать другую БД:
		$user = DB::connection('sqlite')->select('select * from users WHERE id = ?', [1]);

	Можно настроить слушатель для каждого sql запроса. Для этого в AppServiceProvider в boot():
		DB::listen(function ($query){
            		dd($query);}) - выведем объект запроса или выполним любое др. действие

111) Транзакции
	Методы транзакций фасада DB работают как для DB::, так и для Eloquent
	Если MySQL,то должна исп. InnoDB

	Для транзакций используется метод DB::transaction().
	Если хоть один sql запрос будет с ошибкой или выбросится исключение, то произойдет откат всех запросов 
	этой транзакции. Если ошибок или исключений не будет, то операции выполнятся.
		DB::transaction(function (){
            		DB::delete("DELETE FROM users WHERE id = ?", [1]) - не удалится
            		DB::update("UPDATE users SET name = ? WHERE id = ?", [160, 'Гриша']);}) - неправ. id = Гриша
	
	Внимание! Если два раза удалить одно и то же, либо обновить удаленное и пр., то ошибки не будет, 
	остальные sql запросы в транзакции все равно выполнятся. 
	Наверно потому, что возвращается 0, а не исключение или ошибка: 
		DB::delete("DELETE FROM users WHERE id = ?", [1]) - удалили пользователя
		dd(DB::update("UPDATE users SET name = 'Вася' WHERE id = ?", [1])) => исключения или ошибки нет, вернется 0

111) Можно проверять или возвращать результат транзакции, для этого нужно использовать return:
	$result = DB::transaction(function() use ($request, $user){
		$post = Post::create($request->all);
		.... другие действия ...
		return $post })

111) Использование внешних переменных в DB::transaction:
	$data = [1,2,3];
	DB::transaction(function() use ($data){ // пишем так, иначе не могу использовать внешнюю переменную $data
          $order = Order::create($data);
          dd($order->id);});

111) Транзакции возможны и для eloquent:
		DB::transaction(function() use ($request){
			$user = User::create($request->all());	
			$user->roles()->attach(Role::where('name', 'general')->first()); // назначаем роль
			$user->roles()->attach($request->role); // или так
			if(...) {throw new \Exception('User could not be created');} // можно вызвать откат так
										        но дорогостоящая операция, лучше
							                                вручную (есть ниже)
			AlertNewUser::dispatch($user)->afterCommit(); // испускаем событие только если все успешно}, 3)
								         цифра 3 - количество попыток транзакции
     Откат будет автоматически если внутри возникнет исключение. Мы можем сами пробрасывать ошибку при необходимости,
     чтобы вызвать откат. Но это дорогостоящая операция, можно сделать через флаг $success:

111) Ручной механизм транзакций:
	DB::beginTransaction();
	try {
	      $user = User::find($id);
	      $user->name = 'Василий';
	      $user->save();
	      $user->roles()->sync(...); 
	      DB::commit();}
	catch(\Exception $e) {
	      DB::rollBack();} // откат если было исключение
  
       Можем сами откатывать по условию, если, что-то не устроило: 
	 $success = false;
	 DB::beginTransaction();
	 try { ... ; $success = true }
	 catch (\Exception $e) {DB::rollBack(); - можно откатить здесь при исключении}
	 if ($success){DB::commit();} - если все устроило подтверждаем
	 else {DB::rollback();} - или откатываем

111) При try мы можем показать ошибку через флеш переменную:
	return redirect()->back()->with('error', $e->getMessage())
     Если без try, то ошибка пробросится в обработчик Ларавель и будет просто 500 страница	

111) Построитель запросов
	Внимание! Методы построителя также работают для Eloquent!

	Построитель запросов укорачивает код:
		$users = DB::select('select * from users') - просто фасад DB с сырым sql
		$users = DB::table('users')->get() - через построитель запросов

	Построитель запросов работает с коллекциями и использует методы коллекций:
		DB::table('users')->get() - вернулась коллекция, мы можем использовать ее метод first
		DB::table('users')->get()->first - вернулась тоже коллекция, однако:
		DB::table('users')->get()->first() - теперь вернулся объект, это тоже, что ...->get()[0]
						     а sql такой: select * from `users` limit 1

	Коллекцию можно перебирать как массив:
		$users = DB::table('users')->get();
		foreach ($users as $user) { echo $user->name; }

	Методы построителя запросов:

	   Внимание! Методы можно ставить цепочкой: DB::table('users')->where('age', '>', 18)->get()  

	   Также можно часть запроса положить в переменную, а затем продолжить сколько угодно раз:
		$var = DB::table('users')->where('id', '>', 30);
		$var = $var->where('id', '<', 50);
		dump($var->get()), в итоге: select * from `users` where `id` > 30 and `id` < 50

	   Также можно объединять запросы через union():
		$first = DB::table('users')->where('id', '<', 30);
		$second = DB::table('users')->where('id', '>', 20)->union($first)->get();
		В итоге: (select * from `users` where `id` > 20) union (select * from `users` where `id` < 30)
	
	   DB::table('users')->get() - получить результат в виде коллекции
	   DB::table('users')->first() - получить первый объект коллекции: select * from `users` limit 1 
		->insert(['name'=>'Денис', 'email' => '1@mai.ru']) - вставка
		->insertGetId(['name'=>'Денис', 'email' => '1@mai.ru']) - вставка и получение id записи
		->upsert([...]) - вставить если не существует, иначе просто обновить
		->where('id', 1)->update(['email' => '2@mai.ru']) - изменение записи, метод update
		->where('id', 1)->update(['options->enabled' => true]) - можно обновлять строку json по ключу
		->where('id', 100)->delete() - удаление записи
		->truncate() - удаление всех записей, обнуление id
		->dd() - посмотреть получившийся sql запрос

		->find(100) - получить по id, вернется объект, а не коллекция
		->find([1,2,3]) - получить несколько по массиву id
		->pluck('email') - коллекция только email [почта, почта, почта ...]: select `email` from `users`
		->pluck('email', 'name') - коллекция email с ключами [имя=>почта, имя=>почта ...]
		->chunkById(100, $callback($users)) - можно перебирать коллекцию частями, 
						      части будут передаваться в колбэк, будет несколько sql запросов:
						      ...limit 100 offset 0, потом limit 100 offset 100
						      Также работает в Eloquent: Page::chunkById(...)
		->lazyById - работает как chunkById, но будет один sql запрос и в обратный вызов идет вся полученная LazyCollection 
			     select * from `users` order by `id` asc limit 1000
		->count() - количество записей: select count(*) as aggregate from `users`
		->max('id') - наибольшее значение по полю
		->min('price') - наименьшее значение по полю
		->avg('time') - среднее значение
		->sum('cost') - сумма значений по полю: select sum(`cost`) as aggregate from `users`
		->exists() - существует ли запись, вернет true или false
		->select('name', 'email') - получить только опр. поля, похоже на pluck, только можно много полей
					    select `name`, `email` from `users`
		->join('contacts', 'users.id', '=', 'contacts.user_id') - присоединение join
		->select('email')->distinct() - только уникальные результаты: select distinct `email` from `users`
		->union(второй запрос) - объединение запросов

		->where('age', 18) - условие where с оператором "="
		->where('age', '>', 18) - условие where
		->where('name', 'like', 'Д%') - начинается на Д
		->orWhere('age', 18) - оператор OR (если несколько условий OR, то orWhere нужно использовать с помощью замыкания)
		->whereBetween('votes', [1, 100]) - между двумя значениями
		->whereIn('id', [1, 2, 3]) - содержится в переданном массиве (исп. в безопасном удалении)
		->whereNull('updated_at') - значение поля равно null
		->whereNotNull('updated_at') - не равно null
		->whereDate('created_at', '2016-12-31') - соответствует дате
		->whereYear('created_at', '2016') - ищет по году в дате 2016-12-31,  также есть whereMonth, 
						    whereDay, которые также ищут по полной дате
		->whereColumn('updated_at', '>', 'created_at') - оператор на два столбца по одной строке

		->orderBy('name') - сортировка, по умолчанию - по возрастанию
		->orderBy('id', 'desc') - по убыванию 
		->oldest() - сортировка по полю created_at как User::orderBy('created_at', 'asc')->get();
		->latest() - сортировка по полю created_at как User::orderBy('created_at', 'desc')->get();
		->inRandomOrder() - случайная сортировка
		->inRandomOrder()->first() - случайная запись: select * from `users` order by RAND() limit 1
		->reorder('email', 'desc') - удалить промежуточные сортировки и сортировать по полю
		->groupBy('age') - группировка по полю
		
		->take(3)->get() - сколько взять записей: select * from `users` limit 3
		->offset(10)->take(3)->get() - пропустить 10, взять 3: select * from `users` limit 3 offset 10
		->limit(2) - то, же что и ->take(2)
		->skip(5) - то, же что и ->offset(5)

		->increment('votes') - увеличение на 1 в поле
		->increment('votes', 5) - либо на 5
		->decrement('votes') - уменьшение на 1
		->decrement('votes', 5) - либо уменьшение на 5

111) Пагинация
	0) Нужно подключить tailwind, например: <script src="https://cdn.tailwindcss.com"></script>
	1) В tailwind.config.js в content вставляем:
		'./vendor/laravel/framework/src/Illuminate/Pagination/resources/views/*.blade.php',
	2) Запускаем в консоли:
		npm run dev или npm run build (если выкладываем)
	3) В верстку вставляем показ пагинации:
		{{ $users->links() }}
	4) В контроллере:
		$users = User::paginate(5); - длинная пагинация с цепочкой кнопок
        	return view('index', compact('users'));

    Если Тайлвинд отключили, то для кнопочек достаточно раскомментировать @tailwind utilities;

    Методы пагинации:
	paginate(5) - длинная пагинация
	simplePaginate(5) - только две кнопки: назад и вперед	
	cursorPaginate(5) - подойдет если выборка огромная или пагинация с бесконечной прокруткой. Работает в несколько
			    раз быстрее если данных много. Однако будут только кнопки вперед и назад.
	
    В пагинацию можно встраивать метод where:
	$users = User::where('age', '>', 18)->paginate(15)
 
    При установке доп. гет параметров, например, при работе фильтров, эти гет параметры сбрасываются при пагинации.
    Для того, чтобы сохранить гет параметры используем withQueryString():
	$users = User::paginate(5)->withQueryString();

    Для того, чтобы вручную добавить нужные гет параметры используем appends():
	$users = User::paginate(5)->appends(['sort' => 'price']) => http://start.loc/?sort=price&page=6
		
    Добавить хеш к каждой странице пагинации:
	$users = User::paginate(5)->fragment('photo') => http://start.loc/?page=10#photo

    Настроить длину пагинации во вьюхе для метода paginate(у него показываются кнопки с цифрами):
	{{ $users->onEachSide(2)->links() }}

    Настроить вид пагинации:
	1) php artisan vendor:publish --tag=laravel-pagination	- вьюхи пагинации появились в resources/views/vendor/pagination
	2) По умолчанию используется tailwind.blade.php, можно редактировать
	3) Можно установить другой вид пагинации из папки resources/views/vendor/pagination:
	   в AppServiceProvider в boot устанавливаем файл:
	     для paginate(): Paginator::defaultView('pagination::semantic-ui') - обязательно добавляем pagination::
	     для simplePaginate(): Paginator::defaultSimpleView('pagination::simple-default')
	     Для бутсрапа из той же папки: Paginator::useBootstrapFive() или useBootstrapFour() + подключить бустрап

111) Миграции
	Фасад Schema позволяет работать со всеми БД поддерживающимися Ларавель.
	Шаблон миграции можно изменить опубликовав stubs: php artisan stub:publish

	php artisan make:migration create_goods_table - создание миграции, Ларавель попытается угадать имя таблицы
							по тому как назвали миграцию
	php artisan migrate:status - посмотреть выполненные и невыполненные миграции
	php artisan migrate --pretend - посмотреть sql команду миграции
	php artisan migrate --force - принудительная миграция, несмотря на ошибки
		    migrate:rollback - откат последней партии миграций
		    migrate:reset - полный откат всех миграций
		    migrate:refresh - откатить все и заново накатить
		    migrate:refresh --seed - откатить, накатить и провести все посевы
		    
	Schema::create('users', $callback) - создание таблиц
	Schema::table('users', $callback) - работа с существующей таблицей: вставка, обновление и пр.
					    если изменяем поля, то нужен пакет composer require doctrine/dbal и делаем так:
					    $table->string('name', 50)->change();
    	if (Schema::hasTable('users')) - если таблица существует
	if (Schema::hasColumn('users', 'email')) - если таблица существует и содержит поле email				
	Schema::connection('sqlite')->create('users',$callback) - указание в какой БД создавать таблицу
	Schema::drop('users') - удалить таблицу (или dropIfExists)
	
   Создание полей:
	$table->bigIncrements('id') - UNSIGNED BIGINT поле для первичного ключа id
	      ->id() - UNSIGNED BIGINT с названием поля 'id', то есть укорочение для bigIncrements('id')		
	      ->bigInteger('num') - поле BIGINT
	      ->unsignedBigInteger('num') - поле UNSIGNED BIGINT, целые числа от 0 до 18446744073709551615, занимает 8 байт
	      ->binary('photo') - поле типа BLOB (двоичные данные до 65 КБ. Можно превращать в blob строки, изобр и пр.
			          let blob = new Blob(['hello,', 'Denis'], {type: 'text/plain'})
	      ->boolean('confirmed') - поле BOOLEAN
	      ->char('name', 100) - CHAR (до 255 символов) указанной длины
	      ->string('name') - VARCHAR по умолчанию установится до 255 символов
	      ->string('name', 100) - VARCHAR до 100 символов
	      ->string('name', 16000) - VARCHAR максимум вышло 16000 символа 
	      ->text('description') - поле TEXT (до 65 Кб), подходит для хранения больших текстов
	      ->mediumText('description') - поле MEDIUMTEXT (до 16 Мб)
	      ->longText('description') - поле LONGTEXT (до 4 Гб)
	      ->date('created_at') - поле DATE YYYY-MM-DD
	      ->decimal('amount', 8, 2)  - дробные числа, где 8 макс. общее число чисел, 2 - сколько после запятой
					   расчеты (напр. сложение, округление больших чисел) если число менее 10 знаков
				           будут точнее, использовать для цен и подобного, где важна точность после запятой
	      ->double('amount', 8, 2) - то же, но использовать там где не важна точность расчетов. Напр. округление:
					 1/3 => 0.33333334326441 Однако быстрее, меньше памяти, этот тип стандартизирован 
					 для всех.
				         double('amount', 8, 2) - максимум 8 цифр, где 2 будет после запятой (00 по умолчанию)
	      ->float('amount', 8, 2) - наименее точно, не рекомендуется для финансовых операций
	      ->enum('answer', ['yes', 'no']) - ENUM, для вставки допустимы только значения из списка, как радиополе
	      ->set('answer', ['yes', 'no']) - для вставки допустимы только значения из списка, но несколько, как флажки
	      ->foreignId('user_id') - поле UNSIGNED BIGINT
	      ->foreignUuid('user_id') - поле UUID (вместо id по типу: aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee)
				         Плюсы UUID: можно объединять неск таблиц, без конфликтов id
						     всегда разные, даже если сгенерированы на др. сервере
					 Минусы: больше памяти, отладка сложнее (WHERE id = 'df3b7cb7-6a95-11e7-8846-b05adad3f0ae')
	      ->uuid('id') - поле UUID (36 символов)
	      ->ulid('id') - поле ULID (26 символов)
	      ->integer('salary') - поле INTEGER
	      ->json('options')	- поле JSON	 	
	      ->point('position') - поле POINT - координаты
	      ->rememberToken() - поле VARCHAR(100) для хранения ключа аутентификации (запомнить меня)
	      ->softDeletes('deleted_at') - поле TIMESTAMP для мягкого удаления 
	      ->time('time') - поле TIME HH:MM:SS
	      ->dateTime('created_at') - поле DATETIME YYYY-MM-DD HH:MM:SS, года: 1000-9999
	      ->timestamp('added_at') - НЕ ИСПОЛЬЗОВАТЬ, заменить на dateTime
                                        поле TIMESTAMP, кол. секунд с 1 января 1970, года: 1970 - 2038 !!!
					если будем использовать даты выше, то лучше делать ->dateTime (DATETIME)
                                        также обязательно делаем ->timestamp('...')->nullable(), иначе поле самостоятельно обновляется
                                        при обновлении соседних полей
	      ->timestamps() - создает сразу два поля created_at и updated_at типа TIMESTAMP (работает до 2038 года)
	      ->tinyInteger('votes') - поле TINYINT - числа от -128 до 127, занимает 1 байт
	      ->unsignedTinyInteger('votes') - TINYINT UNSIGNED - целые числа от 0 до 255, занимает 1 байт 		        

   Модификаторы столбцов
	$table->string('email')->nullable() - по умолчанию NULL  // отмена в down: nullable(false)->change()
	      ->after('column') - поместить после столбца column
	      ->first() - поместить столбец первым
	      ->autoIncrement() - автоинкремент (первичный ключ)
	      ->default(0) - установить значение по умолчанию
	      ->unsigned() - беззнаковое поле для цифры
	      ->timestamp('time')->useCurrent() по умолчанию вставляется CURRENT_TIMESTAMP
	      ->timestamp('time')->useCurrentOnUpdate() при обновлении вставляется CURRENT_TIMESTAMP
	      ->unique() - уникальный индекс, например: $table->string('email')->unique()

   Индексы:
	Индексы для поля типа $table->id():  автоинкремент, PRIMARY(первичность + уникальность) пропишутся автоматически
	Также можем делать самостоятельно:
	       $table->primary('id') - первичный ключ PRIMARY(первичность + уникальность)
	       ->primary(['id', 'parent_id']) - составной ключ
	       ->unique('email') - уникальный индекс
	       ->index('state') - простой индекс (для быстроты поиска по этому столбцу)
	       ->fullText() - полнотекстовый индекс, применяется для поиска слов по большому объему текста. 
			      Лучше, чем LIKE для больших объемов (миграция: $table->text('text')->fulltext(), иначе не создается)	

   Внимание! В Ларавель мы не сможем изменять столбцы просто так! Понадобится установить пакет:
	      composer require doctrine/dbal
   Также для изменения столбцов ->timestamp понадобится в config/database.php вписать:
	      'timestamp' => TimestampType::class - более подробно в документации
   
   Изменение столбца, используем change():
	      $table->string('name', 50)->change() - например изменили длину. Также повторяем прежние модификаторы полей, иначе затрутся
	      $table->string('name', 50)->nullable()->change() - сделали null по умолчанию // отмена в down: nullable(false)->change()

   Удаление столбца:
	      $table->dropColumn('salary')  или  $table->dropColumn(['votes', 'avatar', 'location']);

   Переименование столбца:
	       $table->renameColumn('from', 'to');

   Установить длину строки по умолчанию для миграций в AppServiceProvider
	Schema::defaultStringLength(191) - глобальное изменение длины строки для всех миграций
   Это бывает нужно, если исп mysql старше 5.7.7

111) Фабрики, посевы
	Внимание! При работе сидеров не обязательно защищаться от массовой вставки, то есть заполнять $fillable

	Короткий порядок посевов:
	1) php artisan make:factory PostFactory // для имеющейся модели создаем фабрику
	2) В этой фабрике пишем нужные поля в методе definition: 
		return [ "text" => $this->faker->realText(), ];
	3) В DatabaseSeeder в методе run: 
		Post::factory(1000)->create(); // этот метод можно вызывать где угодно
	4) php artisan db:seed // эта команда запускает метод run из DatabaseSeeder

	То есть можно без DatabaseSeeder вызывать этот метод в контроллере:
	Post::factory()->create(); - в бд создастся одна запись
	Post::factory(10)->create(); - 10 записей

	Также можно создавать модели без сохранения их в БД:
	$post = Post::factory()->make() - создать одну модель
	$posts = Post::factory()->count(3)->make() - создать три модели

	Post::truncate() - удалить все модели

	Полный порядок посевов:
	1) php artisan make:seeder GoodSeeder
	2) Прописываем этот сидер в папе сидеров DatabaseSeeder:
		$this->call(GoodSeeder::class), либо неск. сидеров в массиве: [UserSeeder::class, GoodSeeder::class]
	3) php artisan make:factory GoodFactory -m Good
	4) В фабрике:
		public function definition(){
        	   return [
            	     'name' => $this->faker->word(),
                     'date' => $this->faker->date(),];}
	5) В GoodSeeder:
		public function run(){ Good::factory(100)->create(); }
	6) php artisan db:seed

	Как работает: запускается папа сидеров DatabaseSeeder, он запускает сидер GoodSeeder, тот вызывает фабрику 100 раз.
        Также можно пропустить GoodSeeder, а сразу запускать в DatabaseSeeder фабрику: Good::factory(1000)->create()

	Если в папе сидеров DatabaseSeeder несколько сидеров, то можно вызвать отдельный:
		php artisan db:seed --class=GoodSeeder

	Откатить все миграции и заново засеять:
		php artisan migrate:fresh --seed

111) Redis - (remote dictionary server) высокоскоростная БД, хранящая данные в виде ключ-значение в оперативной памяти.
             Что-то среднее между БД и memcached, так как данные в отличие от Memcached время от времени пишутся на диск и в случае 
             перезагрузки сервера можно восстановить.
	     Можно хранить строки, списки, множества и пр. Создана позднее, чем memcached, выглядит как Memcached на стероидах.
	     Эффективен для хранения токен-сессия. Хранят данные, которые потерять не страшно.
	     Размер значения в виде строки до 512 Мб, в виде других данных больше.
	     Используется если данные имеют отличную от строк структуру и важна сохранность. 
             Redis имеет несколько структур для хранения данных. 
	     На бегете 128МБ - 30 руб/мес.
             Панель redis есть в openserver (сначала включить модуль)
             Для Ларавель нужен пакет predis  

     Memcached - тоже наподобие высокосокростной БД, хранящей в оперативной памяти ключ-значение. Размер ключа до 250 байт,
		 значения - до 1 Мб. Хранит только строки, например фрагменты кода HTML.
                 Данные могут потеряться (место закончилось, перезапуск системы, перезапись данных если закончилось место и пр). 
	         Использовать если: значения по ключам небольшие, сохранность данных не важна.  
		 Настраивать почти не нужно, применяется в основном для кеширования сессий. Используется чаще чем Redis.
                 Memcached - кеширующий сервер. Для него есть PHP расширения memcached (много методов) и memcache - основные методы
		 На бегете 128МБ - 30 руб/мес.
                 Панель memcached есть в openserver (включить модуль сначала)

    Установка Редис с Ларавель и openserver:
	1) В openserver включаем redis
	2) composer require predis/predis
	3) CACHE_DRIVER=redis
	   SESSION_DRIVER=redis
	   REDIS_CLIENT=predis - это обязательно
	4) Проверяем:  
	   Cache::put('key', 111);  Session::put('key', 222); - теперь файлы кеша и сессий в storage/framework не создаются
	5) Хранилище можно просматривать в openserver PhpRedisAdmin => выбрать в списке нужную database,
           evo создает и пишет в папку laravel_cache

    С установкой Мемкэш возникли проблемы, но все равно, достаточно будет поменять драйвер в .env и все подстроится под мемкэш на хостинге.
		
111) Eloquent
	Каждая таблица в БД имеет соответствующую модель Eloquent. 
     
	php artisan make:model User - генерация Модели
	php artisan make:model User -m  - с миграцией (Schema::create('users', $callback))
  				    -f - с фабрикой
	    			    -s - с сидером
				    -c - с контроллером
	php artisan make:model Flight -mfsc - создать модель, миграцию, фабрику, наполнитель и контроллер
				      -mfsr - создать модель, миграцию, фабрику, наполнитель и ресурсный контроллер (resource)
			              --all - создать модель, миграцию, фабрику, наполнитель, политику, контроллер и реквест

    По умолчанию Модель называем в ед. числе от соответствующей таблицы в БД.

    Когда мы сохраняем свойства в Модель, например: $user->name = "Света", то они записываются в свойство-массив Модели
	protected $attribute = [] , получится ["name" => "Света"]
    Также в классе Модели мы можем вывести эти свойства:
	dd($this->attributes);
    Но из объекта уже не получится:
	dd($user->attributes) - ошибка, нужно обращатся как: $user->name

    В Модели есть также массив $this->original, в котором хранятся изначальные свойства Модели, до ее изменения.
    Например получили Модель:
	$user = User::find(41);
	dd($user->name) - вывело Семен
	$user->name = 'Василий' - изменили на Василий

	$user->getAttributes()["name"] - выведет новое имя Василий
	$user->getOriginal()["name"] - выведет старое имя Семен

	То есть, доступ к массиву attributes: ->getAttributes()
	Доступ к массиву original: ->getOriginal()
	
    Если названия разнятся, то прописываем в модели имя таблицы:
		protected $table = 'my_users';

    Модель подразумевает, что первичный ключ таблицы в БД - id. Если другой, то меняем:
		protected $primaryKey = 'user_id';

    Модель предполагает, что первичный ключ - автоинкрементное число. 
    Если изменяем это поведение, то:
		public $incrementing = false - отмена автоувеличения для первичного ключа
		protected $keyType = 'string'; - установка первичного ключа в виде строки

    Для автоматического создания моделью UUID ключей необходимо:
	1) В миграции поле типа: $table->uuid('id') или ulid('id') // 36 или 26 символов
	2) В модель добавить трейт: use HasUuids или HasUlids
	3) Все, теперь при создании записей автоматически задаются лексикографически возрастающие ключи типа: 
		9c0a8ceb-ae29-4df8-b914-675619996c3b  - uuid
		01hxvk5q7swe5fxz4c13jq5ha4 - ulid

    Когда сериализуем Модель в json, например, при передаче по сети или в массив можно скрыть поля:
		protected $hidden = ['password'] - теперь в User::all()->toArray() - не будет поля password
							    User::all()->toJson() - не будет поля password
    Eloquent и следовательно Модели не поддерживают составные первичные ключи. Но мы можем установить уникальные индексы.
    Насколько понял это: $table->string('email')->unique()

    По умолчанию миграции вставляют в таблицы поля created_at и updated_at, если они нам были не нужны и мы удалили 
    из миграций $table->timestamps(), то нужно также отключить заполнение этих полей в Модели:
    		public $timestamps = false

    Изменение updated_at можно отключать для одной операции перед сохранением в БД:
		$user = User::find(1);
		$user->age = 25;
		$user->timestamps = false;
		$user->save();

    Изменение названий для полей created_at и updated_at:
		const CREATED_AT = 'creation_date';
    		const UPDATED_AT = 'updated_date';

    Изменение БД для Модели:
		protected $connection = 'sqlite';

    Установить в Модели значение по умолчанию для поля (если не установили ->default('...') в миграции):
		protected $attributes = ['age' => 18,];

    Для Моделей работают все методы построителя запросов, а для all и get методы коллекций (так как они возвр. коллекции):
		$users = User::where('active', 1)->orderBy('name')->take(10)->get();
    		foreach ($users as $user){} - так как коллекции можно перебирать
		$count = User::all()->count()

    И all() и get() возвращают коллекции и создают одинаковый запрос select * from `stories`, только:
	all() - может использоваться только вначале: $stories = Story::all()->count();
	get() - может как запрашивать все, так и достраивать построитель условий: 
		$stories = Story::get() - запросить все
		$stories = Story::where('id', 1)->get()

    Если обрабатываем множество записей, то грузить их все в память через get() не обязательно:
		User::chunk(100, $callback) - если записей 1000, то будет 10 запросов sql, но меньшее потребление памяти за раз
		foreach (User::lazy() as $user){$callback} - подобно chunk запросы по 1000 записей, но чище код, и модель для записи создается только при ее вызове, предпочитаемый способ для больших объемов
		foreach(User::cursor() as $user){$callback} - один sql запрос, в памяти создается за раз 1 модель, но в памяти все необработанные записи, поэтому если записей миллион, то большое потребление памяти

    Сравнение затрат памяти и скорости для 100 000 записей:
		get() - 277 Мб - 2.72sec
		lazy() - 9 Мб - 5.4sec
	 по 500 chunk() - 6 Мб - 7.56sec

    Методы получения одного объекта (не коллекции):
		$user = User::find(1) - по id
		$user = User::where('email', '1@mail.ru')->first() - первую модель попадающую под условие

    Если модели не нашлось, то либо выбрасываем исключение и обрабатываем его, либо 404 страница:
    		$user = User::findOrFail(1) - по id или выбросить исключение
		$user = User::where('email', '1@mail.ru')->firstOrFail() - или выбросить исключение
    Это полезно, когда следующий код идет, например: $user->name - будет ошибка если модели не нашлось

111) Вставка в БД
	1 способ
	 	$user = new User;
		$user->name = $request->name;
		$user->save();
	2 способ
		$user = new User;
        	$user->fill(['name' => 'Денис', 'surname' => 'Роднов']) - то есть в fill можно вставить массив 
        	$user->save();
	3 способ
		$user = User::create(['name' => $request->name]) // нужно заполнить fillable или guarded в Модели
	4 способ
		$user = User::create($request->all()) // нужно заполнить fillable или guarded в Модели

111) Обновление
	1 способ
		$user = User::find(1) - получили из базы
		$user->name = $request->name; - заменили
		$user->save(); - сохранили
	2 способ
		$user = User::find(1);
        	$user->fill(['name' => 'Денис', 'surname' => 'Роднов']) - то есть в fill можно вставить массив
        	$user->save();
	3 способ
		User::where('id', '>', 100)->update(['name' => 'Вася']) - возможно массовое и единичное обновление
	4 способ
		User::findOrFail($id)->update($data);

     Метод update короче и под капотом имеет тот же метод fill. И update и fill работают только со свойствами, 
     которые можно заполнять массово. Отличие методов в том, что при update мы обращаемся ко всем свойствам и пытаемся
     созранить их, а при fill можно разделить на этапы:

     Заполняем незащищенные:
	$user->fill(['first_name' => 'John', 'last_name' => 'Doe',]);
     Заполняем защищенные:
	$user->password = 'secret'; 
	$user->save();


	$user->isDirty() - проверяет изменили ли что-то в Модели после ее получения
	$user->isDirty('title') - изменили ли какое-то поле (то есть делали ли $user->title = '...')
	$user->getOriginal() - получить массив исходной Модели до ее изменения в коде
	$user->getOriginal('name') - получить значение поля до его изменения (т.е. до $user->name = '...')

111) Создание или обновление в одном действии
	$user = User::updateOrCreate(['name' => 'Денис'], ['salary' => 5000]) - найти запись где поле name равно
								                Денис и изменить salary на 5000 или создать такую запись

111) Удаление
	User::find(1)->delete()
	User::where('active', 0)->delete() 
	User::destroy(1)
	User::destroy([1,2,3]) - удаление нескольких записей
	User::truncate() - удаление всех записей и обнуление id
	$count = User::destroy([1]) - вернется количество удаленных записей, можно проверять, что успешно удалили
	User::find(1)->forceDelete() - жесткое удаление без возможности восстановления

111) Мягкое удаление
	1) Добавляем в тело Модели трейт use SoftDeletes;
	2) Проводим миграцию: up(): $table->softDeletes(),  down(): $table->dropSoftDeletes()
	3) User::destroy(1) - теперь удаление работает мягко
	
     Мягко удаленная модель исключается из результатов поиска. Для работы с ней используем withTrashed():	
	Good::withTrashed()->find(5)->name - показать имя
	Good::withTrashed()->find(5)->trashed() - проверка на мягкое удаление
	Good::withTrashed()->find(5)->restore() - восстановить // deleted_at станет null
	Good::withTrashed()->history()->restore() - восстановить модель использующую отношения
	Good::onlyTrashed()->where('hit', '1')->get() - получить только мягко удаленные модели

111) Копирование модели
	Бывает полезно, если новая модель мало отличается. 
	$user1 = User::create($request->all())
	$user2 = $user1->replicate()->fill(['name' => 'Ирина']) - меняем одно поле
	$user2->save()

111) Встраивание ограничений в построитель запросов (диапазоны, scope)
	Можно создавать глобальные диапазаоны, которые будут отрабатывать всегда, либо создавать локальные диапазоны,
	которые встраиваем сами.
	
      Локальные диапазоны
	1) В модели создаем функцию диапазона:
		public function scopeGetOldest($query, $date){
        		   return $query->where('date', '>', $date);}
	2) В контроллере используем:
		$goods = Good::getoldest('2023-01-15')->get();

111) $post1->is($post2) - проверка, что модели одинаковые
     $post->author()->is($user) - работает и с отношениями

111) События модели: retrieved, creating, created, updating, updated, saving, saved, deleting, deleted, 
		     restoring, restored и replicating
     Без вызова событий: $user = User::withoutEvents(...)
     	
     Прослушивать события Модели можно через:
	 А) observer - удобно если слушаем много событий модели (есть выше в другом месте, рекомендую)
	 Б) Через замыкание в методе booted() Модели (проще всего)
	 В) через отдельные классы для каждого события
	 
	Прослушиваем через замыкание в методе booted() Модели:
	Все просто, пишем в Модели Good:
		protected static function booted(){
        		static::updated(queueable(function ($good) { - updated - здесь пишем какое событие слушаем, колбэк ставится в очереди
            			dd($good->name);}));} - будет срабатывать при каждом обновлении объекта Модели

	Прослушиваем через отдельные классы для каждого события:
	1) Создаем класс события GoodDeleted:
		php artisan make:event GoodDeleted
		
		class GoodDeleted {
			protected $good;
			public function __construct(Good $good){$this->good = $good;}}

	2) В модели Good делаем связку событие - класс события:
		protected $dispatchesEvents = ['deleted' => GoodDeleted::class];
	   То есть вместо того, чтобы при удалении вызывать класс события вручную: event(new GoodDeleted($good)), 
	   мы установили это по умолчанию

	3) Создаем слушателя для этого класса:
		php artisan make:listener DeleteGoodListener --event=GoodDeleted

	4) В EventServiceProvider привязываем слушателя к классу события:
		protected $listen = [GoodDeleted::class => [DeleteGoodListener::class],]

	5) Пишем в слушателе нужную логику:
		class DeleteGoodListener implements ShouldQueue{ //не забываем настроить очередь, если исп ShouldQueue
			public function handle(GoodDeleted $event){
        			dd($event);}}

	Прослушиваем через наблюдатель: php artisan make:observer UserObserver --model=User
        Рекомендую. Слушается много событий модели и понятно все.

111) Вместо двух команд: 
	php artisan make:event GoodDeleted
	php artisan make:listener DeleteGoodListener --event=GoodDeleted
     Можно только связать класс события и слушатель в EventServiceProvider:
	protected $listen = [GoodDeleted::class => [DeleteGoodListener::class],]
     и запустить:
	php artisan event:generate - в папке Providers создастся готовые GoodDeleted и DeleteGoodListener, которые
	                             переносим в папку Events и Listener

111) Отношения в Моделях
        Описывал выше, здесь буду только неясные моменты.

     Один к одному
	Родительская модель User и зависимая Phone. У одного пользователя только один телефон
	В зависимой Phone создается поле user_id (на основе названия родителя User), состыковка будет по этому полю user_id 
	и первичному ключу id из родительской User

	Получение телефона пользователя в модели User:
		public function phone(){
        		return $this->hasOne(Phone::class);} - если сделали согласно соглашениям id - user_id
			return $this->hasOne(Phone::class, 'foreign_key');} - если не по соглашению, а id - foreign_key
			return $this->hasOne(Phone::class, 'foreign_key', 'local_key');} - если у родителя связь происходит не по id и 
											   не по protected $primaryKey Модели, 
											   а по local_key, т.е.:  local_key - foreign_key
	Получение записи телефона: 
		$phone = User::find(1)->phone

	Получение родительской записи по зависимой записи в Модели Phone:
		public function user(){ - берется название этого метода user + _id, соответственно поиск будет по user_id - id
        		return $this->belongsTo(User::class);}
			return $this->belongsTo(User::class, 'foreign_key') - если в зависимой ключ не user_id, а foreign_key
			return $this->belongsTo(User::class, 'foreign_key', 'owner_key') - если связь по: у зависимой foreign_key,
											   а у родителя owner_key
		$user = Phone::find(2)->user

     Один ко многим
	У одного поста Post много комментариев Comments.
	
	Получение комментариев из родительской Модели Post:
		public function comments(){
        		return $this->hasMany(Comment::class);} - будет искаться по полю post_id в зависимой таблице comments
			return $this->hasMany(Comment::class, 'foreign_key') - по полю foreign_key в зависимой таблице comments
			return $this->hasMany(Comment::class, 'foreign_key', 'local_key') - foreign_key (зависим.) - local_key (в род.)
	
	Получение коллекции комментариев:
		$comments = Post::find(1)->comments;
		
	Получение комментариев и продолжение запроса:
		$comment = Post::find(1)->comments()->where('active', 1)->first()

	Получение родительской записи по зависимой в Модели Comment:
		public function post(){
        		return $this->belongsTo(Post::class);} - по соглашению: id - post_id
			return $this->belongsTo(Post::class, 'foreign_key'):  id (в родителе) - foreign_key (в зависимой)
			return $this->belongsTo(Post::class, 'foreign_key', 'owner_key'): owner_key (в родителе) - foreign_key (в зависимой)

		$post = Comment::find(2)->post

	Отношения belongsTo, hasOne позволяют предусмотреть запись по умолчанию, которая вернется, если не нашлось ничего (null)
		public function user(){
    			return $this->belongsTo(User::class)->withDefault();} - вернется пустой объект user
			return $this->belongsTo(User::class)->withDefault(['name' => 'Den']);} - можно его заполнить

111) Многие ко многим
	Например есть много пользователей и много ролей. У пользователя может быть несколько ролей и одна роль 
	может принадлежать нескольким пользователям.	
	
	Таблицы: users, roles и связующая role_user.
	Промежуточная называется по имени связываемых моделей в алфавитном порядке (Role, User) и содержит столбцы user_id и role_id

	Функция получения ролей у пользователя:
		public function roles(){
        		return $this->belongsToMany(Role::class);}

		$roles = User::find(2)->roles  // используем только find, first, findOrFail, firstOrFail, 
						  потому что get и all возвращают коллекцию/массив у которых нет метода ->roles
		$roles = User::find(2)->roles()->orderBy('name')->get() - с продолжением запроса, ставим скобки ()

	Ларавель соединит имена моделей User и Role в алф. порядке и будет искать в таблице role_user. 
	Но можно задать другую таблицу: 
		return $this->belongsToMany(Role::class, 'other_table');
	Также поменять столбцы поиска:
		return $this->belongsToMany(Role::class, 'other_table', 'user_id', 'role_id');

	Обратная связь, т.е. поиск у роли ее пользователей работает также через belongsToMany, только меняем класс:
		public function users(){
        		return $this->belongsToMany(User::class);}
	
	pivot - способ доступа к записям промежуточной таблицы role_user из объекта записи.
		Например, получили много ролей у пользователя и у каждой этой роли будет свойство pivot, которое содержит ту запись из
		таблицы связи role_user, которая соединяет модели.
	Однако по pivot по умолчанию мы получим только ключи 'user_id', 'role_id' ?? 
	Для того, чтобы в свойство pivot встраивались другие поля промежуточной таблицы нужно их заранее запросить:
		return $this->belongsToMany(Role::class)->withPivot('active', 'created_by') - встроили в pivot доп. поля
		return $this->belongsToMany(Role::class)->withTimestamps() - встроили в pivot created_at и updated_at. Соотв. эти поля 
				должны быть в role_user, например создадим модель php artisan make:model RoleUser -m
				В миграции уже есть строка $table->timestamps(), т.е. будут вставляться created_at и updated_at
				А в самой Модели RoleUser на всякий случай вставим protected $table = role_user (так как не знаю правильно ли назвал Модель
			        и свяжется ли она с role_user). Ниже есть подробнее о создании Модели для промежуточной таблицы.

		Если мы создаем Модель для промежуточной таблицы, то:
		1) Указываем ее явно через using в методах связываемых Моделей User и Role:
			public function users(){
        			return $this->belongsToMany(User::class)->using(RoleUser::class);}
		2) В самой Модели расширяем класс Pivot:
			class RoleUser extends Pivot { public $incrementing = true; } - и на всякий  случай вставляем инкрементирование первичного ключа, хз, написано так

		В итоге: 
			$user = User::find(1);
			foreach ($user->roles as $role) {
    				echo $role->pivot->created_at;} - то есть вывели дату создания связи в таблице role_user

	Также, если в промежуточной таблице role_user есть другие поля, кроме 'user_id' и 'role_id' можно фильровать по ним:
		return $this->belongsToMany(Role::class)->wherePivot('approved', 1) - запросить только тех пользователей у этой роли,
				в таблице связей для которых approved = 1
	Также есть подобные методы: ->wherePivotIn('priority', [1, 2]), ->wherePivotBetween('created_at', ['2020-01-01 00:00:00', '2020-12-31 00:00:00']);
				    ->wherePivotNull('expired_at') и др.

111) Дополнительные методы для работы с отношениями
	$posts = Post::has('comments')->get() - получить все посты, в которых есть хотя бы один комментарий
     	$posts = Post::has('comments', '>=', 3)->get() - получить посты, в которых есть 3 или более комментариев
	$posts = Post::withCount('comments')->get() - получить посты с доп.полем comments_count, в котором кол-во связанных комментариев
	$book = Book::first(); $book->loadCount('genres') - то же, но для отдельного объекта, получение количества связанных записей
	$posts = Post::withSum('comments', 'votes')->get() - с доп. полем comments_sum_votes (есть также withMin, withMax, withExists)
	$post = Post::first();  $post->loadSum('comments', 'votes'); - то же, но для отдельного объекта

111) Жадная загрузка отношений
	Обычная загрузка:
		$books = Book::all();
		foreach ($books as $book) {
    			echo $book->author->name;} - для каждой книги будет отправляться sql запрос на связанную запись автора
	Жадная загрузка:
		$books = Book::with('author')->get() - вроде author - имя метода
		foreach ($books as $book) {
    			echo $book->author->name;}
	Для жадной загрузки будет два sql запроса:
		select * from books - получить все книги
		select * from authors where id in (1, 2, ...) - получить всех авторов для этих книг

    Если постоянно грузим связанные модели, то жадную загрузку отношений можно включить по умолчанию в Модель через $with:
		class Book extends Model {
			protected $with = ['author']; - аналогично как если прописывали постоянно Book::with('author')->get();
			public function author(){
				return $this->belongsTo(Author::class);	}} - теперь при запросе книг будут 
									     автоматически сразу запрашиваться авторы
    Если из связанной модели нужны только некоторые поля, то грузим так:
		$books = Book::with('author:id,name,book_id')->get(); - в контроллере (написано, что должны включить id)
		protected $with = ['author:id,name,book_id']; - в модели 

    Отложенная жадная загрузка:
		$books = Book::all() - без жадной загрузки, получили только книги
		$books->load('author') - загрузили отложенно всех авторов полученных книг

111) Вставка и обновление связанных Моделей.
	Допустим, есть родительская модель Post и дочерняя модель Comment с полем post_id.
	Для того, чтобы добавить к посту новый комментарий и автоматически заполнить post_id:

	  1) Через метод save(), принимает объект комментария:	
		$comment = new Comment(['text' => 'Комментарий']);
		$post = Post::find(1);
		$post->comments()->save($comment) - в comments вставился комментарий и заполнилось поле post_id 

	     Также есть saveMany, позволяет вставлять несколько комментариев к посту.
		$post->comments()->saveMany([new Comment(['text' => 'Комментарий1']), new Comment(['text' => 'Комментарий2'])])

	  2) Через метод create(), принимает массив:
		$post = Post::find(1);
		$comment = $post->comments()->create(['text' => 'Комментарий']); // метод массовой вставки, возможно понадобится $fillable

	     Также есть createMany, позволяет вставлять несколько комментариев:
		$post->comments()->createMany([['text' => 'Комментарий1'],['text' => 'Комментарий2']]);

	  3) Уже от себя:
		$comment = Comment::create(['text' => 'Комментарий', 'post_id' => id_поста]) - ведь это проще всего
	
111) Вставка записей в промежуточную таблицу в отношениях многие ко многим.
	$user = User::find(1);
	$user->roles()->attach($roleId) - вставили в промежуточную таблицу role_user связь user_id - role_id
	$user->roles()->attach($roleId, ['date' => $date]) - прикрепили данные в дополнительное поле date в таблице  role_user

111) Удаление записей из промежуточной таблицы
	$user->roles()->detach($roleId) - удалили из role_user связь user_id - role_id
	$user->roles()->detach() - удалили все связи для $user в таблице role_user

     Можно удалять несколько записей:
	$user->roles()->detach([1, 2, 3]) - удаляем у пользователя роли с данными id

111) Синхронизация
	$user->roles()->sync([1, 2, 3]) - для пользователя в таблице связей разместим роли с этими id, остальное удалим!!!
	$user->roles()->syncWithoutDetaching([1, 2, 3]) - то же, что и сверху, но не удаляем роли с отсутствующими в массиве id

	$user->roles()->toggle([1, 2, 3]) - если присоединена, то отсоединим и наоборот

111) Обновление updated_at родителя при присоединении, обновлении дочерней модели методом save
	class Comment extends Model{ 
		protected $touches = ['post']; } - теперь будет обновляться updated_at родителя, если дочернюю изменяли методом save


111) Коллекции
	Все методы Eloquent (all, get и др.), которые возвращают более одного результата возвращают коллекции.
	Им доступны как обычные методы коллекций (map, toArray, pluck и пр), так и дополнительные.

	 $users = User::all() - получаем коллекцию и будем продолжать ее методами.
	 $var   = $users->contains(1) - содержит ли пользователя с id = 1
	 $users = $users->diff(User::whereIn('id', [1, 2, 3])->get()) - вернет пользователей, которых нет в переданной коллекции
			->intersect(User::whereIn('id', [1, 2, 3])->get()) - вернет пользователей, которые есть в переданной коллекции
			->except([1, 2, 3]) - вернет модели кроме тех, что с переданными первичными ключами
			->find(1) - вернет модель с этим id, можно передавать массив id: find([1,2,3])
			->fresh('comments') - обновить отношения в памяти
			->load(['comments', 'posts']) - жадно загрузить указанные отношения во все модели коллекции
			->modelKeys() - вернет массив первичных ключей моделей коллекции
			->only([40, 41]) - вернет только модели с указанными ключами, наподобие find([40, 41])
			->toQuery() - позволяет коллекции применить методы построителя запросов: $users->toQuery()->update(['age' => '36',])
				      Будет запрос: update `users` set `age` = 36 where `id` in (1,2,3...). Поле у всех изменилось, сработало.
			->unique() - получить только уникальные модели

111) Аксессоры и Мутаторы
	Позволяют преобразовывать модели при загрузке их в БД или выгрузке из БД.
	Например, можно шифровать запись Crypt::encryptString() перед записью в БД, 
	а при получении расшифровать Crypt::decryptString(). Или преобразовать массив в json при записи в БД, а при 
	извлечении обратно в массив.

    Аксессор - вызывается при запросе свойства у модели (его может у нее не быть) или получении записи из БД 
    Мутатор - при сохранении свойства в модели: $user->name = 'Денис' - на этом моменте сработает мутатор

    Создание аксессора.
	В модели создаем метод, называем его getFirstNameAttribute, где FirstName - свойство модели first_name

	Мутаторы и аксессоры поменялись, код есть выше

	 get: fn ($value) => strtoupper($value), - авт. вызывается при User::find(1)->first_name
						   имя будет всегда преобразовываться в верх. регистр

	$user = User::find(40);
	dd($user->name) => MRS. KAYCEE PACOCHA PHD

    Кроме того, в аксессоре можно выполнять несколько действий или работать сразу с несколькими свойствами Модели,
    а также работать с несуществующими свойствами, если для них есть аксессор:
	protected function fullName(): Attribute  - то есть работаем со свойством full_name, оно не существует
		return Attribute::make(
            		get: fn () => "{$this->first_name} {$this->last_name}");  // в $this хранится вся модель
	
	$user = User::find(40);
	dd($user->full_name) - свойство full_name в модели нет, но раз аксессор существует, то он сработает
			       => Denis Rodnov

    Создание мутатора
	Метод в Модели создаем по типу protected function firstName(), где firstName - свойство first_name

	set: fn ($value) => strtolower($value),
	
	$user = User::find(1);
	$user->first_name = 'Денис'; - здесь сработает мутатор
	$user->save();	

    Пример полный:
	protected function topHeader(): Attribute {
          return Attribute::make(
            set: fn($value) => $value ?? $this->meta_title,);}
		
    Также как и в аксессорах такие свойства могут не существовать у Модели, но если есть такой мутатор, то он
    сработает:
	protected function myWord($value): Attribute {
		return Attribute::make(
            		get: fn () => $this->attributes['name'])}; - у Модели есть protected $attributes, 
									   куда и всегда записываются свойства
	
	$user = User::find(1);
	$user->my_word = 'Денис' - сработает мутатор
	$user->save();

111) Для того, чтобы всегда добавлять определенное несуществующее свойство в Модель:
	В Модели:
	1) protected $appends = ['is_admin']; // теперь это свойство всегда будет создаваться в объекте модели. Или в коде:
					         return $user->append('is_admin')
	2) protected function isAdmin(): Attribute { return Attribute::make(  // будет запускаться этот аксессор
            					get: fn () => ... );}

111) Для того, чтобы дата правильно работала с библиотеками PHP и JS не рекомендуется менять 
     в config/app значение 'timezone' => 'UTC'

111) Преобразование в json: $user = User::find(41)->toJson();

111) $casts - работает примерно как аксессоры, приводит данные к типу при извлечении  из БД
	      Одно но: не может преобразовывать null, будет ошибка

	  class User {
		protected $casts = ['name' => 'boolean'];} - теперь свойство name будет приводиться к булеву значению при извлечении из БД
	
	  Теперь если в базе хранится число 1, то $user->name покажет true/
  
    ОДНАКО ДЛЯ преобразования массив-json будет работать и как асксессор и как мутатор:
    Если в БД храним список в json, то ставим:
	protected $casts = ['list' => 'array']; // где list - поле таблицы в формате json (тип text тоже работает для хранения json)
    И теперь можно помещать массив [1,2,3] в БД без обработки: 
	Product::forceCreate(['list' => [1,2,3]]);
    И получать его из БД без обработки:
	$products = Product::find(10)->list  => [1,2,3]
    Можно быстро обновлять значение в строке json:
	Product::find(10)->update(['list->0' => 222]); => [222,2,3]

    Если сделать так: protected $casts = ['options' => AsArrayObject::class], то можно работать так:
	$product = Product::find(10);
        $product->list[0] = 555;
        $product->save();

    ТАКЖЕ В ОБЕ СТОРОНЫ работает encrypted шифрование:
	protected $casts = ['list' => 'encrypted'];
	Product::forceCreate(['list' => '123']); - автоматически зашифрует в соответствии с ключом APP_KEY
	$product = Product::find(11)->list; - автоматически расшифрует.
    Однако следует иметь: 
	1) шифрованные данные длиннее, нужно давать минимум тип TEXT 
	2) поиск в БД будет невозможен в этом поле
	3) Если меняем APP_KEY, то нужно предварительно расшифровать данные, иначе потеряем?
		
111) Когда к Ларавель идет запрос по API, например, аякс, то приложение должно отдавать данные в json формате.
     Ларавель сама оборачивает объекты в json, например return User::find(40) вернет в браузер строку json,
     также при возврате ассоциативного массива вернется json.
     
     Однако нам может понадобиться настроить какие именно данные мы хотим возвратить. Можно сделать это в самом
     контроллере, например: 
		$user = User::findOrFail(41);
		return ['id' => $user->id, 'name' => $user->name]; - получатель получит массив в json
     
     Но также использовать класс-прокладку UserResource и очистить от лишнего кода контроллер.

     Порядок действий:
	1) php artisan make:resource UserResource

	2) class UserResource extends JsonResource{
		public function toArray($request){
			return ['id' => $this->id, 'name' => $this->name]}} - перечисляем что отдаем, можно добавить
									      свое	
	3) В контроллере: 
	   	$user = User::findOrFail(41);
	   	return new UserResource($user);
	
	4) Вернется json {data: {id: 41, name: "Василий"}}, где data - вставляется авт., это станд. ключ для данных
	   по сети

     Если хотим отдавать массив сущностей, то:
	$users = User::all();
	return UserResource::collection($users) - автоматически обернет все в один json

     Если хотим возвращать json без ключа data, то в AppServiceProvider пишем:
		JsonResource::withoutWrapping();

111) Сериализация
	User::find(41)->toArray() - модель в массив
	User::all()->toArray() - массив коллекций в массив массивов
	
	User::find(41)->toJson() - модель в json
	User::all()->toJson() - массив коллекций в json

	Сериализация в json по умолчанию отрабатывает, когда из контроллера отдаем коллекции, массивы наружу: return User::find(41);
        Например в api. 
	
	В модели можно ограничить атрибуты, остающиеся в сериализованном массиве:
	   protected $hidden = ['password'] - заполняем, чтобы в сериализованный массив не попало поле password  
	   protected $visible = ['id', 'name'] - либо заполняем $visible для определения «разрешенного списка»
        Но иногда нужно возвращать разные массивы, тогда:
	а) Либо пишем вручную:
	   $user = User::find(41);
	   return ['id' => $user->id, 'name' => $user->name] // можно выделить в отдельный метод
	б) Либо создаем ресурс (не тот, что create, update и др), а специальный для сериализации в api возврате объекта
        	1) php artisan make:resource UserResource
		2) class UserResource extends JsonResource {
			public function toArray($request){ - вызывается при возврате наружу объекта + превращается в json
        			return [
            				'id' => $this->id,
            				'name' => $this->name];}}
		3) Возвращаем из контроллера api:
			return new UserResource(User::find(13)); - сработает метод toArray => {"data":{"id":13,"name":"..."}}
		4) Уже встречал, что api сайтов часто возвращают объекты с ключом data
		5) Соответственно по api можно возвращать разные версии массивов используя разные Ресурсы.
		   Называется версифиционирование. Также применяется при обновлении сайта, чтобы старые ссылки работали:
			/api/v1/user-long  - возвращаем new UserLongResource($user)
			/api/v1/user-short - возвращаем new UserShortResource($user)
	
	
	Можно изменить как сериализуется дата в Модели:
	   protected function serializeDate(DateTimeInterface $date){
        	     return $date->format('Y-m-d');}

	Дата до этого метода выглядела вот так: "created_at":"2022-12-13T20:46:47.000000Z"
	Теперь дата в json будет выглядеть как: "created_at":"2022-12-13"

111) last-modified.com/ru/laravel - Пакет для установки заголовка Last-Modified, статуса 304 Not Modified на Laravel
	
111) В Ларавель установлен пакет, который приводит код к упорядоченому виду по правилам Ларавель.
     Он удаляет лишние пробелы, знаки, переносит скобки, вставляет расстояния и пр. - очень удобно
	./vendor/bin/pint - пишем в командной строке и все файлы поправлятся		
	./vendor/bin/pint app/Models - если нужно поправить в определенной папке
	./vendor/bin/pint app/Models/User.php - поправить только один файл	



     
	

	
		


	
		

		
		
	
   


	
	
	



	

	
	
	
		
		
	
		
		
		
	
	

	
		
	 
	
	
	
	
	   
		
	




	

	
		 

	
	

	
				
	

     	
		
	

	

     

	
		     	


		 
	




	






	


		
	
       
	
			

	

	
	
	

	
	
	
	
	

	

	
	
		
		 
	
		 
	
		
	
	

	
	

	
	
	
	
	
	
	

					
	
	
			

	

	


			
	
	




















































	
	
=============================================== Ларавель алгоритм создания сайта ===============================================		
1) composer create-project laravel/laravel my-site

2) Создаем модели, фабрики, миграции:
	php artisan make:model Article -fm

3) В модели устанавливаем поля для массового заполнения:
	protected $fillable = ['title', 'body', 'img', 'slug'];

   Либо делаем так:
	protected $guarded = []; // тогда все поля можно заполнять массово

4) Также в моделях прописываем связи с другими моделями, пример для модели Article:
	public function comments(){
        return $this->hasMany(Comment::class);} // Статья имеет много комментариев

    	public function state() {
        return $this->hasOne(State::class);} // Статья имеет один объект статистики

    	public function tags(){
        return $this->belongsToMany(Tag::class);} // Статья имеет много тегов и теги могут иметь много статей

5) Редактируем миграции:
	    $table->id();
            $table->string('title')->unique();
            $table->string('slug')->unique();
            $table->text('body');
            $table->string('img');
            $table->timestamps(); 

$table->timestamps(); создает поля created_at и updated_at, если они не нужны и мы удалили эту строчку, то отключаем в
модели их заполнение:
 	    public $timestamps = false;

6) В фабрике прописываем функцию генерирования + остальные действия описанные раньше:
	    public function definition() {
        	return [
            		'title' => $this->faker->sentence(6, true),
            		'body' => $this->faker->paragraph(100, true),
            		'slug' => Str::substr(Str::lower(preg_replace('/\s+/', '-', $title)), 0, -1),
            		'img' => "https://via.placeholder.com/600/33CCFF/FFF/?text=LARAVEL8",
            		'created_at' => $this->faker->dateTimeBetween('-1 years'),];}

7) php artisan migrate, php artisan db:seed

8) <base href="/"> - тег html, вставляется в head, добавляет в начало всех относительных ссылок указанное значение 

   Например при base href="/assets/" (последний слеш обязательно) картинка <img src="images/1.jpg"> будет открываться 
   по пути /assets/images/1.jpg	

9) https://autoprefixer.github.io/ru/ - автопрефиксер css
   https://github.com/postcss/autoprefixer - тот же пакет на гитхабе
   https://autoprefixer.github.io/ru/ - автопрефиксер онлайн
   https://cssminifier.org/ - css минификатор
	
 
	



	  	
	
	 



    
     
	
	






	
	
		


		

	
		


		


		
	
	










	

	
	



	




















111) Максимальные размеры загружаемых на сервер файлов:
	Устанавливаются в php.ini (посмотреть phpinfo())
	В опенсервер сборке стоит по умолчанию:
		upload_max_filesize = 50 Мб
		post_max_size = 50 Мб
     Чтобы изменить несколько способов:
	1) в самом коде: ini_set('upload_max_filesize', '10M'); ini_set('post_max_size', '10M');
	2) в файле .htaccess: php_value upload_max_filesize 40M
   			      php_value post_max_size 40M
	3) Редактировать сам php.ini: upload_max_filesize и post_max_size
     
     Также есть константа max_execution_time - максимальное время выполнения скрипта в секундах

111) В JS регулярные выражения задаются косыми черточками / /

111) Поиск по регулярному выражению это то же самое, что str.includes('word') - быстрее, 
     но с флагами и спецсимволами

111) Флаги рег. выражений js:
	1) i - регистронезависимость
	2) g - ищем все совпадения, без флага только первое
	3) m - многострочный режим (используется для ^ и $, считается что они обозначают также начало/конец каждой строки)
	4) s - . значит все символы включая перевод строки \n
	5) u - полная поддержка юникода
	6) y - поиск на конкрентной позиции в тексте

111) У строк есть встроенные функции для работы с РВ:
	let str = 'Lorem ipsum dolor'
    	str.match(/sum/gi) => ['sum', 'sum'] - поиск совпадений
	str.matchAll(/sum/gi) - поиск совпадений, более лучщая поддержка карманов
	'123-456-789'.replace(/-/g, ':') => '123:456:789' - замена по рег. выражению, работает с карманами
	/регулярное выражение/.test(str) => true - проверяет есть ли совпадение
	str.split(/рег. выр./) - функция split разбивает также по РВ, напр. '12, 34, 56'.split(/,\s*/)

111)    \d - любая одна цифра (digit)
	\s - пробел, табуляция \t, перевод строки \n, \r  (space)
	\w - латиница, цифра или _ (word) // русские не ищет, для русских либо /[А-яЁё]+/g , либо /\p{L}+/ug
	\D - не цифра
	\S - не пробел,
	\W - любой символ, кроме \w
	. - любой символ, включая пробелы, но не включая перевод строки \n
	    как раз для этого есть флаг s - тогда точка вообще любой символ
	\p{категория} - поиск по категории символа в юникод (нужен флаг u), где категории, например:
		L - буква в любом языке (любого регистра, латиница, кириллица, арабская и др.)
		N - цифра в любом языке (арабская, латинская, римская и др.)
		P - знаки пунктуации
		S - символы (валюта, математика и др.)

Примеры: 'Ах! Все, сделали?'.match(/\p{P}/ug) => ['!', ',', '?']
	 '+7-951-44-66 33 2'.replace(/\D/g, '') => 79514466332
	 'Роднов Денис Анатольевич'.match(/\p{L}+/ug) => ['Роднов', 'Денис', 'Анатольевич']

111)    startsWith/endsWith - встроенные методы проверки начала и конца строки

	Регулярные выражения позволяют использовать флаги, группировки
	^ - начало строки 
	$ - конец строки
 	^...$ - полное совпадение строки
	\b - граница для \w: должно быть \w с одной стороны и "не \w" – с другой. Кириллица не работает.
	     например: 'lorem ipsum!'.match(/\bipsum!\b/) => null
	               'lorem ipsum!'.match(/\bipsum\b/g) => ['ipsum']
	     Можно искать цифры в ряд без пробелов: \b\d{15}\b - 15 цифр подряд, не меньше и не больше
	
	     	

111)    arr.join() - соединить массив в строку с указанным разделителем
	[1,2,3].join() => 1,2,3,4
	[1,2,3].join('') => 1234
	[1,2,3].join(' ') => 1 2 3 4
	[1,2,3].join('-') => 1-2-3-4

	str.split() - разделить строку в массив по указанному разделителю
	'Lorem ipsum'.split() => ['Lorem ipsum']
	'Lorem ipsum'.split('') => ['L', 'o', 'r', 'e', 'm', ' ', 'i', 'p', 's', 'u', 'm']
	'Lorem ipsum'.split(' ') => ['Lorem', 'ipsum']
	'Lorem-ipsum'.split('-') => ['Lorem', 'ipsum']

111) Магические константы php:
	__FILE__ - Полный путь и имя текущего файла
	__DIR__ - Директория файла, без последнего слеша
	__CLASS__ - Имя класса
	__METHOD__ - Имя метода класса
	__NAMESPACE__ - Имя текущего пространства имён
	__LINE__ - Текущий номер строки в файле

111) В phpstorm можно одновременно создавать папку и файл внутри:
	Создаем файл и в названии пишем: pages/about.php (либо без php если выбрали тип файла)

Также работает для консоли: php artisan make:controller Test/TestController

111) ctrl + alt + L - автоформатирование кода

111)  ul>li*5>a>{$$} - таблица с 5 li c ссылками с возрастающими анкорами

111) Для того, чтобы убрать нижний инлайновый отступ у картинок достаточно:
	img {vertical-align: top}

111) В фигме выделение нужного элемента: ctrl + click, не нужно кликать много, чтобы пройти от всех родителей

111) На сайте webfonts.pro нашлись шрифты, которых не было в гуглфонтс

111) Фигма показывает все шрифты макета в предустановленном плагине Font Fascia

111) <img src="http://via.placeholder.com/300x200"

111) онлайн экстрактор css: http://extractcss.com // хоть не выделяет весь css при копировании, но копирует все

111) Shift + Ctrl + \ - скрытие левой панели в фигме (Figma)

111) @import url('style.css') стилей css должны быть в самом верху, иначе не работает
     Пишут, что при импорте сначала грузится одна страница, она запрашивает другую и т.д., то есть дольше,
     но можно пренебречь.

111) Пишут, что SASS более мощный чем SCSS

111) Дебаг (Live edit) перестает работать если перезагрузить страницу

111) Способ дать отступ всем кроме последнего элемента: 
	ul li + li {margin-left: 30px}
+ выбирает соседа на одном уровне вложенности идущего сразу после

111) Способ дать отступ всем элементам кроме первого:
	ul > * + * {margin-left: 30px;}
> - выбирает прямые потомки ul

111) Приоритет селекторов: 
	теги: 0001
	классы: 0010
	id: 0100
	инлайновые стили: 10000
	если вес одинаковый, то приоритет имеет селектор объявленный последним
	important перебивает все стили

Класс родителя в селекторе имеет такой же вес, как и класс самого элемента, пример:
   <div class="parent"><p class="child">Текст</p></div>
    p.child {стиль 1} // вес: 011
   .parent p {стиль 2} // вес: 011 - будет применен!!! так как объявлен последним

Самое просто решение таких ситуаций: задавать элементам свои инд. классы и навешивать стили на них, 
так как вложенные селекторы создают неочевидные веса.
Один из приципов БЭМ: порядок применения стилей не должен влиять на итоговые стили.
	
111) Для инлайнового элемента нельзя задать: 
	1) width и height
	2) margin-top и margin-bottom
	3) margin: auto (но можно использовать 90%)
     Можно задать: 
	1) любой padding
	2) margin-left и margin-right в пикселях, процентах
		
111) Изменение цвета кнопок при наведении можно делать изменением opacity

111) Центровка элементов хедера: лого, nav, кнопка. Делаем display: flex. 
     По умолчанию будет justify-content: flex-start;
     Для кнопки задаем margin-left: auto, она уезжает вправо до конца.
     Для лого задаем нужный margin-right, nav двигается на нужное расстояние в центр.
     
111) Размер контейнера и пэддинги боковые: 
	Допустим по макету контент занимает 1370px.
        Контейнер делаем 1400px и даем боковые пэдинги 15px.
	В pixelperfect макет ложится идеально в мастштабе 1.0

111) Блокировщики рекламы могут блокировать блоки с классами advert (с англ. объявление), promo

111) Только тот сайт, который редактируется и пополняется - становится лучшим.
     Поэтому на сайтах нужно предусмотреть возможность гибкого редактирования и хорошую админку.

111) Текст параграфов оборачиваем не просто в див, но и в p, чтобы новые параграфы вставлялись c отступами.

111) Фоновые картинки в cms удобнее менять, если они стоят тегом img, а не прописаны в css бэкграундом

111) Допустим есть часто повторяющаяся кнопка c классом btn <a href="#" class="btn">Нажать</a>
     По бэм методологии ее нужно обернуть в доп. див, 
     чтобы внешние отступы от контента задавать обертке, а не самой кнопке

111) Если центрируем внутри кнопки надпись с помощью .dfc; , то нужно ставить display: inline-flex,
     тогда кнопка по прежнему инлайновая и все отцентрировано. 
     Если сделать ее flex, то она становится блочной и растягивается.

111) Код цвета в фигме можно определить так: тыкаем на любой текст, слева в Fill -> квадратик с цветом -> пипетка
     Например для цвета по умолчанию пока грузится фоновая картинка

111) max-width/max-height блоку задается, чтобы при адаптации, уменьшении экрана он тоже уменьшался

111) min-width/min-height задается, чтобы блок увеличивался, если в нем появляется больше элементов/текста

111) Можно в резет устанавливать свойство inherit элементам, чтобы дочерние теги наследовали от класса родителя,
например теги p наследовали стили от родительского div

111) &nbsp; - неразъединяемый пробел, вставлять там где не нужен перенос

111) При установке display: flex элементу он и его элементы становятся блочными, но для внутренних задаются 
значения по умолчанию:
	flex-grow: 0 - элементы не растут и занимают размер по содержимому
	flex-shrink: 1 - элементы сужаются при недостатке места
	flex-basis: auto - элементы занимают размеры по содержимому
	align-content: stretch - если размеры не заданы, то по поперечной оси растягиваются до размеров родителя

Иначе говоря, если надпись в кнопке отцентровали через .dfc, то она становится блочной и растягивается.
Если же ее диву-обертке дать класс flex, то так как она становится элементом флекса,
то ее flex-grow: 0 => она занимает размер по содержимому

111) Размещение фона картинкой в верстке, нужно для того, чтобы через cms меняли фоны:
     .img-wrap { // див обертка для картинки
    	position: absolute;
    	left: 0;
    	top: 0;
    	width: 100%; // размеры можно менять
    	height: 100%; // размеры можно менять
    	background-color: #E9EBE8; // фон на время загрузки картинки
    	z-index: -1;} // чтобы текст дива родителя был поверх картинки

     .home-bg-img img {
    	width: 100%;
    	height: 100%;
    	object-fit: cover;}
	vertical-align: top; // это свойство можно добавить, чтобы не было нижнего отступа если картинка не блочная

111) Браузер начал уменьшать поле html больше чем уменьшенный экран при адаптиве, добавил user-scalable=no
и все заработало. Но в универсальной настроке его нет. Отвечает за возможность масштабирования пользователем.
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
Где: initial-scale - насколько увеличить/уменьшить сайт при загрузке в браузер
     maximum-scale=1.0 - во сколько раз пользователь может увеличить сайт касаниями
     user-scalable - разрезаем ли пользователю увеличивать сайт касаниями

Универсальная настройка: 
	<meta name="viewport" content="width=device-width, initial-scale=1">

111) object-position по умолчанию 50% 50%.
     Можно двигать изображение даже за пределы полей элемента.

111) Эти символы в регулярных выражениях нужно экранировать: [ ] \ ^ $ . | ? * + ( )

111) [xyz] - искать один из символов

111) [a-z] искать один из символов в диапазоне

111) /[ф]/i - любой регистр, правильно работает для кириллицы, замена [А-я]

111) [А-я] - первая заглавная, иначе не работает

111) [\s\d] - либо пробел, либо цифра

111) \d, \w, \s - сокращенная запись для наборов [0-9], [a-zA-Z0-9_], [\t\n\v\f\r ]

111) Поэтому \w не ищет кириллицу

111) [^a], [^0-9кнд] - любой символ за исключением всех после шляпки

111) Два варианта исключения в рег. выражениях: 
	1) [^...]  - через квадратные скобки и шляпку
	2) \D, \W, \S

111) Внутри [] квадратных скобок не нужно экранировать спецсимволы, 
     если только они не начинают означать что-то другое.
     [.,] - в квадратных скобках точка означает точку
     [a\-z] - ищем a, тире, z

111) Диапазоны, отрицания, один из в JS создаются только через []

111) Квантификаторы:   
	\d{5} - 5 чисел в ряд без промежутков
	\b\d{5}\b - 5 чисел в ряд исключая числа длиннее
	\d{3,5} - от 3 до 5 чисел
	\d{3,} - от 3 и более чисел
     Сокращенная запись:
	+ - 1 и более - {1,}
	? - 0 или 1 раз - {0,1}
	* - 0 или более - {0,}

111) По умолчанию квантификаторы +, ?, * находятся в жадном режиме
     +?, ??, *? - переключение в ленивый режим
	
111) При квантификаторе движок продвигается по регулярному выражению по порядку:
	Первый символ рег. выр.:  захватывается макс. кол-во удовлетворяющих символов -> результат запоминаем
	Второй символ рег. выр.: с конца результата отбрасываем символы, пока не начинает удовлетворять

111) ? - переводит квантификаторы в ленивый режим
         поиск идет не до конца строки, а до первого подходящего совпадения для квантификатора ->
         начинаем проверять следующий символ рег. выражения -> если символ не подходит, то шаг назад
	 и квантификатор захватывает новый символ и т.д.

Иначе говоря в обычном режиме квантификатора поиск идет отсечением конца предыдущего результата 
-> захватывается наиболее длинное подходящее выражение:
Ищем слово в кавычках: 'Слова: "будить" и "Будда" имеют общие корни'.match(/".+"/) => "будить" и "Будда"

В ленивом режиме: по одному символу -> захватывается первое подходящее
Ищем слово в кавычках: 'Слова: "будить" и "Будда" имеют общие корни'.match(/".+?"/) => "будить"

Ленивый квантификатор часто можно заменить, например для примера выше: "[^"]+"
	
111) () - скобки группируют символы вместе, нужны для:
	1) применения квантификаторов для группы символов '01:32:54:67:89:AB' => /^[0-9a-f]{2}(:[0-9a-f]{2}){5}$/i
	2) помещения в карманы
	3) в работе оператора или | для группы символов Lorem (ipsum|dolor) sit

111) (go)+ - квантификатор применяется ко всему соответствию в скобках => gogogo

111) Помещение в карман: 
	let str = 'sss xaaa-bbbx zzz sss xaaa-bbbx zzz';
	let res = str.match(/x(a+)-(b+)x/);
	Нашел одно совпадение и разбил на карманы:
	console.log(res[1]); // выведет 'aaa'   - 1 карман
	console.log(res[2]); // выведет 'bbb'   - 2 карман

111) В самом регулярном выражении можно обращаться к карманам через \номер_кармана 
     или через именованный карман (именуем: ?<имя>, обращаемся: \k<quote>) 
	let str = 'lorem ipsum lorem ipsum'
  	let regexp = /(\w+)\s(\w+)\s\1\s\2/
  	regexp.test(str) => true

111) Внимание: в регулярках к карману обращаемся черех \номер, а в replace через $номер

111)  (?: ) - несохраняющие скобки, они группируют, но не кладут в карман.
	      Например, в выражении пользуемся карманами и нужно что-то сгруппировать, чтобы не попало в карман

111) (?<name>рег выр.) - помещение в именованный карман

111) Отличие match от matchAll заключается в способности разбивать совпадения на карманы.  

	Здесь примеры с именованными карманами, но это не важно, 
	без имени карманы просто лежат в общем массиве как res[1], res[2] и т.д. 

	match без флага g найдет первое совпадение с регуляркой и разобьет его на карманы // res.groups => {year: '2019', month: '04', day: '30'}
	match с флагом g найдет все, но ничего не разобьет по карманам  // ['2019-04-30', '2019-04-30']

	let str = "2020-05-10 2019-04-30";
  	let res = str.match(/(?<year>\d{4})-(?<month>\d{2})-(?<day>\d{2})/)
	res.groups => {year: '2020', month: '05', day: '10'}
	res.groups.year => '2020'
	
	matchAll - с флагом g найдет все, разобьет по карманам и создаст массив объектов res.groups
	matchAll - без флага g  - ошибка

	let str = "2020-05-10 2019-04-30";
  	let results = str.matchAll(/(?<year>\d{4})-(?<month>\d{2})-(?<day>\d{2})/g)
	for (let res of results) {
      	     console.log(res.groups) // {year: '2020', month: '05', day: '10'}, {year: '2019', month: '04', day: '30'}
}	

111) Изменение строки методом replace:
	let str = '1 23 456 xax';
	let res = str.replace(/(\d+)/g, '($1)'); // $ обозначает номер кармана
	res => '(1) (23) (456) xax' // обернули каждое совпадение в скобки

	let str = "2020-05-10 2019-04-30";
	let res = str.replace(/(\d{4})-(\d{2})-(\d{2})/g, 'Год: $1, месяц: $2, день: $3; ')
	res => Год: 2020, месяц: 05, день: 10;  Год: 2019, месяц: 04, день: 30; 
	
	Можно строку поменять местами: 
	let str = "2020-05-10 2019-04-30";
  	let res = str.replace(/(\d{4})-(\d{2})-(\d{2})/g, '$3-$2-$1')
	res => 10-05-2020 30-04-2019
	
	
111) Поиск цветов, может быть 3 или 6 символов, применяем группировку и квантификаторы:
	let str = "color: #3f3; background-color: #AA00ef; and: #abcd";
	let regexp = /#([0-9a-f]{3}){1,2}/gi;	

111) Проверка мак-адреса:
	let str = '01:32:54:67:89:AB'
	let rexexp = /^[0-9a-f]{2}(:[0-9a-f]{2}){5}$/i

111) Поиск любого числа:
	let str = "-1.5 0 2 -123.4.";
	Мой вариант: let regexp = /-?[\d]+\.?\d*/g;
	Вариант Кантора: let regexp = /-?[\d]+(\.\d+)?/g;

111) Разбивка на число, знак, число ариф. операций типа: 1 + 2, 1.2 * 3.4, -3 / -6, -2 - 2
     	let res = str.match(/(-?\d+(?:\.\d+)?)\s?([*/+-])\s?(-?\d+(?:\.\d+)?)/)	
	Результаты: res[1], res[2], res[3]

111) [] - работает как или для одиночных символов или групп (\d, \w)
     | - работает как или с любыми выражениями	

111) gr(a|e)y => то же самое, что gr[ae]y
     gra|ey => либо gra, либо ey
     Люблю HTML|CSS найдёт Люблю HTML или CSS
     Люблю (HTML|CSS) найдёт Люблю HTML или Люблю CSS

111) То есть без скобок | делит выражение пополам от начала до конца

111) let str = "1 индейка стоит 30€";
     str.match(/\d+(?=€)/g) - найди число если за ним стоит Евро => 30

111) let str = "1 индейка стоит 30€";
     str.match(/\d+(?!€)/g) - найди число за которым НЕ стоит Евро => 1 и 3

111) str.replace(рег.выр., на что заменяем) - один из самых полезных методов замены
'123-456-789'.replace(/-/g, ':') => '123:456:789'
'lorem ipsum'.replace(/\w+/g, str => str.toUpperCase()) - для умной замены вторым аргументом может быть функция

111) В фигме тоже можно группировать слои через ctrl+g.
Была картинки в картинке и экспортировались по одной, получилось их сгруппировать и эксп. как одну

111) Текст псевдоэлементов можно прописывать в дата-атрибутах родительского элемента 
     и затем в content обращаться к атрибуту:  
	<h3 data-number="01">Заголовок</h3>
	h3:before {content: attr(data-number);}

111) Итоговые размеры можно посмотреть в Computed.
     Так, если прописали размер в процентах, то в Computed увидим высчитанный размер в пикселях.

111) calc доступен для %, px, rem, em, vw, vh, vmin и т.д.
     
     Доступны четыре стандартных операнда:
	+ — сложение;
	- — вычитание;
	/ — деление;
	* — умножение.

111) В calc операторы сложения и вычитания обязательно с двух сторон должны отбиваться пробелом. 
     Иначе браузер воспримет их как часть числа
	width: calc(100% - 20px); // в умн. и дел. не обязательно

111) calc может применяться везде в css вместо цифры:
	transition: transform calc(0.5s + 120ms);

111) Установка высоты в зависимости от ширины. 
Бывает, что картинки в карточках непропорциональные, 
либо изменяются непропорционально карточке при смене ширины экрана. 
Это исправляется установкой точных пропорций обертке:

1) Трюк с padding-top, который рассчитывается от ширины родительского блока.
	.wrapper {width: 100%; padding-top: 126%;} - теперь высота 126% от ширины обертки, которая сама 
равна ширине родителя. Следует иметь ввиду, что padding-top расчитывается от ширины родителя, 
а не от ширины самой обертки. Поэтому иногда нужно делать две обертки, внешней задавать нужную ширину, 
внутренней ширину 100% и padding-bottom
Так как пэддинг сдвигает дочерний элемент, то изображение абс., обертка рел. (пример для картинки с object-fit):
position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover;

Пример для установки соотношения сторон для видео:
.video-conteiner {
    position: relative;
    width: 100%;
    padding-top: 56.25%;
  }

  .video-conteiner iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

2) aspect-ratio:  
div {width: 100%; aspect-ratio: 0.72;}
aspect-ratio - пропорции ширины и высоты, так aspect-ratio: 1 - Ш и В равны, 
aspect-ratio: 0.5 - В в два раза больше Ш.
Свойство еще поддерживается плохо.
Его заменой может быть padding-bottom в процентах. Будет рассчитываться от ширины элемента

111) Кнопка "играть" на видео:
     У кнопки убираем границы, фон делаем прозрачным.
     Внутрь button помещаем svg

111) Для облегчения навешивания событий js можно кнопку проигрывания положить в затемняющий слой overlay,
     при нажатии кнопки скрывать overlay(скроется и кнопка), при наведении на видео показывать overlay

111) Пустым (напр. фоновым) дивам не забываем указывать высоту. 
     Так пустой див width: 300px или width: 100% будет невидимым, так как высота без содержимого - 0.

     А ширину не обязательно, для блока она по умолчанию 100 процентов от body

111) Если у родителя высота задана в min-height, то дочерний блок с высотой height: 100%; виден не будет,
     решается установкой родителю дополнительно height: 1px:
	.parent { min-height: 300px; height: 1px;}
	.child {height: 100%;}

111) Как мы знаем большинство свойств заданных в процентах вычисляются по отношению к такому же свойству родителя.
Однако, что, если у блока-родителя высота не задана и вычисляется по содержимому?

Высота height дочернего элемента, указанная в %, работает только если для внешнего блока явно указана высота.
Так, если родитель без явной высоты заполняется по содержимому - height, указанная в % для дочерних работать не будет.

Два выхода: 
	1) либо явно задаем высоту родителю
	2) либо дочерний делаем position: absolute и применяем ему высоту в процентах. 

PS: Флексы row по умолчанию align-items: stretch - тянутся во всю высоту.
	
111) Если нужно, чтобы подчеркивание::after было шире слова-контейнера, то даем left: -5px; right: -5px;

111) Двигать ::after можно не только позиционированием left, bottom и др., но и padding для родителя

111) list-style-position: inside; - маркеры списка внутри списка, не выходят за пределы ul // задается li
     list-style-position: outside; - маркеры снаружи ul // задается li

111) ul li + li {margin-top: 20px;} - у li margin-top: 20px; если есть вышестоящая li // интересная замена :not(:first-child)
     .logo + * {margin-top: 82px;} - делаем отступ между logo и любым элементом под ним

111) Меню ul с ссылками не забываем оборачивать в nav

111) Если секции задаем цвет бэкграунд, то ей же задаем color для текста внутри

111) Если у картинки закругленные края, то можно закруглить края ее родителю и сделать ему overflow: hidden;
     Это удобнее в случаях, если есть затемняющий картинку слой overlay, 
     который будет вылезать если border-radius задан только картинке

111) navigator.cookieEnabled - включены ли куки в браузере
     navigator.userAgent - браузер
     navigator.online - браузер онлайн или нет
     navigator.language - язык браузера

     <noscript></noscript> - содержимое тега выполняется, если в браузере пользователя отключен js

111) php функция range(от, до, шаг=по умолчанию 1) - создает массив (цифровой, буквенный, прямо и в обратную сторону)

111) Полезный совет для того, чтобы при отсутствии адаптивной верстки сайт удобно отображался на мобильных устройствах:
	1) Комментируем meta name="viewport" // включается увеличение и уменьшение
	2) Для body прописываем min-width немного больше контейнера, например, 1600px. // картинки не съезжают
     Теперь сайт увеличивается и уменьшается, есть боковой скролл, ничего не съезжает
     При адаптировании, соответственно раскомментировать вьюпорт и убрать body min-width

111) Для того, чтобы контейнер вел себя резиново задаем ему такие свойства:
	.container {
    		max-width: 1400px; // Свой размер.Контейнер на больших экранах будет ограничен. 
   		width: 100%; // при уменьшении экрана контейнер будет сжиматься, не будет скролла
    		padding-left: 15px; // чтобы при уменьшении контент не прилипал к краям
    		padding-right: 15px;
    		margin-left: auto; // выравниваем контейнер по центру
    		margin-right: auto;}

111) unset: сбрасывает css свойство до его унаследованного значения, если свойство наследуется, 
            или до его начального значения, если свойство не наследуется.

111) Если при адаптиве текст заходит на картинку и по цветам получается так, что плохо читается, можно:
	-- сделать картинку фона более прозрачной, то есть добавить для img opacity + можно дополнительно 
	   изменить фон или прозрачность rgba диву-обертке картинки, будет другой эффект, чем один opacity картинке
	-- сдвинуть картинку через background-position или object-position (можно в px, процентах)

111) height: auto, width: auto и др. - свойство auto означает, что будет задаваться по содержимому 

111) Если что-то можно сделать через CSS – лучше делать это через CSS - 
   - поддерживать и развивать его проще, чем JS

111) <dialog> - готовое модальное окно в html

     <dialog>Привет, мир!</dialog> - скрытое окно
     <dialog open>Привет, мир!</dialog> - окно показывается сразу

     <dialog id="elem">
    	Живи долго и процветай!
    	<button id="closeWindow">Закрыть</button>
     </dialog>

     Управление таким окном встроенными js функциями:
     	showWindow.onclick = () => elem.show() - показать
	showWindowAsModal.onclick = () => elem.showModal() - показать как модельное
	closeWindow.onclick = () => elem.close() - закрыть (также закрывается событием submit на внутренней форме окна)
      
     Также для отмены прокрутки нужно указать в стилях:
	body:has(dialog[open]) { overflow: hidden; }

111) У форм есть также method="dialog". 
	Такая форма:
	1) не перезагружает страницу и не отправляет данные
	2) не очищает поля
	3) если она внутри <dialog>, то форма закрывается по submit
	4) ее данные нужно отправить с помощью ajax

     <dialog open>
	<form action="" method="dialog"> -
	<input type="text" name="name">
	<button type="submit">Отправить</button>
     </dialog>

111) Встроенные всплывающие html окошки (тултипы, подсказки и пр.)
     <div id="pop" popover>Подсказка</div>  // при popover="manual" при клике по фону не будет скрываться
     <button popovertarget="pop">Показать подсказку</button>
   
     Кнопка button будет как открывать, так и закрывать подсказку. 
     Также подсказка закроется при клике по фону.
     
     Можно создать отдельные кнопки для открытия и закрытия элемента:
	popovertargetaction="show" - будет только открывать окошко
	popovertargetaction="hide" - только закрывать (например назначить на крестик)

     Стилизация css:
	[popover] - само окошко
	[popover]::backdrop - подложка (оверлей)

     Анимация css:
        @starting-style { [popover]:popover-open { ... }} - начальные стили
	[popover]:popover-open { ... } - стили открытого окошка
        [popover]:not(:popover-open) { ... } - стили при закрытии

     JS управление:
	.showPopover() — открыть
	.hidePopover() — закрыть
	.togglePopover() — переключить состояние

111) Атрибуты формы:
	autocomplete - включает или выключает подсказки для формы. По умолчанию включено. Можно уст у отд инпутов
	novalidate - уст для тега form, отключает проверку полей формы. Например не проверять правильность email, required и пр.
	formnovalidate - то же, но уст для кнопки submit
	enctype - способ кодирования. Для отправки файлов делаем multipart/form-data

111) Браузер запоминает что показывать в подсказках по name инпута.
     Но мы можем указать какие подсказки показывать сами:
	autocomplete="tel" - телефоны
	autocomplete="name" - имена
	autocomplete="current-password" - пароль
	autocomplete="cc-number" - номер карты и т.д.

111) Можно располагать инпуты, кнопки и др. элементы формы вне формы таким образом:
	<input form="item" name="name"> - привязываемся к id формы
	<form action="" id="item"></form> - уст id у формы

111) <form action="">
    	<input type="image" name="coords" src="1.jpg"> - при нажатии на картинку форма отправляет запрос с координатами клика
     </form>				                 например при методе гет будет переход на: ?coords.x=212&coords.y=70

111) Атрибуты инпутов:
	autofocus - фокус на элементе при загрузке страницы
	list - привязывает к инпуту подсказки из datalist
	spellcheck="false" - отключение проверки правописания

111) <input list="cities"> - теперь здесь раскрывающийся список с подсказками

     <datalist id="cities"> - скрытый список подсказок
    	<option value="Москва">
    	<option value="Петербург">
    	<option value="Челябинск">
     </datalist>

111) <textarea placeholder="Первая строка&#10;Вторая строка"></textarea> - &#10; переносит текст в placeholder

111) У textarea можно включать и отключать вставку символов переноса при отправке на сервер
	wrap="hard" - в тексте будут стоять служебные символы переноса строк
	wrap="soft" - текст отправляется одной строкой, это значение по умолчанию

111) Атрибуты кнопок:
	formaction - переопределяет action для формы, данные отправляются на указанный адрес
	formmethod - переопределяет method (post, get)
	formnovalidate - отмена валидации формы

111) Скрытие элементов:
	visibility: hidden - без схлопывания соседей
	dispay: none - соседи схлопываются
	<h1 hidden>Заголовок</h1> - то же самое, что dispay: none
	opacity: 0 - без схлопывания соседей, возможен transition, применяется ко всем потомкам 
		     в отличие от transparent, rgba
	background: transparent, color: transparent и т.д. - без схлопывания соседей
	background: rgba(255,255,255,0); - возможно регулировать оттенок
	elem.remove() - удаляется из верстки в отличии от способов выше, соседи схлопываются
	position: rel, fix, abs + left: -100%, у body: overflow: hidden 
	font-size: 0; - скрывает текст (или color: transparent)
	max-height: 0; overflow: hidden; - еще один вариант

111) Класс для скрытия элемента, но без display:none :
	width: 0.1px;
  	height: 0.1px;
  	opacity: 0;
  	overflow: hidden;
  	position: absolute;
  	z-index: -1;

111) Эталонный пиксель примерно равен 0,26 мм.
     Пиксели монитора имеют другие размеры.
     Браузер рассчитывает по своим алгоритмам как показывать размеры.
     Так хром показывает линию минимум 0.07px, мозилла - 0.08px.
     Думаю зависит также от разрешения экрана.
     У меня 30px = 10 мм, то есть 1мм = 3px

111) Размер шрифта примерно равен  расстоянию от верха самой большой буквы до низа самой маленькой.
     Но хвосты букв gf могут немного вылезать, поэтому line-height обычно делают чуть больше, 
     чем размер шрифта

111) 1em равен размеру шрифта (font-size) своего элемента.

111) Размер в % высчитывается от такого же свойства РОДИТЕЛЯ.

     Например: width: 30%; height: 50%; означает, что ширина равна 30% ширины родителя, 
	       высота - 50% от высоты родителя.
     Но есть исключения:
	1) margin-left, margin-right в % берется от ширины родителя
	2) line-height в % берется от текущего размера шрифта
	3) при position:fixed, процент берётся от ширины/высоты окна
	4) padding-bottom в % берется от ширины родителя
	5) height в % имеет подводные камни: 
		- свойство height, указанное в %, работает только если для внешнего блока указана высота.
		- если у родительского элемента не установлено height, а указано min-height, 
		  то, чтобы дочерний блок занял 100% высоты, нужно родителю поставить свойство height: 1px;

111) 1rem - высота равная font-size тега html.
     По умолчанию html имеет font-size: 16px и его можно переопределять, например 10px => 
     станет удобно рассчитывать размеры элементов в rem

111) vw - % от ширины viewport
     vh - % от высоты viewport
     vmin - наименьшее из (vw, vh), в IE9 обозначается vm // может применяться при переворачивании планшета?
     vmax – наибольшее из (vw, vh)

     Единицы vh и vw более удачны для расчета по ширине видимой области экрана, 
     так как % будет расчитываться по ближайшему родителю или body.
     А у body есть margin 8px(хром) по умолчанию, он отталивается от html на 8px.
     Но можно установить:  body {margin: 0;} и тогда ширина body равна viewport	

111)  Если при уменьшении экрана начинает появляться боковой скролл и непонятно какой элемент влияет,
то можно либо убирать по одному элементу, либо поставить курсор на правый/левый край экрана и прокручивать, 
там где выступ будет оранжевый цвет
     Также можно:
	* { outline: 1px solid red;} - увидим какие элементы вылезают

111) Задавать блокам размеры только в max-width: 500px(%)  и т.д.
     Иначе придется много править при адаптиве, а так они сами уменьшаются
     Избегайте использования фиксированной ширины в CSS, если вы хотите, чтобы ваша тема была адаптивной.

111) css свойство columns делит текст на колонки, например:
	columns: 3 - на три колонки

111) Для инпута можно задавать несколько label

111) Свойство float прижимает элемент к левому или правому краю, при этом:
	 --- работает как margin-left: auto, но работает и для inline элементов, которые становятся блочными
	 --- инлайновые элементы его обтекают
	 --- блочные элементы без float относятся к нему как вынутому из потока
         
111) margin- auto работает только если элемент display: block или flex и ему задана ширина или высота.
     То есть нельзя для span задать никакой margin auto, нужно делать его block или использовать float
     Элемент при display: flex сам становится блочным и его дочерние элемент тоже, им можно.

111)  	Способы прижать к краю: 

	1) margin-left: auto и margin-right: auto работают только для блочных элементов + задать ширину
	   (вертикально не работает, вертикальные margin становятся 0)
	
	2) Внутри флексов работают все margin (left,right, top, bottom): auto  горизонтальные и 
	   вертикальные даже без размеров. Если же задать общий margin: auto, то элемент будет отцентрирован 
	   по центру родителя.
	
	3) float left, right работает для как для блочных, так и для строчных элементов, 
	   соседи их начинают обтекать

	4) Задавать margin в %, например 90%, работает со строчными(по горизонтали) 
           и блочными элементами (вертикаль и горизонталь)

111) Если у блоков нет margin, то они прилегают друг к другу вплотную
     
111) Инлайновым элементам нельзя изменить размер, но можно пэддинг

111) Между инлайновыми элементами будет пробел, если он есть в верстке (другая строка - тоже пробел)

111) inline-block применяется для того чтобы:
	 --- задать размеры строчным элементам
	 --- перенести в одну строку блочные, например li у ul (лучше флексом)

111) vertical-align применяется для выравнивания:
	1) Содержимого td
	2) inline и inline-block элементов в своей строке: 
		такие элементы лежат в общей строке и vertical-align (top, bottom) прижимает их к верху, 
		низу общей строки.
		Это заметно, если элементы разные по высоте.
		middle - элементы выравниваются по какой-то гипотетической букве X в строке
		Очень удобно, что vertical-align можно устанавливать в px и %, соответственно поднимать 
		или опускать элемент в общей строке.
		Внимание: высота строки задается низом и верхом элементов, а не высотой родителя.
		Высоту общей строки можно изменять: 
			--- высотой шрифтов
			--- высотой inline-block элемента
			--- line-height элементов
	3) vertical-align: top для картинки убирает нижний инлайновый отступ

111) Для выравнивания элементов по табличному типу можно задавать им табличные display:
	<form style="display: table">
  	    <div style="display: table-row">
    		<label style="display: table-cell">Имя:</label>
    		<input style="display: table-cell">
  	    </div>
  	   <div style="display: table-row">
    		<label style="display: table-cell">Фамилия:</label>
    		<input style="display: table-cell">
  	   </div>
	</form>

Те же элементы таблицы это обычные элементы с нужным display:
	<table>    { display: table }
	<tr>       { display: table-row }
	<td>,<th>  { display: table-cell }

111) Элемент с display: table-cell может быть полезен для вертикального выравнивания, 
     он может быть вне таблицы и применять для содержимого vertical-align: middle
     Хотя проще превратить его в display: flex; и отцентрировать содержимое.

111) Так как блочные элементы относятся к элементу с float как вынутому из потока (но строки внутри обтекают), 
то для разграничения этих элементов используется пустой <div style="clear:both"></div>

111) float заставляет блочные элементы вести себя как инлайн-блочные и вставать в одну строку.
     Так как они как бы вынуты из потока и их не видит даже родитель, то последним сиблингом надо ставить
     <div style="clear:both"></div>, этот способ делает также, чтобы родитель принял высоту блоков float

111)  box-sizing: border-box; - делает border и padding входящими в ширину элемента, а не расширяющими его,
                                например, если блок разделен на два дива шириной 30% и 70%, то их границы и пэддинги
				ничего не сломают.

111) При уменьшении экрана уменьшаются и блоки флекса, но можно сделать чтобы какой-то блок уменьшался мало, 
а второй больше, просто задаем ему flex-shrink: 6
Помогло когда был блок с фото и блок с текстом, уменьшал размер блока с текстом (шрифт оставался таким же, переносился).

Во флексе элементы по умолчанию: flex: 0 1 auto (flex-grow: 0; flex-shrink: 1; flex-basis: auto;)

111) Ctrl + F5 жесткое обновление с кешем

111) Элементы с любым позиционированием, если не изменены z-index подчиняются правилу: 
     элемент ниже в верстке перекроет элемент выше в верстке.
     Однако z-index ограничен контекстом наложения родителя.

	position: static: 
		1) позиционирование по умолчанию
		2) положение можно изменять через margin,  можно также накладывать друг на друга 
		   через отрицательные margin
		3) z-index не работают
		
	position: relative:
		 1) двигается относительно текущего положения, другие элементы не двигаются
		 2) z-index больше, чем у статических элементов
	         3) можно перекрывать элементы relative назначая им разные z-index
		 4) нельзя одновременно устанавливать противоположные координаты: left-right, top-bottom (у абс можно)
		 5) Блок relative остается блочным (блоки abs и fix ведут себя как инлайн-блочные)

	position: absolute:
		 1) нужному предку устанавливаем relative
		 2) элемент выпадает из потока, другие элементы ведут себя так, как будто элемента нет
		 3) сам элемент без top-b-l-r остается на прежнем месте, нижние в верстке элементы двигаются
		    как будто он пропал
		 4) координаты top/bottom/left/right для нового местоположения отсчитываются 
	            от ближайшего позиционированного родителя или body. 
                    Отступы от левого верхнего угла также можно задавать марджинами и пэддингами.
		 5) абсолютному элементу можно устанавливать противоположные координаты: left-right, top-bottom.
		    Он будет тянуться, например: left: 10px; right: 10px; top: 10px; bottom: 10px;
		    Или растянуть по всему родителю: left: 0, right: 0, top: 0, bottom: 0	
		 6) элемент занимает место как инлайн-блочные элементы, то есть не до конца экрана как блоки.
		    Так, если пустому диву abs назначим высоту 50vh, то мы его не увидим, у него нет содержимого, 
		    нужно устанавливать ширину.
                 7) чтобы нижние элементы не сдвигались можно устанавливать верхним 
		    или нижним соседям нужный margin, padding или вставлять пустой блок с такими же размерами.

	position: fixed:
		  1) это тот же absolute со всеми его свойствами
		  2) позиционирует себя относительно window
		  3) при прокручивании страницы остается на своем месте

	position: sticky:
		  1) Элемент вне области залипания ведет себя как static
		  2) Когда элемент залипает, то ведет себя как fixed
		  3) все просто: .header {position: sticky; top: 0;}
        	  4) Важно! sticky залипает только в пределах своего родителя, пока не достигнет его конца. 
	             Поэтому шапку sticky нужно делать сразу в wrapper
		  5) Важно! sticky не работает если у родителя overflow со значением hidden, scroll или auto
                     Если нужна прилипающая шапка и боковое меню, то для body: overflow-x: hidden;
                     Если просто body: overflow: hidden, то прокрутки на всей странице не будет
    			
111) Для абсолютно позиционированного элемента height: 100% или bottom: 0 всегда равен высоте окна, 
а не документа. 
Так, если мы хотим сделать фон до конца сайта через height: 100% или bottom: 0, то фон исчезнет 
при прокрутке окна. Дело в том, что в CSS 100% относится к ширине внешнего блока («containing block»). 
А какой внешний блок имеется в виду здесь, ведь элемент изъят со своего обычного места?

Поэтому два варианта: 
	1) Либо делаем элемент без позиционирования, тогда height 100% будет до конца документа 
	   (но внешние элементы будут его видеть и находиться сверху/снизу)
	2) Либо делаем элемент fixed: {position: fixed: height: 100vh; width: 100vw; z-index: -10}
	   Элемент стоит на своем месте при прокручивании, но так как fixed и abs ведут себя как инлайн-блочные,
	   то нужно задавать ширину

111) Для абсолютно позиционированного элемента растягивание на весь родитель:
{top: 0; right: 0; bottom: 0; left: 0;} - ведет себя везде одинаково
{top: 0; left: 0; width: 100%; height: 100%;} - если нет box-sizing: border-box + установлен пэддинг => вылезает

Кроме того, width и height имеют приоритет перед right и bottom, то есть такой код:
{height: 100%; width: 100%; top: 0; left: 0; bottom: 0; right: 0;} - будет отрабатывать c такими же косяками 
по пэддингу как  {top: 0; left: 0; width: 100%; height: 100%;}. 

Получается, смысл устанавливать width: 100%; height: 100%; есть только если мы будем менять ширину и высоту, 
она поменяется так как width и height имеют приоритет перед right и bottom.
Ширину, например, менять при выезжающем мобильном меню.

111) Абсолютные, relative и fixed элементы перекрывают элементы static всегда, 
     для того, чтобы находиться под элементом можно просто установить им отрицательный z-index
     Навешивать на перекрываемый relative и увеличивать z-index не нужно.

111) Если нам нужно сдвинуть блок к краю, то можно обойтись без позиционирования, обычным float, 
     тогда текст не будет перекрываться

111) Шаблоны создаваемых файлов phpstorm: Settings/Preferences | Editor | File and Code Templates

111) Горизонтальное выравнивание:	 
		1) text-align центрирует по горизонтали содержимое блока:
			1) инлайн и инлайн-блочные элементы
			2) текст без тегов (нода text)

		2) margin: auto центрирует по горизонтали сам блок, нужно задать ему ширину

		3) позиционированием : {display: inline-block; position: relative; left: 50%; transform: translateX(-50%);}
		
		4) Флексами: display: flex; justify-content: center;
			
111) Вертикальное выравнивание:
		1) Позиционированием: ({position: absolute; top: 50%; transform: translateY(-50%);})
		2) Позиционированием строки с применением line-height: {position: absolute; top: 50%; margin-top: -0.625em;}
		   Высота строки по умолчанию 1.25em, соответственно поднимаем: margin-top: -0.625em;
		3) Строку выравниваем по центру делая line-height равным высоте родителя, 
	           так как line-height наследуется, то его можно задавать родителю
		4) Родитель - display: table-cell; vertical-align: middle;
		5) Флексами: display: flex; align-items: center;
		6) Флекс родителю, а дочернему: margin-top: auto, margin-bottom: auto
				
111) Самые удобные варианты расположения по центру:
	1) Флексами:
		display: flex;
                justify-content: center;
                align-items: center;
	2) Флекс родителю, маржин авто дочернему:
		.parent {display: flex;}
		.child {margin: auto} // потому что внутри флекса работают все margin: auto
	3) Позиционирование + трансформ:
		position: absolute;
        	left: 50%;
        	top: 50%;
        	transform: translate(-50%, -50%);
	4) Гридом: 
		display: grid;
  		place-items: center;
	5) https://doka.guide/recipes/center/ - другие способы

Флексы удобны тем, что применимы когда высота родителя и элемента неизвестны + не надо позиционировать

Внимание, при одновременном позиционировании и назначении display: flex возможны подводные камни, так как
display: flex переназначает display как block

111) line-height задает коэффициент к высоте текущего шрифта:
     	line-height: 1 - высота строки равна font-size
    	line-height: 100% - высота строки равна font-size

     Либо высоту абсолютную высоту: line-height: 20px

111) Размеры картинок через js нужно получать через window.onload или DOMContentLoaded для document, 
так как иначе скрипт отработает когда картинка не загрузилась и размеры будут 0

111) Бэкграунд, чтобы изображение фона входило в див:
  	background-size: contain; // чтобы растягивалось пропорционально background-size: cover;
	background-image: url("key.png");
	background-repeat: no-repeat;
	background-position: center;

111) background-color: red; background-image: url("") - будет и картинка и фон

111) В браузере line-height: normal соответствует примерно  1.25, 
     
111) line-height рекомендуется задавать в коэфициентах или процентах к шрифту, чтобы при изменении font-size 
     автоматически подстраивалась высота строки

111) font: 20px/1.5 Arial,sans-serif; - сокращенная запись

111) font-size наследуется, у корневого тего html он по умолчанию 16px.
     Соответственно все шрифты по умолчанию 16px.

111) Мы можем задавать font-size в процентах. Так: font-size: 125% означает 125% от текущего шрифта.
     Так как свойство наследуется от родителя, то font-size: 125% дочернего тега берется от размера font-size
     родительского, если у него шрифт тоже в процентах, то каскадом задавать не получится. 
     Для прикрепления к одному неизменяемому размеру можно использовать rem, который считается 
     от font-size у html  - 16px.

111) Затемнение всего кроме фокусируемого инпута https://coverr.co/

111) В svg рисуют с помощью кривых безье. Каждая группа - отдельная линия. Перекрасить линии можно добавляя
свойство fill="#fff" в path

111) vertical-align: top убирает нижний инлайновый отступ у картинок и видео в том, числе

111) html тегом <object> можно встроить на страницу PDF файл

111) Готовый втроенный в html аккордеон:
	<details>
        	<summary>Открыть аккордеон</summary>
        	<p>Скрытый текст Скрытый текст Скрытый текст</p>
    	</details>

111) Запустить видео: videoFile.play(), где videoFile - нода видео
     Остановить видео: videoFile.pause()
     Показывает на паузе ли: videoFile.paused

     Своя кнопка в центре видео, запускающая видео + слушатель клика:	

	if(videoFile.paused) {
		videoFile.play()
		videoIcon.src = 'img/pause.svg'
	} else {
		videoFile.pause()
		videoIcon.src = 'img/story/play-white.svg'
	}

111) По нажатию иконка Играть меняется на Пауза. 
     Для того, чтобы иконка менялась нужно не забыть указать в теге img width и height

111) Текст по умолчанию находится в состоянии white-space: normal - переносится на пробелах, 
     если достигает конца контейнера

Способы изменить это поведение:
    А) Текст может вылезти за контейнер:
     	1) <pre> - текст будет выведен так как набран в верстке со всеми отступами, переносами, проблелами, 
                но моноширинным	
	2) white-space: pre - текст будет выведен так как будто в теге <pre>, но своим шрифтом
	3) white-space: nowrap - текст не переносится, пробелы ужимаются
	
    Б) Текст автоматически переносится на пробелах в конце контейнера:
	1) white-space: normal – по умолчанию, пробелы ужимаются, переносы пользователя игнорируются + авт. перенос
	2) white-space: pre-wrap - переносы и пробелы пользователя сохранены + авт. перенос
	3) white-space: pre-line - переносы пользователя сохранены, но пробелы ужаты + авт. перенос. 
			           pre-line можно задать чтобы переносилось как заполнил пользователь в textarea админки
    
Свойство overflow-wrap: break-word позволяет перенести буквы длинных слов на новую строку, если это слово не влазит 
в ширину контейнера. По умолчанию: normal - выходит за пределы контейнера

111) Отмена переноса для частей строки:
	1) &nbsp; - non breaking space
	            Пример: 200&nbsp;₽
	2) &thinsp; - тонкий неразрывный пробел (короче чем &nbsp;)
		    Пример: 15&thinsp;000&thinsp;000 рублей
	3) <span style="white-space: nowrap">такого уровня…</span>
	
	4) Для больших текстов есть авт. расстановщики неразрывных пробелов:
		artlebedev.ru/typograf/
		typograf.ru/

111) Обрезка текста и добавления многоточия:
	text-overflow: ellipsis; // работает только если есть следующие два свойства
  	white-space: nowrap;
  	overflow: hidden;

     Возможно понадобится задавать overflow: hidden; для родителей, если элемент может растягиваться по родителю
	
111) outline - самая внешняя граница, при любом box-sizing растет наружу, не двигает элементы и не влияет на размер
     border при box-sizing по умолчанию растет наружу и меняет размеры элемента
     border при box-sizing: border-box; растет внутрь и не меняет размеры элемента

     То есть outline задается когда хотят задать рамку без изменения других css свойств. То же самое делает 
     border при box-sizing: border-box, но он ужимает содержимое элемента.
     outline же, растущий наружу просто перекрывает соседние границы

111) В отличие от border outline можно задавать только со всех сторон разом. Не существует outline-top ...

111) outline-offset: 10px; - outline отступает на 10px  от границы элемента

111) Outline часто используют чтобы выделить элемент при hover, focus и т.д. чтобы ничего не прыгало

111) box-sizing: 
	1) content-box - по умолчанию, при добавлении пэддинг и border элемент увеличивается, 
	                 но width/height не меняются и обозначают то, что находится внутри padding
	2) border-box - пэддинг и border растут внутрь, width/height обозначают внешние границы элемента

111) margin-top и margin-bottom у блочных элементов схлопываются. Как пример: абзацы <p> у которых по умолчанию
есть вертикальный и горизонтальный отступ - схлопываются для удобства верстки.
Горизонтальные margin не схлопываются.

111) отрицательные margin двигают элемент и тянут нижние элементы за собой.
     position: relative элементы за собой не тянет

111) Отрицательный margin-top двигает сам элемент вверх
     Отрицательный margin-bottom двигает нижний элемент к себе вверх
     Отрицательный margin-left двигает сам элемент влево
     Отрицательный margin-right двигает правый элемент к себе влево

111) Margin и padding, заданные в процентах, вычисляются относительно ширины контейнера

111) Символам юникод (напр. &#10094; - стрелка)	можно задавать те же свойства, как и шрифтам: color, font-size,
font-weight и т.д.

111) border-radius можно задавать углам по отдельности (начиная с левого верхнего по часовой) 
    border-radius: 5px 0 0 5px;

111) при position: absolute элемент становится как инлайн-блочный. 
     Мы можем сделать его блочным сделам ширину 100%

111) Отступы абсолютно позиционированному элементу от верх-лев угла родителя можно задавать 
     назначая ему пэддинги и марджины

111) Переходы transition не запускаются для элементов, у которых одновременная смена display:none -> display:block, 
однако работают анимации.  Подробнее: 
Транзишн с прозрачностью не работал в слайдере, где менялся также display:none -> display:block :
	1) .none {display:none; opacity: .4; transition: .5s linear}
	2) .visible {display:block; opacity: 1}
Блок резко появлялся.

Однако решилось установкой перeхода через анимацию, когда становилось display: block, то она запускалась:
 .slides {
        display: none;
        animation: fade;
        animation-duration: 1.5s;
    }
    
    @keyframes fade{
        0% {
            opacity: .4;
        }
        100% {
            opacity: 1;
        }
    }

Однако есть костыль:  setTimeout(()=>{div.style.opacity = 1}, 0.1);  

ВАЖНО: более элегатное решение плавного появления: переход работает с visibility:
	.elem {visibility: hidden; opacity: 0; transition: all .2s;} 
	.elem:hover {visibility: visible; opacity: 1;}
Так как visibility занимает место, то позиционировал абсолютно такой элемент, чтобы селект появлялся поверх нижнего блока, а
не свигал его по умолчанию.

111) emmet via создает картинку 300*300

111) У инлайн и инлайн-блочных элементов предусмотрен запас снизу под хвосты букв

111) Решения нижнего отступа у картинок (строчных элементов) внутри контейнера:
	1) Сделать картинку блочной или inline-block: img {display: block} // под блочным элементом ничего не резервируется
	2) Прописать картинке img {vertical-align: top;} // шрифт в диве-обертке поднимается по верхнему краю, 
							    а высота картинки будет выше буквы с хвостом
	3) Сделать обертке line-height: 1px; - этот способ работает также если картинка меньше буквы с хвостом.
                                               Например, шрифт по умолчанию: 16px. Вместе с хвостами 
					       это где-то 19px.  У картинки 18px выровненной по верху все равно
					       будет нижний отступ, поэтому просто уменьшаем высоту строки обертке.

111) Блок без назначенных размеров подстраивается под растущее содержимое. Однако, если размеры ему заданы явно, 
например: div {width: 200px; height: 80px;} то при переполнении содержимое начинает выходить за его границы.
Как это регулируется:
	1) overflow: visible // по умолчанию, содержимое отображается за границами блока
	2) overflow: hidden //	переполняющее содержимое не отображается
	3) overflow: auto // при переполнении показывается полоса прокрутки
	4) overflow: scroll // верт и гор. полосы прокрутки показывается всегда
	5) overflow-x/overflow-y // overflow для горизонтали, вертикали  

111) в php при выводе сложной переменной в двойных кавычках, например с другим текстом: 
echo "Ширина: {$this->width}" ее нужно группировать фигурными скобками

111) $var равен $var1 или null, если $var1 не существует:
       $var = $var1 ? $var1 : null
       $var = $var1 ?? null // сокращенная запись

111) CSS селекторы:
    Примеры:
	[name="value"] - селектор на атрибут
	:visited - селектор на псевдокласс
	.c1.c2 - одновременно два класса

     Отношения потомков:
	div p - элементы p, являющиеся любыми потомками div
	div > p - только непосредственные потомки

     Отношения соседей:
	div ~ p - все p, которые идут после div на одном уровне вложенности
	div + p - если p идет первым за div на одном уровне вложенности

     Родитель:
     	label:has(input:checked) - выбирает родителя который содержит элемент (не поддеживает файрфокс)
				   Для того, чтобы хорошо поддерживалось сделал:
					 input перед label и использовал input:checked + label (label как кнопка, a input radio невидимый)

     По месту среди потомков любого типа:
        :first-child - первый потомок своего родителя (li:first-child) // :nth-child(1)
	:last-child - последний потомок своего родителя
	:only-child - единственный потомок своего родителя
	:nth-child(2) - второй потомок своего родителя
	:nth-child(even) - четные потомки // :nth-child(2n)
	:nth-child(odd) - нечетные потомки // :nth-child(2n+1)
    
    По месту среди потомков одного типа - во время подсчета игнорируются элементы с др. тегами:
	:first-of-type - первый потомок такого типа
	:last-of-type - последний потомок такого типа
	:only-of-type - если единственный потомок такого типа
	:nth-of-type(3) - третий потомок такого типа

    Селекторы на атрибуты:
	[width] - установлен атрибут, неважно с каким значением
	[name = "text"] - атрибут строго равен 
	[href ^= "http:"] - атрибут начинается с
	[data-radius $= "px"] - атрибут заканчивается на "px", например data-radius = "20px"
	[class |= "my"] - атрибут либо равен my, либо начинается с my-
	[value *= "тел"] - атрибут содержит подстроку "тел", например равен "телефон" или "тел"
	[class ~= "item"] - атрибут содержит отдельный item, например class="elem item" , class="item"

    Селекторы на псевдоклассы:
	:not(.class) - все кроме, например: input[type="checkbox"]:not(:checked)
	:focus - если в фокусе
	:hover - при наведении
	:empty - пустой, без детей, даже текстовых
	:checked - отмеченный инпут
	:disabled - отключенный, например <input type="text" disabled>
	:enabled - включенный, с которым можно взаимодейстовать
	:target - подсвечивает элементы у которых id совпадает с хешем в url
	:invalid - неправильно заполненный <input>, <select> и <textarea> (напр. не соответствует type="email")
	:valid - правильно заполненный
	:required - инпуты с атрибутом required, то же самое [required]
	:optional - инпуты у которых нет required
	:read-only - элементы у которых есть disabled или readonly
	:read-write - доступные для изменения, т.е. у которых нет disabled или readonly
        :placeholder - стили самой подсказки
        :placeholder-shown - выбирает <input> или <textarea> у которых видна подсказка (плейсхолдер) 
                              :placeholder-shown { text-overflow: ellipsis; } - длинная подсказка обрежется многоточием

    Селекторы псевдоэлементов:
	:before, :after - выделяет псевдоэлементы, современная запись с двумя :: , 
			  но такую не поддерживаем IE8 

    a:has(img) - если ссылка содержит изображение
    selector:has(.class1, .class2) - выберет родителя в котором есть элементы с любым из двух классов
    selector1:has(.class1):has(.class2) - выберет родителя если внутри него одновременно 
                                          есть элементы с классами (не обязательно в одном и том же элементе)

    :is() - позволяет группировать селекторы
	    div :is(p, span) { color: red;} - покрасятся все p и span внутри div
	    :is(div, p) span { color: green; } - покрасятся все span внутри div и p
	    div:is(.class1, .class2){} - вместо div.class1, div.class2 {}	

    :where() - то же самое, что :is(), только у :is() вес равен внутреннему селектору, а у :where() он равен нулю всегда

    Пример:
	div.meow { color: blue;} - сработает этот
        div:where(.meow) { color: red; } - класс внутри :where не добавляет вес

    :where() и :is() - прощают ошибочные селекторы внутри себя, существующие селекторы будут работать

111) min и max могут быть установлены для date, month, week, time, datetime-local, number и range

     :in-range - введенные данные входят в min и max (валидные)
     :out-of-range - введены невалидные данные

111) Показ ошибки ввода через css:
	<input type="number" min="10" max="15">
	<span>Неверные данные</span>

        span { display: none; } 
        input:out-of-range + span { display: inline; } - если ввели неверно показывается ошибка

111) Например нужно чередовать три цвета для неопр. числа блоков. Тогда:
	.card:nth-child(3n+1) {background: red;}
	.card:nth-child(3n+2) {background: blue;}
	.card:nth-child(3n+3) {background: green;}
     То есть цифра здесь - количество цветов
     А в n подставляются числа по порядку от 0

111) img:not([alt]), img[alt=""] - выделит все изображения без alt

111) Переменные js нельзя писать через тире. Поэтому если id задан с тире, напр. id="widget-title",
     то такая переменная не найдется готовой в window, нужно искать через document.querySelector('#widget-title')

111) js перемешать массив: arr.sort(()=>0.5-Math.random())
     Обычная функция sort поочередно вычисляет разность соседних a-b: первое число со вторым, второе с третьим и т.д.  
     В зависимости от знака разности числа меняются местами. 
     Здесь же на каждом сравнении возвращается случайное положительное или отрицательное число, массив перемешивается.

111) phpStorm плагин для генерирования случайных строк, чисел, массивов: ALT + R

111) forEach в цепочку изменения массива/строки не вставишь, так как он ничего не возвращает, для этого нужен map:
	str.split(/\s+/).map(elem=>{return elem[0].toUpperCase() + elem.slice(1)}).join(' ');

111) функция match помогает переложить строку в массив по регулярному выражению:
	'5321234324234234'.match(/.{1,3}/g) => ['532', '123', '432', '423', '423', '4']

111) Округление JS:
	toFixed(num) - оставляет num после запятой // 1.12345.toFixed(2) => 1.12
     	toPrecision(num) - оставляет в числе num цифр // 1.12345.toPrecision(2) => 1.1
	Math.round - до ближайшего целого // Math.round(3.56) => 3
	Math.floor - до ближайшего целого в меньшую сторону // Math.floor(3.9) => 3
	Math.ceil - до ближайшего целого в большую сторону // Math.ceil(3.2) => 4

111) css спрайт через бэкграунд:
     Делается полоска из иконок, вставляется в нужный див через:
	 background-image: url("img.jpg");
	 background-size: cover;
	 background-position-y: -1px;

     Сдвиг на нужную иконку через background-position-x: -35px;

     Преимущества:
	1) Меньше HTTP запросов к серверу
	2) Можно загрузить изображения, которые понадобятся в будущем
	3) Если схожая палитра, то совмещенный файл будет весить меньше

111) Растровое изображение содержит информацию о расположении и цвете каждого пикселя (.png, .jpg, .gif, .bmp)

111) Для сложных изображений лучше подойдет растр, для простых - svg

111) Достоинства svg:
	1) У svg иконки можно менять цвет при наведении + др. css свойства, у обычных картинок нет
	2) svg иконки меньше весят
     Недостатки: 
	1) изображения из спрайта не кешируются браузерами, при перезагрузке могут дергаться
	2) Если одно изображение из спрайта с ошибкой, то все иконки не показываются

111) Встраивание svg:
	В верстку:
		1) В верстку как src:
			  <img src="equilateral.svg"> 

		2) В верстку как inline:
			 <svg width="100" height="100"><rect width="100%" height="100%" fill="green" /></svg>
			     	- в этом случае для svg можно назначать css стили размеры, цвет и т.д., например:
			    	svg {fill: #c71e1e; width: 50px; height: 50px;}

		3) В верстку через спрайт

		4) В стили бэкграундом: 
			background-image: url("image.svg");

111) SVG спрайты:
	1) Создаем svg файл, где будем хранить массив иконок, например sprite.svg

	   Внутри создаем массив иконок в теге svg: <svg xmlns="http://www.w3.org/2000/svg">.....</svg>.
           Иконки храним в тегах symbol, назначаем каждому symbol -  id.
	   В symbol можно хранить содержимое иконки без обертки svg, обычно это <g>, <path>
           В тег symbol переносим viewBox иконки

	      <svg xmlns="http://www.w3.org/2000/svg">
	          <symbol id="instagram" viewBox="0 0 512 512">
                     	<g><path d="M505,257c0,34.8-0.7,69.7,0  z..........."/></g>
                 </symbol>
         	<symbol id="telegram" viewBox="0 0 256 256">
                        <path d="M505,257c0,34.8-0.7,69.7,0  z..........."/>
         	</symbol>
	      </svg>
		   	 
	2) В верстку вставляем иконки через use + добавляем класс, т.к через id у symbol иконка не редактируется:
	          <svg class="inst">
     		       <use href="sprite.svg#instagram"></use> // указываем имя файла и айди иконки
    		  </svg>

	3) Редактируем иконку через класс:
		  .inst {
        		width: 50px;
        		height: 50px;
        		display: inline-block;
			fill: red }
	Если fill не применяется, то нужно удалить его в инлайновых стилях внутри кода иконки

111) css свойства svg:
	fill - цвет заливки иконки (черная по умолчанию, можно делать transparent если нужен только ободок)
	stroke - цвет обводки

111) svg в спрайте при работе через компиляцию scss не хотели показываться, заработали когда вставил в верстку как svg

111) Иконки из svg спрайта не менялись пока не открыл сайт в другой вкладке. До этого не помогал даже режим разработчика без кеша

111) Правила css оформления:
	1) Кажое свойство на отдельной строке
	2) В перечислении каждый селектор на отдельной строке
	3) Свойства, сильнее влияющие на структуру документа, идут первыми:
			position, left/right/top/bottom
			width, height, margin, padding
			border
			list-style-type, overflow
			background, color, font
	
111) Системы сборки соберут в один файл в том числе  @import css

111) Типы данных в MySQL (внимание, в совр. версиях MySQL 255 символов и др. значит 255 символов кириллицы, латиницы)
	CHAR - строка до 255 символов, можно указать длину CHAR(20), если строка меньше этой длины, то заполняется пробелами до 20
	VARCHAR - строка до 65535 символов (до 65 КБ), если указать длину VARCHAR(500), то хранит именно столько + байт на длину,  
		  занимает меньше места, чем CHAR, но подвержен фрагментации и из-за этого может проигрывать в скорости
	TEXT - строка до 65535 символов (до 65 КБ), подходит для хранения больших текстов
	MEDIUMTEXT - текст длиной до 16 МБ

	ENUM('value1', 'value2') - для вставки допустимы только значения из списка

	BLOB - двоичные данные до 65 КБ
	MEDIUMBLOB - двоичные данные до 16 МБ
	LARGEBLOB - двоичные данные до 4 ГБ

	BOOL и BOOLEAN - синонимы TINYINT(1), можно вставлять числа 0, 1 или true, false без кавычек
	
	TINYINT - числа от -128 до 127, занимает 1 байт
	TINYINT UNSIGNED - целые числа от 0 до 255, занимает 1 байт
	INT - целые числа от -2147483648 до 2147483647, занимает 4 байта
	INT UNSIGNED: целые числа от 0 до 4294967295, занимает 4 байта
	BIGINT UNSIGNED - целые числа от 0 до 18 446 744 073 709 551 615, занимает 8 байт
	DECIMAL - дробные числа, нужны параметры precision - максимальное количество цифр (до 65), 
		  scale - максимальное количество цифр после запятой, по умолчанию 0
		  DECIMAL используется для фин. операций, где расчеты должны быть предсказуемы, если много цифр после запятой
	DOUBLE - дробные числа с плавающей точкой двойной точности от -1.7976 * 10308 до 1.7976 * 10308, занимает 8 байт.
		 Быстрее, меньше памяти, но если много цифр после запятой, то неточности округления
	DATE - дата в формате YYYY-MM-DD, занимает 3 байта
	DATETIME - дата и время в формате YYYY-MM-DD HH:MM:SS, занимает 8 байт, года: 1000-9999
	TIMESTAMP - количество секунд с 1 января 1970, занимает 4 байта, года: 1970 - 2038 !!!
	TIME - время в формате HH:MM:SS, занимает 3 байта
	YEAR - год в формате YYYY или YY, занимает 1 байт, года: 1901-2155 или 70-69 (1970 — 2069)

	JSON - JSON-формат

111) слаг (slug) - используется для создания ЧПУ ссылок

111) Чтобы таблица БД при удалении строчек начиналась с id=1 нужно очищать ее через Операции->Очистить таблицу

111) Есть онлайн сервисы форматирования верстки, когда скопированная верстка вставилась без отступов, например:
	https://serblog.ru/demo/format-html/

111) Диалог перед удалением, переходу по ссылке делается через confirm, если пользователь нажмет Отмена, 
     то перехода/отправки формы не будет:

	1) инлайновый js: onclick="return confirm()"
	<a href="delete.php?id=22" onclick="return confirm('Вы уверены?')">Удалить</a>
	
	2) обычный js:
	btn.addEventListener('click', function (e) {
	      let flag = confirm('Вы уверены?')
              if(!flag) return false
              alert('Это выполнилось так как согласились')})

111) Получить адрес страницы из js:
	window.location.href - полный адрес, включая гет параметры
		       .origin - протокол + домен + порт
		       .protocol - протокол (http:)	
		       .host - домен+порт
		       .hostname - домен
		       .pathname - строка пути после домена
		       .search - гет параметры, включая ? (?key1=value1&key2=value2)

111) location - сокращенное window.location

111) location.reload() - перезагрузка страницы с помощью JS

111) <meta http-equiv="refresh" content="3"> - перезагрузка страницы раз в 3 сек. с помощью html

111) <meta http-equiv="refresh" content="2;url=https://www.yandex.ru"> - перенаправление по ссылке через 2 секунды

111) location.href = 'https://url.ru' - перейти по адресу 
     location.assign('https://url.ru')

111) Добавить несколь классов:
	elem.classList.add("first","second","third");
	elem.classList.add(...["first","second","third"]);

111) Удалить несколько классов:
	elem.classList.remove("first", "second", "third");
		
111) Более-менее нормальные всплывающие окна последние https://adminlte.io/themes/v3/pages/UI/modals.html

111) БЭМ примеры https://9elements.com/bem-cheat-sheet/#card+i

111) В селекте multiple, чтобы на сервер отправилось несколько значений нужно писать в имени скобки массива:
	 <select name="tags[]" multiple="multiple"><option>Alabama</option><option>Alaska</option></select>

111) Плавное увеличение высоты элемента, например, в аккордеоне можно сделать:
	по умолчанию: max-height: 0; transition: max-height .2s; // height тоже работает
	при клике на ноду: elem.style.maxHeight = elem.scrollHeight + 'px'

111) scrollHeight - высота элемента вместе со скрытой (в т.ч. непрокрученной частью). 
                    Показывается правильно даже если задать height: 0.
		    В то, время когда clientHeight, offsetHeight в этом случае равны нулю 
		    scrollHeight показывает высоту элемента включая скрытую часть

111) Плавное появление (расширение) элемента можно делать задавая по умолчанию высоту или ширину 0, 
     при событии - с помощью транзишена увеличиваем до scrollHeight/scrollWidth

111) w3schools предлагает элементы по которым кликают (табы, плюсики, моб. меню и др) всегда делать в виде button

111) Часто используют переход при наведении #eee => #ccc

111) Один из способов убрать горизонтальное расстояние между инлайн элементами 
     (там где в верстке новая строка) - float: left;
     Либо лучше в блоки их превратить или .df (то же самое), потому что после float нужен clear

111) Переход с display: none в display: block; на самом деле без проблем работает с навешиванием анимации на opacity:
	@keyframes show {
         0% {
            opacity: 0;
         }
         100% {
             opacity: 1;}}
 
111) Офигенно! Вместо навешивания класса active для показа активного элемента при загрузке страницы 
можно кликнуть на него через js:  
	buttons.firstElementChild.click() // клик размещать после навешивания слушателя событий 

111) Разделять файл подключения к базе, файл функций и файл верстки index.php
     В верстке сверху подключать require_once 'connection.php'; require_once 'functions.php';
     В верстке использовать ТОЛЬКО готовые переменные: 

	<?php foreach ($countries as $country): ?>
            <p><?= $country['id_country'] ?>"><?= $country['country_name_ru'] ?></p>
        <?php endforeach; ?>

111) Скрипт можно грузить в шапке, прописав defer

111) Компилятор SCSS включал предварительно установиви npm install -g sass
     Затем в 	 настройках Tools ->  File Watchers ->выбрал Output path to refresh, 
     Working directory (не знаю, нужно ли было) и Show console - Never   
     Компилировал в ту же папку  

111) Записываем в переменные основные цвета сайта.

     Основные цвета в файле scss сделал через css переменные:
	:root { --color: red; } 
	body {color: var(--color);} // в style.css становилось также body {color: var(--color);}
	

     Можно и через переменные scss, но тогда в style.css будет переменные исчезают, будет body {color: red;}
	$color: red;
	body {color: $color;}

111) Шрифт можно загружать через импорт на самый верх файла css/scss:
	@import url('https://fonts.googleapis.com/css2?family=Work+Sans:wght@400;500;600&display=swap');

111) Интересный подход для контейнеров, контейнеры прописывает как header__container, а в классах находит их:
	[class *="__container"] { стили }
     То есть в дальнейшем можно изменить контейнер для какого-то блока задав полное имя, оно приоритетнее.

111) Добавил эммет logo : <a href="#" class="header__logo"><img src="img/logo.svg" alt="Логотип"></a>

111) maw - max-width
     mah - max-height

111) В scss тоже можно писать внутренние теги:
	.header__logo {
  	        max-width: 200px;
  		img {
    		  max-width: 100%;}}    =>    .header__logo img {max-width: 100%;}

111) В scss повторение тега можно заменять через &, например родитель .menu__link, а внутри &:hover, 
&:before, &:first-child и т.д.
     То есть & используется только внутри скобок родителя и заменяет его имя

111) Подключил скрипт js с русским языком внутри и при просмотре его содержимого через ссылку на сайте 
(посмотреть код страницы и т.д.) были кракозябры, переконвертировал файл 
через notepad++ (Кодировки -> преобразовать в UTF8 с BOM) и стало без кракозябр

111) Подключение визуального редактора Summernote описано в Полезное

111) Удобно! Найти и заменить в phpStorm - CTRL + R

111) Размеры можно задавать в процентах от ширины, высоты  вьюпорта, даже шрифт.
     Так, font-size: 2.5vw означает 5% от ширины вьюпорта, например, при 1000px это 25px
     Это дает уменьшение шрифта в зависимости от экрана без медиазапросов.

111) clamp(<min>, <ideal>, <max>) - css функция для установки ВЫЧИСЛЯЕМОГО свойства, определяющего: длину, 
частоту, угол, время, проценты, число дробное или целое. Заменяет медиазапросы.

По умолчанию будет установлено <ideal>, если же его вычисленное значение будет меньше <min>, то будет установлен
<min>. И также если значение вычислится больше <max>, то будет установлен <max>

На практике это означает, что свойство плавно подстраивается под ширину экрана и др. в опр. границах. Например,
текст уменьшается при измении viewport.

Примеры: 
p {font-size: clamp(1.5rem, 5vw, 3rem);} - текст по умолчанию 5vw, но не меньше  1.5rem и не больше 3rem.
                                           
.logo {width: clamp(350px, 75%, 800px);} - лого занимает 75% контейнера, но не меньше 350px и не больше 800px

Когда происходит перестроение при медиазапросах, например flex-direction,  clamp не применять, адаптируется неудачно.

111) Для создания clamp есть сервис https://min-max-calculator.9elements.com/
     Пишем мин и макс значения свойства и границы вьюпорта, когда должно быть min и max (к примеру 320-1400px; 992-1400px).
     clamp(минимум, -0.042rem + 2.71vw, максимум)

111) Для подстраивания значения по умолчанию меняем значение с vw:
	clamp(0.5rem, 0.114rem + 3vw, 1.563rem);

111) Поменять местами значения переменных через деструктуризацию:

	let a = 10
	let b = 20

	[a,b] = [b,a] // теперь a = 20, b = 10

111) Инлайн элементы не меняют размеры, если им назначать ширину, высоту, пэддинги.
     Так, пытался сделать ссылку как кнопку меню, а фон не выходил за края надписи при назначении пэддингов,
     сделал инлайн-блок и стала нормальная менюшка с большими кнопками.

111) Добавлять классы и смотреть что происходит можно через кнопку .cls в хроме:
     - выбираем элемент
     - нажимаем .cls и вводим класс, теперь его можно убирать-добавлять через галочку

111) Выпадающие списки  можно делать без селектов:
     	<div class='parent'>
	Выбрать город:
	<div id='res'>здесь будет результат</div>
	   <ul class='list'> // выпадающий список - дочерний элемент parent и клики по нему  = клик по parent
	      <li class='item'>Челябинск</li>
	      <li class='item'>С.Петербург</li>
	   </ul>
	</div>

#parent {position: relative;}
.list {position: absolute; top: 100%; left: 0; right: 0; opacity: 0; transition: all .2s; visibility: hidden; pointer-events: none;}
.list.active {opacity: 1; visibility: visible; pointer-events: all; transform: translateY(5px);}

То есть выпадающий список размещен под parent и скрыт. Навешиваем на parent клик. При клике тоглим для list класс active.
Также проверяем target клика: 
if(e.target.classList.contains('item')){ //если кликнули на элемент выпадающего списка
    res.textContent = e.target.textContent // в res прописывается выбранный текст, а список исчезает

111) Боковое выезжающее меню можно делать при помощи позиционирования и left: -100%, но красивее получается, когда
     меню выезжает, а надписи внутри не двигаются, как будто бы меню как ширма своим фоном их показывает. Делается это 
     при помощи установки для меню ширины 0 и overflow: hidden, транзишн; а при классе .active его ширина увеличивается 
     до нужных размеров. Выглядит красивее, чем если надписи тоже ездят c меню.

111) Также можно сделать эффект, как будто выезжающее меню двигает контент, делается добавлением для контента 
margin-left равного ширине выехавшего меню.

111) У body белый фон по умолчанию. Его можно затемнять через background-color: rgba(0, 0, 0, 0.4).
     В этом случае затемняется только фон body, либо другого элемента для которого применяется rgba. 
     Цвета внутренних элементов остаются как были.

111) Абсолютно позиционированные элементы не видят пэддингов внешних контейнеров, поэтому чтобы такой элемент 
не выходил за пределы контейнера при адаптиве можно делать его position: static; 

111) Можно сделать так, чтобы содержимое дива ограниченного размерами можно было прокручивать назначив ему overflow: auto;
     По умолчанию содержимое просто выходит за границы дива, так же его можно прокручивать, например сделать что-то типа
     слайдера с картинками. На видео верстальщик так сделал для логотипов партнеров в мобильной версии. Самом контейнер
     кроме того сделал горизонтальным флексом, а логотипам задал flex: 0 0 auto чтобы не уменьшались.

111) Секрет отрицательных боковых марджинов. 
     Как правило контейнерам дается левый и правый пэддинг, для того, чтобы при уменьшении экрана контент не прижимался
     к краю экрана. Однако, если внутри контейнера есть элементы с паддингами, например для отступа друг от друга, 
     то такие паддинги складываются с паддингом контейнера, получается двойной паддинг между контейнером 
     и элементами прижимающимися к нему. Для того, чтобы этого избежать делают так:

     .container (padding-left: 15px, padding-right: 15px)
	.row (margin-left: -15px; margin-right: -15px;) // теперь между контейнером и block паддинг 10px, а не 25px
           .block (padding: 10px)

     Также это применяется для внутренних блоков с паддингами, чтобы для дочерних паддинг не удваивался им или блоку 
     прокладке дают отрицательный марджин.	

111) Атрибуты:
	disabled - не активирован (напр, инпут)
	readonly - нельзя изменить текст внутри (напр. инпута)
	checked - выбран
	maxlength="" - ограничивает количество символов в инпуте
	novalidate - атрибут тега form, отменяет проверку данных формы браузером, напр. отменяет required
	autofocus - этот инпут получает фокус при загрузке страницы
	form - атрибут указывает форму/формы, которым принадлежит инпут (может быть вне формы)
	min, max - атрибуты работают для инпутов: number, range, date, month, week, time. 
		   Например: <input type="date" min="2000-01-02">

111) Новые теги:
	<var> - для переменных в тексте
	<kbd> - для клавиш клавиатуры (в бустрапе оформлено красивой клавишей)
	<input type="week"> - удобный выбор недели
	<mark>подсвеченный текст</mark> - текст с желтым фоном, исп для справки или обозначений
	<u>Подчеркнутый текст</u> - td underline
	<s>Зачеркнутый.</s> - зачеркнутый текст // похоже на <del>
	<small>Мелкий шрифт</small> - уменьшенный текст, исп для авторских прав и юридического текста

111) checkValidity() - встроенная в браузер функция проверки ипутов.
      
      Пример использования:
	<input type="number" id="input" min="1000">
	<script>
         input.addEventListener('input', function (e) {
		console.log(input.checkValidity())}) возвращает false при каждом вводе пока значение меньше 1000
	</script>
	
      Можно проверять инпуты по атрибутам: required, min, max, pattern, min-maxlength, type

111) Курсы те, что покупал качают с https://supersliv.biz/

111) $res = json_decode(file_get_contents($url), true) - так мы получим из json строки ассоциативный массив, а не объект,
							 можно обращаться к значениям как arr['key'], а не obj->key

111) cursor: wait - крутящийся шар ожидания (progress - вместе со стрелочкой)
     cursor: none; - курсор исчезает

111) https://feathericons.com - svg иконки с возможностью масштабирования и утолщения
     https://fonts.google.com/icons - тоже позволяет менять размер, цвет и иконок больше

111) &times; - крестик для закрытия
     &#9776; - бургер

111) Open, close, top и др. слова не годятся для названия id элементов, так как зарезервированы для window

111) Пэддинг - это внутренняя часть элемента в отличие от margin. Поэтому расстояние до border-bottom 
               можно регулировать нижним пэддингом

111) Если размеры начинают прыгать при наведении и добавлении расширяющих элементов вроде border-bottom и др, 
то можно заранее всем элементам назначить прозрачный border-bottom

111) Ширина блока абсолют, если не задана становится по содержимому. 
     Если устанавливали ее, например, 100%, то чтобы сделать по содержимому делаем: width: auto

111) Iconify - в фигме плагин поиска иконок. Импортируем, Ctrl+G, теперь можно менять цвет, поворачивать, 
               затем экспортируем куда нужно

111) Css переменные можно вставлять и в инлайн css, например цвет в инлайновой svg: stroke="var(--accent-color)"

111) Привязывать события через on можно двумя способами: 
	1) window.onscroll = func
	2) window.onscroll = function() {func()}; // то есть код в фигурных скобках выполнится только при событии
	3) window.onscroll = ()=>{}

111) Свойство height относится к свойствам, которые лучше не анимировать, потому что анимировать желательно всего
     два свойства — opacity и transform. 
     При анимации height браузер начинает перерасчитывать макет, а значит на каждом фрейме анимации будет вызвана 
     перерисовка.

100) Если у блока не задан размер и мы пытаемся его анимировать через 100%, например top: -100% (для исчезающего меню),
то браузер не знает высоты и превращает 100% в auto, а это свойство не анимируется, блок исчезает мгновенно. Поэтому ему
достаточно задать высоту для плавности или лучше использовать elem.style.transform = 'translateY(-100%)' - это работает
и transform не перерисовывается

111) Эффект появляющегося меню при скролле вверх:
	Само меню делаем position: fixed; top: 0; left: 0; right: 0;
	let currentScroll = window.pageYOffset // при загрузке страницы currentScroll = 0

    	window.onscroll = () => { 
        	if(window.pageYOffset > currentScroll){ // window.pageYOffset - текущая прокрутка окна вниз. Когда скроллим вниз текущая прокрутка всегда больше начальной прокрутки
        		nav.style.transform = 'translateY(-100%)'} // поднимает меню вверх
		else { 
			nav.style.transform = 'translateY(0%)'} // если текущая прокрутка меньше начальной прокрутки показываем меню
		currentScroll = window.pageYOffset} // в конце каждого скролла переписываем значение начальной прокрутки
       Колесо мышки отправляет событие скролла на зазубринках (напр. периодичность 100px).

111) Анимировать желательно всего два свойства — opacity и transform, поскольку они не перерисовывают страницу 
на каждом пикселе.

111) transform:
	1) rotate - вращение вокруг transform-origin(X-ось, Y-ось, Z-ось(не обяз)): 
		    left, right, center, top, bottom, center или любые единицы для размеров (отр. и полож.)
		    По умолчанию transform-origin => center
	2) scale - масштаб, также есть transform-origin (X-ось, Y-ось). По умолчанию scale(1)
	3) skew - скос, также есть transform-origin. Пример skew: прямоугольник => параллелограмм
	4) translate - сдвиг элемента по оси X и оси Y, есть translateX и translateY. 
                       Удобен в процентах, например влево на 100%

Свойства transform можно применять через пробел: transform: rotate(30deg) scale(3);
Новый transform перезаписывает весь старый, даже если добавили другое свойство.

111) Если в верхнем блоке абсолютно позиционированный элемент, например, узор накладывается на нижний блок, 
но должен исчезать, то 2 решения:
	1) Абсолютному элементу z-index: -1
	2) Нижнему блоку position: relative, теперь у него есть z-index больше, чем у позиционированных элементов 
           из верхнего блока, так как в верстке он следующий

111) Если нужно, чтобы svg иконки меняли цвет, то нужно их вставлять как тег svg, потому что через <img src='1.svg'>
     к ним не подберешься

111) Элегантное решение для удаления элементов через js: просто делаем длину nodeList родителя = 0, 1 и т.д.
     Например:
	<select>
            <option selected="selected">Выберите язык</option>
	    <option selected="selected">Java</option>
	    <option selected="selected">PHP</option>
        </select>

	let select = document.querySelector('select')
	select.length = 1 // Оставили в селекте только первый option

111) animation-fill-mode - в каком положении останавливается анимация:
	forwards - элемент останется там, где остановился

111) Можно писать двойные кавычки внутри двойных, если экранировать последние:
	return "<?php echo Route::currentRouteNamed($route)) class=\"active\"";

111) родитель.insertBefore(элемент, перед кем вставить) - вставка ноды, обычно после createElement

111) elem.setAttribute('class', 'img-zoom-lens') - установка атрибута элементу

111) result.style.backgroundImage = 'url("1.jpg")' - установка фона через js

111) background-size - позволяет не только вписывать фон в родительский блок (cover, contain) по его размерам, 
                       но и масштабировать (увеличивать/уменьшать) фон (background-size: 1000px 1000px - увеличился как лупой)

111) Определение координат курсора относительно блока, где его лев-верх угол: 0,0:
	x = block.getBoundingClientRect().left - e.pageX - window.pageXOffset
	y = block.getBoundingClientRect().top - e.pageY - window.pageYOffset

	block.getBoundingClientRect().left - расстояние левого края блока от левой границы экрана
	e.pageX - расстояние от курсора до левой границы документа (экран)
	window.pageXOffset - прокрутили ли экран влево и насколько

111) Пунктирная линия через background-image:
	.list__line {
    		background-image: linear-gradient(90deg, black 0%,  black 50%, transparent 50%, transparent 100%);
    		background-size: 6px 1px; // размер фона
    		background-position: center; // выравниваем по центру элемента
    		background-repeat: repeat-x;} // по какой оси повторяем фон

     <флекс> 1_элемент ------- 2_элемент </флекс>

     Соответственно, для линии делаем flex-grow: 1, она будет занимать свободное пространство между элементами флекса

111) linear-gradient(90deg, black 0%,  black 50%, transparent 50%, transparent 100%) - черная линия с 0 до 50%, 
     										       затем прозрачный цвет с 50 до 100%
     background: linear-gradient(45deg, black, red) - равномерный красивый переход от цвета к цвету
     background: linear-gradient(red 30px, blue 50px) - плавный переход от цвета к цвету между 30 и 50px

111) Добавить в Гит:
	Кликаем на папку => git => add
     По умолчанию лаварель разрешал грузить только public и resourse

111) Браузер сохраняет файлы css, js, img в кеше. 
     Если мы изменили эти файлы на хостинге, браузер может все равно использовать файлы из кеша.
     Решения:
	1) Добавления гет параметра к файлу: href="styles.css?v=1"
	2) Удобно добавлять таймстамп: href="styles.css?time=<?= time()" // всегда будет скачиваться браузером
	3) Добавление хеша по содержимому файла, если файл изменили, то хеш другой. Делается в вебпак.
	   Мое решение под вопросом: <= href="styles." . hash_file('md5', 'styles.css') . ".css" ?>

111) Выгрузка проекта Ларавель на хостинг с гитхаба: обычно там нет папки vendor, но есть composer.json
     Загружаем файлы на хостинг
     Создаем в корне хостинга файл алиасов .bash_profile
	alias composer='~/composer/composer.phar'
	alias php='/opt/php80/bin/php' // адрес интерпретатора хостинга может быть другим
     Создаем в корне хостинга папку с композером composer/composer.phar
     Для подключения консоли к хостингу Tools->Start SSH Session->хост, логин, пароль
     Заходим в папку где лежит composer.json через cd и запускаем команду в консоли: composer install
     Установятся зависимости с composer.json.

     Описание есть ниже

111) Подлкючение PhpStorm к хостингу по FTP:
	1) Tools -> Deployment -> Browse Remote host -> три точки -> название соединения
	2) Ввожу хост, логин и пароль - OK -> появляются файлы

111) Команды php, composer, artisan которые ввожу в консоли - это алиасы на пути к файлам php.exe, composer.phar и др.
     Писал эти алиасы в корне на хостинге в .bash_profile:
	alias composer='~/composer/composer.phar' - папка composer с файлом composer.phar лежит рядом с public_html
	alias php='/opt/php80/bin/php' - для запуска команды php через подключенную по SSH консоль

	Внимание! Папки opt не видел у себя в файлах, видимо это папка хостинга с интерпретатором php,
        соответственно на др. серверах может быть другой путь.
	
	При изменении алиасов понадобилось: не забыть залить файл на хостинг, перезайти по SSH заново!!!

	Проверка, что алиасы работают:
	В консоли подключенной к хостингу по SSH пишем: composer -v и php -v

111) Все есть в PhPStorm, не нужно ебать мозг со всякими SSH программами типа Putty.
     Tools->Start SSH Session -> вводим хост, логин, пароль и консоль работает с хостингом

111) cd ~ - попасть через консоль в корень сайта

111) Проекты на гитхабе должны лежать по папкам, у меня сейчас studing - как проект, соответственно рядом создаю 
     проекты других сайтов, можно будет копировать через зеденую кнопку Code

111) www.favicon.cc - создание фавиконок по квадратикам

111) Создание фавиконки
	1) Она должна по максимуму занимать квадрат, желательно в ней красную картинку
	2) https://realfavicongenerator.net/ - создаем, настраиваем вид
	3) Важно! Чтобы не загрязнять корень нужно на этом сайте прописать путь до подпапки
           с иконками, например: /template/images/favicons/
	4) Получаем код, вставляем в шапку
  	5) Иконки грузим в /template/images/favicons/
	6) Проверяем на том же сайте что все установилось

111) <link rel="icon" type="image/x-icon" href="/favicon.ico"> - ссылка для фавиконки, должна быть в корне

111) Браузер кеширует иконку, если поменять может не заработать, как исправить:
	<link rel="icon" type="image/x-icon" href="/favicon.ico?v=2">

111) Режим инкогнито в Хроме позволяет входить на сайты как с чистого компьютера, а при закрытии все сбрасывать:
	1) Браузер не запоминает логины, пароли и др данные из форм, закрываем окно и все сбрасывается
	2) Не запоминает историю посещения сайтов и историю загрузок
	3) Не сохраняет куки, то есть сессии $_SESSION начинаются заново
	4) Отключены расширения, напр. если сайт ругается на блокировщик рекламы, то можно зайти через Инкогнито
	5) На сайты можно заходить с разных учеток через разные окна Инкогнито (т.к. учетки работают через куки)
	6) Общее правило: инкогнито позволяет скрыть ваши действия в интернет от другого человека пользующегося этим компьютером

     Однако:
	1) IP адрес и некоторые другие данные все равно видны для сервера
        2) Инкогнито никак не защищает для интернет-банкинга, покупок и пр.
	3) Куки удалятся, но по др. данным все равно можно отследить компьютер, напр. для рекламы

     Включить Инкогнито: три точки справа сверху -> Новое окно в режиме инкогнито

111) microtime() - php функция, возвращает время timestamp в микросекундах, в отличие от time() - в секундах

111) Если инпут нужен строго по границам ячейки td, то, чтобы не растягивал ее делаем:
	<input size="1" style="width: 100%">

111) number.toLocaleString() - добавит в число пробелы, напр.: 32500 => 32 500

111) https://umfac.ru/ - здесь хорошая прокрутка, плавная, на js видимо
 
	
     

	

	





























	

================================================ Выгрузка проекта Ларавель на хостинг с гитхаба  ========================================================
0) Статья: https://odlord.ru/perenos-laravel-na-hosting-beget-iz-repozitoriya/
0) Не забываем после изменения файлов через PhPstorm нажимать кнопку выгрузки
1) Зеленая кнопка Code для выгрузки, загружаем на хостинг 
2) Создаем файл алиасов, папку composer, грузим туда composer.phar и устанавливаем зависимости composer install по SSH как описано выше
2) Также композер можно попробовать запускать готовый бегетовский: composer-php7.1 install
3) Проверяем в консоли: php artisan (из папки проекта)
3) Создаем на хостинге базу данных
4) На гитхабе файл .env не лежит, так как по умолчанию в исключених, копируем его на хостинг 
  и меняем настройки доступа к БД: DB_DATABASE, DB_USERNAME, DB_PASSWORD
5) php artisan key:generate
6) php artisan migrate --seed 
         Если ошибка, то в AppServiceProvider пишем Schema:defaultStringLength(191)
	 Какие-то таблицы при ошибке могли разместиться, удаляем их через хостинг
7) Дальше решаем вопрос с тем, что по умолчанию хостинг отправляем запросы в public_html, 
  у нас же рядом с этой папкой лежит папка сайта
  Как вариант:	
	1) Удаляем с хостинга public_html : rm -rf public_html (в Бегете удалишь, но нужно подняться на уровень вверх, есть описание)
	2) Делаем симлинк через консоль: ln -s ~/project/public public_html
	3) Или попробовать переименовать public_html в public, раз уж получилось удалить public_html
8) Меняем в .env APP_URL на адрес нашего сайта без слеша в конце: APP_URL=https://co55752.tmweb.ru
   Это адрес для формирования ссылок (на картинки и др.)
9) php artisan storage:link // создает в public ссылку на storage, чтобы грузились картинки
10) в .env  APP_DEBUG=false  и  APP_ENV=production
11) Не забываем, что бд пустая, если не было посевов и картинок нет, начнутся проблемы с путями
    к картинкам(storage и др), с драйверами в filesystems (local, public), загрузкой в нужный storage на сервере картинок, 
   но решаемо

Оптимизация Ларавель в продакшене, взято с главы Развертывание:
	1) composer install --optimize-autoloader --no-dev  // оптимизировали файл автозагрузчика классов Composer, без пакетов для разработки
							       после этого удалились классы, сайт слетел. помогло composer install		              							       
	2) php artisan config:cache  // кэширование настроек в один файл
	3) php artisan route:cache  // кэширование маршрутов
	4) php artisan view:cache // кэширование вьюх
	5) composer dump-autoload –optimize // Тэйлор сказал.

Внимание, теперь данные всегда будут браться из кеша даже при изменении кода, нужно будет перезаписывать кеш при изменениях

Оптимизация работы с БД:
	1) Выбирать только нужные столбцы:
		Product::query()->select(['id', 'title', 'slug'])->get(); - вернется коллекция, в моделях будут только эти поля
		Product::pluck('name'); - вернется массив значений из поля 'name'
	2) Использовать жадную загрузку:
		Products::query()->select(['title'])->with('brand:id,name')->get() - сразу загрузили данные из полей id,name 
										     из связанной таблицы брендов
	3) Cвязь, когда выбирается только последняя связанная сущность по таблице связей:
		function lastOrder(){ $this->hasOne(Order::class)->latestOfMany() } - метод в модели User
		Users::query()->with('lastOrder')->get() - используем в контроллере
 		@foreach($users as $user) {{ $user->lastOrder->total }} @endforeach - теперь в цикле не будет множества запросов							     
	4) Если предполагается пользовательский поиск по полю, то при создании таблицы используем для этого поля индекс:
		$table->index('title');
           Однако следует иметь, что такой поиск будет работать только если:
	       искомое значение полностью соответствует ячейке,         например: ищем ЩЕТКА = в БД лежит ЩЕТКА 
	       искомая часть является началом того, что лежит в ячейке, например: ищем СЧЕТ = в БД лежит СЧЕТНАЯ 	
	   	

12) Если используются очереди queue, то понадобится установить supervisor для авт. перезапуска очередей

----------------------------------------- Как привязать хостинг к PhPStorm ----------------------------------------------------------
	Настройка PhPStorm для работы с хостингом:
	Tools -> Deployment -> Browse Remote host -> три точки -> 
	
	1) Создаю поддомен
	2) Получаю ssl сертификат (от 30 мин)
	2) Создаю папку сайта (любое название) в файловом менеджере, внутри папку public_html
	3) Прилинковываю папку сайта к поддомену на https://cp.beget.com/sites (максимум 10 сайтов на аккаунт) // 10 минут // так и не понял, двумя способами прилинковал, чтобы под папкой был синий url
	4) Создаю фтп аккаунт на https://cp.beget.com/ftp , путь в директорию /testbot.aushra.ru/public_html - это общий аккаунт для всех папок внутри public_html
	5) Настраиваю PhPStorm для работы с хостингом (удобно для проекта в новом окне, так как будет свое соединение):
	    Tools -> Deployment -> Browse Remote host -> три точки -> название соединения
	    FTP , cinedr.beget.tech, логин и пароль от фтп аккаунта, Root path: / , Web server URL: http://cinedr.beget.tech
	6) Теперь клик на название соединения, new: создаю папку, и файл: study/index.php 
	7) В файле есть кнопка справа со стрелкой: Upload current ... - обновляет файл на хостинге
	8) Адрес файла в браузере: поддомен/study/
	9) Можно привязать папку из левой колонки с файлами (не забыть про ignore github):
		Tools -> Deployment -> Configuration -> Mappings
		В Local path выбираю папку на компьютере (C:\Users\Owner\Desktop\studing\без синхронизации с гитхабом\телеграмм боты\бот 1)
		В Deployment path выбираю папку на сервере (/study)  (если выбрать в фтп (pechati/public_html), тогда здесь будет просто /)
		Web path оставляю /
	По одному фтп соединению (оно настроено на общую папку public_html) можно синхронизировать разные папки с компьютера,
	делается добавлением нового пути в Tools-Deployment-Configuration-Mappings. Например, у меня соединены две разные папки 
	с ботами с двумя разными папками в public_html
	10) Тыкаю на папку в левой колонке с файлам правой кнопкой -> Deployment-> использую download и upload для обновления, либо 
	можно через Tools -> Deployment -> Browse Remote Host

111) При ошибке в файле php сайт ничего не будет показывать

111) Если Deployment пакетов произошел с ошибками, не все ушли, то проще заархивировать пакеты и разместить через браузер
в файловом менеджере beget, там есть распаковщик

----------------------------------------------  Как выгружал с компьютера сайт на Ларавель -------------------------------------------------

111) Регистрируем домен, создаем БД

111) Чтобы настройки БД совпадали на локальной версии и на хостинге:
	OpenServer PhpMyAdmin -> Выбираем базу -> Операции -> Переименовать базу данных в -> cinedr_rodnovspb - как назвали в Бегет

111) Меняем настройки домена и БД в .env. Описано для соответствия с Бегет:
	 DB_HOST=localhost
	 DB_DATABASE=cinedr_rodnovspb
         DB_USERNAME=cinedr_rodnovspb
         DB_PASSWORD=mLe1kJ0* - пароль с Бегет

111) Сразу ставим в .env SSL: APP_URL=https://...

111) Меняем настройки доступа к БД в openserver:
	Выбираем базу -> Привилегии -> там где имя хоста localhost -> 
        Редактировать привилегии -> Информация учетной записи -> 
        Имя пользователя: cinedr_rodnovspb, Пароль: mLe1kJ0* - пароль с Бегет +  подтверждаем пароль -> Вперед, выскочит ошибка, но работает
     Теперь для БД cinedr_rodnovspb в OpenServer создался новый пользователь, этого пользователя мы прописали в env, настройки доступа к БД
     теперь совпадают в env в локалке и на хостинге

111) Заходим в локальную БД в openserver -> phpmyadmin, выбираем БД -> экспорт -> быстрый, получаем файл sql

111) Заходим на хостинг, phpmyadmin, переходим в созданную ранее БД -> импорт файла sql

111) Создаем фтп аккаунт на Бегет в разделе FTP:
     Логин, пароль, путь: /pechati-darom.ru  - именно такой, чтобы SSH позволил удалить public_html + галочка SSH

111) Создаем соединение SSH в шторме:
	Tools->Start SSH Session:
	  Host: cinedr.beget.tech
	  Логин и пароль от FTP
          Теперь эта консоль управляет папкой pechati-darom.ru и приложением на хостинге:
	  Проверяем: php8.1 -v 
     Можно также создать в корне .bash_profile и написать алиас:
	alias php='/usr/local/php/cgi/8.1/bin/php' => теперь можно будет писать просто: php artisan и т.д.

     Путь на другом хостинге может быть другим, смотрим где лежат интерпретаторы через composer diagnose

     Команды SSH консоли:
	ls - список файлов, которые не скрыты
	ls -a  - список файлов включая скрытые (скрытые файлы, папки в линкус начинаются на точку)
	ls -la  - список файлов и папок, включая скрытые + информация о файлах (размер, разрешения и пр.)
	pwd - где находимся. Абсолютный путь относительно корня ОС к текущей папке
	cd /папка - зайти в папку
	cd .. - подняться на уроверь выше
	rm -rf * - удалить все файлы, папки, симлинки в папке где находимся. Быть осторожным, проверять pwd где находимся
		   флаги: -r - рекурсивное удаление папок, подпапок и их содержимого, 
			  -f - force, принудительное удаление без запросов на подтверждение 
	mkdir test - создать папку test
	rm test - удалить папку test в текущей директории
	rm myfile.txt - удалить файл myfile.txt в текущей директории
	ln -s public public_html - симв. ссылка. Все запросы с папки public_html будут уходить в папку public. Предварительно
                                   саму папку public_html удалить 

111) Удаляем через SSH консоль папку public_html:
	rm -r public_html/  (если не получилось поднимаемся на уровень вверх)

111) Если сайт новый и еще не делали эти команды на локальной версии, то делаем: 
	php artisan storage:link
	npm install

111) На локальном сайте выполняем команды:
	npm run build
	php artisan key:generate 
	php artisan config:cache

111) Архивируем файлы локального сайта в zip, закачиваем и разархивируем их в pechati-darom.ru
     Можно после соединения FTP через Deployment->Upload to, но это долго

111) Так как запросы в Бегете по умолчанию идут в public_html, то создаем симлинк, перенаправляем их через SSH в public:
        Не забываем удалить public_html, чтобы не было конфликтов  ( rm -r public_html/ )
	ln -s public public_html
        Где первый аргумент public - папка куда ведет ссылка, а public_html - название ссылки. Сервер не найдет папку
        public_html, а найдет символическую ссылку с таким именем и перейдет по ней в папку public

	То есть можно сделать ln -s public_html/public 123 и появится ссылка 123, ведущая в public_html/public
	
     Еще нашел вариант с созданием в корне .htaccess, где задать команду: RewriteRule ^(.*)$ public/$1 [L] - но не проверял

111) Этот пункт делать не надо. Для того, чтобы на сервере появилась ссылка в public на сторадж можно 
     просто эту ссылку отправить с локалки на сервер: Deployment->Upload To (после настройки ftp соединения)

     Прошлый способ создания этой ссылки оставлю на всякий случай:
     Проверить работу, загрузку картинок, стилей. Если все работает, то делать этот пункт не надо. 
     Он здесь был прописан, но начались трудности как сделал его. Возможно симлинк с локалки все же правильно отрабатывает на хостинге
	- Заходим в pechati-darom.ru на хостинге и удаляем прошлый симлинк из папки public (он будет как папка storage)
	- Делаем новый симлинк в SSH: php8.1 artisan storage:link
	
     Также в сети есть способ создания симлинка без консоли, в роутах создаем и запускаем url:
	Route::get('/foo', function () {Artisan::call('storage:link');});

111) Заходим в Управление сайтами, нажимаем на шестеренку у нужного домена, 
	ставим php 8.1
	Редирект с HTTP на HTTPS

111) Проверяем сайт - должен работать, картинки тоже показываются

111) Теперь настраиваем показ файлов сайта в Шторме:
	
     В phpstorm Tools->Deployment->Configuration->+->FTP->придумываем имя->вводим данные:
	Host: cinedr.beget.tech
	User name: логин FTP
	Password: пароль FTP
	Галочку Save password и проверяем: Test Connection
	Root path: /
	Web server URL: http://cinedr.beget.tech

     Во вкладке Mappings:
	Local path: C:\Users\Owner\Desktop\studing\openserver-sites\pechati-darom.ru
	Deployment path: /  
	Web path: /

111) Открываем соединение:
	Tools->Deployment->Brouse Remote Host - справа появляется панель

111) Настраиваем:
	Tools->Deployment->Automatic Upload - галочка (если ошибка, то Tools->Deployment->Configuration->Mappings->Deployment Path -> Ставим слеш)
		Либо, можно не ставить эту галочку, чтобы выкладывать только нужные файлы. Иначе, при редактировании локальной версии автоматически будет 
        	выкладываться на хостинг. А если в .env включили дебагбар APP_ENV=local и APP_DEBUG=true, то и на хостинге он будет.
	Tools->Deployment->Options->Delete remote files when local are deleted - галочка
	Tools->Deployment->Options->Upload changed files ... ->Always (+ здесь же галочка Delete remote ... when deleted)

111) Если на хостинге сайт начинает показывать ошибку отсутствия файла сессии:
file_put_contents(файл_сессии.php): failed to open stream: No such file or directory.
Чтобы избавить от нее удаляем с локалки bootstrap/cache -> удаляем config.php и выкладываем папку через deployment

111) Удаляем на локальном сайте и синхронизируем (правая кнопка->Deployment->Upload to):
	1) storage/logs ->удаляем логи
	2) bootstrap/cache -> удаляем config.php (может быть ошибка без папки cache, 
           поэтому возможно придется закешировать php artisan cache:cache и выложить на хостинг через deployment)
    Если в Deployment выбираем Sync with deployed, то во вкладке Compare ставим: Size and Timestamps и нажимаем справа зеленую стрелку


111) Запускаем openserver
     Запускаем через закладки Шторм - теперь php работает в Шторме, можно отключить openserver.
     Галочку в Шторме Tools -> Deployment -> Automatic Apload
     Иногда Шторм не отправляет измененный код верстки, можно добавить-убавить пробел, чтобы он отправил файл на хостинг
     php artisan optimize:clear - очищаем кеш на компе
     php8.1 artisan optimize:clear - очищаем кеш на хостинге  в ssh консоли
     npm run build на компьютере, файлы начинают грузиться на сервер, ждем
     php8.1 artisan optimize  - заново кешируем на сервере
     Tools -> Deployment -> Automatic Apload - убираем галочку, чтобы при изменении файлов на компе не передавалось на сервер

111) После optimize:clear хостинг все равно писал путь к нескомпилированным скриптам как при разработке с компа. Помогло npm run dev. Возможно совпадение.

111) В папке public на хостинге были какие-то левые папки еще. Удалил всю папку и заново разместил ее с локалки.

111) На хостинге  в .env должно быть: APP_DEBUG=false, APP_ENV=production

111) Можно для openserver иметь сайт site.loc, а для хостинга site.ru
     Тогда в APP_URL будет стоять: https://site.ru
     Ошибок с asset не будет, так как получится, что на локальный сайт стили, картинки и пр. будут грузится с сайта на хостинге 

111) При остановке и включении openserver были сложности с тем какую версию (локальную или на хостинге) браузер откроет. 
     Вводил несколько раз адреса по-разному

111) Следующие действия описаны выше в Оптимизация Ларавель в продакшене	

111) Также в бегете можно запускать композер:  composer install (composer-php8.1 install)
     Также можно загрузить composer.phar и запускать на хостинге: 
	php8.1 composer.phar install

111) Статья по выкладке проекта Ларавель на Бегет:
	https://odlord.ru/perenos-laravel-na-hosting-beget-iz-repozitoriya/

111) Выкладка сайта Ларавель на хостинг через гитхаб:
	0) Создаю на компе в папке сайта папку public_html
           В него помещаю сайт Ларавель, либо устанавливаю новый:
	   composer create-project laravel/laravel:^10.0 ./
	1) Если не делал:
		 создаю символическую ссылку php artisan storage:link
		 npm i (пакеты node_modules не нужны на хостинге, это чтобы папка build была замечена гитом)
		 npm run build
	2) VCS => Enable Version ... - создаем локальный репозиторий .git
	3) Создаем рядом с public_html файл .gitignore, там пишем .idea
	4) Рядом с composer.json  помещаем composer.phar, а на хостинге теперь будем повторно запускать:
	    php8.1 composer.phar install 
	    или php8.1 composer.phar update
	    или /path/to/my_php/php composer.phar update --no-dev
	    возможно на хостинге сработает встроенный композер: composer-php8.1 install
	5) Удаляю с gitignore строку /public/build, так как на бегете нет node и не получится делать npm run build
	6) В public_html создаю .htaccess, чтобы запросы уходили в public
		<IfModule mod_rewrite.c>
    		   RewriteEngine On
    		   RewriteRule ^(.*)$ public/$1 [L]
		</IfModule>
	7) Меняю в config/database.php 'collation' => 'utf8mb4_general_ci',
	8) Выделю все, git -> add (добавляю в локальное отслеживание)
	9) Git => GitHub => Share Project - выкладываю на гитхаб
	          Здесь разрабатываю сайт на компе, комичу в гитхаб, либо сразу подключаю выкладку на хостинг по пушу:
	10) На хостинге подключаю SSH и ФТП в папку сайта (выше чем public_html), устанавливаю сертификат, меняю версию php = 8.1
	11) Tools => Start SSH, вводим данные
	12) В ssh терминале на хостинге: git init (папка .git д.б. рядом с public_html)
	13) Создаю ключи:
		mkdir ~/.ssh
		ssh-keygen -t rsa -q -N '' -f ~/.ssh/id_rsa
	14) репозиторий => settings => deploy keys => add deploy key =>
    	    в title пишем "key", в поле вставляем ключ pub 
	15) Для того, чтобы гитхаб мог сам заходить на хостинг:
    		Репозиторий => Settings => Secrets and variables => Actions => NEW repositary
    		HOST - cinedr.beget.tech  // сервер SSH
    		LOGIN - логин от этого SSH
    		PASSWORD - пароль SSH
	16) Отключаем уведомления на почту: Settings => Email notifications
	17) Продолжаем в ssh терминале:
    		git remote add origin git@github.com:адрес_репозитория
    		git pull origin master
    		git reset --hard
	18) Рядом с public_html на компе добавляю папку .github из полезное => авт. выкладка улучшенная
            Шторм добавит ее в отслеживание, соглашаемся
	19) В шторме делаю пуш в гитхаб, папка .github должна уйти на хостинг
        20) Удаляем папку .github и файл .gitignore с хостинга
            Вносим .github и .gitignore на хостинге в .git/info/exlude
	21) Создаем FTP соединение:
		Host: cinedr.beget.tech
		Логин, пароль
		Root path = /public_html
		Deployment path = Web path: /
	22) Отправляем .env на хостинг и меняем там данные (если грузить файл через бегетовский менеджер файлов, то удаляется точка)
	    На хостинге создаем базу, вносим в env ее данные (название БД совпадает с логином)
	    	APP_ENV=production
	    	APP_DEBUG=false
		Сайт, БД (создаем базу на хостинге, вносим в env ее данные, название БД совпадает с логином)
	23) В SSH: 
		php8.1 artisan storage:link
		php8.1 artisan key:generate
                php8.1 composer.phar install
		php8.1 artisan migrate
	23) Сайт работает
        24) Так как после npm run buils каждый раз создаются новые файлы в build, то придется их добавлять в гит
	25) Перед итоговой выкладкой можно сделать:
		php artisan optimize:clear
		php artisan optimize - кеширует роуты и настройки (php artisan config:cache и route:cache)
		php artisan view:cache
		composer dump-autoload –optimize (composer dump-autoload -o) - были ошибки из-за php 5 версии на хостинге
		composer-php8.1 dump-autoload - это для композера с хостинга, работает без ошибок

111) 


	



     
































































	
	
=======================================================Бутстрап =============================================================

111) Подключение бустрап 5:
     1) С помощью CDN, самый простой способ, подлключится все без возможности редактирования:
	<head>
	  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
	   <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-u1OknCvxWvY5kfmNBILK2hRnQC3Pr17a+RTT6rIHI7NnikvbZlHgTPOOmMi466C8" crossorigin="anonymous"></script>
	</head>	

     Можно подсоединить так, затем через Network -> Save as

     2) С помощью npm в папку проекта: 
	npm init - создаем корневой package.json (npm init -y  - будет стандартный файл), 
	npm install bootstrap - устанавливаем пакет бустрапа
	выбираем package.json из папки бустрапа и устанавливаем его зависимости npm init
	
	Теперь у нас есть папка dist с итоговыми стилями и скриптами и папки ресурсы: js и scss, в которых лежат стили 
	и скрипты отдельных компонентов (кнопок, карточек, форм, меню, переменные и т.д.). Мы можем изменять компоненты, 
	либо удалять ненужные (меняя импорты в общем bootstrap.scss), затем компилировать все в итоговые файлы 
	в папку dist командами описанными в package.json пакета бутстрап, например: npm run dist.
	Затем копируем итоговые файлы из dist и подключаем их.
	
     3) Скачать сборку и подключить нужное. В сборке много файлов и несколько итоговых. Отдельные файлы подключаем когда 
	нужны определенные стили, например, сетка(bootstrap-grid), reset(bootstrap-reboot) и др.
	
	Итоговые  минифицированные файлы, которые подключаем:
	bootstrap.min.css
	bootstrap.bundle.min.js (бутсрап + popper)

	Либо несжатые файлы:
	bootstrap.css
	bootstrap.bundle.js
	
	Также есть bootstrap.min.js - без библиотеки popper.

	Если подключаем Bootstrap v3 и v4, то понадобится подключить библиотеку jquerry.	

111) Установка пакетов npm в папку проекта:
	1) npm init // в папке создается package.json (npm init -y  -станд. файл)
	2) npm install bootstrap@5.2.1 --save  // пишем имя пакета и флаг
	
--save-dev - пакеты для (упрощения) разработки, пропишутся в devDependencies: например Гальп, Вебпак, тесты
-- save - пропишутся в dependencies (по умолчанию, можно флаг save не использовать), 
          будут использоваться при работе сайта, например Бутстрап, библиотеки вывода дат на сайт и др.

111) Если package.json в папке не будет, то пакет установится в ближайшем родительском node_modules

111) npm install - установятся зависимости из dependencies файла package.json

111) Файлы .map - нужны для связывания css и js файлов с их исходными файлами, используются для отладки 
                  исходных файлов прямо в веб-браузере

111) https://feathericons.com - svg иконки с возможностью масштабирования и утолщения
     https://icons.getbootstrap.com - svg иконки от бутстрап
     https://fonts.google.com/icons - svg иконки от гугл с возможностью масштабирования и утолщения - предпочтительный вариант	
     https://fontawesome.com - фонтэвесом, выбирать free
     https://heroicons.com/ - svg иконки от tailwind

111) Резиновая верстка - размеры задаются в процентах, при уменьшении экрана элементы уменьшаются
     Адаптивная верстка - при уменьшении экрана структура сайта может перестраиваться

111) Подключение только сетки бустрап - bootstrap-grid.min.css

111) Сетка бустрап использует деление на 12 колонок, предназначена для верстки mobile-first и состоит из:
	1) Внешние контейнеры (имеют лев и прав пэддинг 0.75rem = 12x):
		.container - адаптивно-фиксированный, выровнен по центру экрана, меняет размер на контрольных точках:
			     < 576px - 100%
			     ≥ 576px - 540px
			     ≥ 768px - 720px
			     ≥ 992px - 960px
			     ≥ 1200px - 1140px
			     ≥ 1400px - 1320px  
		.container-fluid - всегда занимает 100% ширины экрана

	 2) .row - flex-контейнеры
	 
         3) .col - адаптивные flex элементы

111) Можно сказать, что col применяется для установки ширины элементу.
     Если блоку внутри row не задан col или col не попадает под брейкпойнт, то ширина его 100%.
     col-12 - по умолчанию блок будет шириной 12 колонок, то есть 100% доступной ширины родителя
     col-sm-4 - начиная с вьюпорта 576px блок будет занимать 4/12 ширины родителя (то есть 1/3 или 33.33%)
     col-md-7 - начиная с вьюпорта 768px блок будет занимать 7/12 ширины родителя (7/12*100 = 58.3%)
     col-lg-5 - начиная с 992px будет занимать 5/12 ширины родителя

     Элементы с классом col должны находиться во флекс-контейнерах row. В бутстрап по умолчанию включен flex-wrap,
     поэтому если в одном row расположены элементы превышающие в сумме 12 колонок, то будет перенос.

     Начначенный класс col будет действовать пока ширину не переназначит другой col, например:
     <div class="col-12 col-sm-6 col-xl-3"> - по умолчанию 100% родителя, после вьюпорта 576px - 50%, после 1200px - 25%
     <div class="col-6"> - на всех разрешениях 50% родителя\
	
     Также есть классы без количества колонок: col, col-sm, col-md, col-lg, col-xl
     Они равномерно будут занимать все доступное пространство.
     Например в одном row три блока col будут занимать по 33.33% ширины row, если нет других элементов.

     Следует учесть, что блоки внутри row не имеющие никакого col или не попадающие под брейкпойнт имеют width: 100%,
     они перенесутся. Например, col-lg-5 рядом с col, имеет ширину 100% ширину row до 992px (он перенесется),
     а после 992px он встанет в одну линию с col и будет занимать 5/12 родителя, а col поделят оставшееся 
     пространство поровну.
     
     Также есть классы col-auto, col-sm-auto, col-md-auto, col-lg-auto и col-xl-auto.
     Они устанавливают ширину блока по содержимому.
	 
     Важное правило: col нельзя вкладывать в col, только в промежуточный row (т.к. это флекс контейнер)

111) Для адаптива под разные размеры экрана используются префиксы, добавление которых означает, что класс начинает 
     работать на опр. брейкпонте. Если на следующее разрешение класс не установлен, то нынешний класс будет работать до конца.
     Например, единственный класс col-6 означает, что на всех точках остановы ширина блока 50%
	
     Контрольные точки:
	без префикса: > 0  // (xs) extra small - смартфоны
	sm: >= 576px // small - планшеты
	md: >= 768px // medium - ноутбуки
	lg: >= 992px // large - компьютеры
	xl: >= 1200px // extra large - компьютеры
	xxl: >= 1400px // extra extra large - компьютеры

     Контрольные точки настраиваются в _variables.scss

111) Выравнивание внутри флекс-контейнерa row по вертикали (то же align-items):
     class="row align-items-start"
     class="row align-items-center"
     class="row align-items-end"
    
     Выравнивание самого блока внутри row по вертикали (то же align-self):
     class="col align-self-start"
     class="col align-self-center"
     class="col align-self-end"

     Выравнивание row justify-content:
     class="row justify-content-start"
     class="row justify-content-center"
     class="row justify-content-end" 
     class="row justify-content-between"
     class="row justify-content-around"

111) offset - сдвиг col вправо на опр. число колонок
	class="col-4 offset-4"> - сдвиг на 4 колонки (33.3%) вправо на всех разрешениях
	class="col-4 offset-md-4"> - сдвиг на 4 колонки начиная с 768px

111) Margin и padding назначают по схеме: свойство+сторона - брейкпойнт - размер.
     Размер зависит от переменной $spacer в Bootstrap scss, по умолчанию $spacer = 1rem = font-size html = 16px:
	0 - равно 0
	1 = 16px * 0.25 = 4px
	2 = 16px * 0.5 = 8px
	3 = 16px * 1 = 16px
	4 = 16px * 1.5 = 24px
	5 = 16px * 3 = 48px
	
     Примеры:
	p-3 - padding: 16px
	pb-2 - padding-bottom: 8px
	pt-lg-4 - padding-top: 24px начиная с 992px
	m-5 - margin: 48px
	mx-5 - margin-left и margin-right: 48px
	my-4 - margin-top и margin-bottom: 24px
	mx-auto - margin-left и margin-right: auto

111) scss/_variables.scss - здесь расположены переменые цветов, размеров, брейкопойнтов, теней, др. свойств.

111) Классы .h1, .h2, .h3, .h4, .h5 и .h6 применяются для стилизации текста стилями заголовков, пример:
	<div class="h3">Некоторый текст...</div>

111) Классы .display-1, .display-2 ... .display-6 выводят текст в виде высоких тонких заголовоков:
	<h1 class="display-3">Заголовок с оформлением display-3</h1>

111) Выравнивание текста внутри блока:
	text-left(right, center, justify)
     Также можно добавить префиксы:
	text-md-right - начиная с 768px выравнивать текст по правому краю

111) Цвет текста:
	text-muted - приглушенный (полупрозрачный/серый)
	text-primary - важный (синий)
	text-success - успех (зеленый)
	text-info - примечение (голубой)
	text-warning - предупреждение (оранжево-желтый)
	text-danger - ошибка (красный)

111) Скрытие текста:
	d-none - добавлен display: none !important;

111) Изображения:
	.img-fluid - чтобы изображение не выходило за пределы родителя.
                  <img class="img-fluid" src="1.jpg"> - то же самое, что: {height: auto; max-width: 100%; }
	.rounded - скруглены углы
	.rounded-circle - круглое/овальное (border-radius: 50% !important;)
	.shadow - с тенью (box-shadow)

111) Таблицы
	Для придания таблице стилей бустрапа задаем ей класс table: <table class="table">
	.table-striped - для придания таблице полосатости <table class="table table-striped">
	Фон таблицы:
	.table-primary - синий
	.table-secondary - серый
	.table-success - зеленый и т.д.
	Пример: <table class="table table-dark table-striped">
	.table-hover - ряды при наведении меняют цвет
	.table-bordered - очерчены границы ячеек
 	.table-sm - таблица становится компактсной за счет уменьшения пэддингов ячеек

111) Формы
	Элементы формы обязательно располагаем в сетке бустрапа - в col
	Для инпутов устанавливаем атрибут type, потому что бустрап применяет стили по этому атрибуту.
	.form-label - для лэйблов у инпутов
	.form-control - для инпутов, устанавливает стили: блок, ширина 100%, паддинги, шрифт, цвет, 
			бордер, бордер при фокусе и др.
	.form-select - для select (.form-select.form-select-sm - уменьшены пэддинги)
	.form-check-input - для radio и checkbox (лэйблы .form-check-label)
	.form-switch - при добавлении к  обертке превращает checkbox в красивый переключатель
	.form-check-inline - отменяет блочный порядок для radio и checkbox
	.form-range - класс для инпута type="range"
В бустрапе в инпуты можно вставлять кнопку, добавляющую часть текста, например @mail.ru, есть много др.
вариантов инпутов: с встроенными раскр. списками, комбинированием кнопок, списков.
	.form-floating - если обернуть в этот класс лэйбл и инпут, то при фокусе лэйбл поднимается
	.is-invalid - добавляет инпуту красную рамку и восклицательный знак справа 
                      или другие стили в зависимости от типа инпута
	.is-valid - добавляет красивую галочку и зеленую рамку или др. стили по типу инпута

111) Цветные блоки оповещения:
	alert alert-success - успех, зеленый
	alert alert-info - нейтральная информация, голубой
	alert alert-warning - предупреждение, желтый
	alert alert-danger - ошибка, предупреждение
	alert alert-primary - важно, синий
	alert alert-secondary - менее важно, серый

111) Цветные кнопки:
	btn btn-primary - синяя
	btn btn-secondary - темная серая
	btn btn-success - зеленая
	btn btn-info - голубая
	btn btn-warning - желтая
	btn btn-danger - красная
	btn btn-dark - черная
	btn btn-outline-primary - белая с рамкой, а при наведении меняет цвет
	btn-sm и btn-lg - маленькая и большая кнопки

111) Цветные ссылки:
	link-primary - синяя
	link-success - зеленая и т.д.
	
111) .btn-group - группирует кнопки в гор. ряд без промежутков
     .btn-group btn-group-sm - ряд маленьких кнопок
     .btn-group btn-group-lg - ряд крупных кнопок

111) .accordion - раскрывающиеся списки в виде клавиш

111) Background - образуется как bg + цвет, например:
	bg-secondary - синий фон
	bg-success - зеленый
	bg-dark	- черный и т.д.
     Фон становится более красивым при добавлении класса bg-gradient
     Фон становится прозрачнее при добавлении класса bg-opacity-10 (25,50,75)

111) Плашки - цветные выделения текста, похожи на кнопки только без кликов: 
	badge bg-secondary - как кнопка
	badge bg-primary rounded-pill - со скругленными углами

111) Пагинация:
	<ul class="pagination">
	  <li class="page-item"><a class="page-link" href="#">1</a></li>
	</ul>

 pagination-sm - уменьшает пагинацию

111) Хлебные крошки:
	<ul class="breadcrumb">
	   <li class="breadcrumb-item"><a href="#">Photos</a></li>
	</ul>

111) Список в виде клавиш:
	<ul class="list-group">
  	   <li class="list-group-item">First item</li>
	</ul>

111) Карточки:
	<div class="card">
  	   <div class="card-body">
		<p class="card-text">Текст</p>
	   </div>
	</div>

111) .dropdown - кнопка с выпадающим меню как у селекта
                 Есть вариант с выпадающей небольшой формой, удобно.

111) .collapse - кнопка с показом поля с текстом

111) .modal - модальное окно, может быть удобно для вызова формы отправки сообщения

111) .navbar - навигационное меню, есть меню с формой поиска, с раскрывающейся боковой панелью

111) .nav-tabs - вкладки

111) .placeholder-wave - прогресс-полоска с волной

111) .progress-bar - разные виды прогресс баров

111) В бустрапе есть всплывающие окошки наподобие title по клику

111) Также есть tooltip - более красивые title при наведении

111) Есть отслеживание прокрутки, когда при прокрутке переключается активный элемент меню

111) .spinner-border - крутящийся спиннер загрузки
	  
111) ratio-16x9 - настраивает соотношение сторон видео, иозображений, блоков

111) text-truncate - обрезает текст, вставляет многоточие

111) border-bottom (сторона или просто border) - устанавливает рамку

111) border-primary (цвет) - цвет рамки

111) text-primary (цвет) - цвет текста

111) d-none, d-inline, d-block, d-flex и т.д. - установка display

111) opacity-25 (50, 75, 100) - непрозрачность

111) position-absolute top-0 start-50 - установка position

111) w-25, h-50 (25, 50, 75, 100, auto) - ширина и высота в %

======================================================= Конец бутсрапа =============================================================

111) Боковые кнопки у слайдера сделал так:
     Общий блок с двумя кнопками сделал: display: flex;
     Развел кнопки в разные стороны: justify-content: space-between;
     Общий блок отрегулировал вертикально через position: absolute; и top.
     Двигал кнопки задав им свой position: absolute (получается относительно общего блока) 

111) HTTP - HyperText Transfer Protocol
     HTTPS - HyperText Transfer Protocol Secure

111) Посмотреть сырой HTTP запрос/ответ в хроме:
	Network -> Headers -> Request Headers -> View source (может не показываться)

HTTP запрос: 
	GET /test/ HTTP/1.1  // стартовая строка
	Host: localhost:63342
	Заголовки

        Body // тела может не быть, при POST запросе здесь крепятся get параметры формы, двоичные данные, json

HTTP ответ:
	HTTP/1.1 200 OK // стартовая строка
	Заголовки

	<!DOCTYPE html> // тело ответа
	<html>
	<Верстка>	
	</html>

Коды ответа: 2хх - успешно, 3хх - перенаправление, 4хх - ошибка на стороне клиента, 5хх - ошибка сервера

Заголовки:
	Host - запрашиваемый домен
	Date - дата и время запроса
	Last-Modified - когда ресурс был изменен, отправляет сервер
	if-Modified-Since - есть ли изменения с даты, отправляет клиент, используется для загрузки из кеша браузера
	Content-Type - MIME тип содержимого, описание ниже
	Content-Language - язык документа
	Content-Lengt - размер содержимого тела запроса в байтах
	X- - свои заголовки
	Location - адрес перенаправления
	Referer - с какого сайта переход
	User-Agent - операционная система, браузер, название бота. В начале всегда пишется Mozilla/5.0 по историч. причинам.

111) header — функция php для отправки HTTP заголовков. Пример:
	header("Location: https://example.com");
	
111) В теле HTTP ответа и содержится содержимое ответа: верстка, CSS текст, двоичные данные картинки и так далее.
После первого HTTP запроса/ответа браузер анализирует ответ и автоматически отправляем новые HTTP запросы по
ссылкам верстки: на загрузку стилей, скриптов, картинок и др.

111) Один HTTP запрос на объединенный файл будет быстрее, чем несколько запросов на отдельные файлы.
Поэтому стили, скрипты, svg, картинки и др. принято объединять в один файл.
Стили и скрипты объединяются сборщиками (Gulp, Webpack)
SVG в спрайты, картинки тоже в спрайты (показываются через background сдвигом).

111) Все файлы(скрипты, стили, картинки, шрифты и др.) доступны в Network, тип файла можно выбирать во вкладках.

111) Способы перенаправления:
  	1) HTTP ответ с кодом 3хх   // серверный способ
  	2) <meta http-equiv="refresh" content="0; URL=https://www.ya.ru/" />  // content - задержка в сек перед перенаправлением
  	3) window.location = "https://www.ya.ru/" // js перенаправление, можно делать по условию

Приоритет перенаправлений: HTTP, meta, location
Лучше использовать HTTP

Случаи использования перенаправлений:
  1) Связывание доменов:
	а) с www.example.com на example.com
	б) старый домен => новый домен
	в) принужденное HTTP => HTTPS
  2) Сохранения ссылок рабочими, например для сео при перестройке сайта
  3) Временные ответы для небезопасных запросов.
     GET - безопасные запросы не изменяют состояние сервера, POST - небезопасные, изменяют состояние сервера
     Например после POST отправки формы нажатие кнопки перезагрузки повторно отправит запрос.
     Для таких случаев отправлять 302-303 код (в php header("Location: https://example.com") отправляет 302 код)
  4) Временные ответы на долгие запросы

Перенаправления могут быть установлены или в настройках сервера, или в каждой директории в файле .htaccess
Директивы в .htaccess относятся только к папке, где размещен .htaccess, а оттуда распространяются на дочерние 
папки. Для внутренних папок можно настраивать свои редиректы.
Директивы: Redirect, RedirectMatch или RewriteRule

Примеры:
RewriteCond %{THE_REQUEST} ^[A-Z]{3,9}\ /index\.(php|html|htm)\ HTTP/
RewriteRule ^(.*)index\.(php|html|htm)$ $1 [R=301,L]

Redirect permanent / http://www.example.com
Redirect 301 / http://www.example.com

111) HTTP передает данные как есть, HTTPS шифрует их при помощи криптографических протоколов SSL и TLS.

SSL/TLS-сертификат ― это цифровая подпись сайта.
Порядок работы:
  1) Браузер пользователя просит предоставить SSL-сертификат
  2) Сайт на HTTPS отправляет сертификат.
  3) Браузер проверяет подлинность сертификата в центре сертификации.
  4) Браузер и сайт договариваются о симметричном ключе при помощи асимметричного шифрования.
  5) Браузер и сайт передают зашифрованную информацию.

HTTPS использует для подключения 443 порт. 
HTTPS влияет на СЕО.

111) В стартовой строке запроса можно передавать get параметры:
	  GET /page/?param1=value1&m2=value2 HTTP/1.1  // сама строка параметров называется Query String

Отправленные гет параметры можно посмотреть также во вкладке Payload

111) Html формы отправляют данные инпутов либо методом GET по умолчанию, либо POST. Метод прописывается в action:
	<form action="/page/" method="GET"> 
        HTTP запрос:  GET /page/?test1=value1&test2=2 HTTP/1.1

	<form action="/page/" method="POST">
	HTTP запрос: POST /page/ HTTP/1.1
	Гет параметры в POST запросах отправляются в теле запроса: test1=value1&test2=2

	После отправки формы при обновлении страницы данные отправляются опять. 
	Есть способы избежать этого, например самый частый: 
	header('Location: /path'); exit;  // Можно даже на ту же страницу. Обновление будет чистым, 
						без заполненных POST-ов и GET-ов.

	После header('Location: url) нужно обязательно делать exit, так как иначе код ниже команды 
        перенаправления тоже будет выполняться.

111) В заголовке Content-Type указывается MIME тип присланного содержимого. Примеры:
	text/html - HTML код
	text/css - CSS код
	text/javascript - JavaScript код
	text/plain - простой текст
	image/png - PNG изображение
	image/jpeg - JPG изображение
	application/json - JSON

111) fetch - это способ отправить http запрос через js без перезагрузки страницы
     По умолчанию отправляются get запросы, в options можно изменить метод, прикрепить заголовки, body:
	fetch(url, [options])

     В body можно отправить:
	1) Строку, в том числе JSON
	2) FormData
	3) Blob/BufferSource
	4) URLSearchParams

111) Сервер может в заголовке устанавливать куки:
	Set-Cookie: name=john

     Теперь браузер при каждом HTTP запросе на этот сайт будет отправлять их серверу в заголовке:
	Cookie: name=john

    document.cookie - установка и чтение кук через js
    header("Set-Cookie: key=value") - php установка кук через заголовок
    header( "Set-Cookie: key=value; HttpOnly" ) - то же с запретом чтения этой куки в js 
    setcookie("TestCookie", "value", time()+3600) - php функция для установки кук, генерирующая в итоге header("Set-Cookie: ...")
    setcookie("TestCookie", "value", httponly:true ) - то же с запретом чтения этой куки в js 
    $_COOKIE['TestCookie'] - прочитать куки в php

111) ini_set( 'session.cookie_httponly', 1 ) - по умолчанию все куки будут httponly

111) Куки сайта в хроме можно посмотреть в заголовках, все куки браузера - в Application -> Cookies

111) На основе кук работают php сессии. 
     session_start(); // запуск сессии, сервер проверяет есть ли сессионная кука в HTTP заголовке cookie
                      // если нет устанавливает http-only куку, что-то наподобие Set-Cookie('PHPSESSID', 'value')
     session.auto_start = 1 // либо автоматически запускаем сессии (в php.ini) без прописывания session_start 

111) Внимание!
     $_SESSION:
	1) сессия от php, хранится на хостинге, либо в openserver в c:/openserver/userdata/temp
	2) обычно имеет идентификатор в куках PHPSESSID (у ево установился как evo175zdpv - написано в phpinfo())
	3) информацию можно посмотреть в phpinfo()
	4) настраивается командами самого php

     Session::put(..) и session() в Ларавель 	
	1) отдельная технология от Ларавель
	2) Имеет идентификатор в куках laravel_session (лара) или evo_session (для ево)
	2) хранит данные в файлах сайта (редисе, бд и т.д.)
	3) Настраивается в session.php

     И $_SESSION и сессия ларавель устанавливают отдельно друг от друга свой идентификатор сессии в куках и обмениваются им 
     с браузером.
     Например при работе Evocms в браузере записано две куки: 
	1) evo175zdpv (в значении  название файла в c:/openserver/userdata/temp от php интерпретатора, работает массив $_SESSION) 
	2) evo_session (в значении название файла в core/storage/sessions, работает ларавелевский помощник session())

111) При посещении пользователем (даже неавторизованного) сайта php и Ларавель создают ему 2 отдельных файла сессии (либо запись в Redis, БД у Ларавель)
     и отправляют в браузер куку с названиями этого файла.
     Например от php кука с ключом PHPSESSID, а в значении - название файла сессии.
     
     В файл сессии обычно грузятся переменные вашего визита (корзина, авторизован ли и пр.). 
     В коде эти переменные доступны в php массиве $_SESSION['...'] или в ларавелевском session('...')

     Браузер же хранит куку с идентификаторами сессии (PHPSESSID и laravel_session) и каждый раз отправляет их при обращении к сайту
     с помощью HTTP заголовка cookie.
     Соответственно каждый раз php интерпретатор и Ларавель проверяют есть ли в куках запроса идентификатор сессии и используют файл
     сессии пользователя
	
     В php есть встроенные функции для открытия сессии $_SESSION - session_start и пр.
     Сессию можно закрыть и запретить работу с $_SESSION - session_write_close

     Файлы php сессий не хранятся на сервере вечно, устаревшие файлы сессий время от времени очищаются.
         session.gc_maxlifetime - настройка для времени жизни php сессии
         session.php/'lifetime' - время жизни сессии Ларавель и Ево
     В них указываются время жизни сессионного файла с момента последнего обращения к нему.
     По крону на сервере можно, например, раз в 30 мин. пробегаться по файлам сессии и удалять устаревшие файлы-сессии (наз. сборка мусора).
     session.gc_probability и session.gc_divisor - настройки для периодичности удаления php сессий
 
     В cms насколько понял это делается автоматически. 

111) Важно! В отличие от js сессий  файл php сессии хранится на сервере после закрытия вкладок. Соответственно,
     при следующем заходе клиента браузер передает в куках PHPSESSID, находится файл сессии и теперь можно использовать данные пользователя
     из прошлой сессии ($_SESSION['key'])

     session_destroy() - удаляет файл с переменными сессии.
     unset($_SESSION['key']) - удаляет переменную сессии

     Php сессии и js сессии не пересекаются. 

     Для передачи переменных из/в js и php можно использовать:
	1)  AJAX (fetch)
	2) <?php session_start(); ?>
	   <script>let a =  '<?= $_SESSION['name']; ?>' </script> // обязательны кавычки
	3) location = '/?num=5' // передаем перезагрузкой страницы + гет параметр
	4) <a href="?num=5">Ссылка</a> // по клику на ссылку + гет параметр 

     Соответственно таким образом можно взаимодействовать между js localStarage, sessionStorage, php $_SESSION,
     и др. без аякс запросов.

111) checkdate(9, 16, 2015) - php функция, проверяет существует ли дата

111) preg_match('#^e+$#', 'eee') - проверка регуляркой, возвращает 1 или 0
 
     preg_match('#^/(\d+)/(\d+)/?$#', '/12/457', $match) - расфасовка по карманам, возвращает 1 или 0 + создается массив $match

111) range(start, end) - создает массив от и до

111) API  - набор URL, к которым можно обращаться для работы с данными, пример:

	    https://belarusbank.by/api/kursExchange?city=Брест 

111) REST API - взаимодействие с сервером через HTTP запросы определенным образом:
	GET http://api.loc/users/ - получить всех пользователей
	GET http://api.loc/user/1/ - получить пользователя с id = 1
	POST http://api.loc/user/ - создать пользователя
	PUT http://api.loc/user/1/ - изменить пользователя с id = 1
	DELETE http://api.loc/user/1/ - удалить пользователя по id

111) Простейшая работа api:
	Сайт api отдает информацию: echo 'test' // обычно это echo json_encode(...)
	Другой получает: $res = file_get_contents($url); // json_decode(file_get_contents($url), 1)

111) Работа через гет параматры:
	if(isset($_GET['num'])) echo rand(1, $_GET['num'])  // http://api.loc/?num=50  вернет случайное число

111) header('Content-Type: application/json') - установка MIME типа отдающегося материала, 
						напр. перед тем как отдаем echo json_encode(...)

111) Пример api с запросом в БД:
	$id = $_GET['id'];
	$query  = "SELECT * FROM users WHERE id=$id";
	$result = mysqli_query($link, $query);
	$user   = mysqli_fetch_assoc($result);
	header('Content-Type: application/json');
	echo json_encode($user, true);

111) Авторизация по api, работа с токенами
	Храним токены, напр. в БД.
	Пользователь должен вставлять свой токен в запрос: http://api.loc/index.php?num=100&token=12345
	Проверяем токен: if (isset($_GET['token']) and $_GET['token'] === $token){ продолжаем; } else { echo 'error';}

111) getallheaders() - получить все заголовки полученного запроса
     $_SERVER - более подробный массив данных сервера (корень, хост, ip, адрес скрипта и пр.) + полученные заголовки запроса в ключах HTTP_
     $_SERVER['QUERY_STRING'] - гет параметры

111) Установить заголовки можно через cURL, guzzle

111) gettype($var) - php функция показывает тип данных

111) Если переменная содержит знаки ариф. операций и числа, например: 2022-10-20, то при вставке нужно брать ее в одинарные кавычки,
     иначе mysql выполняет арифм. операции, даже если поле строковое:
	$now = '2022-10-20'
	UPDATE users SET date = '$now', count_day = 9 WHERE id = $id

111) Postman предназначена для тестирования работы API, для отправки запросов POST и GET.
     В отличие от curl имеет графический интерфейс.

111) Перенаправление всех HTTP запросов на один файл через .htaccess:
	RewriteEngine On
	RewriteRule .+ index.php // здесь регулярное выражение .+ 

    Внимание! Важно! При такой переадресации в адресной строке остается адрес который вводили, например: http://rest.loc/good/1/
                     Полезно при создании rest api, есть ниже, то есть ловим адреса через preg_match и карманы

     $_SERVER['REQUEST_URI'] -  с какой страницы пришли
     $_SERVER['HTTP_REFERER'] - с какого сайта пришли, устанавливается не всегда, нужно проверять на isset
     
111) date('L', timestamp) - проверка на високосный год

111) Разбор адреса по карманам для целей API:
	http://api.loc/1/100/ - запрашиваем

	$uri = $_SERVER['REQUEST_URI']; // какой адрес запрашивался. Перед этим все запросы через .htaccess перенапр. на index.php
	$res = preg_match('#^/([0-9]+)/([0-9]+)/?$#', $uri, $match)) // 0 или 1 + массив карманов $match
	if($res){
	    if (isset($match[1]) and isset($match[2])) { код }
	} else { echo 'ошибка'; }

111) 	Заголовоки клиента (обычно браузер) и сервера (тот кто отдал верстку или данные) видно в network->headers

	Сервер устанавливает заголовки при отправке данных клиенту (браузеру): 
		header("Set-Cookie: key=value")
		header("MY_HEADER: 1234"); ?> // будет видно в браузере в network->headers

	Сервер получает заголовки установленные клиентом:
		getallheaders()  — возвращает все заголовки установленные клиентом (браузером)
	
     	Клиент устанавливает заголовки:
		1) Автоматически браузером: get запрос url +  добавление браузерных заголовков
		2) через cURL в консоли и в коде // то есть отправляется HTTP запрос на сайт/сервер
		3) через Guzzle при запросе url
		4) в fetch в объекте options {headers: {Authentication: 'secret'}}
		5) ...
	
	Клиент (браузер) получает заголовки установленные сервером:
		get_headers('http://ya.ru') - возвращает все заголовки из ответа сервера на HTTP-запрос

111) API - описание взаимодействия с сайтом сторонних приложений (Application Programming Interface)
           Это позволяет взаимодействовать приложениям написанным на разных языках.

111)  Обычный запрос браузера - GET и направлен на получение верстки. Если мы хотим другие способы взаимодействия с сайтом или получить
      данные в удобном формате (напр. json), то применяется REST API.

      В основе REST API лежит идея разделения операций CRUD  через методы HTTP:
	CREATE - POST
	READ - GET
	UPDATE - PUT
	DELETE - DELETE

	GET http://api.loc/users/ - получить всех пользователей // в БД SELECT
	GET http://api.loc/user/1/ - получить пользователя с id = 1 // в БД SELECT
	POST http://api.loc/user/ - создать пользователя // в БД INSERT
	PUT http://api.loc/user/1/ - изменить пользователя с id = 1 // в БД UPDATE
	DELETE http://api.loc/user/1/ - удалить пользователя по id // в БД DELETE

     Однако формы html поддерживают только методы GET и POST, а в php есть доступ только к $_GET и $_POST.
     
     Решение 1: Устанавливаем над формой свой http заголовок с методом отправки, например $_SERVER['HTTP-X-HTTP-METHOD'] = 'DELETE'. 
		header('HTTP-X-HTTP-METHOD: DELETE') - не срабатывает.
		Форма отправляет запрос (get или post) на какую-нибудь страницу, а на ней можно получить метод отправки 
                и выполнить операцию с БД.
			if(isset($_SERVER['HTTP-X-HTTP-METHOD'])){ и он равен DELETE, то .. и пр. }

     Решение 2: использовано в Ларавель. Создаем скрытый инпут с методом:	
	<input name="_method" type="hidden" value="PUT">, далее тоже через условия

111) Работать с сайтом по api можно:
	1) Без аутенификации, сессий, кук, csrf
	2) Через аутентификацию в HTTP заголовке Authorization. 
           Authorization: Basic [зашифрованное Base64] - браузер покажет окошко логина и пароля. Здесь логин и пароль кодируются с помощью Base64 при передаче и никак 
           						не шифруются и не хэшируются. Поэтому для безопасности нужно использовать https.
	   Authorization: Bearer "токен" - токен будет искаться в поле api_token таблицы users - есть в документации по Api аутентификации
	3) Через токен (прикрепляется гет параметром в ссылку - как в верификации почты или post паарметром в body).
	4) Через посредник sanctum в Ларавель. Устанавливается в роутах. Крепит и кодирует куки XSRF-TOKEN и laravel_session
	5) Через пакет Passport (более расширенный, чем sanctum, добавлена oAuth2)

111) Принцип api работы пользователя с разных устройств: 
	Создается таблица: user_id, token, expire (время жизни токена)
	Пример: 12 sdfs65sdf7 123544545
     Теперь для одного пользователя можно хранить несколько токенов и иметь возможность выходить со всех устройств.

     Для того, чтобы пользователь постоянно не выходил из системы как только закончится основной токен придумали
     refresh_token (токен для обновления основного токена)
     Как работает: пользователь при каждом api запросе отправляет свой основной токен со временем жизни 1ч (его могут перехватить)
     Как только токен устаревает приложение отправляет на определенный адрес сайта refresh_token,
     основной токен и refresh_token перегенерируются и высылаются, теперь пользователь пользуется другим токеном.
     Смысл в том, что основной токен легко перехватить, так как он в каждом запросе, а токен обновления используется только каждый час.

111) Sanctum - встроенный в Ларавель пакет для выдачи пользователям токенов API без OAuth
     Passport - дополнительно к токенам предлагает OAuth2 аутентификацию

     Оба пакета можно использовать для только для входа в свое приложение.

111) Санктум работает с помощью таблицы personal_access_tokens. 
     Для пользователей нужно генерировать токены. 

111) Для разделения обычных запросов (web) и api запросов (api)	применяются посредники web и api. 
     Применяются роуты routes/web.php (для http://site.ru) и routes/api.php (для http://site.ru/api)
     RouteServiceProvider сам добавляет путь /api к ссылкам, писать его в маршрутах не надо.	 
     Это настраивается в RouteServiceProvider (там же настраивается ограничение api запросов для пользователя/ip в 60/мин.) 
     Для web запросов в Kernel также срабатывают посредники: кодирование кук, добавление кук в ответ, старт сессии, показ ошибок из сессии,
     проверка csrf. Для api запросов sanctum добавляет и кодирует куки XSRF-TOKEN и laravel_session.

111) Соответственно для работы через api пишем роуты в routes/api.php:
	Route::group(['middleware' => 'auth:sanctum'], function (){ - для аутенитификации применяется Санктум, для теста можно посредник закомментировать
    		Route::get('/', [MainController::class, 'index']); - для адреса http://site.ru/api/
    		Route::post('/register', [MainController::class, 'register']);}); - для адреса http://site.ru/api/register
        Гет запросы можно проверять через браузер, а post через Tools->Test RestFul

111) Обязательно указываем заголовки, если передаем/принимаем json: 
	Accept: application/json - говорим, чтобы Ларавель присылала json
	Content-Type: application/json - говорим, что отправляем на Ларавель json
     
111) Также как и в обычных роутах  Ларавель для api запросов также возвращает ошибки, только уже в json.
     Чтобы показ ошибок отключить: APP_DEBUG=false

111) В ответ на api запрос принято добавлять заголовки результата операции, например: 
	return [1]; - вернет HTTP/1.1 200 OK
	return response()->json([1], 201) - вернет HTTP/1.1 201 Created - используется для post запросов создания записи в БД
	return response()->json(['api'], Response::HTTP_BAD_GATEWAY) - в Response можно посмотреть константы кодов
      
111) JSON API - так понял, что частный случай REST API. Здесь мы от сайта на запрос ждем не верстку, а json ответ с какими-то данными 
     (есть также вариант пересылки в xml формате).
     Например: 
	https://site.ru/list  - список пользователей в виде верстки для браузера
	https://site.ru/api/list - json массив пользователей (return User::all() - Ларавель авт. преобр. в json)

111) PHPStorm позволяет работать с api через Tools-> Test Restful.
     Нужно установить заголовки: Accept: application/json и Content-Type: application/json
     json строку передаем в Request Body -> Text -> {"name": "Денис", "email": "rodnovspb1@mail.ru", "password": 111}
     Если post запрос, то не забываем в роутах: Route::post 
     Также Ларавель ждет куки от sanctum, закомментировать этот посредник в маршруте, если не научился ставить.

111) SPA (Single Page Application) - концепция работы сайта на js без перезагрузки страницы. При заходе на сайт грузится больше информации
     с display:none, а дополнительная информация подгружается через аякс запросы. Например, пользователь переходит с одного товара 
     на другой, но страница при этом не перезагружается. Одностраничные приложения чаще всего используют в сервисах, где пользователь 
     проводит на одной странице много времени или совершает с ней какие-то действия (соц сети, почта и др.). Например не нужно перезагружать
     страницу, чтобы увидеть оповещения о сообщениях (вебсокеты??) 
	Достоинства SPA: 
	  1) Страница не перезагружается, можно бесконечно прокручивать вниз
	  2) Скорость - после загрузки страницы она работает быстро, как приложение на телефоне
	Недостатки:
	  1) Первый экран грузится дольше 
	  2) Проблемы с SEO - поисковики работают с отдельными страницами  у каждой из которых есть заголовок, описание и остальные метатеги.
                              SPA - одна страница или подгружает новую информацию через js 
	  3) Нужно знать JS

111) Для работы с приложениями обычных CRUD операций (GET, POST, PUT, DELETE и др) не хватает,
     так как команд может быть много (например: добавить товар в Избранное). Поэтому приходится придумывать ссылки:
	GET /good/12/ - показать товар
	GET /good/12?like=1 - добавить в избранное методом GET и гет параметром
	POST /good/12/like - добавить в избранное методом POST

111) Как работает браузер:
	1) Один раз анализирует заголовки
	2) В соответствии с ними показывает страницу
     Если мы что-то вывели на экран(даже пробел, перенос), а затем установили заголовки через функции:
     	1) header()
     	2) set_cookie()
     	3) session_start()
     то они уже не отработают.

     Поэтому заголовки должны быть всегда отправлены/установлены до вывода, при этом заголовки могут отправляться только один раз.

111) С этим связана проблема старта сессии session_start() после вывода чего-то. 
     Ведь сессии обычно работают через куки (второй способ передать PHPSESSID через url), а они отправляются через заголовки.

111) session_start():
	1) Проверяет, есть ли в присланных куках от браузера переменная сессии PHPSESSID (если куки отключены, то в GET или POST)
	2) Если есть, то выгружает из файла сессии переменные ($_SESSION)
	3) Если нет, то на сервере создается файл сессии (к ларавель session() не имеет отношения), 
           браузеру устанавливается кука с идентификатором файла PHPSESSID, 
           которую он теперь будет присылать каждый раз (по сроку хранения сессии)

111) Можно в PHPMyAdmin зайти во вкладку SQL и нажимая кнопочки получать готовые запросы на редактирование, как если:
     Нажал: UPDATE, получил UPDATE `goods` SET `id`='[value-1]',`name`='[value-2]',`price`='[value-3]' WHERE 1

111) beforeunload на window срабатывает при закрытии вкладки, записал данные в Localstorage, все хорошо сохранилось

111) Autoprefixer - плагин для установки префиксов для css: background-clip: text; -webkit-background-clip: text;
	

	

     

	
	
	
	
     
		
     
     

     


	





	
	
















======================================================= Композер =============================================================
111) packagist.org - хранилище композера

111) Способы работы с композером:
	1) Композер (Composer) есть в openserver. Для установки библиотек с его помощью открываем phpstorm через 
    	   закладки openserver, теперь можно устанавливать через команду в консоли composer и т.д.
           PhPStorm может спросить использовать ли общий файл композера из studing. 
           Если отказаться composer.json создастся в текущей папке
	2) Вручную прописываем путь до файла композера, затем команда
	3) Грузим в нужную папку composer.phar (это php архив) и в консоли используем: 
		php composer.phar команда
		или: /path/to/my_php/php composer.phar update --no-dev
         
111) Если на хостинге нет композера, то можно загрузить туда файл composer.phar и пользоваться композером

111) Установить композер в папку можно через phpstorm: Tools->Composer->Init Composer

composer.phar - файл, через который композер и запускается. То есть обращаясь: composer install мы обращаемся к этому файлу
                через алиас и запускаем его команду install. Для этой команды нужно быть в одной папке с composer.json
composer -v - быстрая проверка работы, показывает версию

111) require 'vendor/autoload.php'; - теперь все классы библиотек композера и его автозагрузки доступны в файле

111) Пример:
	composer require guzzlehttp/guzzle - установили библиотеку в папку, если не было создался composer.json

111)	Хорошее описание здесь: phpprofi.ru/blogs/post/52

        composer dump-autoload --optimize - обновить загрузчик, т. к. появились новые классы,  без установки или обновления других пакетов.
                                            Ключ --optimize используется для преобразования PSR-0 в автозагрузку как для classmap, 
                                            чтобы автозагрузчик был наиболее быстрым. 
                                            Это + ключ optimize настоятельно рекомендуется для production (вы можете получить 20% прирост)
	composer init - создание файла composer.json в текущем каталоге
	composer install - установит текущие библиотеки для работы приложения из файла composer.lock 
                          (например, если скопировали Ларавель с гитхаба), а если его нет из composer.json + создаст composer.lock
	composer update - обновит пакеты в vendor в соответствии с последними версиями из composer.json + создаст composer.lock с установленными версиями

	composer.lock - в этом файле хранится список текущих установленных версий библиотек
	                Соответственно, если проект кто-то копирует и пишет composer install, то устанавливаются версии из этого файла
        
	composer remove phpmailer/phpmailer - удалит пакет из папки vendor и его запись в composer.json

	composer update package_name - обновит только 1 пакет

        То есть когда мы устанавливаем чистый проект, то зависимости устанавливаются в соответствии с composer.json, 
        создается composer.lock в котором прописываются версии установленных библиотек. Если передаем наш проект другому программисту,
        то composer install запускает установку из composer.lock, теперь у нас одинаковые версии библиотек.

	Команды в composer.json: 
	"require": {"guzzlehttp/guzzle": "~6.5.0"} // мажор.минор.патч
	~ - можно обновлять по патчам (последняя цифра)
	^ - можно обновлять по минорам и патчам
	* - последняя стабильная версия ("require": {"guzzlehttp/guzzle": "*"})
	@stable - стабильная

        Загружаем библиотеки, подключаем их все через require 'vendor/autoload.php';

	"require" - библиотеки для работы приложения
	"require-dev" - для разработки (упаковщики, тесты и др.)

	Соответственно, библиотеки для разработки устанавливаем с флагом --dev:
	composer require phpunit/phpunit --dev

	composer install - установит на компьютер все пакеты (для разработки и продакшена: require + require-dev) из файла composer.lock 
	
	composer install --no-dev  - установятся только require библиотеки (для продакшена)
 			             то есть этой командой мы можем удалить перед продакшеном
				     библиотеки для разработки - для них сработает команда removing
	
	composer create-project laravel/laravel test-laravel  - команды для создания готовых проектов, эта команда 
                                                                создаст в этой папке папку test-laravel с приложением Ларавель

	--prefer-dist - флаг, исп. по умолчанию. Композер скачивает стабильные запакованные версии проекта, быстрый способ

	--prefer-source - композер скачает исходники проекта. Подходит для модификации исходного кода

111) Композер composer часто применяется для автозагрузки классов в приложении, более подробно ниже в 
     Автозагрузка классов композером

111) В JSON последняя строчка должна быть без запятой, это правило применяем для composer.json

111) composer.lock - хранит текущий список установленных зависимостей и их версии.
                     Команда composer install устанавливает версии с него если он есть, а если нет в соответствии с 
	             composer.json + создает composer.lock
	

	
	
	


































	
    

	
     
	
======================================================= PHP =============================================================	 

111) Guzzle - библиотека отправки HTTP запросов.
     
     Например, мы можем запрашивать json по api так:
	$var = file_get_contents('https://api.apilayer.com/key=123') - отправили http запрос, получили json

    С `file_get_contents` вы просто используете оболочку файловой системы для получения содержимого URL-адреса. 
    Если все, что вам нужно сделать, это выполнить операцию GET для высоконадежной конечной точки URL, чтобы получить ее содержимое,
    тогда подойдет `file_get_contents`.

    С Guzzle вы можете добавлять всевозможные параметры во время запроса, такие как заголовки для авторизации, 
    делать асинхронный запрос, анализировать полные коды ошибок в стандартные объекты исключений и т. д.
   
111) Если в JSON пришел объект, то распарсить его в ассоциативный массив можно c добавлением флага:
	$res = json_decode(file_get_contents($url), 1); // 1 - true

file_get_contents(url) получает содержимое файла в строку, а также получает данные по url, на этом основано общение 
с апи ботов: мы записываем в url гет параметры сообщения и запрашиваем с помощью file_get_contents этот урл, сервер в ответ
присылает данные, которые эта функция возвращает.

Также мы можем ввести в php код:
echo file_get_contents("https://yandex.ru/search/?text=file_get_contents+%D0%B4%D0%BB%D1%8F+url&lr=2");
и на странице покажется та же страница, как если введем ее в адрес браузера

111) Автоматический запуск сессий, 2 способа:
	1) В файле php.ini session.auto_start = 1
	2) В .htaccess php_value session.auto_start 1

111) set_time_limit(сек) - Задаёт время в течение которого скрипт должен завершить работу. 
                           Если скрипт не успевает, вызывается фатальная ошибка.
     По умолчанию максимальное время установлено в php.ini в max_execution_time (30 секунд)
     В open server это значение 60 секунд (может быть другое)

111) phpinfo(); - посмотреть php.ini, прописывать в файле php
     php -i - посмотреть php.ini в командной строке
     
     В опенсервере: Дополнительно -> PHP информация

111) Для того, чтобы поменять настройки php только для одного сайта в openserver можно:
	1) Заходим в Дополнительно -> Конфигурация -> файл настроек Apache (Apache_2.4-PHP_8.0-8.1+Nginx_1.23_servera.conf)
	2) Находим блок <Directory "%ssitedir%/*">
	3) Под ним в новом блоке пишем настройки для сайта bitrix.loc:
		<Directory "%ssitedir%/bitrix.loc">
    		   php_admin_value display_errors 0
		</Directory>
	4) Перезапускаем openserver
	5) Также вместо этого можно скопировать рядом лежащий Apache_2.4-PHP_8.0-8.1+Nginx_1.23_vhosta.conf 
           и поместить его в корень сайта поместив внутрь команду php_admin_value display_errors 0

111) Бесконечный цикл в php:  while(true) {}
	Пример:
		$i = 0;
		while (true){
    		   echo $i . PHP_EOL;
    		   $i++;
     		   sleep(1);}

111) PHP_EOL - константа хранящая символ перевода строки для данной сборки php(/n), например: 
	echo "текст<br>" - 1 способ
	echo 'текст' . PHP_EOL - 2 способ,  вставится /n

111) sleep(сек) - пауза в выполнении скрипта, например можно сделать бесконечный цикл и внутри него sleep(сек),
                  когда выполнение будет доходить до этой строчки наступает пауза, затем код продолжает выполняться.
		  Пригодилось в замедлении ответа бота на сообщение

111) Sleep::for(1.5)->minutes() - фасад в Ларавель работающий подобно sleep()

111) Скрипты в PhpStorm можно запускать не только в браузере, но и в консоли:
	1 способ: выбираем файл, правой кнопкой: run (файл), так запустил цикл while
		  Кнопка run - та же треугольная кнопка из панели debug
	2 способ: пишем в консоли php index.php, ответ придет также в консоль. Например, отправил через Guzzle 
		  get запрос на google.com

111) php интерпретатор - файл php.exe лежащий в openserver, можно посмотреть в phpinfo(), записан в константу:
	PHPBIN  c:\openserver\modules\php\PHP_8.0\php.exe

111) Когда прописываем в консоли php + др команды (artisan и т.д.), то обращаемся к интерпретатору php
     Можно делать то же самое в консоли windows: c:\openserver\modules\php\PHP_8.0\php.exe имя_файла.php

    Если в опенсервере меняем версию php, напр. на php8.1, то в шторме в настройках php не забываем поменять путь интерпретатора php

111) http_build_query([массив]) - php функция устанавливающая гет параметры 
	Пример: $url .= "?" . http_build_query(['width => 300', 'cost'=>500])
	
     Есть также: http_build_cookie(), http_build_url

111) В js гет параметры устанавливаются через searchParams: 
	let my_url = new URL("https://stackoverflow.com")
	my_url.searchParams.set('key', 'value')
    	console.log(my_url.toString())

111) file_put_contents(__DIR__ . '/logs.txt', 'текст сообщения', FILE_APPEND) - добавление в конец logs.txt сообщения,
	например для логирования. FILE_APPEND - флаг отвечает за добавление в конец файла

111) $var = print_r(data, 1) - флаг true делает так, чтобы print_r возвращала данные в переменную, а не выводила на экран.
			       В этом случает print_r возвращала красивый массив в файл лога

111) array_merge($array1, $array2) - объединяет массивы в один. Если есть одинаковые строковые ключи, то из них останется один.
				     Однако, числовые ключи не перезаписываются: array_merge([1,2],[1,2]) => [1,2,1,2]

111) [...$arr1, ...$arr2] - то же самое, называется spread, но не работает со строковыми ключами
				     		
111) __DIR__ - константа покажет путь к папке текущего файла, нет последнего слеша, 
               поэтому если конкатенируем с файлом, то __DIR__ . '/'. Пример: require_once  __DIR__ . '/db.php';
     __METHOD__ - покажет имя метода класса

111) Переключить на другую версию PHP:
     2 способа:
	1) Меняем интерпретатор php в настройках PhPStorm, например C:\OpenServer\modules\php\PHP_5.5\php.exe
           Cтраница в браузере не показывается почему-то, поэтому смотрим через run в консоли
     	2) Создаем домен в openserver, настройки -> Модули -> меняем версию php и сервер Apache, сайт смотрим по домену openserver	
	
111) file_get_contents('php://input') - позволяет читать тело POST запроса на сайт
     Получается, что $_POST - тело запроса разложенное по ключам
     Извлечение данных json из тела POST запроса:
	$var = json_decode(file_get_contents('php://input'));

111) Если отправляем данные в через фетч:
	fetch('req.php', { 
		method: "POST",
		body: JSON.stringify({query: 1111})})
     То не получится получить их в $_POST, нужно использовать:
	$query = json_decode(file_get_contents('php://input'))

111) $_REQUEST - универсальный массив для POST и GET данных

111) Строковые, числовые данные в POST могут прийти в обычном формате, например при отправке через форму:
	POST /page.php HTTP/1.1  // HTTP заголовки
	---------начинается тело -----------
	key1=value1&key2=value2&key3=value3
     
     Также возможна POST отправка сложных данных (объекты, массивы), их кодируют в json, тогда нам приходит такой запрос:
	POST /page.php HTTP/1.1
	---------начинается тело -----------
	{"key1":"value1","key2":"value2","key3":"value3"}

    $_POST в этом случае их не может раскодировать, но мы можем получить такие данные в сыром виде: 
    file_get_contents('php://input')

    file_get_contents('php://input') не работает при отправке формы multipart/form-data, 
    в этом случае файлы принимает: $_FILES, данные - $_POST

    Также $_POST не принимает данные PUT / PATCH / OPTIONS / HEAD / DELETE , их тоже нужно принимать
    через file_get_contents('php://input')

111) file_get_contents(url) - функция отправляет get запросы на указанный адрес и возвращает ответ, также читает файлы в строку

111) Логирование в файл:
	file_put_contents(__DIR__. '/log.txt', print_r($update, 1), FILE_APPEND);

111) Случайное изображение:
	"https://api.lorem.space/image/face?w=500&h=500&random=" . rand()

111) Для переноса строки в php echo используем PHP_EOL

111) file_put_contents($filename, $content) - функция записи в файл заменяющая fopen(), fwrite() и fclose() 
     Подходит если нужно только содержание файла, для более сложного http запроса используем библиотеку cURL

111) cURL - набор библиотек, в которых реализуются базовые возможности работы с URL страницами и передачи файлов. 
	    Библиотека поддерживает работу с протоколами: FTP, FTPS, HTTP, HTTPS, TFTP, SCP, SFTP, Telnet, DICT, LDAP,
	    а также POP3, IMAP и SMTP. Она отлично подходит для имитации действий пользователя на страницах и других 
	    операций с URL адресами.

111) При require переменных с другого файла phpStorm их не видит на текущей странице, для того, чтобы не подсвечивал:
	/**
 	* @var array $inline_keyboard
 	*/

111) В phpStorm файл время от времени сохраняется в Локальную историю, если потерял изменения можно тыкнуть правой кнопкой
на файл и посмотреть историю

111) Импортировать класс в PhpStorm:
	Пишем Str::class , кликаем на Str, далее Alt + Enter

111) Composer - php менеджер зависимостей такой же как js менеджер пакетов npm из ноды. Для установки библиотеки в папку можно прописывать 
команду установки, например: 
	composer require dejurin/php-google-translate-for-free
Или можно отредактировать файл composer.json:
	"require": {
    		"dejurin/php-google-translate-for-free": "^1.0"}
и вызывать команду composer update (наподобие npm i)

Насколько понял при первоначальной установке библиотеки композером он сам создает в нужной папке файл composer.json 
в отличие от node, которая установит библиотеку в ближайшей родительской node_modules, 
если не создали package.json (npm init)
npm init создает файл package.json или установливает прописанные пакеты из файла package.json если он уже создан.

111) Переустановить пакеты черех композер:
	1) Удалить пакет
	2) composer install - проверяет есть ли composer.lock. Если есть устанавливает пакеты в соответствии с ним.
                              если нет, то устанавливает пакеты в соответствии с composer.json и создает composer.lock

111) composer update - устанавливает последние версии в соответствии с composer.json и пересоздает composer.lock

111) /../ - выйти из директории на уровень вверх
     __DIR__ . '/../folder/ - выйти из текущей директории на уровень вверх и зайти в папку folder

111) Загрузка библиотеки из Гитхаба:
	1) Находим страницу в гитхабе
	2) Нажимаем зеленую кнопку Code -> Download Zip

     Либо через композер.

111) Кудлай советует для работы с письмами использовать библиотеку PHPMailer вместо функции mail
	
111) ALT + Enter при клике на класс импортирует его пространство имен;

111) $num = $var ?? $var2; // - функция проверки на null: 
					если $var не равен null, то $num = $var
					если $var = null, то $num = $var2
     Иначе говоря: $num = isset($var) ? $var : $var2

111) str_contains('где ищем', 'что ищем') - поиск подстроки в строке

111) extract(['key1' => 123, 'key2' => 456]) - превращает ключи массива в переменные

111) str_replace('что ищем в строке', 'на что меняем', 'строка') - заменяет подстроку

111) is_file(путь) - является ли файлом

111) __FILE__ - волшебная константа, показывает путь до файла в котором константа вызвана

111) ob_start() - буферизация вывода
     $var = ob_get_clean() - Запись буфера в переменную и очистка
	Пример:
	ob_start()
	require_once index.html
	$content = ob_get_clean()

111) htmlspecialchars($str) - преобразует спецсимволы в html сущности, нужна для защиты от внедрения тегов, например:
	htmlspecialchars('<script>alert(1)') => '&lt;script&gt;'

111) mysqli_real_escape_string() - экранирует спецсимволы \n, \r, \, ', "  в SQL запросе.
				   Например, нужно экранировать апостроф, чтобы не было ошибки в:
				   INSERT INTO orgs SET name = 'Papa's grill' 
     Пример:
	$name = "Papa's grill";
	$name = mysqli_escape_string($link, $name) => "Papa\'s grill" // $link - из настроек соединения, нужно для опр. кодировки
	$query = "INSERT INTO orgs SET name = '$name'"

     Но вообще лучше использовать Eloquent или RedBeanPHP, там все встроено.

111) Emmets:
	php => <?php  ?>
	rt => return true
	rf => return false

111) require - одно и то же можно подключить несколько раз на странице, например, часть верстки
     require_once - аналогично require но PHP проверит, включался ли уже данный файл, и если да, не будет включать еще раз.
		    Например, при подключении переменных.

111) Отключение кеша в Хром: Network - Disable cache, работает при открытой консоли

111) Вызов кода:
	php => <?php  ?>
	ph => <?= ?>

111) button:hover - наведение на кнопку
     button:active - только на время нажатия
     button:focus - нажатие, но не было клика в другом месте экрана

111) in_array(значение, массив) - php функция, определяет есть ли значение в массиве

111) Windows + Alt + R - видеозапись экрана (начать и остановить). Сохраняется в папке Видео

111) В font-awesome есть крутящиеся элементы:
	fa fa-spinner fa-spin - крутится загрузка
	
	





































======================================================= Фреймворк с нуля  =============================================================

111) Фреймворк с нуля от Кудлая - папка shop.loc в openserver-sites

111) Перенаправление HTTP запроса в папку public можно делать в настройках openserver, когда папку домена указываем как
     site.ru/public, либо в настройках корневого .htaccess.  Кудлай говорит, что если есть openserver, то первый вариант
     правильный.
     .htaccess - файл настройки сервера Apache, распространяет действие на все дочерние папки.

111) Автозагрузка классов композером:
     1) В корне создаем composer.json:
	{
		"autoload": {
    		   "psr-4": { // автозагрузка классов в соответствиии с psr-4
      		       "wfm\\" : "vendor/wfm/", // если пространство имен wfm\, то классы ищутся в папке vendor/wfm
      			"App\\" : "app/"}}, // если ПИ App\, то классы ищутся в папке app/
  		"require": {} - тут будут установленные пакеты
	}
    
     2) Далее в консоли composer install : 
			a) создастся папка vendor, установится композер
			б) в папке vendor лежит файл autoload.php
     3) В vendor создаем папку wfm
     4) Теперь для автозагрузки классов нужно в index.php  require_once autoload.php. 
        Кудлай делаем это в init.php, а тот уже подключает к index.php

     5) В init.php задаем константы приложения:
		define("ROOT", dirname(__DIR__)) и т.д.
	        require_once .../autoload.php

     6) В index.php:  require_once dirname(__DIR__) . '/config/init.php';

111) Путь к корню сайта:
	1) dirname(path) => C:\Users\Owner\Desktop\studing\openserver-sites\shop.loc  // значение настройки сервера
	2) $_SERVER['DOCUMENT_ROOT']; => C:/Users/Owner/Desktop/studing/openserver-sites/shop.loc // фактический путь
	
	dirname(path) и $_SERVER['DOCUMENT_ROOT'] могут отличаться

     	dirname(__DIR__)  - возвращает имя родительского каталого текущей папки

111) Для того, чтобы приложение прекращало работу, если используется неподдерживаемая версия PHP:
	if(PHP_MAJOR_VERSION < 8) {die('Необходима версия PHP >= 8');}

     Проверяется изменением версии PHP и сервера в openserver

111) Способ записи в переменную  массива из другого файла:
	return [...] // Массив в файле возвращаем через return
	$params = require_once 'params.php'; // в переменную $params записываем этот массив

111) Ручное выбрасывание ошибки:
	throw new Exception('Сообщение ошибки', код, файл, номер строки); // нужно импортировать класс?? use Exception;

111) http_response_code() - установка кода HTTP ответа

111) global $var внутри функции позволяет вызвать/записать в глобальную область видимости переменную

     $num = 10
     function __construct(){
        global $num; // обращаемся именно к глобальной переменной, иначе в функции будет локальная переменная
	$num = $num*2}

111) $_SERVER['QUERY_STRING'] - строка запроса после домена:
	http://shop.loc/page/contact?num=5      =>       page/contact&num=5

111) urldecode("str") - перекодирует url наподобие js decodeURIComponent. Должна быть кодировка utf-8

     urldecode("Ant%C3%B4nio+Carlos+Jobim") => Antônio Carlos Jobim
     utf8_decode(urldecode("Ant%C3%B4nio+Carlos+Jobim")) => раскодировать по utf8

     header('Content-Type: text/html; charset=UTF-8') - установка кодировки UTF-8

111) $query = trim(urldecode($_SERVER['QUERY_STRING']), '/')-раскодировать строку запроса после домена, убрать последний /

111)    trim($str) - убрать пробелы по краям
	trim($str, '/') - убрать слеши по краям
	ltrim($str, '/') - убрать слеш слева (left)
	rtrim($str, '/') - убрать слеш справа (right)

111) Превращение строки с тире в CamelCase:
	$name = str_replace('-', ' ', $name); // заменяем тире на пробелы
	$name = ucwords($name); // каждое слово с большой буквы
	$name = str_replace(' ', '', $name); // удаляем пробелы

        Пример: 'new-product' => 'NewProduct'

111) Псевдонимы, алиасы в mysql:
	SELECT p.*, pd.* FROM product p JOIN product_description pd on p.id = pd.product_id
     Насколько понял ставим после названия таблицы букву, она будет псевдонимом

111) $_SERVER['HTTP_REFERER'] - адрес с которого был переход на страницу

111) $num1 = 1;
     $num2 = &$num1; - амперсанд обращается к ссылке на переменную в php

111) После php редиректа обязательно прописываем die иначе код дальше будет выполняться
	header("Location: $redirect");
    	die;

111) array_key_exists($key, $array) - есть ли такой ключ в ассоциативном массиве
     in_array($value, $array) - есть ли в массиве значение

111) str_replace можно использовать для удаления из строки лишнего, применяя пустую строку для замены, например:
	$url = str_replace('https://', '', 'https://ya.ru/'); => ya.ru/
	trim(str_replace('https://', '', 'https://ya.ru/'), '/') => ya.ru  // так убрали слеши с краю

111) explode(разделитель, строка, кол-во частей) - разбивает строку по разделителю и помещает в массив
     explode('-', '2025-12-31') => ['2025', '12', '31']

111) implode(разделитель, массив) - преобразует массив в строку с указанным разделителем
	implode('-', ['a', 'b', 'c', 'd', 'e']) => 'a-b-c-d-e'

111) array_shift($arr) - удаляет из массива первый элемент и возвращает его
     array_unshift($var) - добавляет в начало массива элемент или несколько
     array_push($var) - добавляет в конец массива один или несколько элементов
     array_pop() - удаляет последний элемент из массива и возвращает его

111) $$param - превращает значение переменной $param в название новой переменной.
     Например:
	$param = 'key'
	$$param = $param => создается новая переменная $key = 'key'

111) file_exists(файл) - проверка существования файла

111) array_merge($arr1, $arr2) - слияние массивов

111) md5($str) - хеширует строку. Функция быстрая, поэтому для безопасности не рекомендуется
             Пример: md5('Hello') => 8b1a9953c4611296a827abf8c47804d7
     md5_file(file.txt) - хеширует файл
	     Пример: md5_file(1.txt) => d41d8cd98f00b204e9800998ecf8427e

     То есть, получается Ларавель хеширует названия файлов при загрузке в Storage

111) serialize() - способ преобразовать данные в строку, работает аналогично json_encode(), но:
	1) правильно обрабатывает все PHP-шные типы данных, включая объекты ваших собственных классов и т.д. 
	2) формат json более универсален при обмене данными между приложениями
	3) json_encode() быстрее чем serialize()

111) serialize(данные) - преобразует данные без потери их типа и структуры для хранения, передачи, записи в файл.
                         Так, можно сериализовать объекты, массивы и записывать их в файл txt
			 file_put_contents('file.txt', $arr)
                         В БД такие данные нужно хранить в поле типа BLOB

    Пример: serialize(array("Red", "Green", "Blue")) => a:3:{i:0;s:3:"Red";i:1;s:5:"Green";i:2;s:4:"Blue";}

    unserialize(данные) - обратное действие, преобразование сериализованных данных в переменные/вывод PHP

111) time() - php функция показывает timestamp в секундах

111) unlink($file) - удаляет файл по полученному пути

111) Тень под блоками  делать такого же цвета как блок и большой, выглядит красиво, пример: https://kontur.ru/
     Бургер с компа там тоже открывает красивое плавное меню.

111) novalidate - отменяет валидацию формы у инпутов  required, pattern, email, url: 
		  <form action="" method="post" novalidate>

111) Насколько понял, механизм кеширования - это запись многократно используемого кода в файл/БД, 
     чтобы потом просто возвращать верстку/данные, без генерирования

111) ob_start() - буферизация вывода, чтобы не было вывода на экран
	Пример:
		ob_start();
       	        require 'index.php';
        	return ob_get_clean();

111) Для корзины удобно использовать сессии и выгружать их в БД при оформлении заказа

111) abs(num) - php функция, модуль числа

111) Fontawesome  заработал https://fontawesome.com, в поиске вбивать название иконки, выбирать free

111) unset($_SESSION['cart'][$id]) - удаление переменной, напр. сессии

111) По умолчанию php функции возвращают void (то есть запрет возврата), это поведение меняют через return
     Используют: return false, return true, return.
     	1) Дальнейший код функции прекращает выполнение
	2) Функцию можно протестировать
	3) Удобство чтения, ист. причины
  
111) return можно использовать за пределами функции в глобальной ОВ.
     В этом случае он прекращает выполнение дальнейшего кода файла.  

111) void - показывает, а также запрещает функции что-то возвращать, void - в переводе "пустой"
            Если функция void что-то возвращает, то: Fatal error: A void function must not return a value
	
      public function func() : void { код, который ничего не возвращает }

      php функции без возврата (void по умолчанию, void установленный, а также просто  return) - возвращают NULL

111) array_keys(массив) - получает ключи массива и записывает их в новый массив
     array_values(массив) - получает значения массива и записывает их в новый массив
     implode(разделитель, массив) - превращает массив в строку вставляя между значениями разделитель

111) 	MariaDB (ответвеление MySQL) - самая популярная БД, исп для простых приложений
	PostgreSQL (произошла из Ingres) - говорят самая передовая БД для серьезных приложений
	SQLite - БД для небольших приложений с небольшим трафиком, находится в одном файле
	
111) RedBeanPHP - класс для работы с БД, библиотека, одна из представителей ORM (Object-Relational Mapping) инструментов.
	          ORM - прослойка между кодом и БД, позволяет работать с БД через объекты.
		  Например, Laravel использует ORM Eloquent (можно использовать отдельно от Ларавель).
		  ORM позволяют обезопасить работу с БД, напр. через PDO, подготовленные выражения.
     Работа:
	1) Установка 
	     1 способ: качаем с сайта download/Download RedBeanPHP 5 all-drivers   файл rb.php
                       require 'rb.php';
		       R::setup(); // проверяем, что нет ошибок, дальше можно смотреть по композеру

	     2 способ: через композер, Кудлай говорит лучше через него
		       1) создаем composer.json // в phpstorm New -> composer.json
		       2) Прописываем зависимость: "require": {"gabordemooij/redbean": "dev-master"}
		       3) composer install // устанавливаем зависимости
		       4) require_once 'vendor/autoload.php'; // подключаем автозагрузчик классов композера
		       5) Подключаться можно прописывая:
			   А) use \RedBeanPHP\R as R; // импортируем пространство имен в каждом файле где нужен класс
			      R::getAll("SELECT ...") // используем класс
	
			   Б) \RedBeanPHP\R::getAll("SELECT ...") // используем класс каждый раз прописывая путь до класса
			
			   В) class_alias('\RedBeanPHP\R', '\R') - один раз в файле (c R::setup ??)
			      R::getAll("SELECT ...") // используем класс
					
		       6) R::setup(); // проверяем, что нет ошибок
		       7) Окончательно настраиваем:
			   R::setup( 'mysql:host=localhost; dbname=mydatabase', 'user', 'password'); // просто настройки, само подключение 
													будет когда начнем работать с БД
		       8) var_dump(R::testConnection()); - возвратит true, если подключились к БД, можно проверять перед работой с БД и выбрасывать ошибку

		       В итоге подключение: require_once 'vendor/autoload.php'; class_alias('\RedBeanPHP\R', '\R'); R::setup( 'mysql:host=localhost;dbname=lists','root', '' );
					
	2) Работаем: $products = R::getAll("SELECT * FROM product"); // используем класс R, также есть R::getAssoc
		     $count = R::count('product', "category_id IN (?, ?, ?)", [$a, $b, $c]); // количество по условию
		     R::count('orders', 'user_id = ?', [$user_id]); // количество по условию
		     $count = R::count('product') // количество

111) Пример 1:
	public function get_product($slug, $lang): array {
        return R::getRow("SELECT p.*, pd.* FROM product p JOIN product_description pd ON p.id = pd.product_id 
	                  WHERE p.status = 1 AND p.slug = ? AND pd.language_id = ?", [$slug, $lang]);}

     Сокращения названий таблиц применяем тогда, когда более одной таблицы, т.к. при одной таблице достаточно 
     писать свойства без названия таблицы, пример:
   	R::getAll("SELECT * FROM product_gallery WHERE product_id = ?", [$product_id]);
	
    Пример 2 - через объектную модель:
	$order = R::dispense("orders"); // создаем бин, в скобках указана таблица
	$order->user = 'Денис'; // записываем в свойства бина значения, схоже с Ларавель
	$order->sum = 5000;
	$order_id = R::store($order); // сохраняем в БД, возвращается id вставленной записи

    Пример 3 - произвольный sql запрос:
	R::exec("INSERT INTO products SET product = ?", [$name]);

111) http_response_code(404) - получает или устанавливает код ответа HTTP

111) Можно хранить массив в отдельном файле c return сверху: return ['key' => 'value'] // файл config.php
     А затем в другом файле записывать его в переменную: $arr = require_once 'config.php'

     Также любят хранить в свойствах класса: public static array $params = [];  
     При создании объекта класса вызывают метод заполнения этого массива:
		public static function load(){if(file_exists('config.php')){ // упрощенно
            		self::$params = require_once 'config.php';}}

111) Получается файлы php, которые просто return массив в других местах записываются в переменные, если будет:
	$var = require_once 'config.php'
	Также можно выполнять код и в конце делать return результата, также запишется в свою переменную, но в новом файле 
	становятся доступны подключенные переменные, функции и др. код.

	Файл config.php:
		$str = 'строка';
		return 10;

	Подключаем в другом файле: 
	1 случай:
	require_once 'config.php' 
	echo $str; // выведет 'строка'
	
	2 случай:
	$var = require_once 'config.php';
	echo $var; // выведет 10
	echo $str; // выведет 'строка', то есть переменная доступна, хоть и записывали все в переменную $var

	3 случай (через буфер обмена):
	ob_start();
        require_once 'config.php';
        $var = ob_get_clean();
	echo $var; 

111) include включает и выполняет указанный файл, если файла не нашлось, то E_WARNING
     require делает то же самое, но при ненахождении файла ошибка E_ERROR // то есть строже
     include_once и require_once - если код из файла уже был включён, он не будет включён и выполнен повторно

111) require (include) подключает файл, переменные:
	Верстка - автоматически выведется
	Переменные - станут доступны (даже если был ob_start())
	return - возвратит значение (сможем записать его в переменную)

	Если подключаемый файл ничего не return, то require возвращает true, выводит верстку, делится переменными.

	Рассмотрим случаи для подключаемого файла file.php:
	1) Просто верстка
	<div>lorem ipsum</div>

	require_once 'file.php' - на экран выведется верстка
	$var = require_once 'file.php' - на экран выведется верстка, в переменную запишется true

	2) Верстка и return:
	<div>lorem ipsum</div>
	return [1,2,3]

	require_once 'file.php' - на экран выведется верстка
	$var = require_once 'file.php' - на экран выведется верстка, в переменную запишется [1,2,3]
	ob_start(); require 'file.php'; $var = ob_get_clean();  - в переменную запишется верстка, автоматически не выведется
	ob_start(); $var = require 'file.php'; ob_get_clean();  - в переменную запишется [1,2,3], верстка не выведется

111) ob_start() складывает в буфер обмена все то, что страница выводит в браузер: верстку, echo, print_r
     Например, нам нужно require_once файл, но в нем есть верстка, соответственно верстка выведется в новом файле.
     мы можем весь вывод забуферизировать и очистить, либо записать в переменную и очистить буфер.
	
     file.php:
---------------------------------
 	<?php
  	$str = 'строка';
	echo 555;  ?>

	<div>Параграф</div>	
---------------------------------
     Другой файл:

     ob_start()
     require_once 'file.php';
     $var = ob_get_clean(); // если не очистить буфер через ob_get_clean(), то вывод будет

     Ничего не выведется. 
     Также мы все равно имеем доступ к переменным подключенного файла: echo $str => выведется 'строка';

     То есть ob_start() + ob_get_clean() повлияли только на вывод верстки, переменных через echo, <?=  ?>, все остальное 
     работает как при обычном require, переменные из подключаемого файла все равно будут доступны и др.
	  
     ob_start() - «Начните запоминать все, что выводится в браузер, но пока ничего с этим не делайте»
 
     Удобно также для записи верстки в переменную:
	Вместо: <?php $content = '<div><span>text</span><a href="#">link</a></div>'; ?>	
	Можно: 
		<?php ob_start(); ?>
		   <div>
    		     <span>text</span>
    		     <a href="#">link</a>
		   </div>

		<?php  $content = ob_get_clean(); ?>

     Также, используется, например, если в скрипте есть вывод, а в нашем коде ниже 
     мы используем header(«Location: /newaddress»)), которая не сработает, если был вывод раньше (ошибка Заголовки уже отправлены)

     Также в буфере можно вставлять переменные в подключаемый файл, выполнять другие действия, например, 
     заменять слова перед выводом на экран и др.
		        if (file_exists($viewPath)) {
				  ob_start();
				  $data = $page->data;
				  extract($data);    // массив в переменные
				  include $viewPath; // подключаем файл с представлением
				return ob_get_clean()};
	
      Еще вариант - функция в классе для возврата верстки по пути:
		protected function getHtml():string {
        		ob_start();
        		require_once $this->tpl; // в tpl хранится путь к файлу
        		return ob_get_clean();} // возвращаетс верстка
                 Теперь в переменную мы можем записать верстку: $var = getHtml()

111) $var = $a ?? 1; тернарное уловие на isset, $var = $a, если $a != null, иначе $var = 1;
                     То же самое длиннее: $var = isset($a) ? $a : 1;
     $var = $a ?: 1 - тернарное условие на if, то же самое: $var = $a ? $a : 1; то же: if($a) {$var = $a} else {$var = 1}

isset($var) - работает только с переменными, нельзя проверить напрямую isset(5)

     Разница: 
	$a = ''       if($a) - false
	$a = 0        if($a) - false
	$a = null     if($a) - false
	$a = []       if($a) - false
	$a = '0'      if($a) - false
	$a = false    if($a) - false
	$a = 'false'  if($a) - true
	$a = ' '       if($a) - true

	$a = ''       empty($a) - true
	$a = 0        empty($a) - true
	$a = []       empty($a) - true
	$a = '0'      empty($a) - true
	$a = null     empty($a) - true
	$a = ' '      empty($a) - false
	
	$a = ''       isset($a) - true
	$a = 0        isset($a) - true
	$a = []       isset($a) - true
	$a = '0'      isset($a) - true
	$a = null     isset($a) - false

	if - генерирует уведомление, если переменная не определена
	empty - безопасно проверяет на if без уведомлений, если переменная не определена
	isset безопасно проверяет на null
	is_null - противоположность isset, но генерирует уведомление, если переменная не определена 

        empty - установлена ли переменная и не равна '', 0, '0', null, false 
	isset - установлена ли переменная и не равна null	

111) @ - оператор подавления ошибок, используется в выражениях, на экран не будут показывать ошибки, код будет продолжаться
	$a = @file_get_contents('file.php') ?? 11111; // file.php - не существует
	echo 555; 
	выведется 555;

111) В php также как в js есть стрелочная запись функции:
	fn($value) => $value > 1

111) Пагинация на основе класса:

	$pagination = new Pagination($page, $perpage, $total);

	При создании объекта пагинации передаем в конструктор:
	   1) текущую страницу из гет параметра page текущей страницы
           2) количество товаров для вывода на одну страницу
	   3) общее число товаров

	Класс внутри метода getHtml() создает верстку пагинации, добавляет ссылкам гет параметры и пр., далее маг. метод
        public function __toString(){ return $this->getHtml(); }
	
	Теперь просто в верстке делаем echo и магический метод возвращает верстку:
	<?= $pagination ?>

111) Вместо index.php можно разместить файл с другим названием, только сайт нужно будет открывать как: test.ru/file.php
     Получается по умолчанию ищется файл index. Это поведение можно изменить в .htaccess

111) В гет параметр селекта отправляется значение value, если его нет, то textContent.
     При выборе option №1 url будет: ?salary=1,  при выборе option №2: ?salary=2000
     <form> // можно без формы, на основе аякс запроса по событию change для select, либо изменение window.location (ниже)
	<select name='salary'>
	   <option value="1">1000</option>
    	   <option>2000</option>	
     	</select>
        <input type="submit">
     </form>

111) Принцип Белый список для запроса к БД.
     Допустим, запрос к БД формируется на основе get параметра sort из url, который в свою очередь формируется, например по селекту.
     Формируем допустимый список гет параметров и их sql команды:
	$values = ['title_asc' => 'ORDER BY title ASC', 'title_desc' => 'ORDER BY title DESC'] // белый список допустимых значений
	$order_by = '';
	if(isset($_GET['sort']) && array_key_exists($_GET['sort'], $values)){ // если get параметр sort установлен и есть в белом списке
            $order_by = $sort_values[$_GET['sort']]; // формируем подстроку sql запроса
        }
	$query = "SELECT * FROM products $order_by" // в строку запросу подставляем команду

111) Совмещение js переменных и php переменных:
	let PATH = '<?= PATH ?>'; // теперь js переменная PATH равна php переменной PATH

111) Получается форма form + submit создает переход на новую страницу с изменением параметров массива:
	 1) $_GET: формируется через get параметры, код php читает $_GET и формирует страницу на их основе (запрос к БД и пр.)
                   То же самое происходит при переходе по ссылке <a href="?sort=asc">Отсортировать</a>, только здесь заданы жестко
	 2) $_POST: после submit переменные вставляются в body http запроса, на их основе  формируется $_POST, сервер отдает новую 
                    страницу на основе обработки php скриптом массива $_POST

Получается, что форма это просто переход на другую или эту же страницу с созданием массивов $_GET или $_POST, 
а ссылка это простой переход с жесткими переменными $_GET

Получается, с сервером мы взаимодействуем отправляя http запросы с GET или POST переменными или установкой заголовков,
которые сервер видит в $_SERVER.

Взвимодействовать с сервером (отпр. http запросы) мы можем:
	1) Аякс запросы. http запрос отправляется без перезагрузки страницы. Можем менять заголовки, $_GET, $_POST
	2) С помощью form и submit. Меняем $_GET, $_POST
	3) По ссылке. Меняем $_GET
	4) Через js изменением location по событию. Пример: window.location = 'http://ya.ru?sort=asc' // запросили страницу через get запрос
                                                                                                       + изменили массив $_GET
	В последнем варианте нам не нужна форма, мы отслеживаем событие js, например, change на инпуте и переходим на другой адрес
        с установленными get параметрами (например, по option value), а php код их читает и выдает страницу  

111) Удобная фильтрация товаров как раз реализуется через js change + изменение window.location с новыми гет параметрами

111) <select>
	<option selected disabled>Выберите пункт</option> / disabled позволяет запретить выбор этого option
	<option>Пункт 1</option>
     <select>

111) Товары из корзины можно хранить в куках. Например в списке id товаров.
	Получить из кук:
	$wishlist = $_COOKIE['wishlist'] ?? '';
	if($wishlist){ $wishlist = explode(',', $wishlist); }

111) array_map() - php функция, прогоняет массив черех функцию.
		   	1) Встроенная функция:
		   		$list = array_map('intval', $arr) - приведение всех элементов массива к числам
			2) Наша функция: 	
				$func = function($value) {return $value * 2;};
			        $list = array_map($func, $arr) 

111) array_unique($arr) - убрать дубли, причем строки и числа - разные элементы
     array_unique($arr, SORT_NUMERIC) - элементы сравниваются как числа
     array_unique($arr, SORT_STRING) - элементы сравниваются как строки

111) in_array($id, $wishlist) - php функция, проверяет есть ли элемент в массиве

111) В куках можно сохранять только строки.
	1) Массивы преобразуем в строки через json.stringify()
	2) Массивы преобразуем в строки через implode(',', $arr);
	
     Затем устанавливаем куки: setcookie('key', $str, time() + 3600*24*7*30, '/');

111) Аякс добавление в корзину и прочее:
	Просто отправляем гет или пост запрос на страницу сайта, передаем гет параметром данные.
	То же самое, как переходим по ссылке, но без перехода.
        Например аякс запрос добавления товара в корзину: fetch(`site.ru/add=${id}`)
	Одновременно в функции аякс запроса меняем иконку у товара на Добавлено, например, через смену класса (удобно
	если иконки фонтэвесом)

111) За счет кук реализованы сессии (сеансы). Если в браузере для этого сайта сохранена кука: PHPSESSID = 59cdpbffv....
то при обращении к странице сайта он ее отправит и сервер найдет у себя файл сессии 59cdpbffv.... и выгрузит оттуда переменные. В файле сессии можно хранить много переменных, а в куках для домена только 4кб и не более 50 штук.

Как правило, рекомендуется использовать файлы cookie только для отслеживания сеанса через куку PHPSESSID, потому что
куки передаются туда и обратно при каждом http запросе

111) Код в php уничтожается при формировании страницы, соответственно все служебные контейнеры с переменными тоже.
     Сохранение информации в php реализуем через куки, а лучше сессии, а контейнеры при загрузке страницы
     будут брать переменные уже оттуда и переиспользовать их через свои статические свойства, методы. 

     Например сохраняем товары корзины и сеанс или куку.
     При загрузке страницы служебный класс читает и записывает в свое свойство куки и затем формируем иконки           проверяя через иф свойства служебного класса.

111) sweetalert2 - библиотека показа красивого алерта, например, добавления товара в корзину

111) array_search($key, $arr) - ищет элемент в массиве и возвращает ключ элемента

111) property_exists($this, $k) - проверка на существование свойства в объекте, например при создании объекта через 			          конструктор передаем в него асс. массив со свойствами:
	public function __construct($options = []){
        	foreach ($options as $k => $v) {
            		if (property_exists($this, $k)) { // если в классе обозначено свойство
                	$this->$k = $v;}}}  // то заполняем его
			         
111) method_exists() - то же самое, но для проверки существования метода в созданном объекте

111) Интересный способ создания сущностей через конструктор на примере вставки верстки:
	class Page(
		protected $data; // сюда складываем верстку переданную в конструктор
		protected $class; // здесь класс
		public function __construct($options = []){ // принимем массив ['data' => '<p>Текст</p>', 'class' =>'myClass']
			foreach ($options as $k => $v) { 
            			if (property_exists($this, $k)) {
                			$this->$k = $v;}} // заполняем свойства $data и $class
		$this->run()} // тут же в конструкторе при создании объекта возвращаем верстку

		protected function run(){ // функция возврата верстки
			echo "<div " . $this->class . ">" . $this->data . "</div>"})

    Вставляем верстку в нужное место:
	<div> <?= new Page(['data' => '<p>Текст</p>', 'class' =>'myClass']) ?> </div>

111) Можно брать часть кода из файла дампа sql и вставлять в phpmyadmin только нужные таблицы, например, выбрать только 
создание таблицы CREATE TABLE `user` и вставку туда данных INSERT INTO `user`

111) В php классах можно объявлять:
	1) public array $attributes = [] - тип хранимого свойства
	2) public static function func():bool {} - тип возвращаеммых функцией данных может применяться вне класса

111) valitron - простая php библиотека для валидации инпутов форм, есть на packagist.org (vlucas/valitron)

111) Показ и удаление флеш сообщений:
	<?php if (!empty($_SESSION['errors'])): ?> // если в сессию была записана ошибка
		<div> <?php echo $_SESSION['errors']; unset($_SESSION['errors']); ?> </div> // показываем и удаляем переменную сессии
	<?php endif; ?>

111) Заполнение полей формы, если была ошибка валидации. Чтобы пользователь не вводит два раза
	1) После отправки формы помещаем в сессию массив со значениями инпутов
		$_SESSION['form'] = ['name' => 'Денис', 'salary' => '500000'] // пропущен этап создания массива инпутов
	2) Создаем функцию:
		function get_field_value($name) {
    			return isset($_SESSION['form'][$name]) ? htmlspecialchars($_SESSION['form'][$name]) : '';}
	3) Вставляем функцию в верстку:
		<input name="name" value="<?= get_field_value('name') ?>">
		 <?php if (isset($_SESSION['form'])) { unset($_SESSION['form']);} ?> // чтобы после обновления данные из формы пропали

111) $pass = password_hash('пароль', PASSWORD_DEFAULT) - хеширование пароля перед вставкой в БД, соль вставляется сама
     password_verify('введеный_пароль', "хешированный_пароль") - проверяет, соответствует ли пароль хешу. Совместима с паролями созданными 
     через crypt().  Функция безопасна для атак по времени - то есть проверка соответствия не прекращается как только найден первый несоответствующий
     символ, строка проверятся до конца и злоумышленник не знает на каком символе могла быть прекращена проверка.

111) Звездочка для пометки обязательности элемента:
	1) Задаем элементу класс required
	2) .required::before {content: '* '; color: #f00; font-weight: 700;} // звездочка добавляется для элементов с классом required

111) Принцип запуска методов во фреймворке при переходе по ссылке:
	http://shop.loc/cart/add?id=5
	cart - название контроллера
	add - название метода в контроллере
	id - гет параметры передающиеся в метод

111) <select name="cities[]" multiple> - если отравляем много значений через multiple, то в name указываем массив name="cities[]"
	<option></option>
	<option></option>
     </select>

111) В сессию php можно грузить также массивы:
	$_SESSION['user']['email'] = cined@mail.ru

111) Транзакции в MySQL - группа запросов, рассматриваемых как одно целое. БД выполняет либо все полученные запросы 
                          как только получит команду commit, либо ни одного.
			  Транзакции не прерываются другими запросами к БД, т.е. когда выполняются запросы транзакции, 
			  доступ к записям никто получить не может.

111) Механизм транзакций в RedBeanPHP - помогает сохранить данные в несколько таблиц, при ошибке хотя бы в 1 табл. откатит изменения:
	public static function saveOrder($data):int { // функция сохранения данных в несколько таблиц
        R::begin();
        try {
            $order = R::dispense("orders"); // в скобках указываем название таблицы
            $order->user_id = $data['user_id'];
            $order->note = $data['note'];
            $order->total = $_SESSION['cart.sum'];
            $order->qty = $_SESSION['cart.qty'];
            $order_id = R::store($order); // сохраняем

	    $user = R::dispense("users"); // то же самое для другой таблицы
            ...

            R::commit(); // фиксирование успешности всех изменений, БД выполняет изменения
            return $order_id;
        } catch (\Exception $e){
            R::rollback(); // если R::commit() не сработает, то в БД отрпавляется команда rollback - откат изменений
            return false;}}

	В общем структура RedBean транзакций:
		function download($data) {
        		R::begin();
        		try {
            		   Здесь запись нескольких строк в БД
            		R::commit();
            		return true;
        		} catch (\Exception $e){
            			R::rollback();
            			return false;}}
	
111) В MySQL есть встроенный механизм транзакций
      	
	function save(){
		$db = mysqli_connect('localhost','Viktor','1234','for_cart');
		mysqli_autocommit($db,FALSE); // отключаем автокоммит после каждого запроса
		try {
			$sql1 = "INSERT INTO zakazi (title) VALUES ('$value')";
			$result1 = mysqli_query($db,$sql1);
			
			if(!$result1) {
				throw new Exception();}

			$sql2 = "INSERT INTO otpravleno (ti1tle) VALUES ('$value')";
			$result3 = mysqli_query($db,$sql2);

			if(!$result3) {
				throw new Exception();
			}

		} catch(Exception $e){
			mysqli_rollback($db); } // если была ошибка отправляем в БД команду на откат всех изменений
		mysqli_commit($db);} // если не было ошибок в БД отправляеся команда на выполнение изменений

111) $res = array_merge($arr1, $arr2) - сливает массивы в один общий

111) rtrim($str) - удаляет пробелы в конце строки
     rtrim($str, ',') - удаляет указанный знак в конце строки

111) if($var = func()){} - в $var записывается результат функции func, затем проверяется условие
			   Теперь ниже мы можем использовать $var

111) $_ENV - глобальный массив переменных оболочки PHP. 
	     Используется, например, при запуске приложения для настройки доступа к БД через встраивание в оболочку PHP собственных глобальных переменных.
	     Массив $_ENV может быть пуст,  для включения нужно активировать его в php.ini
     Для работы с $_ENV в php существуют функции:
     getenv('key') - получение переменной среды $_ENV
     putenv('key') - установка

     В Ларавель такие переменные среды для приложения записываются в файл .env (обр. через помощник env()

111) sprintf() - php функция форматирования строки с вставкой переменных в нужные места
		Пример:
			$num = 5;
			$location = 'tree';
		$format = 'There are %d monkeys in the %s';
		echo sprintf($format, $num, $location); => There are 5 monkeys in the tree
		 
111) Отправка писем:
	1) Устанавливаем phpmailer
		composer require phpmailer/phpmailer
	2) Куча строчек настройки, есть в описании пакета и в папке сайта shop/Модели/Order))

     В тело письма пишем обычную верстку html, вместе с тегами head, body, как обычная страница.
     В верстку вставлена таблица table, в почтовом ящике она нормально открывается.

     Верстку грузил через буфер обмена (здесь вставки переменных в верстку нет, но есть выше):
	    ob_start();
            require \APP . "/views/mail/{$tpl}.php"; // выбор макета в соотв. с аргументом функции
            $body = ob_get_clean();

111) Функция вывода верстки по нужному пути:
	function getPart($file){
		$file = "/views/{$file}.php";
		if(is_file($file){
			require $file; // вставится в верстку
		} else {
			echo "Файл {$file} не найден";})}
    
     Теперь вставляем в верстку: <?php getPart('cabinet_sidebar'); ?>

111) Хранение файлов на сервере с доступом только авторизованным пользователям. Например, цифровых товаров.
     Файлы на сервере храним с измененным названием: file.txt -> file.txt.sDoUIYdmHmH
     Соответствия названий файлов и измененных названий храним в БД.
     Ссылку на файл в личном кабинете пользователя делаем через id записи в БД: <a href="download?id=<?= $file['id'] ?>">Скачать</a>	
     По клику на ссылку php проверяет сессию пользователя, ищет файл по id в БД и отдает меняя название файла на оригинальное (берет из БД).
     Код написан в Полезное -> выдача файла на скачивание.txt	
     Соответственно:
	1) Неавторизованный пользователь не сможет скачать файл
	2) Даже если попробовать прописывать путь к файлу вручную по типу: public/file.txt, 
           то будет пусто, п.ч. хранится по изменному названию: public/file.txt.sDoUIYdmHmH
     
111) AdminLTE
	https://github.com/ColorlibHQ/AdminLTE/releases/tag/v3.1.0 // есть версии выше
	В самом низу ссылка на зип архив, качаем
	в папке pages примеры страниц
	Качаем страницы, устанавливаем для них прикрепленные скрипты, стили, напр. основная страница - blank.html
	В формах есть текстовый редактор

111) <base href="/"> - тег html, вставляется в head, добавляет в начало всех ОТНОСИТЕЛЬНЫХ ссылок указанное значение
                       применяется, чтобы не вставлять в отностельные ссылки повторяющуюся папку
     Правила: 
	1) Ссылка из base должна оканчиваться на слеш <base href="http://shop.loc/admin/">, иначе не работает, 
           даже если отн. ссылка нач. со слеша. При проверке оказалось, что ссылка в base усекается до первого слеша с конца, поэтому не работает.
	   Как если  <base href="http://shop.loc/admin"> будет обрабатываться как http://shop.loc/
	2) base нужно прописывать в head. Будет работать только для следующих ниже ссылок
	3) Будет работать только для ОТНОСИТЕЛЬНЫХ ссылок, то есть <a href="link.html">ссылка</a>, или  <a href="pages/link.html">ссылка</a>
           если ссылка будет абсолютная  <a href="http://site.ru/link.html">ссылка</a>, то будет открываться она 
	4) Относительные ссылки пишем без начального слеша: <a href="link.html">ссылка</a>
	5) В коде ссылка будет выглядеть так как прописана в коде: <a href="link.html">ссылка</a>, 
	   а при открытии в начало будет добавлять адрес из base, в итоге откроется файл: http://shop.loc/admin/link.html
	6) Сработало для стилей, скриптов, картинок, ссылок
	7) Применял для создания страниц для админ-панели: <base href="http://shop.loc/admin/">

111) Вставка в верстку переменных: 
	<h1><?= $title ?? '' ?></h1>

111) strtr(где меняем, массив замен) - php функция замены символов/подстрок в строке, подходит для транслитерации
	strtr('вода', ['в' => 'v', 'о' => 'o', 'д' => 'd', 'а' => 'a']) => voda

111) Текстовый редактор и файловый редактор - Кудлай предлагает CKEditor и CKFinder, лежат в папке Полезное

111) Поиск строк по букве/фразе в БД удобно выполнять через метод sql LIKE

111) is_numeric($num) - является ли переменная числом или строкой с числом

111) Редактирование записи из БД: если запрос через get, то показываем запись в инпуте формы.
	                          если запрос через post (т.е. отредактировали и нажали submit), то обновляем запись в БД

111) public function getProduct($id):array|false {} - функция возвращает либо массив, либо false
     public function update_product($id):bool {} - вернет true или false

111) key(['key1'=>123, 'key2'=>321]) - php функция возвращает первый или единственный ключ массива: key1

111) Выбрасывание ошибки php:
	throw new \Exception('Not found product', 404);
	throw new \Exception('Деление на ноль.');	

111) Установка отмеченного чекбокса:
	<input type='chekbox' <?= $product[$checked] ? 'checked' : '' ?>>

111) array_diff($array1, $array2) - php функция сравнивает array1 с одним или несколькими другими массивами и возвращает значения 
				    из array1, которые отсутствуют во всех других массивах.

111) Проверка, что файл в инпут type='file' загружен и нет ошибок:
	if(empty($_FILES) || $_FILES['file']['error']){} // массив не пустой и в нем не заполнено встроенное поле ошибок

111) end([1,2,3]) - php функция, возвращает последний элемент массива: 3
     current([1,2,3]) - то же, но первый элемент: 1 (так как внутренний указатель текущего элемента установлен на первый элемент)
	
111) in_array(что_ищем, массив) - php функция, проверяет наличие в массиве

111) uniqid() - php функция, генерирует случайный id на основе timestamp. Подойдет для простых случаев, например, 
                добавления случайной строки к расширению загруженного файла (чтобы не могли скачать с сервера введя путь и имя файла)
                Криптографически небезопасна. Пример: uniqid() => 63b7685db28d4

111) Функция загрузки файла из инпут type='file' на сервер.
	Сначала файл доступен в массиве $_FILES в папке временных файлов: 
		 $_FILES['file']['tmp_name'] => C:\OpenServer\userdata\temp\upload\phpEDCA.tmp
	С помощью php функции move_uploaded_file его перемещают в нужную папку

	Пример: 
	  function upload_file() {
            $file_name = $_FILES['file']['name'] . uniqid(); // получаем наподобие бот.txt63b76ca38377d
            $path = WWW . '/downloads/' . $file_name; // устанавливаем путь где будем хранить файл 
							 C:\shop.loc/public/downloads/бот.txt63b76ca38377d
            if(move_uploaded_file($_FILES['file']['tmp_name'], $path)){ // перемещаем туда файл
              return ['original_name' => $_FILES['file']['name'], 'filename' =>  $file_name];} // возвращаем правильное и измененное 
												  имя файла для сохранения их в БД
            return false;}

111) Функция удаления файла с сервера и БД:
	function delete($id) { //передаем id записи имени файла в БД
        $file_name = R::getCell("SELECT filename FROM download WHERE id = ?", [$id]); // получаем имя бот.txt63b76ca38377d
        $file_path = WWW  . '/downloads/' . $file_name; // получаем путь к файлу на сервере
        if(file_exists($file_path)){ // если по этому пути файл существует
            if(unlink($file_path)){ // удаляем его
                R::exec("DELETE FROM download WHERE id = ?", [$id]); // удаляем запись из БД
                return true;
            } else {
                return false;}}
            return false;}

111) unlink(путь_к_файлу) - удалить файл

111) @ - php оператор подавления любых сообщений об ошибках, ставится перед командой
	 Например: @unlink(путь_к_файлу);

111) Показ ошибок настраивается в php.ini в директиве error_reporting
     В файле мы можем настраивать ее так в начале каждого скрипта:
	error_reporting(-1) - показ всех ошибок (то же: error_reporting(E_ALL) и ini_set('error_reporting', E_ALL))
	error_reporting(0) - отмена показа ошибок
	error_reporting(E_ERROR | E_WARNING | E_PARSE) - показ определенных ошибок
             
     Что значат ошибки:
	1) Неустранимые ошибки:
		E_FATAL - скрипт не может продолжать работу
		E_ERROR - серьезная, но скрипт скорее всего продолжит работу, скорее всего неправильно. Напр., вызов несуществующей функции
		E_PARSE - синтаксическая, например, забыли поставить круглую скобку
	2) Устранимые, скрипт продолжит работу:
		E_WARNING - предупреждения, например, если функция требует 3 параметра, а Вы передаёте только 2
		E_NOTICE - напр., не объявил переменную, или обратился к несуществующему элементу массива
		E_DEPRECATED - при использовании устаревших функций и др.

111) Связывание в SQL:
	SELECT o.*, op.* FROM orders o JOIN order_product op ON o.id = op.order_id WHERE o.id = $num

111) Преобразование get параметра в цифру:
	$status = $_POST['status']; // принимаем 1 или 0
	$status = ($status == 1 ? 1:0);

111) Показ кнопок на изменения статуса:
	<?php if ($order['status'] == 0): ?> // принимаем переменную $order['status']
            <a href="order/edit?&status=1">Изменить на Завершен</a> 
        <?php elseif($order['status'] == 1): ?>
            <a href="order/edit?status=0">Изменить на Новый</a>
        <?php endif; ?>

111) if($_SERVER['REQUEST_METHOD'] == 'POST') - проверка, что http запрос был методом POST
     $request->isMethod('post') - то же самое для Ларавель


==================================================================================================
    
Отправка fetch запроса на контроллер Ларавель на примере простой функции:
  1) в head прописываем токен: 
	<meta name="csrf-token" content="{{ csrf_token() }}">
  2) Прописываем принимающий роут:
	Route::post('/bills', [BillController::class, 'bill'])->name('bill'); - не забываем, что post
  
  2) Фетч запрос (X-CSRF-TOKEN нужен только для POST запросов):
	fetch('{{ route("bill") }}', { // в num отправляем что нужно, кавычки ставим снаружи фигурных скобок
	    method: 'POST',
            headers: {                                                                     // для Ево токен по-другому: body: JSON.stringify({qqq: 1, _token: csrf_token}), код в разделе ево
		'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').content, //прикрепляем токен, был еще
		'Accept': 'application/json', 						     same-origin, но работает без него
                'Content-Type': 'application/json'},  					     или 'X-CSRF-TOKEN' : document.querySelector('input[name="_token"]').value
	    body: JSON.stringify({a: 1, b: 2})}) - для примера отправляем объект. Можно всю форму: body: new FormData(form)                                                             
                .then(res=>res.json()) - если ждем json                           А получить:  $request->all();                                                          								
                .then(res=>console.log(res)) // делаем что нужно				     
                .catch(e=>console.log(e))

  3) Метод контроллера:
	use Illuminate\Http\Request; - в шапке импортируем именно этот класс

	public function bill(Request $request){ - 
        	return $request->all();} // что получили, то в ответ и отправили, просто для примера

  4) Можно использовать именованные маршруты во встроенных js скриптах внутри blade файла:
	fetch("{{ route('getProducts') }}"{...}) - указываем кавычки

  5) Узнать в контроллере с какой страницы пришел fetch запрос:
	url()->previous() - полный адрес
	basename(url()->previous()) - path
  6) Прикреплять в форму 'X-CSRF-TOKEN' нужно только если запрос POST. Если форма отправляется как get, то он не нужен

========================================= DaData ==================================================

Бесплатная подписка позволяет делать 10000 запросов в сутки.

Для автодополнения используем методы ::prompt

Вообще можно отправлять post запросы напрямую через скрипт js и fetch на сервере Dadata, но у меня в браузере включилась защита,
в интернете было написано, что срабатывает addblock.

Сделал проще: установил пакет Dadata для Ларавель. Теперь запрос сначала идет на свой сервер, тот отправляет запрос через пакет
на сервере Dadata (через curl), получает ответ и присылает на сайт. Называется проксирование через свой сервер. Вроде мало сказывается на 
задержке. + в пакете много методов.

Как пользоваться пакетом Dadata в Ларавель:
  1) Регистрируемся на Dadata, получаем токен и secret в личном кабинете
  2) Устанавливаем пакет: 
	composer require movemoveapp/laravel-dadata
  2) В .env прописываем:
	DADATA_TOKEN=токен
	DADATA_SECRET=secret
	DADATA_TIMEOUT=10
  3) Создаем контроллер Dadata: 
	php artisan make:controller DadataController
  4) Используем в контроллере (в Ево по-другому, есть ниже):
	public function getIp(Request $request){
		try { // оборачиваем в try, так как Dadata может прислать коды 400 (некорректный запрос), 429 (много запросов) и пр.
			$inn = $request->inn; - авт. распарсивание входящего json и получение значения по ключу inn
			$ip = DaDataCompany::prompt($inn, 5, [CompanyStatus::ACTIVE], CompanyType::INDIVIDUAL); } // вернет найденные ИП 
			return json_encode($ip); // или делаем что нужно. Здесь я отправил список ИП на сайт для автозаполнения
		catch (\Exception $e) {										 
            		dd($e->getMessage());}} //для отлаживания, вернет ошибки в браузер

  5) Также можем работатать с контроллером через fetch, например автозаполнение названия организации по введенному ИНН:
	a) Создаем принимающий роут:
		Route::post('/dadata', [DadataController::class, 'getIp'])->name('getIp'); // идет на метод выше
	б) На странице вставляем токен csrf ( он же <input type="hidden" name="_token" value="...">) 
				       или в шапке <meta name="csrf-token" content="{{ csrf_token() }}">
	в) Отправляем в скрипте фетч запросы на этот роут	
		document.addEventListener('DOMContentLoaded', function() { // чтобы нашлась нода кнопки, так как скрипт гружу в шапке
		    let input = document.querySelector('#inn')
		    input.addEventListener('input', function (e) {
			fetch('/dadata', {
            		    headers: {
                		'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').content, // для Ларавель обязательно X-CSRF-TOKEN. Для Ево нужно вставлять как body: JSON.stringify({qqq: 1, _token: csrf_token})
                		'Content-Type': 'application/json',					   // чтобы Ларавель приняла запрос
                		'Accept': 'application/json',},						   // либо если есть @csrf, то ищем:
            		    method: "POST",								   // document.querySelector('input[name=_token]').value
            		    body: new FormData(form),}) // здесь передаем что нужно, в данном случае вся форма
							      // можно: body: JSON.stringify({inn: input.value}) // для Ево добавлять _token: токен
				.then(response => response.json())
            			.then(result => console.log(result)) // либо делаем что нужно
            			.catch(error => console.log("error", error));})	})}, false);
	г) А в принимающем методе getIp возращаем в фетч:
			return response()->json($ip);

Таким образом мы проксируем запросы через свой сервер, а на сервере через пакет Dadata 
выполнять различные методы (параметр count определяет количество возвращаемых значений, максимум 20):
   DaDataAddress::standardization('Челябинск, Елькина, 63Б - 102'); - отдаст информацию по адресу (гео, индекс, область и пр)
   DaDataAddress::iplocate('46.226.227.20', 2);::prompt('москва хабар', 2, Language::RU) - подсказки по адресам (исправит опечатки, покажет список адресов)
   DaDataAddress::iplocate('46.226.227.20', 2);::geolocate('55.878', '37.653', 2) - определит адрес по координатам
   DaDataAddress::iplocate('46.226.227.20', 2) - город по ip адресу
   DaDataAddress::postalUnitByAddress('Челябинск, Елькина', 2, Language::RU) - ближайшее отделение почты
   DaDataAddress::postalUnitById(453805, 2, Language::RU) - отделение почты по индексу
   DaDataName::standardization('Денисс анотольевич роднов') - исправит опечатки, напишет в прав. порядке, 
							      разобьет на имя, фам, отч; предоставит 
						              массив ФИО по падежам
   DaDataName::prompt('Викто', 10, Gender::UNKNOWN, [Parts::NAME]) - автодополнение (меняем Parts, для фамилии, отчества)
								    Также работает как Пунто Свитчер: «fynjy» → «Антон»
   DaDataEmail::standardization('rodnovspb@mail/ru') - исправляет опечатки в email, классиф. адреса на личные (@mail.ru), 
						         корпоративные (@myshop.ru), ролевые (info@)
   DaDataEmail::prompt('anton@', 10) - подсказывает почту, исправляет опечатки
   DaDataPhone::standardization('89514466332') - работа с телефоном: город, оператор, форматирует телефон 
   DaDataCompany::id('7707083893', ...)	- поиск организации/ИП по ИНН, ОГРН, отдаст также адрес
   DaDataCompany::prompt('Роднов', ...) - автодополнение при вводе, ищет по ИНН, ОГРН, названию, фио ИП;
					  учитывает где находитесь по ip. Присылает полные реквизиты вместе
				          с найденным значением
   DaDataBank::id('044525225') - банк по БИК, ИНН, названию
   DaDataPassport::standardization('8006134167') - проверка на действительность паспорта

111) Использование Dadata вне Ларавель:
	1) composer require hflabs/dadata + нужен установленный Газзл
	2) require_once 'vendor/autoload.php'; - здесь грузим автозагручик композера
	3) $token = "3a3164b3b82f2d86aa1ff7625eef3eeb3ba4202e";
	   $secret = "8eefefbeddf3678c71d5d9d4749d617510d4cb32";
           $dadata = new \Dadata\DadataClient($token, $secret);	
	   $ip = $_SERVER["REMOTE_ADDR"] - основное решение, но есть расширенные функции             
	   $result = $dadata->iplocate($ip); - город по ip
	4) Когда человек ходит по страницам сайта каждая страница заново пытается отпределить ip
	   Яндекс и Гугл боты также запускают определение ip
	   Поэтому:
		1) Запоминаем результат и не делаем повторных вызовов на других страницах
		2) Вызываем API из браузера, а не с сервера — это защитит от ботов.

111) Установка Dadata в Ево:
	1) cd custom  (в файле composer.json должно быть  "require": {})
	2) composer require movemoveapp/laravel-dadata
	3) Все остальное как в Ларавель, но в Dadatacontroller:
		use MoveMoveIo\DaData\DaDataCompany; use MoveMoveIo\DaData\Enums\CompanyStatus;use MoveMoveIo\DaData\Enums\CompanyType;
		$dadata = (new DaDataCompany)->prompt(701407934037, 1, [CompanyStatus::ACTIVE], CompanyType::INDIVIDUAL); //отличается от Лары
	4) На уровне core/composer.json сделать composer update, составится карта классов, автоматически притянется custom/composer.json
           он есть в разделе core/composer.json => "include": ["custom/composer.json"],
        4) В фетч запросах csrf токен крепим в body под ключом _token
	   				 	
		






		     
     



	

	





		


	
		


	

     
    
	
     

	
	

	
	

    




     
         
	





======================================================= PHP классы  =============================================================
111) ООП придуман для того, чтобы уменьшить дублирование кода

111) Класс - это шаблон, по которому создается объект. Благодаря классу можно создавать по одному шаблону много объектов.
Передавая разные параметры в конструктор мы создаем отличающиеся объекты.
Кроме того, класс внутри себя может самостоятельно заполнять дополнительные свойства на основе переданных в конструктор.

Пример:
--------TelegramBot.php----------------------
	class TelegramBot{
	    protected $token;
	    public function __construct($token){
		$this->token = $token;}
	    public function sendMessage($url, $text){
		return json_decode(file_get_contents($url, $text))}}
--------index.php----------------------
	require_once 'TelegramBot.php';
	$telegram_bot = new TelegramBot('5681045251') // создали через конструктор
	$telegram_bot->sendMessage('http://url.ru', 'сообщение')

То есть классы позволяют создавать однотипные объекты с набором встроенных свойств и методов, использовать 
в сторонних файлах свои методы(статические),  переиспользовать методы друг в друге через extends, трейты и др.

При расширении класса через extends автоматически запускается метод __construct родительского класса. 
В нем можно прописать логику срабатывающую для каждого дочернего класса.
Например, выгрузку из БД каких-либо данных и запись их в public свойство

111) Там, где в php используется '->' - происходит работа с объектом созданным на основе класса

111) Один класс должен располагаться в одном файле, и кроме класса в нем ничего не должно быть

111) Имя файла должно совпадать с названием класса, это не обязательно, но пригодится.
     И название файла и класс пишем в CamelCase

111) new FirstClass() - создание объекта класса 

111) Свойства (public, protected, private, static) - те же переменные установленные в классе, могут быть пустыми или 
                                             с уст. значениями.
111) Методы также могут быть public, protected, private, static, если модификатор не задан, то по умолчанию public.
     Пишутся в CamelCase: public function getCarInfo(){}

111) public - свойство и метод можно использовать и изменять у объекта вне класса, наследуется классами-потомками (раньше называлось просто var)
     private - вспомогательные свойства и методы, которые доступны только в классе-родителе, не наследуются
     protected - наследуются другими классами, но нельзя применять в объектах
     static - используются только с именем класса через ::, либо внутри самого класса через self::

111) Внутри класса к его свойствам и методам можно обращаться:
	1) self::  - к статическим и константам (будет работать и для нестат., но не рекомендуется)
	2) $this->  - к нестатическим
	class User {
		public $var = 5; - 
	        private static func1(){}
		
		public func2(){
			echo $this->$var; - обращаемся к нестатическому свойству
			echo self::func1();}} - обращаемся к статической функции	

111) Объект класса наследует public МиС родительского класса, обращаться к ним можно через ->
     $obj = new Car();
     $obj -> property // обращаемся
     $obj -> newProperty = 80 // можно устанавливать объекту новые МиС, но это не рекомендуется

111) static - методы и свойства класса, которые можно использовать без создания объекта. 
     То есть в таких методах нет обращения к свойствам объекта.
     Используются так: 
	Class::func()  - метод
	Class::$property - свойство

    В статичное свойство класса можно грузить информацию, а затем обращаться к ней. Пример на основе неизменяемого контейнера:
	---------------------Класс - контейнер --------------
	class MyClass {
		public static $container;

		public static function loadContainer(){
        		if(is_null(self::$container)){ // если пустой, то
            		self::$container = запрашиваем информацию из БД}} // запрашиваем информацию из БД и грузим в контейнер

		public static function show(){ // Функция показа валюты, напр.
        		self::loadContainer();
        		return self::$container;}}

	--------------------- Родительская страница --------------
		<?php
			require_once 'MyClass.php';

			for ($i = 0; $i < 10; $i++){
    				include 'card.php';}

	--------------------- Карточка card.php --------------
		<div>
		  <h1>Карточка</h1>
		  <p>  <?php echo MyClass::show() ?></p>
		</div>

Например, для каждой карточки товара нам нужна валюта. Карточки создаются через foreach на главной странице и вставляются через
include. Первая карточка запускает show(), грузится контейнер, при создании следующей карточки show() берет валюту уже из контейнера.
То есть MyClass на первой интерации был создан из дочернего кода и существует глобально, пока родительская страница отрабатывает код.
Также переменные вместо контейнера можно сохранять в сессию.

111) Сложные имена переменных, в том числе свойства объектов можно писать в двойных кавычках, 
     но нужно брать в фигурные скобки:
	echo "Моя машина: {$obj->brand}"

111) error_reporting(-1) - показывать в отчете все ошибки 
                           (аналогично error_reporting(E_ALL) и ini_set('error_reporting', E_ALL);)
     error_reporting(0) - выключить показ ошибок (некоторые все равно будут показываться, например parse)

Показ ошибок: 
Включение показа ошибок, если страница на хостинге:
	error_reporting(E_ALL);
	ini_set('display_errors', 'on');

Отключение показа ошибок на выложенном сайте:
	error_reporting(0);
	ini_set('display_errors', 'off');

Эти настройки меняют переменные в php.ini

111) Внутри методов класса если обращаемся к его же свойству или методу, то обращаемся через $this->свойство/метод
     $this - текущий объект

111) Конструктор: 
	Устанавливать свойства объекту можно через $obj -> свойство :
	$obj = new Car();
        $obj -> color = 'blue'

     	Но можно и более простым способом, при создании объекта через конструктор:
	public function __construct(){}

     Функция-конструктор автоматически вызывается при создании нового объекта
     Пример:
	class Car {
    	  public $color; 
    	  public $speed;
    
    public function __construct($a, $b){
        $this->color = $a;
        $this->speed = $b;}}

Теперь при создании объекта сразу заполним его через конструтор:
	$car = new Car('white', 1000);

В конструктор можно вставлять значения по умолчанию: public function __construct($b, $a='yellow') // опциональные параметры 
												     указываем последними

111) Деструктор __destruct вызывается в момент удаления объекта. Можно, например, распечатать что-нибудь, 
     закрыть соединение с БД и др.

111) static - методы и свойства класса, использующиеся без создания объекта. Вызываются только у класса через ::  
              Также у них возможны модификаторы доступа: public, protected, private

111) Class::$property  - обращение к статическому свойству снаружи класса
     Class::func() - обращение к стат. методу снаружи класса
     self::$property - к свойству внутри класса
     self::func() - к методу внутри класса

     Пример:
     class Car {
	public static $countCar = 0;
	public function __construct() {
        self::$countCar++;}} // при каждом создании объекта возрастает счетчик $countCar
	

111) self применяется для обращения к статичным МиС и константам самого класса внутри себя. 
     Однако будет работать внутри класса и обращение через Car::$countCar++,
     но не рекомендуется, например можем переименовать класс и все сломается.

111) Константы - неизменяемые свойства класса. Как и к static к ним также обращаются только через класс:
	class Car {const CAR_SPEED = 300}
	Car::CAR_SPEED - снаружи класса
	self::CAR_SPEED - внутри класса

111) Car::class - специальная константа хранящая имя класса включая пространство имен

111) Быстро добавить геттер или сеттер на свойство класса в phpStorm: мышку на свойство + ALT + Enter
     Пример геттера:
	 public function getCpu(){
        	    return $this->cpu;}

111) Наследование классов - для чего? 
     Допустим у нас есть сущности Книга и Ноутбук. У них есть общие свойства: Название, Цена 
     и свои свойства: Автор и Процессор. Работу с такими сущностями организуют так:
	- создается родительский класс Product с общими свойствами Название, Цена
	- создаются классы для уникальных свойств: Book {Автор}, Laptop {Процессор}
	- Для создания объекта Book наследуем класс с общими свойствами Product:
		class Book extends Product {}
     Наследование класса возможно только один раз и наследуются только публичные методы и свойства. Однако возможны цепочки
     наследований.

111) И у дочернего Book и у родительского Product есть функции-конструкторы ожидающие свое аргументы. 
     Для того, чтобы при создании объекта класса Book заполнился и родительский конструктор, в классе Book используем
     обращение к родительскому конструктору через parent::__construct:
	class Book {
		public $author; // уникальное свойство объекта Книга
		public function __construct($name, $price, $author){ // конструктор объекта Book
        	parent::__construct($name, $price); // заполняем родительский конструктор Product
		this->author = $author;}} // заполняем свое свойство $author

	$book = new Book("Робинзон", 200, 'Дефо'); // создаем объект класса Book, конструктор родителя всегда
						   // принимает аргументы первым

111) Также через parent:: можно обращаться к родительскому методу, например:
	$out = parent::getProduct(); // обращение к родительскому методу getProduct

     Также можно в дочернем классе создать такой же метод, воспользовавшись результатом одноименного родительского:
	public function getProduct() { // свой одноименный метод для дочернего класса
        	$out = parent::getProduct(); // используем результат родительского getProduct()
        	$out .= "Процессор: {$this->author}<br>"; // вставляем свое свойство author
        	return $out;}

111) При наследовании автоматически запустится родительский конструктор. Но если в дочернем классе также есть свой конструктор,
     то он перепишет родительский. Поэтому, чтобы родительский тоже отработал нужно в дочернем конструкторе выполнить parent::__construct:
	public function __construct() {
    		parent::__construct();
		...какой-то код...
	}

111) Наследование.  МиС доступны внутри самого класса, а также:
 	public - доступность у всех классов-потомков и объектов
	protected - доступность только у классов-потомков
	private - доступность только внутри класса, в котором объявлено

111) Соответственно, публичное свойство можно изменить в самом объекте, доступа к protected и private у него нет
     
111) Для работы с private и protected свойствам извне используются геттеры и сеттеры внутри класса. Одним из их 
достоинств является возможность контроля устанавливаемых и возвращаемых значений:
	 public function setDiscount(int $discount) { // сеттер, ждет число
        	$this->discount = $discount;}

	    public function getDiscount(): int { // геттер, возвращает число
        	return $this->discount;}

111) Абстрактные классы используются только для наследования другими классами их методов и свойств. 
     Из абстрактных классов нельзя создать объект - это основная причина их создания.

111) Абстрактные классы могут содержать как обычные методы, так и абстрактные не имеющие тела.
     Реализация таких методов - уже задача потомков. Если класс содержит хоть один абстрактный метод, 
     то он он должен тоже должен стать абстрактным.

111) Интерфейс - класс в виде набора публичных методов имеющих свойства абстрактных, то есть:
	1) Методы публичные
	2) Методы не имеют тела
	3) Все методы обязательны к реализации в потомках
	4) От интерфейса нельзя создать объект

111) Интерфейсы - те же классы, соответственно могут иметь константы.

111) Именование интерфейсов: interface iMath(); 

111) Классы могут наследовать через extends только один другой класс.
     Однако классы могут наследовать любое количество интерфейсов - методом implements

111) Если запрашиваем в файле несколько классов, интерфейсов, то в require_once важен порядок. 
     То есть если классы расширяются внутри, то сначала подгружаем то что запрашивается, потом файл, который запрашивает.

111) var_dump($a instanceof A) - является ли объект $a экземпляром класса 
     var_dump($a instanceof iA) - instanceof подходит и для интерфейсов

111) При расширениях классов объекты являются экземплярами как родительского класса, так и расширяющих его родителя
классов, интерфейсов, по цепочке.

Если class C extends B и class B extends A, то объекты класса С являются экземплярами классов С, B, А.

111) function offerCase(Product $book) { // мы можем использовать в функции контроль типов принимаемых объектов
    	 echo "<p>Автор: {$book->getAuthor()}</p>";}

Практический пример контроля типов - в Ларавеле принятие реквеста с инпута: function(Request $request){}

111) Автозагрузка классов.
    Для загрузки в файл сторонних классов нам требовалось для каждого класса прописывать require_once "Класс"
    Однако есть способ загружать класс автоматически когда он понадобится.
    Для этого создаем функцию загрузки класса:
	function load($class) {
    		$path = __DIR__ . "/classes/{$class}.php"; // класс вызывается в index.php, рядом с ним лежит папка classes
    		if(file_exists($path)) {
		      require_once $path;}}

   И регистрируем ее: 
	spl_autoload_register('load')

   Итого, как работает:
	1) Создаем функцию автозагрузчик load($class) и регистрируем ее в spl_autoload_register('load')
	2) $book = new Book(); // вызываем класс
	3) Если класс ранее не был подключен, то срабатывает spl_autoload_register, вызывающая load($class), в параметр 
	   передается запрашиваемое имя класса. 

111) spl_autoload_register вызывается только в случае подключения несуществующего класса. Сама по себе не срабатывает.
     в PHP есть встроенная spl_autoload_register() c автозагрузкой, описание ниже

111) В spl_autoload_register можно регистрировать несколько функций, последовательно вызывающихся:
	spl_autoload_register('func1')
	spl_autoload_register('func2')

111) Пространства имен нужны чтобы избежать конфликт классов имеющих одинаковые имена.
     Задается через namespace App\Http\Controllers;
     Для упрощения автозагрузки классов рекомендуется задавать пространства имен совпадающие с путем к файлу.

     В namespace принято прописывать путь к файлу начиная от корня приложения. Например: 
     namespace Core\Data\

     Это своеобразный ПРЕФИКС к имени класса и не более того, подключать классы через require или 
     автозагрузчик - нужно все равно.

     Теперь при вызове класса мы всегда должны использовать имя класса вместе с этим префиксом.

     Пример:
	класс Page в папке user:
		namespace User
		class Page {}
	класс Page в папке Admin:
		namespace Admin
		class Page {}

     Поключаем оба класса в файле index.php:
	require_once '/admin/Page.php'; // Нам по-прежнему нужно подключать классы
	require_once '/users/Page.php'; // Потому что пространство имен это только префикс и ничего больше
	$adminPage = new \Admin\Page; // Как видим теперь наш класс имеем префикс с пространством имен
	$userPage = new \User\Page; // И поэтому нет конфликта одинаковых имен

111) При обращении к классу по пространству имен начинаем с можно обращаться относительно корня или текущего ПИ:
	new \Admin\Page - обращение к классу относительно корня приложения
	new Model\Page - относительно пространства имен самого файла
     
111) namespace можно задавать не только классам, но и обычным файлам.
     Теперь при обращении к другим классам можно пропускать совпадающее имя
	namespace Admin\Data;
	
	new Core\Controller; // эквивалентно \Admin\Data\Core\Controller

111) Другой способ сократить обращение к namespace подключаемого файла - использование импорта пространств имен через use:

	Вместо $data1 = new \Core\Admin\Data();
	
	Мы можем заранее импортировать пространство имен:
		use \Core\Admin\Data;  //где Data - файл Data.php
		$data1 = new Data();

        Например, в коде верстки нужны статические методы объекта php.
	<head>
    	  <?php use \Bitrix\Main\Page\Asset; ?>

    	<title><?php $APPLICATION->ShowTitle(); ?></title>
    	<?php Asset::getInstance()->addCss(); ?>   // вместо \Bitrix\Main\Page\Asset::getInstance()->addCss();

111) Как видим в верхнем примере после use к файлу можно обращаться просто по имени.
     Когда имена разных подключаемых файлов совпадают перезначзначаем имя:
	use \Core\Admin\Data as NewName

111) Встроенная в PHP spl_autoload_register()

	spl_autoload_register() запускает автоматическое require запрашиваемых классов, но нужно выполнить соглашение:
	1) путь по папкам сайта к подключаемому классу/файлу должен совпадать с пространством имен файла
	2) имя файла должно совпадать с именем хранящегося в нем класса
	3) имена папок должны быть в нижнем регистре

     spl_autoload_register пишется в начале того файла, который вызывается через адресную строку браузера. 
    
     Внутри функция запускает встроенную в php функцию загрузки запрашиваемых файлов.

     Пример: 
	Есть корневой файл index.php, рядом папка pages с файлом Book.php, в нем класс Book
     	Код файла  Book.php:
		namespace pages; // по соглашению указываем путь от корня к этому файлу
		class Book {public $book = 'Робинзон Крузо';} 
	
	Код index.php:
		spl_autoload_register();
		$book = new \Pages\Book(); // встроенный автозагрузчик ищет класс по пути \pages\book.php
		echo $book->book;

	Мы можем сократить обращение к файлу:
		namespace Pages; // либо use pages\Book;
		spl_autoload_register();
		$book = new Book(); // встроенный автозагрузчик ищет класс в файле \pages\Book.php
		echo $book->book;

	Как работает: 
	Допустим запрашиваем класс \Pages\Book =>
		function loader($path){
			$file = __DIR__ . "/" . "{$path}.php"; // создается путь /pages/Book.php
			if(file_exists($file)){
			     require_once $file}} // файл Book.php подключается к index.php

	Мы опустили, то, что в Линуксе, установленном на большинство серверов есть разница между слешами \/,
        Windows поймет путь с обоими вариантами, а для сервера в автозагрузчике происходит замена слешей:
		$file = str_replace('\\', '/', $path) . '.php'; // теперь все слеши /

	Также для Линукса при автозагрузке важен регистр названий, для Windows нет.

111) PHP не поддерживает множественное наследование классов, то есть мы можем наследовать 
только один класс (но несколько интерфейсов).
Трейт представляет собой класс(набор свойств и методов), который можно включить в другой класс, причем 
в отличие от обычных классов можно наследовать несколько трейтов.
В отличие от интерфейсов трейты имеют реализацию функций.

Можно сказать, что трейт - просто переиспользуемый код. Применяется, если не хотим дублировать одинаковый код в разные классы.
Рекомендуют трейты не применять, так как трейты перезаписывают одинаковые методы друг друга и пр. Хотя в Ларавель куча трейтов.

Объявление: trait Helper{}
Наследование трейтов: class User { use Helper;} // use именно в теле. Если use вне класса, 
						// то это загрузка пространства имен

Загрузка пространства имен:
	1) use app\folder  - над классом
	2) namespace Admin\Data -  над классом
	3) new Core\Controller в коде или в классе
	4) new \Admin\Data\Core\Controller в коде или в классе

Как и при наследовании класса, теперь мы можем обращаться к МиС трейта как к своим: echo $city->getName();
От трейта, так же как от абстр. класса и интерфейса нельзя создать объект.

111) Наледование:
	1) классов: class User extends Employee // можно наследовать только один класс
	2) интерфейсов: class User implements Salary, Description // можно наследовать несколько интерфейсов
	3) трейтов: class User { use Trait1; use Trait2;} // наследуем неск. трейтов

111) Цепочка методов:
     $var = $book->doAction1()->doAction2()
     Для того, чтобы была возможна цепочка вызовов мы должны в предыдущем методе doAction1() прописать 
     возврат объекта:
	public function doAction1() {
	----делаем что-то----
		return $this;}

111) Магические методы вызываются автоматически при опр. действии над объектом.
	1) __construct - вызывается при создании экземпляра класса (объекта)
	2) __destruct - при удалении объекта (напр. логирование, закрытие соединение с базой и т.д.)
	3) __toString - вызывается при попытке приведения объекта к строке
		public function __toString(){
			return $this->name;}
		$user = new User('john', 25);
		echo $user; // вызывается __toString
	4) __get - срабатывает при попытке обратиться к несуществующему или защищенному свойству
         
          Например: $obj->property
	  Если property защищено или не существует, то при наличии сработает функция:
		public function __get($property){} // в параметр передается запрашиваемое свойство
	  Таким образом можно реализовать геттер любых приватных свойств:
		class Test {
		private $prop1 = 1;
		private $prop2 = 2;
		
		public function __get($property){ // вызывающееся свойство попадает в параметр
			return $this->$property;}} 
		Работа: (echo new Test())->$prop1 => 1 // срабатывает геттер, если в классе есть свойства
                                                       // называющиеся также, то свойство покажется

	5) __set - вызывается при попытке изменить значение несуществующего или скрытого свойства. 
		   Параметрами принимает имя свойства и значение, которое ему пытаются присвоить.

111) spl_autoload_register() - очень удобно, не нужно прописывать постоянно require классов.
                               однако нужно правильно обращаться к пространству имен, чтобы классы 
                               авт. загружались если лежат в др. папке:
				 1) через namespace
				 2) через use <пространство>
				 3) через слеши в коде new \Admin\Data\Core\Controller

111) Паттерн проектирования Синглтон:
     Нужен для того, чтобы нельзя было создать более одного объекта класса. Например, при создании 
     объекта подключения к БД. Класс будет возвращать или созданный ранее объект или создавать объект 
     и запоминать его для возврата при новых обращениях.
     Пример:
	class DB {
    		   private static $instance; // здесь храним состояние
    		   private function __construct(){} // запуск можно реализовать в getInstance()
    
    		   public static function getInstance(){
        	   if(self::$instance === null) { // если еще не было создано объектов
            		self::$instance = new self();} // создаем объект
        		return self::$instance;}} // возвращаем или давно созданное состояние 
						  // или только что созданное первое

	 И в $a и в $b запишется один и тот же объект.
         $a = A::getInstance(); 
	 $b = A::getInstance();  





























                    
			       
======================================================= PHPStorm  =============================================================

111) Живой показ прямо в IDE - рядом со значком браузере Built in Preview

111) Отключить индексирование папки: правая кнопка -> Mark Directory as -> Excluded

111) Советует Excluded папку public в Ларавель

111) Скрытие панелей: View -> Apperance

111) Редактирование шаблонов: Настройки -> Editor -> File and Code Templates (Верстка, классы, трейты и др.)

111) Установка и просмотр горячих клавиш: Settings -> Keymap -> В поиске пишем что ищем или нажимаем в лупе
	Установил:
	   1) Проект Ctrl + 1
	   2) Для терминала Ctrl + 2
	   3) Для папок Ctrl + E - показать недавние файлы
	   4) Коммит и пуш Ctrl + K
	   5) Главное меню сверху Ctrl + 1
	   6) Настройки Ctrl + Alt + S

111) В Live Template можно задавать не только короткие куски кода, но и большие

111) Файлы с токенами, паролями делаем отдельно как config.php и теперь проекты можно выгружать в гитхаб,
 а config.php добавляем в gitignore

111) Если не получается запустить файл php, проверить, что для нужной директории назначен php интерпретатор в Настройки->PhP
     Это происходило, когда открывал папку в новом окне

111) Для соединения с хостингом используют ФТП и SSH (зашифрованное соединение)
     Для подключения консоли PhPStorm к проекту на хостинге:
	Tools->Start SSH Session -> вводим хост, логин, пароль фтп и консоль работает с хостингом

111) Если команда в консоли при обращении к серверу подвисла, то Ctrl + С

111) Поиск в папке:
	Нажимаем на папку 
	Ctrl + Shift + F - поиск текста внутри файлов
 	Ctrl + Shift + R - поиск и замена строки

111) Поиск файла в проекте:
	Ctrl+Shift+N

111) Shift * 2 - поиск файла

111) PhPStorm позволяет исправлять ошибку в слове через Alt + Enter

111) Устанавливаем плагин Ларавель, активируем в PHP->Laravel-> две галочки
     Теперь можно переходить во view из кода + что-то другое

111) Alt + Ins - добавление щаблонов: PhPDoc, конструктор, геттер, сеттер и др.

111) Быстрое создание конструктора для свойств класса:
	Пишем нужные свойства, напр.: protected $name и т.д.
	Alt + Ins => конструктор => выбираем нужные свойства
	Получаем конструктор:
		 public function __construct($name){
        		$this->name   = $name;}

111) Очистка кеша Laravel:
	php artisan cache:clear - очищает кеш фасада Cache::put(...) (из storage/framework/cache или Redis/Memcached)
	php artisan config:clear - очищает кеш конфига, провайдеров, фасадов из bootstrap/cache/ (config.php, packages.php, services.php)
	php artisan view:clear - очищает вьюхи в storage/framework/views
	php artisan route:clear - очищает роуты в bootstrap/cache/
	php artisan event:clear - очищает события bootstrap/cache/events.php
	php artisan clear-compiled - удаляет файл bootstrap/cache/compiled.php
     Или все за 1 раз: php artisan optimize:clear

111) Закешировать все ПОСЛЕ развертывания приложения на сервере:
	php artisan optimize - кеширует конфиг, роуты, провайдеры и др. в bootstrap/cache/
			       насколько понял включает две команды: php artisan route:cache и php artisan config:cache
	php artisan event:cache - кеширует события в bootstrap/cache/events.php
	php artisan view:cache - кеширует вьюхи в storage/framework/views (или сами закешируются по время работы сайта)
        composer dumpautoload -o - пересборка/кеширование классов автозагрузчика композер в vendor/composer

111) composer dumpautoload -o - композитор сканирует все приложение и создает массив связей классов и файлов 1-к-1,
     например: App\Controllers\AuthController =>  app/Controllers/AuthController.php
     Без этой команды композер сначала проверяет пути в карте классов в composer.json.
     Не найдя там ищет файл дальше в соответствиии с PSR-4: App\Controllers\AuthController =>  app/Controllers/AuthController.php
     Команда  composer dumpautoload -o помогает избежать процесса поиска и составить связи заранее.

111) Создание классов, трейтов, интерфейсов:
	New -> PHP Class -> здесь можно менять Class, Trait, Interface

111) Чтобы Шторм видел переменные, объекты и их методы и пр. передающиеся из других файлов:
	<?php
	/** @var $this \wfm\View */
	/** @var $category array */
	/** @var $total int */
	/** @var $pagination object */
	/** @var $breadcrumbs string */
	?>

111) Над списком папок есть кнопки:
	 Collapse all - сворачивает все папки
	 Expand all - разворачивает все папки

111) По умолчанию Шторм запоминает переменные для автозаполнения из файлов не более 2.5мб.
     Увелчиение: Help > Edit Custom Properties  затем вставляем строчку idea.max.intellisense.filesize=6000  // теперь до 6мб
     Перезагружаем Шторм.
     Так, была проблема с чтением большого файла Tailwind

111) Если Шторм не показывает добавление в файл гитигнор, то этого файла скорее всего нет в этой папке, нужен файл gitignore
     Если файл уже под контролем (был ранее добавлен в репозиторий), то .gitignore на нем работать не будет
	Решения:
		- удалить файл -> закомитить -> добавить в .gitignore -> вернуть файл
		- удалить из индекса (git update-index --assume-unchanged your-file) -> добавить в .gitignore

111) Перенос строки в нужное место: выделяем строку, нажимаем на выделение и переносим мышкой

111) Создание файловой БД sqlite в phpstorm
	1) view->tool windows->database-> на панели database нажимаем +
	2) Выбираем Data Source -> sqlite - тут же можно выбрать другую БД и подключиться к ней
	3) В появившемся окне если нужно меняем имя файла с identifier.sqlite
	4) Во вкладке Advanced в поле Working directory оставляем или меняем путь для этого файла с корня на другой
	5) Меняем в .env: 	
		DB_CONNECTION=sqlite
		DB_FOREIGN_KEYS=true
		Нужны только эти 2 строки, остальное можно закомментировать	
	6) Важно! Нужно поменять константу DB_DATABASE, чтобы в ней стоял абсолютный путь к файлу identifier.sqlite
	   Проще это сделать сразу в config/database.php => sqlite:
		'database' => dirname(__DIR__). '/identifier.sqlite'
	6) php artisan migrate - миграции прошли успешно
	   User::query()->create([...]) тоже получилось

111) Файлы и папки стали коричневыми? Закрыть Шторм и удалить папку idea

111) Пропали папки? Только файлы? Сверху выбрать Project Files и возможно удалить папку idea

111) Сворачивание всего кода: CTRL SHIFT —
     Разворачивание всего кода: CTRL SHIFT +

     Сворачивание части кода на котором курсор: CTRL —
     Разворачивание части кода на котором курсор: CTRL +

111) Очистить консоль: Ctrl + L	

111) Phpstorm использует интерпретатор php от openserver, поэтому и запускаем phpstorm через закладки openserver и 
     в шторме появляется вкладка php (если запустить шторм напрямую без openserver ее не будет и php не будет работать)
     Можно запустить шторм через закладки openserver, а потом выключить openserver, php будет работать

111) php -v  - показ версии php
     
     php -m  - показ установленных расширений в php (curl, mysqli, openssl, PDO и пр)

111) Включение расширений php:
 	Находим файл php.ini (в папке php), раскомментируем нужное расширение, перезагружаем openserver  

111) Чтобы PHPStorm не тормозил и не держал в памяти лишние классы и переменные можно открыть через Open только нужную папку
 
     Также, можно исключить то, что не нужно для помещения в память:
	1) Находясь в папке проекта заходим в Settings -> Directories
	2) Выбираем нужные папки и помечаем: 
		Source - помещается в память
		Excluded - исключенные папки, не помещаются в память 
	3) Это же можно делать в проекте правой кнопкой мыши: Mark Directory As -> ...

111) После установки плагина Ларавель (пеерход в папки при нажатии на файл) нужно его активировать:
	Settings -> PHP -> Laravel -> Enable plugin

111) Подключиться к БД из openserver:
	Database -> + -> прописываем User и Database -> Test Connection -> в правой панели значок обновления -> появились таблицы

111) Поиск в шторме по всему проекту + по названиям файлов + по их содержимому:
	Navigate -> Search Everewhere

     Остальное по всему проекту нормально работает со случайным успехом!!! Ни CTRL + F, ни остальная лабуда

111) Убрать выделения в шторме:
	1) Тыкаем на выделение
	2) ALT + Enter - показывается подробное меню
     
111) Запись видео Windows 10:
	Windows + ALT + R

111) Шторм грузил процессор на 100 процентов при каждом вводе символа:
	File -> Power Save Mode  - все стало нормально, но стали хуже подсказки










======================================================= Tailwind  =============================================================

Tailwind во время сборки автоматически удаляет весь неиспользуемый CSS.

На сайте есть установка для Ларавель в Руководства по фреймворкам.

В Tailwind цифры у классов часто значат 4px, например, pt-3 - padding-top: 12px

Чтобы Шторм помогал искать классы подключаем минифицированные стили tailwind из папки Полезное.
Сначала не читал файл 2.7мб, помогло изменение idea.max.intellisense.filesize (сверху)
Офигеть, тогда ищет даже, если вводить tc -> text-center

0) Быстрая верстка для разработки (но не для боевой версии):
	Добавляем в head: <script src="https://cdn.tailwindcss.com"></script>
	Рядом с файлом для шторма помещаем tailwind-v2.min.css

   Также есть такой способ:
	<link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
	
1) Здесь будет описание для чистой папки.
	1) npm init -y  -создаем pj заполненный по умолчанию
	2) npm install -D tailwindcss - устанавливаем
	3) npx tailwindcss init - инициализируем весь проект на основе tailwindcss (создается tailwind.config.js)
	4) в tailwind.config.js указываем какие файлы будут работать с tailwind:
		content: ["./src/**/*.{html,js}"], - все файлы html и js в указанных папках
	5) Создаем основной файл стилей input.css и прописываем в нем:
		@tailwind base;
		@tailwind components;
		@tailwind utilities - нужна для кнопок пагинации, остальное можно закомментировать
	6) npx tailwindcss -i ./input.css -o ./css/style.css --watch  // запускаем в консоли, сканируются файлы, создается исходящий css,
                                                                         который и подключим к сайту, либо прописываем команду в package.json
									 (есть выше далеко)
	7) После вышеописанной команды создается файл style.css с обнуляющими стилями, подключаем его к сайту

2) Установка с Ларавель (он уже там стоит так понимаю в новых Ларавелях, поэтому только настраивать)
	1) Устанавливаем Ларавель: composer create-project laravel/laravel my-site // в этой папке создастся папка my-site с Ларавель
	2) Устанавливаем tailwind: 
		npm install -D tailwindcss postcss autoprefixer
		npx tailwindcss init -p
	3) В tailwind.config.js пишем:
		content: [
      			"./resources/**/*.blade.php",
      			"./resources/**/*.js",
      			"./resources/**/*.vue",],
	4) В resources/css/app.css пишем:
		@tailwind base;
		@tailwind components;
		@tailwind utilities; - это отвечает в том числе за кнопочки пагинации, верхнее можно закомментировать если не нужно
	5) в resources/css/app.js пишем
		import './script'; // это наш файл script.js, лежащий рядом с app.js
	6) Запускаем vite:
		npm run dev  // vite не создает готовый файл, а сразу отправляет скомпилированный код в браузер
	ВАЖНО:  npm run build - режим продакшн, vite создаст ужатые css и js в public и сам пропишет путь к ним в шапке
	Также проверяем, что в .env стоит ASSET_URL=http://barmaley.ru, иначе vite неправильно прописывает путь после npm run build	

	Имеем ввиду, что если запустить сайт без запуска vite (nmp run dev), то будут применены стили от последнего npm run build
	То есть без vite сайт нужно обязательно скомпилировать(npm run build), чтобы применялись последние изменения
	
	7) в head главного шаблона вместо стилей вставляем:
		 @vite(['resources/css/app.css', 'resources/js/app.js'])

	8) Все работает в живом режиме, страницу можно не перезагружать, можно писать в стиле tailwind, только не забывать, 
           что скомпил. стили не существуют, а vite создает их налету и отправляет в браузер, нужно будет перед выкладкой npm run build
	   Также если не делали npm install после установки Ларавель, то сделать, иначе npm run dev не работает
	9) Можно подключить <link rel="stylesheet" href="/build/assets/tailwind-v2.min.css">
	   Чтобы шторм помогал
	
	10) Чтобы vite компилировал наши файлы в один исходный файл:
	   	свои стили css пишем либо:
			а) в файле app.css под командами @tailwind как обычные стили
			б) подключаем в app.css свой файл @import './style.css';  НАД командами @tailwind // соотв. в итоговом файле наши стили будут сверзу, потом стили tailwind
	   	свои скрипты js:
			a) пишем в app.js
			б) размещаем рядом с app.js и подключаем в нем же:  import './script.js'

         Подключение локальных шрифтов @font-face через vite.
         Vite будет ебать мозг. Поэтому подключаем шрифты отдельно от Vite:
		1) Помещаем шрифты в папку fonts, туда же помещаем fonts.css с командами @font-face
		2) Подключаем шрифты в шапку отдельно от vite <link rel="stylesheet" href="{{ asset('storage/fonts/fonts.css') }}">
		3) Изменять шрифты можно в style.css, который работает с vite, будет работать
		4) Очищаем кеш Artisan::call('optimize:clear');

	11) https://www.youtube.com/watch?v=Rhd2dA8w4Gg - показано как подключить вслед vue

Получается с Vite работать тоже просто:
	1) npm run dev или npm run build
	2) Вставляем в head @vite(['resources/css/app.css', 'resources/js/app.js'])
	3) Свои стили и скрипты либо пишем рядом с app.css, app.js и импортируем, либо прям в них
	4) Картинки же и др. храним сразу в public/images/  и подключаем по пути туда или через asset()  ?? иначе не знаю

Создание набора стилей для повторяющихся элементов, например, для кнопок:
	1) Создаем класс со стилями в главном файле css (там где @tailwind base ...)
		@layer components {
    			.btn {@apply py-2 px-4 bg-blue-500 text-white font-semibold rounded-lg shadow-md active:bg-blue-700;}}
	2) Используем класс .btn как в обычном css

Произвольные стили:
	<p class="[font-size:30px]">Lorem ipsum dolor sit amet.</p>
	<p class="text-[20px] text-[#bada55]">Lorem ipsum dolor sit amet.</p>

Каталог цветов: https://tailwindcss.ru/docs/customizing-colors/          // bg-blue-500
Каталог расстояний: https://tailwindcss.ru/docs/customizing-spacing/     // p-6

Шпаргалка: https://nerdcave.com/tailwind-cheat-sheet

======================================================= Гит  =============================================================

git init - создание локального репозитория

git add index.html - добавляем один файл/папку в отслеживание (staging area)

.gitignore - файл запрещающий отслеживание файлов/папок (его НУЖНО заносить в репозиторий ), создаем до git add .

Написание правил в .gitignore, будут исключены из отслеживания:
  /.httaccess - только файл в корне
  .httaccess - все файлы httaccess на всех уровнях
  logs - все папки logs и файлы с именем logs (без расширения) на всех уровнях и их содержимое
  logs/ - все папки logs на всех уровнях и их содержимое
  core/.httaccess - только файл в папке core
  *.log - все файлы с расширением .log
  !folder - отмена игнорирования
  !important.log - отмена игнорирования
  logs/**/debug.log - на месте двух звездочек множество каталогов или ни один
  debug[a-z].log - в квадратных скобках работает наподобие регулярных выражений

git add . - добавляем все файлы в отслеживание (staging area)


git rm -r -f --cached .idea - если случайно добавлили в отслеживание папку, то так удалять оттуда. Но прописать ее в .gitignore

git restore --staged .idea  - удалить файл/папку из отслеживания и сбросить до последнего коммита без удаления из репозитория
git restore file.txt - сбросить изменения до последнего коммита

git status - проверить состояние файлов 

Состояния файлов:
modified - изменен
staged - индексирован
committed - зафиксирован

Changes not staged for commit - файлы отслеживаются, но текущее состояние не проиндексировано через add
Changes to be committed - состояние проиндексировано, но не сделан слепок через commit
nothing to commit, working tree clean - изменения зафиксированы через commit и помещены в репозиторий, изменений нет

Важно! Гит не отслеживает изменение файлов, для установки слепка нужно делать две команды:
1) git add file_name (git add -A) - внести изменения в индекс (staging area)
2) git commit -m "комментарий" - отправить зафиксированные изменения из индекса (из staging area) в репозиторий

Или второй вариант одной строчкой:
git commit -am "Комментарий" - выполняется add + commit

git add . - это нужно выполнять перед каждым ручным git commit -m "комментарий"
git commit -m "комментарий" - создаем слепок файла и отправляем его в репозиторий

Насколько понял, шторм сам делает git add перед commit

Удаление:
git rm -f index2.html - удалить файл из индекса и из директории
git rm -f -r папка - удаляет папку с файлами из индекса и из директории
git commit -m "удалил файл" - нужно делать слепки после удаления

git restore --staged .idea  - удалить файл/папку только из отслеживания (т.е. отмена команды add)

branch (ветки) - ссылки на определённый коммит
merge (cлияние веток) - объединение изменений из одного коммита с другим коммитом + все предыдущие коммиты
rebase - копирование коммитов и перенос в другое место. Получается чистая линейная история коммитов
HEAD - символическое имя последнего коммита в текущем состоянии

Создание веток:
1) Тремя командами
	git branch develop - создание ветки develop
	git branch - показать все ветки
	git checkout develop - переключиться на ветку develop
2) Одной командой:
	git checkout -b develop - создать ветку и переключиться на нее

При переходе на другую ветку (checkout) всегда делаем commit или stash, иначе изменения удалятся.

git stash - поместить изменения в память (заархивировать изменения), если не хочется делать commit при переходе на другую ветку
git stash pop - вернуть изменения (вставятся в текущую ветку!)

git checkout -f HEAD - перейти в состояние последнего commit

git log --oneline - показать слепки

Слияние веток
1) git checkout master - переходим в главную ветку
2) git merge feature - вливаем в нее изменения из ветки feature
3) git branch -d feature - удаляем вторую ветку

git fetch - обновить ссылки на ветки (например, после удаления ветки)

Если строки изменялись в обеих ветках будет конфликт.
git checkout --ours index.html - посмотреть вариант файла текущей ветки
git checkout --theirs index.html - посмотреть вариант файла второй ветки
git checkout --merge index.html - посмотреть слияние веток и конфликт строчек

Если при git merge feature были конфликты:
1) Редактируем файл как нужно
2) Делаем коммит git commit -am "Комментарий"
3) Удаляем ненужную ветку git branch -d feature

Отмена изменений
git reset --hard - удаление незакомиченных изменений и переход к последнему сохраненному комиту
		   (аккуратнее, если сделать на файлах компа, то сотрутся незакомиченные изменения. Папка => Local history => Revert)
                
git reset --soft - переход к последнему коммиту, но несохраненные изменения не удаляются

Привязка внешнего репозитория:
git remote add origin https://github.com/user/project.git - означает, что назвали внешний репозиторий как origin.
                                                            теперь на компе можно вместо длинной внешней ссылки  писать origin

git remote show origin - показать информацию о том как связаны локальные и удаленные ветки
git remote - показать имя внешнего репозитория (т.е. origin)
git remote rm origin - удалить связь между лок. и внеш. репозиторием

Привязка к гитхабу
1) По ssh (нужно генерировать ключ и вставлять публичный в репозиторий)
	git remote add origin git@github.com:rodnovspb/example.git  // внешний репозиторий назвали как origin
	git push -u origin master -  отправили на внешний репозиторий (ссылка origin) в ветку master

2) По https:
	git remote add origin https://github.com/rodnovspb/example.git
	git push -u origin master - отправили данные на гитхаб с текущей ветки master на удаленную ветку master
        git push - то же, сокращенно

3) Удаление привязки:
	git remote rm origin

Клонирование репозитрия на компьютер:
	git clone https://github.com/rodnovspb/example.git
        git clone https://github.com/rodnovspb/example.git my-project - с изменнением имени папки

Отправка на гитхаб:
	git push origin master - отправляем на удаленную ветку master, 
                                 при этом имя текущей локальной ветки совпадает с именем удаленной ветки (master => master)
	git push - то же, сокращенно
        git push origin main:master - если отправляем с текущей локальной ветки main на удаленную master
	
Получение с гитхаба:
        git pull origin master - получаем изменения с гитхаба (по ссылке origin) с ветки master
	git pull - то же сокращенно

======================================================= Гитхаб  =============================================================

1) Качаем гит с https://git-scm.com/
2) В папке с проектом правой кнопкой: Git Bash Here
3) Вводим git init , появилась папка .git
4) Устанавливаем настройки:
	 git config --global user.name "rodnovspb"
	 git config --global user.email "rodnovspb@mail.ru"
	 git config --list - вывод настроек
 git help init - здесь можно посмотреть настройки, флаги команд

5) git status - покажет файлы отслеживаемые и неотслеживаемые (красным цветом) файлы проекта 
6) Вводим git add . - добавит все файлы проекта в отслеживание
7) git status - увидим, что файлы теперь зеленые
8) git commit -a -m "first comment"  //делаем первый коммит, -a означает все файлы
9) git status - покажет, что On branch master и nothing to commit
10) Меняем какой-нибудь файл, вводим git status, гит покажет измененный файл красным цветом
11) git add . - вводим
12) git status - теперь покажет измененный файл, но уже зеленым
13) git commit -m "second comment" - вводим комментарий
14) git status - теперь файлов для коммита уже нет
15) Принцип работы: 
	0) git add index.html - добавить один файл/папку в отслеживание
	1) git add . - добавляем все файлы в индекс гита (staging area)
	2) git commit -m "second comment" - делам слепок в локальный репозиторий + добавляем комментарий
	3) git push - отправляем слепок во внешний репозиторий
16) На гитхабе создаем новый репозиторий (папку этого проекта, например git), появятся команды для размещения:
	git remote add origin https://github.com/rodnovspb/git.git // назвали внешний репозиторий как origin
	git branch -M main
	git push -u origin master // отправляем слепки во внешний origin репозиторий в ветку master (лок. master => удал. master)
    Вводим поочередно их в консоль гита, видим, что файлы скопировались на гитхаб
17) По ссылке commits видим наши комментарии и соответствующие им изменения файлов

18)  Как второй разработчик получает изменения - то есть клонирует проект и отправляет обратно в гитхаб:
	1) Запускает в своей папке Git Bash
	2) На Гитхабе в папке проекта есть кнопка Code -> Clone, 
           копируем адрес проекта (https://github.com/rodnovspb/git.git)
	3) git clone https://github.com/rodnovspb/git.git - проект копируется в нашу папку в папку git. Так понимаю, чтобы 
	   без вложенностей двух папок нужно консоль запускать прямо с рабочего стола
	4) Допустим второй разработчик создает новую страницу или меняем файлы проекта
	5) Перезапускаем консоль из папки проекта (до этого были во внешней папке)
	6) git status - видим измененный файл
	7) git add . - добавляем в индекс
	8) git commit -m "комментарий второго разработчика"
	9) git push - отправляем на гитхаб

19) Как теперь первый разработчик получает изменения:
	а) Первый вариант:
	   1) git fetch origin - изменения грузятся в папку .git (локальный репозиторий), но мы еще не видим новый файл
	   2) git merge origin/master - теперь файл изменился в рабочем пространстве 

        б) Второй вариант:
              git pull origin master - полная версия
              git pull - сокращенная версия

        в) Третий вариант в Шторме:
              Git => Pull

    Создание веток
	1) git status - перед созданием ветки проверяем, что ничего комиттить не надо, изменений нет. Иначе создаем коммит
			перед ветвлением
	2) git branch branch2  - создаем ветку branch2 (главная ветка называется main) см. 6 пункт, там короче
	3) git branch -v - покажет все ветки проекта, зеленым выделена текущая ветка main
	4) git checkout branch2 - переходим в ветку branch2
	5) git branch -v - проверяем что выбрана ветка branch2 (зеленым цветом)
	6) Внимание, вместо второго этапа можно было ввести git checkout -b branch2 - одновременно создать и перейти 
	   в новую ветку
	7) Меняем что нужно в своей ветке в файлах, пушим

    Слияние веток	
    	Когда наступает этап объединения веток в одну скорее всего получится, что будут конфликты в одних и тех же строчках.
    	Как это разрешить. 
	1) Качаем программу с https://kdiff3.sourceforge.net => вкладка download => качаем и устанавливаем
    	2) Переходим в главную ветку git checkout main
	3) Проверяем, что изменения были закоммичены git status
	4) git config --global merge.tool kdiff3 - указываем утилиту применяемую для разрешения конфликтов веток
	5) git merge branch2 - в ветку в которой находимся main вливаем branch2
	6) git config --global mergetool.kdiff3.md '"C:\\Program Files\\KDiff3\\kdiff3" $BASE $LOCAL $REMOTE -o $MERGED'
	7) git mergetool - сливаем
	8) В программе kdiff3 видим конфликтные строки, устраняем конфликты и нажимаем на дискету (сохранить)
	9) Короче у меня не работало это, поэтому хз

     git log - показ логов (коммиты, авторы, время). Чтобы выйти нажать q
     git log --pretty=format:"%h - %an, %ar : %s" - более удобный показ логов
     git log -p -2 - показ изменений последних двух коммитов

 Создание проекта и выгрузка в гитхаб через phpstorm
 	1) Создаем папку без шторма, открываем как project в phpstorm, создаем файлы
	2) Если нет вкадки Git, то VCS -> Enable Version Control Integration
	3) Появилась вкладка Git
	4) Если нет файла .gitignore, то создаем
	5) Выбираем файлы в gitignore: git->add to .gitignore -> .gitignore
	   Добавил сам файл .gitignore. Важно перед выгрузкой правильно добавить файлы в .gitignore, иначе потом проблемы будут,
           так как уже выложенные на гитхаб файлы не исключишь, так как gitignore только командует не отслеживать файлы
	6) Теперь можно выбрать весь проект и Git->add - файлы стали зелеными, добавились в индекс   
	7) Git->Githab->Share Project on Githab, придумываю имя репозитория или оставляю как есть
	8) Ctrl + K, вводим имя любое и почту rodnovspb@mail.ru
	9) Ура, исключенные в .gitignore файлы не выгрузились на гитхаб, хоть и были зелеными
	10) Удалить репозиторий с гитхаба: зайти в проект на гитхабе -> settings -> в самом низу Delete this repository



======================================================= Гитхаб с phpstorm  ===================================================
0) Короткий порядок:
	0) Создаем папку проекта и открываем ее как новый проект в шторме
	1) VCS => Enable Version Control => Git
	2) Создаем .gitignore (вносим .idea)
	3) Выделяем все, git => add
	4) Git => GitHub => Share Project on GitHub + галочка private 


0) Структура:
	1) Рабочее пространство
	2) Staging area (отслеживаемые index файлы до создания слепка через commit)
	2) Локальный репозиторий (хранит слепки рабочего пространства после commit)
	3) Удаленный репозиторий (хранит полученные слепки с локального репозитория)

0) Смысл:
	1) На компе создается локальный репозиторий для отслеживания изменений (командой git init или Enable Version Control)
	2) В индекс этого репозитория добавляются файлы (git => add)
	2) На гитхабе также создается репозиторий (Share Project on GitHub)
	3) Время от времени в репозитории компа делаются слепки файлов (add + commit)
	4) Через push слепки отправляются на удаленный репозиторий
	5) через clone скачивается весь удаленный репозиторий на комп
	6) через pull запрашиваются изменения файлов и новые файлы с удаленного репозитория
           с удаленого репозитория на комп (первое что делается в начале рабочего дня)

0) Цвета:
   - зеленый цвет - файлы добавлены в локальный репозиторий и отслеживаются
   - темно-зеленый цвет - файлы игнориуются через gitignore
   - красный цвет - файл не отслеживается, так как еще не добавлен в отслеживание (нужно сделать git -> add)
   - белый цвет - с состояния сделан слепок (commit), с тех пор файл не изменяли
   - голубой цвет - файлы изменены, требуется commit

0) должна быть скачана программа гита
1) В проекте заходим в настройки шторма => Version Control => Git
2) Path to Git: C:\Program Files\Git\cmd\git.exe
   Set this path only current project
3) Version Control => GitHub => Настраиваем вход в аккаунт гитхаба
4) В терминале в корне:
	git init // то же в шторме VCS => Enable Version Control => Git
   Команда одновременно создает локальную ветку master (т.е. git branch -M master)
   В проекте появилась папка .git, отслеживающая изменения в проекте
   Файлы окрасятся в красный цвет - гит видит файлы, но они не добавлены в индексацию
   Если файлы не окрасятся, перезагрузить шторм

5) Проверяем файлы .gitignore
   Для ево в core/custom/.gitignore делаем:
	.env
	vendor

файл .gitignore - не запрещает добавление файлов в репозиторий, а только игнорирует их изменения
   Поэтому файлы из gitignore могут быть по ошибке выложены на гитхаб через команду git add файл или git add *
   Однако, если добавить в индекс через Git => add, то шторм правильно учитывает этот файл при добавлении в индекс файлов 

   !!!! Способ убрать файл из индекса и из удаленного репозитория, если случайно заиндексировал и отправил на гитхаб:
	git rm --cached remove_file
        вносим файл в gitignore
        git commit -m "Excluding"
	git push

6) Далее нужно добавить файлы в отслеживание, выделяем все файлы и  Git => add.
   
6) Далее опишу команды работы с гитом для понимания:
	git add файл/папка  - добавление файла/папки в индекс для отслеживания
	git commit -m "init project" - создание слепка файлов (версию) + заметка, файлы стали белыми
	git status - отображение списка измененных, добавленных или удаленных файлов 
        git reset - сбросить весь индекс в локальном репозитории

7) Выделяем все файлы CTRL + A => правая кнопка => Git => add // шторм проигнорирует файлы из .gitignore
   
8) Далее 2 варианта:
	1) Ручной:
	   - создаем вручную репозиторий на гитхабе
	   - присоединяем наш проект к этому репозиторию:
	         git remote add origin git@github.com:rodnovspb/название_репозитория.git
	   - загружаем проект на гитхаб:
		git push -u origin master
	2) Либо шторм сам создаст репозиторий и загрузит проект туда:
		Git => GitHub => Share Project on GitHub + галочка private

7) Далее как происходит разработка:
	1) master - основная ветка, откуда проект грузится на хостинг
	2) develop - ветка для разработки, собирающая изменения от разработчиков, на ней сайт тестируется, 
                     если все хорошо изменения отправляются на master
	3) прочие ветки - создают разработчики для себя от ветки master и отправляют изменения в develop

7) Нужно создать вторую ветку - develop, где будем разрабатывать проект
	- заходим в гитхаб на репозиторий (это же можно сделать в шторме)
	- master => пишем develop => Create branch => в гитхабе создалась ветка
8) Теперь у нас есть две ветки (master и develop) на гитхабе, но на компьютере мы про них не знаем:
	git branch -r // покажет только одну удаленную (remote) ветку
9) Поэтому вытягиваем информацию об удаленных ветках:
	git fetch  // или Git => Fetch
10) Проверяем:
	git branch -r // теперь знаем, что есть 2 удаленные ветки master и develop
11) Но на компе только одна:
	git branch // покажет одну ветку
12) Для того, чтобы после fetch новая ветка появилась на компе нужно один раз на нее переключиться:
	Git => Branches => Remote => Выбираем несуществующую на компе ветку => Checkout
	git branch // теперь и на компе 2 ветки (master и develop)
13) Теперь на компе две ветки и на гитхабе две ветки, теперь можно переключаться между ветками через:
	Git => Branches => выбираем ветку => Checkout
	git checkout develop // либо в терминале
14) Теперь при переключении также видим разные ветки в шторме и изменения отправляются на соответствующую ветку.
    Например, переключились на ветку develop, в шторме показываются файлы из develop и изменения будут отправляться
    в ветку develop
   
15) Как скачивать проекты другому сотруднику:
	1) Заходим в репозиторий, нажимаем на зеленую Code, копируем адрес https
	2) На рабочем столе: git clone https://github.com/rodnovspb/pechati.online.git
	3) из файла .env.example создается файл .env, туда пишутся полученные от начальника настройки
	3) composer install - скачаются php пакеты, установятся в vendor (в evo нужно заходить в core)
	4) npm install - скачаются js пакеты, установятся в node_modules

16) Создание и решение задач сотрудниками, пишем в гитхабе:
	1) Ставим задачу создать контроллер: issue => create controller
	2) создаем ветку от master для решения этой задачи: 1_create_controller
	3) на компе сотрудника: 
	     - git fetch - получили ветку задачи 1_create_controller в локальный репозиторий
	     - Git => Branches => 1_create_controller => Checkout // переключились на нее в 
	4) После выполнения работы по созданию контроллера:
	    - git add - добавляем его в индекс
            - делаем коммит (слепок) и пушим в ветку 1_create_controller удаленного репозитория
	5) далее нужно отправить изменения из ветки 1_create_controller в develop:
	    - заходим на гитхабе на ветку 1_create_controller
	    - Pull requests => Create pull request
	    - выбираю ветку с решенной задачей 1_create_controller
	    - в левом верхнем углу выбираю куда отправим изменения: base: develop
	    - проверяем, что можно вставлять
            - Create pull request
	6) Во вкладке Pull requests появился наш запрос, принимаем:
	    - Merge pull request
	    - Confirm merge
	7) Теперь в ветке develop есть наши изменения, тестируем работу сайта
        8) Теперь нужно из develop выгрузить на ветку master:
	   - Pull requests => New pull request 
	   - в левом верхнем углу выбираю куда отправим изменения: base: master <- compare: develop
	   - Create pull request => Merge pull request => Confirm merge

111) Разрешение конфликтов в шторме:
	1) Заходим в ветку
	2) Git => Merge => выбираем присоединяемую ветку
	3) Если есть конфликты появятся действия, выбираем Merge
	4) Изменяем в окне посередине как надо.
           Сверху есть волшебная палочка для решения легких конфликтов
	5) Git push

111) Работа через гитхаб с хостингом по SSH + гитхаб в deploy.yml описана в evo

111) Работа когда при push на гитхаб он будет сам отправлять файлы на хостинг по фтп (без других команд)		
	1) Заходим в репозитории в Settings=>Secrets and variables => Actions,
	создаем три секрета через NEW repositary:
	FTP_USERNAME - логин фтп, что создали в public_html
	FTP_PASSWORD - пароль фтп
	FTP_SERVER - cinedr.beget.tech
	2) в папке проекта создаем папку и файл .github/workflows/deploy.yml
        код есть в Полезное/автоматическая выгрузка с гитхаба, проверяем название ветки
	3) Теперь при push в указанную ветку на гитхаб он будет автоматически отправлять файлы на хостинг    

======================================================= CKEditor  =============================================================
Стоимость ckfinder для одного сайта около 5000 руб.
Ограничения бесплатной версии:
	1) Нельзя удалять файлы
	2) Нельзя загружать больше 10 файлов в одну папку
	
 1 способ - без картинок:
 Простой редактор с малым набором методов и без загрузки картинок
 Для него достаточно подключить в шапке ckeditor.js (есть также ссылки cdn), дать id="ckeditor" для textarea и прописать в подвале:
	<script>ClassicEditor.create( document.querySelector( '#ckeditor' ) ).catch( error => {console.error( error );} );</script>
 Все будет работать. Но мало функций, самое простое.
 Эта строчка только для этого способа, для 2 и 3 способа ее писать не нужно.

2 способ - много функций + картинки:
Делаем все как ниже, но грузим только ckeditor + подключаем только его в шапку

3 способ вместе с файловым менеджером:
0) Нужно чтобы работал php не меньше 8.1, также нужно заранее прилинковать storage в public
1) Папка с исходниками есть в полезное
2) Грузим в public папку ckeditor (из полезного) с папками ckeditor и ckfinder
   В папке ckeditor (2013 год) лежит еще папка с цветным ckeditor (2012 год), лучше не удалять, иначе может ошибка быть.
3) В head админской части вставляем:
	<script src="{{ env('APP_URL') . '/ckeditor/ckeditor/ckeditor.js' }}"></script>
  	<script src="{{ env('APP_URL') . '/ckeditor/ckfinder/ckfinder.js' }}"></script>
4) Нужному textarea прописываем id="ckeditor"
5) Если хотим, чтобы строка автоматически не оборачивалась в <p>, то под textarea вставляем:  // уже настроил в config.js, скрипт вставлять не надо
	 <script>CKEDITOR.config.autoParagraph = false;</script>
6) Изменить высоту редактора:
	<script>CKEDITOR.config.height = '100';</script> // это ставим после textarea, либо меняем в config.js
7) В подвал верстки или в файл подключаемого скрипта js вставляем: 
		 <script>
          		window.addEventListener('load', ()=>{
              		if(document.body.querySelector('#ckeditor')){
                  		CKEDITOR.replace('ckeditor', {
                      		filebrowserBrowseUrl:  "/ckeditor/ckfinder/ckfinder.html",
                      		filebrowserUploadUrl:  '/ckeditor/ckfinder/core/connector/php/connector.php?command=QuickUpload&type=Files'})}})
      		</script>
8) Все остальные настройки уже установлены в ckfinder/config.php
   CKFinder сам создаст в storage нужные папки (images, files) или воспользуется уже существуещей images
   Если на хостинге возникнут проблемы, то проверить, что для папок установлены права 0777
   Также в документации написано, что в ckfinder/config.php => $config['authentication'] нужно проверять, что пользователь аутентифицирован,
   но никак не могу проверить, может костылем: скрипт с filebrowserBrowseUrl вставлять в верстку, но обернуть в @auth
9) Редактирование шрифта редактора: 
	1) В файле contents.css в классе .cke_editable // уже заменил в папке редактора, ничего менять не надо
	2) Либо скриптом после textarea:
		<script>CKEDITOR.addCss(".cke_editable{font-size: 24px;}");</script> // уже не надо
10) Редактирование шрифта кода в редакторе:
	ckeditor => skins => moono => editor.css => .cke_source  // в папке редактора заменил, все готово
11) Если пользователь может редактировать текст через ckeditor, то такой текст выводим только через {!! ... !!}
    Для очистки лишних тегов и их редактирования можно использовать пакет mews/purifier, есть внизу
12) Встраивание ckfinder ссылкой:
	1) В шапке должен быть загружен сам скрипт: 
		<script src="http://pechati.loc/ckeditor/ckfinder/ckfinder.js"></script>
	2) На странице помещаем:
		<div id="ckfinder"></div>
    		<script>CKFinder.widget('ckfinder', {height: 600});</script>
	3) Теперь при открытии этой страницы появится файловый менеджер

13) Добавление кнопки "Выбрать файл на сервере" 
   (там же можно загрузить, но не более 10 файлов в папку, придется создавать много папок или крякать):
	1) Подключаем ckfinder на страницу, если еще не подключен:
		<script src="/ckfinder/ckfinder.js"></script>
	2) Вставляем поле, где будет показываться путь к выбранному файлу:
		<input type="text" name="url" id="url" />
	3) Если грузили картинку и нужно изображение загруженного файла:
		<img id="img" src="" alt="">
	4) Вставляем кнопку вызова файлового менеджера:
		<button onclick="openPopup()">Select file</button>
	5) Вставляем скрипт:
		<script>
      		    function openPopup() {
        		CKFinder.popup( {
          		  chooseFiles: true,
          		  onInit: function( finder ) {
            		     finder.on( 'files:choose', function( evt ) {
              			let file = evt.data.files.first();
              			document.getElementById( 'img' ).src = file.getUrl(); // показываем изображение
              			document.getElementById( 'url' ).value = file.getUrl();} ); // показываем путь к файлу
            		     finder.on( 'file:choose:resizedImage', function( evt ) { // срабатывает если меняли размеры
              			document.getElementById( 'url' ).src = evt.data.resizedUrl; // показываем изображение
              			document.getElementById( 'url' ).value = file.getUrl();} );}} );} // показываем путь к файлу
  		</script>
	6) Внимание, так как Ckfinder сам грузит файлы на сервер, также в форме с ckfinder мы не используем 
	   инпут type="file", то мы не сможем валидировать через Ларавель такие изображения по типу: image|max:5120
	   Потому что там нет этого поля type="file", валидировать сможем только поле с путем к файлу.	
	7) Транслитерировать русские имена файлов не нужно, CKfinder делает сам, я настроил
		https://kisameev.ru/php-lesson/ckfinder-3---delaem-avtomaticheskiy-translit-zagruzhaemyh-faylov
	8) CKFinder автоматически сжимает картинки при загрузке на сервер, не нужно думать над размерами
	9) Для CKFinder в Опенсервере должен стоять PHP не меньше 8.1 версии, поэтому для корректной работы вместе с 
	   MySQL нужно выбирать ее в Опенсервере тоже для работы с php 8.1

	Вот верстка:
	<div>
           <img id="imgol" src="{{ old('src') ?? asset('images/no-photo.jpg') }}">
        </div>
        <button onclick="openPopup()" type="button">Выберите файл</button>
        <input type="text" name="src" id="url" readonly value="{{ old('src') ?? null }}" />
        <span>@error('src') {{ $message }} @enderror</span> // В Ларавель можем проверять верхний input только на required


111) mews/purifier - пакет для Ларавель очищающий лишние теги и атрибуты, а также редактирующий их. Пакет расставит параграфы, удалит вредоносный код 
                     (более известный как XSS), обеспечит соответствие стандартам W3C и пр.
                     Функция strip_tags тоже удаляет теги HTML и PHP из строки, но не так настраиваемая как этот пакет
     composer require mews/purifier
     php artisan vendor:publish --provider="Mews\Purifier\PurifierServiceProvider" - опубликовать настройки
     В настройках разрешить вставку style="", так как ckeditor вставляет их + настроить под все что может вставить ckeditor

     Примеры:
	1) <p>Всё сущее - <invalidTag>увековечить</invalidTag></p> => <p>увековечить</p>
        2) <script type="text/javascript">alert("hacked by Alexander Blok");</script>   =>  ""
        3) <a href="https://ya.ru">Ссылка</a> => <a href="https://site.ru/redirect?url=https://ya.ru">Ссылка</a>
	4) <a href="https://ya.ru">Ссылка</a> => <a href="https://ya.ru" target=_blank>Ссылка</a>
   
     Использование: {!! clean($content) !!}


======================================================= Парсинг  =============================================================
https://code.mu/ru/php/book/parsing

0) Файл есть в парсинг/сбор email с сайтов

0) Запускать файл php удобно в шторме через Run, там же можно выводить текущую операцию и останавливать парсинг

1) ini_set('max_execution_time', '10000') - установка максимального времени работы скрипта в секундах
   set_time_limit(0) - снимаем ограничения на работу скрипта. Рекомендовано использовать обе команды.

   ini_set('memory_limit', '2048M') - устанавливаем ограничения на оперативную память в МБ 

   ignore_user_abort(true) - если скрипт запускается через браузер, то он оборвет соединение со скриптом через 1-2 минуты 
		             (страница не отвечает). Скрипт в таком случае закончит работу. Чтобы он продолжал работать вводим 
			     эту команду.

2) Изменить ограничения на время работы скрипта и память можно на локальном компьютере и VPS хостинге. Обычный хостинг не позволит.

3) preg_match_all('#src\s*=\s*(["\'])(.+?)\1#su', $str, $matches, PREG_PATTERN_ORDER) - найти все теги src
   $str = preg_replace('#<!--.+?-->#su', '', $str) - очистить строку от комментариев
   strip_tags('lorem <b>ipsum</b> dolor sit amet') - очистить строку от внутренних тегов (здесь от <b>)		
   trim(' str  ') - удалить пробелы с двух сторон
   preg_replace("#\t+#", ' ', $elem) - заменить табы на пробелы

4) Библиотека DiDOM:
	https://github.com/Imangazaliev/DiDOM/blob/master/README-RU.md

	DiDOM НЕ ПРЕДНАЗНАЧЕН для загрузки документов из интернета, эта возможность добавлена для простейших случаев.
	Например, если страница не существует, то Дидом вылетит с ошибкой.
	Если требуется что-то сложнее (обработка ошибок, авторизация), лучше воспользоваться cURL или оберткой для него,
	типа Guzzle, после чего передавать загруженный HTML в DiDOM

	Рабочая функциия с cURL:
	function getPage($path) { //грузим сюда путь
    	  $curl = curl_init($path);
    	  curl_setopt($curl, CURLOPT_RETURNTRANSFER, true);
    	  curl_setopt($curl, CURLOPT_COOKIEFILE, 'cookie.txt');
	  curl_setopt($curl, CURLOPT_COOKIEJAR,  'cookie.txt');
    	  curl_setopt($curl, CURLOPT_FOLLOWLOCATION, 1);
    	  curl_setopt($curl, CURLOPT_HEADER, false);
	  curl_setopt($curl, CURLOPT_TIMEOUT, 5); - остановить курл, если сайт завис, очень полезно для парсинга
    	  $html = curl_exec($curl);
    	  curl_close($curl);
    	  return $html;}

	composer require imangazaliev/didom
	
	require_once './vendor/autoload.php';
	use DiDom\Document;

	$document = new Document('http://test.loc/', true) - это будет работать, но лучше грузить документ вот так:
	$document = new Document(строка html из Газзл);
	
	Работа c Дидом через Газзл.
	Теперь все не будет сваливаться, если страница, например, не существует:
	  use DiDom\Document;
	  use GuzzleHttp\Client;
	  use GuzzleHttp\Exception\BadResponseException;
	
	  $client = new Client();
	  try { 
		$response = $client->request('GET', 'адрес_сайта')->getBody()->getContents(); - Газзл получает верстку
		$document = new Document($response); - грузим верстку в Дидом
		   if($document->has('title')){
			... работаем с сайтом через Дидом ...	}   
	  } catch(GuzzleHttp\Exception\ClientException $e){
		$response = $e->getResponse();
        	$responseBodyAsString = $response->getBody()->getContents();}



	$document->->first('p')->text() - текст первого попавшегося абзаца
	$document->->first('p')->html() - outerHTML первого попавшегося абзаца
	$document->->first('p')->innerHtml() - innerHtml первого попавшегося абзаца
 	$document->first('.xxx')->text() - текст п.п. тега с классом xxx
		        ('#ccc')->text() - текст п.п. тега с id ccc
			('[type="text"]')->text() - текст п.п. тега с атрибутом
    	$document->first('a')->href - атрибут href ссылки
	$document->first('a')->attr('href') - то же самое
	$document->first('#block')->first('.elem')->text() - ищем элемент с id, затем в нем другой элемент с классом
	$document->find('p') - получить все абзацы массивом. Затем можно вывести их тексты в цикле: foreach($document->find('p') as $p) { echo $p->text() }
												    либо короче: ->find('p::text') - массив текстов
		 ->find('div.elem') - массив всех div с классом elem

	$document->find('div.block p.elem::text') - тексты абзацев с классом elem внутри родительского дива с классом block

	if($document->has('p')){ ... } - проверка, на наличие
	$document->count('div') - количество элементов
	$document->first('ul')->count('> li') - количество пунктов в списке

	Методы find(), first(), has(), count() доступны также и для найденного элемента.

	DiDOM поддерживает стандартный поиск по css селекторам:
	$document->find('#foo.bar') - элемент с id="foo" и классом bar
 		 ->find('[name]') - с атрибутом name
		 ->find('input[name="foo"]') - инпут с name="foo"
		 ->find('input[name="foo"][value="bar"]') - инпут с name="foo" и value="bar"
		 ->find('input[name!="foo"]') - инпут в котором name не равно "foo"
		 ->find('a[href^=https]') - адрес начинается на https
		 ->find('img[src$=png]') - изображения с расширением png
		 ->find('a[href*=example.com]') - ссылки, содержащие в своем адресе строку "example.com"
		 ->find('a.ccc::text') - массив текстов всех ссылок с классом ccc. То же самое, что если в цикле получать
		 ->find('a.ccc::text()') - то же самое
		 ->find('> a') - все ссылки, которые являются прямыми потомками текущего элемента

	$element->setInnerHtml('<a href="#">Foo</a>') - изменение innerHtml найденного элемента
	$element->setValue('Foo') - изменение значения

	echo $document->first('.main-box')->html() - вывод верстки элемента

	$innerHtml = $element->innerHtml() - innerHtml элемента

	echo $element->text() - текст узла и потомков

	$element = new Element('span', 'Hello')	- создание элемента
	echo $element->html() => "<span>Hello</span>"

	$document->first('.main-box')->tagName() - название тега
	
	$element->parent() - получение родительского элемента
	$element->closest('.foo') - ближайший родитель с заданным селектором

	$item->previousSibling() - предыдущий элемент
	$item->previousSibling('span') - предыдущий элемент соответствующий селектору
	$item->previousSiblings() - все предыдущие элементы
	$item->previousSiblings('span') - все предыдущие элементы соответствующие селектору
	$item->nextSibling() - следующий элемент
	$item->nextSibling('span') - следующий элемент соответствующий селектору
	$item->nextSiblings() - все следующие элементы
	$item->nextSiblings('span') - все следующие элементы соответствующие селектору
	
	$document->first('div')->firstChild()->text() - текст первого потомка
     			       ->lastChild()->text() - текст последнего потомка

	$element->ownerDocument() - получение документа
     
	$element->attributes() - получение всех атрибутов
	$element->removeAllAttributes() - удаление всех атрибутов
	
	$document->first('title')->remove() - удаление элемента

111) Нормализация путей:
	<a href="/dir/sub/page.html">text</a> - путь относительно домена
	<a href="dir/page.html">text</a> - путь относительно текущей страницы
	
	if(!str_starts_with($item->href, 'http')) { echo 'http://targ.loc' . $item->href }

	$href = preg_replace('#рег выр#', 'на что меняем', 'строка'); - функция замены по рег. выр.
	$normUrl = preg_replace('#(/\?|/\#).*$#', '', $normUrl) - удаление гет параметров и хешей из url, 
								  иначе много одинаковых ссылок

111) Получили пустые строки или кракозябры?
	1) $html = iconv('windows-1251', 'utf-8', $html);
	2) $html = mb_convert_encoding($html, 'windows-1251', 'utf-8');
	3) $html = iconv('windows-1251', 'utf-8', $html);
	   $html = "\xEF\xBB\xBF" .  $html; - добавили BOM-метку
	4) Может быть, что ответ в gzip, тогда:
	   curl_setopt($curl, CURLOPT_ENCODING, 'gzip');

111) Если на сайте есть карта сайта, то можно получить ссылки через нее.

	sitemap.xml - файл всех ссылок сайта (карта сайта)  для поисковых роботов. 
	              Иногда может включать ссылки на другие sitemap.xml
	Нужен когда:
		1) Нет хлебных крошек
		2) Много страниц
		3) Глубокая вложенность

     https://печати-челябинск.рф/sitemap.xml - так его можно получить. Иногда другой путь указан в robots.txt
    
     Как указывать карту сайта в robots:
	User-agent: * 
	Sitemap: https://rodnovspb.ru/sitemap.xml

     Структура:
	<url>
	   <loc>http://site.ru/v_nerabochee_vremya</loc> - ссылка
	   <lastmod>2005-01-01</lastmod> - последнее обновление
	   <priority>0.8</priority> - приоритет страницы
	   <changefreq>daily</changefreq> - частота изменения страницы
	</url>

    Например на сайте pechat74.ru ссылки на страницы других нас. пунктов как раз расположены в sitemap.xml:
	<loc>https://pechat74.ru/bakal</loc>
	<loc>https://pechat74.ru/chebarkul</loc>
	<loc>https://pechat74.ru/zakaz/zaschita-ot-poddelki/troitsk</loc>
	<loc>https://pechat74.ru/zakaz/zaschita-ot-poddelki/uiskoe</loc> и т.д.

    Превращение sitemap.xml в объект (для перебора ссылок):
	$xml = simplexml_load_file('http://targ.loc/sitemap.xml') // функция simplexml_load_file исп для разбора xml
	
    Объекты SimpleXMLElement  полученные функцией simplexml_load_file() перебираются через ->children():
	foreach ($xml->children() as $item){
    		echo $item->loc . "<br>";}

111) Скачивание файлов при парсинге
	1) Сначала проверям, что функция file_get_contents() сможет открывать url:
		$val = ini_get('allow_url_fopen') - должно быть 1
	   Если не включена, то:
		ini_set('allow_url_fopen', true)

	2) Получение и сохранение картинки:
		$data = file_get_contents('http://targ.loc/img.png') - получение
		file_put_contents('img.png', $data) - сохранение

111) То есть принцип скачивания сайта:

     1)Либо просто забираем верстку и нормализуем все ссылки (картинки, скрипты, стили) 
	на абсолютные (ведущие на сайт донор):
	   $response = $client->request('GET', 'адрес_сайта')->getBody()->getContents() - скачаваем верстку через Газзл
	   $document = new Document($response); - создаем объект сайта через Дидом
	   $links = $document->find('a')
	   foreach($links as $link){ 
		$href = normalise($domain, link->href);
		$link->attr('href', $href)} - заменили ссылку на абсолютную, ведущую на сайт донор
					      Также можно $link->href = $href
	   echo $document->html() - выводим верстку с измененными ссылками или сохраняем где нужно
     
     2)	Либо скачиваем верстку и все файлы из нее к себе на сервер:
	  Все то же, но вместо изменения ссылок файлов на абсолютные качаем эти файлы:
		$links = $document->find('a')
		foreach($links as $link){ 
		   $data = file_get_contents($href);
        	   file_put_contents('имя файла', $data);}

111) Отправка формы методом GET

	Допустим есть страница с формой и на ней же код обрабоки формы:
	<form action="" method="GET">
	   <input name="num1">
	   <input name="num2">
	   <input type="submit">
	</form>

	<?php 
	   if (!empty($_GET)){ 
	   $num = $_GET['num1'] * $_GET['num2'];
    	   echo "<p id='qqq'>$num</p>";}
	?>

   Чтобы отправить на такую форму данные и получить результат нужно запросить страницу формы прикрепив get параметрами
   нужные данные.
	Другая страница:
	$client = new Client(); - Газзл
	$response = $client->request('GET', 'http://targ.loc/?num1=10&num2=20')->getBody()->getContents() - запрос страницы формы с отдачей гет параметров
	Можно вывести весь ответ: echo $response; или поместить ответ в DiDOM:
	$document = new Document($response) - грузим страницу в Дидом, чтобы прочитать абзац с ответом
	echo $document->first('#qqq')->text() - получили результат

  Таким образом для того, чтобы спамить чужие формы достаточно отправлять на action формы get и post запросы 
  прикрепляя требуемые формой параметры	

111) Запрос страницы методом POST на action формы (целевая форма работает через POST):
	$client = new Client();
	try { $response = $client->request('POST', $url, [
        	'form_params' => [
            	   'num1' => 11,
            	   'num2' => 22,]])->getBody()->getContents();}
    Запрашиваем страницу одновременно прикрепляя данные в тело запроса.

111) Мы не сможем увидеть код php целевой страницы, а именно что он ждет и как обрабатывает, поэтому нужно внимательно
     смотреть на инпуты формы, что она отправляет и отправлять то же.
     Например:
	1) Отправлять данные именно на action формы
	2) Отслеживать input type='hidden', есть ли name и value у input type="submit"
	3) Отслеживать добавляются ли скрытые инпуты через js

111) Защита через JS от спама формы:
	let form = document.querySelector('form');
	form.addEventListener('submit', function() { - по событию submit добавляем скрытый инпут
	let hidd = document.createElement('input');
	hidd.type = 'hidden';
	hidd.name = 'xxx';
	hidd.value = '+++';
	form.append(hidd);});

	if (isset($_POST['xxx']) and $_POST['xxx'] == '+++'){} - в php коде проверяем наличие добавленного инпута

	Ларавель защищается от спама форм двумя способами:
	1) Проверяет, что инпут type="hidden" name='_token' (csrf) актуальный
	2) Проверяет сессионную куку.
	
	Если на форму Ларавель отправить запрос вот так, то Ларавель примет заказ:
	$response = $client->request('POST', 'http://pechati.loc/ooo', [
			'form_params' => [
				'requisites__name' => 222, - инпут формы
            			'_token' => 'AktDlf0hh1Wck3gDAaRa4YSM01ahL8HPUDDY8Ars'], - csrf токен
			'headers' => ['Cookie' => 'pechatiloc_session=eyJpdiI6IkCJtYVkIn0']]) - сессионная кука

111) Капча
	Принцип работы:
	  1) На странице с формой изображение капчи <img src="capcha.php">
	     Грузится она со скрипта capcha.php.
	  2) capcha.php отдает картинку капчи и записывает расшифровку в сессию
	  3) Пользователь вводит свою расшифровку и она сравнивается с расшифровкой в сессии
     
111) Если спамим на форму с изображением-капчой отдающейся php скриптом, то этапы:
	1) Заходим на страницу формы:

	$headers = ['Accept: ...', ...] - устанавливаем браузерные заголовки (Accept, Referer, User-Agent, cookie)
	$url = 'https://javascript.ru/forum/register.php';
	$curl = curl_init($url);
	curl_setopt($curl, CURLOPT_SSL_VERIFYPEER, 0);
	curl_setopt($curl, CURLOPT_SSL_VERIFYHOST, 0);
	curl_setopt($curl, CURLOPT_RETURNTRANSFER, true);
	curl_setopt($curl, CURLOPT_COOKIEFILE, 'cookie.txt');
	curl_setopt($curl, CURLOPT_COOKIEJAR, 'cookie.txt');
	curl_setopt($curl, CURLOPT_HTTPHEADER, $headers); 
	$html = curl_exec($curl); - получаем верстку
	$document = new Document($html);
	$imgSrc = $url . '/' . $document->first('#imagereg')->src;  - получаем путь к php скрипту отдающему изображение
	
	2) Получаем само изображение:
	$curl = curl_init($imgSrc);
	curl_setopt($curl, CURLOPT_SSL_VERIFYPEER, false);
	curl_setopt($curl, CURLOPT_RETURNTRANSFER, true);
	curl_setopt($curl, CURLOPT_COOKIEFILE, 'cookie.txt');
	curl_setopt($curl, CURLOPT_COOKIEJAR, 'cookie.txt');
	curl_setopt($curl, CURLOPT_HTTPHEADER, $headers);
	curl_setopt($curl, CURLOPT_FOLLOWLOCATION, true); - без этого не работало
	$data = curl_exec($curl);
	file_put_contents('cap.jpg', $data); - сохраняем изображение в файл
	
	3) Отправляем изображение капчи на расшифровку:
	$keyRucaptcha = '090cf340b43935740f22863d01dbf2c2'; - ключ с рукапчи
	$solver = new \TwoCaptcha\TwoCaptcha($keyRucaptcha);
	$result = $solver->normal('cap.jpg')->code; - здесь будет расшифровка
	
	4) Предварительно отправив ручками запрос на форму и отследив в браузере куда идет запрос и 
	   что она ждет (вкладка Payload) заполняем запрос:
	$headers = ['Accept: ...', ...]
	$data = ['name' => 'Денис', 'password' => 111]
	$curl = curl_init('https://javascript.ru/forum/register.php?do=addmember');
	curl_setopt($curl, CURLOPT_COOKIEFILE, 'cookie.txt');
	curl_setopt($curl, CURLOPT_COOKIEJAR,  'cookie.txt');
	curl_setopt($curl, CURLOPT_POST, 1);
	curl_setopt($curl, CURLOPT_POSTFIELDS, http_build_query($data));
	curl_setopt($curl, CURLOPT_FOLLOWLOCATION, true);
	curl_setopt($curl, CURLOPT_SSL_VERIFYPEER, false);
	curl_exec($curl); - но этот сайт так и не захотел региcтрировать и просил включить js или куки

	Ошибка была видна в curl_getinfo($curl)['http_code'] => 403 код

111) Рабочий способ обхода рекапчи V2 Я не робот, капча вставляется на сайте через js:
	ini_set('max_execution_time', '10000');
	set_time_limit(0);
	ini_set('memory_limit', '2048M');
	ignore_user_abort(true);
     
	1) Заходим на страницу с формой
	$page = 'https://amdy.su/2021/08/23/cache/'; - страница формы
	$curl = curl_init($page);
	curl_setopt($curl, CURLOPT_SSL_VERIFYPEER, false);
	curl_setopt($curl, CURLOPT_RETURNTRANSFER, true);
	$html = curl_exec($curl);
	$document = new Document($html);
	$html = $document->text(); - получили верстку
	preg_match_all("#'sitekey\W*(\w+\S*)'#", $html, $match); - получили sitekey из текста js скрипта

	2) Отправляем на рукапчу sitekey и целевую страницу
	$keyRucaptcha = '090cf340b43935740f22863d01dbf2c2';
	$solver = new \TwoCaptcha\TwoCaptcha($keyRucaptcha);
	$result = $solver->recaptcha([
    		'sitekey' => $match[1][0],
    		'url'     => $page,
		])->code; - здесь получим расшифоровку
	
	3) Посмотрели в action формы или через инструменты разр. куда идет post запрос формы, скопировали ушедшие 
	   заголовки, посмотрели ушедшие данные, создаем запрос:
	$headers = ['Accept : text/html ...', 'Referer: ..., 'User-Agent: ...'] - скопировали из заголовков
	$data = [
		'comment' => 'Спасибо за материал',
		'author' => 'Денис',
		'g-recaptcha-response' => $result] - в этот элемент вставляем расшифровку и отправляем заодно формой
						     Элемент может быть другой, смотреть в Рукапче 

	4) Отправляем форму
	curl_setopt($curl, CURLOPT_POST, 1);
	curl_setopt($curl, CURLOPT_SSL_VERIFYPEER, false);
	curl_setopt($curl, CURLOPT_HTTPHEADER, $headers);
	curl_setopt($curl, CURLOPT_POSTFIELDS, http_build_query($data));
	curl_setopt($curl, CURLOPT_COOKIEFILE, 'cookie.txt');
	curl_setopt($curl, CURLOPT_COOKIEJAR, 'cookie.txt');
	curl_setopt($curl, CURLOPT_FOLLOWLOCATION, true);
	curl_exec($curl);
	
111) Перезапуск скрипта по расписанию (например для проверки новых материалов на сайте):
	1) Через обновление вкладки браузера плагином:
	   https://chrome.google.com/webstore/detail/tab-auto-refresh/oomoeacogjkolheacgdkkkhbjipaomkn?hl=ru
	2) Через js:
	   setInterval(()=>{ location.reload(); }, 100000)
	3) Если сайт на хостинге, то cron

111) Если парсим большой и дорогой сайт, то могут забанить или выдать капчу.
     Поэтому нужно имитировать пользователя, например останавливать скрипт на неравномерные промежутки
     времени:  
	sleep(3) - 3 секунды

111) Если забанили по ip парсинг с локалки, то скорее всего у вас динамический ip, перезагружаем роутер,
     меняем ip.

111) Из-за sleep() парсинг будет медленнее, но можно увеличить скорость если отправлять запросы меняя
     свой ip:
	1) Покупаем доп. ip на хостинге
	2) Меняем время от времени ip сайта со скриптом:
		curl_setopt($curl, CURLOPT_INTERFACE, 'ip адрес')

111) Также можно купить прокси и переключать ip при парсинге так:
		curl_setopt($curl, CURLOPT_PROXY, 'ip адрес');

111) Для имитации пользователя не забываем устанавливать куки:
	$cookieFilePath = $_SERVER['DOCUMENT_ROOT'] . 'cookie.txt';
	curl_setopt($curl, CURLOPT_COOKIEFILE, $cookieFilePath); // в адресе не должно быть русских папок
	curl_setopt($curl, CURLOPT_COOKIEJAR,  $cookieFilePath);

111) Куки лучше установить прямо в $headers в заголовке cookie (смотрим в браузере их)
    
111) Также смотрим в панели и вставляем заголовки. 
	curl_setopt($curl, CURLOPT_USERAGENT, 'Mozilla/4.0 (compatible; MSIE 7.0; Windows NT 5.1)');

111) Иногда у сайта есть API, который может отдать нужные данные. Поэтому проверяем наличие API,
     прежде чем писать парсер.	

111) Парсер на php будет получать только изначальный код страницы. Если что-то на ней меняется в
     браузере пользователя через js, то для парсинга нужно использовать Selenium (автоматизация
     действий браузера).

111) Также парсер не увидит данные, которые запросил сайт через fetch, отслеживаем такие запросы
     в Network->fetch/xhr->смотрим заголовки соединений (там есть адреса)





======================================================= .htaccess  =============================================================
За работу .htaccess отвечает php модуль mod_rewrite. 
Проверить phpinfo() -> на строке Loaded Modules должен быть загружен mod_rewrite
Команды пишем в условии, проверяя что модуль включен:
	<IfModule mod_rewrite.c>
		...
	</IfModule>

Начинаем файл с:
	RewriteEngine On
	RewriteBase / - Трепачев говорит писать всегда. Директива нужна для работы RewriteRule. В сети пишут не обяз.

RewriteEngine On - включение файла .htaccess
RewriteEngine Off - отключение (либо все комментируем)

# - директива закомментирована (напр. #Redirect ....)

Команда Redirect (в таких url всегда указываем слеши)
Redirect /index.php /test.php - редирект с файла index.php на test.php
Redirect 301 /index.php /test.php - редирект с добавлением кода HTTP 301 (перемещено навсегда)

Можно писать редиректы подряд:
	Redirect /page1.php /index.php
	Redirect /page1.php /page1/
	Redirect /folder/page2.php /index.php - перенаправление из подпапки
	Redirect /index.php https://ya.ru/ - перенаправление на другой сайт

Редирект на другой сайт может быть полезен, если переехали на новый домен:
	Redirect /about.php https://site.ru/about.php

Команда RewriteRule 
Работает с регулярными выражениями, загружает указанную страницу, причем введенный адрес 
остается как был введен. Здесь в отличие от Redirect начальный слеш не используем.
Можно оборачивать в карманы (), которые используем как $1 и т.д.
		
RewriteRule ^dir/ index.php  - все адреса начинающиеся на dir/ загрузят страницу index.php
                               Причем в браузере будет показываться введенный адрес
RewriteRule ^dir/page\.php index.php - по запросу site.ru/dir/page.php будет загружаться страница index.php
				       Так как рег. выражения, то точки экранируем
RewriteRule ^dir/([^/]+)/ index.php?get=$1 - загрузится index.php с передачей параметров гет параметров из кармана

RewriteRule (.+)\.html$ $1.php - при запросе site.ru/index.html будет загружена site.ru/index.php
				 Адрес в браузере будет site.ru/index.html		
RewriteRule .+ index.php - любой адрес будет загружать страницу index.php, причем в браузере показывается введеный url

RewriteRule !test\.php index.php - отрицание (нет в регулярках php). Все адреса кроме test.php загрузят index.php

В .htaccess выполняются все следующие директивы, даже если мы попали в правило.
Например:
RewriteRule .+ index.php
RewriteRule .+ test.php - по итогу загрузится test.php

Для того, чтобы остановить выполнение, если правило сработало используем флаг [L]:
RewriteRule .+ index.php [L] - загрузится index.php, дальнейшие проверки не выполнятся
RewriteRule .+ test.php

Флаги RewriteRule:
	[NC] - nocase, нечувствительность к регистру
	[QSA] - query string append, к полученному url добавить изначальную строку запроса
	[L] - last, остановить процесс преобразования на этом месте	
	[N] - next, запустить преобразование полученного url c начала .htaccess
	[F] - возвратить ошибку 403 (доступ запрещен)
	[R] - редирект с 302 кодом (временно перемещен)
	[R=code] - редирект с опр. кодом

Переменные:
	%{HTTP_USER_AGENT} - поле User-Agent от браузера
	%{REMOTE_ADDR} - ip пользователя
	%{REQUEST_URI} - URI
	%{QUERY_STRING} - get параметры после ?

Команда RewriteCond
Используется как условие выполнения следующего за ней RewriteRule.
Тоже можно исп. регулярные выражения.
Внимание! Здесь начальные слеши ИСПОЛЬЗУЕМ.
RewriteCond %{REQUEST_URI} !/test\.php - если uri не является test.php
RewriteRule .+ index.php - то загружаем index.php (в общем здесь то же, что RewriteRule !test\.php index.php)

RewriteCond %{REQUEST_URI} \.php$ - если uri заканчивается на .php
RewriteRule .+ index.php - загружаем index.php

RewriteCond %{HTTP_HOST} ^www.site\.ru$ [NC] - если домен адреса www.site.ru
RewriteRule ^(.*)$ https://site.ru/$1 [R=301,L] - перенаправляем на домен без www
Таким образом: www.site.ru/page1 => site.ru/page1

RewriteCond %{SERVER_PORT} !^443$ - если адрес не https
RewriteRule .* https://%{SERVER_NAME}%{REQUEST_URI} [R=301,L] - постоянный редирект на такой же адрес https

Несколько RewriteCond работают как логическое И:
RewriteCond %{REQUEST_URI} \.php$ - если uri заканчивается на .php
RewriteCond %{REQUEST_URI} ^/dir/ - а также начинается на /dir/, то
RewriteRule .+ index.php - загружаем index.php

RewriteCond %{REQUEST_URI} \.(php|html)$ - если оканчиванивается на php или html
	можно переписать с флагом:
RewriteCond %{REQUEST_URI} \.php$ [OR] - если оканчиванивается на php
RewriteCond %{REQUEST_URI} \.html$ - или оканчивается на html

RewriteCond %{REQUEST_URI} \.php$ [OR] - если оканчиванивается на php
RewriteCond %{REQUEST_URI} ^dir/ - или начинается на dir/

RewriteCond %{REQUEST_FILENAME} -f   - если файл существует (где REQUEST_FILENAME полный путь к файлу)

RewriteCond %{REQUEST_FILENAME} -s - если файл существует и он не нулевого размера

RewriteCond %{REQUEST_FILENAME} -d - если папка существует (например вводим site.ru/dir/)

RewriteCond %{REQUEST_FILENAME} !-f - отрицание. Если файл не существует, то

RewriteCond %{REQUEST_URI} !^/test/ - здесь RewriteCond избавляет от зацикливания, без него было бы:
RewriteRule (.+) test/$1  /test/test/test/test/test/

RewriteCond %{REQUEST_URI} (.+) - в карман можно ложить и в RewriteCond
RewriteRule (.+) index.php?get=%1 - использовать карман из RewriteCond нужно через %

RewriteRule ^test\.php index.php [NC] - флаг NC используется, чтобы игнорировать регистр (test = TEST)

ErrorDocument 404 /404.php - если страница не найдена показывать 404.php

Если разместить в папке .htaccess со строчкой deny from all, то файлы станут закрытыми:
	RewriteEngine On
	RewriteBase /
	deny from all

site.ru/dark.php - не будет работать
include 'dark.php' - однако это будет работать

Перенаправление всех запросов в папку:
RewriteEngine on 
RewriteCond %{HTTP_HOST} ^(www.)?domain.com$
RewriteRule ^(.*)$ /folder/$1 [L]

Включение сжатия в gzip на сервере, пишется в htaccess:
php_flag zlib.output_compression On
php_value zlib.output_compression_level 5

Перенаправление с припаркованного домена на основной домен:
RewriteCond %{HTTP_HOST} ^(www\.)?pechati-shtampy-izgotovlenie\.ru$ [NC]
RewriteRule ^(.*)$ https://site.ru/$1 [R=301,L]


Защита сайта от просмотра и роботов через пароль от браузера. Например, при разработке сайта и показа его клиенту
Можно вызывать окошко аутентификации при помощи htaccess:
AuthType Basic   - Basic означает базовую браузерную аутентификацию
AuthName "Private zone. Only for administrator!" 
AuthUserFile /path/to/www/.htpasswd  - путь к файлу .htpasswd где лежит пароль (можно узнать через файл php: echo dirname(__FILE__))
require user admin

Файл .htpasswd: - сгенерировать данный файл можно с помощью файла htpasswd.exe (описание есть в интернет)
admin:$apr1$5yC6wlzB$9qbRcHkGjk3DjeIbtu6w21 - пользователь admin и его хешированный пароль. 


======================================================= OctoberCMS  =============================================================
	
Установка Октября через OpenServer

111) Создаем в openserver новый домен и его папку

111) Скачиваем и распаковываем в эту папку install-master с сайта Октября, чтобы файл install.php находился в корне

111) Создаем БД в phpMyAdmin

111) Запускаем файл install.php: http://october.loc/install.php, выбираем язык

111) Вписываем доступ к БД: MySQL, localhost, имя БД, root

111) Вводим лицензионный ключ, нажимаем Install

111) Нажимаем кнопку Administration Panel и создаем учетную запись администратора

111) Файлы установятся. Демо сайт лежит в папке themes, туда же нужно будет грузить новый сайт

111) Прописываем нужные настройки в .env (константы .env авт. прописываются при установке Октября и редактируются из админки)

111) Сайты лежат в папке themes.
     Для того чтобы создать новый сайт: админка->фронтенд шаблоны->создать пустой шаблон->в папке themes создается
     структура папок(pages, partials и пр.). Здесь же можно найти шаблон на маркетплейсе.
     Активируем новый шаблон. Теперь он будет показываться вместо Демо сайта.

111) Смотря на демо-шаблон создаем свой сайт. Названия папок смотрим в Демо (например assets/images)


======================================================= Docker  =============================================================
     
Vagrant - программа, позволяющая через консоль создавать виртуальные машины VirtualBox для установки окружения.
          Описание окружения и программы для установки пишутся в скриптах для вагранта.
          Можно писать даже какие таблицы создавать в БД.

Докер в отличии от Вагранта позволяет запустить одну операционную систему и множество одинаковых программ на этой системе.
Вагрант запустит 3 операционной системы под 3 одинаковой программы.

docker -v - проверка версии докер в консоли

docker run -e MYSQL_ROOT_PASSWORD=root mysql  - докер скачает убунту + mysql и установит пароль на БД      

docker run -e MYSQL_ROOT_PASSWORD=root mysql ls - покажет список файлов установленного образа

docker-compose.yml - конфигурационный файл докера, в котором указываются команды что установить и как настроить, вместо того, чтобы
                     писать это руками в консоли

docker-compose up -d  - запустить создание контейнеров по docker-compose.yml

docker-compose down - останавливает все сервисы и уничтожает контейнеры

docker-compose up -d --scale mysql=3 - запустить создание 3 баз mysql в соответствии с файлом docker-compose.yml

docker-compose up --build -d  - пересоздать контейнеры в соответствии с изменениями в docker-compose.yml

volumes - в этой строчке из docker-compose.yml можно указывать путь где хранить сохраненные данные контейнера

Пример файла docker-compose.yml:
	version: '2'
	services:
    	   mysql:
              image: mysql:8
              volumes:
                 - ./storage/docker/mysql:/var/lib/mysql - здесь будут храниться сохраненки
              environment:
                 - "MYSQL_ROOT_PASSWORD=secret"
                 - "MYSQL_USER=app"
                 - "MYSQL_PASSWORD=secret"
                 - "MYSQL_DATABASE=app"

Теперь так как есть строчка volumes создастся папка docker и не удалится при docker-compose down, a при новом запуске 
контейнеров через docker-compose up -d  данные возьмутся из сохраненок

Проверяем, что все расшщирения из файлов .docker установились правильно. Например, у меня не устанавливалось mcript, вылезала ошибка
для php 8.0
	
======================================================= Действия для выкладки сайта  =============================================================	

- Яндекс вебмастер https://webmaster.yandex.ru/sites/
- Яндекс метрика https://metrika.yandex.ru/list
- Добавить сайт в гугл и загрузить карту сайта https://search.google.com/search-console
- загрузить карту сайта в яндекс https://webmaster.yandex.ru/site/indexing/sitemap/
- фавиконки https://realfavicongenerator.net/
- robots
- живосайт 


Запускаем openserver
Запускаем через закладки Шторм - теперь php работает в Шторме, можно отключить openserver.
Галочку в Шторме Tools -> Deployment -> Automatic Apload
Иногда Шторм не отправляет измененный код верстки, можно добавить-убавить пробел, чтобы он отправил файл на хостинг
php artisan optimize:clear - очищаем кеш на компе
php8.1 artisan optimize:clear - очищаем кеш на хостинге  в ssh консоли
npm run build на компьютере, файлы начинают грузиться на сервер, ждем
php8.1 artisan optimize  - заново кешируем на сервере
Tools -> Deployment -> Automatic Apload - убираем галочку, чтобы при изменении файлов на компе не передавалось на сервер
		
============================================= Evo CMS  ========================================================
0) Документация: https://github.com/0test/evo-newdocs/tree/main/v3
	         https://github.com/0test/lessons-evolution-blog/tree/main
		 https://evo3blog.aharito.ru/		
1) Создаем в openserver домен, например, evo.loc привязываем к папке на компьютере
2) Создаем в phpMyAdmin БД, например evo,  utf8mb4_general_ci - как рекомендует evo
2) С сайта https://evocms.ru/ грузим evo, сохраняем файлы в папке сайта
3) Заходим на evo.loc, устанавливаем cms. 
   На этапе создания БД прописываем имя БД evo, префикс evo_
   Устанавливаем логин и пароль администратора	
   https://evo.loc/manager/ - !!! входим через https, через http не войдет.  Вводим логин и пароль, указанные при установке

   Настройки:
   Заголовок сайта: Название сайта
   Controller Namespace: EvolutionCMS\Main\Controllers\ - чтобы запускались контроллеры из пакета main  // переменные настроек также можно уст. в файле core/custom/config/cms/settings.php
   Способ кэширования - Отключить для администраторов
   Суффикс для дружественных URL = пустое поле
   Тип сервера: https
   Лента RSS новостей - удалить
   Безопасность/Лента RSS безопасности - удалить
   Цветовая схема - Все темное
   Группировать ТВ параметры: Секциями в Общее
   Показать навигацию => Да
   Страница ошибки '404': - тут id 404 страницы указать, как создам (показывается на несуществующую или страницу без галочки "Публиковать" - быстрее проверить в инкогнито)
   Первая страница: - тут id страницы открывающейся по https://site.ru, по умолчанию 1
   Сохранить

   По умолчанию файлы сессий и файлы переменных Cache хранятся в core/storage, можно перенастроить на Редис:
   1) Включаем модуль Редис в Опенсервер
   2) в .env:
   	CACHE_DRIVER=redis 
   	SESSION_DRIVER=redis 
   	REDIS_CLIENT=predis
   3) Проверяем: Session::put('key', 333); Session::get('key')
   4) Теперь файлы кеш и сессий не создаются в core/storage, а создаются в Редисе (есть панель просмотра)
      Скомпилированные файлы blade (та же верстка, но @if замененено на <?php if($templates): ?>) по-прежнему 
      создаются в core/storage/blade
      
3) Открываем шаблон Minimal, переименовываем в Главная с псевдонимом main + Создать файл шаблона при сохранении
   Переименовываем имеющийся ресурс в Главная + псевдоним index, удаляем из содержимого ресурса текст
  
4) Переходим в терминале в папку core:
	cd core
	composer update
	php artisan package:create main - создаем папку с контроллерами и пр. (core/custom/packages/main)

4) Дальнейшие готовые файлы есть в Полезное -> Готовые файлы evo

4) Если нужно, чтобы работала сессия:
   Раскомментировать в core/custom/routes.php строку Route::fallbackToParser();
   Иначе сессия session() из Ларавель не видна в контроллерах страниц и итоговых страницах

4) Флеш переменные на Ево проще через обычную сессию делать:
	$_SESSION['success'] = true;
	
	@if($_SESSION['success'])
           <div>Успешно</div>
           @php
               unset($_SESSION['success'])
           @endphp
    	@endif

4) Важно!
   По каким-то причинам в evo функции dd, dump, var_dump в шаблонах работают только если зашел в админку, может кешируются шаблоны
   без админки

4) Создаем папку core\custom\packages\main\src\Models, здесь будем складывать модели

4) Размещаем файл настроек core/custom/.env (берем из прошлого сайта, меняем домен, бд, пути)

   Готовый env копируем из старого сайта. 
   Константы путей для локального env можно смотреть в панели менеджера => Настройки => Системная информация.
   
   Константы для хостинга Бегет.
   Если русские домены, то пишем их кракозябрами.
	APP_ENV=local - будем использовать, напр., для отключения tracy если сделать production
	MODX_BASE_PATH=/home/c/cinedr/evo.ru/public_html/   //пути можно узнать в ssh командой pwd
   	MODX_BASE_URL=/
	MODX_SITE_URL=https://evo.ru/
	MODX_SITE_START_URL=https://evo.ru
	MODX_SITE_HOSTNAMES=evo.ru
	MGR_DIR=manager
	MODX_MANAGER_PATH=/home/c/cinedr/evo.ru/public_html/manager/
	MODX_MANAGER_URL=https://evo.ru/manager/

   Для одновременной разработки на локалке и периодической выкладки на хостинг эти файлы должны отличаться на компьютере и хостинге.
   Как отключить выкладку на хостинг: Tools -> Deployment -> Configuration -> Exluded Paths
                                      Или проще: В remote host правой кнопкой на папке/файле -> exclude path
				      Файлы не будут копироваться при изменениях, но все равно выкладываются при deployment

4) Внимание! После выкладки на хостинг нужно зайти в админку через хостинг и обновить контрольную сумму, иначе не работали скрипты

4) Это для опенсервера: в нем запрещен доступ к папкам, начинающимся на точку. В evo на точку начинается папка .thumbs, чтобы превью из нее были доступны:
	openserver -> дополнительно -> конфигурация -> файл настроек Апача -> откроются файлы настроек апача и нгинкс, 
	ищем строчку well-known и комментируем:
	#<LocationMatch "/\.(?!well-known)">  
    	#	Require all denied
	#</LocationMatch>

	# Disable access to hidden files/folders
    	#location ~* /\.(?!well-known/) {
        #    deny all;
        #    log_not_found off;
        #    access_log off;
        #}
	  
4) Настраиваем роуты и посредники:
	1) core/custom/routes.php - тут пишем роуты
	2) core/custom/config/middleware.php - тут прописываем посредники для всех маршрутов:
		'global' => [ EvolutionCMS\Middleware\VerifyCsrfToken::class,], - будет работать для @csrf в формах

4) Если начинает показываться ошибка CSRF attempt, то:
	0) Была ошибка в env: MODX_SITE_URL стоял http домен и стили, скрипты запрашивались по http - изменить на https
           Остальное делать, если не помогло.
	1) В core/custom/config/cms/settings.php поставить 'validate_referer' => false
	2) Либо сразу в админ панели: Безопасность -> Проверять серверные заголовки = Нет
	3) Если ошибка исчезнет поставить обратно "Да" и проверить
	4) Насколько понял шибка выскакивает только на http адресе, а на https нет

4) Для того, чтобы evo не показывала ошибку изменения файловых путей при периодической выкладке на хостинг:
   в файле core/custom/config/cms/settings.php:
	'filemanager_path' => env('MODX_BASE_PATH'),
    	'rb_base_dir' => env('MODX_BASE_PATH') . 'assets/',
	'site_url' => env('MODX_SITE_URL'),
	'site_start_url' => env('MODX_SITE_START_URL'), 

4) Далее нужно установить:
	1) tinyMCE: Модули -> Найти и установить tinyMCE
	2) ElementsInTree -  панелька страниц, шаблонов, плагинов и пр.
	3) Также DocLister - готовые сниппеты, напр. DLMenu для авт. создания меню
        4) Также FormLister - неудобная работа с формами (проще формой из блейда отправлять данные в контроллер по типу Ларавель),
			      но без него не устанавливается ClientSettings
	5) SimpleGallery - крепит к документам вкладку галереи, в которую можно грузить фото. 
			   Пути фото сохраняются в таблице evo_sg_images. Фото можно выводить через DocLister
		           В настройках simplegallery нужно прописать к чему ее привязывать:
			   Плагины -> Конфигурация -> Templates = 1 -> В документах по этому шаблону появилась вкладка simplegallery
	6) ClientSettings - модуль переменных, которые может менять заказчик из админки (их значения вставляются в верстку), например:
			    телефоны, адреса, скрипты (метрика)
			    Переменные сохраняются в таблице evo_system_settings, начинаются с client_
			    assets/modules/clientsettings/config/ - здесь файл с полями
			    Переменная будет называться по ключу в массиве settings: 'settings' => ['menu' => [...]] => переменная client_menu
			    В админке появилась вкладка Настройки сайта для редактирования значений переменных

			    Если хотим размножаемое multitv, то:
				1) 'type' => 'custom_tv:multitv'
				2) assets/tvs/multitv/configs/  - создаем файл, название должно совпадать и именем переменной ClientSettings

			    Можно поставить:
				   'type' => 'custom_tv:selector' - будет готовый список с выбором страниц
			    Также этот тип можно настроить по-своему в assets/tvs/selector/lib/название_переменной.controller.class.php

			    {{ $modx->getConfig('client_delivery') }} - вывод поля в верстке	
			    $this->evo->getConfig('client_delivery') - в контроллере
		    
	7) multiTV - 1) При создании ТВ или в ClientSettings можно будет выбрать тип ввода multiTV. Можно добавить новые поля (картинка, ее заголовок, альт, thumbs и пр.)
		     2) Через "+" можно вставлять дополнительные однотипные ТВ (например другие фото в фотогалерею)
                        Чтобы отключить плюс: $settings['display'] = 'single';
                        Документация: https://docs.evo.im/04_extras/multitv.html
                     Требуется файл настроек в assets/tvs/multitv/configs/название_tv.config.inc.php
		     Для поля richtext можно настраивать редактор настройкой 'theme' => 'mini'
	7) MultiCategories (может быть отключен, включить)
                           Документы, куда можно помещать другие сделать папками-категориями (галочка Контейнер)
			   В настройках плагина: Templates, Documents - документы которые можно будет помещать в несколько категорий
			   		         Parents - папки-категории, в которые можно будет помещать другие документы
			   Создается таблица evo_site_content_categories в которой напротив id документа появились id родительских папок-категорий
		            
	7) multiFields - более продвинутый вариант multiTV. Позволяет размножать как сам блок, так и его внутренние части
                         А) С clientsettings:
			       1) В настройках переменной assets/modules/clientsettings/config/footer.php:
					'item' => [ - файл с таким именем нужно будет создать по нему же будет имя настройки, т.е. client_item (обычно client_footer)
      					    'caption' => 'Подвал',
      					    'type' => 'custom_tv:multifields',],
			       2) Создаем файл assets/plugins/multifields/config/item.php с настройками multifields
			 Б) В ТВ параметры:
			       1) В ТВ параметре slider выбираем тип поля multifields
			       2) Cоздаем файл настроек assets/plugins/multifields/config/slider.php 
			    Пример настройки для размножения внутренней части:	
			 	'settings' => [], 'templates' => ['list' => ['type' => 'row','title' => 'Заголовок','items' => ['item' => ['type' => 'text','title' => 'Введите пункт списка',],],],]
                                Подсказали, чтобы внешний блок не размножался ставить ему: 'actions' => false (или  'actions' => ['hide'])
	8) PageBuilder - создание и редактирование блоков на странице (формы, галерея и пр). 
                         То есть блоков, наполненных разнообразным содержанием (фото, ссылки, дата, раскр. список, разные инпуты)
                         Пользователь может выбирать тип блока (слайдер, карточки и пр.), менять их местами.

			Пример создания страницы-лендинга:
			1) Устанавливаем PageBuilder
			2) Элементы => Плагины => PageBuilder:
				Tab name: Конструктор страниц
				Add type: images
				Placement: content
			3) Создаем ТВ с именем landing, заголовком Конструктор страницы, типом ввода pagabuilder
			   Привязываем его к нужному шаблону
			4) Создаем контейнер блоков: assets\plugins\pagebuilder\config\container.landing.php:
				<?php
				    return [
  					'title' => 'Лендинг',
  					'show_in_templates' => [ 12 ],  // id шаблона
  					'addType' => 'images',
  					'placement' => 'tv',    //  Показывать в ТВшке
					   ];
			5) Теперь в assets\plugins\pagebuilder\config\ создаем сами блоки и привязываем их к контейнеру:
				landing_slider.php - блок слайдера
				Внутри привязываем его к контейнеру: 'container' => 'landing',
				Описание есть на https://github.com/0test/lessons-evolution-company/blob/main/ => 19 урок
			6) В контроллере шаблона получаем данные и отправляем их во вьюху:
				$data = evolutionCMS()->runSnippet('PageBuilder', [
            				'container' => 'landing',
            				'renderTo' => 'array']);
        			$this->data['pagebuilder'] = $data[0];
			6) Во вьюху пробрасывается массив $pagebuilder с нашими блоками
                           В его ключе config хранится название блока
			7) Для каждого блока в папке landing создадим одноименный blade и будем подключать его в цикле и прикреплять
                           его переменные:
				@foreach($pagebuilder as $item)
            			   @if(View::exists('landing.'.$item['config']))
                		     @include('landing.'.$item['config'],$item)
           			   @endif
        			@endforeach
			8) Создаем вьюхи блоков и принимаем в них переменные
          
			Другой пример:
			1) Создаем настройки блока в assets/plugins/pagebuilder/config/my_block.php
			2) На странице ресурса во вкладке PAGE BUILDER создаем блок
			3) Переменные блока доступны в блейде: @dd($modx->runSnippet('PageBuilder', ['renderTo'=> 'array'])) 

	9) Selector - можно обойтись, используется для тегов и прочего связывания ресурсов между собой
	10) TemplatesEdit3 - позволяет создавать вкладки страницы для тв параметров. Настраивается в шаблоне страницы
		             После установки все поменяет местами
                             Установленные подсказки хранятся в json файлах, поэтому перекочуют на прод (сотрутся там, если были)
	10) Возможные настройки для хостинга есть на https://github.com/0test/lessons-evolution-blog/blob/main - первоначальные настройки

6) В cms заходим в правую верхнюю шестеренку:
	 Конфигурация->Интерфейс и представление -> Редактор -> TinyMCE5
						    Theme -> Full 
						 -> Сохранить
   Также для единообразия можно встроить множество классов в Tiny и дать пользователю выбирать их, а инлайновые стили отключить

7) Плагины -> CodeMirror -> Конфигурация -> Настраиваем размер шрифта кода и пр.

7) Раскомментируем строчки в htaccess:
	php_flag zlib.output_compression On // для Content-Encoding: gzip - сжатия на сервере и ускорения работы
	php_value zlib.output_compression_level 5

	На опенсерсервере HTTP => HTTPS сработало это:
	RewriteCond %{HTTPS} off
	RewriteCond %{HTTP:X-Forwarded-Proto} !https
	RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

	Редирект с www на без, работает на бегете
	RewriteCond %{HTTP_HOST} .
	RewriteCond %{HTTP_HOST} ^www.(.*)$ [NC]
	RewriteRule ^(.*)$ https://%1/$1 [R=301,L]

	C index.php на корень
	RewriteCond %{THE_REQUEST} ^[A-Z]{3,9}\ /index\.(php|html|htm)\ HTTP/
	RewriteRule ^(.*)index\.(php|html|htm)$ $1 [R=301,L]

	# For Friendly URLs
	RewriteRule ^(.*)$ index.php?q=$1 [L,QSA,NC] - чтобы url был нечувствит. к регистру

   Редирект со старых страниц на новые работает косячно, добавлялся параметр q из верхнего правила, поэтому делал так.
   Ставить этот код выше For Friendly URLs
	RewriteRule ^pechati_ip.html$ pechat-ip [R=301,NC,L] 	

7) Включаем Tracy: 
	1) В .env на локалке: APP_ENV=local   (в .env на хостинге ставим production)
	2) Создаем файл core/custom/config/tracy/active.php с кодом:
		if(env('APP_ENV') && env('APP_ENV') == 'local'){ return 'adminfrontonly';}
		else { return false;}
   Внимание! Tracy появится только на https адресе

8) Можно установить vite по инструкции с Полезное/vite для evocms

7) Создаем views/layouts/layout.blade.php :
   <!DOCTYPE html>
   <html lang="ru">
   <head>
     <meta charset="utf-8"> - кодировку ставим в первых 1024 байтах документа
     <meta name="viewport" content="width=device-width, initial-scale=1"> - увеличение пальцами
     <base href="{{ $modx->getConfig('site_url') }}"> // чтобы отн. пути раб. правильно, уст. в custom/config/settings.php: 'site_url' => env('MODX_BASE_URL')
     <meta name="csrf-token" content="{{ csrf_token() }}">
     <meta name="description" content="{{ $documentObject['description'] }}" /> - заполняется по полю страницы "Описание"
     <link rel="stylesheet" href="/template/css/app.css"> - создаем на корневом уровне папку template со стилями, скриптами
     <link rel="canonical" href="{{$modx->getConfig('site_start_url')}}{{urlProcessor::makeUrl($documentObject['id'])}}" />
     <title>{{ $documentObject['pagetitle'] }}</title> - по полю "Заголовок страницы"
   </head>
   <body>
     @include('parts.nav')

     @yield('content')

     <script src="/template/js/script.js"></script> - скрипты из папки template
     @stack('scripts') - можно через @push вставлять скрипты со страниц
   </body>

7) В index.blade.php - файл шаблона
      @extends('layouts.layout')
      @section('content')
         {!! $documentObject['content'] !!} - берется из поля "Содержимое ресурса"
      @endsection

7) Свои пакеты через require устанавливать в папке custom.
   Если в core, то при обновлении сайта затрется core/composer.json
   Проверить, в require должно быть "require": {} // было [] и возникала ошибка
   Далее на уровне core/composer.json сделать composer update (проверить подойдет ли composer dump-autoload)

7) И чего будет состоять сайт:
	1) Повторяющиеся части: views/parts/header.blade.php
	2) Обертки layouts:  views/layouts/layout.blade.php - тот же смысл, что в Ларавель
			     Внутри layout: @include('parts.header') 
					    @yield('content') - принимаем текст с @section('content') из шаблона сущности

	3) Шаблоны сущностей (главная, список постов, пост, контакты и пр.)
           Они создаются в админке:  Элементы -> Шаблон -> Новый шаблон -> Имя: Главная
					      Псевдоним: home, news_main ... (без тире. По ним будут называться контроллеры: MainController.php, NewsMainController.php) 
					      Новая категория (создаем): Шаблоны или Категория (выбираем созданную ранее): Шаблоны
 					      Галочка "Создать файл шаблона при сохранении" 
        				      -> Сохранить -> создался файл  /views/home.blade.php
					      Шаблоны хранятся в evo_site_templates

	   Внутри home.blade.php: @extends('layouts.layout') - встраиваем обертку
	   			  @section('content') ... @endsection - передаем в обертку текст

	4) Ресурсы или страницы. Создаются через "Новый ресурс". Внутри ресурс привязывается к нужному шаблону сущности и передает
				 в него переменные заполненные заказчиком => создается страница
	                         У ресурсов можно создавать дочерние ресурсы, полезно при формировании вложенных меню
	                         По псевдониму ресурса создается url
				 Страницы хранятся в  evo_site_content
				 Получить id: $modx->documentIdentifier
					      $documentObject['id']
					      $this->evo->documentIdentifier
 
                                 Можно создавать родительскую папку-ресурс для других ресурсов и вкладывать туда их.
                                 Чтобы такой ресурс-папка не была доступна в адресной строке в ее настройках:
				 отключаем "Показывать в меню", устанавливаем шаблон blank, отключаем HTML-редактор,  отключаем публикацию, отключаем участие в url 

	5) ТВ-параметры - доп. переменные, назначаются шаблонам, пользователям. На странице появляются новые поля, 
			  напр: Цена, keywords и пр.
			  Создание: Элементы => Параметр => Новый параметр
			  Там же можно выбрать тип поля: текст, флаг, выпадающий список, мультиТВ и пр.
			  Возможные значения для checkbox, radio, option и пр. устанавливаются в полях Возможные значения, Значение по умолчанию:
				Возможные значения:
				 - для одного флажка: Показывать==true  // Подписан будет как "Показовать", а в value будет стоять true
						      ==true  // будет просто флаг без подписи
				 - для нескольких флажков и селектов: option1==value1||option2==value2
				 - для нескольких флажков запросом в БД прописываем в поле Возможные значения: 
					@SELECT name, id FROM [+PREFIX+]cases WHERE name != '' AND deleted_at IS NULL
                                        приходят выбранные id вот так: 1||2||3
			
			  Можно поместить ТВ и мультиТВ в подписанные блоки, если назначить им категорию
			  
			  ТВ-переменные хранятся в таблицах начинающихся на prefix_site_tmplvars
			  Если нужна возможность добавления через +, то выбираем тип ввода multiTV

			  Эти переменные авт. передадутся на страницу в общем контейнере переменных страницы, напр.: $documentObject['keywords']

			  В контроллере они доступны так:  evo()->documentObject['keywords']
			  				   evo()->getTemplateVarOutput('urgency')

			  Также в контроллере их можно получить отдельным запросом к БД:
			  	$page = Page::withTVs(['links'])->find(evo()->documentObject['id'])
			  	$links = $page->links
			  Это может быть удобно, например, в модели Page extends SiteContent можно сразу 
			  извлечь их из json обертки (она используется для тв в виде массивов):
				protected $casts = ['links' => 'array'];

			  У страницы есть служебный заголовок $documentObject['pagetitle'], также понадобится создать тв metatitle,
			  чтобы использовать его в <title>$documentObject['metatitle']</title>

			  Также description можно брать из $documentObject['description']

	   МультиТВ - переменная может включать блоки из нескольких полей, даже разнотипных и клиент может через + размножать эти блоки
		        По умолчанию для мультиТВ создаются поля key и value по шаблону default
			Можно создать свои щаблоны в папке assets/tvs/multitv/configs/ называя их по имени ТВ переменной

			Правило: если используется свое поле multitv, то нужен шаблон, совпадающий по имени с переменной.

			Такие переменные хранятся в БД в массиве обернутом в json, извлекать нужно в контроллере и отдавать в блейд:
				$this->data['urgency'] = json_decode($this->evo->documentObject['urgency'][1], true)['fieldValue'];

			Миниатюры картинок 'thumb' нужно ставить перед самими картинками в config, тогда показываются без искажений

			Создание поля multitv в переменной delivery из clientsettings:
			   1) Создаем файл настроек переменной: assets/modules/clientsettings/config/delivery.php
			   2) В нем: 'type' => 'custom_tv:multitv',
			   3) Создаем файл assets/tvs/multitv/configs/delivery.config.inc.php  // название совпадает с переменной clientsettings

			Внутри multitv можно устанавливать только простые поля, нельзя установить поле типа selector 

			Для создания тв параметров пользователям:
			   1) Инструменты -> Создать новую роль
			   2) При создании тв-параметра во вкладке "роли" назначить параметр роли
			   3) На странице пользователя назначить ему эту роль
			   4) dd(UserManager::getValues(['id' => 1])) - получить тв параметры пользователя с id=1

		        Функция извлечения мультитв из json:
			  public function getMultiTv($value) {
    				$value = json_decode($value, true) ?? [];
    				return $value['fieldValue'] ?? [];}

	6) Роуты - такие же как в Ларе. Направляют запросы в контроллеры 
		   Route::get('/send', [MainController::class, 'send']);

7) Ссылки сайта, их пути, id кешируются в evo (настройка "Кэширование карты ссылок") и затем можно пользоваться без обращения к БД:
	urlProcessor::makeUrl(5) - адрес страницы (псевдоним) по ее id, работает в контроллере и блейде
	{{ urlProcessor::makeUrl(5) }} - можно передать переменную

	@makeUrl(5) - адрес страницы (псевдоним) по ее id:
		<a href="@makeUrl($documentObject['id']))">ссылка</a> // переменная в шторме видна как строка, но @makeUrl ее правильно обрабатывает

	Проверка текущей страницы для установки активного класса в меню:
	$documentObject['id'] === $id // сравение с id из массива ссылок меню
	$documentObject['alias'] ===  request()->path() - по алиасу, не знаю правильно ли так

7) Даты: {{ date('d.m.Y H:i', $new['createdon']) }} => 24.01.2024 12:53

8) В шаблоны сущностей вставляем в нужные места переменные страниц-ресурсов.
	В шаблонах доступны переменные: 
		$modx - контейнер приложения 
		$documentObject - переменные страницы

	<h1>{{ $documentObject['pagetitle'] }}</h1>
	<img src="{{ $documentObject['post_photo'] }}" alt="">
	<div>{{ $documentObject['introtext'] }}</div>
	<section>{!! $documentObject['content'] !!}</section> - отключаем экранирование, так как в content будут теги из TinyMC
   Названия переменных берем в исходном коде инпутов админ-панели, в tracy->view->documentObject, также это ТВшки и пр.

   {{ $modx->getConfig('site_name') }} - способ вывода переменных из настроек, например, пользовательских: custom/config/settings.php 
      $modx->getDocument(9) - получить документ по id

9) Контроллеры - если имя совпадает с именем шаблона, то в контроллере запускается метод public function __construct(){}
		 Если расширяется стандартный базовый контроллер, тогда работу обычно выполняет метод render
		 То есть если вызывается адрес существующей страницы (адрес берется по ее псевдониму), то в контроллере автоматически отработает
		 метод __construct(), в котором можно отдать в нее переменные
		 Если страниц под такой адрес нет, то работает как в Ларавель, запрос идет в маршруты, оттуда в любой метод контроллера, он отдает
                 данные или даже свое представление: return view('page'). Это полезно при post запросах, также при отдаче страниц, 
		 которые не создаются в админке.
		 В любом контроллере доступен контейнер evo() или EvolutionCMS(), а также: 
			evo()->documentObject['pagetitle'] - переменные страницы (если контроллер отработал при рендеринге страницы)
			evo()->documentObject['id'] - id страницы
			evo()->getConfig('site_name') - переменные настроек
			evo()->documentObject['link'] - прикрепленные ТВ переменные
			Не путать с $modx - это то же самое, что evo(), но она доступна только в шаблонах
			Вообще контейнер EvolutionCMS() (то же evo()) записывается в базовом контроллере в переменную $this->evo 
		 Если мы расширяемся от базового контроллера, то авт. запускается его метод __construct, можно писать любую логику для всех
                 шаблонов с контроллерами
		 		
   Вариант с базовым контроллером (но прокидывать данные можно и без него: __construct(){ evo()->addDataToView(['a' => 111]) }
   1) Копируем базовый контроллер BaseController.php в core/custom/packages/main/src/Controllers
      с https://github.com/0test/evo-newdocs/tree/main/v3/ => Создание сайта => Контроллеры
      Если мы расширяемся от базового контроллера, то авт. запускается его метод __construct при отдаче любой страницы через дочерние контроллеры, 
      соответственно в нем можно писать логику для всех страниц дочерних контроллеров

   2) Cоздаем контроллеры по псевдонимам шаблонов:
	MainController.php:
		<?php  namespace EvolutionCMS\Main\Controllers;
		       class MainController extends BaseController { 
				public function render(){
 					 $this->data['qqq'] = 5; } } // во вьюхе появилась переменная $qqq, так и обращаться: {{ $qqq }}

9) Если есть повторяющиеся методы только для нескольких контроллеров, то вместо вставки в BaseController можно использовать трейты:
	trait CatalogTrait { public function getCatalog(){ $this->data['products'] = 5 } }

	use CatalogTrait;
	$this->getCatalog(); - в нужном контроллере используем методы трейта

10) Модели - то же , что в Ларавель
	     Свои модели создаются в core/custom/packages/main/src/Models/Blog.php 
             Их пространство имен: namespace EvolutionCMS\Main\Models;
	     Они расширяют класс Model
	     Префикс в модели  указывать не нужно: protected $table = 'example' для таблицы evo_example
	     Готовые модели существующих таблиц есть в core/src/Models, например модель User, SiteContent

	Для работы с документами используем модель SiteContent, она привязана к таблице site_content (документы сайта) 
	и содержит в себе дополнительные методы для работы с таблицей site_content (напр. withTVs), а также обычный Eloquent класс Model:

	class Blog extends SiteContent { 
		protected static function booted(){ ... } } внутри авт. логика выбора выбора страниц у которых parent или template с таким-то id
						            смотрим на https://github.com/0test/evo-newdocs/blob/main/v3/ => Создание сайта => Модели

	А в контроллере пользуемся этой моделью Blog:

	namespace EvolutionCMS\Main\Controllers;
	class BlogsController extends BaseController {
		public function render() {
        		$this->data['blogs'] = Blog::withTVs(['blog_mainphoto'])->active()->paginate(10);}} // даже пагинация как в Ларавель
	Теперь во вьюхах по шаблону blogs доступна переменная $blogs

	Если расширяли базовый контроллер:
	$this->evo->documentObject - массив документа в контроллере, напр.: $this->evo->documentObject['id'] 

	Если не расширяли, то обычным способом (работает в обоих случаях):
	evo()->documentObject['id']

	Вариант для собственной таблицы через обычную модель Eloquent:
	<?php namespace EvolutionCMS\Main\Models; 
	use Illuminate\Database\Eloquent\Model; - обычная модель

	class Blog extends Model { // здесь расширили обычную Eloquent модель
    		protected $table = 'blogs';} - название таблицы без префикса
	
	dd(Blog::all()); - в контроллере получаем записи
	
11) Как все работает:
	views/layouts/layout.blade.php - главная обертка, создается вручную
		@include('parts.header') - куски прочего кода
		@yield('content') - содержимое секций из шаблонов

	views/page.blade.php - шаблон сущности, создается в админке с галочкой "создать blade". Псевдоним - категория будущей страницы,
			       пишется в адресе, напр. pages: http://evolution.loc/pages/stranica	
		@extends('layouts.layout')
		@section('content')
		   <title>{{ $documentObject['metatitle']</title> - тв-шка
	   	   <h1>{{ $documentObject['pagetitle'] }}</h1> - принимает переменные из ресурса (т.е. страниц админки)
	 	   <a href="@makeUrl($documentObject['id']))"> - формирование ссылки по id документа
		@endsection

	Ресурсы - страницы, создаются в админке по шаблону. Их заполняет клиент. Псевдоним - адрес страницы
                  Через "дочерний ресурс" можно создавать вложенные ресурсы: Каталог => Телефоны (раздел каталога) => Нокия (товар)
                  Вложенные ресурсы имеют путь соответствующий вложенности: 
                  	https://evo.loc/katalog/phones/nokia

	ТВшки - дополнительные поля на странице редактирования ресурса. Назначаются для родительского шаблона ресурсов.
		Через них можно вставлять содержимое seo тегов, крепить картинки к страницам и пр.
		Сложные ТВшки с дополнительными полями или множественным добавлением через + создаются через multiTV или PageBuilder

		ТВшки на странице доступны как обычные переменные ресурса: $documentObject['links']
		В контроллере они доступны здесь: evo()->documentObject['links']

		Если же получаем в Модели, то наследуем ее от SiteContent и применяем метод withTVs: 
			$pages = Page::withTVs(['metakeywords'])->get() // так как ТВшки хранятся в другой таблице

			$page = Page::withTVs(['metakeywords'])->find(1) // пример с одной записью
			$page->metakeywords // твшка доступна как обычное свойство

		ТВ параметры умеют работать с @ командами, например автоматически заполнять значения по умолчанию или возможные значения для 
		списков, радио, чекбоксов:
			@EVAL - выполняет любой php код
			        Например сформируем возможные значения для списка: @EVAL return ['key1' => 1, 'key2' => 2] // массив можно формировать запросами к БД @SELECT
			@SELECT menutitle, id FROM [+PREFIX+]site_content WHERE parent = 0 ORDER BY menuindex ASC
		В поле типа "текст" и пр. текстовых в значении по умолчанию не срабатывало, работало только в списках, радио и чекбоксах	
	
	Контроллеры - создаются по названию шаблона (post.blade.php => PostController.php), запускаются авт. при формировании 
                      страницы по шаблону. В контроллерах обычно помещают переменные в контейнер приложения.
	
	Модели - называются так же как в Ларавель по названию таблицы. Есть специальная модель SiteContent для работы с документами
                 сайта, а также другие встроенные модели для таблиц сайта

	ClientSettings - модуль, позволяет клиенту редактировать значения переменных из верстки. 
		         Например, создали через него переменную header_codes, вставили в верстку: {!! $modx->getConfig('client_header_codes') !!}
			 Теперь клиент через админку может править значения из шаблонов. Так понял аналог ТВшек

	Чанки - переиспользуемые части кода html. Нельзя встраивать переменные php админки      
		1) Создаем в админке чанк "chunk" с html кодом. Можно называть по-русски: "Мой чанк"
		2) Используем в блейде: {!! $modx->getChunk('chunk') !!} 
		3) Либо получаем в контроллере: evo()->getChunk('chunk')

	Сниппеты - это PHP-скрипты, обычно возвращают вывод в браузер (напр. меню). Можно встраивать переменные php админки
		   Создаются в админке, как php код: 
		   <?php 
			$tel = $modx->getConfig('client_tel');
			return "Наш телефон: $tel";

		   {!! $modx->runSnippet("qwerty") !!} - получить в блейде
		   {{$modx->runSnippet("qwerty")}} - получить в блейде
		   evo()->runSnippet("qwerty") - в контроллере

                   evo()->runSnippet('phpthumb', ['input' => $image, 'options' => '', 'webp' => 1])
		   или {{ Helper::webp($image) }}

	Настройки - параметры системы
		    {{ $modx->getConfig('site_name') }} - вызов в блейде
		    evo()->getConfig('site_name') - вызов в контроллере

	Роуты - такие же как в Ларе.
		Это создание своих роутов для пакета main. Лучше, думаю, пока пользоваться роутами core/custom/routes.php,
		потому что для них можно просто включить посредники в core/custom/config/middleware.php
		1) Копируем файл core/custom/routes.php.example в core/custom/packages/main/routes.php
		2) В core/custom/packages/main/src/MainServiceProvider.php:
			public function register(){
        			Route::group(['middleware' => 'reviews'], function(){
            			$this->loadRoutesFrom(__DIR__ . '/../routes.php');});}
		3) Теперь можем писать свои роуты на любые запросы/пути

	Модули - дополнительные возможности в админку, можно писать самому. Например: пришедшие заказы, показ и редактирование отзывов

	Плагины - код привязываемый к системному событию, выполнящийся при его испускании
		  Например: при отдаче документа пользователю (событие OnWebPagePrerender) заменить мат на звездочки

	Помощники:
		1) phpThumb - метод работы с картинками
			Примеры: 
				HelperProcessor::phpThumb($this->tvname,'w=300,zc=1');
				<img src="{{ evo()->runSnippet('phpthumb', [
					'input' => $documentObject['item_photo'],
					'options' => 'w=600,h=700,zc=1']) }}" />

		2) UserManager - класс работы с пользователями, содержит доп. методы, в отличие от просто модели User
			Примеры: UserManager::get($id) - получить объект пользователя
			         UserManager::create(...) - регистрация пользователя
				 UserManager::delete(...) - удаление
				 UserManager::login(...) - авторизация
			         UserManager::logout(...) - выход
				 UserManager::getValues(...) - получение ТВ пользователя и т.д.
		3) DocumentManager - класс работы с документами, содержит доп. методы в отличие от модели SiteContent
			Примеры: DocumentManager::get($id)->toArray() - получить документ (здесь сразу перегнали модель в массив атрибутов)
				 DocumentManager::create(...) - создать
				 DocumentManager::delete(...) - удалить
				 DocumentManager::peblish(...) - опубликовать и т.д.

111) В ево при вставке класса через class="@if() @endif" добавляются лишние пробелы, чтобы было без них:
	class="menu__link{{ $documentObject['id'] === $id ? ' active' : null }}{{ $id === 9 ? ' red': null }}"

111) Написание модуля:
	1) Качаем как zip из github.com/mnoskov/evocms-module-example
	2) в папке core:  php artisan package:create cases(название) // создалась папка core/custom/packages/cases
	3) Удаляем из cases папки чанков, модулей и сниппетов, файл readme
	3) В этой папке в src в CasesServiceProvider:
		- protected $namespace = 'cases';
		- пишем название модуля 'Оснастки' в registerRoutingModule()
		- заменяем методы register() и boot() теми же методами из пакета evocms-module-example/src из ExampleServiceProvider.php
	        - удаляем из register() метод $this->publishes()
	4) Копируем из mnoskov/src/Controllers ExampleController в cases/src/Controllers и переименовываем в  CaseController
	4) Копируем папку mnoskov/src/Services в cases/src, изменяем название на CaseItems.php и внутри этих двух файлов изменяем 
	   Example на Cases, прописываем правильную модель бд из main/src/models (создать ее)
	5) Копируем из mnoskov папки views и lang в корень cases (помещены рядом с src внутри cases)
	6) Из boot() удаляем $this->loadMigrationsFrom
	7) Копируем также из mnoskov routes в корень cases, вставляем где надо CaseController и cases
	8) composer update
	9) В админке: Очистить кеш
	10) В модулях появился модуль "Оснастки", вылетает ошибка контроллера
	11) В Services в обоих файлах заменяем на нужную модель и др.
	11) Также в CaseController вместо Example и example вставляем Cases, case и др. (в соответствии с файлом routes)
	12) в папке views заменяем использование example на cases
	13) Вставляем валидацию полей в нашу Модель, так как она используется в методе save контроллера:
		public $rules = ['name' => ['required', 'string', 'max:255'],];

111) Написание модуля простой способ:
	1) cd core
	   php artisan package:create cases(название)
	2) удаляем все внутри, вставляем папку cases из Полезное/модуль ево
	3) Переименовываем все. 
	   Внимание! Если шторм предложит сделать refactor при переименовании, то оказываемся!! 
	   Удаляем галочки, если предложит искать зависимости, иначе переименует лишнее.	
        3) В файле composer.json меняем 4 надписи, 
           иначе модуль после composer update не появится
	3) в пакете main должна существовать модель таблицы
	3) composer update (либо composer dump-autoload)

111) Создание собственных стилей template/css/admin.css для админки вызывает много ебатни, проще подключать к каждому модулю в layout 
	А) На время разработки подключаем стили напрямую в manager/views/partials/header.blade.php
	Б) Чтобы эти стили не слетели во время обновления сайта у клиента:
	    1) Копируем папку manager/media/style/default в manager/media/style/my_custom
	    2) В core/custom/.env создаю:
		MODX_MANAGER_THEME=my_custom
	    3) В core/custom/config/cms/settings.php создаю:
		 'manager_theme' => env('MODX_MANAGER_THEME') // это жесткая установка без возможности изменения в админке (в Интерфейс и представление)
	    4) В файле manager/media/style/my_custom/CSSMinify.php по-прежнему в минифицированный style запрашиваются стили из
               default, пусть. Добавляем туда строку:
		  MODX_BASE_PATH . 'template/css/admin.css', // путь к админским стилям на сервере по папкам 
	    5) Удаляем manager/media/style/my_custom/css/styles.min.css  // так как файл CSSMinify.php запускается только если удалена styles.min.css

111) Также стили можно подключать в каждом модуле

111) Сервисные методы в Ево и Ларавель можно встраивать в контроллер без метода __construct в сервисном классе:
	В контроллере:
	publuc function index(ServiceClass $obj){ - автоматически создастся объект класса ServiceClass у которого можно использовать методы
		return $obj->myFunc()}

	В ServiceClass:
	public function myFunc(){ - то есть мы не прописали __construct, а пользовать функцией можно инъецировав объект этого класса
		return 111; } 
     ЧатГПТ сказал, что это только для Ларавель и она позволяет создавать объекты классов без явного вызова конструктора.
     В php языке конструктор обязателен.

111) В ево есть парсер маршрутов от Лары (работают с routes.php) и парсер от Ево (работает с псевдонимами страниц в админке).
     При http запросе url сначала запускается парсер Лары и ищет маршрут в файлах routes.php, если не найдет, то запускается
     парсер Эво.

     Также есть косяк: фасад и помощник сессии Session - от Ларавель и его флеш переменные не очищаются на странице отданной эвопарсером.
     Ни в контроллере страницы принудительно, ни перезагрузкой страницы.
     То есть записали флеш (return redirect()->back()->with('error', 'ошибка')) и она будет всегда,
     пока пользователь ходит по страницам созданным Эвопарсером (все страницы с псевдонимами в админке).

111) Поэтому все, что связано с флеш переменными ($error, $old, $message) в ЭВО не используем, так как они не удаляются при 
     перезагрузке. 
     Можно использовать на url из routes.php

     Флеш переменные для ево делаем на обычном $_SESSION['success'] = true
				               unset($_SESSION['success']) - удаляем флеш

111) В Tracy показывается сессия $_SESSION от php интерпретатора. 
     session() в Ево не имеет к этой сессии никакого отношения, он хранит свои данные в соответствии с настройками core/config/session.php,
     например в файлах  core/storage/sessions

     Соответственно и @error, @old и пр. не показываются в tracy

     Сам ево по умолчанию использует сессию $_SESSION, в Tracy можно увидеть переменные оттуда: _token, modx.session.created.time,
     mgrLastlogin и пр.

111) В Ево нет ConvertEmptyStringsToNull и TrimStrings.
     Их можно встроить в промежуточный класс, который отнаследовать от Model, а все модели затем наследовать от 
     этого промежуточного класса.
     Либо просто встроить в нужную модель:
	public function setAttribute($key, $value) { // будет работать для всех атрибутов
    		if ($value === '') { $value = null; }
 		if (is_string($value)) { $value = trim($value); }
		parent::setAttribute($key, $value);}

111) В ево csrf токен при фетч запросе нужно передавать в body, а не устанавливать в headers как 'X-CSRF-TOKEN':
	body: JSON.stringify({inn: number[0], _token: csrf_token})

111) Готовый код ajax отправки с Ево:
	<form id="form">
	    @csrf
	    <input type="text" name="name">
            <button type="submit" id="btn">Отправить</button>
	</form>

        let form = document.querySelector('#form')
	let btn = document.querySelector('#btn')

	form.addEventListener('submit', function (e) {
            e.preventDefault()
            btn.disabled = true

	    fetch("/form", {
        	method: 'POST',
        	headers: {
            	  'Accept': 'application/json',
                  'Content-Type': 'application/json'
        	},
        	body: JSON.stringify(Object.fromEntries(new FormData(form))) }) - просто форму в Ево нельзя отправлять, нужно вытащить наружу _token 
            .then(res=>res.json())                                                способ не подойдет, если нужно отправлять файлы          
            .then(res=>console.log(res))
            .catch(e=>console.log(e))}})

        
	Route::post('/form', [MainController::class, 'sendForm']);

        class MainController {
	    public function sendForm(Request $request){ return $request->all(); }}

111) Логирование в Ево:
	Log::info('сообщение об ошибке'); - сохраняется в public_html/core/storage/logs
        evo()->logEvent(124, 1, 'Текст сообщения!', 'Заголовок сообщения'); - сохраняется в  БД в evo_event_log

111) Бэкап:
	1) Инструменты -> Резервное копирование -> Выделить все таблицы -> Создать и загрузить ...
	2) Копируем всю папку сайта в отдельную папку
	3) в папке core: php artisan make:site update
	4) composer dump-autoload
	5) Также можно сохранять таблицы в папке сайта: Резервное копирование -> Резервное копирование -> Дать название и сохранить
	6) Затрется core/composer.json, свои пакеты устанавливать в custom/composer.json, далее на уровне core composer update

111) Перенос сайта на хостинг (лучше через гитхаб выкладывать):
	1) php artisan cache:clear-full
	2) очищаем таблицы в phpmyadmin от тестовых данных
	3) Пакуем сайт в zip, распаковываем в public_html
	4) Дамп базы в phpmyadlmin, создаем БД на хостинге, записываем имя и пароль (имя можно другое), импорт туда дампа
	5) В разделе Сайты на хостинге устанавливаем для сайта:
		 php 8.1 (применится черех неск. минут)
		 Редирект с HTTP на HTTPS
	6) На хостинге в core/custom меняем env:
		DB_USERNAME=имя
		DB_PASSWORD=пароль
		DB_DATABASE=совпадает с именем
		MODX_BASE_PATH=/home/c/cinedr/111222333.ru/public_html/   //пути можно узнать в ssh командой pwd
   		MODX_SITE_URL=https://111222333.ru/
		MODX_SITE_HOSTNAMES=111222333.ru
		MGR_DIR=manager
		MODX_MANAGER_PATH=/home/c/cinedr/111222333.ru/public_html/manager/
		MODX_MANAGER_URL=https://111222333.ru/manager/
	7) Проверяем, сайт открывается

111) Таблица evo mysql с хостинга экспортируется на локальный phpmyadmin без трудностей, все сразу зарабаотало
     Удалил все внутренние таблицы с локальной бд и вставил в эту бд выгрузку с хостинга

111) Невидимая капча recaptcha и запрос баллов по нажатию на отправку формы:
	0) Код есть в Полезное -> 
	1) Получаем публичный и секретный ключи: https://www.google.com/recaptcha/admin/create
           Русские домены на всякий случай кракозябрами
	   Ключи в env: 
		RECAPTCHA_SECRET=6LdP...
		RECAPTCHA_PUBLIC=6LdP...
           В core/custom/config/cms/settings.php: 
		 'recaptcha_secret' => env('RECAPTCHA_SECRET')
		 'recaptcha_public' => env('RECAPTCHA_PUBLIC'),
	   В контроллере используем: evo()->getConfig('recaptcha_secret')
	2) Вставляем на сайт код:
	   <script src="https://www.google.com/recaptcha/api.js?render={{ $modx->getConfig('recaptcha_public') }}"></script>
	3) Здесь логику менял, нужно балл обрабатывать на сервере, а не в файле js
	   Оценка отправляется на php файл
	4) Статистика капчи - https://console.cloud.google.com/security/recaptcha

111) Скрыть значок капчи:
	.grecaptcha-badge {
    	    visibility: hidden;
	}

111) Капча через формлистер: 
	https://github.com/0test/lessons-evolution-company/blob/main/ => 18 урок

111) [~1~] - создание ссылки на документ с id=1 в полях админки Evo (в базу вставится готовая ссылка)

111) @evoParser($documentObject['content']) - команда в blade, если в поле content типа richtext админки 
                                              использовали [~1~] и подобное

111) Текст из блоков PageBuilder хранится в отдельной таблице, для того чтобы он индексировался evoSearch
     нужно его настроить (есть в интернет)
     	
111) PageBuilder в Evo
	1) Устанавливаем
	2) Создаем ТВ-параметр pbcontent, тип ввода - pagebuilder, сущест. категория - content,
           заголовок - Построитель страницы, привязываем к шаблону
	3) Создаем файл настроек assets/plugins/pagebuilder/config/container.pbcontent.php
		<?php
		   return [
    			'title' => 'Содержание',
    			'placement' => 'tv', // tab | content 
			// 'addType' => 'icons' // как показывать тип поля в админке ];
	4) В TemplateBulder ставим на шаблон наш параметр pbcontent
	5) В assets/plugins/pagebuilder/config создаем файлы, 
           например mytext.php, mycards.php и блок с неизменяемой формой myform.php:

mytext.php:
<?php
return [
    'title' => 'Текстовый блок',
    'container' => 'pbcontent', // ['pbcontent_1', 'pbcontent_2']
    'image' => 'assets/plugins/pagebuilder/images/text.jpg',

    'fields' => [
        'title' => [
            'caption' => 'Заголовок',
            'type' => 'text',
        ],
        'content' => [
            'caption' => 'Текст',
            'type'    => 'richtext',
            'theme'   => 'mini',
            'options' => [
                'height' => '200px',
            ],
        ],
        'padding' => [
            'caption' => 'Отступы',
            'type'    => 'dropdown',
            'elements' => [
                's-pad' => 'Сверху и снизу',
                's-padtop' => 'Сверху',
                's-padbot' => 'Cнизу',
            ],
            'default' => 's-pad',
        ]
    ],
];



mycards.php:
<?php
return [
    'title' => 'Карточки',
    'container' => 'pbcontent', // ['pbcontent_1', 'pbcontent_2']
    'image' => 'assets/plugins/pagebuilder/images/text-photo.jpg',

    'fields' => [
        'title' => [
            'caption' => 'Заголовок',
            'type' => 'text',
        ],
        'cards' => [
            'type'    => 'group',   // тип поля для размножения элементов
            'caption' => 'Карточки',
            'fields'  => [
                'title' => [
                    'caption' => 'Заголовок',
                    'type' => 'text',
                ],
                'img' => [
                    'caption' => 'Изображение',
                    'type'    => 'image',
                ],
                'content' => [
                    'caption' => 'Содержание',
                    'type' => 'textarea',
                ]
            ]
        ],

        'padding' => [
            'caption' => 'Отступы',
            'type'    => 'dropdown',
            'elements' => [
                's-pad' => 'Сверху и снизу',
                's-padtop' => 'Сверху',
                's-padbot' => 'Cнизу',
            ],
            'default' => 's-pad',
        ]
    ],
];


		
myform.php (при его выборе будет "В этом блоке нет полей для заполнения" - это нормально)
<?php

return [
    'title' => 'Форма',
    'container' => 'pbcontent',
    'image' => 'assets/plugins/pagebuilder/images/button.jpg',
];

	6) Получение блоков:
	   -- В контроллере:
		$pagebuilder = evo()->runSnippet('PageBuilder', ['container' => 'pbcontent', 'renderTo' => 'array'])[0];
	   -- В верстке blade:	
 	@php
            $pagebuilder = evo()->runSnippet('PageBuilder', ['container' => 'pbcontent', 'renderTo' => 'array'])[0];
        @endphp
        @if(!empty($pagebuilder))
            @foreach($pagebuilder as $item)
                @if(View::exists('parts.pagebuilder' . $item['config']))
                    @include('parts.pagebuilder' . $item['config'], $item)
                @else
                    <p>Вьюха не существует</p>
                @endif
            @endforeach
        @endif
		

111) Все коды пишем всегда в env!!!!!!!!!
     Потому что контроллеры прямыми ключами отправятся в гит!!!
     Далее принимаем из .env в core/custom/config/cms/settings.php	
     И используем: $modx->getConfig('app_env')


111) Выкладка evo через гитхаб:
0) Папка 111222333.ru и папка public_html в проекте и на хостинге.
   Опенсервер настроен на 111222333.loc (так будем открывать локальный сайт на компе), ведет в папку public_html
   Имя домена и имя папки для openserver только на латинице, иначе ошибка

0) Вообще можно взять папку 111222333.ru и скопировать оттуда все в папку сайта

0) Это делать не нужно, если скопировали 111222333.ru
   Сразу вставляем vite.config.js и package.json, создаем папку template/build и др. (полезное/vite для evo)
   npm i

0) Заходим в БД 111222333.ru (cinedr_pechati), выгружаем ее на рабочий стол
   Создаем базу данных в openserver, например cinedr_pechat с кодировкой ut8mb4_general_ci, импортируем туда

0) Настраиваем локальный .env по пути public_html/core/custom/.env

0) Настраиваем файл core/config/database/connections/default.php (только ключ database)

0) Заходим на локальный сайт, он работает
    
1) Создал фтп + ssh доступ к 111222333.ru (не в public_html)
2) Через ssh:
	mkdir ~/.ssh
	ssh-keygen -t rsa -q -N '' -f ~/.ssh/id_rsa
3) Создал на компе папку проекта, поместил туда:
	.github (с Полезное/автоматическая выгрузка/улучшенная)
	 public_html с сайтом evo
	.gitignore (с .idea)
4) В public_html/core/custom/.gitignore:
	.env
4) Создаем gitignore для незашифрованных файлов js
5) VCS => Enable
6) Все выделяю, git => add
7) Git => Github => Share project
8) В ssh терминале на хостинге: git init
10) Для работы хостинга с гитом через ssh консоль (там есть и способ https://) копируем публичный ключ на гитхаб:
    репозиторий => settings => deploy keys => add deploy key =>
    в title пишем "key", в поле вставляем ключ pub с хостинга 

    На одном хостинге это не сработало. Грузилось только если репа была открытой.  Ключ нужно было вставлять в ssh ключи всего аккаунта,
    а из репозитория удалить:
    Аккаунт => Settings => SSH and GPG keys => SSH keys

10) Отключаем уведомления на почту: Settings => Email notifications

11) Для того, чтобы гитхаб мог сам заходить на хостинг:
    Репозиторий => Settings => Secrets and variables => Actions => NEW repositary
    HOST - cinedr.beget.tech  // сервер SSH
    LOGIN - логин от этого SSH
    PASSWORD - пароль SSH

111) В файле deploy.yml нужно, чтобы ssh шел в папку с .git
     Иначе перед git pull пишем cd public_html/ или куда надо

12) Продолжаем в ssh терминале:
    git remote add origin git@github.com:адрес_репозитория 
	была ошибка fatal: detected dubious, но все решилось предложенной гитом командой
	+ заново git remote add origin git@github.com:адрес_репозитория 
    git pull origin master
    git reset --hard

13) Вставляем .htaccess с запретом (копируем с любой папки где есть запрет) в:
    - папку .git на хостинге
    - папку .ssh на хостинге
    - папку github на компе

14) Удаляем папку .github и файл .gitignore с хостинга
    Вносим .github и .gitignore на хостинге в .git/info/exlude

15) Настраиваем Deployment в Шторме (чтобы не косячило возможно отключить ssh и перезагрузить шторм)
    Также Root path = Deployment path = Web path = /  (иначе при отправке файла не туда отправляется)
    
15) Настраиваем .env на хостинге

16) Создаем БД на хостинге, грузим туда файл базы

17) На хостинге нет некоторых файлов и папок, загрузим через фтп.

    Изменяем .gitignore корневой:
    Ставим вместо просто .htaccess => /.htaccess    // чтобы на сервер отправлялись все внутренние htaccess

    Отправляем на хостинг public_html/assets/images (если ошибки, то частями)
    Отправляем на хостинг public_html/assets/.thumbs (если ошибки, то частями)
    Вставляем правильный .htaccess в корень сайта на хостинге
    Вставляем правильный .env в core/custom на хостинге
    Вставляем на хостинг public_html/core/config/database/connections/default.php и меняем там database
    Вставляем с компа на хостинг файл core/install
    Создаем на хостинге папку core/storage/sessions 
    Переименовываем sample-robots в robots или если он уже есть готовый устанавливаем там правильный host

    Теперь все должно работать.

    Ошибки можно смотреть в серверном логе ошибок.

17) Отключаем плагин Updater:
	Элементы -> Плагины -> Updater -> Плагин отключен

17) Очишаем на хостинге таблицы:
	evo_files
	evo_orders
    Очищаем папки: 
	assets/.thumbs/files
	assets/files - здесь удаляем только папки с файлами
	 
17) Можно вручную делать:
	git pull origin master
	git reset --hard (если файлы на хостинге отличаюттся от файлов в гитхабе, например, если вручную грузили что-то на хостинг)

17) Если вручную изменяли отслеживаемые файлы на хостинге, то нужно делать из консоли:
	git reset --hard
    Иначе actions заканчивается с ошибкой

18) Также работает выгрузка по push со шторма на гитхаб,
    в actions запускается git pull origin master 

    Если установлен vite, то перед push нужно зайти в public_html и сделать npm run build
    Возможно собранные файлы изменили названия и понадобится их заново внести в git

19) Можно сделать вторую ветку develop:
	- вручную на гитхабе (Git => Fetch, Git => Branches => develop => Checkout)
	- или попробовать в шторме

20) На хостинге бегет команды артизан и композер:
    php8.1 artisan
    composer-php8.1 dump-autoload

21) Можно обфусцировать файл js в build

22) Получить ключи рукапчи в https://www.google.com/recaptcha/admin и изменить их:
	1) в .env - здесь секретный, в других местах обычный
	2) в файле js
	3) В строчке вставки скрипта в шапку:
		order-page.blade.php
		fast_order.blade.php
	4) npm run build и запушить

111) Отправка почты через smtp в Ево:
	1) Получаем пароль для внешних приложений в mail.ru
	2) Конфигурация->Почта:
	   Обратный адрес e-mail: rodnovspb@mail.ru
           Метод отправки писем: Через SMTP-сервер
           SMTP авторизация: Да
           SMTPAutoTLS: Да
 	   Защита SMTP: SSL
           Адрес SMTP-сервера: smtp.mail.ru
           SMTP порт: 465
           SMTP почта: rodnovspb@mail.ru
           SMTP пароль: пароль для внешних приложений
	3) html = view('email.order', compact('var'))->render(); // пишем верстку в blade в таблице
	4) Отправка в контроллере:
     	   $params = array();
           $params['subject'] = 'Пришла заявка';
           $params['body']    = html;
           $params['to']      = 'qwerty@qwerty.ru';
           evo()->sendmail($params);

111) Роут возвращает код sitemap:
	Route::get('/sitemap.xml', function () {
  		$sitemap = evo()->runSnippet('DLSitemap', [], 0, 'sitemap');
  		return response($sitemap, 200, [
    			'Content-Type' => 'application/xml']);});

     Также простое создание карты сайта:
	https://github.com/0test/lessons-evolution-company/blob/main/ => 20 урок

111) Коды метрик можно вставлять через ClientSettings.
     Код пишется в поле типа Textarea (mini)
     А а верстку вставляется через: {!! $modx->getConfig('client_header_codes') !!}


=================================== Как получить с хостинга измененные данные через Гит =====================================

0) У Deploy key при его создании на гитхабе установить галочку:
	Allow write access - то есть хостинг может отправлять изменения на гитхаб
   Если галочки не было, то перегенерировать и заново добавить ключ на гитхаб с этой галочкой:
	 ssh-keygen -t rsa -q -N '' -f ~/.ssh/id_rsa
1) Через ssh хостинга:
	git commit -a -m "Сообщение коммита"
	git push origin master

2) Через ssh локального сайта:
	git pull
	


============================================= Обновление evo ========================================================
Документация: https://github.com/0test/evo-newdocs/blob/main/v3/  05_Обучение и советы,  002 урок
0) Проще отключить плагин updater, чем пляски эти, которые ничем не закончатся

1) Заходим в папку core
2) php artisan make:site update
3) composer update
4) php  ../install/cli-install.php --typeInstall=2 --removeInstall=y
4) Если не помогает, то:
	удаляется папка vendor
	composer update --no-dev
	php  ../install/cli-install.php --typeInstall=2 --removeInstall=y
	И все равно нихуя не работает
4) Проверить удалилась ли папка install, иначе:
	git rm -f -r install
5) Если эта папка оказалась на хостинге, то удалить вручную и сделать:
	git reset --hard  - чтобы при actions не было ошибок



	
	
============================================= Семантика  ========================================================	

111) Перед созданием сайта первым этапом идет проработка семантического ядра - чтобы было понятно под какие фразы затачивать
     сайт, создавать страницы. Какие фразы пользователи вводят в поисковик - то есть какие фразы использовать в тексте страниц. 

111) Виды частотности в Яндекс Вордстат:
	1) Базовая - без операторов. Включает любые запросы содержащие слова в любой форме
	2) Фразовая - "" Включает только указанные слова в любой форме. То есть частота "сайт" = частоте "сайты"
	3) Точная - "!" Включает указанные слова в точной словоформе.
	4) Порядковая - []. Помогает определить порядок слов в ключевике "[!купить !сайт !москва]" - 2 показа в месяц

111) Важно! 
	В вордстате "купить сайт санкт-петербург" - 0 (ноль) показов !!!
	             купить сайт санкт петербург - 50 840 показов (то есть любые запросы содержащие эти три слова в любой форме)

111) В объеме всех запросов большую часть запросов составляют низкочастотные, поменьше СЧ и меньше всего ВЧ. 
	Пример: "лыжи" - 2848 показов в месяц (ВЧ)
	         лыжи - 280504 показов в месяц (СЧ + НЧ). То есть это любые сочетания со словом Лыжи

        ВЧ и СЧ запросы лучше всего прокачивать через страницы рубрик/категорий.
        Пример: "купить лыжи" - ВЧ, делаем ее страницей категории, а внутрь помещаем каталог лыж:
	"купить зимние лыжи", "купить осенние лыжи"

111) Потенциал запроса в SEO определяет его точная частота "!"
     Порядковая частота [] помогает определять порядок слов при написании контента под запрос
     Подавляющее большинство запросов в любом ядре приходится на НЧ запросы.

111) Поисковые запросы делятся по:
	- частотности (ВЧ, СЧ, НЧ, СНЧ)
	- целенаправленности (информационные +как +что и пр., с действием +купить, +смотреть и пр., навигационные "печати на Елькина")
	- геозависимости (смотрим выдачу, также есть функция в KeyKollector)
	- коммерческие-некоммерческие (смотрим выдачу)
	- сезонности (смотрим историю запросов в вордстате и Гугл Тренды, также есть функция в KeyKollector)
	- смысловому назначению (равные по смыслу, близкие по смыслу, разные по смыслу)
	- конкретике (неточные, точные, уточняющие, указание юр.лица, бренда и пр.)
	- количеству слов (однословные, 2, 3, многословные)
	- конкурентности (высококонкурентные, среднеконкурентные, низкоконкурентные)

111) Под коммерческие запросы создаем коммерческие страницы (цены, примеры и пр.)
     Под некоммерческие запросы пишем информационные тексты (можно полотна с инфографикой)
     Ориентируемся на выдачу яндекса. Как он понимает этот запрос и что ему нравится -  по тому что содержат лидеры выдачи.

111) Коммерческость запроса определяем по выдаче Яндекса.
     Часто в такие запросы входят слова: купить, заказать, цена, стоимость, скидка, акция, доставка, услуга и пр.
     Часто коммерческие запросы содержат названия регионов, городов, сел и пр.

111) Коммерческие запросы одновременно геозависимые, особенно для физических товаров - то есть в выдаче первыми будут местные сайты.

111) Равные по смыслу запросы должны распределяться в одинаковые кластеры и продвигаться на одинаковых страницах
     Близкие по смыслу должны применяться в тексте страниц
     Разные по смыслу должны продвигаться на разных страницах

111) Равные по смыслу: синонимы, транслитерация, кириллические аналоги (joomla = джумла), сокращения (Питер=Санкт-Петеррбург=СПб),
                       равнозначные фразы (купить пиццу = заказать пиццу), 
                       переформулировки (войти в админку Битрикс = попасть в панель управления Битрикс)
     Равные по смыслу определяем логически, по подсветке в выдаче, по пересечению запросов в выдаче

111) Близкие по смыслу очень часто встречаются вместе и используются для описания друг друга. Что должны применять для ключей:
	Для SEO: оптимизация, продвижение, Яндекс, Гугл, вордстат, title, частота и пр.
	Для Audi: автомобиль, двигатель, кузов, запчасти и пр.
	Для Сайт: создание, продвижение, конструктор, хостинг и пр.

111) Конкретика запроса:
	Неточные (купить смартфон). В выдаче каталоги, категории, списки товаров
	Точные (купить смартфон Huawei 730). В выдаче статьи, карточки товара, но не категории
	Уточняющие (купить смартфон Huawei 730 золотистый). Это более точные многословные запросы
	Витальные (joomla, huawei, ГазПром) - первые в выдаче официальные представители
     
111) Количество слов в запросе: чем меньше слов в запросе, тем выше частотность, но => тем сложнее его продвинуть
		                чем больше слов, тем проще продвинуть запрос

111) Конкурентность запросов (эффективность, сложность продвижения) - насколько сложно продвинуть запрос в ведущие позиции
		             Это: высококонкурентные, среднеконкурентные, низкоконкурентные
     На конкурентность влияют:
	- чем выше частотность, тем сложнее продвижение
	- количество документов в выдаче по запросу
	- наличие в выдаче сервисов поисковиков (Маркет, карты, youtube и пр.)
	- наличие в выдаче википедии, авито, дубльгиса и пр.
	- количество вхождений запроса в заголовки страниц из топа
	- количество главных страниц сайтов в топе
	- качество страниц сайтов из топа
	- seo факторы страниц из топа (возраст страницы и домена, обратные ссылки, семантический охват и пр.)
	- другие критерии

111) Конкурентность запроса определяем:
	1) Логически по перечисленным факторам - самый надежный способ
	2) По формулам KEI (keyword effectiveness index) - можно настроить в коллекторе

============================================= KeyKollector  ========================================================

111) Настройка коллектора, прогрев аккаунтов и пр.: https://youtu.be/Yh1T7RQqNMU?si=0gT5KpachLF8lrw3

111) Данные коллектора зависят от сезонности, так как он использует ворстат, а тот показывает запросы за прошлый месяц.
     Поэтому для сезонных запросов собираем запросы в сезон, либо дополнительно пользуемся готовыми базами (Букварикс)

111) Яндекс показывает максимум 2000 запросов по одной ключевой фразе.

111) Также годовая частотность Директа может прыгать от месяца к месяцу:.
     Пример изменения годовой статистики за 2 месяца: Соционика таблица, "Соционика таблица", "!Соционика !таблица": 
     Было: 10000, 4000, 4000
     Стало: 25000, 17000, 17000

111) Списки минус слов, а также списки тематических слов есть в подвале Букварикса
	
111) Базовую частоту запросов во много раз быстрее можно получать через Я-Директ -> Выбираем Сбор частот Яндекс Wordstat
     Также xmlriver платно позволяет снимать вордстат запросы

111) Парсинг через Я Вордстат позволяет увидеть только базовую частоту.
     Парсинг через Я-Директ -> Я-Вордстат позволяет дополнительно увидеть фразовую и точную частоту

111) ЯВордстат в нормальном режиме получает 50 фраз раз в 28 секунд (500 фраз за 280 секунд или 5 минут)
     ЯДирект раз в 13 секунд. Здесь больше всего капч
     SERP (result page, поисковая выдача) - один поисковый запрос раз в 10 секунд  

111) Способы убыстрить KeyKollector:
	Использование MOAB Tools для быстрого получения запросов из Я-Вордстат (5000 бесплатно), поисковых подсказок
	XML лимиты Яндекса - покупаем у Яндекса запросы на поисковую выдачу (SERP). 
        	Есть бесплатные на xml.yandex.ru/settings/ - сейчас 240 лимитов (но разделены по часам)
                Можно получить: геозависимость, вхождения запроса в заголовки топа, кол-во главных страниц в топе, 
                кол-во документов, позиции сайта и др.
                Лимиты можно покупать на xmlproxy.ru/limits - 20 руб. за 1000 запросов независимо от времени суток.
		Лимиты для гугла можно покупать на xmlriver.com
	Прокси-серверы (то есть другие ip): proxyline.net, proxy6.net, proxywhite.com, proxymania.ru и пр. Стоимость около 100 руб/мес за 1 ip
        Вообще, говорят, что работает и без них, даже если много аккаунтов Яндекса в программе.
                Требования к прокси и пояснения:
		   1) Протокол IPv4
		   2) Страна не важна (говорит сам выбирает Россию)
		   3) Сервер только в одни руки (не Shared)
		   4) Чем популярнее сервис, тем выше вероятность нахождения прокси в бане (говорит, что можно договориться о замене)
		   5) Чем дороже прокси, тем меньше вероятность нахождения его в бане
        Прокси вставляем в Коллекторе в Настройки -> Сеть
	На каждый прокси должен быть свой яндекс аккаунт. Прокси прописываем в поле аккаунта
	На каждый прокси можно добавить +1 поток к парсингу (делается во вкладке настройки Вордстат, Директ, SERP и т.д.)


111) Первый этап - поиск базисов. 
     
     Создаем группу Базисы и начинаем ее заполнять

     Базисы - или маркеры, синонимичные слова, как обычно называют товар, услугу, понятие. Нужны для поиска расширений (хвостов) 
              этих слов - целевых запросов пользователей.
              Могут быть многосложными, когда нельзя отбросить часть базиса не потеряв интент: "калькулятор бухгалтерских услуг"

     Базисы для сайта могут быть из разных тем, в зависимости от направлений деятельности. 
     Например: "бухгалтерские услуги", "налоговый учет", "сдать отчетность" и т.д.

	Правила:
	1) Каждый новый базис должен иметь уникальное написание (причем разные словоформы - не значит уникальность)
	2) Оставлять одну словоформу ("сайт", вместо "сайты", "сайтов") Остальные словоформы найдутся при парсинге
	   Однако "тапки" и 'тапочки' - разные базисы
	3) Каждый новый базис не должен полностью включать ранее найденный базис
	  'joomla' - базис, 'шаблон joomla' - не базис, найдется при парсинге слова 'joomla'
          'ортопедический магазин' - базис, "ортопедический интернет-магазин" - не базис, найдется при парсинге 'ортопедический магазин'
          Удобно проверять новые найденные слова по включению прошлого базиса через фильтр, пишем там изначальный базис, найдет включающие его выражения
	4) Базис не должен включать служебные части речи: союзы, предлоги, частицы и пр.
		"аксессуары авто" => при парсинге найдутся и "акс к авто" и "акс для авто"

     Тут есть еще этап перемножения базисов у Царевского. Насколько понял, перемножение нужно, чтобы не набирать много мусора по односложным базисам.

     Для поиска базисов используем:
	1) словарь синонимов
 	2) вордстат и его вторую колонку
	3) поисковую выдачу и анализ  сайтов конкурентов
	4) яндекс-картинки
	5) по каким запросам ищут сайты-конкурентов: 
		keys.so/ru/tools/keywords-by-pages и др.
		topvisor и отчет о сайте-конкуренте за 17 руб.
	6) рекламные объявления конкурентов    
	7) похожие поисковые запросы Яндекс-Гугл в коллекторе - в Яндексе расположены снизу страницы
           поисковые подсказки Яндекс-Гугл в коллекторе - в Яндексе всплывают при введении слов
	8) как называют это на форумах
	9) в яндекс картинках
	10) слова с ошибкой, устаревшие, жаргон, по статье из вики, на транслите и др.
            если слова с ошибками Яндекс в запросе меняет, то не использовать их в синонимах
	11) названия на английском, замена прилагательного существительным, аббревиатуры
	12) сервис дополняющих запросов: keys.so/ru/similarkeys
	    много фраз дает Букварикс: bukvarix.com (дает много мусора, парсить его выражения с добавлением минус слов)
	13) Собираем статистику по фразам на сайтах конкурентов (сервисы из интернет)
	14) Похожие ключи: 
	       keys.so/ru/similarkeys
	       arsenkin.ru/tools/sp/  
	       ru.megaindex.com/a/wordcluster

	Сбор фраз из Яндекс Вордстат -> Кнопка "Распределить по группам" -> Вставляем общий список базисов (обувь, кроссовки, тапки, сапоги)->
	-> Создались отдельные папки под каждый базис  

     Внимание! Если сайт существует, у него много разделов и ключевых слов, то правильнее работать с ним ПОЭТАПНО:
     взяли в работу раздел, проработали его семантику, структуру, разместили статьи, начали работать с другим разделом.
     Результаты будут бвстрее и клиент увидит их.

111) Второй этап - поиск поисковых запросов (вордстат и подсказки) и их частот (Я-Директ)
	Внимание! Разные настройки региона при сборе статистики Директ для информационных запросов и коммерческих запросов.
        Для коммерчких запросов НУЖНО указывать регион бизнеса/сайта.
        Для информационных запросов регион задавать не нужно, либо Россия.

	Можно в безбраузерном режиме до 360 потоков, пока не банят

        Также Вордстат показывает введенные запросы только за последний месяц !!!! 
        Он может быть несезонным, либо изначально собрали не все базисы.
        Поэтому важно включить запросы с сайтов-конкурентов.

        Платно можно снимать вордстат в xmlriver

	1) Нажимаем на шапку колонки базисов, копируем их в буфер обмена (один и тот же буфер будем вставлять несколько раз)
	2) а) Сбор фраз из Яндекс Вордстат
           Нажимаем красную кнопку Сбор фраз из Яндекс Вордстат, нажимаем распределить по группам, появляется окно, вставляем базисы
           В окошке групп создались группы на каждый базис
           Выбираем "Не проверять дубли фраз ..." (потом удалим через Явные и неявные дубли), Левая колонка, Регион если геозависимые запросы
	   Ручками находим минус-слова в вордстате и вставляем их, чтобы не парсились фразы их содержащие
           Также универсальные минус-слова есть в подвале Букварикса
           Нажимаем Начать

	   б) Сбор поисковых подсказок Яндекс
	   Яндекс вводит базис и начинает перебирать его добавляя в конце цифры, буквы (лат и кириллицу)
	   Внимание! Яндекс показывет разные подсказки для разных форм слов, напр.: "создание сайтов" и "создание сайта"
	   Нажимаем Поисковые подсказки Яндекс
           Нажимаем распределить по группам
	   Вставляем в окошко базисы
	   Не проверять дубли фраз, Регион если геозависимые запросы
	   Нажимаем Начать

	   в) Сбор частот
           Здесь важно какой ставим регион, так как коммерческие запросы нуждаются в установке региона, а информационные нет.
           То есть правильнее сначала определить геозависимость запросов, а затем собирать частотность по региону для коммерческих 
           и по России/Без региона для информационных.

           Здесь можно к запросам добавить то, что выдает Букварикс и удалить дубли.

	   Можно в безбраузерном режиме до 360 потоков, пока не банят
	   Выделяем в группах все группы кроме Базисы и Корзина, нажимаем Мультигруппа => получилась общая таблица
           Выделяем все фразы в этой таблице и нажимаем СБор статистики Я-Директ - нормально работает в браузерном режиме
	   В окне выбираем: Цель парсинга - Вордстат, Формат запросов прогноза "!слово", выделяем все три флажка
	   Начать

	   г) Сбор незаполненных частот
	   Получили частоты, упорядочиваем по частоте и возможно видим, что что-то осталось пустое
	   Выделяем только фразы с пустыми частотами, нажимаем Лупу - Сбор частот Вордстат, ставим три галки и запускаем
	   Насколько понял, частотность будет из вордстата, а значит только за месяц

	   д) Если в запросе какой-либо группы уперлись в 41 страницу (или примерно 2050 ключей). Это также видно потому, что частоты обрываются не доходя к 0 
	   Копируем все запросы группы, вставляем в Сбор фраз их Яндекс Вордстат
           Должно быть условие: Пропускать фразу, если есть в любой группе
	   Разгруппировывать не нужно, пусть вставляется в эту же группу
	   Начать
	3) Определяем ВЧ, СЧ, НЧ:
	   Упорядочиваем по колонке фразовой частоты ""
           Верхние строчки, примерно 5 процентов от всех строчек - ВЧ, записываем границы частот таких строк, например: от 10 и выше
	   Ниже идут СЧ, их примерно 15 процентов от всех строк, записываем их частоту, например: от 4 до 9
	   Ниже идут НЧ, таких строк больше всего, записываем частоту, например: от 1 до 3
	4) Выделяем цветами ВЧ, СЧ, НЧ: выделяем строки, в контекстном меню выбираем цвета

	Я-Директ показывает частоты как за месяц, так и за год. 
  	Я-Вордстат показывает частоты только за прошедший месяц

111) Третий этап - чистка запросов и формирование стоп-слов
     Смысл в том, чтобы удалять мусорные сочетания в папку Корзина, чтобы потом не парсить их заново.
     Пропуск таких фраз делается через флажок "Пропускать фразы, которые есть в любой другой группе"

     Можно просматривать список фраз и создавать минуса/чистить список:
	1) Нашли минус в сочетании, ставим галочку слева от фразы. Таким образом ставим галочки дальше
	2) Затем Правая кнопка => Отправить отмеченные фразы в минус-слова
	3) Выделяем в списке нужные слова (проверять внимательно)
	4) Показать найденные фразы
           Список не прокручивается, можно окно закрыть и после прокрутки заново "Отправить отмеченные фразы в минус-слова"
	5) Проверяем, что минус-слово не удаляет нужные фразы
	6) Если все хорошо нажимаем кнопку Добавить (минуса отправляются в список минус-слов)
	7) Либо снимаем галочку с ненужных минус-слов и нажимаем Добавить
	8) Закрываем окно, выделяем полученные фразы, Коп/перенести в корзину
	9) Закрыть режим предпросмотра

      Если добавили лишнее, то: Данные => Минус слова => В поиске ищем нужное, галочка => Удалить отмеченные
	
	 1) Ищем запросы с нулевой "" или "!", если запрос из базовой частоты не пригодится. 
            Можно через  фильтр в шапке колонки частоты, по условию = 0
	    Выделяем -> Коп./перенсти -> Перенос отмеченных строк в Корзина
	 2) Формируем минус слова и удаляем содержащие их фразы:
	    Данные -> Анализ групп -> Ищем минуса и отправляем в минус-слова, Закрываем вкладку анализа
            На вкладке списка слов нажимаем Минус-слова -> Показать Найденные фразы -> Выделяем все и переносим в Корзину
	 3) Создаем группу Информационные запросы (под такие запросы можно сделать специальные разделы коммерческого сайта)
            Искать такие запросы можно по словам: Что, Где, Как и пр.
            Создаем в минус-словах папку "Информационные", грузим туда список таких слов, Показываем фразы и переносим их в группу Информационные
         4) Удаление явных и неявных дублей
	    Явные дубли - сочетания, которые есть в другой группе. Добавлялись, так как при парсинге отключили проверку наличия в других группах
	    Неявные дубли - для поисковика одинаковые сочетания (хоть разный порядок, разные словоформы, с вхождением служебных частей речи и пр)
	    Например, неявные дубли: "ремонт телефона", "ремонт телефонов", "телефон ремонт". Из них нужно оставить наиболее частотный запрос.
            При сборе запросов из вордстат яндекс не выдает неявные дубли, они появляются если получали запросы из других сервисов или собирали запросы
            не за 1 раз случайно повторяя ключевики.   
	    Нажимаем Данные -> Неявные дубли -> Умная отметка (под ней Анализ, там настраиваем чтобы отмечались все, кроме с наибольшей частотой)
            Изучаем, проверяем что помечено правильно, удаляем
	 5) Далее можно удалять ненужные запросы по регулярным выражениям вводя их в поиск (нажать режим рег выражения):
		\d+ - цифры
		[a-z]+ - с латиницей
		\w\s - слова из одной буквы
		^слово - начинающиеся на слово
		слово$ - оканчивающихся на слово
	6) Удаляем запросы-пустышки c большой разницей между базовой и кавычками, напр.: с базовой > 1000, в кавычках < 10
	10) Данные => Анализ групп => По отдельным словам => Ищем и выделяем слова, которые не подходят => Применить изменения (ставим все три галочки)
            => Из основной таблицы переносим выделенные фразы в корзину 
	11) Остальные просматриваем и удаляем вручную

111) Четвертый этап - сбор аналитики (гео и объявления из директа)
	Запускаем анализ геозависимости (xml покупка лимитов ночью через сервис) и получаем:
	- геозависимость (колонка ГЕО по Яндексу)
	- количество документов по фразе (не так важно) (колонка SERP по Яндексу)
	- количество содержащих фразу заголовков в топ
	- количество главных страниц в топ
	
   Иметь ввиду, что для отпределения ГЕО отправляется 2 запроса на 1 фразу (по разным городам)

   Геозависимые запросы - выдача формируется с учетом местонахождения пользователя, показываются местные сайты
   Если запрос геозависимый, то равно коммерческий и будут показываться местные сайты.
   То есть для геозависимых запросов разная выдача при запросе из разных регионов, 
   а у геонезависмых похожая выдача если запрашивают из разных регионов.

   Коммерческая выдача разная во всех регионах. Информационная примерно одинаковая. Поэтому через разность выдачи в городах можно
   определить коммерческость запроса.

   Геозависимость помогает разделять коммерческие (хотят купить-заказать что-то) и информационные (хотят почитать) запросы
	Коммерческие:
		- все геозависимые запросы (геозависимость от 50 процентов)
		- все с топонимами (сделать стрижку в москве - но для яндекса он ГЕОНЕЗАВИСИМЫЙ, так как запрашивающий может быть в минске)
                  геонезависимость запросов с топонимами: если в запросе указан город, то выдача будет одинаковой с любого города.
		- все содержащие фразы действия: купить, заказать, арендовать ... (но "создать" и пр. - информационные)
		- все содержащие фразы: "стоимость", "цена", "тарифы", 'недорого', "дорого", "дешево", "сколько", 
				        "стоит", "оплата", "скидка", 'деньги', 'под ключ', 'продукт', 'товар'
		- часто запросы с преобладанием главных страниц в выдаче (упорядочиваем по главной)
		- часто запросы с большим количеством title в выдаче
		- запросы с объявлениями директа
		- запросы с большим значением списываемой суммы за директ
	Информационные:
		- негеозависимые без топонима
		- содержащие предлоги вопросы: как, почему, зачем
		- содержащие слова "бесплатно"
		- содержащие слова самостоятельности: "самому", "сам", "с нуля", "пошагово", "инструкция", "онлайн", "гайд"

    Удобно сделать анализ групп и искать фразы содержащие города, глаголы действия, указание на цены
    Например в анализе групп выделили "заказать", нашлись: заказать выписку, заказать сайт, заказать бланк ->
    далее в результатах фильтруем содержащие "сайт"

    Сомневаешься? Забей в поисковик и посмотри коммерческие или инф. сайты показывает.
    Платная проверка на коммерческость запроса: arsenkin.ru/tools/commerce/, tools.pixelplus.ru/tools/complex-queries-analyser

    Также при парсинге геозависимости составляется база запросов для дальнейшего поиска пересечения заголовков 
    Анализ групп -> По поисквой выдаче => 1) пересечение заголовков 2) LSI ключи.

    Правило: нельзя на одну страницу привязывать коммерческие и информационные запросы, они размывают друг друга.
             Яндекс перед формированием выдачи по запросу относит запрос или к комм. или к инф. и поэтому нужно и страницы
             делать либо под инф., либо под коммерческий запрос, при этом смотря в выдаче информационные или коммерческие сайты
             показываются по запросу. Если в выдаче информационные сайты, то продвинуть коммерческую страницу НЕ ПОЛУЧИТСЯ, так 
	     яндекс будет отдавать предпочтение информационным сайтам.

    Информационные страницы невозможно вывести в топ по коммерческим запросам.

    Пример: "скорость интернета" - вся выдача в информационных сайтах, продвигать по нему коммерческую страницу не получится,
                                   нужно создать информационную страницу и заманивать оттуда в коммерческий раздел

    Вывод: смотрим информационный или коммерческий запрос и создаем аналогичную страницу. Можно даже две:
	"Купить телевизор" - коммерческая страница под коммерческий запрос
	"Как выбрать телевизор" - инф. страница под инф. запрос

    Сбор объявлений из директа:
	1) Кнопка Сбор объявлений из Я-Директа, ставим Россия (чтобы правильно учитывались запросы с топонимами)
	2) Добавляем колонку количество объявлений в Я-Директ

    К коммерческим яндекс относит страницы содержащие слова-действия, цены, доставку, имеющие в адресе: catalog, price, shop, product и пр.

111) Смотрим что показывает поиск при запросе ключевика. Если все страницы информационные, то значит пользователи по такому ключу искали
     информационные сайты и продвинуть свою коммерческую высоко НЕ ПОЛУЧИТСЯ. 
     Например: "планировка квартиры" - весь поиск состоит из информационных страниц и свою коммерческую по ключевику "планировка квартиры"
     мы в них не пропихнем. Нужно продвигать сочетание слов когда ищут именно услугу, например: "заказать планировку квартиры"
    
111) Одновременно с геозависимостью собирается кол-во док-в в выдаче, кол-во заголовков в топ и главных страниц и похоже сама выдача, так 
     как затем можно выполнять анализ групп по поисковой выдаче.
	Важность этих значений по убыванию:
	1) Как много страниц в выдаче имеют в заголовке запрос - самое важное
	2) Как много главных страниц по запросу (кроме одностраничников, они не конкуренты многостраничному)
	3) Наличие сервисов Яндекса в выдаче
	4) Количество документов (говорит НЕ ПРИДАВАТЬ ЗНАЧЕНИЕ)

     Формулы расчета KEI:
	0) Для понимания сложности продвижения запроса ориентируемся на:
	 	1) Бюджет по Яндексу или списываемую сумму за клик
		2) Количество объявлений по директу

	1) KEI популярности запроса 1:
		( "!" * 3 + Базовая ) / Базовая   // берем запросы с KEI > 1. На все что меньше время не тратим
	        ( Module_YandexWordstat.QuotePointFrequency * 3 + Module_YandexWordstat.BaseFrequency) / Module_YandexWordstat.BaseFrequency

	2) KEI популярности запроса 2:
		("!" * 3 * 100 / Базовая) + 100  // берем запросы с KEI > 150. На все что меньше время не тратим
		(Module_YandexWordstat.QuotePointFrequency* 3 * 100 / Module_YandexWordstat.BaseFrequency) + 100

	3) KEI для совпадения точной фразы и базовой (от 0 до 100%)
	   	Смысл в том, что если продвигаем точную, то зацепим много фраз с базы.
	   	((Module_YandexWordstat.BaseFrequency + Module_YandexWordstat.QuotePointFrequency) / (Module_YandexWordstat.BaseFrequency  + 0.01) - 1 )   * 100
	
	4) KEI конкуренции запроса (считать выдачу за год):
		((1 + (Главные * 2) + Заголовки страниц) * (Документы / "!")) / (1 + ("!"/12)) // Берем KEI с наименьшими значениями
		((1 + (Module_SERP_Yandex.MainPagesCount* 2) + Module_SERP_Yandex.TitlesWithKeywordFoundCount) * (Module_SERP_Yandex.DocsCount/ Module_YandexWordstat.QuotePointFrequency)) / (1 + (Module_YandexWordstat.QuotePointFrequency/12))

	5) KEI эффективность запросов (считать выдачу за год): - эту формулу следует комбинировать с вышестоящей, можно найти запросы с минимальной конкуренцией и с наибольшей эффективность для продвижения
		(( "!" / 12) + "!" ) / (2 + Главные + Заголовки страниц) // Берем KEI с максимальными значениями
		(( Module_YandexWordstat.QuotePointFrequency/ 12) + Module_YandexWordstat.QuotePointFrequency) / (2 + Module_SERP_Yandex.MainPagesCount+ Module_SERP_Yandex.TitlesWithKeywordFoundCount)

	6) KEI для показа заполненности выдачи главными страницами и заголовками страниц (от 0 до 100%)
		KEI = (Тайтл **3 + Главная **3) / 20 : // берем наименьшие
		(Module_SERP_Yandex.TitlesWithKeywordFoundCount * Module_SERP_Yandex.TitlesWithKeywordFoundCount * Module_SERP_Yandex.TitlesWithKeywordFoundCount  + Module_SERP_Yandex.MainPagesCount * Module_SERP_Yandex.MainPagesCount * Module_SERP_Yandex.MainPagesCount)/20

	7) KEI:
		30 * ((Главные)^3 + (Заголовки страниц)^3) + 20 * Заголовки страниц + 40 * Главные + 0.00001 * Документы + 0.3 * База + 0.5 * "!" // берем минимальные значения
		30 * ((Module_SERP_Yandex.MainPagesCount * Module_SERP_Yandex.MainPagesCount* Module_SERP_Yandex.MainPagesCount) + (Module_SERP_Yandex.TitlesWithKeywordFoundCount* Module_SERP_Yandex.TitlesWithKeywordFoundCount* Module_SERP_Yandex.TitlesWithKeywordFoundCount)) + 20 * Module_SERP_Yandex.TitlesWithKeywordFoundCount+ 40 * Module_SERP_Yandex.MainPagesCount+ 0.00001 * Module_SERP_Yandex.DocsCount+ 0.3 * Module_YandexWordstat.BaseFrequency+ 0.5 * Module_YandexWordstat.QuotePointFrequency
	
	8) KEI: // берем с минимальными значениями. Если значительная разница между базой и точной "!", то не применяем
		pow(Module_SERP_Yandex.MainPagesCount;3) * (0.4+0.00001)+pow(Module_SERP_Yandex.TitlesWithKeywordFoundCount;3) * (0.6+0.00001) + (pow(Module_YandexWordstat.BaseFrequency;2)/Module_YandexWordstat.QuotePointFrequency) / (pow(log(Module_YandexWordstat.BaseFrequency;Module_YandexWordstat.QuotePointFrequency+1);3)) / (100+0.01) + (Module_YandexWordstat.QuotePointFrequency/(50+0.01))
	
	9) KEI соотношения базы и точной частотности:
		База / "!" // значения больше 50 можно удалять
		Module_YandexWordstat.BaseFrequency/ (Module_YandexWordstat.QuotePointFrequency + 0.01)

	10) KEI для определения высокочастотных запросов с малым количеством документов:
		KEI = 1000 * (кол-во запросов в день**2) / кол-во документов 
		1000 * pow((Module_YandexWordstat.BaseFrequency / 360); 2) / Module_SERP_Yandex.DocsCount  // внутри годовую делим на 360 дней
		- До 10 – плохие запросы, с ними не стоит работать
		- От 10 до 100 – нормальные запросы
		- От 100 до 400 – отличные запросы, которые дадут большой эффект
		- От 400 – самые лучше запросы, которые нужно делать в первую очередь

	11) KEI = Главная^3*0,4+Заголовки страниц^3*0,6 + (База^2/"!")/((log(База; "!")+1)^3)/100 + "!"/50    // берем минимальные значения
		POW(Module_SERP_Yandex.MainPagesCount; 3) * 0.4 + POW(Module_SERP_Yandex.TitlesWithKeywordFoundCount; 3) * 0.6 + ((POW(Module_YandexWordstat.BaseFrequency;2)/Module_YandexWordstat.QuotePointFrequency) /POW(LOG(Module_YandexWordstat.BaseFrequency; Module_YandexWordstat.QuotePointFrequency)+1; 3))/100 + Module_YandexWordstat.QuotePointFrequency/50
	

	12) KEI конкуренции по заголовкам, главным и кол-ву документов (чем выше, тем выше конкуренция)
		( Module_SERP_Yandex.MainPagesCount * 6 ) + ( Module_SERP_Yandex.TitlesWithKeywordFoundCount) + ( Module_SERP_Yandex.DocsCount / 73000 )
		
        
111) Пятый этап - кластеризация

        Как понять какие запросы помещать на одну страницу, а для каких запросов создавать отдельные страницы?
        Вести запросы "Ремонт стекла", "Ремонт заднего стекла", "Ремонт дисплея" и "Ремонт экрана" на одну или отдельные страницы?
        В этом помогает автоматическая кластеризация по поисковой выдаче.
        Проверять на совместимость можно все запросы пачкой или отдельные фразы по инструкции ниже ("Сравнить выдачу").

	Кластеризация - разделение запросов на группы в зависимости от интента (пользовательской потребности).
		        Запросы разделяются на смысловые группы, каждая из которых должна продвигаться на отдельной странице.
			Также кластеризация помогает сформировать анкоры внутренних ссылок  и анкоры внешних ссылок для покупки
	                Внизу есть настройки для авт. кластеризации по поиск. выдаче.

	Например: 
	   Кластер: Купить телевизор - ВЧ запрос, делаем как рубрику для СЧ и НЧ запросов (купить телевизор LG, купить телевизор Samsung и пр.)
	   Его запросы: купить телевизор, телевизор цена, телевизор стоимость

	   Кластер: обучение в США для русских
	   Его запросы: обучение в США для русских, обучение в америке для русских, учеба в сша для россиян

     Если определили интент (создали кластер, поместили запросы на страницы) не так как считает поисковик, то начинается каннибализация:
     поисковик начинает по одному запросу менять страницы показа, наносится ущерб весу страницы.
 
     Смысл кластеризации в поиске одинаковой потребности пользователя. 
     Также нельзя объединять в один кластер коммерческие и инф запросы. Яндекс понимает, что пользователь ввел коммерческий запрос "Купить сайт",
     поэтому для такого запроса наполняем страницу только соответствующими коммерческими запросами из этого кластера: "цена сайта",
     "срок создания сайта" и пр. Если мешать комм. и инф., то страница будет считаться менее качественной, так как имеет ненужную информацию.

     Нельзя помещать одинаковые интенты в разные кластеры (будет каннибализация). Нужно нагружать только одну страницу одинаковой потребностью, 
     одинаковыми ключами. 
     Пример страниц, которые могут съедать веса друг-друга:
	1) "Список печатей" и "Каталог печатей и штампов"
	2) "Печать по оттиску", "Восстановление печати", "Дубликат печати"

     Правила кластеризации:
	1) 1 кластер = 1 интент (потребность) = 1 страница
	   Например: интенты: "купить хуавей 730" = "хуавей 730 цена" = 1 страница
	2) Кластеры можно вкладывать друг в друга по уточненности запроса:
	   Кластер 1 уровня: магазин цифровой техники
	   Кластеры 2 уровня: купить смартфон, купить телевизор (для него будут свои вложенные, которые здесь не укажу)
	   Кластеры 3 уровня: купить смартфон хуавей, купить смартфон самсунг ...
	   Кластер 4 уровня: купить смартфон хуавей 730, купить телефон хуавей 830 ...

     Сначала кластеризуем автоматически по выдаче. Как это работает:
	- Если по разным запросам в поиске показывается примерно одинаковый набор url, то скорее всего эти запросы связанные,
          следует помещать их в один кластер и создавать одну посадочную страницу. 
	- Следует иметь ввиду, что робот может ошибаться и в дальнейшем интент может быть распознан им более правильно, поэтому думаем головой

     Степень кластеризации 3 (В КК Сила группировки 3) - по запросам "шары заказать" и "шары купить" в выдаче пересекаются 3 одинаковых url разных сайтов

     В Коллекторе режим группировки:
	- пересечение (hard): фраза встречается во всех выборках
	- объединение (soft): фраза встречается от выборки к выборке

     Настройки для авт. кластеризации по поисковой выдаче:
	1) Выполнить анализ геозависимости или получение поисковой выдачи 
	2) Данные => Анализ групп, ввести настройки. Галочки только там, где указано, в других полях их нет.
	   - по поисковой выдаче v3 (для инфозапросов можно также пользоваться "по отдельным словам")
	   - галочка Следить за статусом отметки
	   - колонка "!", колонка "Я - Позиция"
	   - режим группировки:
		для коммерческих запросов:  - пересечение (то есть hard, встречаемость url в каждой выборке)
	        для инфо запросов: - объединение (то есть soft).
	   - сила группировки:
	        для коммерческих запросов:   4 
                для инфо запросов: 3
	   - мин. размер группы - 2
	   - галочка Вычислить лучшие фразы в группе
	   - галочка Записывать лучшую фразу в свой заголовок
	   - галочка Показывать статистику групп
	   - несгруппированные фразы - Поместить в единую группу
	   - ограничение - Топ 10
	3) Получили группировку по кластерам. 
	4) Теперь выбираем кластеры, которые нас устраивают:
	   - правим в них заголовок
	   - ставим галочки:
	     1) первая галочка означает, что выбрали все ключи в указанной группе (проверяем их, какие не нужны отключаем)
	     2) вторая галочка означает, что выбрали эту группу для создания структуры
	5) Выделяем в правой панели "Управления группами" папку где создадутся новые папки
	6) Сверху: Результаты => Создать структуру => Выполнить
	7) Теперь у нас создались отдельные папки с 4 раза пересекающимися в топ-10 ключевиками
        8) Запросы из "Несгруппированные" не были в достаточном количестве пересечений, их кластеризуем вручную

     Пересечение по поисковой выдаче умно, поместило в одну группу запросы!!!:
	entj соционика тип
	социотип джек
	командир соционика

     Также можно быстро проверять единичные запросы на совместимость двумя способами:
	Через "анализ групп":
	  1) Ставим галочки нужным запросам, фильтруем: Отметка фразы = Истина
	  2) Данные => Анализ групп => По поисковой выдаче V3 => Рассматривать только отфильтрованные фразы => Остальные настройки по инструкции
	  3) Видим, показывает их поисковик как совместимые запросы для одной страницы или нужно создавать отдельные страницы.

        Через "сравнить выдачу":
	  1) Выделяем два запроса
	  2) Во вкладке "Данные SERP" нажимаем "Сравнить выдачу"
	  3) Показывается цветной график пересечения url

     Также через Анализ групп можно посмотреть какие сайты чаще всего встречаются по выбранным запросам и уровень конкуренции:
	1) В папке с запросами Данные => Анализ групп:
	   - по сайтам-конкурентам
	   - галочка Следить за статусом
	   - колонка суммы - "!", колонка средних -  Я-Позиция
           - мин. процент охвата фраз - 3
	   - режим поиска - домен
	   - показывать статистику групп
	2) Выведется рейтинг сайтов по охвату запросов и сами запросы, по которым они в ТОП-10
	3) Смотрим сколько в рейтинге мелких сайтов, которые можно подвинуть.
           Если на первых местах много крупных площадок и агрегаторов, то бороться будет сложно.

     Далее кластеризуем вручную, создаем подкластеры и пр.
	
     Как удобно кластеризовать через Анализ групп вручную:
	Например корневая группа: Купить телефон. Будем помещать в нее подгруппы
	   Увидели группы: самсунг, ксяоми и пр, которые можно сделать подгруппами.
           Ставим галку слева + выделяем строку -> создать структуру -> Для выделенных групп создать группы внутри активной группы + 
           + переместить + удалить фразы и группы из результатов группировки -> Выполнить
           Теперь в корневой группе Купить телефон появились подгруппы Самсунг и Сяоми

     Если есть запрос: "ортопедическая обувь для девочек", то дополняем кластер или создаем отдельный:
     "ортопедическая обувь для мальчиков"

     Коммерческие запросы разделяем на отдельные кластеры, информационные - в один кластер по похожести:
	- коммерческая страница - один интент (купить пылесос)
	- информационная страница  - все связанные интенты (образование туч, что такое дождь, какие бывают тучи и пр)

     Чем больше интентов охватывает информационная статья, тем лучше будет ранжироваться (сюда же LSI ключи).
     Связанность информационных интентов определяем ПО ПЕРЕСЕЧЕНИЮ И ВЫДЕЛЕНИЮ ИХ В ПОИСКОВОЙ ВЫДАЧЕ,
     этот инструмент есть в KeyKollector: Анализ групп -> По поисковой выдаче v3

     Таким же методом пересечения можно искать LSI ключи для коммерческих запросов:
	Печати и штампы: Купить печать, Изготовление печатей и штампов, Штампы купить, Печати и штампы в Санкт-Петербурге

     Структуру папок удобно смотреть и перетаскивать их в Управление группами => Редактор структуры  

     Автоматическая кластеризация в России - KeyKollector, за рубежом - Rush Analitics 

111) Промежуточный этап анализа структуры сайта и показателей сайта у конкурентов  

     Таблица также понадобится для понимания роста показателей сайта через полгода работы.

     Все запросы => Данные => Анализ групп => По конкурентам (есть настройки) => Режим поиска: имя домена
     При изучении не берем во внимание сайты-гиганты, агрегаторы и пр.
     Получим список доменов-лидеров в области

     Основные цели изучения конкурентов:
	1) Понять почему люди пользуются этими сайтами и поисковик это замечает
	1) Стать лучшим сайтом в области (лучшая структура, лучшее УТП и пр., лучшие показатели)
	
     Ниже используется keysso, но у него бесплатно не более 10 отчетов

     Нужно выписать показатели лидеров области и создать рекомендации к чему стремиться (хотя бы к средним значениям):
	1) Вставляем конкурентов в Вебмастер => Качество => Показатели качества,
           выписываем их характеристики (отзывы, турбостраницы, ИКС, знаки, Бизнес)
	2) Возраст темы сайта по вебархиву
	3) Кол-во страниц в индексе у Яндекса и Гугла (site:site.ru - Результатов: примерно)
	4) Кол-во запросов в топ-10 (смотрим по Коллектору в получившейся таблице из Анализа групп)
	5) Показатели сайта по keys.so (страниц в выдаче, запросы в топ-1,3,5; входящие ссылки и пр.)
           Можно загружать несколько сайтов: keys.so => Сайты => Пакетный анализ
	6) Популярные страницы сайтов по keys.so (писать статьи под их запросы в первую очередь)
	7) Общие запросы для лидеров области: keys.so => Сайты => Сравнить сайты
	8) Объявления сайта в Директе по keys.so, какие ключи рекламируют
	9) Скорость загрузки сайта
	10) Мобильная версия
	11) Объем текста на страницах категорий и страницах статей/товаров/услуг
	12) Виды контактов (соц, мессенджеры, чат, 8800 и пр.)
	13) Кол-во обратных ссылок
	14) Сертификаты, прайсы, акции, вопросы-ответы, калькуляторы и пр.
	15) Другая полезная информацих о сайтах-конкурентах
 
111) Шестой этап - создание структуры сайта + максимально удобного меню (чтобы быстро находили и кликали)
                   на этом же этапе продумывается внутренняя перелинковка (меню, сайдбар, крошки. ссылки с текста)

       Сначала смотрим лидеров области по сайтам-конкурентам.
       Найти лучшую структуру сайта можно по сайтам наиболее обширно охватывающим ваши запросы:
	1) Данные => Анализ групп => По сайтам-конкурентам (другие настройки есть еще)
	2) Увидим рейтинг сайтов по охвату запросов и нахождению в ТОП-10 + сами запросы
	3) Смотрим мелкие сайты (не ютуб, авито и подобное) на высоких местах. Если у них такой охват, значит они за что-то нравятся Яндексу

       ВЧ запросы хорошо продвигаются как рубрики/категории для СЧ/НЧ запросов
       Однако не нужно писать портянки на страницах категорий/рубрик, яндекс может понизить.
       Лучше оптимизировать ВЧ запросы в категориях через ссылки на подкатегории, товары

       Для утверждения структуры сайта клиентами удобно пользоваться программой XMIND, XMIND Zen - красивее

       Для того в каком порядке на сайте (чем левее и выше, тем ценнее место сайта) показывать кластеры (страницы) 
       нужны общие частотности всех ключей в них входящих:
	1) Управление группами (Справа, под которой написано Вид) => Отображать вычисляемые значения
	2) Нажимаем в шапке колонки "!" на знак Е, выбираем Сумма, Сохранить
	3) Теперь у папок(страниц) появились значения сумм частотностей их фраз
	
        На основе этих сумм также можно вычислить примерную посещаемость и прибыльность страницы:
	   Например сумма частотностей 1000. Первая страница в выдаче получает около 50% CTR, 
           соответственно, если продвинем страницу на первое место ее будет посещать 500 чел/мес.
           Конверсия, например, у нас 5%, соответственно будем иметь 25 клиентов в месяц с этой страницы если выведем ее на 1 место.
	
	На коммерческих сайтах информационные статьи можно размещать в разделе Блог.

	Для коммерческих сайтов свойственны:
	- списки товаров и услуг (сюда назначаются общие товарные запросы "купить телефон")
	- карточки товаров и услуг (точные товарные запросы "купить телефон хонор 730")
	- прайс-листы
	- навигационные страницы (контакты, доставка, оплата, о компании ...)
	- служебные страницы (корзина, кабинет ...) 

	На главную страницу коммерческих сайтов направляем:
	- коммерческие общие запросы (изготовление печатей, товары для авто, создание сайтов)
	- витальные запросы (Мир печатей, печати на Елькина)
	- запросы с типом сайта (магазин печатей, салон красоты, веб студия)
	- посмотреть в колонке SERP (заголовки/главные) под какие запросы затачивают главные страницы другие сайты

	На главную страницу информационных сайтов направляем:
	- если сайт узконаправленный, то самый ВЧ запрос и его вариации (соционика, плоскостопие)
	- запросы с типом тематики (медицинский портал, сайт о психологии, справочник болезней)
	- посмотреть в колонке SERP (заголовки/главные) под какие запросы затачивают главные страницы другие сайты
	- прочее

	Для понимания какие страницы (списки, карточки и пр) нравятся Яндексу под запросы
	смотрим его поисковую выдачу.  

	Создаем папку Семантика, куда помещаем все кластеры.
	Создаем папку Сайт, в нее помещаем папки страниц (Главная ->, Телефоны->Купить Ксяоми, Телевизоры->...),
        а внутрь этих папок распределяем  кластеры с запросами.

	При создании структуры сайта предусмотреть наличие EAT факторов.

111) Промежуточный этап анализа целевых страниц и их показателей у конкурентов

     Запросы кластера => Данные => Анализ групп => По конкурентам (есть настройки) => Режим поиска: страница
     При изучении не берем во внимание сайты-гиганты, агрегаторы и пр.

     Основная идея - написать лучшую страницу по теме

     Можно отслеживать последние статьи по теме у конкурентов:
	date:ГГГГММДД - поиск страниц по дате:
		        соционика типирование date:20231107 - статья опубликована 7 ноября 2023 г.
			соционика типирование date:>20231107 - статья опубликована после 7 ноября 2023 г.
		        соционика типирование date:20231101..20231107 - опубликована между 1 и 7 ноябрем 2023 г.
		        соционика типирование date:202311* - опубликована в ноябре 2023 г.
		        соционика типирование date:2023* - опубликована в 2023 г.

111) Седьмой этап - написание текста страниц
        Самое важное - написать лучшую статью по теме наиболее полно отвечающую намерению пользователя.

	Для СЕО рекомендуют использовать "" и "!" запросы, для Директа базовую частотность

        Важное правило: не переспамить страницу, молодые сайты легко пессимизируются за переспам ключевика.
        На старых сайтах к переспаму поисковики относятся лояльнее, а молодую страницу легко пессимизируют.
        Признак переспама - нерелевантная страница или место за топ-100.
        Ориентируемся на среднее значение по лидерам.

	Объем текста и структуру страниц смотрим в выдаче по аналогичным запросам.
        Объем текста (особенно для инфостраниц) можно вычислять на основе среднего значения лидеров запроса используя advego
	Перед расчетом исключить сильно отличающиеся по объему сайты.
       
	Можно через keys.so посмотреть какие страницы конкурента собирают больше всего посетителей и первыми 
        писать статьи под такие ключевики.

        Дополнить страницу ключами можно на keys.so и topvisor.com
        Вводим туда ссылки на страницы из топа и видим по каким запросам на них заходят.

        Первый абзац должен содержать ключевое выражение в точной формулировке

        Абзацы в 4-6 предложений, чтобы были удобны для чтения. В интернете читают по диагнонали, длинные тексты не читают.

        При написании страниц сразу продумывать внутреннюю перелинковку на другие страницы.
        Важно! Ссылки перелинковки должны быть интересными, чтобы на них кликали, иначе бесполезны.

	При написании текста помним что такое SEO пассажи, когда нельзя разделять сочение ключевых слов опр. знаками препинания.
	Разделители пассажей: . ! ? ... <p>, <div>, <h1> - <h6> и пр.
	Аккуратно с ключами в одном пассаже, можно переспамить.

	Правило написания страниц: 
	- в title, h1, h2, h3 точные вхождения ключа и словоформы
	- саму статью ключами не спамить, а использовать больше LSI ключи

	Качество текста:
	1) полезность - решение интента
	2) уникальность  
		Для инф. сайтов - 90-100%, для коммерческих не так важна.
		Проверить: text.ru, content-watch.ru, advego.com
	3) объем.  
		Важен для информационных запросов. Объем без полезности, уникальности и пр. ничего не значит.
                Для инфостатей можно использовать 1500 знаков. Для интернет-магазинов 500-1000 знаков.
                Для рубрик интернет-магазинов - около 500 знаков. Причем это нужно больше для Гугла, яндексу достаточно
                широкого охвата товаров на странице рубрики.
		Водность - использование вводных слов и подобного (так или иначе, таким образом, в любом случае ...)
		Если убрать фразу и текст ничего не потеряет - убирать.
	4) структура и оформление.
		Структура, формат, разметка.
		Советы:
			- делить текст на подразделы, секции с заголовками (внутри заголовков ключевые выражения)
			- списки, таблицы для упрощения восприятия содержания
			- strong, i, b, em 
	5) изображения, видео, комментарии, даты публикации или редактирования и пр.
	6) соответствие текста запросу (релевантность), наличие ключевиков
		Распределяем ключевики по тексту без злоупотребления
	        Текст проверяем на водность, спамность и пр. в инструментах ниже.		
	7) LSI ключи (latent semantic indexing, скрытое семантическое индексирование)
		Вхождение в текст близких по смыслу фраз (SEO: СЯ, ключевые слова, поисковые запросы, Яндекс ...)
	8) Экспертность текста, авторитетность источников, использование специальной терминологии (есть ниже с курса П. Александрова)

	Советы по выбору запросов из кластера для помещения в title, description, h1, h2 и пр.:
	1) Упорядочиваем запросы по фразовой частоте " "
	2) Смотрим на кол-во title в заголовках Яндекс и Гугл: чем меньше их в выдаче, тем проще продвинуть запрос

       Примечания:
	1) Для коммерческих запросов:
	 	- уникальность текста не важна
		- важны характеристики товара (цена, отзывы, изображения и пр.)
	2) Чем больше семантический охват интернет-магазина, тем лучше для продвижения
	   То есть можно создавать отдельные страницы под товар с изменением в одном свойстве:
	   	- айфон серый (title, description, h1, h2 отличаются только словом "серый")
		- айфон зеленый ...

       Теги:
	- title (100-150 символов)
	  Очень важный фактор ранжирования и ПФ (кликабельность на поиске)
	  В title помещаем самый ВЧ запрос из кластера, желательно в начало. 
	  Не размывать заголовок словами не из кластера, вводными и пр.
	  Рекомендуется писать грамотные заголовки "Купить телевизор в Москве", а не "Купить телевизор Москва"
	  Уникальность title (хотя бы 1 словом отличаться от др. страниц)
	  Яндекс подсвечивает слова в заголовке соответствующие запросу, даже разные по написанию (карта сайта = sitemap)
          Главный ключ использовать не более 2 раз.
	
	- description (140-200 символов)
	  До 200 символов (1-3 предложения)
	  Из него чаще всего формируется аннотация сниппета, особенно в Google
	  Пишем в description хороший текст для живых пользователей
	  Д.б. уникальным среди других страниц
          Главный ключ в различных словоформах использовать 2-3 раза, не более.

	- keywords - пишут, что если писать не более 2-3 ключей, то будет учитываться

	- h1
	  Только один на странице 
	  Может частично или полностью дублировать title. Если ВЧ запросов два, то один помешаем в title, другой в h1 или совмещаем,
          используем в h2, h3
	  Уникальность (отличие хотя бы 1 словом) 
	  Не размывать словами не из кластера
	  Писать грамотно - название товара/услуги/статьи,  не нужно в них вставлять лишние ключи 
          портящие восприятие: "Купить телевизор LG-102 с доставкой по Москве" - лучше это будет в title, 
          а в h1 правильнее: "Телевизор LG-102"

	-h2, h3
	 Использовать как подзаголовки разделов
	 Использовать ключевые запросы в заголовках и LSI слова
	 Использовать как матрешку: внутри подразделов h2 другие подразделы h3 и т.д.
	 Ключевики в h2, h3 имеют больший вес, чем в тексте подразделов (но не злоупотреблять), помещаем запросы из кластера
         Рекомендуется высоту шрифта заголовков делать: h1 > h2 > h3 > h4

	-strong, em
	 Лучше не выделять то же, что уже есть в h1, h2, h3
	 Лучше выделять второстепенные ключевики, не чаще 1 раза, когда это действительно нужно
         Выделять текст важный для пользователя иногда захватывая ключевые слова.

	-alt
	По ним поисковик понимает что на картинке
	Писать что изображено на картинке, не спамить словами Купить, Заказать и пр.
        Можно косвенно вставлять ключевики: на странице "Ловля форели" есть картинки удочки. Сделать ей alt: "Удочка для ловли форели"

	-названия картинок
	Не спамить словами kupit, zakazat
	писать так: apple-iphone-11.jpg

 	Проверка текста на text.ru (также есть advego.com):
	1) Орфографические ошибки
	2) Заспамленность и вода с числами. Кликаем на число, выделяются слова. 
           При наведении на вопрос показываются допустимые значения заспамленности и воды.
	   Даже если значения выходят за допустимые Яндекс оценивает текст в совокупности. Если переспамлен, но
           пользователям удобен и читают, то норм.
           Используем в тексте вместо основных связанные LSI ключи.
	3) Частота ключевиков	


	Порядок написания текста для страницы:
	1) Определяем интент страницы, собираем его ключевые запросы
	2) Находим лидеров выдачи по этим запросам: arsenkin.ru/tools/check-top и keykollector
	3) Анализируем лидеров по p2pi.ru/site-analyze.html
	   Заполняем таблицы: 
		- ссылка, символов, вода, заспамленность, кл. тошнота, ак. тошнота
		- title, desc, keywords, h1, h2, словоформы ключей
		- текст каждой страницы с p2pi.ru копируем в общий файл блокнота
	4) Находим средние значения, на которые будем потом ориентироваться
	5) Текст с блокнота помещаем в advego.com/text/seo
	   Получаем семантическое ядро и LSI ключи
	   

	1) Порядок поиска LSI ключей:
	1) Для одной страницы по одному интенту собираем маркерные ключи, ищем лидеров выдачи:
		arsenkin.ru/tools/check-top	// выставляем регион, даже если сайт без геопривязки
	2) Находим список ссылок-лидеров под разноцветной таблицей (без агрегаторов)
	3) Норм. тема: 
	          keys.so/ru/similarkeys
	          arsenkin.ru/tools/sp/  
	          ru.megaindex.com/a/wordcluster 

        Задание для копирайтера автоматически можно создать здесь: arsenkin.ru/tools/copyrighters

        Тексты можно проверять на первоисточник: вводим пару предложений в кавычках в яндекс, на первом месте показывается
        тот сайт, который яндекс считает первоисточником

	Совет для увеличения посещаемости: сначала писать статьи под НЧ запросы, так как под них в индексе меньше заточенных
        страниц и легче продвинуться, затем СЧ, ВЧ. 
	Также изучать трафиковые страницы конкурентов в keys.so

	Некоторые данные можно делать динамическими,  например, чтобы год вставлялся php скриптом.

111) Восьмой этап - мониторинг позиций сайта

        Страницы после индексирования сразу попадают в топ-100

        Перые значимые результаты продвижения ждать не раньше 3-4 месяцев.
        Если сайт молодой, то дольше.
        Работать над сайтом нужно хотя бы 4-6 мес, иначе нет смысла продвигать.
        Продавать нужно свое время на работу с сайтом, а не позиции, они часто непредсказуемы.

        Отчеты о показателях сайта seo-reports.ru - платная служба

	Позиции 500 запросов можно отслеживать в Вебмастере:
	Вебмастер => Поисковые запросы => Статистика запросов (загружаем запросы в "Управление группами" и отслеживаем позиции)

	Внимание, сначала заполняем ГЕО (там считаются заголовки и морды в первой десятке)
	Если ГЕО не заполнено, то при сборе позиций оно тоже вычисляется, но уже в первой тридцатке (в соотв. с настройками сбора позиций)
	Либо собираем позиции в десятке, чтобы гео заполнилось по первой десятке.
	
	В настройках глубину исследуемых позиций ставим 30 (или 10)
	Парсинг->Анализ позиций -> Яндекс, Гугл - место и изменение позиции. Также появятся адреса страниц
	Парсинг->Анализ позиций->Показать панель доп. статистики - тут можно просматривать график движения позиций
	
	Парсинг -> Анализ релевантных страниц -> Яндекс, Гугл - адреса релевантных страниц по запросам
	Парсинг -> Анализ релевантных страниц -> Показать панель доп. статистики - список соотвествующих запросу страниц сайта по убыванию

	Позиции можно собирать и анализировать: topvisor.com/ru, allpositions.ru
        Вставляем в топвизоре url статьи, он покажет динамику позиций запроса.
	На topvisor.com можно узнать структуру сайта, его поисковые запросы, найти конкурентов, определить ключевики,
	определить схожесть сайтов по ключам, определить конкурентность, сравнить позиции с конкурнтом

	ru.megaindex.com/analyzeword - здесь можно посмотреть помесячный рейтинг сайтов из ТОП-50 по запросу, клевая штука
	keys.so/ru/tools/history-serp - то же самое платно
	topvisor.com - снимки поисковой выдачи по запросам, платно, пользуется П. Александров
	Вебмастер => Поисковые запросы => Статистика запросов (загружаем 500 запросов в "Управление группами" и отслеживаем позиции) 
	
	Если показывается НЕРЕЛЕВАНТНАЯ страница, то скорее всего:
	1) она пессимизирована за переспам ключевика (смотрим количество ключей у конкурентов)
	2) страница недооптимизирована
	2) происходит каннибализация из-за того, что другая страница имеет такие же ключи
           в этом случае происходит постоянноая смена релевантной страницы в выдаче 

        Решения каннибализации:
	1) 301 редирект с одной на другую
	2) убрать ключи с другой страницы          
	
111) Девятый этап - реклама в Директ
	Формат запроса прогноза ставим "слово"
	Минус слова не ставим, они нужны только для неточных форм слов
	Если перепроверяем в Яндексе, то ставим такие же настройки как в коллекторе: слова оборачиванием в кавычки, регион, период

	direct.yandex.ru/registered/main.pl?cmd=advancedForecast - панель прогноза бюджета в яндексе

	Парсинг коллектором Я-Директа выведет прогнозируемые значения:
	1) Показы (100) - количество показов объявления
	2) Переходы (18) - количество кликов на объявление
	3) CTR - кликабельность (Переходы / Показы или 18/100 => 18%)
	4) CPC - средняя ставка, для аукциона. Сколько готовы отдать за клик
	5) Сп. сумма (12) - примерная стоимость клика - самое важное для запуска рекламы
	6) Бюджет (сп. сумма * переходы или 12*18 => 216 руб.) 

	Рекомендации для Директа:
	0) Ищем подходящие для рекламы фразы по колонке "Списываемая за клик сумма"
	1) Использовать минус слова (уже сформировали их при создании СЯ)
	2) Использовать "" и "!", либо много минус слов на запросы без кавычек
	3) Под фразы из кластера (или под фразы с совпадающими ключами) можно создавать отдельные объявления: 
	   "купить фотоаппарат" => объявление "Купить фотоаппарат в СПб"
	   Это увеличивает кликабельность (CTR) и снижает стоимость кликов 
	4) Правильно устанавливаем целевые страницы объявлений

	Директ Коммандер - программа для управления объявлениями когда их много
	direct.seodroid.ru - сервис для управления рекламными компаниями за 450 руб/мес:
		- автоматически меняет ставки для удержания позиций
		- исключает невыгодные площадки в РСЯ
		- собирает запросы (можно пополнить минус слова)
		- авт. установка utm меток и т.д.


-------------------------------- Информация о KeyKollector с другого курса ----------------------------------------------------------

111) Рукапча  9d7fadb4fff092d2a487e45acbcd905f
     Покупка номеров: https://sms-activate.org/ru  // лучше все таки старые, живые аккаунты

111) Данные для ввода в Коллектор:
     Яндекс аккаунт: rodnovspb:cined261475288;#tel:+79507261797
     Если не хочет заходить в аккаунт через keykollector зайти в него сначала через Эксплорер

111) Замена в коллекторе символов на пробелы:
	«»':/→&;)(²\"?|@,;!#$%^&;*(){}|!"№;%:?*_-=–[]￼`‘’®ː&;&;?.,✪—×ì”“•􀉟™🌱`~←°⁷《》∅“➞�¼½�õ¶￵₽●�⅞⅘？£+

111) Создаем скрипт на несколько задач:
	Копируем весь наш список ключевиков в буфер
	Данные -> Планировщик задач: 
		1) Создать фразы из Я Вордстата -> Задать параметры -> Кнопка распределить по группам
        		(распределилось автоматически) -> Добавлять + оператор к словам -> Регион не задаем (для макс охвата)
		2) Собрать похожие поисковые запросы Яндекс - ни одну капчу не разгадал, не использовать
                   Задать параметры -> как в первой задаче распределяем и т.д.
		3) Собрать похожие поисковые запросы Гугл (уст страну и язык)
		4) Создаем группу "Солянка", далее задача: Создать фразы из Я Вордстата -> Два раза нажимаем в окошке на
                   группу "Солянка" и вставляем все ключевики без распределения по группам -> Режим сбора "Правая колонка"
		5) Собрать поисковые подсказки Яндекс 
		6) Собрать поисковые подсказки Яндекс Директ
		7) В конце нужно спарсить частотности: Собрать прогноз Я-Директ -> Выбрать все группы
		8) Сохранить, Создать скрипт, Продолжить (парсинг начался)
		
111) Справа в панели управления группами создаем новую группу, просто выделяем ее
     На большом экране правой кнопкой, добавить фразы, в открывшееся окно вставляем фразы из буфера
     Собираем частоту: Парсинг->Сбор статистики из Я-Директ -> Меняем регион и выбираем Сбор статистики из Я-Директ
     Также все три галочки в Формат запросов частот , срок - год, чтобы исключить сезонность, нажимаем Начать
     Из полученной таблицы удаляем нулевики: ставим галочки, удаляем отмеченные фразы

111) Теперь парсим дополнительные слова для оставшихся ключевиков:
	Сбор фраз из Я-Вордстат
	Выбираем регион, Добавить + к словам
        Нажимаем Распределить по группам, выбираем свою группу, нажимаем красную папку с двумя стрелками, Начать
        Теперь программа будет поочередно вводить словосочетания из группы в Вордстат и собирать связанные запросы

111) Наша группа пополнилась запросами, но не хватает статистики для словосочетаний "" и "!"
     Теперь заново нажимаем Парсинг->Сбор статистики из Я-Директ, настраиваем как в прошлый раз и заново собираем
     Таблица заполнилась статистикой, изучаем запросы
     При нажатии CTRL + Клик открывается Яндекс с выделенным запросом

111) Здесь будет информация больше для директа:
	- выбрать только слова включающие слово. Например, для уточнения что еще отправлять в минуса. Для этого
          справа сверху вводим нужно слово, например: "бесплатно", нажимаем Enter, остаются только словосочетания с ним
	- отправить какие-то слова из фраз в минуса: выделяем фразу -> отправить выделенные фразы в минус-слова -> 
	-> разбить на слова -> выделяем нужное слово -> Добавить (добавили слово "бесплатно" в минус) ->
        -> Теперь так же просматриваем включающие это слово фразы: 
           выбираем Тип поиска, например: Независимый от словоформы (если не меняется смысл от формы) / Зависимый от словоформы ->
        -> Показать найденные фразы -> Ставим на них галочки -> Удалить отмеченные фразы -> Закрыть режим предпросмотра
        Теперь в проекте появилась корзина, где лежать оставшиеся и удаленные словосочетания
	- копируем слова из файлика с минусами, вставляем их в минус-слова, ищем бесполезные фразы

111) Очень удобно использовать: Данные -> Анализ групп - покажет статистику встречаемости каждого слова
     Ставим галки СПРАВА от ненужных слов -> Правой кнопкой Отправить все слова из заголовков помеченных 
     групп в минус слова -> Выделяем все -> Добавить
     Теперь на нашей вкладке (Вкладка По словам) нажимаем на скролл, галочка "Синхронизировать статус отметки фраз", Применить
     Мы добавили слова в Минус, теперь нужно Применить минус слова на списке: Минус-слова -> Показать найденные фразы
     Выделяем все -> Удалить фразы

111) Теперь удаляем неявные дубли: нажимаем Неявные дубли -> Умная отметка -> Проверяем -> Удалить фразы ->
     -> Средней кнопкой мыши на вкладку -> Сохранить

111) Теперь просматриваем оставшийся список и отправляем в минус слова что еще найдем

111) Словосочетания можно помещать в отдельные группы. Например, если в них есть слово "дешево":
	- ищем через поиск такие фразы -> Выделяем -> Коп/Перенести фразы -> Создаем подгруппу или группу -> 
          -> перенос + отмеченных -> Появилась новая группа

111) Экспорт данных: Выбираем группу -> Файл -> Экспорт -> Фразы и статистика -> Экспортировать
	
111) Если продвигаем в другом регионе, то используем в коллекторе другой домен: ru - yandex.ru, by - yandex.by и пр.
     Через браузер если, то проверяем в расширенных настройках, что регион установился.

111) Процент вероятсности кликов в зависимости от позиции сайта
     	1 место – 30% 
     	2 место – 16% 
     	3 место – 10% 
     	4 место – 8% 
     	5 место – 7% 
    	6 место – 5% 
     	7 место – 4% 
     	8 место – 3% 
     	9 место – 2% 
     	10 место – 4%

--------------------------------------------- Царевский ---------------------------------------------

111) Прочитать:
	https://yandex.ru/support/direct/ - справочник директа
	https://yandex.ru/support/metrica/ - справочник метрика

111) Отсекать минус слова (бесплатно, самостоятельно и пр.).
     Использовать словарь синонимов для минус слов (дешево, недорого, скидки)
111) Чем выше CTR (процент кликабельности на показы), тем меньше понадобится ставка.
     Как если: ставка 1 руб с CTR 10% по количеству показов и бюджету равно ставке 10 руб с CTR 1%
111) Отсюда следует: повышаем CTR, чтобы не повышать ставку в аукционе
111) Правило Паретто: 20% действий приносят 80% результата. То есть не работаем много с СНЧ ключевиками, они стреляют редко,
     а занимают много времени на настройку.
111) Для Яндекса существует три формы слова: существительное, глагол (включая причастия и деепричастия)
     и прилагательное (включая наречечия)
     То есть: купить=куплю=купит=купивший=купи=купленный
	      хороший=хорошо=хорошую=хорош

111) Проверить считает ли Яндекс слова одинаковыми: 
	Если слова для Яндекса одинаковые выборка будет включать только одно слово: "заказной" при запросе "заказной заказно"
	
111) Точную форму можно сформировать через !, тогда: !сделавший не равно сделать

111) Теперь нужно сформировать список для помещения в keykollector. 
     Нужно выжать все односложные и многосложные сочетания, которые возможны. Нам понадобтся все синонимы. 
	
      Разбиваем фразы в Экселе по столбцам: 
          Что? формы базового слова и его синонимы: сайт, интернет-магазин, лэндинг, лендинг (могут быть составными)
          Что сделать? продающие уточняющие слова: заказать, сделать, купить
          Где? место: Москва, СПБ, Питер
          В четвертом слова описания: 
	    Качество? фирменный, удобный
	    Время? быстро, срочно, сегодня, завтра
	    Сколько стоит? дешево, недорого
	    Для чего, какая цель покупки? в подарок, для поездки
 	    Исполнитель? мастер, разработчик, студия
	    Cостав? мрамор, пластик и пр.

      Некоторые базовые слова, часто в услугах (что?)  могут состоять из двух слов или быть составными.
      Например: "ремонт квартир", "создание сайтов". В этом случае для каждого слова создаем видоизменения: 
      ремонт: правка, исправление
      квартира: студия, коттедж, дача, хата
      В итоге первая колонка ЧТО? уже включает словосочетания: 
	ремонт квартир, ремонт студии и пр.
	регистрация фирм, открытие ООО, создание организации
      Составное слово "бронежилет": бронированный жилет, защитная разгрузка и пр.

  
     1) Открываем Вордстат, вбиваем односложник, напр: "сайт" или 'ремонт квартир' и добавляем в список все подходящие слова
     2) Методом елочки и кавычек удлиняем фразы от 2 до 8 и добавляем в список их: 
      	    "сайт сайт", "сайт сайт сайт" ...
            "лэндинг лэндинг лэндинг", "лэндинг лэндинг лэндинг лэндинг" ...
	    "купить сайт сайт сайт"
            "ремонт квартир квартир"
        Вордстат понимает их как: найди трехсложные и тд. сочетания включающие слова из кавычек, то есть:
	    "сайт сайт сайт сайт сайт сайт" => "валберис интернет магазине сайт интернет магазин"
        Коллектор передаст их в вордстат и получит сочетания нужной длины

     3) Ищем другие частые неявные сочетания слов методом перегруппировки: 	
     	  Для того, чтобы не соревноваться с конкурентами по одним и тем же ключевикам нужно найти частые запросы,
          но по которым не рекламируются конкуренты. Для этого получаем базовое слово:
	  односложное: самсунг, стол, вольво
	  двусложные: ремонт квартир, создание сайтов (каждое слово - базовое, без которого ключевик теряет смысл)
	
    Далее ищем любой сервис по "Генерация уникальных ключевых слов" - keys.so/ru/tools/combine или prodalet.ru/module/tools/phrases
    перегруппировываем все и формируем сочетания ключевиков.
    Получаем слова с хорошей частотностью, часто которых нет даже в Вордстате

111) После того, как мы сгенерировали ключевые словосочетания (сайт, создание сайта, создание сайта в СПБ дешево и пр.)
     нужно получить их статистику и узнать через вордстат ЧТО ЕЩЕ пользователи ищут вместе с такими запросами. 
     Вбивать в вордстат долго, можно использовать: keykollector, словоеб, магадан.
     В интернет есть видео. Например мы делаем кухни и доставляем их.
     Из отчета узнаем, что наряду с "кухня доставка" часто вводят "доставка еды с кухни" - получаем минус слово "еда"
     Теперь прописываем это минус слово, либо берем ключевик в кавычки: "кухня доставка"

111) Списки общих минусовых слов есть в интернет. Также в них можно включить города. Например, если вы продаете кухни 
     только в Калуге, то нам не нужен запрос: "заказать кухню Вологда"

111) Обычно в одной кампании несколько сотен минус слов. 
     Чем точнее мы пропишем ключевики и уберем минус слова, тем выше будет CTR и соотвественно ниже цена клика.
     Минус словам можно задавать форму: -!ремонтом

111) https://youtu.be/qKOQXJr-ots - как работать с keykollector

111) Операторы:
	"..." - фиксирует количество слов, не учитывает форму слов. Работает как заменитель минус словам
		"создание сайта" = "создание сайтов" = "сайт создание" но не равно "создание сайтов самостоятельно"
 	Одна из тактик: не испольльзуем двойные кавычки, а постепенно включаем новые минус слова

	+ - обязательность стоп слов (вспомогательных предлогов, союзов, частиц и пр).
		квартира +с ремонтом - теперь предлог "с" обязателен, в выборку не попадет квартира без ремонта
                однако: хостел +в СПБ теперь не равно хостел СПБ. Поэтому лучше не использовать

	! - фиксирует форму слова (число, падеж, время)
		"купить !собаку" не равно "купить !собаке"

	[] - порядок слов. Слова внутри нельзя поменять местами
		"билет [москва питер]" не равно "билет [питер москва]"

	(...|...) - использование любого слова из скобок
		    купить машину (недорого|ваз) = "купить машину недорого"  или  "купить машину ваз"
                                  (яндекс|yandex)
               
111) "[!купить !сайт +в !москве]" - точная фраза с порядком слов, формой слов и количеством

111) Непонятные ключевики вбиваем в вордстат и поиск и смотрим. Яндекс исп маш. обучение и скорее всего 
     уже понял что имел ввиду пользователь:
	"труба 1 2" => "труба 1/2 дюйма"

111) Горячие запросы: нас точно ищут ("заказать сайт в СПБ", "ремонт квартир в Москве" - но отминусовать ненужное, так
     как во втором случае сработает "уборка квартир после ремонта в Москве")
     Теплые запросы: непонятно точно ищут ли нас ("создание сайтов" - а вдруг на Тильде?)
     Холодные запросы: интересуются, но точно пока не закажут ("сделать сайт самостоятельно")

111) Очень важно - понять кто твоя ЦА, что и как она ищет и что у нее болит.
     На сайте важно поработать с возражениями (Дорого, но принесет в разы много денег и т.д. Еще возражения: долго,
     не доверяю и пр)

111) Правила написания объявления:
	- важно понять ЦА и писать объявление для них
	- каждое слово важное. Одних одно слово отпугнет, других привлечет
	- самое важное и ключевое пишем в заголовке, так как остальное могут не читать
	- если привлекаем внимание чем-то, то на сайте это же на видном месте (бесплатная доставка)
	- в заголовке слова из запроса, чтобы яндекс дополнительно выделил такие слова жирно, будет выше кликабельность и ctr
	- в ВЧ запросах при подаче объявления это обязательно, т.к. цена сначала считается по предполагаемому CTR
	- занять наибольшее пространство за счет заполнения в объявлении всего что можно
	- использовать заглавные буквы и цифры где можно, они крупные (не сутки, а 24 часа; аббревиатуры и пр)
	- можно использовать символы: !, :), ?, %
	- важно: что ищет -> такое объявление -> такая заточенная страница
          например: запрос "заказать лендинг" -> объявление "создание лендингов" -> переход на страницу по лендингам
	- использовать отсекающие фильтры:
		по месту: ремонт в Мытищах
		по ЦА: акция для ИП
		по цене: премиум товары, изготовление дешево, цена от 5000 руб.
		также фильтры: по полу, возрасту, по услуге (продажа, аренда)
	- призывы к действию (забронируйте, проверьте, не пропустите)
	- специфические для отрасли призывы к действию (забронируйте, арендуйте)
	- сначала собираем важное мозг. штурмом, а затем собираем из этого паззл:
		ключевые свойства, УТП: 15 лет опыта, качество по ГОСТУ, гарантия
		первый шаг если есть: бесплатный замер, консультация, попробовать бесплатно, 3Д тур
		второстепенные выгоды: точные сроки, всегда на связи и пр.
		сервисные характеристики: личный менеджер, склад, без залога
		фильтры (уже писал)
	   После этого собираем паззл из наиболее выдающегося

111) Ошибки:
	- без воды, только конкретика (не "быстро", а "за 4 часа")
	- без остроумия, креатива, могут не понять, нужна суть товара
	- без мешанины 10 преимуществ, только ключевое, что зацепит
	- не быть похожим на конкурентов, изучить их объявления и выделиться среди них
	- лучше не использовать ценовые войны, если только цена не отличается разительно
	- рассказывать не о себе, а о преимуществе работы с тобой (не 10 лет на рынке, а 10 летний опыт и качество)

111) Посмотреть все рекламируемые объявления по запросу:
	https://yandex.ru/search/ads
     Смотрим конкурентность, используемые фразы.
     Если есть объявления, значит здесь заработать в директе можно

111) Собираем количество объявлений в Я-Директ через Коллектор

111) Еще советы:
	- отключить Доп. релевантные фразы. Чтобы яндекс не подбирал их сам и не сливал бюджет
	- включить Расширенный георг. таргетинг (если в введеном запросе есть "сайты в СПБ", то показывалось в Карелии)
	- выставить ограничения бюджета, чтобы не слить его при ошибке настройки
	- посмотреть в статистике для кого/чего (пол, возраст, устройство ...) конверсия выше-ниже, выставить повыш-пониж коэффициенты
	- настроить время. Не обязательно отключать ночью, ведь ночью и конкурентов меньше и цена клика ниже
	- в быстрые ссылки можно вставлять одинаковую ссылку, если менять якорь: #text1, #text2
	
111) Не всегда релевантная страница - лучшая для перехода. Может быть, что главная окажется более продающей
	
111) ЯДирект - аукцион. Объявления располагаются в порядке уменьшения произведения: ставка * CTR * эфф-ть рекламной кампании * эфф-ть объявления
     среди таких же объявлений у других. 

111) На "эффективность объявления" влияет наличие ключевых фраз в заголовке, ведет ли объявление на страницу сайта с ключевой фразой, 
     время нахождения на сайте после перехода и пр. 
     Яндексу выгодно, когда пользователь доверяет объявлениям и получает что хочет при переходе по ним. 

111) Чем выше CTR тем меньше нужна ставка для вхождения в блок
     CTR у каждой ключевой фразы свой

111) Ставка — максимальная цена, которую рекламодатель готов заплатить за клик по своему объявлению, 
     и на нее ориентируется система, когда оценивает, какие объявления и в каком порядке победят в аукционе

111) Итоговая цена клика обычно меньше ставки в два раза. Если настроено правильно , то: Ставка 40 руб => за клик спишут 20 руб.
     Сколько Яндекс спишет зависит от того где он покажет объявление. Если будет показано на самой наилучшей позиции, 
     то спишет больше, если например внизу страницы на самой дешёвой позиции, 
     то списываемая цена будет в несколько раз меньше чем ставка.
    
     Получается, что цена клика зависит от аукциона (то есть от конкуренции: количества конкурентов, региона, времени и пр.)

111) РСЯ - сеть сайтов на которых есть реклама от Яндекса.
     Здесь можно напомнить о товаре/услуге, если человек заходил на сайт, но не купил (ретаргетинг)
     Также можно рекламировать просто.
     Здесь можно рекламировать объявления по теплым и холодным запросам.
     CTR здесь не особо важен, важнее охват.
     Цена клика на РСЯ ниже.

111) На РСЯ в отличии от директа можно использовать творческий подход: интрига, напоминание, продающие заголовки

111) При ретаргетинге нужно использовать условия, выставленные по целям из метрики: был на сайте в течение последних 7 дней, но не заполнил заявку

111) CTR = (клики/показы) * 100
     CV (конверсия сайта) = (звонки и заявки / посетители с рекламы) * 100
     CV (конверсия отдела продаж) = (кол-во продаж / звонки и заявки) * 100


============================================= Турбо страницы  ========================================================		

111) Турбостраницы яндекс сохраняет у себя, используется простая специальная верстка и поэтому сайт отдается быстрее
     Турбостраницы доступны даже если сайт не доступен

111) Можно установить Яндекс Метрику

111) Создаем файл турбостраницы, например: turbo.xml
     Размещаем на сайте, не должен быть закрыт для поисковика в robots.txt	
     В вебмастере в Турбо-страницы -> Источники -> Ссылка на источник -> добавляем ссылку на файл, например: https://example.com/turbo.xml	
     В Турбо-страницы -> Настройки -> Веб‑аналитика -> Вставляем номер счетчика основного сайта из Метрики (Настройка -> Номер 33057573)
     Текст турбостраниц, структура, картинки, другие элементы должны соответствовать мобильной странице сайта. Можно делать турбостраницы
     только для некоторых страниц сайта  
     В вебмастере в настройках есть функции вставки в турбо-сайт мобильного меню, шапки, телефона, метрики и пр.
	
111) Структура страницы:
	<?xml version="1.0" encoding="UTF-8"?>
	<rss xmlns:yandex="http://news.yandex.ru"
     		xmlns:media="http://search.yahoo.com/mrss/"
     		xmlns:turbo="http://turbo.yandex.ru"
     		version="2.0">
	<channel> - начало канала
	<!-- Информация о сайте-источнике -->
	<title>Мир печатей</title> - название сайта
	<description>Канал сайта о печатях</description> - краткое описание RSS-канала
	<link>http://печати-челябинск.рф/</link> - домен сайта

	 <!-- Каждую страницу помещаем в тег item -->
	 <item turbo="true"> - каждая страница должна быть в элементе item. Можно отключить турбо страницу если поставить здесь false
	    <turbo:extendedHtml>true</turbo:extendedHtml> - включаем обработку всех тегов, иначе некоторые могут игнорироваться
	    <link>http://печати-челябинск.рф/</link> - ссылка на оригинальную страницу сайта с этой статьей, здесь это Главная

            <turbo:content> - содержимое страницы
		<![CDATA[

		   <!-- Тут пишем страницу как в обычном HTML, но соблюдаем струкртуру -->
		   <header> - шапка страницы обязательна, тут пишем заголовок страницы, можно добавить картинку
		 	<h1>Печати и штампы | Мир печатей</h1>
			<figure> - можно вставить изображение-превью
            		   <img src="http://example.com/img.jpg"/> - JPEG, PNG, GIF
        		</figure>
			<h2>Заголовок второго уровня</h2>
			<menu> - это меню можно создать в Вебмастере -> Настройки -> Меню -> Основное меню
            		    <a href="http://example.com/page1.html">Текст ссылки</a>
            		    <a href="http://example.com/page2.html">Текст ссылки</a>
        		</menu>
		   </header>	

		   <p>Компания "Мир печатей" специализируется</p> - пишем текст страницы. Разбиваем на абзацы. 
								    Используем все теги (div, p, span, table, img и пр.)	
		   
		]]>		
	    </turbo:content>
	 </item>

	<item turbo="true"> - следующая страница
		... 
	</item>

     	</channel> - конец канала
	</rss>

111) В вебмастере -> Турбостраницы -> Отладка можно проверять как будут выглядеть страницы

111) CSS стили для турбо-страниц:
	1) Через отладку пишем страницу item и css к ней
	2) Копируем css, вставляем в форму на вкладке CSS, сохраняем
	3) Код страницы item вставляем в сам файл xml
     Ограничения:
	1) Нельзя в стилях выбирать по тегу и id (div, ul, #elem и пр.), выбираем их по классу 
	2) Не работают инлайновые стили


======================================================= SEO оптимизация  =============================================================

111) Всем ссылкам прописываю title с нужными ключевиками

111) Первый экран - самый важный, здесь обязательно нужны ключевики

111) РАЗНООБРАЗИЕ информации:
	- цифры (чем больше, тем лучше)
	- списки и таблицы
	- картинки
	- цены
	- любые другие структуры

111) Чем интереснее сайт для пользователя, тем лучше

111) В первом предложении сайта обязательно ключевики

111) На все внутренние страницы проставить перекрестные ссылки с анкорами на заточенную под ключевик страницу

111) Заполнить по максимуму вспомогательную информацию: 
	- адрес
	- телефон, почту, вотсап, телеграм
	- карту
	- график работы
	- форму обратной связи
	- ссылки на социальные сети
	- файлы для скачивания
	- страничку в Яндекс и гугл картах с отзывами и пр.

111) Думаю считается доля ключевика в каждом абзаце

111) Не любит длинные абзацы - лучше коротко, но емко

111) Грузить текст на seolik.ru/analysis-text и смотреть, чтобы нужные ключевики были самыми частнотными во главе списка, 
     а не левые "ненужные" слова
   
111) Мобильная версия или если нет отключить перестраивание сайта под экран

111) Яндекс вычисляет коэффициенты переоптимизации и пр ориентируясь на средние значения по выборке. То есть анализируем
     конкурентов по области деятельности и приближаемся к среднему значению у них

111) Много полезных служб: https://arsenkin.ru/tools/

111) Чем ближе фраза к логотипу, тем сильнее!!!  Сделать еще ее и анкором как на wordstat.yandex.ru
	   	
111) Логотипу давать класс включающий слово logo   

111) Яндекс очень хорошо относится к наличию в статье связанных по теме ключей:
     LSI-ключи — слова и фразы, связанные с основным запросом семантически, по смыслу.
                 LSI-ключи раскрывают основную тему, наполняют ее и обогащают

     Примеры: 
	туры в европу: отдых, экскурсионный, автобусный, путешествие, цена
	автосервис: ремонт, автомобиль, обслуживание, отзыв, работа, адрес, телефон, автотехцентр
	цветы купить интернет-магазин: розы, букет, дешево, доставка, полевых, женщина, девушка, мосцветторг
        сайты: хостинг, домен, SSL сертификат, cms, метрика, посещаемость, трафик и пр.

     Как искать LSI ключи:
	0) В коллекторе после определения ГЕО: Данные -> Анализ групп -> По поисковой выдаче
	   Найдутся пересекающиеся заголовки   
	1) Вордстат
	2) Поисковые подсказки при вводе в Яндекс и Гугл
	3) Похожие запросы:
		arsenkin.ru/tools/sp/  // парсинг подсветок и тегов
		keys.so/ru/similarkeys // дополняющие запросы
		ru.megaindex.com/a/wordcluster // запросы конкурентов и похожие запросы


--------------------------------------------- Гриды ---------------------------------------------
 
111)  Смысл гридов - в расчерчивании сайта на сетку и гибком размещении по ней элементов сайта
      
      Сначала контейнер расчерчивается на сетку-грид (ряды и столбцы) с помощью grid-template(сокр. вариант), причем так как ячейки 
      могут объединяться, то не обязательно они в итоге будут расположены по этой сетке (только начало и конец ячейки лежит на сетке) 

      grid-template-columns - колонки и их ширина
      fr - фракция. Считается длина свободного пространства и делится поровну на количество fr
      grid-template-columns: 1fr 1fr 2fr - на три колонки, третья в два раза больше 1 и 2
        		     200px 50% 1fr - 3 колонки (200px, 50% от контейнера, оставшаяся часть)
			     repeat(3, 1fr) - на 3 одинаковые колонки
			     repeat(3, 1fr) 4fr - 3 одинаковые и одна большая 4fr
			     100px auto - 2 колонки в 100px и все остальное пространство
 			     repeat(auto-fill, 100px) - авт. разделить контейнер на столбцы по 100px
			     repeat(auto-fill, minmax(150px, 1fr)) - авт. столбцы шириной минимум 150px, иначе перенос

     grid-template-rows - ряды (строки) и их высота

     grid-template и grid - сокр. запись для рядов и колонок (сначала ряды, затем колонки)
     grid: 1fr 1fr 1fr / 1fr 1fr 1fr; - 3 на 3 одинаковые ячейки	
     grid-template: 1fr 1fr 1fr / 1fr 1fr 1fr; - 3 на 3 одинаковые ячейки

     grid-row - занятие ячейкой линий сетки по вертикали (объединение рядов сетки по вертикали)
     grid-row: 1/2; - с 1 линии до 2
     grid-row: 1/4; - с 1 до 4 линии

     grid-column - занятие ячейкой линий сетки по горизонтали (объединение столбцов сетки по горизонтали)

     column-gap - расстояние между столбцами	    
     row-gap - расстояние между рядами
     gap - сокращение для одновременного column-gap и row-gap

     Если в сетке вставлено больше блоков чем ячеек, то браузер дорисовывает грид - получаются ячейки неявного грида. Например:
	.parent {
      		display: grid;
      		grid-template: 50px / 50px 50px 50px;} - 1 ряд и 3 колонки, всего 3 ячейки
	Если вставить 4 блока, то появится новый ряд внизу - неявный грид
     grid-auto-columns: 100px; - ширина ячеек неявного грида
     grid-auto-rows: 100px; - высота ячеек неявного грида

     Если ячейкам заданы не fr (то есть ячейки не подстраиваются под весь размер родителя), а размеры и получается, 
     что общие размеры получившейся сетки меньше размера родителя, то сетку можно двигать внутри родителя-грида:
     justify-content: end - выравнивание сетки по горизонтали справа (также center, start)
     align-content: end - выравнивание по вертикали внизу (center, start)

      	 
     По умолчанию содержимое ячейки занимает все пространство ячейки. 
     Можно выравнивать его внутри всех ячеек:
     justify-items: end - содержимое в правой части ячейки (center, start)
     align-items: end - содержимое ячейки в нижней части ячейки (center, start)	
     place-items: center end; - сокращенный вариант (по вертикали, по горизонтали)

     justify-self: start - выравнивание содержимого для отдельной ячейки по горизонтали
     align-self: center - выравнивание содержимого для отдельной ячейки по вертикали
     place-self: end center - сокращенный вариант (по вертикали, по горизонтали)

     Если элементы накладываются друг на друга, то порядок наложения можно регулировать 
     с помощью oreder и z-index. Они работают одинаково, но порядок z-index главнее.
 
	
	
--------------------------------------------- Vue ---------------------------------------------

111) https://v3.ru.vuejs.org/

111) https://element-plus.org/ - компоненты кнопок, форм, каруселей, всплывающих окон и пр с встроенными js эффектами

111) Вью позволяет создавать SPA приложения:
	1) первоначальная загрузка немного дольше
	2) моментальный отклик при переходе по ссылкам
	3) так как нет перезагрузки страницы, то можно создавать игровые приложения и пр., где браузер один раз загрузил
	   приложение и больше не перезагружается, только иногда отправляет fetch запросы
	4) spa приложения не любят поисковики?, так как сначала получают пустую страницу, 
	   которую на лету формирует браузер по js скриптам
	5) nuxt позволяет отдавать готовую сформированную страницу с использованием компонентов vue? 
	6) Для общения с сервером используются fetch запросы и json ответы с помощью api бэка. В ларавель для Вью есть inertia.js	

111) Насколько понял, компонент - это js объект с версткой, переменными и функциями

111) Можно создавать несколько обработчиков Вью на страницу:
	const app1 = createApp({...})
	const app2 = createApp({...})

	app1.mount('#container-1')
	app2.mount('#container-2')

111) Реактивность - Vue.js активно отслеживает состояние JavaScript и при его изменении быстро обновляет DOM.

111) Смысл реактивности в том, что при работе пользователя со страницей (spa сайтом) не происходит перезагрузки,
     но vue автоматически оптимально перестраивает DOM дерево в соответствии с измененными js переменными компонента.
     При перестроении затрагиваются только измененные элементы.

111) Встраивать компоненты друг в друга не получится через import в обычных страницах html, насколько понял нужен webpack, cli и подобное

111) Создаем корневой элемент: <div id="app"></div>
     Внизу есть установка Вью из терминала - пользуйся этим способом

111) Подключаем в шапке Вью2: 
	<script src="https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js"></script>
     Если работаем в CLI, то не нужно

111) Инициализируем корневой элемент:
	<script>
	new Vue({
		el: '#app',
		data: { - в этом массиве пишем переменные , в CLI записывается как data(){ return { name: 'Вью', ... } }
		   name: 'Вью',
		   flag: true,
		   url: 'https://ya.ru'	},
		methods: { - здесь методы
		   sayHello: function (){
			return 'Привет';},
		   sayHello(){ - сокращенная запись
			return 'Привет';}}})
	</script>

	<h1>Строка: {{ name }}</h1> - переменная при рендеринге вставляется в верстку
	<h1>Метод: {{ sayHello() }}</h1> - отрабатывает функция
	В {{ flag ? 1 : 2 }} код работает как в js, но можно писать только одно выражение и условия д.б. в тернарной форме
	
	Кроме js кода в {{}} можно вызывать и методы компонента: {{ increment() }}, хотя для этого лучше использовать computed свойства.
        В методах внутри {{}} нельзя выполнять изменение данных или запуск асинхронных процессов, для этого лучше исп. события жизненного
	цикла компонента, напр.: created

	Также подобный js код можно писать в значениях динамических атрибутов: 
		:title="num + 1"

	В {{..}} и "" код по умолчанию экранируется с помощью браузера для защиты от xss уязвимости

111) Если значение атрибутов встраивается через переменную, то делаем атрибуты динамическими:
	<a :href="url">Яндекс</a> - динамический атрибут href + переменная без фигурных скобок
	<a v-bind:href="url">Яндекс</a> - то же самое
	<a :[key]="url"> ... </a> - сокращенная запись для динамического названия атрибута. В такой записи есть ограничения на запись
				    динамического атрибута, например :[someAttr] - неверно, преобразуется в :[someattr] и сломается

111) Вывод переменной как верстки:
	link: '<a href="https://ya.ru">Яндекс</a>' - переменная из data
	<h2 v-html="link"></h2> - ссылка вставится внутри тега h2

111) События:
     <button @click="riseCounter">Увеличить</button> - запуск функции по событию "клик"
     <button @click="counter2++">Увеличить</button> - внутри запуска по событию можно писать js код
     <button @click="riseCounter(5)">Увеличить</button> - в функцию можно передавать параметры
     <button @click="riseCounter(5, $event)">Увеличить</button> - через $event можно передавать объект события
     <button @click="func">qqq</button> - если параметры в функцию не передаются, то event передавать не нужно,
				          принимается без передачи: func(e){console.log(e.target)}

     <div @mouseover="onHover">Текст</div> - изменение цвета по наведению	
     onHover: function (event){ event.target.style.color = 'red'}

111) Модификаторы событий:
     <a @click.prevent="https://ya.ru/" href="">Ссылка</a> - отмена поведения e.preventDefault() через модификатор prevent

     <form @submit.prevent="onSubmit"></form> - отмена отправки формы браузером и перезагрузки страницы 

     @click.self - срабатывание события только на этом элементе, без срабатывания на всплывших событиях из дочерних элементов

     @click.once - обработчик клика будет вызван только один раз, то есть: elem.addEventListener("click", func,{once:true})

     <div @mousemove="func">
	<span @mousemove.stop="">Не изменять</span> - модификатор для e.stopPropagation() 
     </div>

     <input type="text" @keyup.enter="func"> - модификатор для события нажатия на enter
     <input type="text" @keyup.enter.space="func"> - цепочка модификаторов

     @click.middle - если нажата средняя кнопка мыши (также left, right)

     Также вместо модификаторов можно использовать обычный js код в функциях:
	e.preventDefault()
	e.stopPropagation() и пр.

111) Классы

     :class="{ active: isActive }" - добавление класса active в зависимости от значения переменной isActive (true или false)
			             Тег может иметь свой встроенный класс, этот динамический класс просто будет добавляться к нему
				     Объект { active: isActive } может быть вынесен в переменную компонента, а в class вставлять его переменную

     :class="[{ active: isActive }, errorClass]" - всегда добавляется errorClass, а active по условию

     :style="{fontSize: text +'px', color: 'red'} - стили. Если динамические, то передаем в объекте и исп. camelCase написание

111) Двусторонее связывание элементов формы и переменных в одном компоненте:
	<input v-model="text" />  то же, что 
        <input :value="text" @input="text = $event.target.value" />

     Как работает:
	1) При отрисовке компонента элемент заполняется из переменной
	2) При изменении элемента пользоватем (события input, change) данные попадают в переменную
	   	
     v-model:
     <input type="text" v-model="inputValue"> - v-model записывает в value установленное значение переменной, 
					        а при событии input также меняет эту переменную на новое значение value
				                Применяется для элементов формы: input, select, textarea
     
111) Двусторонее связывание родителя и дочернего компонента через v-model во Вью 3:
	<app-input v-model="text"></app-input> - вставляем компонент в родитель

     Такая переменная в дочерний пробрасывается в modelValue:
	props: { modelValue: [String, Number]} - принимаем переменную в пропсе дочернего

	<input :value="modelValue" @input="updateInput"> - вставляем ее в инпут

	updateInput(event){ this.$emit('update:modelValue', event.target.value)} - испущенная функция update дает команду на изменение принятой переменной
											
     Можно также вместо v-model использовать свое название переменной:
	<app-input v-model:body="text"></app-input> - в родителе устанавливаем название отправленной переменной как body 

	props: { body: [String, Number]} - принимаем переменную из родителя

	updateInput(event){ this.$emit('update:body', event.target.value)} - испускаем в родителя событие изменения принятой переменной

     Пример со своим названием переменной:
	<my-dialog v-model:show="dialogVisible" /> - родитель

	<button @click="hideDialog"></button> - дочерний
	hideDialog(){ this.$emit('update:show', false) } - меняем переменную родителя на false

     Пример с v-model и modelValue:
	В родителе:
	   flag: true
	   <div v-if="flag">Lorem ipsum dolor sit amet.</div>
  	   <app-button v-model="flag"></app-button>
	В дочеререм:
	   props: ['modelValue']
	   <button @click="$emit('update:modelValue', !modelValue)">Кнопка</button>

     То есть v-model в связке родителя и дочернего просто сокращает код за счет того, что не нужно писать функцию на изменение родительской 
     переменной, в дочернем можно просто вызвать встроенную функцию update:  $emit('update:modelValue', var)

     Таким образом можно двусторонне связать несколько атрибутов тега в связывании родитель-дочерний, используя разные названия:
	v-model:body, v-model:text и т.д.

111) Во Вью2 двусторонее связывание родителя и дочернего компонента работает по ключу value:

	<app-input v-model="text"></app-input> - v-model передает свойство из родителя в пропс дочернего компонента,
						 во Вью2 оно доступно по ключу value: props: ['value'],
        Теперь нужно привязать в дочернем событие @input
	   <input :value="value" @input="updateInput" >
	И испускать новое значение в родителя:
	  updateInput(event){this.$emit('input', event.target.value)}

111) Модификаторы v-model:
    		
     v-model.lazy:
     <input type="text" v-model.lazy="inputValue"> - модификатор lazy, переменная меняется только при событии change 
				                     (если type='text', то при потере фокуса) на инпуте

     v-model.trim - обрезание пробелов

     v-model.number - строки в цифры

     v-once - элемент один раз отрисуется и не будет меняться

111) Циклы, условия:

     v-if и v-else:
     <h2 v-if="isVisible">Текст</h2> - v-if удаляет/добавляет элемент в DOM в зависимости от полученного true/false
     <h2 v-else>Другой текст</h2>
     <button @click="isVisible = !isVisible">Скрыть</button>

     <div v-if="posts.length > 0">Список статей</div>
     <h1 v-else>Статей нет</h1> - v-else должен сразу следовать за тегом с v-if

     <template v-if="flag"> - если нужно обернуть в условие несколько тегов, то можно использовать обертку template, которая в верстке не показывается
	<div>111</div>
    	<div>222</div>
     </template>

     <template v-if="item.measure">{{ item.measure }}</template> - также template помогает, если обертка не нужна, а проверка нужна
 
     <div v-if="obj?.value"></div> - показывать если существует объект obj и его свойство value не приведется к false. Хорошо подходит для фетч запросов
				     на API

     v-show - добавляет style="display: none;", а не стирает элемент из DOM как v-if
     <h2 v-show="isVisible">!!!!!!</h2>

     v-for:
	<div v-for="item of people">{{ item }}</div> of и in одно и то же здесь
        <div v-for="item in people">{{ item }}</div>

     Так как элементы массива могут повторяться и чтобы не было ошибок при одинаковых элементах, при изменении массива нужно применять ключи:
        <div v-for="(item, index) in people" :key="index">{{ item }}</div> - ключом является индекс элемента
	<div v-for="post in posts" :key="post.id">{{ item }}</div> - ключом является id объекта post

     Если перебираем объект, то в первый параметр попадает ключ, во второй - значение, в третий - индекс:
	people: {a: 1, b: 2}
	<div v-for="(value, key, index) in people" :key="key">{{ value }}</div>, где key: a,b; value: 1,2; index: 0,1

        <div v-for="item in 10">{{ item }}</div>
        <div v-for="item of [1,2,3]">{{ item }}</div>

     v-for и v-if нельзя использовать в одном теге, так как у v-if приоритет и могут быть ошибки.
     Можно что-то из них поместить в обертку, например в template 

     Цикл можно использовать для отрисовки компонентов, но переменные понадобится передавать в пропсы, 
     так как у компонентов своя изолированная область видимости:
     	<my-component v-for="item in items" :key="item.id" :item="item"></my-component>

111) Ссылка на ноду (не совсем аналогичная document.querySelector, потому что внутри есть от Вью свойства) создается через ref:
	<h1 ref="heading">{{ title }}</h1> - в объекте Vue появилась ссылка на эту ноду
        this.$refs.heading - здесь нода
        this.$refs.heading.style.color = 'red' - изменили цвет h1

     Пример 1:
	<input type="text" ref="inp">
	mounted() { this.$refs.inp.focus() } - при сборке компонента навели на него фокус

     Пример 2:
	<app-elem ref='elem'></app-elem>  дочерний компонент
	
	Теперь из родителя можно иметь доступ ко всем свойствам и методам дочернего компонента:
	   this.$refs.elem.log()
	   console.log(this.$refs.elem.num)

111) Ссылка на собственные методы объекта Vue:
	this.$destroy() - удаление объекта из памяти

111) Функции срабатывающие внутри объекта Vue в его жизненном цикле:
	beforeCreate, created, beforeMount, mounted, beforeUpdate, updated, beforeDestroy, destroyed

111) Уставновка Vue в терминале:
     1 вариант с Vuex
	npx @vue/cli create vue // создать папку vue с проектом // или npx @vue/cli create .  - если папка с проектом уже создана ранее
	в процессе выбираем manually
	далее в процессе пробелом выбираем пункт Babel, Vuex, Router
	далее выбираем history mode для роутера
	далее выбираем: in dedicated config files (хранение настроек в отдельных файлах)
	далее в сохранить ли пресет настроек выбираем N
     После установки пакета:
	cd vue // если создавали внутри проекта папку vue при установке вью
	npm run serve - запустится сервер, даст ссылку на сайт

     2 вариант с Pinia
	Заходим в общую папку с сайтами, в ней вводим:
		npm init vue@latest
	При установке пишем название создаваемой папки с сайтом
	Выбираем что устанавливать
	Далее по командам:  cd папка, npm install, npm run dev

111)	Можно установить sass:
	npm i sass

	Создаем файлы sass (напр.: variables.sass, common.sass), импортируем их в главный файл main.sass:
	@import "./variables"
	@import "./common"

	В файле main.js подключаем main.sass:
	import './assets/styles/main.sass'

	Как пишутся стили sass:
	   В variables.sass переменные:
	   $white: #fff
           
	   Сами стили:
	   .wrapper
  	     margin: 0
             padding: 0
	     
             &-inner // получается .wrapper-inner
	      display: flex
	      color: darken(#b37399, 20%) // затемняем на 20%

	В компонентах ставим lang:
	<style scoped lang="sass">
	  @import "./assets/styles/main" // зачем-то импортируем еще раз, хотя импортировали в main.js
	</style>

111) Удаляем c index.html все лишнее

111) Устанавливаем расширение в хром Vue devtools, в его настройках разрешаем открывать локальные файлы
     Это расширение появится в инструментах разработчика

111) В шторме быстрое создание структуры компонента: vbase

111) Компоненты:

     А) Локальная регистрация:
	1) Даем компоненту имя: List.vue
	2) Прописываем его в родительском компоненте в поле components:
		components: { 'app-list': List } // Шторм авт. импортирует его
	3) Используем:
		<app-list></app-list>
	4) Если называть файл компонента с двойным названием AppList, то достаточно:
		components: {AppList} - название тега уже писать не нужно
		<app-list></app-list>

     Б) Глобальная компонентов, чтобы не приходилось частоиспользуемые компоненты (напр. кнопки) каждый раз импортировать:
	1) В самом компоненте MyButton прописываем имя: 
		export default {
  		   name: 'my-button'}; // либо: MyButton. Тег будет <my-button>
					  
	2) Создаем файл со списком компонентов UI/index.js:
		import MyButton from "@/components/UI/MyButton.vue";
		export default [MyButton] - !!!массив, здесь только один компонент для примера

	3) В файле main.js:
		import components from "@/components/UI"; - импортируем файл index.js
		const app = createApp(App)
		components.forEach(component => {
			app.component(component.name, component)})
		app.mount('#app')

111) Входные параметры (пропсы).
	Входные параметры прописываются в верстке как обычные атрибуты тега для коренного элемента компонента.
	Как если: 
		<app-button :var="text"></app-button>
		<app-button var="text"></app-button>

	Приведут к созданию верстки в дочерних компонентах:
		<div var="qwerty"><button>Кнопка</button></div> // полученные атрибуты привязались к корневому тегу компонента
		<div var="text"><button>Кнопка</button></div>
	Соответственно Вью при создании пропсов компонента возможно просто их считывает с этих атрибутов

	Также и на самих тегах внутри компонента:
		flag: true - переменная компонента
		<button :disabled='flag'>Кнопка</button> - тег компонента
	Приведет к тегу:
		<button disabled>Кнопка</button>

	Отправка из родителя:
	1) title='text' - статический параметр отправляет строку text. Таким образом можно отправлять только строки

	Если отправляем не строки или отправляем переменные, то используем динамические параметры:
	2) :title='var' - динамический параметр отправляет переменную
	3) :title='var' + '/' + 'var2' - динамический параметр отправляет результат выражения
	4) :is-published="false" - отправляем булево значение
	5) :func='setNum' - отправляем функцию
	6) :comments="[234, 266, 273]" - отправляем массив
	7) :author="{ name: 'Veronica', company: 'Veridian Dynamics' }" - отправляем объект

	Получение в дочернем:
	1) props: ['number', 'func']
	2) props: {'number': Number, 'func': Function}

    Пропсы передаются однонаправленно из родителя в дочерний. В дочернем их изменять нельзя, но можно испускать события
    изменения в родителя:
	1) $emit, функцией
    И переопределять в свое свойство без мутирования (в js объекты передаются по ссылке, изменять в дочернем их не нужно):
	2) Переопределив в свою переменную: 
		props: ['num'] - приняли
		counter = this.num - записали в собственную переменную
	3) Переопределив в computed свойство:
		props: ['num'] - приняли
		computed: { size(){ return this.num + 'px'} }

111) Выходные параметры из родителя автоматически становятся инлайн атрибутами корневого контейнера дочернего компонента.
     Например:
	<app-input :aaa="search" class="qqq"></app-input> - в родителе вставляем дочерний компонент, передаем атрибуты, пропсы
	
	<div aaa="Из родителя" class="qqq"> - корневой контейнер дочернего принял атрибуты

	Чтобы отменить такое навешивание в composition API:
	defineOptions({ inheritAttrs: false })

     Также к принятым атрибутам можно получить доступ в переменной $attrs.
     Объект $attrs включает в себя все атрибуты, которые не приняты в props или emits 
     (например, class, style, прослушиватели v-onи т. д.).

     Можно перенаправить атрибуты с корня на другой внутренний тег:
	defineOptions({ inheritAttrs: false }) - отключаем навешиваение на корень
	<div v-bind="$attrs"></div> - прописываем в другом теге	
	
111) Валидация пропсов:
	props: {
      	   text: {
        	type: String, - Строка
        	required: true, - обязательное значение
        	default: 'Lorem ipsum'} - значение по умолчанию (при required не имеет смысла)
	
111) Принцип работы:
	1) Создается компонент Car.vue:
	   <template>
  	      <div>
    		<h3 class='car'>Название: {{ name }}</h3>
  	      </div>
	   </template>

	   <script>
  	      export default {
    		 data(){
      		   return {
        		name: 'Форд',}}}
	   </script>

	   <style> .car { font-size: 24px; } </style>  - блок со стилями

	2) Встраиваем компонент в родитель:
	   <template>
	      <div>
		 <app-car></app-car> - встраиваем по переменной из components
	      </div>
	   </template>

           <script>
	   import Car from "@/components/Car.vue"; - импортировали компонент

	   export default {
  	     data(){
    		return { ... } - тут переменные
  		   },
  	     components: {
    		appCar: Car }} - принимаем компонент в переменную-тег, можно также: "app-car": Car или просто Car
	   </script>

111) Родительский компонент -  тот, в котором встроен тег дочернего, напр.: <app-car />

111) Родитель передает данные в компонент через атрибуты :var='text' в пропсы, а дочерний в родитель испускает событие через $emit 
     (при этом родитель слушает событие @..., либо используем связку через v-model:text в родителе и $emit('update:text') в дочернем)
     Также это можно заменить на Vuex

111) Передача параметров из родителя в дочерний компонент:
	<app-car :name="carName" /> - прокинули в дочерний компонент переменную name со значением из переменной carName
     В дочернем:
	<template><div><h2>{{ name }}</h2></div>
	props: { name: String, } - ждем из родителя переменную name, д.б. строка, либо можно просто: props: ['name']

111) Правило: принятые пропсы в дочерних компонентах изменять/удалять нельзя, они их только выводят
     Если нужно изменить/удалить какие-то полученные данные, то:
	
	1) Испускаем команды на изменение из дочернего: @click="$emit('remove', post) - можно передать параметр
	   Родитель слушает событие: @remove="removePost" - и запускает свою функцию, куда параметром попадает принятое значение если оно есть

	   Если компоненты вложены, то пробрасываем событие по цепочке из компонента в компонент вверх до родителя

	      <post-list :posts="posts" @remove="removePost"/> - родитель передавший посты слушает событие remove из дочерних
	      removePost(post){ this.posts = this.posts.filter(p => p.id !== post.id)} - функция родителя удаляющая пост из массива

	      <post-item @remove="$emit('remove', post)"/> - промежуточный компонент слушает remove и сам его испускает
	      <my-button @click="$emit('remove', post)">Удалить</my-button> - кнопка из промежуточного компонента испускает событие remove
		
	2) Запускаем полученную в пропсе родительскую функцию изменения
	     <app-list :deleteNum="funcParent"></app-list> - передаем в дочерний свою функцию изменения переменной
	
	     <button @click="deleteNum">Изменить</button>
	     props: { deleteNum: Function } - получаем из родителя функцию изменения

	3) Используем связку v-model:text в родителе и $emit('update:text') в дочернем

	4) используем Vuex
     		state: { numbers: [1,2,3]},
		mutations: {
		   changeNumbers(state, val){ state.numbers.push(val)}},

		<button @click="$store.commit('changeNumbers', 1)">Добавить</button> - в компоненте изменяем массив
	
111) Проброс данных из дочернего компонента в родительский через $emit:
     1) <button @click="changeName">Изменить имя</button> - кнопка в дочернем компоненте для запуска функции
	changeName(){ - функция в доч.комп., срабатывающая после нажатия кнопки
        	this.$emit('nameChanged', 'Денис')} - выброс события nameChanged с данными

	<app-car @nameChanged="carName=$event"/> - в родителе слушаем событие nameChanged и меняем значение переменной на полученное имя,
                                                   либо запускаем функцию в methods и ловим в ней полученные данные и т.д.
     2) Можно испускать так:
	<app-list v-model:text="message"></app-list> - родитель, обязательно использовать v-model
	
	props: ['text']
	<button @click="$emit('update:text', '111')">-</button> - дочерний изменяет свойство text в родителе

     3) Также удобнее пользоваться общим хранилищем данных Vuex, вместо того, чтобы пробрасывать по цепочке их, можно:
		<div>{{ $store.state.text }}</div> - принимать данные из store
		<button @click="$store.state.text='222'">Изменить текст</button> - изменять данные в store

        Также можно полученные данные из store записать в computed свойство:
		computed: { text() { return this.$store.state.text;} - здесь this нужен
	И выводить: 
	       <div>{{ text }}</div>

111) Также из дочернего можно вызвать родительский метод, если передать его в пропс:
	<app-car :func="changeFunc" /> - передаем в дочерний компонент свою функцию
	changeFunc(){ ... } - функция в родителе

	props: { changeFunc: Function } - в дочернем принимаем функцию в пропс
	<button @click="changeFunc">Изменить</button> - и запускаем ее

111) Проброс события через шину:
	1) export const eventEmitter = new Vue()  - создаем шину в main.js перед инициализацией главного Vue
	2) eventEmitter.$emit('counterUpdated', 3) - в любом компоненте создаем событие, можно пробросить параметр
	3) created() { - системное событие создания компонента (но еще не смонтирован в DOM)
      		eventEmitter.$on('counterUpdated', (num)=>{ this.counter+=num })} - слушаем событие, принимаем параметр, изменяем свойство

111) Простой способ хранения общих переменных и методов в объекте для composition API:
	1) Создаем модуль store.js:
		import {reactive} from "vue"; - для того, чтобы объект менялся реактивно во всех компонентах
		export const store = reactive({ - экспортируем объект
			count: 0,
			increment(){
				this.count++}})

	2) Импортируем реактивный объект в любой компонент:
		import {store} from "../store"; // (webpack или vite? собирает)
		
		<button @click="store.increment">+</button> 
  		{{ store.count}}

     Также можно использовать Vuex (лучше Pinia) для управления общими переменными для компонентов, тогда не нужно пробрасывать события, а просто
     менять общую переменную в компоненте

111) Проброс из родителя во все дочерние через provide-inject
	provide: { num: 111 } - переменная num доступна во всех дочерних
	inject: ['num'] - принимаем num в дочернем компоненте любой степени вложенности

     Если нужно пробросить переменную родителя, то:
	number: 10 - переменная родителя
	provide() { return { num: this.number }} - отправляем ее
	inject: ['num'] - принимаем в дочерних

     Переменные provide-inject по умолчанию не реактивны. Сделать реактивными их можно по инструкции с сайта Vue

111) Между компонентами передавать переменные, запускать методы можно через хранилище Vuex
     Vuex - хранилище переменных, методов к которым имеют доступ все компоненты
	
111) Стили компонента становятся глобальными. Чтобы они были только для компонента:
	<style scoped> ... </scoped> => к тегам добавится атрибут data-v-... и стили будут применяться так: h2[data-v-274929fc] {}

111) Слоты
	Из родителя в дочерний можно передавать верстку, текст в слотах:

	<app-button>Текст</app-button> - передаем

	<template> - компонент AppButton
	  <div>
	     <slot></slot> - принимаем
	  </div>	
	</template>
  
    Текст по умолчанию:

	<slot>Текст</slot> - текст по умолчанию в слоте, если в него ничего не передать из родителя
	
    Именованные слоты:
  
	<app-car>
	   <template v-slot:header>  - именованный слот, сокращенно: #header
	      <h1>Текст</h1>
	   </template>
	</app-car> 	
	
	<template> // дочерний компонент AppCar
  	   <div>
	      <slot name="title"></slot>
	   </div>
	</template>

     Пример вставки компонента кнопки с передачей ее текста в слоте:
	Родитель:
	   <my-button @click="createPost">Добавить</my-button>
	   components: {MyButton},
	   methods: {createPost(){...}}

	Кнопка MyButton.vue:
	   <template>
  	      <button class="btn">
    		<slot></slot> - здесь принимается текст
  	      </button>
	   </template>

111) Валидация:
	Нужно установить пакет vuelidate
        Для Вью 2 установка:
	1) cd vue
	2) npm install @vue/composition-api
	   npm install @vuelidate/core @vuelidate/validators
	3) <input
            @blur="v$.email.$touch()" - показывать ошибку при событии blur
            v-model="email"
            :class="{'is-invalid' : v$.email.$errors.length}" - добавлять класс, если есть ошибки
            type="text">

	   <div v-for="error of v$.email.$errors" :key="error.$uid"> - вывод ошибок в цикле
             <div>{{ error.$message }}</div>
           </div>
	4) Это в компоненте:
	   import { required, email } from '@vuelidate/validators'
	   import { useVuelidate } from '@vuelidate/core'
	   setup () { return { v$: useVuelidate() }},
	   data(){ return { email: ''}},
	   validations() { return { email: {required, email}}}	
	
111) Vuex - общее хранилище переменных для компонентов (сейчас переходят на Pinia)
          
	1) В папке vue если не устанавливали на этапе CLI:
		npm install vuex@2 --save //пишем соответствующую версию для vue
	2) В папке src создаем store/index.js:
	   import Vue from "vue";
	   import Vuex from 'vuex'
	   Vue.use(Vuex)

	   export default new Vuex.Store({  // либо export default createStore({то же самое, что ниже})
	     state: {
		counter: 10}, - это общая переменная для компонентов
	     mutations: { changeNumber(state, val){ state.counter +=1} }, - здесь методы для изменения переменных Vuex, val - если принимаем параметр
	     getters: {}, - здесь для получения переменных Vuex
	     actions: {}}) - здесь для внешних запросов и изменения переменных
	3) В main.js:
	   import store from './store'
	   new Vue({
  		render: h => h(App),
  		store: store  - добавляем эту строку, либо app.use(store).mount('#app')
	   }).$mount('#app')	
	4) Теперь в компонентах доступ к этой переменной:
		$store.state.counter
	5) Также доступ к методам изменения переменных mutations:
		@click="$store.commit('changeNumber', 1)" - можно передать параметр
	6) Доступ к методам получения переменных:
		$store.getters.doubleLikes

111) jsonplaceholder.typicode.com - сайт, возвращающий json объекты с постами, комментариями и пр по fetch запросу

111) Axios - библиотека для асинхронных запросов (подобно фетч):
	0) <my-button @click="fetchPosts">Запрос статей</my-button>
	1) npm i axios - если не установлена
	2) import axios from "axios";
	3) async fetchUsers(){
             try {
          	const response = await axios.get('https://jsonplaceholder.typicode.com/posts?_limit=10')
          	console.log(response)}
             catch (e){
          	console.log('ошибка')}}

111) Фетч загрузку из внешних ресурсов в компонент можно делать на этапе монтирования mounted:
	mounted(){ this.fetchPosts() }

111) Индикатор загрузки:
	1)  isPostLoading: false - переменная
	2) <post-list :posts="posts" v-if="!isPostLoading"/> -показывать посты как только переменная изменится
    	   <h2 v-else>Идет загрузка</h2> - пока грузится показывать индикатор	
	3) async fetchPosts(){
		try { this.isPostLoading = true; - начинаем показывать индикатор
		      фетч запрос }}
		catch (e){ console.log('ошибка')} 
		finally { this.isPostLoading = false } - отключаем индикатор

111) props - ожидаемые из родительского компонента переменные, которые вставляются в дочерний

     watch: - функции срабатывающие при изменении переменной компонента. Название функций должно совпадать с названием переменной:
	      переменная list: 10
	      наблюдающая функция list(newValue){...} - где newValue - новое значение переменной
	      Функции в watch часто используются для асинхронных или затратных операций в ответ на изменение переменных.

	      если наблюдаемая переменная объект, то порядок такой: 
		posts: { handler(val){ console.log(val) },
        		 deep: true}

    computed: - функции подобные watch, но кеширующие результат и делающие перерасчет только при изменении используемой в расчетах переменной.
	        такие функции должны что-то возвращать: return this.arr.filter(...), в верстку вставляются как переменные: {{array}}
		называться computed функции могут как угодно.
		Computed свойства используются для сложных проверок и вычислений, которыми лучше не загружать верстку {{}}
		Когда кеширование не нужно можно использовать methods
	
111) Пример computed свойства:
	<div>{{ length }}</div>
	computed: {
	   length(){
	       return this.posts.length > 5 ? 'Массив больше 5' : 'Массив меньше 5'}}
     Это можно сделать и через метод, но он будет запускаться при каждой перерисовке, а computed свойство кеширует результат 
     и пересчитывается только если его зависимости из компонента изменяются

     сomputed свойство возвращающее return Date.now() не будет изменяться, так как внутри нет зависимостей из компонента
				    return Date.now() + this.num - будет изменяться при изменении num

111) Анимации и переходы с помощью Вью:
	1) Элемент, который будет изменяться оборачиваем в transition и даем имя:
		<transition name="anima"> - имя любое
		   <div>Текст</div>
		</transition>
	2) Вью отслеживает изменения элемента (по transitionstart, transitionend, animationstart, animationend) и навешивает 
	   ему классы по этапам:
	     1) Элемент появляется: anima-enter-active
	     2) Элемент исчезает: anima-leave-active
	     3) Элемент движется от появления к исчезновению и наоборот: 
			anima-enter-from, anima-enter-to, anima-leave-from, anima-leave-to	
	3) Задаем css свойства этим классам и настраиваем переходы

111) Анимация списка производится с помощью transition-group:
	arr: [{id: 1, text: 'qq'}, {id: 2, text: 'zz'}]

	<ul>
      	   <transition-group name="list">	
	     <li v-for="item in arr" :key="item.id"> // key д.б. уникальным
	         {{ item.text }}
	         <button @click="deleteItem(item.id)">Удалить</button>
	     </li>
           </transition-group>
        </ul>

	deleteItem(id){ this.arr = this.arr.filter(elem => elem.id !== id)}

	.list-enter-active,.list-leave-active { transition: all 1s ease; }
  	.list-enter-from,.list-leave-to { opacity: 0; transform: translateX(50px);}

111) Уникальный id можно делать через timestamp:
	id: Date.now()

111) В компоненте можно просто писать статические данные:
	<template>
  	   <h1>Главная</h1>
	</template>

111)    import Home from "@/views/Home.vue";
	const currentView = Home

	<component :is="currentView" /> - загрузка компонента через :is, при этом в currentView можно вставлять компонент

111) Composition API
	0) В таком коде нет this

	1) <script setup> - скрипты пишем так

	2) Не нужно использовать data, methods, пишем код без оборачивания в них:
		let num = ref(10) // реактивная переменная 
		function func(){ return num.value } // в коде к реактивной переменной обращаемся через value, в верстке без value

	2) Переменные оборачиваем в ref.
	   ref принимает аргумент и возвращает его, завернутый в объект ref с .value свойством
	   Использование обертки в виде объекта нужно для отслеживания изменения переменной (то есть для возможности реактивности),
	   в том числе если в переменной находится многовложенный объект.
	   В js изменение переменной мы отслежить не можем, а изменение объекта можем, поэтому переменную помещаем в объект-обертку.

		import {ref} from "vue";
		let flag = ref(false) // такая переменная становится реактивной

	  При использовании в верстке можно обращаться по имени, Вью сам распарсивает value:
		<button @click="num++">+</button>
		<div>{{ num }}</div>

		import {ref} from "vue";
		let num = ref(0)
		
	  В скрипте нужно обращаться по value:
	        <button @click="increment">+</button>

		function increment(){ num.value ++ }

	3) Если нужно хранить в реактивной переменной объект, то вместо помещения его в ключ value объекта ref можно сделать
           сам объект реактивным. Но это не рекомендуется. 
		import { reactive } from 'vue'
		const state = reactive({ count: 0 })

		<button @click="state.count++">{{ state.count }}</button> 

	   В reactive можно помещать объекты, массивы, Map, Set
	   Но нельзя: string, number или boolean
	
	4) Не нужно писать components: {}
	   Достаточно импортировать: import AppButton from "@/components/AppButton.vue";

	5) Пропсы объявляем так:
		defineProps(['title'])
		defineProps({title: String})
		defineProps({title: {type: [String, null]}})
		defineProps({ title: { type: String, default: ()=>{ return 'Текст'}}})

		Выводим как обычно: {{ title }}

		const props = defineProps(['title', 'name']) - можно записать в объект
		console.log(props.title)

	6) К событиям добавляется приставка on: onMounted, также их нужно импортировать:
		import { ref, onMounted, onUnmounted, onUpdated } from 'vue'
		onMounted(()=> console.log(flag.value))

		onMounted(getWeather) - если вызываем одну функцию (здесь getWeather()), то можно исп. короткую запись

	   Все события есть за исключением onCreated, onBeforeCreated

	7) Вычисленные свойства computed - кешируют результат и перекешируют его при изменении зависимых переменных в теле функции:
		<button @click="num++">+</button>
		{{ flag }}
	
		import { computed } from "vue";
		let num = ref(0)
		const flag = computed(()=>{ return num.value > 5 ? 'Больше' : 'Меньше' })
	
	8) Наблюдатели - срабатывают при изменении наблюдаемой переменной:
		import {watch} from "vue";
		watch(num, (newValue, oldValue)=>{ console.log(newValue) })

	9) Обычное испускание событий из верстки осталось:
		<button @click="$emit('add', 1)">+</button> - испускаем в родительском
		<app-button @add="num++"></app-button> - слушаем в дочернем

	   Но испускание из кода скрипта изменилось (раньше можно было просто this.$emit('add', 1)):
		<button @click="send">+</button>

	   	const emit = defineEmits(['add']) - теперь делаем так
		function send(){ emit('add', 1) }

		<app-button @add="num++"></app-button> - в родителе слушаем событие add

	10) Испускание для двустороней привязки с v-model в родителе:
		<input :value="modelValue" @input="$emit('update:modelValue', $event.target.value)" /> - все как в options API
	
		defineProps(['modelValue']) - только пропсы принимаем по-другому

	11) Переменные из родителя во все дочерние через provide-inject:
		import { provide } from "vue";
		provide('message', 123)

		import {inject} from "vue";
		let text = inject('message')

	    Также это можно сделать глобально в main.js:
		app.provide('message', 'hello!')

	    Можно заодно пробрасывать функцию изменяющую переменную в родителе:
		let num = ref(1)
		function increment() {num.value++}
		provide('data', {num, increment}) - пробрасываем в родителе переменную и функцию ее изменения 

		<button @click="increment">+</button> - в дочернем применяем функцию
		let {num, increment} = inject('data')

	12) Собственные директивы:
		1) Локальное объявление:
			const vFocus = { mounted: (elem) => elem.focus() }
			<input type="text" v-focus> - автофокус при показе компонента
		2) Глобальное объявление:
			app.directive('focus', {
				mounted(elem){ elem.focus()}})

	13) Для await не требуется писать async функции, Вью сам все понимает:
		<script setup>
		     const res = await fetch(...)
		     const posts = await res.json()
		<script> 

111) Загрузку разных страниц (компонентов) можно создать через ссылки с хешами. Браузер при изменении хеша не переходит на другие 
     страницы.
     Вью может отслеживать изменение хеша и загружать другое содержание страницы.
     Пример:
	<a href="#about">About</a> - ссылка на другую страницу, при нажатии на нее браузер только меняет хеш url без перехода
	
	const currentPath = ref(window.location.hash)
	window.onhashchange = () => { currentPath.value = window.location.hash } - отслеживаем изменение хеша при нажатии на ссылку

	const routes = { - список хешей и соответствующих им компонентов
  		'/': Home,
  		'about': About }

	const currentView = computed(() => { return routes[currentPath.value.slice(1)]}) - по хешу получаем соответствующий компонент из 
										           массива routes
	
	<component :is="currentView" /> - в верстке грузим содержимое этого компонента

111) Роутинг - позволяет создать SPA приложение (меняется url через history API без перезагрузки, содержание страницы меняется через js)
	1) Роуты пишем в router/index.js 
		const routes = [
  		 {
    		   path: '/', - ссылка
    		   component: Home, - загружаемый компонент
    		   name: 'home' - именование маршрута
  		 },
  		 {
    		   path: '/posts',
    		   component: Posts,
		   name: 'posts'
  		 },
		 {
    		   path: '/post/:id', // id передаем в params router-link с таким же ключом id
    		   component: Post,
		   name: 'post'
  		 },
		 {
    		   path: '/:path(.*)*', // вывод 404 компонента для всех остальных маршрутов. Попадает в {{ $route.params.path}}
    		   component: Error404,
  		 },

        
	2) <router-link :to="{ name: 'home'}">На главную</router-link> - ссылки на имен. маршруты <a href="/">На главную</a>
	   <router-link :to="{ name: 'posts'}">Все статьи</router-link>  после нажатия на ссылку срабатывает router.push(...)

	   параметр :to= принимает такой же объект, с которым работает router.push(...), т.е. в :to можно передавать:
	   :to="{ name: 'post', params: { id: 2}, query: {par : 5}, hash: '#link'  }"  - '/post/2?par=5#link'

	3) <router-view></router-view> - отобразит компонент, соответствующий URL

	4) Для роута:
		 { path: '/post/:id', component: Post, name: 'post' }

	   Ссылка: 
		<router-link :to="{ name: 'post', params: { id: 3 }}">Пост 3</router-link> 
	   	получилось: <a href="/post/3">Пост 3</a>

	   Также можно передавать несколько параметров:
		{ path: '/post/:id'/:name, component: Post, name: 'post' }

	   	<router-link :to="{ name: 'post', params: { id: 3, name: 'cined' }}">Пост 3</router-link> 
	   	получилось: <a href="/post/3/cined">Пост 3</a>

	5) Объект маршрута и методы работы с ним
		route - данные маршрута
		router - методы работы с маршрутом (внутри используется history API браузера)
	
	6) Данные маршрута на примере ссылки: 
		   https://site.ru/post/1?a=1&b=2#qwerty 
	А) В скрипте:
	   <script setup>
	       import {useRoute, useRouter} from "vue-router";
	       const router = useRouter() - методы работы с маршрутом
               const route = useRoute() - объект маршрута

	       route.params.id - 3
	       route.query - {a: '1', b: '2'}
	       route.query.a - 1
	       route.name - post
	       route.path - '/post/1'
	       route.hash - '#qwerty'
	       route.fullPath - '/post/1?a=1&b=2#qwerty'       
	   <script>

	Б) В верстке - все то же самое, но добавляем $ как для composition, так и для options API:
	      <div><button @click="$router.back">Назад</button></div> // go(-1)

	      <h1>Статья № {{ $route.params.id}}</h1>
              {{ $route.query.a }}
	      {{ $route.hash }} и т.д.

	7) В options API в скрипте просто добавляем this: this.$route.id 

	8) Работа с методами роутера:
		import {useRouter} from "vue-router";
		const router = useRouter() - методы работы с маршрутом
	
	        <div><button @click="back">Назад</button></div>

	  	function back(){ router.back()} // то же, что в history API go(-1)

		Методы роутера:
		router.back() - назад go(-1)
		router.forward() - вперед go(1)
		router.push('/') - переход по указанной ссылке, внутри - history.pushState
		router.push({ name: 'posts'}) - переход по именованному маршруту
		router.push({ name: 'post', params: { id: 2}}) - переход на им. маршрут с параметром, т.е. '/post/2' 
		router.push({ name: 'post', params: { id: 2}, query: {par : 5}, hash: '#link'  }) - '/post/2?par=5#link'
		router.go(3) - перейти на три записи вперед, внутри - history.go(3)
	   
	9) Параметры маршрута:
		Если ввели переменную (:var) в маршрут компонента не забываем ее передавать в router-link:
			<router-link :to="{ name: 'home', params: { id : 1000 }}">На главную</router-link>

		path: '/:path(.*)*', - любой маршрут, поместится в переменную $route.params.path
		path: '/users/:id?' - необязательный параметр
		path: '/:id(\\d+)' - id число

		strict: true и sensitive: true - чувствительность к регистру и завершающим /
	
		Можно вкладывать маршруты:
		  {
    		  path: '/post/:id',
    		  component: Post,
    		  children: [{ path: 'profile', component: Profile }]} // сработает при пути /post/:id/profile

		Чтобы вложенный компонент из children отображался нужно в родительский компонент вставить дополнительно:
		  <router-view></router-view> - отображение компонента из children
	
	10) Именованные представления:
		routes: [
    		  {
      		    path: '/',
      		    components: {
        		default: Home, - по умолчанию <router-view /> то же самое, что <router-view name='default'/>
        		sidebar: SideBar - компонент SideBar выведется для <router-view name='sidebar'/>
       		  },},]	
	
		Вывод:
		  <router-view />
  		  <router-view name="sidebar" />

	11) Перенавправления:
	     const routes = [{ path: '/home', redirect: '/' }] - перенаправление с "/home" на "/"
	                     { path: '/home', redirect: { name: 'homepage' } - перенаправление на именованный маршрут

        12) Прокрутка к определенному месту при переходе по ссылкам, напр., чтобы при клике Назад браузер прокрутил на то же место
            где находились раньше:
	      scrollBehavior(to, from, savedPosition) // описание есть в документации
		
111) Импорт вспомогательных функций:
	1) utils/index.js:
		export const func = (val) => { return val.toUpperCase() }	
	
	2) В компоненте используем:
		import {func} from '../utils'
		func('...')
		
111) Выкладка проекта Vue на сервер:
	1) В файле vite.config.js в defineConfig добавляем base: ''
	2) npm run build // появилась папка dist 
	3) npx vite preview // по полученной ссылке можно посмотреть как выглядит собранный сайт
	4) Выгружаем содержимое папки dist на хостинг. 



--------------------------------------------- Оптимизация верстки ---------------------------------------------	

111) Использовать:
	https://pagespeed.web.dev/
	или Lighthous в Хроме
     Там можно смотреть для мобильных устройств и для компьютеров

111) Значения pagespeed.web.dev:
	First Contentful Paint - построение DOM и отображение первого содержимого (тексты, изображения). Для его улучшения можно 
                                 перенести тяжелые скрипты и стили в конец body, но сайт начнет прыгать из-за поздней загрузки стилей
	Total Blocking Time - время, которое страница не будет реагировать на действия пользователя (грузятся скрипты)
	Cumulative Layout Shift - CLS, показатель смещения страницы при загрузке (прыжки содержания страницы). 
				  Происходит из-за применения стилей при медленном интернете, при загрузке картинок если им не даны width, height 
	Largest Contentful Paint - LCP, время отображения самого крупного элемента сайта. Примерно соответствует тому моменту, 
				   когда основное содержимое страницы видно пользователям
	First Input Delay - FID, задержка реакции страницы на первое действие пользователя после загрузки
		                 время реакции страницы на первый клик или скролл

111) Влияние верхних показателей наглядно можно наблюдать в инструментах разработчика включив скорость Slow 3G
	
111) Советы для оптимизации верстки:
	Самое важное для хороших показателей pagespeed первоочередная загрузка критических стилей и адаптированные картинки:

	1) Выделение критических стилей первого экрана (или шапки) и загрузка их в head, а остальное грузим:
	    - либо через preload + onload
	    - или в конце body: 
  
	   <head>
	     <link rel="stylesheet" href="main.css"> - важные стили первого экрана грузим сразу

	     <link rel="preload" href="style.css" as="style" onload="this.rel='stylesheet'"> - такие стили отработают позже
             <noscript><link rel="stylesheet" href="style.css"></noscript> - страхуемся, если в браузере отключен js и не сработает onload
           </head>
		...
	   <link rel="stylesheet" href="style.css"> - тяжелые второстепенные стили грузим в конце
	   </body>

	Также можно грузить стили первого экрана в head как инлайновые, без лишнего http запроса
	   Их удобно писать в файл и вставлять в head через php:
	   <head>
             <style>
		<?= file_get_contents('main.css') ?>
	     </style>
	  </head>
 
	2) Картинки в формате webp
	   Есть php функция imagewebp превращающая картинку в webp на лету при загрузке в админке

	   IE не поддерживает webp, можно скармливать так:
	   <picture>
    	      <source type="images/webp" srcset="1.webp"> - если эта не поддерживается
              <img src="1.jpeg"> - то используем эту
	   </picture>

	3) кеширование страниц на сервере (но это к верстке не относится)

	4) Содержимое сайта начинает дергаться если медленный интернет или тяжелая картинка и структура построилась и вывелась, 
	   а картинка еще не загрузилась, но начинает появляться. 
	   Для избавления от дергания:
	   1) Задаем картинке определенные width, height (но что тогда делать для других экранов?)
	   2) Либо помещаем ее в контейнер и задаем ему размеры (картинке при этом max-width: 100% и пр.)
	   3) Либо делаем ее фоном background-image контейнера + background-size: cover; (если картинка не относится к содержимому страницы)

	5) Если подключаем свои шрифты, то при первом заходе на страницу (пока они не закешировались) будет небольшой скачок шрифтов
           происходящий из-за перестроения вспомогательного шрифта на загруженный. Сгладить это можно:
	   1) Убрав display=swap - теперь сразу будет показываться загружаемый шрифт без перерисовки из вспомогательного
	   2) Подобрав вспомогательным наиболее похожий шрифт:  font-family: 'Lora', "Georgia", sans-serif;
	   3) Также правильной практикой является локальная загрузка шрифтов (gwfh.mranftl.com - гугл шрифты в woff2 формате)
	   4) Можно заранее подгружать шрифты первого экрана в кеш:
		<link rel="preload" href="lora.woff2" as="font" crossorigin /> - загрузили в кеш
		<link rel="stylesheet" href="fonts.css"> - тут команды @font-face на использование загруженного шрифта
	   
	6) Если картинок много и объемные, то можно им задавать loading='lazy' - браузер сам будет решать когда их загружать


--------------------------------------------- Node.js ---------------------------------------------	

111) Язык js работает в браузере на движке V8.
     Node.js позволяет запускать js отдельно от браузера (внутри тот же движок V8)
     В node.js встроены команды работы с файлами, их нет в обычном языке js, так как это браузерный язык и для безопасности js не может
     работать с файлами
     Также в node.js встроены другие, отличные от языка js команды

111) Таким образом node на сервере позволяет создать http сервер работающий на языке js (js работает вне браузера!)
     
111) npm - пакетный менеджер node.js. Для работы использует package.json

111) Пример запуска файлов js вне браузера:
	1) Создаем script.js с console.log(111) или любым js кодом
	2) В командной строке пишем:
		 node script.js => выводится 111

111) В node.js также как в браузере работают модули ES6 (то есть их подключает движок V8 ноды), но для их работы нужно
     в файле package.json прописать: { "type": "module" }
	
	Пример 1:
	export default [1,2,3] - модуль ES6
	import arr from './car.js' - подключаем модуль

	Пример 2:
	export const a = 1
	export const b = 2 - модуль ES6
	import {a, b} from './car.js' - подключаем модуль

	Пример 3 (многие пакеты являются common модулями и отдают переменные через module.exports):
	npm i lodash - в командной строке
	let lodash = require('lodash') - тут подключаем через common модуль (убрать строку "type": "module" из package.json)
	console.log(lodash.sum([1,2,3])) - пользуемся библиотекой

111) Если подключаем модуль в html через браузер нужно писать так:
	<script type='module'>
	   import arr from './car.js'
	</script>

     Для подключения модуля js в файл js просто пользуемся модулями как есть:
	export default [1,2,3] - модуль ES6
	import arr from './car.js' - подключаем модуль
     Получается, подключения выполняет сам движок V8 браузера при парсинге кода скриптов

     Сборщики, напр., webpack просто проходятся по файлам js и собирают весь код с модулями в один файл. То есть там нет магии, 
     сборщики выполняют обычные подключения ES6 модулей, но собирают все в нужном порядке в 1 файл.

111) npm устанавливает пакеты в папке с package.json
     если этого файла нет, то установит пакет в ближайшей родительской папке с этим файлом.
     поэтому не забываем создавать в папке проекта package.json (команда npm init) если будет пользоваться npm	

111) package.json - установленные пакеты и возможные версии установки
     package-lock.json - установленные пакеты, их версии и контрольные суммы

111) Получается, когда пишем в консоли npm run dev, npm run build, то мы обращаемся к командам scripts из package.json, эти команды
     как будто "запускаются в консоли" и нода их выполняет:
	"dev": "vite",
	"build": "vite build"

     То есть команда npm это то же, что команда composer.
     npm обращается к package.json и запускает команды из scripts (вместо npm run dev можно прописать в консоли vite - будет то же самое, так как они связаны)
     composer обращается к composer.json и то же может запускать команды из scripts

     Таким образом можно писать в composer.json/scripts php команды и запускать их через композер

111) Глобальные пакеты npm устанавливаются через флаг -g (npm install -g package-name) в Owner\AppData\Roaming\npm\node_modules в Windows, 
                       доступны на всем компьютере
     Локальные пакеты устанавливаются в папку проекта /node_modules/.bin/ (npm устанавливает пакеты локально по умолчанию), 
                      доступны только в проекте

111) npx (Node Package Execute - Выполнение пакета Ноды) - программа для запуска пакетов с установкой их в папку кеша npm без прописывания 
         пакета в package.json
	 Пакет скачается в папку Owner\AppData\Local\npm-cache\_npx
         Полезна для одноразового использования пакета. Является встроенным дополнением к npm

     npx перед скачиваем пакета проверяет есть ли он в node_modules, если есть запустит оттуда, если нет - скачает в папку кеша npm.
	
     npx your-package-name - запуск пакета
     npx gulp - команда на запуск гальпа без его установки в node_modules

111) js пакеты устанавливаются командой npm в папку node_modules по списку из package.json
     php пакеты устанавливаются командой composer require в папку vendor из списка composer.json

     
=============================================  SEO оптимизация ========================================================

111) Текст написанный от лица специалиста ранжируется выше (фото специалиста, должность или специальность, лицензия, университет, организация)
     Подойдут даже вкрапления в основной текст мнений таких специалистов с их фото.
     То есть текст информационной статьи для хорошего ранжирования должен быть от лица эксперта (имя, должность, фото) или с их блоками.
     Более подробно внизу: EAT факторы 
     Ссылки на статьи с авторитетных сайтов повышают авторитетность самой статьи.

111) Яндекс: "Если алгоритм определит, что контент создан для влияния на поисковую систему и повышения релевантности, 
     позиции этого документа в выдаче могут ухудшиться. Алгоритм проверяет все страницы, которые обходит робот, поэтому, 
     когда проблема будет устранена, а страница сайта переиндексирована, позиции сайта восстановятся."

111) Решил поменять url? Делай редирект 301, будут те же места в выдаче

111) Разнообразие в title и сниппете - дает кликабельность:
	- цифры (Щи, рецепты с фото: 678 рецептов)
	- перечисление интересных блоков сайта

111) Ссылки в статье на авторитетные источники: [1] - ссылка на википедию

111) Разнообразие содержания (интерактивность, калькуляторы, картинки, видео, User Generated Content, комментарии, таблицы, 
     цены, советы, даты, эмодзи и пр.)
     Проще говоря - должно быть САМОМУ интересно читать страницу.
     Также важны: репутация и возраст организации, отзывы, ассортимент
     Разнообразие также хорошо работает для title

111) Есть готовые встраиваемые комментарии от ВК - виджет Комментарии для ВК

111) Для релевантности страницы запросу также полезны ссылки с этой страницы на дополняющие материал другие страницы
     Задача - максимально полно ответить на запрос пользователя

111) На коммерческих страницах важна конверсия - действия пользователя
     На информационных - максимальная полнота ответа и погруженность пользователя в материал

111) Пользователю удобнее при чтении чуть утолщенный шрифт 400

111) Статьи нужно писать под "интент" запроса. Если пишем статью под запрос "как определить социотип", то скорее всего пользователю
     интересны способы определения социотипа и нужна статья с несколькими способами (тесты, типирование, по внешности и пр.)
     + задать title как: "5 способов как определить социотип"

111) Два самых важных поведенческих фактора:
	1) CTR - кликабельность сайта на поиске (вызывает доверие, к нему возвращаются, узнаваемость, сразу переходят на него и пр.)
           Делаем хороший title, сниппет, фавиконку - они должны быть "продающими" 
	2) Правило "последнего клика" - удовлетворить запрос посетителя, чтобы больше не искал

111) Если поисковик увидит попытку манипулирования выдачей - приведет к пессимизации.
     Основная задача - сделать, чтобы пользователю было интересно и удобно.

111) Трендовые запросы/товары можно отслеживать в "Истории запросов" в вордстате

111) Чтобы поисковик понял насколько хорош сайт нужно, чтобы он набрал статистику по нему.
     Для нового сайта нагоняем посещаемость, например через директ => появляется статистика, сайт начинает быстрее продвигаться, чем если
     не нагнать трафика.

111) telderi.ru - биржа продажи сайтов, доменов, тг каналов, вк групп, ютуб каналов и пр.

111) Новые сайты во время разработки и наполнения материалом лучше закрывать от индексации, чтобы не набрать плохую статистику:
	<meta name="robots" content="noindex, follow" /> - не индексировать, но учесть ссылки. Советует этот способ в начале разработки

	<meta name="robots" content="noindex, nofollow" /> - не индексировать и не учитывать ссылки

	<meta name="robots" content="none" /> - запретить весь сайт к индексированию

111) Для индексации сайта Яндексом достаточно добавить на него Яндекс метрику. Притом сайт проиндексируется даже если стоит запрет
     <meta name="robots" content="noindex, nofollow" />

111) Хлебные крошки полезны для внутренней перелинковки

111) rds bar - расширение хром для показа информации о САЙТЕ (сколько проиндексировано, тиц и пр.)

111) SEO PRO Extension - расширение хром для быстрого просмотра seo сведений о СТРАНИЦЕ: title, заголовки, ссылки, альты и пр.

111) netpeak checker - программа для seo анализа сайтов и множества его параметров

111) comparser, netpeak spider - программы для сканирования ВСЕГО сайта, поиска битых ссылок, редиректов, ошибок, дублей, 
                                 проиндексированных страниц, мусора в индексе, тяжелых или долгих страниц, незаполненных h1 и пр.

111) Сделать редирект 301 Moved Permanently с: 
	http://site.ru 
	http://www.site.ru
	https://www.site.ru

     на https://site.ru

     Команды в htaccess:
	RewriteCond %{HTTP_HOST} ^www\.(.*)$ // c www на без www
    	RewriteRule ^(.*)$ https://%1/$1 [L,R=301]

    	RewriteCond %{SERVER_PORT} !^443$ // с http на https
    	RewriteRule .* https://%{SERVER_NAME}%{REQUEST_URI} [R=301,L]

111) 301 редирект (постоянный) переносит вес страницы, позиции страницы не упадут, используем только его!
     302 редирект (временный) вес НЕ переносит

111) Если меняем структуру сайта и нужно переносить веса страниц с сохранением позицией страниц, 
     то просто делаем 301 редиректы в htaccess со старых страниц на новые

111) Смена домена, движка, структуры с сохранением позиций:
	1) Новый домен лучше покупать без истории, либо с такой же прошлой тематикой
	2) Сначала изменяем домен (в панели Бегета или в htaccess 301 редирект). 
	3) ЖДЕМ 4 недели. Поисковик приклеивает старые адреса на такие же адреса нового домена
	4) Меням движок или структуру + пишем редиректы со старой структуры страниц на новую
	5) Изменяем главное зеркало в панели вебмастера и настраиваем счетчики

111) Важен возраст страницы со статьей. Если изменили структуру адреса, но не поставили 301 редирект, то страница надолго упадет,
     пока не накопит ПФ статистику 

111) При смене адреса страницы без 301 редиректа есть примерно месяц, чтобы настроить его с возвращением позиций

111) На все сайты ставим https, в 99% подойдет бесплатный Let`s Ecrypt

111) Мусорные страницы, страницы с примененным фильтром (фильтр добавляется в адрес), админ-панель и все другое, 
     что не должно быть в индексе - ставим каноникал, закрываем/удаляем.
     Такие страницы разбавляют ценность других страниц сайта и подают сигнал поисковику о некачественности сайта

111) Способы закрыть страницы от индексации:
	0) https://suay.ru/?p=790 - хорошая статья
	1) <meta name="robots" content="noindex, nofollow" /> - самый рабочий способ. Не индексировать и не учитывать ссылки страницы
	2) <meta name="robots" content="noindex, follow" /> - не индексировать, но учесть ссылки страницы
	3) robots.txt: // проверить правильность robots.txt в Вебмастер => Инструменты => Анализ robots.txt
		       // robots.txt команды могут игнорироваться и не сработать
		       // Google игнорирует этот файл, поэтому правильнее пользоваться также meta name="robots"
		Disallow: /admin  - закрыта страница https://site.ru/admin  
		Disallow: */folder/* - закрыты все адреса с включением в строку /folder/
	4) Если есть страницы-дубли, совпадающие по содержанию с другой основной страницей, то задаем дублям в head:
		<link rel="canonical" href="http://www.example.com/blog"/> - указываем страницу которая должна остаться в индексе
	5) Disallow: /*&* - закрыть от индексации страницы с несколькими параметрами. Адрес перед гет параметрами должен заканчиваться на слеш
	   Disallow: /*?* - второй вариант закрытия страниц с гет параметрами (в статье пишет осторожно применять)
	   Disallow: /? - третий вариант. Все Disallow для Яндекса являются рекомендациями, страницы могут попасть в индекс
           Disallow: /*?*order=   - - закрыть от индексации страницы с параметром order
	6) Добавить в файл .htaccess строку:  Header set X-Robots-Tag "noindex, nofollow"  // запрет индексации всего сайта
	   Вы также можете разместить файл .htaccess с этой строкой в любой папке, чтобы запретить индексацию только её.

111) Проверить разрешен ли адрес к индексации: 
	Вебмастер => Инструменты => Анализ robots.txt => Разрешены ли URL?

111) Исключаем ненужные страницы также из sitemap.xml

111) Быстрая индексация новой страницы:
	Вебмастер => Индексирование => Переобход страниц => Вводим адрес страницы

111) rel="canonical" - рекомендация роботу какую страницу считать канонической. Применяется, если в выдачу могут попасть страницы 
                       с гет-параметрами, utm-метками. Рекомендуется указывать ссылкой на саму себя на всех страницах 
                       на которых возможны гет-параметры (страницы фильтров) и utm-метки, также на страницах с пагинацией.
                       В тесте Александрова правильный ответ: проставить на всех страницах саму на себя, а на неканонических на осн. страницу

        Для Ларавель:  <link rel="canonical" href="{{ url()->current() }}" />

        Вся статистика и поведенческие факторы со страниц-дублей будут применяться к канонической странице. 

	Указывать в каноническом адресе только абсолютный путь.

	Атрибут применяется для страниц у которых мозможны дубли:
	   https://example.com/blog?size=5&price=500 - страница с примененным фальтром
	   https://site.ru/?utm_source=yandex&utm_medium=cpc - ссылка с utm-метками 
	   https://example.com/folder/blog - та же статья находится по другому адресу
	   https://site.ru?page=1 - страница пагинации

	Применение:
	   Например, есть страница https://site.ru/page
           У нее есть фильтры, срабатывающие с добавлением get-параметров, ссылки с utm-метками и пр.
           Пишем в head https://site.ru/page команду: 
		<link rel="canonical" href="https://site.ru/page"/>
	   Она будет стоять на всех дочерних ссылках с гет-параметрами и utm-метками => вес будет отдаваться канонической ссылке и в индексе
           не будет дублирующих ссылок

        Также rel="canonical" ставят на странице с пагинацией указывая канонической страницу "Показать все"
        То есть site.ru/goods имеет пагинацию: site.ru/goods?page=1, site.ru/goods?page=2 и пр., 
        Ставим на ней: <link rel="canonical" href="https://site.ru/all-goods"/>
	Теперь все страницы с пагинацией и страница goods будут отдавать вес основной странице all-goods

111) rel="nofollow" - команда поисковику не переходить по ссылке и урезать ссылочный вес
	
     <a href="..." rel="nofollow">Ссылка на говносайт</a>

     rel="nofollow" ссылочный вес урезается для указанной страницы, но сама страница-донор все равно теряет свой вес. 
                    Однако если ссылка на ценный или тематический источник, то полезно для сео. 
                    Можно устанавливать внешним ссылкам с комментариев или подозрительным ссылкам.	

111) Важно! Несуществующие страницы должны отдавать 404 код, а не перенаправляться на главную.

     Потому что кто-то разместит ссылки на твой сайт в интернет: https://site.ru/casino, https://site.ru/porno, по ним 
     будет перенаправление на главную страницу и она получит плохие ссылочные анкоры, а адреса еще и влетят в индекс

111) Удалить из индекса страницы можно только если:
	- они отдают код 404 
	- или запрещены к индексации <meta name="robots" content="noindex, nofollow" />

	Удаление:
	- либо ждем
	- либо: Вебмастер => Инструменты => Удаление страниц из поиска

111) Проверять валидность верстки: validator.w3.org
     Проверить ошибки js в консоли браузера
     Проверить скорость сайта по Page Speed или Lighthouse (показатели д.б. в зеленой зоне)

111) После создания сайта можно отслеживать время загрузки страниц и проблемные страницы в Метрике:
	Метрика => Отчеты => Мониторинг => Время загрузки страниц

111) Для мобильной версии НЕ рекомендуется делать субдомен m.site.ru

111) Lighthouse в хром - проверка оптимизирована ли страница для мобильных устройств
	                
111) !!! Слева делать неподвижное fixed меню, а контейнер для текста делать узким. Это удобно для чтения и пользования.
         Как в ВК и vc.ru

111) Микроразметка Schema.org позволяет поисковикам лучше понимать содержимое сайта, а также улучшить сниппеты страниц 
     в поисковой выдаче.

     Описание:
	https://yandex.ru/support/webmaster/schema-org/	

     Посмотреть разметку на крупных сайтах можно в SEO PRO Extension во вкладке Shema

     Примеры: 
	<span itemprop="streetAddress">Льва Толстого, 16</span>
	<h1 itemprop="name">Iphone 6 plus 16 GB</h1>
	<span itemprop="price">36990</span>

     На текущее время Яндекс принимает разметку:
	- Адреса и организации
	- Вопросы и ответы
	- Товар, цена, описание, рейтинг, наличие, отзывы, картинки и пр. - обязательно для интернет-магазинов
	- Изображения (для яндекс картинок)
	- Программы, фильмы, рефераты, рецепты

     Также есть микроразметка:
	- для статей (автор, дата, заголовок, картинка и пр.)
	- "звездочки"

     Микроразметку можно проверить валидатором в Вебмастере и Гугле

111) UTM-метки - гет-параметры, встраиваемые в ссылку, позволяющие определить с какой площадки, по какой рекламе к вам пришел посетитель
                 Можно посмотреть такие переходы в Метрика => Отчеты => Источники => Метки UTM

     Например: гугл, яндекс, вк, ютуб, бесплатный переход, директ, рассылка, банер

     https://site.ru/page?utm_source=yandex&utm_medium=cpc - посетитель пришел с яндекса из контекстной рекламы (cost per click, плата за клик)

     Чтобы в индекс не попадали страницы с utm метками и не создавали дубли
     на странице https://site.ru/page устанавливаем:  <link rel="canonical" href="https://site.ru/page"/>

     Параметры:
	utm_sourсe - название рекламной площадки (utm_source=google)
	utm_medium — тип рекламы (utm_medium=email, banner, cpc, search и т.п.)
	utm_campaign — название рекламной кампании. (utm_campaign=mebel_dlya_doma) Директ ставить автоматически: utm_campaign={campaign_id}
	utm_term — ключевое (поисковое) слово. Директ ставит автоматически: utm_term={keyword}
	utm_content - идентификатор объявления позволяющий различить объявление 

     wpnew.ru/utm-generator - генератор utm-меток

111) title - в последнее время поисковики хорошо относятся к длинным заголовкам. Можно делать 70-130 символов с разным содержимым.
             в  title д.б. самое значимое предложение со всей страницы
             title - играет важную роль в ПФ, так как влияет на кликабельность сайта

    В title использовать главный ключ не больше 1-2 раза
    Первые слова в тайтле имеют самый большой вес

     В title и description включать разнообразное содержимое для кликабельности. Его можно подсмотреть в объявлениях директа:
	- цифры (цены, сроки, количество, гарантия и пр.)
	- знаки препинания, палочки, проценты
	- даты
	- английские слова	
	  
     В title чем ближе к началу, тем значимее место. Самые ценные места первые 13 слов! 
     Нужно использовать точное вхождение ключа сразу, а не через несколько слов.

     Для title работают правила разделения SEO пассажей знаками препинания.

     Точка разделяет ключевики в title между собой:
        Шины Гудиер: цены, отзывы, купить. Интернет-магазин шин. => создается группа "цены шины Гудиер", но не группа "магазин шин цены"
   
     Пример ошибки:
	"Квартиры, студии, недвижимость. Купить в СПБ" - поместили использующиеся вместе ключевики в разные группы.

     То есть ключевые слова часто использующиеся ОДНОВРЕМЕННО используем в пределах одного предложения.  
     Если нужно структурировать но не разделить ключи, то применяем запятые, тире, двоеточия.

     Так, можно синонимы помещать в разные предложения title через точку, так как пользователи не вводят в запрос оба синонима.

     Что Яндекс считает синонимами можно смотреть по выделению слов в выдаче.

     Точка разделяет, а |, -, : не разделяют ключевики в разные группы. Описание пассажей есть внизу.  

111) description - 150-240 символов с пробелами. Не копировать текст из статьи, а разбавить ключевики склонениями, словоформами.

     В description повторение ключа не более 3-4 раз.

     В начале description лучше не повторять сразу ключевик как в title, а использовать словоформы главного ключа, другие ключевики и пр. 
     - чтобы сниппет был читаем на поиске без тавтологии.

     Гугл обычно использует description в сниппете. Яндекс может использовать другой текст, часто это кусок текста статьи 
     где первый раз встречается ключевик.

     Для формирования собственного сниппета для Яндекса помещаем ключ в первый абзац и окружаем текстом, который хорошо будет смотреться
     в сниппете поиска, также, повторяем эти предложения в description.

111) SEO пассаж - последовательность слов, отделенная некоторыми знаками препинания или тегами html. Важные сочетания ключевых слов
                  желательно использовать внутри одного пассажа.

     Разделители пассажей:
	1) Точкой, когда за ней следует пробел и слово с заглавной буквы. То есть:
              "Услуги социон. типирования" - точка НЕ разделяет пассаж
	      "Соционическое типирование. Услуги психолога" - точка разделяет пассаж.
	2) Троеточие
	      "лучшие окна... пластиковые" - здесь слово "окна" разделено от "пластиковые"
	3) Восклицательный знак
	4) Вопросительный знак
	5) <p>
	6) <div>
	7) <h1> - <h6>
	8) <ul>, <ol>, <li>
	9) <table>, <tr>, <td>

      Другие символы (- , : % и пр.) и теги не являются разделителями:
	   "Услуги психолога | Психологические консультации" - в один пассаж входит "Психологические услуги"

      Важно не переоптимизировать пассажи ключевыми словами!!!

111) Смотрим что показывает поиск при запросе ключевика. Если все страницы информационные, то значит пользователи по такому ключу искали
     информационные сайты и продвинуть свою коммерческую высоко НЕ ПОЛУЧИТСЯ. Работает и наоборот: в коммерческой выдаче не удастся высоко продвинуть
     информационную статью.

     Например: "планировка квартиры" - весь поиск состоит из информационных страниц и свою коммерческую по ключевику "планировка квартиры"
     мы в них не пропихнем. 
	Решения:
	 - продвигать сочетание слов когда ищут именно услугу, например: "заказать планировку квартиры"
	 - создать информационную статью "планировка квартиры" и оттуда заманивать клиентов на коммерческий раздел этого же сайта

111) Если в запросе пользователя есть намерение купить, скачать, посмотреть и пр., то на странице должны быть кнопки
    или возможность воплотить намерение. 
    Статья "посмотреть Аватар" не будет в топе без возможности посмотреть этот фильм на этой странице, потому что последний клик пользователя
    никогда не будет на этой странице.

111) Советы для h1:
	1) только 1 раз на странице
	2) должен быть на каждой странице
	3) h1 должен быть выше других h
	4) Не желательно полностью копировать title

     Советы для заголовков вообще:
	1) h2, h3 используем только внутри контента для его структурирования. 
           Остальное стилизуем размерами, стилями (Рубрики, Комментарии, Архив)
     	2) Иерархичность и последовательность заголовков
	3) Размеры шрифта должны совпадать с иерархичностью заголовка (h1 - самый большой, h2 - меньше, h3 - еще меньше)
	4) Только одно предложение в заголовках + не должны быть длинными
	5) Опасно вставлять в каждый заголовок ключевые слова (не спамим заголовки, должно смотреться естественно)

111) Советы для img:
	1) ключ в окружающем тексте помещаем поближе к картинке - он важнее alt 
	2) читаемые названия картинок с ключами. Не больше 1 ключа
	3) в названиях картинок лучше не использовать "купить", "заказать" - это понимается как манипулирование выдачей
	4) слова в названии делить дефисом
	5) в названиях используем только латиницу
	6) не спамить альты и не использовать только ключевики - робот это понимает
	7) alt и title делаем разными для большего охвата ключевиков и словоформ
	8) за картинку могут подать в суд
	9) желательно удалять exif у картинки, можно сделать в фотошопе
	10) если картинка не уникальная - ничего страшного
	11) для ссылок на картинки у яндекса можно настроить свой sitemap.xml
	12) проверить какие картинки сайта в Я-Картинках или Гугл картинках: site:rodnovspb.ru

111) Видео на Ютуб:
	1) Можно размещать на ютубе, указывать там заголовок, описание, ставить обратную ссылку на свой сайт
	2) В видео употребять ключевые слова голосом - роботы понимают голос

111) Неперсонализированную выдачу можно проверять в режиме "Инкогнито"

111) Витальный запрос - пользователь ищет определенный сайт, организацию, поисковик покажет их сайт первым в выдаче: 
                        "мир печатей Челябинск", "кроссовки Адидас", "кредит сбербанк"

111) В продвижении коммерческих запросов важно:
	1) Надежность организации (реквизиты, контакты, отзывы, возраст организации, верификация в Я-Справочнике и пр.)
	2) Качество сайта
	3) Удобство сайта

111) Что важно на коммерческих страницах:
	- присутствие в Я-Справочник (синяя галочка), в Google Мой Бизнес (добавить ключевик вместе с названием компании)
	- цены, их изменение, наличие товара
	- контакты, страница "Контакты" и shema микроразметка на ней
	- ассортимент
	- телефон, его тип (по приоритету: 8800, городской, мобильный), количество контактов, заказать обратный звонок
	- отзывы в Маркет, Картах
	- полное описание товара (цена, изображение, название, производитель, размеры, вес, мощность и пр.)
	- варианты доставки и варианты оплаты
	- наличие страницы "О компании"
	- почта на домене сайта
	- обратная связь
	- все товары доступны в пределах 3-х кликов
	- регион сайта, город сайта
	- удобный поиск по сайту (поиск от Яндекса: wpnew.ru/blog)
	- акции, скидки
	- отзывы к товарам, отзывы о компании
	- упоминание о компании на других площадках, соцсетях
	- группы в соцсетях
	- https
	- онлайн-консультант
	- ссылки

111) Задарма - аренда номеров с приемом звонков на приложении в телефоне

111) Информация на странице Контакты:
	- почта на своем домене
	- форма обратной связи
	- телефон
	- полный почтовый адрес
	- время работы
	- разные способы связи
	- Яндекс/Гугл карта
	- реквизиты
	- микроразметка shema
	- соцсети

111) Мультиязычность и hreflang - astra.red/rukovodstvo-po-hreflang

111) Операторы поиска:
	"купить двери" - фиксирует фразы как в директе (словоформы и порядок могут быть разные)
	"!купить !двери" - фиксирует форму слова (падеж, число, время) и количество слов
	"купить двери +деревянные +в Москве" - искать документы, в которых обязательно содержатся словя "деревянные" и "в"
	купить двери -металлические - искать документы, в которых нет слова "металлические"
	"следуй за * кроликом" - все страницы с этой фразой, включая те, где вместо звездочки размещено любое слово
	социотип | психотип - оператор "или": фраза содержит либо то, либо другой слово
	site:социон.рф типирование  - поиск только по указанному сайту
        site:социон.рф - какие страницы в индексе по сайту и его поддоменам
        host:социон.рф - какие страницы в индексе по сайту (без поддоменов)
        url:социон.рф - проверка в индексе ли страница
	url:социон.рф типирование - поиск фразы по указанному адресу
	domain:com типирование - поиск по определенным доменным зонам
	inurl:socionika социотип - в любой части url содержится socionika
	lang:en socionics - фильтрация страниц выдачи по нужному языку (de, en, fr, ru)
	date:ГГГГММДД - поиск страниц по дате:
		        фестиваль date:20231107 - статья опубликована 7 ноября 2023 г.
			фестиваль date:>20231107 - статья опубликована после 7 ноября 2023 г.
		        фестиваль date:20231101..20231107 - опубликована между 1 и 7 ноябрем 2023 г.
		        фестиваль date:202311* - опубликована в ноябре 2023 г.
		        фестиваль date:2023* - опубликована в 2023 г.

	mime:pdf паспорт - поиск файлов нужного типа (pdf, xls, ods, rtf, ppt, odp, swf, odt, odg, doc)

        В Гугле также как в Яндексе работают операторы: +, -, "", *, |, site: . 

        Также в гугл есть свои операторы ТОЧНОГО вхождения слов в указанную часть. 
	Можно получить наиболее подходящие страницы + комбинировать операторы
	Пример:
	 соционический тест вайсбанда - 17 600 результатов
	 allintext:соционический тест вайсбанда - 1 180 результатов

	intitle:соционикой  - поиск страниц у которых в title есть слово "соционикой"
	allintitle:соционический тест - поиск страниц у которых в title есть вся фраза
	inurl:соционика - аналогично inurl: в Яндексе. Ищет страницы в любой части url которых содержится соционика
	allinurl:типирование соционика - в любой части url содержатся точные слова
	intext:роднов - в основном тексте страницы содержится точное слово "роднов"
	allintext:денис роднов - в тексте страницы содержатся точные слова из словосочетания

	Операторы  intitle, inurl и intext удобно использовать для поиска страниц для построения ссылочной обратной массы.
	Пример: 
	    intext:соционика intitle:форум - найти страницы со словом "форум" в title, а в тексте которых встречается "соционика"
	    allintext:типирование  site:социон.рф - найти страницы содержащие слово "типирование" на сайте "социон.рф"


111) Все таки нужны кнопки на страницах, вызывающие конверсию. Пользователям лень копировать email и легче нажать на кнопку

111) Если на сайте много малоотличимых страниц, то они становятся Малоценными и вылетают из индекса.
     Получается этот фильтр применяетсят в зависимости от количества таких страниц относительно общего количества страниц.

111) Внутренняя перелинковка - клики по внутренним ссылкам прокачивают страницу по анкорам ссылок.
                               особенно это заметно на НЧ запросах.
                               нам нужны клики по таким ссылкам => каждый клик по анкорам прокачивает страницу перехода.

     Создаем удобное меню - с хорошими анкорами, чтобы пользователи переходили по ссылкам меню.
 
     Поэтому также делаются хлебные крошки.

     Ненужные категории нужно убирать, чтобы не было расфокусировки внимания на другие ссылки. 

     Внутренняя перелинковка помогает создавать быстрые ссылки в Яндексе.

     Если по запросу показывается неверная страница => создаем на ней ссылку с нужным анкором на верную.

     Способы создать внутреннюю перелинковку:
	1) Раздел с картой сайта (для поисковика)
	2) Меню в шапке
	3) Сайдбар
	4) Футер
	5) Похожие товары/услуги
	6) Фильтры
	7) Ссылки в тексте

     Важно! Ссылки должны быть ДЛЯ пользователей, чтобы по ним КЛИКАЛИ.
     То есть не перегружать сайт лишней перелинковкой, так как невостребованные ссылки снижают фокус внимания на важные ссылки.

111) В меню, навигации выводить самые популярные запросы, остальные прячем в "показать все" и подобное.

111) Посмотреть какую страницу поисковик считает подходящей под запрос:
	соционика типирование site:социон.рф - покажется список релевантных в порядке убывания

     В сниппетах этой выдачи жирным выделены слова, которые поисковик считает релевантными запросу =>
     именно с этих слов нужно делать ссылку на нужную страницу

     Также релевантные страницы можно смотреть в Коллекторе: 
	1) Выбираем запрос
	2) Анализ релевантных страниц

111) В ссылках статей 80% анкоров - с использованием ключевых слов, 20% - здесь, по ссылке, на этой странице
     
111) Избавляться от ненужных ссылок, чтобы они не разбавляли  вес других ссылок
     То есть сохраняем только те ссылки, по которым будут клики.

111) В фильтрах хорошей практикой является:
	1) Если запрос встречается в Вордстате ("Диван белый", "Холодильник LG"), то при применении фильтра переводим на отдельные страницы
           То есть фильтр как кнопки-ссылки: "Белый", "Черный", "Красный" и ведут на отдельные страницы
	2) Если не встречается в вордстате ("от 10000 до 20000", "В наличии", "4 ядра"), то фильтруем на той же странице 
           гет параметрами + rel='canonical' на страницу без гет-параметров
	3) Также смотрим что происходит у конкурентов. Если в выдаче по этому товару Яндекс хорошо относится к каким-то фильтрам,
           то используем их

111) Советы:
	0) Важность и вес ссылки определяются по действиям пользователя - как часто кликают. Если не кликают, то анкор ничего не значит.
	1) Не обращать внимания на советы по кольцам перелинковок, количеству ссылок на страницу с других страниц и пр.
           Без кликов по ссылкам - количество ссылок на страницу и веса - ничто.
        2) Простое правило: ссылки на самые важные разделы помещаем в меню, сайдбар и т.д., чтобы на них кликали
	3) Стремиться к максимальной кликабельности ссылок
	4) При наведении меняем цвет ссылки или подчеркиваем
	5) Если разбавляем анкоры, то используем ВЧ слова, которые заберут у ключевика меньше веса: "сайт про соционику", 
           а не "портал про соционику". То есть во втором случае "портал" как НЧ заберет больший вес всего анкора на себя.
           Простое правило: редкое уникальное слово не по теме ссылки ЗАБЕРЕТ больший вес анкора на себя.
	   Также работает и в положительную сторону: редкое ключевое слово из ссылки заберет больший вес на себя.
	6) Если в статье две ссылки на одну страницу, то учитывается первая ссылка
	7) Для поисковика если у ссылки нет переходов, то это СПАМНАЯ ссылка

111) Самые востребованные (популярные) ссылки помещаем ближе к левому-верхнему углу

111) YMYL сайты (Your Money or Your Life): питание, психология (получается, соционика), право, политика, лечение, налоги, пенсии, 
                                           недвижимость, страхование, новости, денежные транзакции)
     
     YMYL сайты по Гуглу это те, что «могут потенциально повлиять на будущее благополучие, здоровье, финансовую стабильность 
     или безопасность пользователей»

     Для борьбы с дезинформацией на YMYL сайтах ввели понятие EAT факторов

111) EAT факторы «Экспертность, Авторитетность, Доверие»:
	  1) Возраст сайта/организации
	  2) Указание наград, положительных отзывов
	  3) Подробная статья, также на сайте должны быть другие статьи, детализирующие тематику
	  4) Цитаты и факты из проверенных источников
	  5) На вас ссылаются тематические сайты, вас упоминают в интернет
	  6) Контакты, адрес,  пользовательское соглашение, политика конфиденциальности, обработка персональных данных, https, 
             условия возврата покупок или переводов, гарантии, подробное описание товара/услуги, лицензии, сертификаты, 
             авторы статей и их должности, наши сотрудники, несколько контактов, соцсети, вакансии,  языковые версии сайта, 
	     сотрудничество, скидки, программы лояльности, карта, видео и т.д.

         Также по исследованию падения сайтов при введении YMYL фильтра влияют:
	  1) Отсутствие рекламных ссылок 
	  2) Пользователи НЕ имеют возможности писать комментарии под статьей YMYL
	  3) «Официальный» стиль изложения статьи YMYL
	  4) Биографии авторов своих статей
	  5) Авторы - настоящие признакнные эксперты.
	  6) Статья в википедии об организации

111) Настроить быстрые ссылки в Яндексе: Вебмастер => Представление в поиске => Быстрые ссылки

111) Аудит сайта. Проверяем следующие пункты с помощью краулера (Comparser, Лягушка).

	Высокая значимость:
	1) ВСЕ нужные страницы должны быть в индексе, ненужные - нет.
	   Проверяем индекс Яндекса (Индексирование) и Гугла (Страницы).
	   Удобно просканировать сайт краулером.
           Проверяем соответствие с sitemap.xml
	2) Удобная структура сайта и меню
	3) Без спама в url словами: kupit, zakazat - можно поймать фильтр переспама
           Также не спамить этими словами в названиях картинок и alt
	4) Уровень вложенности товаров/услуг не больше 2-3 кликов
	5) Уровень вложенности в url не делать длинным (без длинных цепочек категорий)
	6) Правильная обработка несуществующей страницы - 404 ответ
           Не нужно делать редирект на главную!
	7) h1:
	   - на каждой странице для индекса, только один h1 (краулером)
	   - h1 не полный дубль title
	   - нет дублей h1
	8) title:
	   - на каждой странице, нет дублей, длина 70-140 символов с пробелами
	9) Оптимизация тегов (h2, h3 и пр.) без переспама
	10) Проверяем на уникальность и первоисточник тексты сайта:
	    1) Берем кусок текста страницы и вставляем в яндекс В КАВЫЧКАХ
	    2) Ваш сайт должен быть на первом месте
	    3) Если ваш сайт не первоисточник - переписываем текст
	11) Нет каннибализации - когда на разных страницах одинаковые ключи и под 1 запрос подходят две страницы.
            Позиции начинают "скакать"
            В вебмастере такие страницы помечаются как Дубли
            Пример: "Входные двери" и "Каталог входных дверей" - яндекс может посчитать, что они подпадают 
            под одинаковое намерение пользователя (интент)
	12) Скорость загрузки сайта в Lighthouse (главная, категории, внутренние) 
	13) Отображение страниц на мобильных устройствах (вебмастер и google mobile friendly)
	14) Отображение сайта на разных браузерах и разрешениях
	15) Хлебные крошки
	16) Shema для страницы Контакты
	17) Shema для интернет-магазинов (товар, рейтинг товара, отзывы о товаре, др.)
	18) Внутренняя перелинковка в шапке (меню), крошках 
	19) Регистрация в Я-Справочнике (лого, адрес, время работы, контакты, синяя галочка)
	20) Регистрация в Google Мой Бизнес (ключевик в названии, лого, адрес, время работы, контакты)
	21) Регион в Вебмастере
	22) Мультиязычность (hreflang) при необходимости
	23) Номер телефона:
	    - на видном месте
	    - если городской, то в формате: +7(495)777-77-77
	    - кликабельный <a href="tel:+79999999999">89999999999</a>
	    - если регион "Вся Россия", то желательно номер 8800
	24) email:
	    - на домене
	    - кликабельный <a href = "mailto:aaaa@bbbb.ru">Email</a>
	    - разметка shema
	25) Для интернет-магазинов:
	    - отзывы на Маркете
	    - удобные фильтры
	    - подробные карточки товара
            - ассортимент

	Средняя значимость:
	1) Редирект с http, www на https://site.ru
	2) В индексе (site:site.ru) д.б. только одна страница главной, без дублей со слешами на конце, index.html, index.php
	3) sitemap.xml:
	   - ссылки на все нужные в индексе страницы
	   - авт. обновление sitemap.xml
	   - проходит валидацию в Вебмастере и Гугл серч консоли
	   - загружен в Вебмастер Гугл серч консоль
	4) Если сайт большой, то карта сайта для людей, ссылка на нее в подвале, автообновление
	5) rel='canonical':
	   - правильность простановки (на фильтрах, пагинации, с utm-метками)
	   - д.б. абсолютный путь
	6) Не делать длинным url
	7) Не спамить url несколькими одинаковыми ключевиками
	8) Краулером проверяем:
	   - отсутствие ошибок 301, 302 редиректов
	   - отсутствие битых ссылок
	9) Заполненность description, отсутствие дублей, длина 150-240 знаков с пробелами
	10) h1 крупнее h2 и т.д.
	11) Нет лишних h
	12) Определяем тип запроса.
            Под коммерческие запросы пишем коммерческие страницы,
            для информационных - информационные
	13) Нет переспама заглавными буквами
	14) Нет переспама ключевыми словами
	15) Сквозная страница Контакты
	16) Уникальность страниц между собой (разные title, заголовки)
	17) Избавиться от лишнего выделения жирным
	18) Содержание и возможности страницы соответствует заголовкам и метатегам
	19) Ключи в url не должны повторяться рядом (домен, категории, страницы)
	20) Нет мусорных страниц
	21) Если сайт принадлежит к категории YMYL, то обязательно:
	    - экспертность источников
	    - страница "О компании" с лицензиями и сертификатами
	22) E-A-T факторы (экспертность, авторитетность и достоверность)
	23) Чистая история домена в вебархиве
	24) Убрать тяжелые и ненужные плагины
	25) Отказы пользователей с разных устройств по Метрике
	26) Размер страницы не более 200 КБ (в краулере)
	27) Время ответа хостинга и его аптайм
	28) Кеширование
	29) Core Web Vitals в Lighthouse для главной и внутренних
	30) Связи страниц внутренней перелинковкой со статей
	31) Нет лишних внутренних ссылок
	32) Продуманный подвал и его ссылки
	33) Метки (быстрые ссылки, теги)
	34) Постраничная пагинация и загрузка через "Показать еще"
	35) На средних и больших сайтах поиск по сайту
            Яндекс предоставляет удобный поиск по сайту: wpnew.ru/blog - пример, site.yandex.ru - инструкция
	36) Правильное название картинок
	37) alt на русском языке с заглавной буквы
	38) Сжать картинки
	39) Форматы картинок: jpg, png, gif, webp
	40) Наличие, запоминаемость, простота названия сайта/организации
	41) Форма обратной связи
	42) Доп. способы контактов (телега, вотсап и пр.)
	43) Полный адрес с индексом
	44) Реквизиты
	45) Схема проезда (Яндекс или Гугл карты)
	46) Время работы
	47) Обратный звонок
	48) Отзывы о компании
	49) Отсутствие сторонней рекламы
	50) Наличие цен и актуальность
	51) Для интернет-магазинов:
	    - если товара не будет больше, то 301 редирект на аналогичный, если будет, то оставить 200 ответ)
	    - отзывы о товарах и возможность их добавления
            - подсказки у характеристик
            - статус товара (в наличии, на заказ и пр.)
	52) Вес страниц до 200кб

	Низкая значимость:
	1) Редирект с зеркал сайта (ищем зеркала на спец. службах)
	2) Файл robots.txt:
	   - закрыты ненужные страницы от индексирования
	   - путь к sitemap.xml
	   - проходит валидацию в Вебмастере и Гугл серч консоли
	3) Нормальная 404 страница
	4) Отсутствие ошибок 5** (краулером)
	5) Нет ошибок в консоли
	6) Заголовок last-modified от сервера
	7) Не нужно открытий в новом окне. Лучше создавать удобное меню и хлебные крошки
	8) Как видит бот страницу из "Сохраненной копии" в поиске
	9) Правильность расположения рекламы на сайте 
	10) Валидный код html и css (валидатор)
	11) Кодировка UTF-8	
	12) Турбо-страницы для информационных страниц
	13) Shema для вопросов-ответов
	14) При наведении на ссылку она окрашивается или подчеркивается
	15) "Похожие товары" или "С этим товаром покупают"
	16) Популярные статьи, товары и пр.
	17) Отключить ненужные "быстрые ссылки" в Вебмастере
	18) Кнопки "Поделиться" в соц. сети
	19) На страницах пагинации rel=canonical на страницу "Все товары"
	20) Кнопка "Подписаться"
	21) Удаляем exif у картинок
	22) Проверить индексацию картинок в Яндекс и Гугл (site:rodnovspb.ru)
	23) Ключевые слова в видео 
	24) title у картинок на русском с заглавной буквы
	25) Фавиконка
	26) Попросить других людей выполнить действия на сайте, найти что-то - наблюдать какие возникают сложности и как ведут себя
	27) Устранить замечания в Вебмастере и Гугл Серч Консоли
	28) Проверить отсутствие вирусов
	29) Мониторинг доступности сайта в Метрике
	30) Настроить авт. бэкапы на хостинге
	31) Онлайн-консультант
	32) Как добраться до офиса с фотографиями
	33) Формы оплаты в подвале (Мир, Виза, Мастеркард и пр.)
	34) Оплата и доставка (разные варианты)
	35) Акции и скидки
	36) Сертификаты, лицензии
	37) Как оформить заказ
	38) Гарантии/возврат
	39) Вакансии

	
111) Удобство сайта
	UX (user experience/пользовательское взаимодействие) - взаимодействие пользователя с приложением: удобство, простота и понятность 
           использования, клиент получает удовлетворение при взаимодействии с продуктом.

	UI (user interface/пользовательский интерфейс) - визуальное восприятие: внешний вид, интерактивность продукта, отзывчивость,  цвета, 
           стиль кнопок, графика, анимация, типография, инфографика, виджеты, поведение, отклики кнопок и т.д.

	В общем, UX — это то, как пользователь взаимодействует с продуктом, а UI — то, как продукт будет им воспринят.

	Высокая значимость:
	1) Должно быть сразу понятно о чем сайт и что предлагает - поясняющий заголовок
	2) На первом экране: значимое для пользователя содержание, основные характеристики, кнопки действий
	3) Чем популярнее информация, тем ближе к левому-верхнему краю любого блока
	4) Удобная навигация по сайту, структура меню
	5) Оформление статей (структура, заголовки, инфографика, без портянок текста)
	6) Узкий контейнер для текста
	7) Удобность конверсии
	8) Короткие формы
	9) Размещать только то, что полезно, интересно и нужно пользователю
	10) Если на сайте портянка и он на высоких позициях, то портянку не убирать, а разбавить инфографикой
	11) Вызвать доверие (гарантии, отзывы, сертификаты, примеры работ, репутация, мнения влиятельных людей, 
            UGC-контент: комментарии и пр.)
	12) Если хороший рейтинг на Маркете, то показываем его (можно динамически вытягивать) вместе с ссылкой на Маркет
	13) Сохранять фокус внимания, не перегружать лишними элементами, деталями, данными
	14) Легкость, воздушность и прочие веяния
	15) Закрытие модалок при клике вне модалки
	16) Опрос фокус-группы (помогут askusers.ru, Яндекс-Взгляд. Но там ЦА может не подойти)
	17) Легко и понятно закрыть намерение (интент) пользователя
	18) Качественные фото
	19) Инфографика в статьях
	20) Контакты (Кто владелец сайта и ответственный за все)
	21) Интуитивное взаимодействие с сайтом
	22) Отсутствие горизонтальной прокрутки (на компьютере и телефоне)
	23) Удобный бургер и меню на мобильной версии
	24) Элементы/блоки на привычных местах
	25) Блоки по диаграмме Гутенберга:
	    - левый верхний угол - самый читаемый
	    - правый верхний - менее читаемый
	    - левый нижний - наименее читаемый
	    - правый нижний - область принятия решения, здесь помещаем кнопки конверсии
	26) Целевое действие без необходимости регистрации
	27) Разные способы оплаты
	28) Заказ в 1 клик
	29) Удобный фильтр и понятное отображение примененного фильтра
	30) Заметная кнопка "Купить", "В корзину"
	31) Заметная цена
	32) Заметное поле "наличие товара"
	33) На первых местах в категориях самые востребованные товары
	34) Отображение на 1 экране важных вещей о товаре/услуге
	35) Корзина на видном месте с обозначением кол-ва помещенных товаров или их стоимости
	36) Удобное удаление товара из корзины
	37) Возможность перейти на товар из корзины
	38) Ясное понимание что лежит в корзине и сколько стоит
	39) Удобность оформления заказа и оплаты в корзине
	40) Помощь или контакты на странице корзины

	Средняя значимость:
	1) Сохранение данных формы в сессии/локалсторадж на случай ошибок или будущего возвращения клиента
	2) Хорошая, запоминающаяся фавиконка заметная на белом фоне в выдаче
	3) История сайта, бренда, информация об этом
	4) Кликабельный логотип ссылкой на главную
	5) Заметные, яркие кнопки конверсии
	6) Удобство чтения текста. Статьи черным цветом на белом фоне
	7) Понятность выражения, без сложных терминов или с определениями терминов
	8) Однообразие цветов и шрифтов
	9) Эффекты при наведении на ссылки, кнопки
	10) Важные вещи текстом, а не картинкой (телефон, почта, адрес и пр.)
	11) cursor='pointer' при наведении на ссылки, кнопки
	12) Красивые несложные таблицы
	13) Невысокая шапка fixed если сайт большой
	14) На форме без антиспама или удобный
	15) Поиск в удобном месте (с выпадающим списком подсказок) если сайт большой
            Яндекс предоставляет удобный поиск по сайту: wpnew.ru/blog - пример, site.yandex.ru - инструкция
	16) Удобное отображение результатов поиска
	17) Регистрация с минимум полей  + "по пути" вместе с целевым действием
	18) Подтверждение, что оплата прошла успешно/неуспешно
	19) Полное понимание о доставке (виды, сроки, стоимость)
	20) Показ временных ограничений или скидки при заказе до опр. времени
	21) Заметные яркие акции, распродажи
	22) Информация о кредитах, рассрочках
	23) Товары, которые покупают вместе с этим товаром
	24) Похожие товары, аналоги
	25) Несколько фото с разного ракурса
	26) Фото в одном стиле
	27) Видео при необходимости
	28) Авт. расчет доставки / калькулятор доставки
	29) Удобность использования промокодов
	30) Удобный и заметный выбор языка
	31) Авт. определение города пользователя
	32) Удобный антиспам
	33) Подсказки ошибок заполнения формы
	34) Письмо с подтверждением регистрации
	35) Целевое действие без регистрации
	36) Уведомление после заказа с сайта что будет дальше и когда

	Низкая значимость:
	1) Отсутствие ненужных или сложных анимаций
	2) Межстрочный интервал
	3) Кнопка плавной прокрути вверх при длинной странице
	4) 404 страница на которой останутся и перейдут по сайту
	5) Открытие ссылок _blank только там где пользователь ожидает это
	6) Без точек в конце заголовков
	7) Если сервер валидирует поля формы, то сначала валидируем браузером через js
	8) Понятные уведомления об ошибках
	9) Подсказки, placeholder-ы на полях формы при необходимости
	10) Авторизация через социальные сети
	11) Удобный личный кабинет
	12) Информация о кредитах, рассрочках
	13) Возможность оплаты по счету от юр. лица
	14) Сколько товаров найдено при применении фильтра
	15) Увеличение фото товара
	16) Упоминание гарантии на товар
	17) Зачеркнутая цена, старая цена
	18) Уведомление о добавлении товара в корзину
	19) Стимулирование к email подписке на рассылку
	20) Отзыв после получения/доставки товара
	21)

111) Семантическое ядро
	Создание семантического ядра - 1 этап создания сайта.
	Затем на основе семант. ядра создается структура сайта

	Yandex Wordstat Helper - расширение в хром

	Недостаток wordstat - то, что показывает статистику запросов за прошлый месяц. То есть не учитываются 
	трендовые запросы. 
	Букварикс показывает статистику за прошлые периоды и показывает их моментально, без парсинга => удобно для
        больших проектов

	keys.so - анализ сайтов-конкурентов

111) Проверить уровень конкуренции группы запросов:
	1) Собрать ГЕО или поисковую выдачу по запросам
	2) Данные => Анализ групп => По сайтам-конкурентам
	3) Смотрим как много крупных сайтов (дзен, юутуб и пр.) и агрегаторов собирают ваши запросы
	4) Изучаем мелкие сайты на высоких позициях => их структуру, содержание, какие запросы они охватывают и пр.
	5) Смотрим какие сайты влетели по завросу в топ  за последние месяцы: 
	        topvisor.com - пользуется П. Александров, платно
		ru.megaindex.com/analyzeword - бесплатно
		keys.so/ru/tools/history-serp - платно
	
111) Советы:
	1) Самое первое - верно определить намерение (интент) запроса
	2) Коммерческий или информационный запрос по мнению поисковика? 
           Продвинуть страницу сможем только верно определив информационность или коммерческость запроса.
	3) Удовлетворить потребность посетителя и стать "последним кликом", чтобы он НЕ ВОЗВРАЩАЛСЯ к поиску
	4) Для информационных статей важен максимально полный ответ + экспертность
	5) Для коммерческих страниц важны характеристики и удобство конверсии (чтобы были действия)
	6) Содержание первого экрана - самое важное
	7) В Google значимость текста, как правило, выше. Также он любит оптимизированные тексты 
	8) Не использовать одинаковые ключи/одинаковые интенты (намерения) на разных страницах => каннибализация, позиции начнут скакать
	9) Объем текста (особенно для инфостраниц) можно вычислять на основе среднего значения лидеров запроса используя advego.
           Перед расчетом исключить сильно отличающиеся по объему сайты.
	10) В статьях используем LSI слова
	11) В подзаголовках используем LSI (для "Двери": петля, глазок, дверная ручка...) и ключевые выражения
	12) ВЧ запросы можно продвигать на страницах категорий/рубрик.
        13) На страницах рубрик не использовать портянки текста, знаков 500 
	14) Длинный текст можно показывать по клику на "прочитать полностью"
	15) Абазацы в 4-6 предложений для удобства чтения   
	16) Стимулировать активность на сайте (чтобы кликали, переходили по ссылкам) => улучшение ПФ.
            Но аккуратно, чтобы не загреметь под накрутку ПФ.
	17) Точные вхождения и словоформы используем в title, h1, h2, h3
            В самой статье ключи не спамим, а больше используем сопутствующие LSI ключи
	18) Снимки поисковой выдачи по запросу помесячно: 
	        topvisor.com/ru - пользуется П. Александров
		ru.megaindex.com/analyzeword - бесплатно
		keys.so/ru/tools/history-serp - платно
	19) Для продвижения ВЧ запросов важен охват темы другими страницами сайта:
	    ВЧ: "купить лыжи"
            На сайте должны быть расширяющие этот запрос страницы: "лыжи на колесиках", "лыжи летние" и пр.

111) Переоптимизация

     Переспам - пессимизируется только один (бывает 2-3) запрос, другие запросы этой страницы чувствуют себя нормально
     Переоптимизация - пессимизируется все запросы страницы

     Признаки переспама:
	1) Упал только ОДИН (иногда 2-3) запрос на 15-35-100 позиций, либо по нему показывается нерелевантная страница
	2) Позиции по запросу резко улучшаются после небольшого изменения вхождения ключа, напр. словоформы
	3) Позиции по некоторым другим ключам страницы НЕ УПАЛИ

     Признаки переоптимизации:
	1) Просела вся группа поисковых запросов, ряд запросов поменял релевантную страницу
	2) Позиции сайта не сильно изменяются при модификации запросов
	3) Сильно упала посещаемость, страница по запросам упала к топ-10 или на 2-3 страницу выдачи

     Падение за переспам обычно сильнее (вылет за топ-20-100 запроса), 
     чем за переоптимизацию (могут упасть на 10-25 позиций все запросы). 

     Также нерелевантная страница может показываться, если недооптимизация (другая страница больше подходит под запрос)
  
     Решение переспама: 
	1) Проходим сайты-конкурентов и через CTRL+F смотрим сколько раз в стреднем встречается слово у них
           Если у нас то же слово значительно чаще, то можем поймать фильтр
	2) Делаем сохранение страницы
	3) Меняем словоформу нескольких ключей, изменяем порядок слов или удаляем их
	4) Если через 3-4 недели позиция хорошо поднимается, то был переспам

     Решение переоптимизации:
	1) Переформатирование текста
	2) Уменьшение текста на 15%
	3) Меняем словоформы
	4) Удаляем избыточные вхождения

     Фильтр "Баден-Баден" накладывается за излишнее употребление ключевиков.

     Рандомизация выдачи - время от времени сайты оказываются на высоких местах, поисковики в это время отслеживают поведенческие
                           факторы и сравнивают их с другими сайтами

     Из-за рандомизации позиции могут скакать кратковременно как вверх, так и вниз. 
     Если позиции упали:
	1) не делать резких движений и исправлений
	2) проверить явные ошибки (ошибка в robots.txt и подобное)
	3) если через дней 10 позиции не встали на свои места - искать причины, исправлять

111) Релевантные страницы

	Причины почему по запросу показывается нерелевантная страница:
	1) Страница недооптимизирована
	2) Каннибализация (другая страница имеет такие же ключи)
	3) Переоптимизация страницы

     Принято составлять связи: запрос - заточенная страница и отслеживать что на самом деле показывает поисковик.
     Для этого удобно использовать topvisor	

111) Поведенческие факторы - наиболее важны для попадания в топ-3:
	Главные ПФ: 
	1) Последний клик и удовлетворение потребности пользователя
	2) Кликабельность страницы на выдаче (нужно сделать "продающий title" и "description")

     Оптимизация страниц позволяет поднять сайт в топ-20.
     Внутри топ-20 значительно возрастает влияние ПФ.	

     Для того, чтобы с топ-10 подняться в топ-3 уже недостаточно просто seo оптимизировать страницу.
     Для этого нужно усилить ПФ: чтобы пользователя кликали на заголовок, сайт был "последним кликом", 
     выполняли действия на сайте, читали статьи

     Другие ПФ:
	1) Процент отказов (в Яндексе отказ - время на сайте мене 15 секунд, в Гугле - просмотр только 1 страницы)
           Стремиться к 10% отказов
	2) Время на сайте
	3) Количество переходов по страницам
	4) Прямые заходы на сайт, сайт должен жить без поискового трафика

     Средний процент отказов в Яндексе по Метрике обычно - 20% 

111) Накрутка ПФ: 
	1) накрутичивается CTR (кликабельность на поиске)
	2) накручивается "последний клик"
	3) через месяц в топе
	4) далее, вероятно, фильтр

     Пользователь вводит поисковую фразу, последним кликом переходит на сайт, выполняет там действия конверсии.

     Наказание на накрутку: резкое падение позиций сроком примено на 1 год.
     Избавиться от фильтра невозможно, нужно перестать накручивать и ждать год.

     Накручивать ПФ на долгосрочных сайтах не рекомендуется, можно спалиться.
     Можно накручивать в очень конкурентной тематике для нахождения какое-то время в топе до фильтра.
     Для накрутки ПФ сейчас используется ZennoPoster

     Фильтры снижают карму сайта даже после снятия фильтра через время.

111) Яндекс умеет распознавать накручивают ПФ злоумышленники или сам владелец сайта.
     Если ваш сайт накручивают и резко возросла посещаемость ботами - не переживать, фильтр не применят.
     Часто резкий рост прямых заходов - нагуливание профилей ботов.

111) Советы для "белого" улучшения ПФ:
	1) Улучшать наиболее посещаемые страницы:
	        Метрика => Отчеты => Содержание => Страницы входа
	        Метрика => Отчеты => Содержание => Популярное
		Вебмастер => Поисковые запросы
        2) Посещаемость по НЧ запросам и удовлетворение намерения (интента) - самый подходящий способ для "белого" улучшения ПФ.
           Поэтому пишем качественные статьи под НЧ, которые сами влетят в топ.
	3) Избавиться от нетематичных страниц, закрыть их от индексации (nofollow, noindex) или сделать процент нетематичных страниц
           не выше 15%.
	   У сайта есть фактор "тема сайта". Тематичный сайт по тематичному запросу показывается выше, чем солянка и блог обо всем.
           Также, статья про рыбалку на сайте про стройку может выстрелить, привлечь посетителей, которые не будучи заинтересованы
           основной темой сайта будут снижать общие ПФ сайта.
 	4) Дополняющие статьи материалы, чтобы пользователь переходил по ссылкам.           
	5) Узнаваемость бренда, чтобы пользователи вводили в поиск название вашего сайта/организации
	6) Проверить отказы страниц, устройств, размеров экранов и пр.: Метрика => Отчеты => Технологии
	7) Расширять семантическоея ядро более узкими НЧ запросами
	8) Изучать конкурентов, если большинство что-то использует (калькулятор например), то тоже создавать.

111) Еще раз про тематичность сайта. Статьи на другие темы размывают тематичность сайта. 
     Яндекс покажет выше строго тематичный и полный сайт, чем сайт с тематичным разделом.
     Отсюда и вред для тематичных запросов при размещении на сайте нетематичных статей. 

111) Яндекс ИКС - сам мо себе не важен. Он зависит от посещаемости сайта, просмотров страниц.

111) Линкбилдинг
	1) Ссылки работают, особенно для Гугла
	2) 20 процентов успеха продвижения - ссылки
	3) Чем авторитетнее источник, тем выше вес ссылки
	4) Для некоторых запросов для попадания в топ-10 достаточно 1-2 ссылок
           Желательно покупать ссылки на запросы по которым страницы уже в топ-50
	5) Без резкого роста и съема
	6) Молодому сайту ссылки покупать можно, но аккуратно, на главную, чтобы кликали
	7) до 50 процентов купленных ссылок могут не показываться и не учитываться в панели.
	8) Для информационных сайтов покупаем ссылки на главную, 
           для коммерческих - на внутренние страницы
	   для интернет-магазина - на категории и важные товары
	9) Эхо ссылок - ссылки после удаления еще какое-то время влияют на позиции.
           То есть купили жирную ссылку, позиция поднялась, после удаления ссылки позиция сразу не упадет.
	10) Возраст ссылки - чем больше возраст ссылки на ваш сайт, тем больше ее вес
	11) Работают также неактивные ссылки (работает как упоминание сайта, бренда)
	12) Лучше всего покупать ссылки со страниц в топе по вашей теме. 
            Переходы по ссылкам усиливают их вес.
	13) Ссылки с nofollow, noindex и просто упоминания тоже учитываются


111) Внешних ссылок с ключевиками в анкорах - не более 25% (во внутренней перелинковке сайта можно 80%)
     В остальных 75% ссылок (ключевики и их словоформы стоят в окружающем пассаже в основном слева от ссылки):
	- название сайта, организации, бренда (~25%)
	- тут, здесь, на странице, ссылка и пр. (~25%)
	- url домена/страницы (~25%)
	- ссылки nofollow

111) Одинаковые анкоры не покупать. Анкоры использовать только 1 раз.
     За одинаковые анкоры легко влететь под фильтр.

111) Разнонаправленность доноров для естественности: блоги, новостники, соцсети, форумы, тематические сайты, 
     сайты вопросов-ответов (майл.ру) и пр.
     Заказ ссылок с соцсетей: zapost.im, kwork

111) Начало продвижения ссылками:
	- не нужно покупать много ссылок сразу
	- первые купленные ссылки направляем на главную страницу
	- анкоры: название бренда, общие слова, url
	- покупать вечные на gogetlinks
	- проверять доноры самостоятельно checktrust.ru
	- проверка внешних ссылок у конкурентов: megaindex и keys.so
	- если в статье несколько ссылок на ваш сайт, то учитывается только первый анкор
	- ссылочные прогоны для молодых сайтов категорически запрещены
          прогонять можно только донора, чтобы на вашу купленную статью были другие ссылки

111) Пояснения про попадание в фильтр из-за ссылок:
	1) Под "Минусинск", как правило" попадают коммерческие сайты за большое количество точных ключей в анкорах.
	2) Чтобы не попасть покупать НЕ БОЛЬШЕ 25% ключей в анкорах, все они разные
	3) Для выхода из "Минусинска" нужно избавляться от покупных ссылок, уменьшать процент ключей в анкорах до 25%
	4) В Google Search Console можно отклонять ссылки

111) Продвижение в агрегаторах работает по таким же способам как в Яндексе: заголовки, описания, отзывы, кликабельность,
     конверсионность

111) Для безопасности проекта нужно диверсифицировать трафик (например, чтобы не попасть как с Инстаграмом)
     или развивать два-три сайта.
	
111) Возможен кратковременный рост позиций за счет того что вас начали обсуждать в сети, ссылаться и пр.

111) Пиксель - скрипт от вк отслеживающий посетителей вашего сайта, чтобы затем показывать им рекламу в ВК	

111) церебро.рф и pro.церебро.рф - сервис для поиска и анализа аудитории ВКонтакте по разным паттернам поведения
     
     Помогают:
	- более точно подобрать аудиторию для рекламы в ВК и уменьшить расходы
	- показать аудиторию состоящую в опр. группах одновременно
	- показывать активным пользователям (кто ставит лайки, репосты и пр.)
		
111) В директе объявления обычно затачивают на запросы без кавычек, в СЕО - только на завросы в кавычках.

111) Всегда нужно отслеживать источники переходов (флаер, директ, реклама в вк, email рассылка и пр.)
     Для этого к ссылкам добавляем utm-метки, которые затем видны в Метрике => Отчеты => Источники => UTM метки.
     Описание есть выше

111) Можно даже отслеживать эффективность места где раздавали бумажные флаеры, для этого встраиваем в QR код на них utm-метку

111) Для отслеживания конверсии с рекламы добавить цели: Метрика => Цели
   
     Можно отслеживать:
	- количество просмотренных страниц посетителем
	- посещение опр. страниц (напр. страница успешно отправленной заявки)
	- js событие
	- клик по телефону, почте, кнопке, поиску
	- отправку формы
	- переход в вотсап, тг
	- скачиваение файла
	- составную цель

     Далее во вкладке "Конверсии" отслеживаем процент успешно дошедших до выбранных целей.
     Например, у сайта по печатям конверсия для цели "Заявка" (url содержит: order-success.html)
     конверсия составляет 4,48 % (из 100 посетителей заявку отправляет 4)

     Видео по увеличению конверсии:  https://www.youtube.com/live/3FjyH2tC5fA?si=ib2CVwnh2DJoOOIY
     Установка целей помогает рассчитать цену лида.
   
     Если для цели заполнить поле "доход", то можно рассчитать ROI

111) ROI - (return on investment) возврат вложений. 

     ROI = (Доход с вложений - размер вложений) / размер вложений * 100%

     Пример: в директ потратили 1000 руб, чистая прибыль с клиентов от рекламы = 5000.
             ROI = 400% 

     ROI < 0 => на рекламу потратили больше, чем заработали с нее
     ROI > 0 => что-то заработали с рекламы
     ROI > 100% - обычно показатель, что рекламу давать стоит (потратили 500 руб, заработали чистыми 1000 руб)

     ROI удобно совмещать с utm-метками, когда реклама идет по нескольким каналам

     В метрика ROI рассчитывается в разделе "Сквозная аналитика".
     Для начала необходимо будет установить цель и доход с нее (например: заявка на сайте - 500 руб.)

111) При установленной цели можно отслеживать запросы, страницы входа, устройства и пр. с которых
     больше всего доходят до цели, больше всего конверсия:
	Метрика => Отчеты => Выбираем отчет => В шапке списка есть поле "Выберите цель"

111) Колтрекинг - на сайте автоматически меняются телефоны под utm-метки. Особенно полезно для сайта услуг, когда клиенты
                  в основном звонят.   
                  Можно совместить с Метрикой: Интеграции => Коллтрекинг => Выбираем разработчика,
                  в этом случае при звонке на номер служба передает эту информацию в метрику

     Пример: https://site.ru/ - показывается 89501111111
	     https://site.ru/?utm-source=vk - показывается 89507777777

     Можно создать свой статический колтрекинг: один телефон на рекламном банере, другой на сайте, третий в ДубльГисе

111) Сквозная аналитика - изучение какие сайты, рекламные площадки показали клиенту вашу рекламу на пути, 
                          когда он все таки сделал заказ.
                          https://roistat.com/ru/
	
111) Рекомендуется собирать подписки на email рассылки для уведомления об акциях, новостях, скидках и пр.
     Можно использовать unisender

111) Коренные обновления алгоритмов поиска происходят 3-4 раза в год.
     Для того, чтобы отследить новости и возможные причины падения читаем vc.ru, searchengines.guru/ru/forum

111) Метрика и Вебмастер позволяют передать отдельные права доступа разработчикам

111) Аффилирование - если поисковик поймет, что сайты принадлежат одному владельцу, то происходит пессимизация одного сайта по 
                     ПЕРЕСЕКАЮЩИМСЯ запросам
     Как избежать:
	1) Разные реквизиты
	2) Разные контакты, адрес
	3) Разные хостинги
	4) Разные аккаунты Вебмастера и Метрики
	5) Разный дизайн, движок и пр.

111) Переезжать на другой движок, хостинг, домен, переделывать структуру нужно постепенно по одному этапу, ждать переиндексации и делать
     следующее действие.

111) Взять за правило отслеживать позиции запросов через topvisor (5 копеек один запрос)

111) Малоценное содержание у Яндекса - страница вылетает из индекса из-за того, что страницы не отличаются друг от друга.

111) Фильтр "АГС" накладывается за неуникальный контент. В выдаче остается около 10 страниц, остальные вылетают из индекса.
     Для снятия фильтра избавиться от неуникального текста, писать в техподдержку. Может пройти год пока фильтр снимется.

111) Фильтр "Новый АГС" накладывается за продажу ссылок. Запросы из топ-10 вылетают за топ-30.
     Для снятия фильтра снять продажные ссылки.

111) Фильтр "Ты - последний"накладывается за неуникальный (бесполезный) контент + спамные ссылки. Резко падает посещаемость с Яндекса,
     но страницы находятся в индексе.
     Для определения фильтра вводим в поиск точное вхождение title или кусок текста. 
     Если сайта нет в топ-10, то скорее всего фильтр.

111) Непот-фильтр накладывается за продажу ссылок.
     Ссылки с таких сайтов не имеют веса и бесполезны.
     Признак фильтра: сайт теряет позиции.
     
111) Фильтр "Ты - спамный" накладывается за переоптимизированные тексты. Падают позиции. Техподдержка Яндекса прямо отвечает, что страницы
     переоптимизированы.

111) Фильтр "Песоцница" - возможный фильтр на 6-12 месяцев для новых сайтов. Рекомендуется сразу наливать трафик для молодых сайтов, 
     чтобы Яндекс набрал поведенческую статистику.

111) Фильтр "Минусинск" ("Пингвин" в Гугле) накладывается за спамные анкоры купленных ссылок.

111) Сайт "Новичок" может за пару-тройку недель попасть в топ по сильным запросам. 
     Затем позиции падают и восстанавливаются только через несколько месяцев.
     Если ПФ отличные - позиции могут не упасть!!!

111) Фильтр "DMCA" накладывается по жалобе за нарушение авторских прав (фильмы, музыка).
     Нужно делать редирект (переезд) на другой домен.

111) Фильтр "НКС" - некачественная страница. Наказываются сайты с большим количеством неуникальных между собой страниц.
     Страницы исключаются из индекса, в вебмастере пишется "Малоценная или маловостребованная страница".
     Проверить в вебмастере процент таких страниц: 
	1) Индексирование => Страницы в поиске => Исключенные страницы + фильтр "Статус" = "Малоценная или маловостребованная ..."
	2) Над графиком нажимаем на Исключенные, показывается диаграмма
	3) Если ММ страниц больше 20-40% - то будет фильтр и наказание всего сайта по всем запросам.

     Способы решения:
	1) Закрываем от индексации повторяющиеся части страниц
	2) rel=canonical на страницах пагинации
	3) Удаляем неуникальные страницы или ставим метатег noindex,nofollow
	4) Уникализируем страницы блоками «Популярные товары», «С этим покупают», «Вам понравится», "Отзывы" и пр.

111) Бан Яндекса - исключение из индекса. Накладывается за нарушение закона (РКН), 
     на дорвеи (цель дорвеев направить посетителей на другой сайт), дубли сайтов.

111) Список нарушений за которые Яндекс накладывает фильтры:
	yandex.ru/support/webmaster/yandex-indexing/webmaster-advice.html

111) Показатель наложения фильтров - падение позиций сайта или письмо от ЯНдекса о наложении фильтра.

111) После снятия фильтра позиции могут быть долгое время ниже прежних, потому что есть параметр "кармы" сайта.

111) Выйти из фильтров иногда помогает смена домена.

111) Автоматические прогоны можно делать только по статьям со ссылкой на ваш проект.

111) Если изменяем url страницы и нужно сохранить позицию, то делаем 301 перенаправление,
     позиция сохранится.

111) Бывает, что посещаемость просела, а позиции на месте - в этом случае возможно:
	1) Стало больше объявлений в Директе
	2) Появились вставки в выдачу (видео, картинки пр.)

111) ru.megaindex.com/analyzeword - история выдачи, рейтинг сайтов помесячно по запросам

111) Оценка эффективности продвижения, рекламы:
	1) Трафик (Посещаемость с поиска)
	2) Позиции (иметь ввиду возможность рандомизации)
	3) Продажи
	4) Конверсия (ставим в Метрике Цели и отслеживаем в Конверсиях) 
	5) Время на сайте, отказы, CTR на поисковой выдаче и пр.
	6) Звонки
	7) Показатели сайта (ИКС, количество страниц в топе, кол-во страниц входа, сравнение с конкурентами и пр.)

111) Стоимость продвижения зависит от: регион, тематика, конкуренция, ссылолчная масса конкурентов, дизайн,
     требуемый объем и качество текстов, новые разделы, страницы, внедряемые возможности сайта и пр.
     П.Александров продает час работы сотрудника за 2000 руб.

111) Узнать примерную посещаемость после года работ: 
	1) Собрать СЯ и сумму "!" частот
	2) Посещаемость будет 5-10 процентов от этой суммы
	3) Можно посмотреть посещаемость у конкурентов через сервисы

111) Причины падения посещаемости.
     Внимание, проверить явные ошибки и подождать 2 недели до выводов.
	- фильтры
	- сезонность
	- обновления алгоритмов
	- переоптимизация и недооптимизация
	- вылетели страницы из индекса
	- смена зеркала
	- отвал "склеек" (не продлили ссылающийся по 301 редиректу домен)
	- отвал ссылок

111) Каждая страница, находящаяся в индексе должна быть качественная:
	- оптимизирована под моб. устройства
	- иметь хоть какую-то посещаемость
	- соответствовать теме сайта
     Иначе для сайта полезнее ее вообще удалить, если не переделывать.

111) UGC-контент (User Generated Content) — это пользовательский контент: отзывы, комментарии, обзоры, фото, видео
	- 70% потребителей доверяют UGC больше, чем брендированному контенту
	- 79% людей говорят, что UGC оказывает достаточно сильное влияние на их решения о покупке
	- клиенты покупают товар в 6 раз чаще, если на странице есть фотографии из социальных сетей
	- 72% покупателей доверяют онлайн-отзывам так же, как личным рекомендациям
     
111) Лучший путь увеличения посещаемости - проработать существующие наиболее посещаемые с поиска статьи по дополнительным запросам, 
     чтобы они влетели по ним в топ-3.

     Порядок увеличения посещаемости на примере одной страницы:
	1) Ищем наиболее посещаемую с поиска страницу:
	   Метрика => Отчеты => Содержание => Страницы входа: Группировки => Поисковая система + Кнопка "Линейный список"
	2) Находим дополнительные запросы для этой страницы:
	   - парсингом ее ключей в вордстате, 
           - анализом ключей конкурентных страниц (keys.so)
	3) Получаем их частотность в Директе
	4) Получаем возиции этой страницы по этим ключам в Коллекторе:
	   Данные => Парсинг => Анализ позиций
	5) Оптимизируем страницу по наиболее частотным запросам по которым она между топ-10 и топ-50:
	   - вхождение в title, description, заголовки, текст
	   - возможно новый url с вхождением ключа и 301 редирект
	   - ставим ссылку с других наиболее релевантных по запросу страниц на эту (Анализ релевантных страниц)

111) Ускорение переклейки по 301 редиректу: Вебмастер => Индексирование => Переобход страниц => Добавляем старые url

111) Поиск упущенной семантики:
     keys.so => Сайты => Сравнить сайты => Вставляем свой сайт и сайт конкурента => Запросы конкурента, которых нет на моем сайте

111) Анализировать сайт конкурента можно в topvisor - 17 руб.

111) Организация работы в команде
	- Scrum. Революционный метод управления проектами - книга о командной работе в спринтах
	- магнитно-маркерная доска на всю стену
	- CRM: Битриск24, ПланФикс, Мегаплан, Worksection, wrike, clickup - советует Александров (так как объединяет неск. программ)
	- YouTrack - управление проектами от JetBrains (до 10 пользователей бесплатно)
          пользоваться YouTrack и не париться
     	- Для бухгалтерии при УСН 6% и сотрудниками Александров сам ведет бухгалтерию в Контур Эльба
	- Для приема платежей на сайте модули банков или Юкасса, Робокасса и др.
	- для учета денег CoinKeeper (личные), Дзен-мани (организация)

111) Порядок продвижения сайта по Александрову:

	1) Технический анализ:
	   - правильные  редиректы с зеркал (http, www)
	   - редирект с адреса со слешем /, index.php, index.html 
	   - ссылки ЧПУ, уровень вложенности (лучше товары и статьи не привязывать к категориям)
           - запреты на индексацию в robots.txt
	   - правильные ссылки в sitemap, все страницы из него д.б. в индексе
	   - прогнать sitemap и robots в валидаторах
           - включить постоянное обновление sitemap.xml, чтобы lastmod менялось
	   - html карта сайта (без запрещенных страниц) и сквозная ссылка на нее в подвале 
	   - Вебмастер: загрузить sitemap, выбрано правильное зеркало (https, без www)
	   - Гугл Консоль: загрузить sitemap, выбрано правильное зеркало (https, без www)
	   - возврат 404 ошибки и хорошая страница для нее
	   - абсолютный путь в rel='canonical' (utm, сортировка, фильтры) + проверить краулером

	2) Сканируем сайт краулером Comparser:
	   - битые ссылки
	   - title, description (заполненность, отсутствие дублей, длина)
	   - h1 (заполненность, отсутствие дублей, отличается от title, один h1 на странице)
	   - редиректы 301, 302 - если можно поменять на прямые ссылки
	   - внешние ссылки (ненужные убрать, на сомнительные сайты - nofollow)
	   - отсутствие ошибок сервера 500
	   - url-ы (длина, без спамных слов "купить" и подоб., без глубокой вложенности)

	3) Соответствие страниц из sitemap страницам в Вебмастере и Гугл консоли:
	   - отсутствие ненужных страниц в индексе
	   - все нужные страницы в индексе
	   - прогнать краулером и проверить на индексацию

	4) Базовые необходимые вещи:
	   - сразу понятно о чем сайт и что предлагает
	   - контент и кнопки действия на первом экране
	   - EAT факторы (авторы, лицензии)
	   - все страницы доступны в пределах 2-х кликов
	   - отсутствие ненужных внутренних ссылок (чтобы не страницы не теряли вес)
	   - поиск по сайту (можно Яндекс-поиск)
	   - наличие бренда (уникальное запоминающееся название)
	   - без назойливой рекламы
	   - постраничная навигация (ссылка на предыдущую, следующую, первую и последнюю страницу, rel='canonical')
	   - динамичная загрузка следующих товаров (кнопка "Загрузить еще")
	   - фавиконка
	   - правильное отображение в браузерах (особенно Safari)	
	   - обязательные сквозные страницы "О нас", "Контакты", "Политика конфиденциальности" (если запрашиваем перс. данные)
	   - микроразметка (для статей, хлебных крошек, контакты, "звездочки", товаров и пр.)
           - валидность кода в валидаторе (html, css)
	   - отсутствие вирусов 
	   - по желанию настроить заголовок last-modified (проверка на last-modified.com/ru)
             есть пакет для Ларавель: last-modified.com/ru/laravel
	   - безопасность:
		- отдельные ftp аккаунты программистам
	        - админка, домен (двухфакторная аутентификация)
                - авт. бэкапы сайта и БД
	        - мониторинг падения сайта в Метрике (чтобы приходила смс)
	        - удалить старые ненужные плагины, темы
	   - исправить ошибки в Вебмастере и Гугл консоли

	5) Адаптивность:
	   - mobile friendly в Гугл консоли для главной, категории, внутренней)
	   - проверка мобильных страниц в Вебмастере
	   - контент на первом экране на адаптивной версии
	   - проверка удобства сайта на разных устройствах в консоли и проверка с телефона пальцами
             пройти заказ от и до пальцами

	6) Скорость:
	   - Lighthouse (главная, категория, внутренняя)
	   - убрать ненужные плагины, модули
	   - проверка поведенческих (отказы) с разных устройств
           - кодировка utf-8
	   - сжатие картинок
	   - кеширование (на WP WP Rocket, Cloudflare)
	   - Турбо страницы на инфостраницах

	7) Удобство сайта:
	   - пункты из "Удобство сайта"
	
	8) Внутренняя пеерлинковка
	   - хлебные крошки
	   - пПохожие записи/товары
	   - грамотное меню
	   - ссылки в статьх
	   - готовые подборки по фильтрам
	   - ссылки в сайдбаре
	   - ссылки в подвале
	   - бысрые ссылки
	   - отсутствие rel='nofollow' на внутр. ссылках
	   - отсутствие ненужных внутренних ссылок
	   - cursor='pointer' + изменение цвета ссылки или подчеркивание
	
	9) Контент:
	   - обязательные страницы О сайте, Контакты
	   - правильность тегов (title, description, h1): уникальность, без переспама, длина
	   - тексты (уникальность, отн. уникальность между страницами, объем, без зомби-страниц)
	   - картинки (название, alt, title, сжатие, формат, проверка на индексацию) - проверить в краулере
	   - списки (нумерованные, маркированные)
	   - заголовки h1-h6 (иерархия, без лишних)
	   - уместные выделения (без лишнего жирного и курсивного текста)
	   - видео (вставлять ceo оптимизированные с ютуба)
	   - скрытие ненужных страниц из индекса ("noindex, nofollow", rel='canonical', robots.txt)
	   - сео оптимизация рубрик, меток
	   - отсутствие каннибализации (правильно кластеризовать. Пример одинакового интента: "определить социотип" и "соционика тест")
	   - соответствие текста намерению пользователя
	   - проверить домен на историю в вебархиве
	   - подписка на обновление
	
	10) Коммерческие факторы:
	   - яндекс справочник (заполнить все поля)
	   - Гугл Мой Бизнес (заполнить все поля)
	   - контакты (shema-разметка, телефон, почта, адрес, реквизиты, схема проезда, время работы, тг, вотсап и пр.)
	   - разные виды оплаты и доставки
	   - обратный звонок
	   - мы принимаем (способы оплаты в подвале)
	   - правильная региональность в Вебмастере, Справочнике, Мой Бизнес
	   - интернет магазин добавляем в Яндекс-Маркет, сайт услуг в Яндекс-Услуги
	   - проверяем отзывы в Маркете или Услуги
	   - страница отзывов о компании и возможность добавления новых
	   - акции, скидки
	   - сертификаты, лицензии
	   - гарантия, возврат
	   - вакансии
	   - цены и их актуальность
           - карточка товара (фото, цена, описание, характеристики, отзывы)
	   - редирект 301 у исчезнувшего товара и "нет в наличии" у того, который появится
	   - очень важен широкий ассортимент у интернет-магазинов

	11) Семантическое ядро:
	   - подбор базисов
	   - сбор в Вордстате
	   - сбор частот в Директе
	   - очистка СЯ
	   - кластеризация
	   - составление структуры сайта

	12) Исправление существующих страниц
	   - фиксация url под кластеры (топвизор)
	   - "зомби-страницы" (исключенные в Вебмастере, без сео-оптимизации, без посещений, не в индексе, не адаптивные, с отказами)
	   - избавиться от каннибализации
	   - улучшение страниц по интенту (смотреть топ страниц конкурентов)
	   - упущенные ключевые слова
	   - обновление неактуального содержания страниц
	   - внутренняя перелинковка со статей
	   - допиливание жирных страниц
	   - исправление нецелевых url по запросам
	   - поиск переоптимизации/недооптимизации

	13) Наполнение новых страниц:
	   - определение страниц для статей и их интента
           - ТЗ копирайтерам:
	        - ключевые слова (геозависимость, коммерческость)
 		- LSI слова
	 	- объем
		- примеры качественных текстов
		- стандартные требования (особое внимание первому абзацу, h1, title, description, списки, h2-h3, медиафайлы)
	 	- экспертность (EAT-факторы)
	   - анализ конкуренции (главные, title в выдаче по Коллектору)
	   - отправка страниц на индексацию (Вебмастер => переобход, запрос на индексирование в Гугл консоли)
	   - комментарии к статьям, отзывы к товарам

	14) Социальные факторы:
	   - кнопки поделиться в соцсетях
	   - присутствие компании в соцсетях
	   - пиксели от ВК если будет реклама там

	15) Поведенческие факторы:
	   - CTR (продающие title и description)
	   - страницы должны удоавлетворять запрос и быть последними кликами
	   - ПФ с разных устройств
	   - довести процент отказов по метрике до 10%
	   - проработать страницы с наибольшим % отказов
	   - идеи как повысить CTR
	   - калькуляторы
	   - добываем посещения извне поиска (прямые заходы, брендовые запросы, email-рассылки, форумы, ютуб и пр.)

	16) Внешняя оптимизация:
	   - анализ внешних ссылок конкурентов (мегаиндекс, топвизор)
	   - выбор запросов для ссылок (те, по которым сайт между топ-20 и топ-100)
	   - составление списка анкоров (брендовые, url, общие слова, сами ключи)
	   - подбор доноров
	   - покупка ссылок
	   - добыча соц сигналов (Кворк, Адвего)
	   - крауд-ссылки (Zapostim, Advego)
	   - попробовать разместиться на страницах-донорах конкурентов
	   - разноуровневые ссылки, прогон страницы-донора хрумером)

	17) Анализ конкурентов
	   - подбор лучших конкурентов (особенно продвинувшихся молодых)
	   - анализ текстов (объем, необходимость, тип страниц и пр.)
	   - анализ title, description, сниппетов
	   - внутренняя перелинковка
	   - анализ тех. данных (время загрузки, адаптивность)
	   - изучение посещаемости, самые посещаемые страницы
	   - изучение пузомерок (возраст, икс и пр.)
	   - изучение структуры (чпу, вложенность, поддомены)
	   - изучение контактов (чат, мессенджеры, формы, 8800, кол-во телефонов)
	   - наличие соц. сетей
	   - изучение внешних ссылок 
	   - служебные страницы (сертификаты, прайс, акции, вопрос-ответ и пр.)
	   - доп. возможности (тесты, калькуляторы, вызов мастера и пр.)

	18) Аналитика
	   - Метрика, Google Analytics
	   - съем позиций по ключевым запросам и релевантным им страницам
	   - установка целей в Метрике (заявка, форма, подписка, звонок и т.д.)
	   - идеи для улучшения конверсии
	   - поиск переспамленных страниц
           - вебвизор

	19) Регулярные работы:
	   - сравнение с конкурентами
	   - регулярное добавление контента	
	   - внешние ссылки
	   - расширение семантики
	   - улучшение ПФ, рост конверсии
	   - реагирование на изменение алгоритмов ПС
	   - сайт должен быть лучшим в тематике

111) Валидный номер телефона: +7(495)777-77-77


==================================================== Выкладка сайта Evo на Бегет с Гитхаба =============================================================================

Далее будут действия в папке site.ru и site.ru/public_html, обратить на это внимание

1) На локальном сайте удаляем кеш:
	1) Инструменты => Очистить кеш
	2) Файлы => Удалить файлы
	3) В базе данных truncate таблицы: evo_files, evo_event_log, evo_manager_log,
           другие свои таблицы относящиеся к локальному сайту (evo_orders)
	4) Экспортируем таблицу

2) Создаем папку проекта site.ru/public_html, помещаем в нее файлы сайта
3) Создаем локальный репозиторий гит в папке site.ru
   В шторме делается так в папке site.ru: VCS => Create Git Repository
   Внутри site.ru создается папка .git

4) Создаем в site.ru .gitignore со строкой .idea (исключает служебную папку шторма)


5) Выделяем папку site.ru: Git => Add

6) Выгружаем на гитхаб: выделяем site.ru => Git => GitHub => Share Project on Github

7) На Бегете домен крепим к сайту, далее
   создаем ФТП + SSH доступ на Бегете в папку site.ru (без public_html)
   Эти пароли в дальнейшем понадобятся

8) Заходим по SSH в папку site.ru (Tools => Start SSH Session, вводим доступы)

9) Проверяем в командной строке pwd, должно показывать
   подобное: /home/o/werewrew/site.ru

10) Удаляем внутри site.ru папку public_html:
	rm -rf public_html

11) Создаем SSH ключи для Бегета:
	mkdir ~/.ssh  (делаем если нет папки .ssh)
	ssh-keygen -t rsa -q -N '' -f ~/.ssh/id_rsa

    Ключи для рег ру (находясь в папке сайта):
	mkdir .ssh
	ssh-keygen -t rsa -q -N '' -f .ssh/id_rsa

12) Внутри .ssh создались ключи

12) Далее продолжаем в консоли, инициализируем гит-репозиторий:
	git init
        git config --global --add safe.directory /home/o/qweqwe/site.ru (ваш путь)

13) Далее:
	git remote add origin git@github.com:путь_к_репозиторию_гитхаба.git (путь с гитхаба с зеленой кнопки)
	
13) Заходим в Гитхаб репозиторий сайта site.ru
    Settings =>  Deploy keys => Add deploy key => В title пишем key,
    в Key Вставляем содержимое публичного ключа из .ssh

14) Заходим там же на Гитхабе в Secrets and variables => Actions
    Нужно прописать SSH доступы для файла .github\workflows
    Создаем три New repository secret:
	HOST c фтп-ssh адресом сервера, например qwerty.beget.tech
	LOGIN - c логином ФТП
        PASSWORD с паролем
			
15) Загружаем в локальную папку сайта рядом с public_html папку .github (прикрепляю), 
    добавляем ее в отслеживание гита git add

В ssh консоли хостинга:
	git pull origin master
Отвечаем yes на вопросы 
Хостинг скачал сайт с гитхаба
	
16) Теперь делаем в проекте на компьютере пуш на гитхаб
    Запускается .github/workflows/deploy.yml и самостоятельно забирает изменения
    с гитхаба
    Может сработать со второго раза

17) В "Управление сайтами" прикрепляем домен к директории,  
    переключаем php версию на php 8.1, делаем редирект на https

18) Создаем базу mysql на Бегете, импортируем туда локальную

18) Далее нужно закачать вручную на хостинг файлы, установить настройки по необходимости:
    public_html/core/config/database/connections/default.php
    public_html/core/custom/.env (пути можно смотреть в ssh консоли через pwd)
    public_html/core/.install (если не было на хостинге)
    public_html/.htaccess (если не было на хостинге)
    public_html/core/custom/config/cms/settings.php
    public_html/core/storage/sessions - создать пустую папку
    public_html/assets/images
    public_html/assets/.thumbs

19) Поменять сайт в robots.txt

21) В папке core в ssh консоли хостинга: 
	cd public_html/core
       
	composer-php8.1 install

 Если композер покажет перерасход памяти, то COMPOSER_MEMORY_LIMIT=-1 composer-php8.1 update

22) Рекапча: www.google.com/recaptcha/admin

23) В reviews отзывы раскомментируем

24) Поебался с точкой в файле .env, почему-то с моего хостинга скопировался без точки

==================================================== Selenium ===============================================================

0) https://www.selenium.dev/selenium/docs/api/javascript/index.html - команды для js
   https://www.selenium.dev/documentation/ - документация

1) Скачать chromedriver.exe по ссылке и поместить в свою папку проекта:
	https://googlechromelabs.github.io/chrome-for-testing/#stable

2) Установить пакет: 
	npm i selenium-webdriver

3) Текст js файла:

const { Builder, By, until, Key } = require('selenium-webdriver');

;(async function () {
    let driver = await new Builder().forBrowser('chrome').build();  // путь к chromedriver.exe найдется автоматически
    await driver.get('https://111222333.ru/');
    let element = await driver.findElement(By.css('.header__street'))
    await element.click();   
})();

Запускаем файл: node script.js

Другие команды, добавляем перед ними await, здесь могут быть примеси с java:
driver.get(url): Загружает веб-страницу по указанному URL, это как вставка в браузер адреса и переход на него
driver.back(): Переход на предыдущую страницу.
driver.forward(): Переход на следующую страницу.
driver.navigate().refresh();: Обновление текущей страницы.

element.click(): Клик по элементу.
element.sendKeys('text'): нажатие клавиш на клавиатуре, ввод текста
element.sendKeys('Печати', Key.ENTER) - ввод текста и нажатие Enter
element.clear(): Очистка текста в элементе.
element.getText(): Получение текста из элемента.
element.getAttribute('attribute_name'): Получение значения атрибута элемента (например, href, value)

await driver.quit(); - закрыть браузер, освободить ресурсы
                                                                                       
111) Установка настроек для запускаемого браузера.
Первый способ:
const chrome = require('selenium-webdriver/chrome')
let options = new chrome.Options()
options.addArguments(['start-fullscreen']) // полноэкранный режим
options.addArguments(['start-maximized']) // во весь экран
options.addArguments("--headless") - безбраузерный режим
let driver = await new Builder().forBrowser('chrome').setChromeOptions(options).build();

Второй способ через капабилити:
let driver = new webdriver.Builder()
    .withCapabilities({'unexpectedAlertBehaviour': 'dismiss'}) - поведение при непредвиденных alert
    .forBrowser('chrome').build();

Также можно настроить профиль Хрома и просто запустить его.
Но если запускаешь свой личный профиль, то браузер нужно закрыть предварительно
options.addArguments("user-data-dir=C:/Users/rodno/AppData/Local/Google/Chrome/User Data");
options.addArguments("profile-directory=Default"); // здесь запускается хром которым сам пользуюсь

Как настроить свой профиль:
1) Создаем папку profile в корне проекта
2) Копируем туда папку Default из C:\Users\rodno\AppData\Local\Google\Chrome for Testing\User Data
3) const path = require('path');
   let options = new chrome.Options()
   chromeOptions.addArguments(`user-data-dir=${path.join(__dirname, 'profile')}`)
   chromeOptions.addArguments(`profile-directory=Default`);
4) Запускаем какой-нибудь сайт через await driver.get('https:///')
   Открывается браузер, можно установить расширения и т.д.
5) Команда driver.quit() будет удалять все куки 

111) Куки
Получаем куки:
const fs = require('fs'); // чтобы нода могла записывать файл
await driver.get('https://www.vk.com/');
Тут, например, заходим на вк используя sendKeys
const cookies = await driver.manage().getCookies(); // получаем куки от вк
fs.writeFileSync('cookies.json', JSON.stringify(cookies)); // записываем их в файл

Устанавливаем куки при следующем заходе через чистый профиль:
await driver.get('https://www.vk.com/');
const cookies = JSON.parse(fs.readFileSync('cookies.json'));
for (let cookie of cookies) {
  await driver.manage().addCookie(cookie);}

await driver.navigate().refresh(); // для применения куки перезагружаем страницу

Таким образом я зашел через свой основной профиль Хрома, сохранил куки оттуда и затем вошел в ВК с чистого профиля хрома.
Так, понимаю, что если заходить через основной профиль, то там куки сохраняются автоматически в профиле. Если через чистый профиль,
то их нужно устанавливать.

111) Можно запускать несколько одинаковых браузеров, для этого просто создаем несколько конструкторов:
let driver = await new Builder().forBrowser('chrome').build();

111) Отмена закрытия окна браузера, если запускаешь скрипт через зеленый треугольник отладчика
     В конце скрипта поставить:  setTimeout(async () => {}, 30000); // 30 секунд

111) Поиск элементов в ДОМ
Правила поиска элементов через findElement:
1) Искать элементы нужно перед целевым  действием, потому что они могут исчезнуть при обновлении страницы и при др. действиях
2) Запрещено кликать невидимые на данный момент элементы или вводить в них текст
   Искать можно только видимые элементы, потому что иначе исключение NoSuchElementError
   Например нельзя найти модалку, тыкнуть на нее и сразу искать элемент модалки

driver.findElement(By.id('element_id')): Поиск элемента по его ID.
driver.findElement(By.cssSelector('css_selector')): Поиск элемента по CSS-селектору.
driver.findElement(By.css('[name=other]')) - по атрибуту
driver.findElement(By.css('[type=button]')) - по атрибуту
driver.findElement(By.css('[checked]')) - по наличию атрибута
driver.findElement(By.css('[src^=http]')) - атрибут начинается с текста
driver.findElement(By.css('label:not(.error)')) - по отсутствию класса
driver.findElements(By.cssSelector('css_selector')): Поиск всех элементов по CSS-селектору.
driver.findElement(By.name('element_name')): Поиск элемента по имени.
driver.findElement(By.xpath('//tag[@attribute="value"]')): Поиск элемента по языку XPath.
driver.findElements(By.className('class_name')): Поиск всех элементов с заданным классом.
driver.findElement(By.linkText("Logout")); - по тексту ссылки

Поиск по наличию текста внутри элемента (textContent):
let element = await driver.findElement(By.xpath("//div[contains(text(), 'Я.Мессенджер')]"));
let element = await driver.findElement(By.xpath("//*[text() = 'Я.Мессенджер']")); - любой тег с таким текстом
let textarea = await driver.findElement(By.xpath("//textarea[@placeholder='Сообщение…']")) - по атрибуту placeholder

Если искомый элемент находится внутри открывшегося фрейма, то сначала нужно переключиться на фрейм:


Метод findElement также как в css querySelector ищет внутри найденного элемента:
let form = await driver.wait(driver.findElement(By.css('.production')))
let input = await driver.wait(form.findElement(By.css('[name=page]')))

Для поиска элемента также можно использовать js код:
let elem = await driver.executeScript("return document.querySelector('h1')");

Проверка в консоли наличия элемента:
1) Открываем консоль, заходим в console
2) Вводим: $$('a.link') - выведутся все ссылки с классом link

findElement выбрасывает исключение NoSuchElementException, если элемент не найден
Как это обрабатывать:
1) Либо помещаем в try, catch: 
try {
let elem = await driver.wait(until.elementLocated(By.css('h11')), 10000);
console.log('Найдено') 
    } catch (e){ console.log('Не найдено') }
2) Либо используем findElements, который всегда возвращает массив
    let list = await driver.wait(until.elementLocated(By.css('h1')), 10000)
    if(list.length > 0) {console.log('Найдено')} else {console.log('Не найдено')}

Ожидания.
Иногда элемент появляется в Дом не сразу, например, если его создает js код. В этом случае, нужно включить у селениума ожидание.
При ожидании селениум периодически отправляет запросы, проверяя наличие элемента (раз в полсекунды или около) 
1) Неявное ожидание задается для всего драйвера при его инициализации и ожидает появления элемента в ДОМ. 
   Если элемент не будет найден в ДОМ сразу, Selenium будет ждать до 10 секунд перед тем, как выбросить исключение.
	let driver = await new Builder().forBrowser('chrome').build();
	await driver.manage().setTimeouts({ implicit: 10000 }); - включили ожидание
	await driver.manage().setTimeouts({ implicit: 0 }); - при необходимости отключили
	try { 
	   await driver.get('https://example.com');
           let elem = await driver.findElement(By.css('h1'));
           let text = await elem.getText();
           console.log('Текст заголовка:', text);
	} catch (e) {
           console.log('Ошибка:', e); }
2) Явное ожидание задается для самой операции через wait и until, ждать можно чего угодно.
	let elem = await driver.wait(until.elementLocated(By.name('q')), 5000); - ждем появления в ДОМ (elementLocated)

	let elem = await driver.wait(until.elementLocated(By.css('h11')), 5000); - ждем появления в ДОМ
        await driver.wait(until.elementIsVisible(elem), 5000); - ждем видимости

   Что можно ждать:
	elementLocated - элемент появился в ДОМ
	elementIsVisible - элемент стал видимым
	visibilityOf - элемент стал видимым
	stalenessOf - стал невидимым 
	elementIsClickable - стал кликабельным (видимый и не disabled)
	textToBePresentInElement - присутствие/изменение текста в элементе
	titleIs - заголовок страницы стал
	alertIsPresent - появился алерт
	elementAttributeToBe - изменился атрибут

   Примеры:
	await driver.wait(until.visibilityOf(elem), 5000);
	await driver.wait(until.elementIsClickable(elem), 5000);
	await driver.wait(until.invisibilityOf(elem), 5000);
	await driver.wait(until.textToBePresentInElement(elem, 'expected text'), 5000);
	await driver.wait(until.titleIs('Expected Title'), 5000);
	await driver.wait(until.alertIsPresent(), 5000);

Также можно задать фиксированное ожидание:
	await new Promise(resolve => setTimeout(resolve, 3000));
	
111) Видимость элемента.
     until.elementIsVisible(...) — это условие, которое проверяет, что найденный элемент не только существует в DOM, 
     но и видим на странице (т.е. нет, например, display: none или visibility: hidden, прозрачность 100% и пр.)
	await driver.wait(until.elementIsVisible(driver.findElement(By.css('.selector'))), 10000) 
	await driver.wait(until.visibilityOf(element), 10000) - подобная функция
	
     Пример:
	button.isDisplayed().then(function() { button.click(); });


111) Вместо async await можно использовать обычный синтаксис:
	driver.findElement(By.name('q')).then(function() {// do something });

111) Получение атрибутов ДОМ элементов - доступны все js свойства ноды:
	elem.getAttribute('textContent')
	elem.getAttribute('offsetWidth')
	elem.getAttribute('outerHTML')
	elem.getAttribute('value')

111) Получение других свойств:
	elem.getCssValue('color')
	elem.getRect() - координаты (height: 39, width: 72, x: 15, y: 100.9765625)

111) Действия с элементами
	elem.click() - кликнуть можно только по видимому элементу
	elem.sendKeys('Привет', Key.ENTER) - ввод текста в поле + нажать Enter
	elem.sendKeys('/path/to/your/file.txt') - загрузить файл в input type='file'

111) Цепочки действий через actions():
	let elem = await driver.findElement(By.css('.requisites__other'));
	await driver.actions() - запускаем эту команду
            .move({ origin: elem }) - переместиться к элементу
            .pause(3000) - подождать
            .press()
            .pause(3000)
            .sendKeys('abc')
            .perform() - обязательно вставлять для запуска цепочки

     Другие действия для цепочки:
	.keyDown(Key.SHIFT) - нажать SHIFT
	.keyUp(Key.SHIFT) - отпустить SHIFT
	.sendKeys(elem, 'abc') - ввести в ноду текст (аналог elem.sendKeys)
	.click(elem) - кликнуть по элементу (аналог elem.click())
	.contextClick(elem) - правая кнопка мыши
	.press(Button.BACK) - нажать кнопку
	.release(Button.BACK) - отпустить кнопку
	.move({origin: elem}) - переместить курсор в центр элемента
	.doubleClick(elem) - двойной клик
	.dragAndDrop(elem1, elem2) - нажать на первый элемент, перетащить его и отпустить над вторым элементом
	.pause(3000) - подождать

     Асинхронное выполнение цепочки:
	const actions = driver.actions({async: true});
	await actions.contextClick(clickable).perform();

     Перемещения на странице:
	.move({origin: elem}) - перемещение/наведение курсора, может вызывать появление всплывающих меню и т.д.
	.scroll(0, 0, 0, 200, iframe) - прокрутка к координатам
        await driver.executeScript("window.scrollBy(0, 500);"); - запуск js кода

111) Окна
Модальные окна просто ждем и кликаем на крестики

Настоящие alert закрываем так:
	await driver.findElement(By.id('alertButton')).click(); - например, действием вызвали алерт
	await driver.wait(until.alertIsPresent(), 5000); - подождали его появления
	let alert = await driver.switchTo().alert();
	await alert.sendKeys('Текст'); - ввели в окно текст
	await alert.accept(); - нажали на алерте ОК	
 	await alert.dismiss(); - отказались (для confirm)

Открытие новых окон:
- либо новый конструктор new Builder()
- либо driver.executeScript('window.open()') - в некоторых браузерах открывается новая вкладка

Переключение на новое окно (вкладку)
const window = await driver.switchTo().window(newWindow)

Расширение окна браузера:
let window = driver.manage().window();
window.maximize();

Пример поиска в открывающемся окне iframe для явных ожиданий:
await driver.get('https://uslugi.yandex.ru/profile/Avegaprint-1047311');
await driver.findElement(By.css('a.WorkerControls-Control_chat')).click();
await driver.findElement(By.xpath("//div[contains(text(), 'Я.Мессенджер')]")).click();
let frame = await driver.wait(until.elementLocated(By.css('iframe.ya-chat-base__iframe')), 8000);
await driver.switchTo().frame(frame);
let textarea = await driver.wait(until.elementLocated(By.xpath("//textarea[@placeholder='Сообщение…']")), 8000);
await textarea.sendKeys('Добрый день!', Key.ENTER)

Это же для неявного ожидания:
await driver.manage().setTimeouts({ implicit: 10000 });
await driver.get('https://uslugi.yandex.ru/profile/Avegaprint-1047311');
await driver.findElement(By.css('a.WorkerControls-Control_chat')).click();
await driver.findElement(By.xpath("//div[contains(text(), 'Я.Мессенджер')]")).click();
let frame = driver.findElement(By.css('iframe.ya-chat-base__iframe'));
await driver.switchTo().frame(frame);
let textarea = driver.findElement(By.xpath("//textarea[@placeholder='Сообщение…']"));
await textarea.sendKeys('Добрый день!', Key.ENTER);

111) Закрывать браузер нужно только через quit(), потому что:
	driver.quit() - закрывает все окна, завершает процесс драйвера, освобождает ресурсы
	driver.close() - закрывает текущее активное окно, но не закрывает процесс драйвера

111) Создание снимка экрана:
	const fs = require('fs');
	const screenshot = await driver.takeScreenshot();
        fs.writeFileSync('screenshot.png', screenshot, 'base64');

111) Прокрутка к элементу (также можно через action move()):
	let elem = await driver.findElement(By.css('.card'));
        await driver.executeScript("arguments[0].scrollIntoView();", elem);

111) Использование прокси:
     Хром не позволяет вводить в адресной строке логин и пароль от прокси, 
     поэтому для установки прокси используем расширение
     1) Создаем профиль хром в папке проекта по инструкции выше
     2) Устанавливаем в этот профиль расширение ProxyControl, прописываем данные прокси	
     3) Все, теперь этот профиль работает через прокси, ничего больше писать в коде не нужно, ip меняются

     Если прокси без пароля (бесплатный), то такие настройки, расширение не нужно:
	options.addArguments(`--proxy-server=138.201.49.224:9999`)
     Также можно в настройках прокси сделать белый ip, чтобы пароль вводить не нужно было, тогда без расширения	




============================================================================================================================

Поиск в js подстроки в строке:
let query = 'ква'
let regex = new RegExp(query, 'i') // регистронезависимость
if(regex.test('МОСКВАБАД')){ return true; } // true


performance.now() - время прошедшее с момента начала загрузки страницы в миллисекундах
                    пока страница открыта оно постоянно увеличивается, это можно проверить в длящемся цикле

111) Можно получать параметры вводимые при запуске скрипта php из командной строки, они записываются в массив $argv:
	Файл index.php:
	$arg = $argv[1]
	echo $arg // выведется 555

        Запускаем в консоли: 
	php index.php 555

111) Можно запускать одновременно несколько php скриптов в консоли:
	1 консоль: php index.php
	2 консоль: php index.php

111) md5(uniqid().time()) - применяется для создания уникального хеша длиной 32 символа
	uniqid() => 6796eaf53f73d
	uniqid().time() => 6796eb15c020c1737943829  // усложняем на основе временной метки
	md5(uniqid().time()) => 75cd03db40419bbdf9b1fb3e0c5d18a9  // md5 преобразует строку в хеш длиной 32 символа

111) Можно запускать несколько скриптов для одной базы и сделать так, чтобы они работали только со своими строками:
	0) Это не защита от перезаписи одних и тех же строк, а разделение строк БД между разными скриптами
	1) Устанавливаем строке поле tmp_uniq типа CHAR длиной 32, ставим уникальный индекс
	2) Сначала устанавливаем блоку строк одинаковый хеш, затем 
	   $tmp_uniq = md5(uniqid().time()); // уникальный 32 символьный хеш
           $db->query("UPDATE links SET tmp_uniq = '{$tmp_uniq}' WHERE tmp_uniq is null limit 10");
           $result = $db->query("SELECT * FROM links WHERE tmp_uniq = '{$tmp_uniq}'"); // получаем эти строки и выполняем длительную операцию
	3) Запускаем несколько скриптов index.php одновременно, они будут работать только над своими строками в базе
        4) ИИ говорит, что UPDATE links SET tmp_uniq = '{$tmp_uniq}' WHERE tmp_uniq is null limit 10" может выбрать одинаковые
           строки и будет ошибка, так нельзя, советует транзакции, если выполняется несколько скриптов с одной БД

111) Чтобы в Windows запустить несколько консолей с командами одновременно:
	1) создаем файл run_scripts.bat и пишем в нем после start консольные команды:
		start php index.php
		start php index.php
		start php index.php
	2) Запускаем run_scripts.bat один раз в консоли:
		./run_scripts.bat - просто пишем в консоли его имя, запустилось несколько консолей и во всех сработала команда php index.php

111) svgedit.netlify.app/editor/index.html - рисование svg
     jakearchibald.github.io/svgomg/ - оптимизация svg, удаление лишнего, советует всегда прогонять svg

111) Теги <map> и <area> используются в HTML для создания изображений, 
     где определенные области изображения могут быть активными ссылками	 
	
	<map name="catalog">
	   <area shape="rect" coords="393,193,2,0" href="url-адрес-1">
           <area shape="rect" coords="500,293,12,10" href="url-адрес-2">		
        </map>

111) 
   
	
		


==================================================== Поключение Vue к Ларавель ===============================================================	

1) composer create-project laravel/laravel .
2) Создаем в phpmyadmin БД
3) Прописываем в .env->DB_DATABASE имя БД
4) php artisan migrate
5) npm install @vitejs/plugin-vue@4.x --save-dev
   npm i vue --save-dev
6) npm install
   npm run dev

7) Добавляем в vite.config.js:
	import vue from '@vitejs/plugin-vue';
        в массив плагинов вставляем vue()

8) Создаем компонент resources/js/components/Example.vue

9) В resources/js/app.js добавляем:
	import { createApp } from 'vue/dist/vue.esm-bundler'; // иначе нет горячей замены
	import Example from './components/Example.vue';
	const app = createApp({});
	app.component('example', Example); - тут регистрируем корневые (родительские) компоненты blade шаблонов
	app.mount('#app');

10) В шаблонах blade вставляем:
        @vite(['resources/css/app.css', 'resources/js/app.js']) - в шапке
	<div id="app">
    	   <example></example>
	</div>

11) ИИ пишет, что запускать в двух консолях:
	npm run dev
	php artisan serve - работало с одной этой командой


==================================================== Планировщик заданий windows ===============================================================	

111) Планировщик заданий => Создать задачу => Называем задачу =>
     В триггерах Создать => Пишем время => В Действиях пишем что запускаем	
	


==================================================== СЕО ===============================================================	

https://ahrefs.com/ru - анализ ссылочной массы
https://www.keys.so/ru - анализ сайтов и запросов конкурентов
https://www.rush-analytics.ru/land/serp-monitor - как меняется выдача (ТОП100) по вашим поисковым ключевым словам   
https://answerthepublic.com/ - поиск тем для инфостатей
https://checktrust.ru/ - оценка донора для обратной ссылки	

111) KGR - keyword golden ratio – это метод поиска ключевых слов с низкой конкуренцией, которые должны быть относительно простыми для ранжирования.
           Ищем запросы с частостностью меньше 250 в месяц и с соотношением количество запросов / количество сайтов с ключами в тайтле < 0.25
           То есть, например, запрос ищут 100 раз в месяц и сайтов с таким ключом в тайтле 20 штук => kgr = 0.2, значит статью писать можно	
     

111) В robots.txt можно устанавливать команду Clean-param
     Например, есть страница фильтрации https://site.ru/catalog/golfy/?sort=asc&order=desc
     Для того, чтобы яндекс не загружал ее пишем:
	User-agent: Yandex - указываем блок для робота Яндекса (для Гугла не работает)
	Clean-param: sort&order /catalog/golfy - теперь /catalog/golfy станет основной если у страницы добавляются параметры sort&order 
 
	
111) создание сайтов site:rodnovspb.ru - если в Гугл будет:
	"Мы скрыли некоторые результаты, которые очень похожи на уже представленные выше" , то  гугл считает такие страницы некачественными,
        их долю нужно уменьшать, иначе падает доверие к самому домену
	
111) robots.txt и метатег robots - не удаляют страницы из индекса, а только рекомендуют роботам не сканировать страницы
                                   Таким образом если страница просканирована нужно ставить ей 404 ответ, чтобы страница точно ушла из индекса
111) robots.txt нужно разбивать на блоки для Яндекса, Гуглбота и * всех остальных роботов,
                чтобы команды правильно работали			    
	
111) Как смотреть пререндеренные js страницы выдаваемые поисковым ботам:
     User-Agent Switcher - расширение для браузера изменяющее User-Agent
     bertal.ru - тоже переключает User-Agent
     Web Developer - отключает js, css для сайта
     В яндекс вебмастере есть просмотр страниц JS 
     В выдаче в Сохраненная копия

111) prerender.io - сохранение статичных страниц js для выдачи боту

111) Link Redirect Trace - расширение показывает переадресации, коды ответов

111) Анализ PageSpeed от Гугла можно встроить в Лягушку и получить эти значения для всех страниц	

111) В Яндекс Вебмастере можно поставить бесплатно на отслеживание 1000 запросов

111) loader.io - служба проверки нагрузки на сайт, покажет как ведет себя сайт, 
                 если подключаются тысячи пользователей
	
111) В comparser можно посмотреть какие страницы закрыты в robots.txt

111) content-watch.ru - пакетная проверка страниц на уникальность 

111) html карта сайта на главной странице позволяет поднять уровень вложенности 
          для внутренних страниц.  	

111) Деоптимизировать title и h1 страниц пагинации.
     Например, первую /trubi делать "Полипропиленовые трубы"
     Последующие в пагинации /trubi?page=2, /trubi?page=3 и пр. делать  Трубы - страница 1, Трубы - страница 2         
	
111) Поиск первоисточника текста:
	1) Берем длинное предложение
	2) Оборачиваем в кавычки, вставляем в поиск Яндекса
	3) Первым в выдаче будет стоять первоисточник	
	
111) pagespeed.web.dev - скорость загрузки сайта + много дополнительного:
	                 - показывает неиспользуемый код js
			 - показывает непроизводительные форматы изображения
			 - большие картинки и пр.
	
111) Кроме Opengraph существует микроразметка Schema.org.
     Определенным тегам добавляются атрибуты itemscope, itemtype, itemprop
     которые затем показываются в сниппетах выдачи.
     Пример разметки товара:
	<div itemscope itemtype="http://schema.org/Product">
	  <h1 itemprop="name">Iphone 6 plus 16 GB</h1>
          <span itemprop="description">iPhone 6 не просто больше...</span>
          <img itemprop="image" src="http://imageexample.com/iphone6plus.jpg">
	</div>

111) google.com/webmasters/markup-helper/?sjid=16405634829758222987-EU - конструктор разметки
               
111) aisoft.su - программы для прогрева домена, валидации email, сбора из выдачи яндекс, яндекс услуг и пр.
	
111) mail-tester.com - проверка письма на баллы по спаму. Чтобы проверять бесплатно более 3 раз заходить через другие ip
	
111) vds-shop.com - smtp сервера для рассылки

111) mxtoolbox.com/blacklists.aspx - проверка ip на наличие в черных спам списках
                                     допускается 1-2 наличия

111) По алгоритму ранжирования релевантности текста поисковому запросу BM25 каждое новое слово уменьшает
     релевантность текста запросу
     Например, запрос "Печать для ИП" 
     Более релевантным будет title "Заказать печать ИП", чем "Заказать автоматическую печать ИП"
	
111) Можно писать повторяющиеся ключи в каждом alt даже в списке картинок. Потому что Яндексу такие alt нужны для опознавания
     для раздела "Картинки". Но без "купить", "заказать" и пр.	
	
111) @Link_indexing_bot - бот для индексации страниц в Яндексе и Гугле	

111) Мгновенная индексация новых или измененных страниц если доступен Вебмастер - через Переобход страниц:

111) Мгновенная индексация если Вебмастер не доступен:
	1) Создаем ключ от 8 символов из a-z, A-Z , 0-9, -
           Например: IndexNow-4vFei2UGL
   	2) Размещаем в корне файл IndexNow-4vFei2UGL.txt с ключом IndexNow-4vFei2UGL
	3) Отправляем гет запрос:
		https://yandex.com/indexnow?url=https://site.ru/new-page&key=IndexNow-4vFei2UGL
	
111) <a href="#top">Наверх</a> 
     <a href="#">Наверх</a> - встроенные способы поднятия наверх страницы

     Для того, чтобы прокрутка была плавной:
	html { scroll-behavior: smooth; }

111) scroll-margin - css свойство добавляющее отступ, чтобы прокрутка была не впритык к элементу	
	
111) Якори в ссылке можно ставить и на внешнюю страницу:
	<a href="https://site.com/page#to">Якорь</a> 

111) Плавная прокрутка с помощью js:

const anchors = document.querySelectorAll('a[href*="#"]')
for (let anchor of anchors) {
    anchor.addEventListener('click', function (e) {
        e.preventDefault()

        let href = anchor.getAttribute('href');

        const blockID = href.substring(href.indexOf('#') + 1);

        document.getElementById(blockID).scrollIntoView({
            behavior: 'smooth',
            block: 'start'
        })
    })
}
	
111) Интересные эффекты:
	ferrumstudio.ru - клеточки под курсором разрушаются

111) counter-reset, counter-increment, content: counter(...) применяются для установки возрастающих
     чисел на любых html элементах

	
111) В таблице может быть несколько блоков tbody со своими tr - удобно разделять их по стилям через свой tbody	

111) fancybox - при нажатии на одну из картинок появлялется модальное окно со стрелками на другие картинки

111) Внутрь input, select нельзя вставить ::before или ::after,
     для вставки используем обертку и привязываем псевдоэлементы к ней input-wrapper::before

	<div class="input-wrapper">
	    <input type="text">
	</div>

     
111) Плавная прокрутка с сайта zigmund.online
	https://www.digidop.com/blog/lenis-smooth-scroll
     Есть в папке "практика"		
	
	
		
==================================================== Битрикс =====================================================
	
111) ЧПУ ссылки
	В типе информационного блока:
		URL страницы информационного блока: #SITE_DIR#/fotogalereya.php/
		URL страницы раздела: #SITE_DIR#/fotogalereya.php/#SECTION_CODE#/
		URL страницы детального просмотра: #SITE_DIR#/fotogalereya.php/#SECTION_CODE#/#ELEMENT_CODE#
	В компоненте включить ЧПУ, также:
		Каталог ЧПУ (относительно корня сайта): /fotogalereya.php/
		Раздел фотогалереи: #SECTION_CODE#/
		Фотография детально: #SECTION_CODE#/#ELEMENT_CODE#/


















		
 



	
 
